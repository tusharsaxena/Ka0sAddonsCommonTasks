### modules/DrillDown.lua  preserved=True  gaming=False
  - modules/DrillDown.lua:415 (LATENT, no behaviour change today) - the ladder's rung (a) now calls `self:Enter(window, row, "Deaths", "deaths")` with a hardcoded literal; the original passed the `statKey` parameter. climbDeathsLadder does not receive statKey at all, so the gate at line 452 (`if statKey == "Deaths"`) and the value handed to Enter are two independent literals that nothing keeps in sync
  - modules/DrillDown.lua:393 (UNREACHABLE difference) - the toggle's truthiness test became an explicit nil test: old `if current and current.guid == row.guid ...`, new `return current ~= nil and current.guid == row.guid ...`. These differ on a non-nil falsy `current`: the old code skipped the toggle, the new code indexes a boolean and raises. I verified it is unreachable - `views` is a plain table f
  - modules/DrillDown.lua:411 (DOC INACCURACY) - `@return string|nil  "enter" or "recap", or nil when neither rung fires` is wrong: climbDeathsLadder also returns "none", at line 415, whenever rung (a) fires and Enter refuses (a row with no guid, or a statKey absent from Const.STAT_BY_KEY). The prose at 405-408 ("Neither rung firing returns nil") reinforces the wrong set. Contract clause 5 names four 
  - modules/DrillDown.lua:396-425 vs 452-456 (COMMENT MOVED AWAY FROM ITS CODE) - the load-bearing "DEATHS IS A LADDER, and the order is the whole of it" block (old lines 405-413, cited verbatim by the contract as the why) survived and was expanded, but its rung (c) clause now sits inside climbDeathsLadder, which does not implement rung (c). The fall-through it explains is at line 456 in OnCellClick. 
  - modules/DrillDown.lua:413-425 (SHAPE, judgement call) - climbDeathsLadder is a verbatim body move of the old `if statKey == "Deaths"` block; every line is byte-identical apart from the statKey literal, and it takes `self` as an explicit first parameter purely so it can call the method it used to call inline. No decision was removed - lizard now reports OnCellClick CCN 9 + climbDeathsLadder CCN 9 +
  - METRICS (no finding, actual output) - `lizard -l lua modules/DrillDown.lua`: 'No thresholds exceeded (cyclomatic_complexity > 15 ...)'. isRepeatClick@392-394 CCN 3, climbDeathsLadder@413-425 CCN 9, OnCellClick DrillDown@440-457 CCN 9 (was 18). File AvgCCN 5.5 over 28 functions, 331 NLOC. The highest in the file is deathRow@615-648 at CCN 15 - at the limit, not above it, pre-existing and not a warn
  - SECRET SAFETY (no finding) - no regression. The `canReadRecaps() and copyRecapIDs(row.deaths) or nil` short-circuit is preserved verbatim at line 414, so copyRecapIDs - which holds the only `#` on this path, over the NeverSecret recap-id array at lines 215/228 - is still not entered when the API is absent. The refactor adds no `table.concat`, no new `#`, no new comparison, and does not touch a met
  - EVIDENCE - tests/test_drilldown.lua run in isolation against the refactored file (scratch copy of tests/run.lua with suites={"test_drilldown"}, suiteInventory=false; the full suite was NOT run, per instructions): 58 passed, 0 failed, 0 skipped. That matches the 58/0 the contract records against the unrefactored code, and includes the three ordering cases - 'The exit toggle is answered BEFORE the D

### modules/HeaderControls.lua  preserved=False  gaming=False
  - modules/HeaderControls.lua:403 — BEHAVIOUR DIVERGENCE (confirmed empirically, not inferred). Old: `local frameCfg = window.config.frame or {}` ran unconditionally on the line after the two guards, so ANY control name — including one no arm matched — forced the `window.config` index. New: `if not action then return end` precedes `action(window, window.config.frame or {})`, so an unrecognised contro
  - docs/ARCHITECTURE.md:696 — STALE COMPLEXITY REGISTER ROW, left behind by this refactor. It still reads `| `onClick` | 24 | `modules/HeaderControls.lua:322-372` | **Peel** — [#38](...)`. All three cells are now wrong: onClick is at 395-403, lizard measures it at CCN 5, and the Peel the row proposes has been taken. The row even specifies the shape that was in fact delivered ("a module-level dispatch
  - modules/HeaderControls.lua:325-327 — COMMENT INACCURACY introduced by the refactor (low severity). The new ACTIONS docstring states the no-op mechanism as "an unknown key is a silent no-op because a missing row is simply nil". That is not what makes it silent — a missing row that is simply nil would be CALLED and would raise "attempt to call a nil value". What makes it silent is the explicit `if n
  - NOT A FINDING, recorded so the next reviewer does not re-derive it: lizard `-l lua modules/HeaderControls.lua` reports "No thresholds exceeded (cyclomatic_complexity > 15 ...)" — file totals 300 NLOC, AvgCCN 5.2, 28 functions, 0 warnings. onClick@395-403 is NLOC 8 / CCN 5 / 53 tokens / 1 param. The two highest survivors are artFor@279-291 (CCN 15) and HeaderControls@443-513 (CCN 15) — both AT the 
  - NOT A FINDING, recorded as the negative result of the checks asked for: no short-circuit was lost (the guard is still the single `if not (window and control) then return end`); no `~= nil` became a truthiness test or the reverse (`not (frameCfg.minimised and true or false)` and `not (frameCfg.locked and true or false)` are character-for-character unchanged, so contract item 3's boolean-not-nil wri

### modules/Feign.lua  preserved=True  gaming=False
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/modules/Feign.lua:304-328 - LOST-COMMENT (low, left behind rather than dropped). Feign.Prune's docblock still narrates all three exits at length - 'A CONFIRMED ZERO HEALTH', 'STANDING BACK UP', 'LEAVING THE GROUP' - but none of that logic remains in the body at 329-362, which is now a four-line dispatch. The STANDING BACK UP and CONFIRMED ZERO 
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/modules/Feign.lua:267-268 - COMMENT REFERENT MOVED (low). 'Seeing the feign is what makes the "stood back up" exit usable - see the state comment above.' In the old file the nearest matching comment was Prune's own STANDING BACK UP paragraph ~15 lines above the code. judgeMember is now defined BEFORE that docblock, so the only 'state comment ab
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/modules/Feign.lua:273 - STALE COMMENT CARRIED ACROSS (low, pre-existing, NOT a regression). 'The trace below used to report `feigned[guid]`, which the eviction three lines down had already nilled' - the eviction is five lines down (prior read at 279, `feigned[guid] = nil` at 284). It was already off by two in the unrefactored code (old 300 -> o
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/docs/ARCHITECTURE.md:694 - STALE CITATION INTRODUCED BY THE REFACTOR (low). The complexity watch-list row still reads '| `Feign.Prune` | 25 | `modules/Feign.lua:247-337` | **Peel** - #37 ...'. Both the CCN and the line range are now wrong (Prune is 329-362 at CCN 13) and the 'Peel' disposition is discharged. This is a live doc, not one of the f
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/docs/automated-tests/RESULTS.md:83 - STALE WATCH-LIST ROW (low). Same row, same numbers: '| `Feign.Prune` | 25 | `modules/Feign.lua` | **Peel** - #37 ...'. performance-§10 makes RESULTS.md the single overwritten-in-place watch list and the place a discharged disposition is reconciled, so this needs the CCN-13 update once the wave lands. Orchest
  - /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/modules/Feign.lua:225 - NOT A FINDING, recorded for completeness. The one comment WORDING change in the diff is evictAbsent's 'The other two eviction paths each end in the trace below' -> 'each end in a trace'. That is a correct adaptation to the move (the other two paths' trace is now in judgeMember, below evictAbsent), not a dropped why-comme

### core/Diagnostics.lua  preserved=True  gaming=False
  - core/Diagnostics.lua:1763-1768 — LOST COMMENT (performance-§11 'MUST NOT drop a comment that records why... the code they explain moves, and the comment stays behind attached to nothing'; anti-patterns #52). The doc block for `reportFeignRoster` ('What the roster looks like RIGHT NOW, so a reader can put a GUID to a name. / The trace is all GUIDs, because a GUID is what the join is on. It is also 
  - core/Diagnostics.lua:1790 — consequence of the above: `reportFeignRoster`, the function that block was written for and the one that is called from two separate paths (1819 and 1848), is now the only report* function in this region of the file with no doc comment at all. The next reader asking 'why is the roster printed here, and why below the entries?' finds nothing at the definition; the answer i
  - core/Diagnostics.lua:667 — NOT a defect, recorded so the reviewer after me does not re-litigate it: `reportDatingRow`'s early `return` on the secret arm is genuinely equivalent to the old `if/else` fallthrough, because the old `printed = printed + 1` sat OUTSIDE the branch (contract item 4) and still does, at 715, in the caller. This was the most likely place for the refactor to break the 'four ro
  - core/Diagnostics.lua:672-674 — ADDED comment, not lost: the 'THE CLIENT HANDS THE EVENTS BACK NEWEST FIRST' rationale for `events[1]` did not exist in the pre-refactor source (verified against git show HEAD). It appears to have been lifted from contract item 6. It is accurate and load-bearing, so it stands, but it is new text arriving inside a behaviour-preserving refactor and should be acknowledg
  - lizard -l lua core/Diagnostics.lua — PASS, no finding. 'No thresholds exceeded (cyclomatic_complexity > 15 ...)'; 59 functions, 988 NLOC, AvgCCN 5.8. Both warned functions cleared: reportDeathDating@698-720 CCN 7 (was 25), reportFeignRoster@1790-1804 CCN 7 (was 17). New helpers: reportDatingHeader@647-654 CCN 8, reportDatingRow@667-689 CCN 12, reportFeignRosterRow@1779-1788 CCN 11. Highest remaini
  - luacheck core/Diagnostics.lua — PASS, no finding. 'Total: 0 warnings / 0 errors in 1 file'. `luac -p` also compiles clean; the three new file-scope locals bring the main chunk to 61, nowhere near Lua's 200-local ceiling.

### modules/Visibility.lua  preserved=True  gaming=False
  - docs/ARCHITECTURE.md:703 - STALE REGISTER CITATION (documentation, not behaviour). The complexity register row still reads `| Visibility.ShouldShow | 23 | modules/Visibility.lua:239-276 |` with a **Peel** disposition and a link to issue #41. The function is now CCN 10 at modules/Visibility.lua:273-306, and the peel it proposes has already landed. tests/test_complexity_register does not catch this 
  - tests/test_visibility.lua:676-681 - COMMENT LEFT ATTACHED TO CODE THAT NO LONGER EXISTS. The header above the veto-ORDER suite says the ordering "is currently implied by nothing but line position in an eight-line if-chain" and that "Any rewrite that turns those eight lines into a table and a loop -- and one is planned -- reorders them the moment a row is typed out of sequence". Both clauses are no
  - modules/Visibility.lua:219-221 - COSMETIC. The banner comment block `-- The predicate` now heads the VETOES data table (line 234) rather than the predicate function it names (Visibility.ShouldShow, line 273). Not a behaviour or metric-gaming issue; noted only because a section banner that no longer introduces the thing it names is the same class of drift as the two items above.

### modules/Export.lua  preserved=True  gaming=False
  - docs/ARCHITECTURE.md:692 — STALE COMPLEXITY REGISTER (the substantive finding). The row still reads `Export.ChatLines | 27 | modules/Export.lua:535-600 | **Peel** — #36`. The peel has now been taken: the function is CCN 12 at modules/Export.lua:607-630, and the two helpers the row itself predicted ("the header build and the ranked line are two functions sharing a name; the hasExtra bookkeeping bel
  - docs/ARCHITECTURE.md:693 — STALE AND NOW CONTRADICTORY. The row reads `onPrintToChat | 16 | modules/Export.lua:1432-1497 | **Accepted.** ... One point over. Re-check: a fifth refusal, or CCN 20.` The function was refactored anyway: it is CCN 12 at modules/Export.lua:1489-1539, a fifth refusal never arrived and CCN 20 was never reached, so neither recorded re-check trigger fired. The register now r
  - docs/ARCHITECTURE.md:542 and docs/ARCHITECTURE.md:458 — the contract's own hygiene note was not swept. 01_CONTRACTS.md's Export.lua section explicitly flags that both places record tests/test_export.lua as 1509 lines and that it is now 1887. Both still say 1509. tests/test_layout_cap.lua asserts membership rather than the figures, so nothing is red — but the number the contract asked this refactor
  - docs/ARCHITECTURE.md:536 — `modules/Export.lua | 1743` in the over-cap census. The file is now 1790 lines (the refactor added 47). Prose, not asserted by tests/test_layout_cap.lua, and the file is still over the cap and still listed, so no gate reddens — but it is a figure this change invalidated.
  - modules/Export.lua:518-522 — COMMENT SCOPE NARROWED. The `..`-rather-than-table.concat house rule ("concat raises on a secret where `..` is on the permitted list, and a formatter can hand back a handle rather than a string on a client we have not met yet") sat inside Export.ChatLines and read as governing every join in the function. It now lives only in chatHeader's docblock. chatLine@563-587 perf
  - modules/Export.lua:1477-1486 — LATENT STRUCTURAL CHANGE IN recipientRefusal (currently unreachable, reported because it is a real narrowing). The old code separated "refuse" (control flow) from "what to print": each arm printed and returned independently. The helper conflates the two — a refusal is signalled solely by returning a truthy message, and onPrintToChat@1497 branches on that. Any future 

### settings/Slash.lua  preserved=False  gaming=False
  - BEHAVIOUR (minor, slash-visible output) — settings/Slash.lua:496. `if NS.Diagnostics then NS.Diagnostics[report]() end` replaces three literal calls (`NS.Diagnostics.Report()`, `.ReportDeathRecap()`, `.ReportIdentity()` at old settings/Slash.lua:439, :450, :461). Where NS.Diagnostics is present but the named method is missing — a stale or partially-loaded core/Diagnostics.lua, exactly the shape th
  - LOST COMMENT (performance-§11 'MUST NOT drop a comment that records why') — settings/Slash.lua:487-493 vs old settings/Slash.lua:454-460. The `identity` block's closing sentence is gone with nothing carrying it: "The report itself says what it needs — the flag on, and a pull running — rather than going quiet when it has neither." That is a WHY comment recording a deliberate client-facing behaviour
  - LOST COMMENT (lesser, same rule) — settings/Slash.lua:492-493 vs old settings/Slash.lua:445-446. The `recap` rationale lost its concrete detail: "the same report the full `diag` carries, without the forty lines of atlas and font output around it" became "each the report the full `diag` carries without the output around it". The magnitude is what made the comment actionable; it now survives only in
  - COMMENT ACCURACY (new text, not a loss) — settings/Slash.lua:487-489. The new consolidated comment asserts "this lookup and the feign branch both sit ABOVE the `NS.DebugLog` guard below" for the read-verb reason ("what a player is asked to run when something looks wrong — typed mid-pull"). The old source never claimed that of `feign`: old settings/Slash.lua:466-470 places feign there because it is

### settings/ColumnBlocks.lua  preserved=True  gaming=False
  - VERIFIED EQUIVALENT (the two constructs that actually changed shape). (a) settings/ColumnBlocks.lua:239 — old `local list = W and W.ReorderList({...})` became `if not W then return nil end; return W.ReorderList({...})`. These diverge only if widgets() can return a non-nil falsy, i.e. `false`. It cannot: widgets()@77-79 is `LibStub and LibStub("LibKa0s-Widgets-1.0", true)`, which yields nil when Li
  - settings/ColumnBlocks.lua:348-354 — ORDERING PRESERVED, confirmed explicitly because it is load-bearing. Inside the walk the sequence is still drawBlock (slot -> acquireBlock -> applyBlock) -> blocks[i]/live[i] -> registerRow -> addBoundaryRule. registerRow reads `block.mmGlyphTexture`, which applyBlock sets, so the ghost icon is still the value just written and not a re-derivation (contract item 
  - docs/ARCHITECTURE.md:706 — DOC DRIFT, and the register's own gate will not catch it. The complexity register row still reads `| NS.ReorderableBlocks | 28 | settings/ColumnBlocks.lua:227-322 | **Peel** — #44 |`. After the refactor the function lives at settings/ColumnBlocks.lua:326-364 and lizard measures it at CCN 10, so both the figure and the line range are stale, and the disposition "Peel" now 
  - settings/ColumnBlocks.lua:283-302 — FUTURE FRAGILITY (not a behaviour change today). registerRow's docstring asserts "the ghost's furniture comes off the same item the block was applied from", but after the split that agreement is a CALLER obligation across two now-file-scope functions and nothing in registerRow can check it. The only call site (:352) does pass the same loop-iteration `item` that 
  - settings/ColumnBlocks.lua:304-319 — SAME CLASS. addBoundaryRule's docstring says "Drawn from INSIDE the walk, immediately after block `boundary`'s slot", which is a constraint on the caller that a file-scope helper cannot enforce; any future call with an `i` not from the walk draws a stray Heading into the scroll. Correct as called today (:354), but the guarantee moved from "structurally impossibl
  - settings/ColumnBlocks.lua:304 — MINOR COMMENT LOSS. A comment-line-level diff of old vs new shows every WHY comment carried over, including all four that name a past bug (the ghost-label pool bug, the closure-over-index bug, the drag-handles-on-non-lists bug, the double-chrome row background), and the refactor ADDED the entry-guard rationale at :329-332 and the Finish rationale at :357-360. The on
  - METRIC GAMING: none found, and I checked both prongs deliberately. No helper is a wholesale body move — the wrapper kept the guard, both ctx writes, the walk and the Finish, and the five helpers are named countEnabled, newReorderList, drawBlock, registerRow, addBoundaryRule, every one a thing a reader of this file would recognise; there is no part2/doTheRest/process. This is performance-§11's perm
  - SECRET SAFETY: not applicable, and nothing new was introduced. The file touches no Combat-restricted value — no table.concat, no `..` on a formatter output, no SafeIterate site. The three `#` uses are pre-existing and all on plain host tables (`#items` at :336, `#live` at :116, `#blockPool` at :125), none of which a formatter can have made secret. No new comparison was added on any value crossing 
  - THE NUMBER (actual lizard output, `lizard -l lua settings/ColumnBlocks.lua`): 16 functions, 166 NLOC, AvgCCN 4.2, "No thresholds exceeded (cyclomatic_complexity > 15 ...)", 0 warnings. Top rows: newReorderList@237-258 CCN 10, NS.ReorderableBlocks@326-364 CCN 10, applyBlock@179-200 CCN 7, NS.CancelReorder@210-220 CCN 7, registerRow@283-302 CCN 5, releaseBlocks@112-128 CCN 4, addBoundaryRule@311-319
  - LINT (actual luacheck output, `luacheck settings/ColumnBlocks.lua`): "Checking settings/ColumnBlocks.lua  OK — Total: 0 warnings / 0 errors in 1 file."
  - TEST EVIDENCE: 35 passed, 0 failed, 0 skipped, 35 total for test_columnblocks against the refactored file — the same count and the same case names the characterization wave recorded against the UNREFACTORED file, including every case that pins a contract invariant ("the boundary is a COUNT of enabled items, never a scan for the first disabled", "every hidden row is registered", "an empty item list
  - ALSO CHECKED AND CLEAN: the deviation register's citation docs/ARCHITECTURE.md:459 and the texture-path rows :609-610 name settings/ColumnBlocks.lua:72-73, and ENABLED_TEX/DISABLED_TEX are still on exactly lines 72-73 — the refactor inserted only below line 219, so the docs/testing.md:264 gate that follows that citation stays green. The ENABLED_TEX/DISABLED_TEX ratified deviation was NOT "fixed" i

### modules/Aggregator.lua  preserved=True  gaming=False
  - modules/Aggregator.lua:1580 — comment drift: "THE FEIGN FILTER, and it lives here rather than anywhere downstream." The judging and the drop no longer live here; the block it heads now only resolves NS.Feign and calls Feign.Prune(). The comment is left attached to code it half-explains, mitigated only by the cross-reference at 1584.
  - modules/Aggregator.lua:1574 — comment inaccuracy: "The max is computed by normalizeCountedMax below" points backwards. normalizeCountedMax is defined at line 1543, ABOVE scanColumn; only its call site (1603) is below.
  - modules/Aggregator.lua:1501 — placeSource takes nine positional parameters (pass, statKey, src, index, isSortColumn, feigned, maxAmount, isCount, touched) but its docstring documents only three (maxAmount, isCount, touched). The undocumented `feigned` is the load-bearing drop flag, and an argument-order slip in a 9-slot positional call is silent — the exact failure mode the collection's own Header
  - docs/ARCHITECTURE.md:690 — stale complexity register row: records `scanColumn | 34 | modules/Aggregator.lua:1462-1565` with disposition "**Peel** — #35" still pending. Actual today: CCN 15 at modules/Aggregator.lua:1563-1604, peel landed. performance-§10 requires the report refreshed; a completed action is still recorded as outstanding.
  - docs/ARCHITECTURE.md:534 — stale cap-census figure: records `modules/Aggregator.lua | 2083`; the file is now 2122 lines. tests/test_layout_cap.lua asserts membership rather than the figures (its own header says the numbers are prose), so nothing is red — but the census is drifting.
  - modules/Aggregator.lua (whole file) — LOC regression: the file grew 2083 -> 2122 (+39 lines, 107 insertions / 68 deletions) during a wave named CCN_LOC_REMEDIATION. It sits 622 lines over layout-§1's 1500-line cap, and this change moved it further over.
  - modules/Aggregator.lua:1599-1600 — hot-path cost: the walk now makes two function calls per source per column where it previously made none, including judgeDeath on every source of every NON-counted column (where `active` is nil and the call returns immediately). Aggregator.Build runs four times a second under a single Perf bracket at line 1743, so the added frames are invisible to the bucket that
  - modules/Aggregator.lua:1563 — scanColumn lands at exactly CCN 15, the cap itself, with zero headroom for the next branch. Across the touched functions the summed CCN rose from 38 (scanColumn 34 + judgeTracer 4) to 41 (15 + 6 + 10 + 6 + 4): the decisions were relocated rather than removed, which performance-§11 tolerates under shape #2 but which leaves the file no slack.

### core/Database.lua  preserved=True  gaming=False
  - core/Database.lua:301-303 — migrations[1]'s doc comment still carries 'The frame is only ever WIDENED, never narrowed...' although the widening code moved out; the same rationale is now restated at core/Database.lua:268-271. Two copies of one 'why' in two places is the drift hazard performance-§11 names, in its softer form. Not a behaviour change.
  - core/Database.lua:447-454 — the 'THE WINDOW'S VALUE WINS OUTRIGHT / the `== nil` rule does not apply here' block now sits above `local data = v5FirstWindowData(windows)`, a call that performs no `~= nil` test at all. The code it explains is in v5LiftWindowData (core/Database.lua:396-410), which restates it. Same duplication for migrations[12]'s colour-mode paragraph (core/Database.lua:756-759) ver
  - docs/ARCHITECTURE.md:685-687 — the complexity register still lists `migrations[1]`/`migrations[4]`/`migrations[12]` as **Accepted** at CCN 16/17/16 with locations core/Database.lua:270-296, :372-401 and :659-684. All three are now CCN 10/5/7 and the line ranges are wrong. docs/ARCHITECTURE.md:626 still claims 'This addon has **23** functions above 15'. tests/test_complexity_register.lua will not g
  - Non-finding, recorded for completeness: core/Database.lua:313 now evaluates `#columns` unconditionally, where the old code evaluated it only inside the `type(frame) == "table"` arm. A SavedVariables array carries no `__len`, so there is no observable difference.

### modules/Roster.lua  preserved=False  gaming=False
  - BEHAVIOUR (the real one) — modules/Roster.lua:407 with helper at modules/Roster.lua:313-317. `logBuiltRoster(#group, petCount)` replaced `if State.debug then NS.Debug(...) end`. The State.debug gate moved INSIDE the helper, so on every completed build with debug off the code now evaluates `#group`, evaluates `petCount`, and performs a function call that the old code did not perform at all. Output 
  - LOST/STALE COMMENT — modules/Roster.lua:311-312. The comment 'ONE line per build, format DEFERRED — the arguments are counters the loop already kept, so nothing is built at the call site (debug-logging-§3)' travelled into logBuiltRoster, where it is now false in both halves. 'Nothing is built at the call site' is precisely what stopped being true at line 407 (two arguments evaluated plus an uncond
  - BEHAVIOUR (latent, unreachable today) — modules/Roster.lua:271-281 and modules/Roster.lua:324-325. The test-mode arm was a structural early return: in the old code, once `State.testMode` and `A and A.TestGroup` were both true, build() was GUARANTEED to return (or raise). It is now a truthiness test on the helper's return value, `local preview = cacheTestModeGroup(); if preview then return preview 
  - BEHAVIOUR (minor, non-observable against a plain _G) — modules/Roster.lua:210-213. `numGroupMembers` reads `_G.GetNumGroupMembers` ONCE into a local and calls that local; the old expression `(_G.GetNumGroupMembers and _G.GetNumGroupMembers()) or 0` read the global TWICE per call, at modules/Roster.lua:230 and again at modules/Roster.lua:396. Arithmetically identical and it matches the file's own h
  - DOCUMENTATION CONTRACT DRIFT — /mnt/d/Profile/Users/Tushar/Documents/GIT/MultiMeters/docs/ARCHITECTURE.md:697. The complexity register still reads '| `build` | 20 | `modules/Roster.lua:249-366` | **Accepted.** ... Re-check: a fourth output joins the walk, or the pet lookup grows a second kind — then the per-unit body peels to `addMember`.' The function is now CCN 9 at lines 323-410 and has been pe
  - NOT A FINDING, recorded so it is not re-litigated — the working tree file is CRLF (592 lines, 592 CR) while `git show HEAD:modules/Roster.lua` is LF. That is correct and expected: .gitattributes pins `* text=auto eol=crlf` for this client-bound repo, so the blob is LF and the checkout is CRLF. A naive text diff of the two shows every line changed; the real executable delta is only the four extract
  - NOT A FINDING — secret safety is intact. `..` is still used for token building (modules/Roster.lua:239, 241, 252) and no table.concat was introduced. `#` is applied only to plain local arrays built in-file (`#units`, `#group`), never where SafeIterate would be the only legal measure. The one new comparison shape, `if not Secrets.IsSafeKey(petGuid) then return 0 end` at modules/Roster.lua:302, nega

### modules/Row.lua  preserved=False  gaming=False
  - BEHAVIOUR (low severity, unreachable in production but a genuine evaluation-order change) — modules/Row.lua:1241. `placeIcons` hoists `local y = (layout.rowHeight - size) * -0.5` ABOVE the `for i, kind in ipairs(slots)` loop; the old code computed it INSIDE the loop body (old modules/Row.lua:1188 at HEAD). When `slots` is empty — i.e. `icons.showIcon` false, which is a shipped-default-reachable st
  - COMMENT HYGIENE (medium) — modules/Row.lua:617-644 vs modules/Row.lua:761. The 28-line doc block that documents `Cell:ApplyBorder` ("Draw (or hide) the thin outline `bars.border` asks for" ... "TWO IMPLEMENTATIONS, AND THE CHEAP ONE IS THE DEFAULT" ... "THE BACKDROP GOES ON THE BAR, NOT ON A CHILD FRAME" ... "why docs/smoke-tests.md asks for it to be checked mid-pull") is no longer adjacent to the
  - DOC-REGISTER DRIFT (medium, not caught by any gate) — docs/ARCHITECTURE.md:698, :699, :648-649, :537. The refactor left four registers stale, and the suites that guard them assert MEMBERSHIP only (tests/test_complexity_register.lua's four cases check folder tally, file existence, no duplicate entry, and followable disposition; tests/test_layout_cap.lua's three check census membership both ways), s
  - LOC (informational, worth the orchestrator's attention on a branch named ccn-loc-remediation) — modules/Row.lua grew 1702 -> 1829 lines (git diff --stat: 226 insertions, 112 deletions, net +114 authored lines; wc -l confirms 1829). layout-§1 caps authored .lua at 1500, and Row.lua was already in the over-cap census with issue #33 as its terminal state, so this does not create a new breach — but a 

### modules/Format.lua  preserved=True  gaming=False
  - docs/ARCHITECTURE.md:695 - the complexity-register row is now false in every column: `| Format.DeathTime | 19 | modules/Format.lua:646-665 | **Accepted.** 15 NLOC at CCN 19 ... |`. After the refactor DeathTime is CCN 10 at lines 671-681, and the cited range 646-665 now lands on the new countdown() docblock instead. The drift is silently un-gated: tests/test_complexity_register.lua:27-31 states it 
  - docs/ARCHITECTURE.md:695 - the disposition this refactor overwrote was **Accepted**, not Peel, and its recorded re-check trigger ('a third style beyond `clock` and `ago`') has NOT fired. A ratified accept in the documented register was acted on anyway without the register being amended. The section's tally line below the table ('Eleven peels, twelve accepts, and no split lands here') will also nee
  - modules/Format.lua:663-665 - stranded WHY-comment. The paragraph 'BOTH BRANCHES ARE ARITHMETIC OR A COMPARISON, and the inputs come off a recap. Each is gated, and a refused input answers nil -- the caller draws an em dash, exactly as it does for a death whose recap has gone.' still sits in Format.DeathTime's docblock, but after the split that function contains no arithmetic and only one of the tw
  - modules/Format.lua:638 - latent hazard introduced, not a regression today: countdown() is now a file-scope local reachable from every function defined below it, and it performs `now - when` with no Secrets.CanAccess(when) guard of its own - that guard exists only in the single caller at line 672. Nothing but the docblock at 634 ('already accessible') stops a future second caller from handing it a 
  - modules/Format.lua:638-649,671-681 - observation, under threshold but worth recording against the register: the split raised the total decision count for this code from 19 to 23 (countdown 13 + DeathTime 10). The extra decisions are the new `or clock` at line 678 and the second function's own base count. Both halves are comfortably under CCN 15 so the gate passes, but the refactor relocated decisi

### modules/Tooltip.lua  preserved=False  gaming=False
  - modules/Tooltip.lua:1933-1935 — ORPHANED COMMENT (performance-§11 MUST NOT). The comment "The event type is read through `plainWord`, because it comes off a recap like everything else and comparing a secret string raises." now sits directly above `local name = event.spellName`, a line about the spell name, not the event type. The `plainWord(event.event)` call it explains moved down to line 1937 in
  - modules/Tooltip.lua:1935-1937 — BEHAVIOUR: evaluation-count change. The old eventColumns computed `local kind = plainWord(event.event)` unconditionally for every event; the new code evaluates it only when `event.spellName == nil`. plainWord (line 196) is pure, so no rendered output differs and the suite stays green — but `NS.Secrets.CanAccess(event.event)` is no longer invoked for named events. In
  - modules/Tooltip.lua:1947-1948 — BEHAVIOUR: ordering change. The time string used to be computed into `timeText` BEFORE the `plainTruth(event.hideCaster)` block; it is now computed inside the return expression, i.e. AFTER it. The two are independent and pure so nothing observable moves, but the order in which eventTimeText and plainTruth run is inverted from the original.
  - modules/Tooltip.lua:2036-2052 — PER-CALL COST: one measurement pass became two. measureEventColumns calls widestTimeColumn (a full `for i = 1, total` walk of `ordered`) and then widestNameColumns (a second full walk). The old drawDeathEvents did the time measurement and the name measurement inside a SINGLE loop over the same array. Identical widths, but up to 2x the loop overhead per hover with CO
  - modules/Tooltip.lua:2405-2406 — NEW COMMENT ASSERTS SOMETHING FALSE. rowClassColor's doc says "A nil colour is a legitimate answer: drawLine falls back to grey." drawLine (line 1548) contains no such fallback. The colour is passed to `lineStyle(config, color)` (1429), where `modeColor` with nothing to read leaves the CONFIGURED colour standing — lineStyle's own comment at 1449 says so — and the gr
  - modules/Tooltip.lua:1872 (nit) — eventCaption's doc claims "Four arms and no more", but the function has three arms (if / elseif / fall-through, with a ternary inside the last). The contract's item 7 does describe four naming OUTCOMES, so the intent is faithful, but as written the doc misdescribes the code's shape for the next reader.

### modules/Window.lua  preserved=True  gaming=False
  - modules/Window.lua:1259 and :1428 - STRANDED DOC COMMENT, a direct performance-§11 violation ('MUST NOT drop a comment that records why ... the code they explain moves, and the comment stays behind attached to nothing'). The LuaDoc line `--- The column labels above the grid.` was left at 1259 where ApplyColumnHeaders used to begin. The function moved to 1428 and now has NO doc comment; the orphane
  - modules/Window.lua:1301 - NEW CROSS-WINDOW SHARED MUTABLE SINGLETON. `local headerDress = {}` is correctly at file scope, so anti-pattern #43 is not tripped, but it introduces state that did not exist before the refactor: `place` (1497) is now a closure over a table shared by every window in the addon, where previously each pass read its values from per-call locals that could not survive the call.
  - modules/Window.lua:1300 - UNBACKED ASSERTION IN A NEW COMMENT. 'ApplyColumnHeaders is never re-entered, so a single table serves every window' is the entire safety argument for headerDress, and there is no test pinning it. It is stated as fact in a file whose surrounding comments are otherwise scrupulous about citing the bug or the rule behind each claim.
  - modules/Window.lua:311 - NEW DOC COMMENT MISDESCRIBES THE NEW CALL ORDER. columnLayout's header says 'The name column has already been placed at x 0 by the caller, so this starts the running x past it and its seam.' At the point columnLayout is actually called (line 424) the caller has NOT placed the name column - layout.nameColumn is constructed afterwards, at 434, from the nameWidth computed at 
  - modules/Window.lua:1318 and :1494 - COMMENT TEXT ALTERED WHILE MOVING. Em dashes in two moved why-comments were flattened to '--' ('has never held a value --' at 1318, "including the name column's --" at 1494) while the em dashes in the columnLayout block moved a few hundred lines earlier (e.g. 'what it always described — the shape a NEW column is born at', line 326) were left intact. Not a §11 vi
  - modules/Window.lua:1370-1426 - BORDERLINE EXTRACTION, reported for the record and not scored as gaming. dressHeaderButton is literally 'everything place() did after positioning', which is the shape performance-§11 warns about. I am not calling it metric gaming because the body was split three ways (newHeaderButton / dressHeaderButton / dressSortArrow) rather than one, each name denotes a thing the
  - modules/Window.lua:334 - PRE-EXISTING, NOT INTRODUCED (recorded so it is not re-found as new). The retained comment 'the frame is clamped instead (MinResize below)' cites a MinResize that does not exist anywhere in the file; the clamp is anchor:SetResizeBounds at 1655. The citation was equally stale in HEAD (old line 351) and merely rode along inside the moved block. Worth its own issue, not a fin
  - VERIFICATION NOTE: my sed to reduce SUITES to test_window did not match the way I expected, so the runner executed the full battery rather than the single suite as instructed. It completed green - 1728 passed, 0 failed, 0 skipped - with no interference observed from the sibling files being edited concurrently, so the result stands as evidence rather than noise. tests/test_window.lua is unmodified 

