{
 "tag": "v1.56.0",
 "items": [
  {
   "id": "WS-01",
   "repo": "WowAddonStandards",
   "title": "Open v2.65.0; AUDIT.md re-vendor check grades by step 5, counts same-day commits and reads a consolidated span bundle",
   "change": "PRE: WowAddonStandards is on master; create branch feat/2026-09-23-review-audit-remediation first (all WS items commit there). (1) standards/STANDARDS.md line 1 -> '# Ka0s WoW Addon Standard (v2.65.0, <date>)'; add a '- **v2.65.0 (<date>):**' entry at the top of '## Changelog' with numbered paragraph (1) for this item. WS-02..WS-08 append (2)..(n) to the SAME entry; nobody bumps again. (2) AUDIT.md, re-vendor bundle check (~:296-347): change `git log --since=\"$horizon\"` to `git log --since=\"$horizon 00:00\"`, because git fills in a bare date's time from the clock and so skips same-day commits. Replace both hard-coded grades (':335' and ':341-342' 'is a **High** finding') with: 'is a finding, graded by step 5 like every other: an unrecorded re-vendor is doc-only, so **Low**, and the entry names the `audit-review-history` MUST it fails'. Add one sentence saying step 5's table governs every grade in step 4. (3) Recorded-side loop: when a folder name carries two tags (`docs/revendor/<YYYY-MM-DD>-v<A>-v<B>/`), emit EVERY `v[0-9]+\\.[0-9]+\\.[0-9]+` on line 1 of its 01_DELTA.md instead of `tail -1`. Leave the single-tag and bare-dated branches unchanged. (4) standards/standards/audit-review-history.md: define the consolidated span bundle as the sanctioned record for a lapsed span. It is `docs/revendor/<date>-v<A>-v<B>/` holding 01_DELTA.md and 05_SUMMARY.md only. Line 1 of 01_DELTA.md is exactly `Delta: LibKa0s v<A> -> v<B> (span: v<A> v<...> v<B>)`, naming every tag. 05_SUMMARY.md has one line per tag, either 'carried by sweep, nothing adopted' or the adoption commit sha. Frozen bundles are never edited, and a base misstatement is corrected in the next bundle.\n\nPLAN-REVIEW CORRECTION: the vendored-side loop walks `git log -- libs/LibKa0s tests/_kit` (both paths), so kit-only re-vendors count.",
   "tests": "none \u2014 docs only: the playbook and standard are prose with no executable gate. Behavioral check instead: before editing, run the CURRENT recorded-side loop over a scratch copy of AuraMaster/docs/revendor/ plus a fake span folder and confirm it records only the last tag (red). Run the amended loop and confirm it records every tag (green).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; head -1 standards/STANDARDS.md | grep -q 'v2.65.0'; grep -n 'horizon 00:00' AUDIT.md; ! sed -n '/Check every re-vendor commit has its bundle/,/Check every bus message name/p' AUDIT.md | grep -n '\\*\\*High\\*\\*'; grep -n 'span:' standards/standards/audit-review-history.md AUDIT.md; T=$(mktemp -d); mkdir -p $T/2026-09-30-v1.18.0-v1.54.2; echo 'Delta: LibKa0s v1.18.0 -> v1.54.2 (span: v1.18.0 v1.19.0 v1.54.2)' > $T/2026-09-30-v1.18.0-v1.54.2/01_DELTA.md; (run the amended recorded-side loop with docs/revendor replaced by $T; expect 3 tags); git ls-files -z | xargs -0 grep -lI $'\\r' (expect nothing: LF repo)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-22",
    "AuraMaster-A-20",
    "ConsumableMaster-A-03",
    "KICKCD-A-02",
    "LootHistory-A-22",
    "PanelMaster-A-16",
    "PartyFrameEnhanced-A-19"
   ],
   "issue_refs": [],
   "depends_on": [],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-02",
   "repo": "WowAddonStandards",
   "title": "Rule the degraded composed-row conflict: hollow composers stand; host verbs write through a declared writeThrough list or refuse with one library-absent line",
   "change": "standards/standards/options-ui.md \u00a71 (the hollow-composer and fall-together passage, ~:50-60): keep 'a library-absent Options stub's composers answer {}' and anti-pattern #73 (no host copies). Replace the fall-together bound with this ruling. On a library-absent load, a host verb whose write targets a composed row (enabled, locked, test mode, the combat re-lock, the launcher stub) MUST do one of two things. (a) Write through the schema seam's declared `writeThrough` path list (LibKa0s-Schema-1.0 minor 2, from LibKa0s v1.56.0 / kit revision 26). The list is data (path strings) handed to both the live instance and the runtime-completing stub, with no row, label, widget, default or validate written by hand; the value's default comes from the AceDB defaults table as before. (b) Refuse with the library-absent line. It MUST NOT raise a Lua error and MUST NOT acknowledge a write that did not land. `enable`/`disable` SHOULD take (a). Choosing (b) for them is a SHOULD deviation recorded in the host's Documented deviations. standards/standards/slash-commands.md \u00a71: add the library-absent line, routed through the locale as one sentence with one placeholder: `%s is unavailable: the LibKa0s library did not load.` (%s = the full verb, e.g. '/wg enable'). Add: a library-absent Slash stub MAY carry exactly one library string verbatim, `LibKa0s-Slash-1.0`'s `DISABLED_LINE_FORMAT`, and MUST pin it against the live library with `Kit.assertLibraryConstant` (kit revision 26). It MUST NOT copy `FormatRow` (help rows print `cmd  desc` plainly), and minimal OnSlash dispatch is sanctioned. \u00a77: cross-reference the sanctioned verbatim refusal line. standards/standards/library-stack.md \u00a77 Schema row: name `writeThrough`, `normalize`, `SetMany` as minor-2 surfaces. anti-patterns.md #73: cross-reference the writeThrough route. STANDARDS.md changelog paragraph (2), citing AbsorbTracker's review item 62 / Info-6 (not plan item AT-62), PartyFrameEnhanced#14 and WhatGroup#22 (whose owner ruling takes route (b)).",
   "tests": "none \u2014 docs only (standard text). The executable halves land in LK-18 (Kit.assertLibraryConstant) and LK-23 (writeThrough + reference stub).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; grep -n 'writeThrough' standards/standards/options-ui.md standards/standards/library-stack.md standards/standards/anti-patterns.md; grep -n 'did not load' standards/standards/slash-commands.md; grep -n 'assertLibraryConstant' standards/standards/slash-commands.md; grep -c 'v2.65.0' standards/STANDARDS.md; grep -rnE '\u00a7[0-9]+\\.[0-9]+' standards/standards/*.md (expect nothing: retired notation)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-23"
   ],
   "issue_refs": [
    "PartyFrameEnhanced#14",
    "WhatGroup#22"
   ],
   "depends_on": [
    "WS-01"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "WS-03",
   "repo": "WowAddonStandards",
   "title": "Rule migration-stamp ownership: defaults declare schemaVersion 0, the runner owns the stamp, profile steps run per profile",
   "change": "standards/standards/savedvariables.md \u00a71: change the template (:10, :18-24) from `global = { schemaVersion = 1 }` / the current version to `global = { schemaVersion = 0 }`. Explain why in the text. AceDB's removeDefaults strips a stored value equal to its default at logout, so a default equal to the current version never persists and the first real migration is skipped (WhatGroup F-003). AceDB also backfills a declared default onto a legacy account with no stamp, which a current-version default would mask (PanelMaster febf108). 0 has neither problem. Add these rules. `NS.SCHEMA_VERSION` is the runner's target. The runner advances the stamp only past a step that returned without raising. A profile-scoped step runs for EVERY stored profile (walk the raw SV `profiles` table), or idempotently on OnProfileChanged with a per-profile stamp; it is never gated by the account-wide stamp alone (KickCD R-01, PartyFrameEnhanced R-15, PrettyChat R-03). Every step is idempotent against a fresh default profile. standards/standards/toc-file.md \u00a72 (:55): 'MUST declare schemaVersion = 0 in defaults'. standards/standards/versioning-git.md (:8): replace 'increment schemaVersion (in defaults)' with 'add a runner step and raise NS.SCHEMA_VERSION; the defaults value stays 0'. standards/standards/open-evolutions.md (:15-24): move 'Migration-stamp ownership' to ruled at v2.65.0, naming PanelMaster, LootHistory and WhatGroup as the worked cases. STANDARDS.md changelog paragraph (3).",
   "tests": "none \u2014 docs only. The rule's executable form is each addon's own red-first migration test (consumer follow-ups).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; grep -n 'schemaVersion = 0' standards/standards/savedvariables.md standards/standards/toc-file.md; ! grep -n 'increment schemaVersion' standards/standards/versioning-git.md; grep -n -i 'every stored profile' standards/standards/savedvariables.md; grep -n -A3 'Migration-stamp ownership' standards/standards/open-evolutions.md | grep -i 'ruled'",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-R-03",
    "LootHistory-A-18",
    "PanelMaster-A-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-04",
   "repo": "WowAddonStandards",
   "title": "Settle events-frames-taint-\u00a71 against library-stack-\u00a71, name LibKa0s-Core's SafeRegisterEvent as the helper, and state architecture-\u00a74's threshold",
   "change": "standards/standards/events-frames-taint.md \u00a71. (a) Add carve-out: an addon that embeds NO AceEvent-3.0 at all MAY carry ONE lazily created private watcher frame for non-unit boundary events (PLAYER_REGEN_DISABLED/ENABLED). The frame is fully unregistered on stand-down and listed in ## Event Subscriptions, with no register row. An addon that embeds AceEvent gets no such carve-out. (b) Name the collection helper: from LibKa0s v1.56.0, 'the single pcalled helper' is `LibKa0s-Core-1.0`'s `SafeRegisterEvent` / `SafeRegisterUnitEvent` / `SafeRegisterEvents` (LK-11), which front-gates with C_EventUtils.IsEventValid when present. The host owns the rejected list and surfaces it in the [Init] summary or a debug verb, and its Core stub carries one-rung pcall bodies. standards/standards/library-stack.md \u00a71: re-word the PrettyChat citation (:24) to cite the new carve-out rather than contradict ef-\u00a71. standards/standards/architecture.md \u00a74 (:60-71): the threshold becomes 'two or more feature modules, or a feature module that registers game events and whose events a second feature module must react to'. The AceAddon object's own event handlers are not a feature module, so a shell that registers events and calls its one feature module directly is below the threshold. Keep the same-target clobber rationale. STANDARDS.md changelog paragraph (4).",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; grep -n 'SafeRegisterEvent' standards/standards/events-frames-taint.md; grep -n -i 'embeds no AceEvent' standards/standards/events-frames-taint.md; grep -n -i 'AceAddon object' standards/standards/architecture.md; sed -n '20,30p' standards/standards/library-stack.md",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-08",
    "PRETTYCHAT-A-11",
    "WHATGROUP-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-05",
   "repo": "WowAddonStandards",
   "title": "Give compat an applicability condition: core/Compat.lua is required only by an addon that owns a deprecated or version-variant call",
   "change": "standards/standards/compat.md (:5, :25): replace 'every addon MUST ship core/Compat.lua' with this. An addon that calls any deprecated or version-variant client API outside LibKa0s's majors MUST own those calls in core/Compat.lua, the only file allowed to make them. An addon with none carries no Compat.lua and records compat-layer as Not applicable, citing this condition. Add: a library-absent fallback rung that calls a global every supported client provides only through a newer namespace (for example GetAddOnMetadata behind C_AddOns) is dead code and MUST be deleted rather than shimmed. standards/standards/library-stack.md \u00a77 (:88): the 'AbsorbTracker and PrettyChat carry none' note now cites the condition. standards/standards/documentation.md \u00a73: the compat-layer.md trigger counts shims in a PRESENT core/Compat.lua. STANDARDS.md changelog paragraph (5).",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; ! grep -n 'every addon MUST ship' standards/standards/compat.md; grep -n -i 'dead code' standards/standards/compat.md; grep -n 'carry none' standards/standards/library-stack.md",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-15",
    "PRETTYCHAT-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-06",
   "repo": "WowAddonStandards",
   "title": "Clarify reserved verbs in sub-trees, the value-hold verb, who pins a library strip, and the minimap row's CLI path polarity",
   "change": "(1) standards/standards/slash-commands.md \u00a72 (:31): enable/disable are reserved at the TOP level, and top-level reuse stays forbidden. Under a feature noun's sub-tree (`/<slash> <noun> enable <id>`) they MAY toggle that noun's items, because the first token already disambiguates (KickCD /kcd spells). (2) standards/standards/options-ui.md \u00a715 and preview-mode.md: an addon whose unlocked view is its preview ships no `test` verb. A one-shot value hold, if kept, lives at `/<slash> debug hold <value> [secs]`, which is not a test mode. (3) options-ui.md \u00a713 Testing: for a strip the library draws (O.TabStrip, O.RenderTabbedSchema), the library suite pins the wrap invariance and a consumer MUST NOT duplicate it (testing-\u00a78). A host-drawn strip is pinned by the host. (4) standards/standards/launcher.md \u00a73, cross-referenced from options-ui.md \u00a715: storage stays LibDBIcon's `minimap.hide`, and a stored `shown` key is still forbidden (anti-pattern #81). The minimap row's schema path, which is the CLI name, MUST read in the row's own shown sense: `<root>.minimap.shown`. It is a row whose get/set closures invert onto `minimap.hide`, so `/<slash> get <root>.minimap.shown` answers true while the button is visible. Commencement: each addon's next release. No SavedVariables migration, because the stored key does not move. STANDARDS.md changelog paragraph (6).",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; grep -n -i 'sub-tree' standards/standards/slash-commands.md; grep -n 'debug hold' standards/standards/options-ui.md standards/standards/preview-mode.md; grep -n 'minimap.shown' standards/standards/launcher.md standards/standards/options-ui.md; grep -n -i 'library suite pins' standards/standards/options-ui.md",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-27",
    "AbsorbTracker-A-14",
    "PRETTYCHAT-A-26",
    "WHATGROUP-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-07",
   "repo": "WowAddonStandards",
   "title": "Text corrections: debug buffer 1500, bootstrap underscore, the settings-panel table, the TOC literal, '.png ships', 'synchronis', and the lone-CR note",
   "change": "(1) standards/standards/debug-logging.md \u00a71 (:22, :126, :199) and NEW_ADDON_CONTEXT.md (:658, :1274): the ring buffer is 1500 lines, not 500, matching LibKa0s DebugLog MAX_BUFFER with its rationale (a perf dump overflowed 500). (2) architecture.md \u00a71 (:7-11): 'destructure both varargs; name the first `_` when the file never reads it'. (3) documentation.md \u00a73 settings-panel.md row (:237): a `Page | Covers` table with one row per settings subcategory page (options-ui-\u00a75), plus the page -> tab -> row tree. (4) toc-file.md \u00a73 (:10, :61): drop the literal 'currently 120007' and point at standards/ADDONS.md and /wow-addon:bump-interface. (5) layout.md \u00a74 (:125): the editable .png is committed but excluded from the package by a .pkgmeta ignore, rather than 'ships but is never loaded'. (6) localization.md \u00a75 BRITISH list: add `synchronis` (91 -> 92). It is admissible because no US word contains it. Record the new published counts 92/30. (7) line-endings.md \u00a77 (:505-510): from kit revision 26, test_eol counts lone CRs (a byte 13 not followed by 10) in every scanned path. Delete 'No file in the collection has one'. STANDARDS.md changelog paragraph (7).",
   "tests": "none \u2014 docs only. The kit halves are LK-06 (lone CR) and LK-07 (list 92).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; ! grep -n '\\b500\\b' standards/standards/debug-logging.md; grep -n 'synchronis' standards/standards/localization.md; ! grep -n '120007' standards/standards/toc-file.md; ! grep -n 'No file in the collection has one' standards/standards/line-endings.md; grep -n 'Page | Covers' standards/standards/documentation.md",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-21",
    "PRETTYCHAT-A-28",
    "WHATGROUP-A-22",
    "WHATGROUP-A-23",
    "ConsumableMaster-A-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WS-08",
   "repo": "WowAddonStandards",
   "title": "v2.65.0 ripple: executive summary, context pack, playbooks, README; close the changelog entry",
   "change": "Carry WS-02..WS-07 into the documents that restate them: standards/EXECUTIVE_SUMMARY.md, standards/NEW_ADDON_CONTEXT.md (savedvariables template `schemaVersion = 0` and the per-profile runner; the bootstrap `_`; the 1500-line buffer; the minimap row at `<root>.minimap.shown`; SafeRegisterEvent through Core and the Core stub's one-rung bodies; writeThrough and the library-absent line in the Slash/Options stub templates; the span bundle), NEW_ADDON.md and AUTOMATED_TESTS.md wherever they quote changed text, README.md's version mention, CLAUDE.md's 'As of vX' doc-set sentence if it moves, and standards/standards/library-stack.md \u00a77's LibKa0s inventory sentence naming v1.56.0, kit revision 26 and the 21-file count (unchanged). Date the v2.65.0 entry and add its one-line summary.",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards; grep -n 'schemaVersion = 0' standards/NEW_ADDON_CONTEXT.md; grep -n 'minimap.shown' standards/NEW_ADDON_CONTEXT.md; grep -rn 'v2.64.0' README.md standards/EXECUTIVE_SUMMARY.md (only historical mentions remain); grep -rnE '\u00a7[0-9]+\\.[0-9]+' standards AUDIT.md NEW_ADDON.md (expect nothing); git ls-files -z | xargs -0 grep -lI $'\\r' (expect nothing)",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "WS-02",
    "WS-03",
    "WS-04",
    "WS-05",
    "WS-06",
    "WS-07"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "WS-09",
   "repo": "WowAddonStandards",
   "milestone": "M1",
   "title": "options-ui-\u00a79: the library MAY park a combat-time category registration and MUST replay it once",
   "change": "Follow-up to WS-08. This carries out the approved plan's first option for LK-25, which WS-08 closed without. In standards/standards/options-ui.md: \u00a75's 'MUST register the parent category eagerly at addon load' bullet and \u00a79's 'is the thing that must happen eagerly' / 'itself never taints' passage gain one sentence: when CreateOptionsPanel is called under InCombatLockdown(), the library MAY park the registration and MUST replay it exactly once at PLAYER_REGEN_ENABLED, parking again if still locked. The host still calls it once at PLAYER_LOGIN and MUST NOT add its own park on top. Ripple inside the still-open v2.65.0: a new numbered point in the STANDARDS.md v2.65.0 changelog paragraph, naming LibKa0s Options minor 24 / LK-25; any NEW_ADDON_CONTEXT.md / EXECUTIVE_SUMMARY restatement of the eager-registration MUST; and anti-patterns if one names the host-side park. No version bump (v2.65.0 is unreleased).",
   "tests": "none (standards text); verify by grep",
   "verify": "grep -n 'PLAYER_REGEN_ENABLED' standards/standards/options-ui.md; grep -n 'MAY park' standards/standards/options-ui.md; git status --porcelain empty",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "WS-08"
   ],
   "effort": "S"
  },
  {
   "id": "LK-01",
   "repo": "LibKa0s",
   "title": "Kit revision 26 groundwork: peel framework.lua's assertion and parity families into asserts.lua, and test_prose's lists into prose_lists.lua",
   "change": "No behavior change. (a) New testkit/asserts.lua returning `function(Kit)`. MOVE from testkit/framework.lua: the fail/fmt locals, Kit.assertEqual/assertTrue/assertFalse/assertNil/assertNear/assertError (:235-303), Kit.setSurfaceSource, callable, resolveSurface, Kit.publicMembers and Kit.assertSurfaceParity (:305-452). framework.lua loads it once, where the block stood and before Kit.expose, from a directory derived from `debug.getinfo(1, \"S\").source` (fallback 'tests/_kit/'). framework.lua drops from 1583 to about 1360, under the layout-\u00a71 cap. (b) New testkit/prose_lists.lua: MOVE BRITISH, ALLOWED and PUBLISHED_BRITISH/PUBLISHED_ALLOWED (test_prose.lua:158-198) and the SKIPPED_DIRS table (:209-213) into it. test_prose.lua loads it through its own dir, which takes it from 1499 to about 1450 and makes later list growth land outside it. LibKa0s's own tests/test_prose.lua exempts the files that quote forbidden spellings (:73): add ['testkit/prose_lists.lua'] = true, and re-point any read of the kit lists. (c) Kit.VERSION 25 -> 26 (framework.lua:20). docs/api/testkit/version-26-docs.md is copied from version-25 (Status Current, Supersedes 25, 'What changed': two new files, no behavior change). version-25 becomes Superseded, with 'Moving to 26'. testkit/README.md's file inventory names both new files. Then `cp -r testkit/. tests/_kit/`. (d) CHANGELOG.md: open `## v1.56.0 \u2014 unreleased`, whose version block reads 'test kit revision 26', with an entry for this change. (e) CLAUDE.md: delete the over-cap census row for testkit/framework.lua, which resolves the 'Ratified deviation row' claim by peeling rather than by adding register row 5, and fix the census counts. docs/automated-tests/RESULTS.md is regenerated at the release (LK-33).",
   "tests": "Characterization first: record `lua5.1 tests/run.lua` totals and `--list` output before the move. After it, both must be identical apart from the kit-revision lines. Add a red-first case to tests/test_kitsync.lua: 'testkit/asserts.lua and testkit/prose_lists.lua exist in both testkit/ and tests/_kit/' (red before the files exist). The kit gate testkit/test_layout_cap.lua must now pass framework.lua under the cap with no census row.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (must print nothing); diff -r testkit tests/_kit (empty); wc -l testkit/framework.lua testkit/test_prose.lua testkit/asserts.lua testkit/prose_lists.lua (all < 1500); grep -n 'Kit.VERSION = 26' testkit/framework.lua; no tests/perf.lua in this repo (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-04"
   ],
   "issue_refs": [],
   "depends_on": [],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-02",
   "repo": "LibKa0s",
   "title": "Kit.assertErrorMatches, and the 23 statement-position assertError calls assert on the raised text",
   "change": "testkit/asserts.lua: add `Kit.assertErrorMatches(fn, needle, msg)`. It pcalls fn, fails if fn did not raise, and fails if `tostring(err):find(needle, 1, true)` is nil, naming both strings. It returns err and is exposed by Kit.expose as assertErrorMatches. Rewrite the 23 statement-position calls to assert on a substring of the raised error: tests/test_launcher.lua:123-128 (x4; the strings now passed as failure messages become the needles), tests/test_kit_inventory.lua:173,182,190,202,230, tests/test_mock_ace.lua:119,120,137,402,403,488,617,618,629, tests/test_loader.lua:116-117, tests/test_options_compose.lua:674-675, tests/test_schema.lua:548. docs/api/testkit/version-26-docs.md: the new member, Since 26. Sync tests/_kit. CHANGELOG v1.56.0 entry.",
   "tests": "Red first: new tests/test_kit_asserts.lua, declared in tests/run.lua, with three cases: passes on a matching needle; fails when fn raises a different message; fails when fn does not raise. Each carries a '-- red under:' line; the first is red before the member exists. Then rewrite the call sites. For each rewritten site, change the needle once locally, confirm red, and restore.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua (total = previous + 3); ! grep -nE '^\\s*(T\\.|Kit\\.)?assertError\\(' tests/*.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-01"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-03",
   "repo": "LibKa0s",
   "title": "AceDB fake fidelity: CopyProfile and DeleteProfile raise as AceDB-3.0 does, and SetProfile strips defaults",
   "change": "testkit/mock_record.lua:241-270. CopyProfile(name, silent) raises AceDB's own message at level 2 when name == current, and when the source is missing unless silent. DeleteProfile(name, silent) raises on the active profile, and on a missing profile unless silent. Copy the messages byte for byte from any consumer's libs/AceDB-3.0/AceDB-3.0.lua:531-537 and :581-587, and cite the line. SetProfile(name) runs a removeDefaults mirror over the OUTGOING profile before switching (AceDB :460-463). Model the scalar and plain-table arms, and name the unmodeled '*'/'**' wildcard arms in a comment. Keep the OnProfileChanged/OnProfileCopied firing as it is. docs/api/testkit/version-26-docs.md 'What changed' marks this as behavioral: consumer suites that call these on a bad name will now see the raise the client would. Sync tests/_kit. CHANGELOG entry.",
   "tests": "Red first in tests/test_mock_record.lua (377 lines): CopyProfile(current) raises the verbatim text; CopyProfile('missing') raises; CopyProfile('missing', true) is silent; DeleteProfile(current) raises; DeleteProfile('missing') raises; DeleteProfile('missing', true) is silent; SetProfile('B') strips a value equal to its default from profile A. Each has '-- red under:' naming the old silent return. Assert with assertErrorMatches (LK-02).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); wc -l testkit/mock_record.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-06",
    "PartyFrameEnhanced-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-04",
   "repo": "LibKa0s",
   "title": "Kit mock: a recording EventRegistry, frame RegisterEvent/RegisterUnitEvent honoring __badEvents, and C_EventUtils.IsEventValid",
   "change": "New testkit/mock_events.lua, loaded by testkit/mock_base.lua through its own dir. mock_base.lua is 1446 lines and may grow by at most 3 (the load line). It provides three things. (1) `EventRegistry` with RegisterCallback(event, fn, owner), UnregisterCallback(event, owner) and TriggerEvent(event, ...); every live callback appears in `M.__registrations()` as `{ kind = \"callback\", event = ..., owner = ... }`. (2) stubFrame's RegisterEvent/RegisterUnitEvent (today metatable no-ops) raise `Attempt to register unknown event \"<NAME>\"` for a name in `M.__badEvents`, read at call time, with the same message the AceEvent path uses at mock_base.lua:715-727. (3) `C_EventUtils.IsEventValid(name)` answers false for names in M.__badEvents and true otherwise, and a suite may set `C_EventUtils = nil` to model an older client. docs/api/testkit/version-26-docs.md documents all three, Since 26. Sync tests/_kit. CHANGELOG entry.",
   "tests": "Red first in a new tests/test_mock_events.lua, declared in tests/run.lua: a registered callback appears in __registrations with kind 'callback'; UnregisterCallback removes it; TriggerEvent reaches it; frame:RegisterEvent('BAD') with __badEvents = {BAD = true} raises; RegisterUnitEvent likewise; IsEventValid answers false/true. Each carries '-- red under:'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); wc -l testkit/mock_base.lua testkit/mock_events.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-01"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-05",
   "repo": "LibKa0s",
   "title": "Kit mock CreateFrame starts shown, as the client does; collection dry-run of the flip",
   "change": "testkit/mock_base.lua:121: `local f = { __shown = true, __scripts = {} }`. Update the comment and fidelity rule 5's note. Then audit this repo's suites that read shown state (grep -l '__shownFrames\\|IsShown' tests/*.lua). Fix a case that relied on hidden-by-default by giving its setup the Hide() the production path performs, never by weakening an assertion. docs/api/testkit/version-26-docs.md 'What changed': a behavioral flip, with a consumer note that a stand-down suite's F_on baseline now sees container frames. Sync tests/_kit. CHANGELOG entry. Collection dry-run, which must not touch any addon repo: `for A in AbsorbTracker AuraMaster BankLedger ConsumableMaster KickCD LootHistory MultiMeters PanelMaster PartyFrameEnhanced PrettyChat WhatGroup; do T=$(mktemp -d); git -C ../$A archive HEAD | tar -x -C $T; cp -r testkit/. $T/tests/_kit/; (cd $T && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua) > <scratchpad>/dryrun-lk05-$A.txt 2>&1; done`. Record every red case per addon in the item's commit message body, and in the plan's consumer follow-ups as fixes the addon owes before its re-vendor.",
   "tests": "Red first: tests/test_mock_base.lua 'a new frame is shown until hidden, as in the client' (red under the current `__shown = false`).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); dry-run outputs exist for all 11 addons; no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-04"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-06",
   "repo": "LibKa0s",
   "title": "test_eol catches a lone CR in any scanned text file",
   "change": "testkit/test_eol.lua terminators() (:175-185): besides counting LF and CR-before-LF, count lone CRs (byte 13 not followed by byte 10) for every tracked path the gate already scans (text attribute not unset, no NUL byte). Fail with path:line for each. Do NOT key the check on the index's -text classification, which would redden real auto-detected binaries such as PanelMaster's tools/artwork/bin/realesrgan-ncnn-vulkan (see the comment at :199). docs/api/testkit/version-26-docs.md. Sync tests/_kit. CHANGELOG entry. Dry-run the gate over all 11 addons (as in LK-05). AuraMaster's tests/page_helpers.lua:80 is expected red and is an addon fix owed before its re-vendor.",
   "tests": "Red first in tests/test_kit_eol.lua with fixtures: a file whose line ends `\\r\\r\\n` fails, naming path:line; a file with a NUL byte is skipped; clean LF and CRLF files pass. Each carries '-- red under:'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); wc -l testkit/test_eol.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-01",
    "WS-07"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-07",
   "repo": "LibKa0s",
   "title": "Prose gate: scan store-root README/RESULTS files, skip docs/superpowers/ and docs/investigations/, and publish 'synchronis'",
   "change": "testkit/prose_lists.lua (from LK-01): SKIPPED_DIRS gains 'docs/superpowers/' and 'docs/investigations/' (documentation-\u00a73 frozen stores). A new named list SCAN_BACK = { 'docs/automated-tests/README.md', 'docs/automated-tests/RESULTS.md', 'docs/perf-analysis/README.md' } names files testkit/test_prose.lua scans even though they sit under a skipped dir. They are named file by file with no pattern, as localization-\u00a75 requires. BRITISH gains 'synchronis', and PUBLISHED_BRITISH becomes 92 after WS-07. test_prose.lua's walker honors SCAN_BACK. LibKa0s's own tests/test_prose.lua (register row 3) applies the same scan-back to this repo's docs/automated-tests/README.md and RESULTS.md, and any hit is fixed in this commit. docs/api/testkit/version-26-docs.md. Sync tests/_kit. CHANGELOG entry. Dry-run over the 11 addons. Expected reds are ConsumableMaster and KickCD docs/perf-analysis/README.md ('analysed', 'neighbours'), ConsumableMaster docs/settings-panel.md:80 ('synchronisation'), and any other store root; each is an addon fix owed before its re-vendor.",
   "tests": "Red first in a new tests/test_kit_prose.lua (declared in tests/run.lua) over a fixture tree in a temp dir: a British word in docs/perf-analysis/README.md is red; the same word in docs/perf-analysis/20260101-000000/ANALYSIS.md is green; in docs/superpowers/specs/x.md it is green; 'synchronisation' in README.md is red; PUBLISHED_BRITISH == #BRITISH == 92.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); wc -l testkit/test_prose.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-05",
    "KICKCD-A-06",
    "PanelMaster-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-01",
    "WS-07"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-08",
   "repo": "LibKa0s",
   "title": "Kit citations carry the section sign; the ASCII gate scopes to the shipped LibKa0s/ payload",
   "change": "LibKa0s tests/test_prose.lua ASCII gate ('no non-ASCII byte reaches a player', :405): scope it to paths under LibKa0s/ only. Kit strings print to a terminal and never reach a player, and the tests/_kit copy never ships (every .pkgmeta ignores tests). Rewrite the comment block (:271-310) to say so. Update register row 3's text if it quotes the old scope. Then respell every kit string literal and case name that cites a section: localization-5 -> localization-\u00a75, line-endings-N -> line-endings-\u00a7N, layout-1 -> layout-\u00a71, testing-9/12 -> testing-\u00a79/\u00a712. The sites are testkit/test_prose.lua, test_eol.lua (e.g. :529-630), test_layout_cap.lua, framework.lua (KIT_GATE_RULE :799-803, whose comment :795-798 is rewritten) and asserts.lua. normRule is unchanged and still matches both spellings in register cells. docs/api/testkit/version-26-docs.md notes that the case names change, so every consumer regenerates docs/test-cases.md. Sync tests/_kit. CHANGELOG entry.",
   "tests": "Red first: (1) tests/test_prose.lua 'the ASCII gate scans LibKa0s/ and not testkit/'. It feeds the scan function a testkit path containing '\u00a7' and expects no hit; it is red under the old scope. (2) A new case in tests/test_kit_inventory.lua: no kit .lua string literal matches '(localization|line%-endings|layout|testing|documentation)%-%d' without the section sign (red before the respell).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; ! grep -nE '\"[^\"]*(localization|line-endings|layout|testing)-[0-9]' testkit/*.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-17",
    "WHATGROUP-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-07"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-09",
   "repo": "LibKa0s",
   "title": "Runner: headed watch-list tables when empty, and the performance-\u00a712 exemption as perf skip reason (2)",
   "change": "testkit/run-automated-tests.sh. (1) fn_table (:713-728) and band_table (:730-): print the header row and separator unconditionally, with no data rows for an empty set, and delete the `printf 'None.'; return` early exits (automated-tests-\u00a74). (2) Perf skip (:343-344): before falling back to reason (1), read the `## Documented deviations` table in docs/ARCHITECTURE.md (addon) or root CLAUDE.md (library), in that order, with the row parsing framework.lua's deviationRows uses. If a row's rule cell matches `performance-\u00a7?12`, set NOTE[perf]='performance-\u00a712 no-combat-path exemption (ratified; <file> -> Documented deviations)'. The manifest skipReason carries it, and md_perf_section (:833-848) prints a reason-(2) paragraph pointing at docs/performance.md. The reason-(1) text is unchanged. A register heading with an unparseable table exits 2 with a message rather than guessing. `KA0S_PERF_EXEMPT=1` is honored only when no register exists. docs/api/testkit/version-26-docs.md. testkit/README.md runner section. Sync tests/_kit, keeping the +x bit. CHANGELOG entry.",
   "tests": "Red first in a new tests/test_kit_runner.lua (declared in tests/run.lua). It builds fixture repos in temp dirs and runs `sh <abs>/testkit/run-automated-tests.sh --no-bundle --suite perf` from inside each: with a performance-\u00a712 register row the output names reason (2); without one it names reason (1); a malformed register exits 2. A second case runs `--suite complexity --no-bundle` over a one-function fixture and asserts the '| Function |' header prints with no 'None.'; it uses Kit.skip when lizard is absent.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; sh -n testkit/run-automated-tests.sh; ! grep -n \"printf 'None\" testkit/run-automated-tests.sh; test -x tests/_kit/run-automated-tests.sh; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); no tests/perf.lua (perf: skip, reason 1 for this repo, which holds no performance-\u00a712 row)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-09",
    "BankLedger-A-04",
    "LootHistory-A-12",
    "PRETTYCHAT-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-01"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-10",
   "repo": "LibKa0s",
   "title": "Core minor 8: printer.Format survives a secret value in a numeric specifier",
   "change": "LibKa0s/Core.lua:455-460, printer.Format: `local ok, out = pcall(string.format, SafeToString(fmt), unpack(parts))`. On failure, emit fmt and the stringified parts joined with single spaces, the same fallback DebugLog.lua:640-658 uses. No floor moves. Bump `MINOR` in Core.lua 7 -> 8. docs/api/Core/version-8-docs.md is copied from version-7 (Current, Supersedes 7, 'What changed', Since 8). version-7 becomes Superseded, with 'Moving to 8'. Add the docs/api/README.md row. `lua5.1 tools/gen-api-members.lua` writes members-8.json. The CHANGELOG v1.56.0 version block adds 'Core minor 8', plus an entry.",
   "tests": "Red first in tests/test_core.lua: printer.Format('%d rows', <secret sentinel>) emits a line containing '%d rows' and '<secret>' without raising (red under: remove the pcall); '%d rows' with 3 still prints '3 rows'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua (test_versioning green: docs + members-8.json + changelog agree); $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Core.lua; no tests/perf.lua (perf: skip)",
   "smoke": "In a Midnight dungeon with a Ka0s addon's debug on, trigger a printer line that formats a combat value; expect a chat line and no Lua error (S-007).",
   "finding_ids": [
    "LibKa0s-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-11",
   "repo": "LibKa0s",
   "title": "Core: SafeRegisterEvent / SafeRegisterUnitEvent / SafeRegisterEvents, the collection's pcalled registration helper",
   "change": "LibKa0s/Core.lua, still minor 8 (LK-10 bumped it; no second bump). Add three lib-level members. `lib.SafeRegisterEvent(target, event, handler, rejected) -> boolean`: if `C_EventUtils and C_EventUtils.IsEventValid` exists and answers false, the name is rejected without a call; otherwise it runs `pcall(target.RegisterEvent, target, event, handler)`, which works for an AceEvent-embedded object and for a Frame. `lib.SafeRegisterUnitEvent(frame, event, rejected, unit1, unit2) -> boolean` does the same through RegisterUnitEvent. `lib.SafeRegisterEvents(target, events, handler, rejected) -> number registered` walks an array. `rejected` is an optional caller-owned array, appended once per name with no duplicates. The library keeps no state and prints nothing; the host surfaces the list (events-frames-taint-\u00a71, WS-04). A Bus-stamped target still records the registration, because the call goes through `target:RegisterEvent`. Keep each function at CCN 10 or below. Extend docs/api/Core/version-8-docs.md: the three members, Since 8, and a 'Degradation' note that a host's Core stub carries one-rung bodies (pcall, no front gate) so Kit.assertSurfaceParity stays green. Regenerate members-8.json. CHANGELOG entry.",
   "tests": "Red first in tests/test_core.lua (586 lines), using the kit's M.__badEvents plus the C_EventUtils mock from LK-04: a bad name among three leaves the other two registered and the bad one in `rejected` once; with IsEventValid present the bad name is rejected with no RegisterEvent call; with C_EventUtils = nil the pcall path records it; SafeRegisterUnitEvent on a frame likewise; SafeRegisterEvents returns the count. Each carries '-- red under: a bare RegisterEvent'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; grep -n 'SafeRegisterEvent' docs/api/Core/members-8.json; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Core.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "/reload with any consumer on the new tag; expect no Lua error and the [Init] line listing zero rejected events.",
   "finding_ids": [
    "AuraMaster-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-10",
    "LK-04",
    "WS-04"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-12",
   "repo": "LibKa0s",
   "title": "Item minor 2: QualityFromLink reads the 11.1.5+ |cnIQ<n> link color, and the quality map is cached only when non-empty",
   "change": "LibKa0s/Item.lua:85: ahead of the hex rung, add `local q = link:match(\"|cnIQ(%d+)\"); if q then return tonumber(q) end`, anchored on digits with no trailing ':' required. Keep the hex rung for stored legacy links. :63-71 buildQualityByHex builds into a local and assigns `qualityByHex` only when at least one entry landed, so an empty early build is retried on the next call. LoadItem (:124, fixed 0.4 s timer) is deliberately unchanged, because it is byte-identical in both hosts; the Item version-2 doc says so. Bump `MINOR` 1 -> 2. docs/api/Item/version-2-docs.md (Current, Since 2) and version-1 Superseded. docs/api/README.md row. gen-api-members writes members-2.json. CHANGELOG 'Item minor 2' plus entry.",
   "tests": "Red first in tests/test_item.lua: '|cnIQ4:|Hitem:19019::::::::60:::::|h[Thunderfury]|h|r' answers 4, and |cnIQ0 answers 0 (red under: drop the cnIQ rung). The existing |cff cases stay as legacy-rung cases. An empty ITEM_QUALITY_COLORS on the first call followed by a populated one answers the right quality (red under: caching the empty map).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "LootHistory on the new tag: loot an uncached epic, then /lh history shows it with epic quality color (S-001).",
   "finding_ids": [
    "LibKa0s-R-01",
    "LibKa0s-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-13",
   "repo": "LibKa0s",
   "title": "Media minor 4: register JetBrains Mono with a western+ruRU langmask and count only what LSM accepted",
   "change": "LibKa0s/Media.lua:274-275: `LSM:Register(\"font\", name, path, LSM.LOCALE_BIT_western + LSM.LOCALE_BIT_ruRU)` when those constants exist, and plain Register otherwise. Count a font or statusbar only if `LSM:IsValid(type, name)` answers true after registering. Rewrite the comment at :251-258: the first registration wins, every consumer's path names identical bytes, and CJK clients are excluded because the face has no CJK glyphs. Bump `MINOR` 3 -> 4. docs/api/Media/version-4-docs.md and version-3 Superseded. README row. members-4.json. CHANGELOG entry.",
   "tests": "Red first in tests/test_media.lua with a fake non-western LSM whose Register refuses a nil langmask: the font registers with the mask and the count is 1 (red under: no mask); a Register answering false and IsValid false gives count 0 (red under: unconditional count).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "On a ruRU client and an enUS client, open any Ka0s debug console; JetBrains Mono renders and appears in SharedMedia font lists (S-004).",
   "finding_ids": [
    "LibKa0s-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-14",
   "repo": "LibKa0s",
   "title": "Bus minor 2: re-stamp tracking wrappers a newer AceEvent re-embed overwrote",
   "change": "LibKa0s/Bus.lua:152-196: keep `rec.wrap[kind]`. Add `restamp(rec)`: for each of the six members, if target[member] ~= rec.wrap[member], adopt the new raw member as rec.raw[member] and re-assign the wrapper. Keep a weak-keyed `created` list beside `held` and restamp every created target at the top of StandDown and StandUp. isDown's debug seam returns a trailing restamped count. The docstring states the residual window: registrations made between a re-embed and the next edge are untracked until that edge. No Ace3 fork and no metatable proxy. Bump `MINOR` 1 -> 2. docs/api/Bus/version-2-docs.md and version-1 Superseded. README row. members-2.json. CHANGELOG entry.",
   "tests": "Red first in tests/test_bus.lua: simulate AceEvent's upgrade loop by re-assigning the target's six members to raw functions, then register an event, StandDown, and assert the registration is gone (red under: no restamp); StandUp replays it; the debug seam reports restamped = 1.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing: lib.Catalog stays at 15 or below); no tests/perf.lua (perf: skip)",
   "smoke": "With an addon that ships a newer AceEvent-3.0 minor loaded after a Ka0s Bus host, run /<slash> disable then enable; no event fires while disabled (S-005).",
   "finding_ids": [
    "LibKa0s-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-15",
   "repo": "LibKa0s",
   "title": "Lifecycle minor 2: document and pin the nested-edge (re-entrancy) behavior",
   "change": "LibKa0s/Lifecycle.lua New's docstring (:75-89): a standDown/standUp callback MUST NOT take or release a hold. If it does, edge() re-enters synchronously and the nested edge runs inside the outer one; the latch stays consistent but host teardown interleaves. Code is unchanged apart from the comment, and a comment still changes the file's bytes, hence the minor bump 1 -> 2. docs/api/Lifecycle/version-2-docs.md adds a 'Re-entrancy' section and version-1 becomes Superseded. README row. members-2.json. CHANGELOG entry.",
   "tests": "Characterization in tests/test_lifecycle.lua: a standDown callback that releases a hold fires standUp nested before standDown returns, and IsDown() ends false. The case pins today's behavior, so it is green on the first run by design; it documents the contract.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-16",
   "repo": "LibKa0s",
   "title": "Launcher minor 2: an isEnabled/disabledLine gate for a rung (a)/(b) left click, and missing-library notices printed once, untagged",
   "change": "LibKa0s/Launcher.lua. Descriptor (:98-127) gains optional `isEnabled` (function) and `disabledLine` (function answering the Slash refusal line). In click() (:178-185), when the left button with onClick present and `isEnabled` present answers false, it emits disabledLine() and returns without calling onClick. Rung (c) is still expressed by omitting onClick, and right-click is unchanged. NO_BROKER/NO_ICON/NO_MINIMAP (:203, :228, :235) are guarded by a per-instance once[key]. The '[LibKa0s] ' prefix is dropped from lib.STRINGS values (:70-75), because the host printer tags them; keys are unchanged, so L overrides still work. Bump `MINOR` 1 -> 2. docs/api/Launcher/version-2-docs.md (fields Since 2) and version-1 Superseded. README row. members-2.json. CHANGELOG entry.",
   "tests": "Red first in tests/test_launcher.lua (439 lines): with isEnabled false, a left click prints disabledLine and never calls onClick (red under: no gate); rung (c) with no onClick still opens settings; right-click still opens settings; two Register calls with no LibDBIcon print NO_ICON once (red under: no once guard); STRINGS carry no '[LibKa0s]'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; ! grep -n '\\[LibKa0s\\]' LibKa0s/Launcher.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "BankLedger on the new tag, adopted: /bl disable, then left-click the minimap button; it prints the collection refusal line and opens nothing (S-006).",
   "finding_ids": [
    "LibKa0s-R-06",
    "LibKa0s-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-17",
   "repo": "LibKa0s",
   "title": "Slash minor 15: CliSet/CliReset print the write seam's refusal instead of echoing the unchanged value",
   "change": "LibKa0s/Slash.lua CliSet (:669-695, the call at :688): `local ok, err, why = d.set(row.path, v)`. When ok == false, print INVALID for the path, then err and why on indented lines, and return. Otherwise echo as today; nil or true still means success. CliReset (:696-, the call at :703): when applyDefault answers exactly false, print a new lib.STRINGS.NO_DEFAULT ('%s has no default to restore'). Document on the descriptor's `set` that it may answer `false, reason[, why]`. Bump `MINOR` 14 -> 15. docs/api/Slash/version-15-docs.md and version-14 Superseded. README row. members-15.json. CHANGELOG entry.",
   "tests": "Red first in a new tests/test_slash_refusal.lua (test_slash.lua is 1327 lines), declared in tests/run.lua: `set` answering false,'Invalid value for x','must be 1-10' prints both lines and no echo (red under: discarded return); set answering true echoes; nil echoes; CliReset with applyDefault false prints NO_DEFAULT.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Slash.lua; no tests/perf.lua (perf: skip)",
   "smoke": "MultiMeters on the new tag: /mm set <a validated row> 999 prints the refusal and the reason, and the value is unchanged (S-003).",
   "finding_ids": [
    "LibKa0s-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-18",
   "repo": "LibKa0s",
   "title": "Degraded Slash stub contract: Kit.assertLibraryConstant and the Slash doc's prescribed stub shape",
   "change": "testkit/asserts.lua: add `Kit.assertLibraryConstant(value, majorName, memberPath, msg)`. It resolves the live library through the surface source Kit.setSurfaceSource registered (the same resolution assertSurfaceParity uses by name), reads memberPath (e.g. 'DISABLED_LINE_FORMAT'), and asserts byte equality, naming both strings on failure. docs/api/testkit/version-26-docs.md documents it, Since 26. docs/api/Slash/version-15-docs.md gains a '## The degradation stub' section implementing WS-02: minimal OnSlash dispatch; `DisabledLine` built from the verbatim DISABLED_LINE_FORMAT bytes and pinned with Kit.assertLibraryConstant; help rows printed `cmd  desc` with no FormatRow copy; composed-row verbs either write through `writeThrough` or print the library-absent line `%s is unavailable: the LibKa0s library did not load.`, never raising. Sync tests/_kit. CHANGELOG entry. Slash.lua does not change.",
   "tests": "Red first in tests/test_kit_asserts.lua: assertLibraryConstant(lib.DISABLED_LINE_FORMAT, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT') passes; a one-byte-different string fails naming both; an unknown major fails with a clear message.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; grep -n 'assertLibraryConstant' docs/api/testkit/version-26-docs.md docs/api/Slash/version-15-docs.md; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-11"
   ],
   "issue_refs": [
    "WhatGroup#22"
   ],
   "depends_on": [
    "LK-17",
    "WS-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-19",
   "repo": "LibKa0s",
   "title": "DebugLog minor 13: batched buffer trim instead of table.remove(1) per line at the cap",
   "change": "LibKa0s/DebugLog.lua:623: D.buffer stays a public ordered array, because tests/test_debuglog.lua:87-142 and AbsorbTracker/BankLedger tests index it. So no ring buffer. Once #D.buffer exceeds MAX_BUFFER + 64, compact by moving the newest MAX_BUFFER lines down in one pass. BufferSize/LastLine/FindLine/CopyText read only the newest MAX_BUFFER lines, so a reader never sees more than the cap. MAX_BUFFER stays 1500, which WS-07 makes the standard's number. Bump `MINOR` 12 -> 13. docs/api/DebugLog/version-13-docs.md, which states the 64-line slack and that D.buffer may briefly hold up to 1564 raw entries while every public reader answers 1500; version-12 Superseded. README row. members-13.json. CHANGELOG entry.",
   "tests": "Characterization first in tests/test_debuglog.lua (892 lines), before editing: record BufferSize, LastLine, FindLine and CopyText at 1499/1500/1501/1600 lines, and '1501st line drops the first'. All must stay green after the change. Then add a red case: 1564 adds cost at most one compaction (count table moves via a wrapped table.remove spy), which is red under per-line remove.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "/<slash> debug on, then generate more than 1600 lines (e.g. /reload spam or a debug scan); the console shows the newest 1500 and copy-all works (S-008).",
   "finding_ids": [
    "LibKa0s-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02",
    "WS-07"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-20",
   "repo": "LibKa0s",
   "title": "Perf minor 13: armed/recording/label stay raw fields (false, never nil), and openDepth resets at window edges",
   "change": "LibKa0s/Perf.lua: initialize P.armed, P.recording and P.label as false (:400-405), and rewrite the :400-401 comment: P.on is a raw field and the metatable exists only for 'suspended'. Change every later nil write to false (:911 P.armed, :921 P.recording, :959, :1011, :1033, :1044 P.label). Audit `== nil` reads of these three first (grep -n 'armed\\|recording\\|label' LibKa0s/Perf.lua LibKa0s/PerfPanel.lua); readers already tolerate false (:307-308 string compare, P.LABELS[false] is nil, 'if not w'). openWindow (:909) and closeWindow (:919) reset `openDepth = 0`. Bump `MINOR` 12 -> 13 (PerfPanel stays 5, key 13.5). docs/api/Perf/version-13.5-docs.md and version-12.5 Superseded. README row. members-13.5.json. CHANGELOG entry.",
   "tests": "Red first: tests/test_perf_core.lua 'a leaked Open in window A does not parent a bracket in window B' (red under: no reset); tests/test_perf_isolation.lua adds `rawget(P, 'recording') ~= nil` after a window closes (red under: nil write). The existing 0 KB pin stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; ! grep -nE 'P\\.(armed|recording|label) *= *nil' LibKa0s/Perf.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Perf.lua; no tests/perf.lua (perf: skip)",
   "smoke": "KickCD or MultiMeters on the new tag: /<slash> perf start, a pull, then /<slash> perf finish; the report names buckets with correct parents and no Lua error.",
   "finding_ids": [
    "LibKa0s-R-11",
    "LibKa0s-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-21",
   "repo": "LibKa0s",
   "title": "Widgets minor 10: ReorderList polls on its own ghost frame and draws its drop line from a library pool",
   "change": "LibKa0s/Widgets.lua: the drag poll moves from `row.frame:SetScript(\"OnUpdate\")` (:999, cleared at :915 and :1080) to the library-owned ghost frame, so a host row frame's OnUpdate is never touched. The drop line (:739-753, cached on the AceGUI-pooled container as __ka0sDropLine) comes from a library pool (LibKa0s-Pool-1.0, or a module-local free list if Widgets does not floor on Pool) acquired per drag, recolored per list's lineColor and released in Cancel/drop. This restores the file's own pooled-frame invariant at :783-795. Bump `MINOR` 9 -> 10 (DragHandle stays 2, key 10.2). docs/api/Widgets/version-10.2-docs.md and version-9.2 Superseded. README row. members-10.2.json. CHANGELOG entry.",
   "tests": "Red first in a new tests/test_widgets_reorder.lua (test_widgets.lua is 1493 lines), declared in tests/run.lua: a host OnUpdate on a row frame survives a drag start and end (red under: SetScript on row.frame); two lists with different lineColor show their own color on the same pooled container (red under: cached line); the drop line is released after Cancel.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Widgets.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "ConsumableMaster Category priority list and KickCD Spells list on the new tag: drag rows; the ghost follows, the drop line shows in each list's color, and the order saves.",
   "finding_ids": [
    "LibKa0s-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-22",
   "repo": "LibKa0s",
   "title": "Schema minor 2: instanceId forwarding, row.normalize, and SetMany (all-or-nothing batch, one bracket line, one announce)",
   "change": "LibKa0s/Schema.lua. (1) R-14: S.Get calls `row.get(instanceId)` (:358); S.ApplyDefault(row, instanceId) forwards the id to S.Set (:497-502). (2) `row.normalize(value, rid)`, after validate passes and before store: it answers the value to store, or `nil, why`, which refuses with INVALID. AuraMaster and ConsumableMaster are the two consumers, so library-stack-\u00a77 bar 2 is cleared. (3) `S.SetMany(entries, opts)`, where entries = { {path=, value=}, ... } and opts = { instanceId=, act=, scope= }. Phase 1 resolves, validates and normalizes every entry and returns `false, err, why, index` before any store. Phase 2 stores each entry in order; when opts.act is given it does so inside one bracket, so one `[Set] <act> <scope>: N rows` line; otherwise each write logs its own line. Each row's onChange runs. Then the descriptor's optional `announceBatch(writes, rid)` is called once with an array of {row, path, value}, or, without it, `announce` runs per write. Returns true. Extract a shared `prepareWrite(row, path, value, instanceId)` used by S.Set and SetMany so each stays at CCN 10 or below. Update the header's 'deliberately does not do' paragraph, since normalize and the all-or-nothing batch are no longer excluded. Bump `MINOR` 1 -> 2. docs/api/Schema/version-2-docs.md (Current, Since 2; the stub table adds SetMany with the same all-or-nothing semantics and log-silence). Its 'Adoption notes' gains per-host mappings: KickCD (row.section read by announce; SESSION/GLOBAL paths as rows with get/set; the master switch as the `enabled` row's onChange, which runs before announce), ConsumableMaster (validate-coerce into normalize; SetManyAndRefresh -> SetMany + announceBatch), MultiMeters (SetByPaths -> SetMany), AuraMaster (partial adoption now possible with normalize). version-1 Superseded. README row. members-2.json (lib-level unchanged; the doc says instance stubs gain SetMany). CHANGELOG entry.",
   "tests": "Red first in a new tests/test_schema_batch.lua (test_schema.lua is 1233 lines), declared in tests/run.lua: SetMany with one invalid entry stores nothing and returns index; an unknown path is refused whole; a valid batch stores all, runs every onChange, calls announceBatch once, and with act logs exactly one bracket line with N = rows changed; normalize's value is stored, and normalize answering nil,why refuses; Get passes instanceId to row.get; ApplyDefault(row, id) reaches Set with id. Extend tests/test_schema.lua's referenceStub with SetMany and pin it against a live instance with the two-table T.assertSurfaceParity.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); wc -l LibKa0s/Schema.lua tests/test_schema.lua tests/test_schema_batch.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-R-14"
   ],
   "issue_refs": [
    "ConsumableMaster#39",
    "MultiMeters#52",
    "KickCD#22",
    "AuraMaster#21"
   ],
   "depends_on": [
    "LK-02"
   ],
   "effort": "L",
   "milestone": "M1"
  },
  {
   "id": "LK-23",
   "repo": "LibKa0s",
   "title": "Schema writeThrough: a declared path list the live seam and the degraded stub both store without a row",
   "change": "LibKa0s/Schema.lua, still minor 2. The descriptor gains `writeThrough` (an array of path strings; the instance builds a set once). In S.Set, when no row is indexed for a path in that set, the value is stored raw via writeTarget/lib.Write (a copy), with no validate, normalize or onChange. `announce` is then called with a synthetic row `{ path = path, writeThrough = true }`, built once per path so no allocation per write. It logs like any write and returns true. A path with a row always takes the row. This covers a partial load (Schema present, Options absent). docs/api/Schema/version-2-docs.md: 'The degradation stub' prescribes that the runtime-completing stub takes the same `writeThrough` list and stores it the same way while refusing every other row-less path. This is the WS-02 route (a) for host verbs writing composed Master-controls rows (enabled, locked, test mode) on a library-absent load; composers stay hollow and host copies stay forbidden (#73). Adoption notes: AbsorbTracker (writeThrough {enabled, locked}; delete composeBlock and the host copies; pin full count, degraded count and the named composer gap), PartyFrameEnhanced (#14: writeThrough for the Slash.lua:117/:152 and Preview.lua:134 paths), WhatGroup (#22: owner ruling takes route (b), the library-absent line, no writeThrough). tests/test_schema.lua referenceStub gains writeThrough. CHANGELOG entry.",
   "tests": "Red first in tests/test_schema_batch.lua: with writeThrough {'enabled'} and no 'enabled' row, Set('enabled', false) stores false and announces with row.writeThrough == true (red under: NOT_FOUND refusal); an unlisted row-less path is still refused; a listed path WITH a row goes through its validate. Then the referenceStub: the same three cases against the stub, plus parity with a live instance on store order.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; grep -n 'writeThrough' docs/api/Schema/version-2-docs.md; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-02",
    "AbsorbTracker-A-03"
   ],
   "issue_refs": [
    "PartyFrameEnhanced#14",
    "WhatGroup#22"
   ],
   "depends_on": [
    "LK-22",
    "WS-02"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-24",
   "repo": "LibKa0s",
   "title": "Options: peel the font preload out of Options.lua into OptionsScroll.lua (behavior-neutral; makes room under the cap)",
   "change": "Move the whole '-- the font preload (minor 17)' block, LibKa0s/Options.lua:334-434 (preloadState, preloadFrame, preloadPath, subscribeLate, lib.__PreloadFonts), into LibKa0s/OptionsScroll.lua as lib-level code. Both callers (Options.lua:408 and :620) already look up `lib.__PreloadFonts` on `lib` at call time, so a partial copy missing OptionsScroll degrades to no preload; make both call sites nil-guarded if either is not. lib.__PatchLSM30Border stays. Options.lua drops from 1476 to about 1376, which is the room LK-25 needs. Bump Options `MINOR` 23 -> 24 and `SCROLL_MINOR` 3 -> 4. The Options key becomes 24.30.3.7.4 here, and LK-26/LK-27 move it further to 24.31.4.7.4 within this release. docs/api/Options/version-<key>-docs.md is written at this item and extended by LK-25..LK-28; the previous 23.30.3.7.3 doc is Superseded. README row. members json. CHANGELOG entry.",
   "tests": "Characterization: tests/test_options_fontpreload.lua (324 lines) must pass unchanged, with the same case count before and after. Add one red-first case: with lib.__PreloadFonts nil (OptionsScroll absent), opening a panel does not raise (red if a call site is unguarded).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; wc -l LibKa0s/Options.lua LibKa0s/OptionsScroll.lua (Options about 1376); $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "Open any Ka0s settings page with a font dropdown; the font previews render in their own faces as before.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-25",
   "repo": "LibKa0s",
   "title": "Options: CreateOptionsPanel parks in combat and replays itself; OpenOptionsPanel answers a boolean",
   "change": "LibKa0s/Options.lua registerMain/CreateOptionsPanel (:1322-1387). Under InCombatLockdown(), park the registration: store the request, and register PLAYER_REGEN_ENABLED on a library-private frame only while something is parked. On that event, replay the parked registration once, then unregister. This runs whatever the host's stand-down state, which also fixes ConsumableMaster R-03's lost category, and calling it again while parked is a no-op. OpenOptionsPanel (:1411-1430) answers true when opened, false when refused in combat (it keeps printing COMBAT_REFUSED), and nil when no category exists. There is no new instance member (no ReplayPending), so no host stub moves. Options already bumped to 24 by LK-24. Extend the Options version doc: park/replay semantics, the return values, and the private frame. Record that frame in the events-frames-taint-\u00a71 register row LK-30 writes (a third widget-owned private site). CHANGELOG entry.",
   "tests": "Red first in tests/test_options_combat.lua (578 lines): CreateOptionsPanel with InCombatLockdown true registers nothing and arms REGEN_ENABLED (red under: no park); firing PLAYER_REGEN_ENABLED registers exactly once and unregisters the event; a second CreateOptionsPanel while parked does not double-register; OpenOptionsPanel returns false in combat and true out of it.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; wc -l LibKa0s/Options.lua (<1500); $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "ConsumableMaster after adoption: /reload while in combat with a target dummy, leave combat, then /cm config opens the panel; repeat with the addon disabled (S: CM smoke \u00a76a).",
   "finding_ids": [
    "ConsumableMaster-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-24"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-26",
   "repo": "LibKa0s",
   "title": "OptionsWidgets minor 31: the slider and color throttles track their own armed flag, ignoring scheduleTimer's return",
   "change": "LibKa0s/OptionsWidgets.lua: slider live commit (:1326-1337) and color throttle (:1468-1482) each keep a library-local boolean, set before calling d.scheduleTimer and cleared inside the callback. Callers stop assigning or testing the handle, so a nil-returning C_Timer.After wrapper (KickCD, LootHistory, MultiMeters) gets the 50 ms throttle. LibKa0s/Options.lua:537-542 descriptor doc: the return value of scheduleTimer is unused. Bump `WIDGETS_MINOR` 30 -> 31 (Options already 24). Extend the Options version doc. CHANGELOG entry.",
   "tests": "Red first in a new tests/test_options_throttle.lua (test_options_widgets.lua is 4086 lines, a census row), declared in tests/run.lua: with a scheduleTimer returning nil, ten OnValueChanged calls inside one mock window schedule exactly one timer and commit once (red under: `if timer then return end`); the same for the color picker; a truthy-handle host is unchanged.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "KickCD on the new tag: drag a color picker and a slider for about 3 s with /kcd debug on; the console shows commits at about 20 per second, not one per frame.",
   "finding_ids": [
    "KICKCD-R-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-25"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-27",
   "repo": "LibKa0s",
   "title": "OptionsTabs minor 4: PageBanner, PageHeader and the chrome divider stop leaking a widget per render; the kit's AceGUI fake surveys Create/Release",
   "change": "LibKa0s/OptionsTabs.lua. PageHeader (:1117) acquires its frame from a per-ctx pool (ctx.__headerPool via LibKa0s-Pool-1.0, which OptionsTabs already floors on with NEEDS_POOL), released in releaseChrome via Pool.ReleaseAll. drawChromeDivider (:577) builds its texture once per ctx (ctx.__ruleTex) and hides it on release; SetParent(nil) is never called on a Region. PageBanner (:1036) keeps ctx.__bannerWidget and AceGUI:Release-s it at the top of the next releaseChrome. releaseLedger (:734-741) is updated accordingly. Kit: testkit/mock_record.lua gains a per-type AceGUI Create/Release survey (M.__aceguiLive(type)) with a two-line hook in testkit/mock_base.lua around :1364. Bump `TABS_MINOR` 3 -> 4 (Options key now 24.31.4.7.4). Extend the Options version doc and docs/api/testkit/version-26-docs.md. Sync tests/_kit. CHANGELOG entry.",
   "tests": "Red first in tests/test_options_tabs.lua (842 lines): after two full renders of a banner page, M.__aceguiLive('Dropdown') == 1 (red under: no Release); the header frame is the same object on the second render; the divider texture is created once per ctx. A kit self-test in tests/test_mock_record.lua covers the survey.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); diff -r testkit tests/_kit (empty); wc -l LibKa0s/OptionsTabs.lua testkit/mock_base.lua (<1500); no tests/perf.lua (perf: skip)",
   "smoke": "AuraMaster: switch between banner pages 50 times, then /dump collectgarbage('count') and a frame count (e.g. /fstack or an addon memory readout) stays flat compared with the first switch (S-002).",
   "finding_ids": [
    "LibKa0s-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-26",
    "LK-01"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-28",
   "repo": "LibKa0s",
   "title": "RenderTabbedSchema moves to OptionsTabs.lua and gains host tabs, a disabled notice and a chrome hook; PageBanner gains an action button",
   "change": "Move O.RenderTabbedSchema (LibKa0s/OptionsWidgets.lua:3820-3921 with its doc block) into OptionsTabs.lua's __AttachTabs. Change Options.lua:1471 to `lib.__AttachTabs(O, d)` so it reaches d.rowsForPage. OptionsWidgets keeps an untabbed fallback `O.RenderTabbedSchema` (RenderRows over the page, at most 10 lines) that the Tabs attach overrides, so a partial copy without OptionsTabs still renders. This is a first peel toward LibKa0s #16 (OptionsWidgets over cap). New optional fields on a 5th argument `opts` (no existing caller changes): `tabs` (an array of {key, label, render = function(ctx)} for bespoke non-row tabs, each placed before a named group via `before`); `disabledFor(cfg)` plus `disabledNotice` (a string or function), which renders the notice in place of rows when disabledFor answers true; `chrome(ctx)`, called after the strip and before rows. O.PageBanner gains an optional right-half `action = { text, tooltip, onClick }`, for the options-ui-\u00a714 picker+create band. Check against anti-pattern #55: each field serves AuraMaster's seven pages, and AbsorbTracker (settings/UnitPanel.lua:327), KickCD (settings/Panel_Render.lua:208), ConsumableMaster (settings/General.lua:402) and MultiMeters (settings/Columns.lua:321) hand-build the same strip shape, so no field is per-consumer. Minors were already bumped by LK-26/LK-27 in this release. Extend the Options version doc (fields Since 31/4). CHANGELOG entry.\n\nPLAN-REVIEW CORRECTION: disabledFor answering true draws the disabledNotice ABOVE the rows and renders the rows disabled (RenderRows opts.disabled plus ctx.__renderDisabled around bespoke render); it does not replace them. A `tabs` entry whose key equals a schema group takes that group's place in the strip and is handed the group's rows (render(ctx, rows)). Library tests pin both, so AM-17 adopts without changing its disabled-state characterization.",
   "tests": "Characterization first: every existing RenderTabbedSchema case in tests/test_options_widgets.lua and tests/test_options_tabs.lua passes unchanged after the move. Then red-first cases in tests/test_options_tabs.lua: a host tab renders through its callback and sits before the named group; disabledFor true shows the notice and no rows; chrome is called once per render after the strip; PageBanner action draws a button whose click calls onClick; with OptionsTabs absent (loader without the file) RenderTabbedSchema renders untabbed without raising.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; wc -l LibKa0s/OptionsTabs.lua (<1500) LibKa0s/OptionsWidgets.lua (smaller than 3922); $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "Any consumer on the new tag: every tabbed settings page draws its strip, switches tabs, and heals a stale tab exactly as before; after AuraMaster adopts, its seven pages and the container picker+create band match the pre-adoption screenshots.",
   "finding_ids": [
    "AuraMaster-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-27",
    "LK-25"
   ],
   "effort": "L",
   "milestone": "M1"
  },
  {
   "id": "LK-29",
   "repo": "LibKa0s",
   "title": "US-English sweep of the authored prose (the roughly 210 British spellings)",
   "change": "Re-run the counter at HEAD with the published BRITISH/ALLOWED lists (testkit/prose_lists.lua, now 92/30) over authored scope: tests/ (excluding tests/_kit), the highest-version docs/api documents only (never a Superseded one), docs/releasing.md, README.md, DEPENDENCIES.md, CLAUDE.md and tools/artwork/*.py. Fix prose, comments and test names (e.g. tests/test_perf_core.lua:383 'a cancelled run' -> 'a canceled run'). Two exclusions. First, `.cancelled`/`IsCancelled` member accesses that model AceTimer/C_Timer handles (tests/test_mock_ace.lua:237, 415, 443-444, 465) stay identifiers: extend register row 1 and tests/test_prose.lua's RATIFIED to cover that path. Second, tests/test_prose.lua's own quoted forbidden-word fixtures stay. Update register row 3's figures in CLAUDE.md to 0 prose hits. Leave the quoted 'was -> is' lines in released CHANGELOG entries.",
   "tests": "Red first: extend LibKa0s tests/test_prose.lua's British gate scope to tests/*.lua and the live docs set named above, with the two exclusions ratified. It must be red on today's tree, listing the hits. Then sweep until green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B luacheck .; $B lua5.1 tests/run.lua; $B lua5.1 tests/run.lua --list | diff --strip-trailing-cr - docs/test-cases.md; $B lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (nothing); no tests/perf.lua (perf: skip)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-28",
    "LK-07"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-30",
   "repo": "LibKa0s",
   "title": "Root CLAUDE.md: figures pointed at their generated sources, a register row for widget-owned private event frames, and a traceable row-1 citation",
   "change": "CLAUDE.md. (a) :268-269 'eighty files': replace the hand count with a pointer to RESULTS.md's generated lint sentence (luacheck's 'in N files'), and do the same at docs/releasing.md:19 and DEPENDENCIES.md:124 so no prose copy of the count remains. (b) Re-run `wc -l` for every band figure at :185-218 and correct tests/test_schema.lua (1101 -> measured) and any figure this milestone moved (Options.lua, OptionsTabs.lua, OptionsWidgets.lua, test_prose.lua, framework.lua). (c) :180 cites lib.__tabsMinor / lib.__tabsShellMinor by symbol, not ':39'. (d) Add a Documented deviations row keyed events-frames-taint-\u00a71 covering the widget-owned private frames: OptionsTabs.lua combat-lock (PLAYER_REGEN_DISABLED/ENABLED, :102-103/:139-140), Widgets.lua popup dismiss (GLOBAL_MOUSE_DOWN, :393) and the Options.lua park frame from LK-25. Why: the section addresses addon event traffic, these frames carry one widget's UI state, and the names are stable core events. It is not justified by 'the library cannot use AceEvent'. Decided <date>. Re-check trigger: registering an event newer than the current expansion, or a fourth private site. Update the 'N rows' sentence. (e) Row 1 evidence: 'Filed by the v1.31.0 review (commit 1f1790c, recorded in docs/api/testkit/version-17-docs.md)'.",
   "tests": "none \u2014 docs only (CLAUDE.md, releasing.md, DEPENDENCIES.md). The kit's register parser (framework.lua deviationRows) still reads the table: run the suite.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; B=~/.claude/wow-addon/bin/ka0s-bounded; $B lua5.1 tests/run.lua; ! grep -n -i 'eighty' CLAUDE.md docs/releasing.md DEPENDENCIES.md; grep -n 'events-frames-taint-\u00a71' CLAUDE.md; grep -n '1f1790c' CLAUDE.md; for f in $(grep -oE '`(LibKa0s|tests|testkit)/[A-Za-z_]+\\.lua`' CLAUDE.md | tr -d '`' | sort -u); do wc -l $f; done (each matches its CLAUDE.md figure)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-08",
    "LibKa0s-A-10",
    "LibKa0s-A-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-29"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-31",
   "repo": "LibKa0s",
   "title": "docs/releasing.md step 7: a manifest precondition, an ANALYSIS.md sub-step, and a release-notes perf-skip line",
   "change": "docs/releasing.md step 7 (:85-160). (1) Hard precondition: `grep -l '\"release\": \"<X.Y.Z>\"' docs/automated-tests/*/manifest.json` prints at least one path, and the stamp being tagged is one of them (the newest clean all-pass bundle). Re-runs for one release are normal, so it does not require exactly one. (2) A numbered sub-step: write `<stamp>/ANALYSIS.md` per the AUTOMATED_TESTS.md prompt, riding in the bundle commit before the tag, and add `test -f docs/automated-tests/<stamp>/ANALYSIS.md` to the precondition block. (3) A release-notes template line filled from the manifest: 'Release gate (docs/automated-tests/<stamp>/): lint pass ... tests pass N ... complexity pass, 0 above CCN 15; perf SKIPPED, not measured: no tests/perf.lua, so the gate covered three suites, not four.' Forward only: no released CHANGELOG entry and no frozen bundle is edited.",
   "tests": "none \u2014 docs only (release procedure). LK-33 exercises all three steps.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; grep -ci analysis docs/releasing.md (>= 1); grep -n 'grep -l .\"release\"' docs/releasing.md; grep -n 'three suites, not four' docs/releasing.md",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-01",
    "LibKa0s-A-05",
    "LibKa0s-A-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-30"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-32",
   "repo": "LibKa0s",
   "title": "Watch-list terminal states: file the owed issues and re-rule the band files",
   "change": "File LibKa0s GitHub issues with `gh issue create -R tusharsaxena/LibKa0s --label state:triaged --label severity:low`, spaced at least 5 s apart. Issues: tests/test_options.lua (peel the render/refresh block into test_options_render.lua); LibKa0s/Widgets.lua (per-widget files); tests/test_widgets.lua (split by widget family); tests/test_schema.lua (split by pipeline stage), and testkit/test_prose.lua only if still in the 1000-1500 band after LK-01/LK-07. Record in CLAUDE.md's census each issue number and a written re-rule for LibKa0s/Options.lua (now about 1416 after LK-24/LK-25: accepted, with the re-check trigger 'the next member added to Options.lua or 1450 lines') and for LibKa0s/OptionsTabs.lua (measured after LK-28: accepted, trigger 1450). The RESULTS.md Disposition cells are written in LK-33, after the release run regenerates the table, and point at these rows.",
   "tests": "none \u2014 docs and issue store only.",
   "verify": "gh issue list -R tusharsaxena/LibKa0s --label state:triaged --search 'peel OR split' --json number,title; cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; grep -n 'Options.lua\\|OptionsTabs.lua\\|test_options.lua\\|Widgets.lua\\|test_schema.lua' CLAUDE.md (each band row names an issue or a dated re-rule)",
   "smoke": "",
   "finding_ids": [
    "LibKa0s-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-31"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "LK-33",
   "repo": "LibKa0s",
   "title": "Cut v1.56.0: version block, release run with ANALYSIS.md and dispositions, and a LOCAL tag (pushed only on the owner's merge approval)",
   "change": "Proposed version v1.56.0 (minor): every change is additive, no NEEDS_* floor rises, and no major changes. Minors: Core 8, Item 2, Media 4, Bus 2, Lifecycle 2, Launcher 2, Slash 15, DebugLog 13, Perf 13 (key 13.5), Widgets 10 (key 10.2), Schema 2, Options key 24.31.4.7.4 (Options 24, OptionsWidgets 31, OptionsTabs 4, OptionsCompose 7, OptionsScroll 4); Env 1, Compat 1, Pool 3, WidgetsDragHandle 2 and PerfPanel 5 are unchanged; kit revision 26. Steps: (1) CHANGELOG.md: date the v1.56.0 block, set its 'Versions in this release' line to the list above, and include a consumer-obligations section. Surface-parity churn: Core stub + SafeRegisterEvent*, Schema instance stub + SetMany; behavioral kit flips: CreateFrame shown, AceDB raises, EventRegistry recorded, lone-CR, store-root prose, \u00a7 case names. (2) docs/api/README.md rows are all present. (3) README.md: test badge from docs/test-cases.md Totals, and the standards pointer rolled to v2.65.0 (`head -1 ../WowAddonStandards/standards/STANDARDS.md`). (4) docs/releasing.md: the provenance template -> v1.56.0; rewrite 'Where v1.56.0 stands' (steps 1-7 done, step 8 pending the owner's approval); step 9 re-sweep of the Consumers table (Core row names SafeRegisterEvent consumers once adopted, Schema row notes writeThrough/SetMany). (5) Commit, confirm `git status --porcelain` is empty, run `~/.claude/wow-addon/bin/ka0s-bounded tests/_kit/run-automated-tests.sh --release 1.56.0`, then check the manifest preconditions from LK-31. (6) Second commit: the bundle, RESULTS.md, `<stamp>/ANALYSIS.md` (it names once the six tags with no bundle (v1.28.0, v1.29.0, v1.36.0, v1.36.1, v1.54.1, v1.54.2), the release-run ANALYSIS gap, that 20260916-093057/ANALYSIS.md:60-62 misread it, and that 20260908-181447/ANALYSIS.md:92 over-counted by one), plus the authored RESULTS.md Disposition cells: the over-cap rows point at 'See the census row, CLAUDE.md \u00a7 Files over the 1500-line cap (#NN)', the band rows cite LK-32's issues or re-rules, and the six CCN-15 functions (Bus.lua lib.Catalog, Compat.lua lib.GetSpellCooldown, OptionsWidgets.lua idHelpIcon, testkit/mock_record.lua liveTimers and M.__fire, testkit/test_layout_cap.lua audit) are recorded as watch entries, in the functions table if the runner lists them or in ANALYSIS.md's watch paragraph otherwise. (7) Final collection dry-run with the full payload (libs/LibKa0s from LibKa0s/, tests/_kit from testkit/) into an archived copy of each of the 11 addons, as in LK-05. Attach the red-case list per addon to ANALYSIS.md's consumer section. (8) `git tag -a v1.56.0 -m 'LibKa0s v1.56.0'` LOCALLY on feat/2026-09-23-review-audit-remediation. Do NOT push the tag or the branch. The owner approves the merge, and only then are the branch merged and `git push origin v1.56.0` run. Consumers' test_vendor_sync resolves the tag from the local sibling checkout, so Milestone 2's re-vendors can proceed against the local tag.",
   "tests": "No new behavior. The release gate is the test: lint/tests/complexity pass, 0 functions above CCN 15, perf recorded as a skip with reason (1) (this repo holds no performance-\u00a712 row), and test_versioning, test_kitsync and the layout cap all green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s; S=$(ls -d docs/automated-tests/2*/ | tail -1)manifest.json; jq -r '.release' $S (1.56.0); jq -r '.git.dirty' $S (false); jq -r '.suites | to_entries[] | \"\\(.key) \\(.value.status)\"' $S; jq -r '.suites.complexity.warnings' $S (0); test -f $(dirname $S)/ANALYSIS.md; grep -l '\"release\": \"1.56.0\"' docs/automated-tests/*/manifest.json; git tag -l v1.56.0; git ls-remote --tags origin v1.56.0 (expect empty until owner approval); head -1 ../WowAddonStandards/standards/STANDARDS.md; grep -n 'v2.65.0' README.md",
   "smoke": "Before the owner approves the merge, install the payload into one consumer in-client (e.g. AuraMaster via its re-vendor branch) and run S-001..S-008 from the review's 03_SMOKE_TESTS.md.",
   "finding_ids": [
    "LibKa0s-R-15",
    "LibKa0s-A-02",
    "LibKa0s-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-32",
    "WS-08",
    "LK-05",
    "LK-06",
    "LK-08",
    "LK-09",
    "LK-11",
    "LK-12",
    "LK-13",
    "LK-14",
    "LK-15",
    "LK-16",
    "LK-18",
    "LK-19",
    "LK-20",
    "LK-21",
    "LK-23",
    "LK-03"
   ],
   "effort": "M",
   "milestone": "M1"
  },
  {
   "id": "LK-34",
   "repo": "LibKa0s",
   "milestone": "M1",
   "title": "Retire the provisional options-ui-\u00a79 row and re-cut the local v1.56.0 tag on the final commit",
   "change": "After WS-09 lands in the sibling WowAddonStandards (read it there): (1) delete the provisional `options-ui-\u00a79` row from CLAUDE.md ## Documented deviations and fix the row-count note; (2) in CHANGELOG.md v1.56.0 and docs/api/Options/version-24.30.3.7.4-docs.md, replace the 'the park is ahead of the standard' wording with a citation of options-ui-\u00a79 (v2.65.0), which now permits it; (3) docs/releasing.md 'Where v1.56.0 stands' and any forward-correction note: the ruling is made (park kept, standard amended) and the tag is re-cut on this commit. Frozen docs/automated-tests bundles are not edited. Commit, then RE-CUT THE LOCAL TAG: `git tag -d v1.56.0 && git tag -a v1.56.0 -m \"LibKa0s v1.56.0\" HEAD`. The tag was never pushed; do NOT push it. Confirm `git rev-parse v1.56.0^{commit}` equals HEAD.",
   "tests": "none (docs); the full suite must stay green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; no provisional options-ui-\u00a79 row in CLAUDE.md; git rev-parse v1.56.0^{commit} == HEAD",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "WS-09",
    "LK-33"
   ],
   "effort": "S"
  },
  {
   "id": "WA-01",
   "repo": "wow-addon",
   "title": "revendor-libka0s: take the delta base from the addon's provenance line, name bundles by tag, and write consolidated span bundles",
   "change": "PRE: wow-addon is on master; create branch feat/2026-09-23-review-audit-remediation. commands/revendor-libka0s.md. Step 3a: the base tag is the one the addon's CLAUDE.md provenance line names; cross-check it against `git log -1 --format=%H -- libs/LibKa0s` and that commit's CLAUDE.md. It is never the library's previous tag. State this and use `git -C ../LibKa0s log --oneline <base>..<new>` and `diff --stat <base> <new>` with that base. Step 3c: read the per-file minor table from the tag's LibKa0s.xml plus `grep -hoE 'local (MAJOR, )?[A-Z_]*MINOR *= *(\"[^\"]+\", *)?[0-9]+'`, which covers TABS_MINOR, COMPOSE_MINOR, DRAG_MINOR, SCROLL_MINOR, WIDGETS_MINOR and PANEL_MINOR; the stale constant table is replaced by that command. Bundle folder: `docs/revendor/<YYYY-MM-DD>-v<tag>/`. When the base-to-new span crosses tags the addon never recorded, write the WS-01 consolidated span bundle: folder `<date>-v<first>-v<last>/`, and 01_DELTA.md line 1 exactly `Delta: LibKa0s v<A> -> v<B> (span: ...)` listing every tag. A frozen earlier bundle whose base was misstated is never edited; the new bundle's 01_DELTA.md notes the correction once. Add a pre-flight check that lists, for every addon in standards/ADDONS.md, whether its newest docs/revendor bundle's line-1 base equals the provenance tag at the previous re-vendor commit. Report only; this finds AuraMaster's v1.54.2-for-v1.53.0 base and any sibling with the same error.\n\nPLAN-REVIEW CORRECTION: every addon had a kit-only re-vendor of v1.54.2 (AuraMaster 329e1a3, ConsumableMaster 1f86d4e, BankLedger 6b12edf, PrettyChat 8421342, WhatGroup 7f38ddd), so the frozen v1.55.0 bundles correctly read \"v1.54.2 -> v1.55.0\". Cross-check the base with `git log -1 -- libs/LibKa0s tests/_kit`, drop the claim that AuraMaster's base is misstated, and expect v1.54.2 (329e1a3) in the dry-run.",
   "tests": "none \u2014 Markdown command spec; no executable harness covers it. Dry-run check: follow the amended Step 3a by hand on AuraMaster (provenance v1.55.0 now; previous re-vendor f6f61d3 v1.53.0) and confirm it yields v1.53.0 as the base of the v1.55.0 span, where the old procedure gave v1.54.2.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/wow-addon; grep -n 'provenance' commands/revendor-libka0s.md | grep -i base; grep -n 'span:' commands/revendor-libka0s.md; grep -n 'TABS_MINOR\\|\\[A-Z_\\]\\*MINOR' commands/revendor-libka0s.md; grep -n 'docs/revendor/<YYYY-MM-DD>-v' commands/revendor-libka0s.md",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-21"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WA-02",
   "repo": "wow-addon",
   "title": "Ship bin/ka0s-bounded so the bare name resolves on the plugin's PATH entry",
   "change": "The plugin's PATH entry is <plugin>/bin, but the runner ships only at scripts/ka0s-bounded. Add `bin/ka0s-bounded`, a POSIX sh exec wrapper: `exec \"$(dirname \"$0\")/../scripts/ka0s-bounded\" \"$@\"`, mode +x, LF, and add it to .gitattributes if one lists scripts. Keep the hook's ~/.claude/wow-addon/bin symlink refresh as it is. agents/review.md:56-60 then works with the bare `ka0s-bounded`, and the absolute-path sentence at :67 stays valid. Update CLAUDE.md :23's layout line to name bin/ and DEPENDENCIES.md if it lists shipped files. scripts/test_bounded_runs.py: add a case that the matcher accepts `ka0s-bounded lua tests/run.lua` (bare) as bounded.",
   "tests": "Red first in scripts/test_bounded_runs.py: the bare-name prefix is classified as bounded (if the matcher already accepts it, the case pins it); plus a shell check that `bin/ka0s-bounded --help` (or a no-op invocation) exits 0.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/wow-addon; python3 scripts/test_bounded_runs.py; test -x bin/ka0s-bounded; sh -n bin/ka0s-bounded; tr -dc '\\r' < bin/ka0s-bounded | wc -c (0); after a plugin reload in a new session: command -v ka0s-bounded",
   "smoke": "In a fresh Claude Code session with the plugin installed, run `ka0s-bounded luacheck .` in any addon; it runs bounded rather than 'command not found'.",
   "finding_ids": [
    "MultiMeters-R-20",
    "ConsumableMaster-R-19"
   ],
   "issue_refs": [],
   "depends_on": [],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "WA-03",
   "repo": "wow-addon",
   "title": "Review agent's cross-addon baseline re-measured at v1.56.0 and derived rather than trusted; plugin 2.4.0",
   "change": "agents/review.md: replace the 2026-09-07 baseline (:357-368, plus the 9-addon counts at :193 and :365) with values measured at LibKa0s v1.56.0 against standards/ADDONS.md: eleven addons; fifteen majors (`grep -c 'major = \"LibKa0s-' ../LibKa0s/tests/majors.lua`); the per-file minors listed in LK-33; kit revision 26; `## Interface: 120100` uniform; and the slash-root count measured with the brief's own command, 22 on 2026-09-23. Each figure sits beside the command that produced it, with an instruction to re-derive at run time and treat a mismatch with the recorded line as a stale brief, not drift, when the library tag moved. Bump .claude-plugin/plugin.json 2.3.0 -> 2.4.0 (additive: bin/ and spec fixes). Update CLAUDE.md's 'Current version' paragraph and README.md if it states the version.",
   "tests": "none \u2014 Markdown agent brief and manifest.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/wow-addon; ! grep -n '120007' agents/review.md; grep -n '120100' agents/review.md; grep -n 'majors.lua' agents/review.md; jq -r .version .claude-plugin/plugin.json (2.4.0); grep -n '2.4.0' CLAUDE.md",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-19",
    "MultiMeters-R-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-02"
   ],
   "effort": "S",
   "milestone": "M1"
  },
  {
   "id": "RV-AT",
   "repo": "AbsorbTracker",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into AbsorbTracker",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by AT-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "AT-01",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-AM",
   "repo": "AuraMaster",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into AuraMaster",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by AM-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "AM-01",
    "AM-02",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-BL",
   "repo": "BankLedger",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into BankLedger",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by BL-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "BL-01",
    "BL-02",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-CM",
   "repo": "ConsumableMaster",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into ConsumableMaster",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by CM-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "CM-01",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-KC",
   "repo": "KickCD",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into KickCD",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by KC-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "KC-01",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-LH",
   "repo": "LootHistory",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into LootHistory",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by LH-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-MM",
   "repo": "MultiMeters",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into MultiMeters",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by MM-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-PM",
   "repo": "PanelMaster",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into PanelMaster",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by PM-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-PF",
   "repo": "PartyFrameEnhanced",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into PartyFrameEnhanced",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by PF-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-PC",
   "repo": "PrettyChat",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into PrettyChat",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by PC-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "RV-WG",
   "repo": "WhatGroup",
   "milestone": "M2",
   "title": "Re-vendor the whole LibKa0s v1.56.0 payload into WhatGroup",
   "change": "Copy ../LibKa0s/LibKa0s/ -> libs/LibKa0s/ and ../LibKa0s/testkit/ -> tests/_kit/ WHOLE from the local tag v1.56.0 (git -C ../LibKa0s archive or worktree at the tag; rm -rf then copy; keep the runner executable), and roll the CLAUDE.md provenance line to v1.56.0 in the same commit. No other edits. If the suite goes red because the new kit/library is stricter, record the failures in the commit body; this addon's M3 items clear them, and the addon is green again at the latest by WG-DOCS. docs/test-cases.md and the README test badge are regenerated by a later item, never by this commit.\n\nThe same commit writes the re-vendor bundle docs/revendor/2026-09-23-v1.56.0/ by hand, following the procedure in the LOCAL ../wow-addon/commands/revendor-libka0s.md as amended by WA-01 (the installed plugin comes from GitHub and does not have WA-01 until the owner merges wow-addon): 01_DELTA.md whose line 1 is exactly \"Delta: LibKa0s v1.55.0 -> v1.56.0\", per-file LibStub minors, both payload diffs, the kit-revision pairing (25 -> 26), which majors this addon consumes, and contract changes; plus 05_SUMMARY.md. Adoption decisions are NOT taken here: they are this addon's M3 items.",
   "tests": "none - payload copy; the suite run is the verification",
   "verify": "diff -r <tag checkout>/LibKa0s libs/LibKa0s and diff -r <tag checkout>/testkit tests/_kit both empty; /home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua; test -f docs/revendor/2026-09-23-v1.56.0/01_DELTA.md && head -1 docs/revendor/2026-09-23-v1.56.0/01_DELTA.md | grep -q \"v1.55.0 -> v1.56.0\"",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-33",
    "WG-01",
    "WA-01",
    "LK-34"
   ],
   "effort": "S"
  },
  {
   "id": "BL-01",
   "repo": "BankLedger",
   "title": "Retention prune rides AceTimer and latches only when it runs; wow_mock stops no-opping C_Timer.After (PRE-RV: lands before RV-BL)",
   "change": "LANDS BEFORE RV-BL (the LK-33 dry-run pre-fix named for BankLedger; green under both kit 25 and kit 26). (1) tests/wow_mock.lua:608-617: delete `M.C_Timer.After = function() end` and rewrite the comment block. The kit's recording After stands (testing-\u00a71, record rather than no-op). The prune stays out of suites because nothing fires it: every mocks.__fireTimers caller (tests/test_browser.lua:447-484, tests/test_ledger_settling.lua:164-181) resets mocks.__timers first. Also check core/ItemSetup.lua:67 (C_Timer.After(0.4, cb) in LoadItem), which now records too. If a suite asserts an empty queue after a LoadItem, reset mocks.__timers in that suite's setup. Never re-add the override. (2) core/BankLedger.lua:216-229 addon:OnEnterWorld becomes: `local st = NS.State; if st.cleanupDone or st.cleanupPending then return end; if not self.ScheduleTimer then return end; st.cleanupPending = self:ScheduleTimer(function() st.cleanupPending = nil; if NS.IsStoodDown and NS.IsStoodDown() then return end; st.cleanupDone = true; if NS.Database and NS.Database.PruneOld then NS.Database:PruneOld() end end, 5)`. The IsStoodDown check stays as a belt, and the latch is set only when the prune actually runs. Rewrite the :221-224 comment: the timer is an AceTimer handle that NS.StandDown's CancelAllTimers cancels, and a stand-down postpones the prune to the next PLAYER_ENTERING_WORLD instead of skipping it for the session. (3) core/State.lua: declare `State.cleanupPending = nil` beside cleanupDone, with a comment (session-only AceTimer handle). (4) NS.StandDown step 1 (core/BankLedger.lua:151-155): after ad:CancelAllTimers(), set `NS.State.cleanupPending = nil` so the next PEW after a stand-up re-arms. (5) docs/performance.md:47 row: the login deferral is an AceTimer that the stand-down cancels, and the once-per-session latch is set when the prune runs.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-BL (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Test first. In tests/test_disabled.lua, extend 'disabled: no timer, ticker or OnUpdate is left armed' (:184-203): reset NS.State.cleanupDone/cleanupPending, then call NS.addon:OnEnterWorld() before disable(). With step (1) alone the case goes red, because the C_Timer.After entry survives CancelAllTimers. Add a red-under comment naming C_Timer.After. Add a case 'disabled: a stand-down inside the prune window postpones the prune rather than canceling it': cleanupDone=false, OnEnterWorld, disable, assert #mocks.__timers()==0 and cleanupDone==false, enable, OnEnterWorld, mocks.__fireTimers(), assert a PruneOld spy ran exactly once and cleanupDone==true. It is red under the latch-before-timer shape. Add 'OnEnterWorld arms the retention prune once per session' (two PEWs, one queued timer). That is +2 cases. Regenerate docs/test-cases.md (lua5.1 tests/run.lua --list > docs/test-cases.md) and move the README test count if it shows one.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output); grep -n 'C_Timer.After = function' tests/wow_mock.lua (no output). No tests/perf.lua: perf is the performance-\u00a712 skip.",
   "smoke": "In client (C-07): /reload, then /bl disable within 5 s of the loading screen. /bl enable, zone or /reload: the next PLAYER_ENTERING_WORLD prunes. With /bl debug on, a history row older than retentionDays is gone after the second loading screen and was not removed while disabled.",
   "finding_ids": [
    "BankLedger-A-03",
    "BankLedger-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "BL-02",
   "repo": "BankLedger",
   "title": "Schema degradation stub gains SetMany (all-or-nothing batch), ahead of the LibKa0s-Schema-1.0 minor 2 parity check (PRE-RV: lands before RV-BL)",
   "change": "LANDS BEFORE RV-BL. Kit.assertSurfaceParity is one-directional (the stub must carry every live member), so adding the member early is harmless under v1.55.0. After RV-BL, the existing parity case at tests/test_surface_parity.lua:315-320 reddens without it. settings/Schema.lua stub:New (:502-561): extract a local `store(row, path, value)` (resolve root, row.set or stub.Write(copy), onChange) shared by R.Set and the new R.SetMany, so each stays at CCN 10 or below. Add `function R.SetMany(entries, opts)`, mirroring docs/api/Schema/version-2-docs.md at the v1.56.0 tag, log-silent like the rest of the stub. Phase 1 walks every entry and stores nothing. It answers `false, words('NOT_FOUND', path), nil, i` for an unknown path, `false, words('INVALID', path), why, i` when validate refuses, and `false, words('NO_ROOT', path), nil, i` with no store. Phase 2: when opts and opts.act, depth = depth + 1 around the writes so the sweep veto reads the same bracket. It stores each entry in order through store(), which runs each row's onChange. Then it calls `d.announceBatch(writes)` when the descriptor carries one, and otherwise d.announce per write. It returns true. Update the stub header's 'Trimmed' paragraph (:450-457): SetMany is carried, with no caller in this addon, for parity. Do not adopt SetMany in any host path.\n\nUpstream: this is the BankLedger consumer follow-up of LK-22 (LibKa0s Schema minor 2 adds SetMany to the instance surface and the docs/api/Schema/version-2-docs.md stub table); the stub SetMany mirrors LK-22's all-or-nothing semantics and log-silence exactly, and the post-RV-BL parity check is against the LK-22 surface.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-BL (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Test first, in tests/test_surface_parity.lua, Schema arm beside :315: 'LibKa0s-Schema degraded: the stub SetMany is all-or-nothing'. Load the degraded arm. Call SetMany({ {path='settings.visibility', value='never'}, {path='settings.visibility', value='bogus'} }) (or any row whose validate refuses; pick one by reading S.Schema's validate fields). It answers false with index 2, and the stored visibility is unchanged. A valid two-entry batch stores both and runs each onChange once (spy NS.Util.ApplyVisibility). It is red before the change because SetMany is nil. Add one case, regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01. After RV-BL: the 'LibKa0s-Schema degraded: the stub instance carries every member' case is green against v1.56.0.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-22",
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "AT-01",
   "repo": "AbsorbTracker",
   "title": "Schema degradation stub catches up to LibKa0s-Schema-1.0 minor 2: SetMany, normalize, instanceId forwarding and the writeThrough store",
   "change": "settings/Schema.lua, HostSchemaStub() (:82-219) only; the descriptor is not touched here (AT-08 adopts writeThrough). (1) Extract a stub-local `prepareWrite(row, path, value, id)` from S.Set (:159-183): resolve root/first/rid for a stored row, run row.validate (refuse `false, 'AbsorbTracker: invalid value for '..path, why`), then row.normalize(value, rid) when present (a nil answer refuses the same way with its why), then the `nowhere to store` refusal. It returns `true, prep` where prep = {row, parts, root, first, rid, value}. Add `commit(prep)`, which stores (row.set or stubLib.Write of a copy) and runs row.onChange. S.Set becomes FindRow -> prepareWrite -> commit -> d.announce, and S.Set, prepareWrite and commit each stay at CCN 10 or below. Today's S.Set is the addon's max at CCN 14. (2) writeThrough: in stubLib.New build once `local through = {}` from `d.writeThrough` (an array of path strings), mapping each path to a cached synthetic row `{ path = p, writeThrough = true }`. In S.Set, when FindRow finds no row and `through[path]` exists, resolve the root, stubLib.Write a copy with no validate, normalize or onChange, call d.announce(synthetic, path, value, rid) and return true. Every other row-less path is still refused with the existing line. (3) SetMany(entries, opts): phase 1 runs FindRow/writeThrough lookup plus prepareWrite for every entry and returns `false, err, why, index` before any store; phase 2 commits in order. Then call `d.announceBatch(writes, rid)` once if the descriptor has it, else d.announce per write. It is log-silent (no bracket line) and returns true. (4) S.Get(path, id) calls `row.get(id)`; S.ApplyDefault(row, id) forwards id to S.Set. (5) Update the stub's doc comment (:66-81) to cite LibKa0s docs/api/Schema/version-2-docs.md 'The degradation stub' and name SetMany/writeThrough. This is the parity churn the v1.56.0 payload brings. The item can land BEFORE RV-AT: Kit.assertSurfaceParity is one-directional (live must be a subset of stub), so an extra stub member is green on v1.55.0.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-AT (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Red-first in tests/test_schema.lua, new section '-- the degradation stub, Schema minor 2'. Build a stub instance from `loadDegraded().__schemaLib:New{ rows = {...}, resolveRoot = function() return store, 1 end, announce = spy, writeThrough = { 'enabled' } }` over a local store with two rows (one carrying validate). Cases: (a) SetMany with entry 2 invalid returns false, 'invalid', why, 2, and entry 1 is NOT stored (-- red under: committing inside phase 1); (b) a valid SetMany stores both, fires each onChange once, and announces twice, or once via announceBatch when given; (c) S.Set('enabled', false) with no row stores raw, fires no onChange, announces with row.writeThrough == true, and returns true (-- red under: dropping the through lookup); (d) S.Set('nope', 1) is still refused; (e) row.get receives the id passed to S.Get. The existing 'parity: the Schema stub's instance carries every member of a live instance' case in tests/test_surface_parity.lua must be green after RV-AT (red on a v1.56.0 payload without this item). Regenerate docs/test-cases.md with `lua5.1 tests/run.lua --list > docs/test-cases.md` and the README Tests badge in the same commit.",
   "verify": "luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output; settings/Schema.lua functions all at CCN 10 or below: lizard -l lua settings/Schema.lua | sort -k2 -n | tail -3); git ls-files '*.lua' ':!libs' ':!tests/_kit' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (empty); lua5.1 tests/run.lua --list > /tmp/l && diff <(tr -d '\\r' </tmp/l) <(tr -d '\\r' <docs/test-cases.md) (empty)",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "LK-22",
    "LK-23",
    "LK-33"
   ],
   "effort": "M",
   "milestone": "M2"
  },
  {
   "id": "WG-01",
   "repo": "WhatGroup",
   "title": "Unregister the EventRegistry 'SetItemRef' callback at stand-down, re-register at stand-up, and make the disabled survey see it",
   "change": "core/WhatGroup.lua:108-114: pull the registration into `local function registerLinkCallback()` (body: `if ADDON_LINK_TYPE then EventRegistry:RegisterCallback('SetItemRef', function(_, linkArg) onDetailsLinkClick(linkArg) end, WhatGroup) end`). Keep the file-load call where it is (the taint reason in the :45-53 comment still holds), and keep the `else hooksecurefunc('SetItemRef', onDetailsLinkClick)` fallback at file load. NS.StandDown (:329-349): add `if ADDON_LINK_TYPE and EventRegistry then EventRegistry:UnregisterCallback('SetItemRef', WhatGroup) end` next to the four UnregisterEvent calls. NS.StandUp (:365-372): call registerLinkCallback() after registerFeatureEvents(self). The registry keeps one callback per (event, owner), so a repeat call replaces the old one and cannot stack. Keep the `NS.IsStoodDown()` gate in onDetailsLinkClick because the hooksecurefunc fallback still needs it. Rewrite the :96-99 comment: the gate now serves only the fallback that cannot be unhooked; the EventRegistry route is really unregistered (slash-commands-\u00a77, anti-pattern #85). Also rewrite the :290-292 comment in registerFeatureEvents, which says the chat-link callback cannot be unhooked. tests/test_disabled.lua:76-82 regNames: after the kit survey and rawRegs, append `callback:<event>` for every owner in `mock.EventRegistry.__callbacks('SetItemRef')` (the local wow_mock fake at tests/wow_mock.lua:668-694). Update the :56-63 header comment. The expected sets in 'disabled 1' (:120) and 'disabled 9' (:409) gain `callback:SetItemRef`. docs/ARCHITECTURE.md:158-161 and :238-241: only the two hooksecurefunc rows survive the stand-down; move the :170 EventRegistry row into the set that is gone while disabled (WHATGROUP-A-02). docs/smoke-tests.md: add the in-client check listed under smoke. This item works with either kit revision, so it can land before RV-WG. The LK-33 dry-run names it the WhatGroup fix that must land before the re-vendor.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-WG (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Test first: extend regNames as described and run the suite. 'disabled 3: the registration set is EMPTY' (tests/test_disabled.lua:136) must go red, naming callback:SetItemRef. That is the survivor made visible (WHATGROUP-A-03). Add '-- red under: dropping the EventRegistry:UnregisterCallback in NS.StandDown' above it. Add a case in tests/test_lifecycle.lua near :227: 'lifecycle: disable drops the SetItemRef callback and enable restores exactly one' asserts `mock.EventRegistry.__callbacks('SetItemRef')[NS.addon]` is nil after /wg disable and non-nil after /wg enable, and that one owner key exists. Then make the core change so both go green. Regenerate docs/test-cases.md with `lua5.1 tests/run.lua --list > docs/test-cases.md` and the README Tests badge in the same commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && grep -n 'UnregisterCallback' core/WhatGroup.lua",
   "smoke": "In client: /wg disable, /wg enable, then Game Menu > Logout (cancel). Confirm no ADDON_ACTION_FORBIDDEN or BLOCKED. Then join via LFG (or /wg test notify) and click the chat 'view details' link: the popup opens. Disable again and click an old link: nothing happens. If Logout taint appears, revert and record a slash-commands-\u00a77 Documented deviations row instead (the finding's own fallback).",
   "finding_ids": [
    "WHATGROUP-A-01",
    "WHATGROUP-A-02",
    "WHATGROUP-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-33"
   ],
   "effort": "M",
   "milestone": "M2"
  },
  {
   "id": "CM-01",
   "repo": "ConsumableMaster",
   "title": "Pre-re-vendor fixes: the three British spellings the kit-26 prose gate will catch, plus any other red the v1.56.0 dry-run shows",
   "change": "SEQUENCING: this item lands BEFORE RV-CM, because RV-CM depends on it. It is the only ConsumableMaster item that goes ahead of the re-vendor, and it exists so the re-vendor commit is green (LK-07 / LK-33 consumer follow-up). (1) docs/perf-analysis/README.md:25 'analysed' -> 'analyzed' and :26 'neighbours' -> 'neighbors' (ConsumableMaster-A-07). (2) docs/settings-panel.md:80 'synchronisation problem' -> 'synchronization problem' (ConsumableMaster-A-06 text half; WS-07 and LK-07 add 'synchronis' to the published list). A scan made while planning found no other hit of the 92-entry list in docs/automated-tests/README.md, docs/automated-tests/RESULTS.md or docs/perf-analysis/README.md. (3) Dry-run the v1.56.0 payload into a throwaway copy without touching the repo: T=$(mktemp -d); git -C /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster archive HEAD | tar -x -C $T; rm -rf $T/libs/LibKa0s $T/tests/_kit; git -C /mnt/d/Profile/Users/Tushar/Documents/GIT/LibKa0s archive v1.56.0 LibKa0s testkit | tar -x -C $T/..x (or checkout the tag and cp -r LibKa0s/. $T/libs/LibKa0s/; cp -r testkit/. $T/tests/_kit/); (cd $T && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua). Classify every red. First, a docs/test-cases.md mismatch that comes only from kit case names now carrying \u00a7 (LK-08) belongs to RV-CM, which regenerates the list in its own commit; leave it alone here. Second, every other red is a real addon defect under the kit-26 flips: CreateFrame starts shown, the AceDB fake raises and strips defaults (this addon runs its own AceDB fake in tests/wow_mock.lua, so expect little there), EventRegistry records, frame RegisterEvent honors __badEvents, lone-CR detection, store-root prose. Fix each in this commit at its production cause. The fixture change is allowed only where the production path really performs the Hide() the old default hid. Never weaken an assertion. List each red and its fix in the commit body.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-CM (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "none for (1) and (2), which are docs only. The failing kit-26 test_prose case in the dry-run is the red-first evidence: record it in the commit body, then show it green on the dry-run copy. For (3), each dry-run red is the red test, and it goes green in the same commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && ! git grep -n -iE 'analysed|neighbours|synchronis' -- docs/perf-analysis/README.md docs/settings-panel.md; re-run the dry-run in (3) and confirm its only remaining red is the docs/test-cases.md case-name diff that RV-CM regenerates",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-07",
    "ConsumableMaster-A-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-07",
    "LK-07",
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "AM-01",
   "repo": "AuraMaster",
   "title": "Pre-re-vendor: remove the lone CR in tests/page_helpers.lua and clear every red the v1.56.0 kit dry-run raises",
   "change": "RUNS BEFORE RV-AM, which has to be green when it lands (LK-06 and LK-33 consumer follow-ups). (1) tests/page_helpers.lua:80 ends \\r\\r\\n. Delete the first CR only: `perl -pi -e 's/\\r\\r\\n/\\r\\n/' tests/page_helpers.lua`. Then run `git add --renormalize tests/page_helpers.lua`. Before this, git classified the file -text because of the lone CR, so the index holds a CRLF blob with 272 CRs. After it, `git ls-files --eol tests/page_helpers.lua` must print `i/lf w/crlf attr/text=auto eol=crlf`. Renormalize is correct here even though line-endings-\u00a77 says 'never renormalize', because that rule assumes a correct index and this index is wrong. (2) Dry-run the v1.56.0 payload without touching the repo: `T=$(mktemp -d); git archive HEAD | tar -x -C $T; mkdir $T/_lk; git -C ../LibKa0s archive v1.56.0 LibKa0s testkit | tar -x -C $T/_lk; rm -rf $T/libs/LibKa0s $T/tests/_kit; cp -r $T/_lk/LibKa0s $T/libs/LibKa0s; cp -r $T/_lk/testkit $T/tests/_kit; (cd $T && git init -q && git add -A && ka0s-bounded lua5.1 tests/run.lua)`. The git init is needed because test_eol, test_prose and test_layout_cap read the tracked set. test_vendor_sync is expected red there (the provenance line still says v1.55.0) and belongs to RV-AM; every other red is fixed in THIS commit, in addon code or addon tests only, never in libs/ or tests/_kit/. Known kit-26 flips to check: CreateFrame starts shown, so a case that relied on hidden-by-default gets the Hide() its production path performs and no assertion is weakened. AceDB fake CopyProfile/DeleteProfile raise on the active or a missing profile, and SetProfile strips defaults (tests/test_bus.lua:126-128, tests/test_bulklog.lua:129-146, tests/test_blizzardframes.lua:128, tests/test_containermanager.lua:480-520); a fixture that copies onto the active profile gets a real source. EventRegistry is recorded. Lone CRs. Store-root prose: an emulation of the 92-word list over docs/automated-tests/README.md, RESULTS.md and docs/perf-analysis/README.md found no hit on 2026-09-23. Case names now carry \u00a7. (3) tests/test_container.lua:299: reword the comment 'Kit frames start hidden: show it first, or the IsShown assertion below could never fail' so it does not depend on the kit's default, e.g. 'Shown first, whatever the kit's CreateFrame default, so the IsShown assertion below can fail.' List every dry-run red and its fix in the commit body.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-AM (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Characterization: the lone-CR one-liner `perl -ne '$c += () = /\\r(?!\\n)/g; END{print $c+0}' tests/page_helpers.lua` prints 1 before the fix and 0 after. The v1.56.0 kit's own test_eol lone-CR case (LK-06) is the red test: red in the dry-run before, green after. For each other dry-run red, the red case itself is the failing test, fixed without weakening an assertion.",
   "verify": "git ls-files --eol tests/page_helpers.lua -> i/lf w/crlf; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 on the v1.55.0 kit; the dry-run in (2) repeated -> only test_vendor_sync red; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . -> only the two pre-existing warnings (Cat.SyncUserCategories, renderCategories) that AM-13 removes; ka0s-bounded lua5.1 tests/perf.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-06",
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "AM-02",
   "repo": "AuraMaster",
   "title": "Record the unrecorded v1.35.0..v1.54.2 re-vendors in one consolidated span bundle",
   "change": "RUNS BEFORE RV-AM (the finding's own ordering: land this before the collection-wide re-vendor writes its own bundle). New folder docs/revendor/<YYYY-MM-DD>-v1.35.0-v1.54.2/, where the date is the commit day, in the WS-01/WA-01 span-bundle format. Two files. (a) 01_DELTA.md. Line 1 is exactly `Delta: LibKa0s v1.34.0 -> v1.54.2 (span: v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.43.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.48.0, v1.48.1, v1.49.0, v1.49.1, v1.50.0, v1.51.0, v1.52.0, v1.53.0, v1.54.2)`. That list is 22 tags. It is derived from the provenance history `git log --format=%h -p 0c6f99f..329e1a3 -- CLAUDE.md | grep '^+Bundles'`, not from the finding's list: the finding missed v1.43.0 (76224d7) and v1.45.0 (8923a1a), and v1.54.2 (329e1a3) is kit-only, so a libs/-only listing cannot see it. Then the output of `git -C ../LibKa0s log --oneline v1.34.0..v1.54.2` and `git -C ../LibKa0s diff --stat v1.34.0 v1.54.2 -- LibKa0s testkit`. Then a table `Tag | Repo commit | Date | What arrived` with one row per tag: 9ba3d01 plus the re-cuts 7b18a4b, 1dc5c61, abc3a5f, 2a3f63e (v1.35.0), f4e09a4, 9aa2483, bd4fa80, fec3784, e4cfa04, 5d2520c, 42bdff6, 76224d7, b66b21b, 8923a1a, 1acb2db, c0552a5, 6462e06, 29aef45, 7787b0f, d1d2f3b, 5c3a093, 482000f, f434521, f6f61d3, and 329e1a3 marked 'kit-only: library bytes identical to v1.53.0'. (b) 05_SUMMARY.md. The sweeps carried these tags with no bundle. It names the adoptions they brought: DragHandle v1.48.0 (6462e06), the entry suffix v1.49.0, and the Lifecycle/Slash minor-14 floor v1.42.0. It states that the frozen docs/revendor/2026-09-23-v1.55.0 bundle's base 'v1.54.2 -> v1.55.0' is CORRECT and stays unedited. It carries NO 'Correction' paragraph, even though the LK-33 consumer note asks for one (see notes). No 02-04 files: nothing is decided in retrospect.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-AM (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none: docs only. The bundle is a frozen record and no suite reads it. The docs gate test_docs 'every .md under docs/ appears in the documentation map' already covers it because docs/revendor/<date>/ is a named frozen store.",
   "verify": "head -1 docs/revendor/*-v1.35.0-v1.54.2/01_DELTA.md shows all 22 tags; for t in $(git log -p -- CLAUDE.md | grep -oE '^\\+Bundles .* v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -oE 'v[0-9.]+$' | sort -u); do grep -l \"$t\" docs/revendor/*/01_DELTA.md >/dev/null || echo MISSING $t; done -> prints nothing for every tag up to v1.55.0; the WS-01 AUDIT.md step-4 re-vendor listing (../WowAddonStandards/AUDIT.md, run with the `--since=\"<horizon> 00:00\"` form) -> no unrecorded tag; the kit-only v1.54.2 is checked by hand against 329e1a3; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "AM-01",
    "WS-01",
    "WA-01",
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "KC-01",
   "repo": "KickCD",
   "title": "Pre-re-vendor: clear KickCD's v1.56.0 dry-run reds (perf-analysis README spellings, LSM fake locale bits)",
   "change": "LANDS BEFORE RV-KC. This is the only KickCD item that precedes the re-vendor, so that the RV-KC commit is green (LK-07 consumer follow-up; LK-05/LK-33 blast-radius rule). (1) docs/perf-analysis/README.md:35 'analysed' -> 'analyzed' and :36 'neighbours' -> 'neighbors'. This is live, rewritten store prose, which kit 26's SCAN_BACK list now scans (LK-07). (2) tests/wow_mock.lua, LSM fake (~:650-669): add the real constants from libs/LibSharedMedia-3.0/LibSharedMedia-3.0.lua:31-35: LSM.LOCALE_BIT_koKR = 1, ruRU = 2, zhCN = 4, zhTW = 8, western = 128. Make LSM.Register record its 5th argument in LSM.__langmask[mediaType][key]. Without the constants, noopLib's __index answers a FUNCTION for LSM.LOCALE_BIT_western, and Media minor 4's `LOCALE_BIT_western + LOCALE_BIT_ruRU` (LK-13) raises at load. (3) Fix here any other KickCD red that the LK-33 dry-run log (<scratchpad>/dryrun-lk33-KickCD.txt) names, addon-side only and never in tests/_kit or libs/. List each one in the commit body.\n\nPLAN-REVIEW CORRECTION: this item lands BEFORE RV-KC (it is in M2, ahead of the re-vendor commit) so the re-vendor lands green; it must pass on both the v1.55.0 payload and the v1.56.0 dry-run.",
   "tests": "Red first, via the LK-05-style dry run against the local v1.56.0 tag, without touching the repo: T=$(mktemp -d); git -C /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD archive HEAD | tar -x -C $T; git -C ../LibKa0s archive v1.56.0 testkit LibKa0s | tar -x -C $T/lk; cp -r $T/lk/testkit/. $T/tests/_kit/; cp -r $T/lk/LibKa0s/. $T/libs/LibKa0s/; (cd $T && lua5.1 tests/run.lua). Expect test_prose red on analysed/neighbours, and test_mediasetup/load red if LK-13 guards by truthiness. Add a tests/test_mediasetup.lua case 'the LSM fake carries LibSharedMedia's real locale bits' (assertEqual(LSM.LOCALE_BIT_western, 128), assertEqual(LSM.LOCALE_BIT_ruRU, 2)). It is red on HEAD because the noopLib function makes type() 'function'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed, +1 case); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); re-run the dry run above: 0 failed; grep -n 'analysed\\|neighbours' docs/perf-analysis/README.md (empty); lua5.1 tests/run.lua --list > docs/test-cases.md and the README Tests badge moved in the same commit.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "LK-33"
   ],
   "effort": "S",
   "milestone": "M2"
  },
  {
   "id": "BL-00",
   "repo": "BankLedger",
   "milestone": "M3",
   "title": "Clear the 1 re-vendor red(s) RV-BL recorded, so every later M3 commit starts green",
   "change": "RV-BL copied LibKa0s v1.56.0 and recorded 1 failing test(s) in its commit body (`git log --grep=\"^RV-BL\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned BL-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL"
   ],
   "effort": "M"
  },
  {
   "id": "BL-03",
   "repo": "BankLedger",
   "title": "Stand-down drops the capture context (L:DropContext): openContext, lastSnapshot, _settleSince and the session end with the latch",
   "change": "modules/Ledger.lua: after L:CloseContext (:803-815), add `function L:DropContext()`. It reads `local context = NS.State.openContext`, calls self:CancelPendingReconcile(), sets `NS.State.openContext, NS.State.lastSnapshot, L._settleSince = nil, nil, nil`, and when context was set calls fireSessionChanged(false, context). There is NO Reconcile/flush, because movements made at the moment of disabling must not be captured. Docblock: this is the stand-down's context teardown, the only path besides CloseContext and disarmGuildBankIfGone that clears these fields, and it needs no event. core/BankLedger.lua NS.StandDown: a new step '3b. THE CAPTURE CONTEXT', after the RefreshUpvalues step (:164) and BEFORE the bus-target loop, because SessionWindow's __ev must still be subscribed to receive SessionChanged(false) and run SW:EndSession, which clears State.sessionActive (modules/SessionWindow.lua:137): `if NS.Ledger and NS.Ledger.DropContext then NS.Ledger:DropContext() end`. docs/ARCHITECTURE.md '### What stands down, and what survives' (~:336-370): add the capture context (open context, baseline, settle window, banking session) to what stands down.",
   "tests": "Test first, tests/test_disabled.lua, +2 cases with red-under comments ('red under: removing the DropContext call from NS.StandDown'). (A) 'disabled at the bank: a movement made while disabled is not recorded after re-enable': mocks.__fire('BANKFRAME_OPENED'), disable(), mutate the bag and bank contents with tests/ledger_support.lua's helpers, enable(), fire BAG_UPDATE_DELAYED and mocks.__fireTimers(), assert #NS.db.global.ledger is unchanged. (B) 'disabled at the bank: the stand-down disarms the context and ends the session': after disable() assert NS.State.openContext == nil, NS.State.lastSnapshot == nil, NS.Ledger._settleSince == nil, NS.State.sessionActive == false. After enable() away from a bank, firing PLAYER_MONEY queues no timer (#mocks.__timers() == 0). Both are red at HEAD (the verify_reason repro). Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output; NS.StandDown stays at or below CCN 15) && line-cap check as BL-01.",
   "smoke": "C-01 case A: at the bank, /bl disable, deposit a stack, /bl enable, move nothing, close the bank. No row appears for the deposit. Case B: at the bank, /bl disable, close the bank, walk away, /bl enable. /bl debug scan shows no open context, and the session window is closed.",
   "finding_ids": [
    "BankLedger-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-01",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-04",
   "repo": "BankLedger",
   "title": "Reset all settings re-runs the enable latch and announces LedgerChanged",
   "change": "settings/Slash.lua Sl:ResetEverything (:161-197), in this order after the wipe-and-merge block and the test-mode end. (1) Print the acknowledgment, as today. (2) Send SETTINGS_CHANGED 'reset', as today. (3) NEW: `if NS.Database and NS.Database.FireLedgerChanged then NS.Database:FireLedgerChanged() end`. Database stays the single sender of LEDGER_CHANGED (core/Database.lua:575, core/Constants.lua:282), so History, Insights (modules/Insights.lua:1000), SessionWindow:PruneMissing (:668) and the panel storage read-out (settings/Panel.lua:170) drop the deleted rows. (4) NEW: `if NS.ReevaluateEnabled then NS.ReevaluateEnabled() end`, as OnProfileReset does (core/Database.lua:19). The wipe restored settings.enabled = true behind the row's onChange, and ReevaluateEnabled fires the latch only on a real edge. It comes after the LedgerChanged send so a stood-down addon's modules rebuild from the empty store on Enable. (5) refreshAfterReset(). Extend the docblock (:133-160) with both calls and why. Behavior change: a full reset made while disabled re-enables the addon. Record it in the docs/ARCHITECTURE.md stand-down section and README Troubleshooting. The Version History line is the owner's at release.",
   "tests": "Test first, tests/test_panel.lua, beside the ResetEverything cases (:411-720), +2. 'ResetEverything while disabled stands the addon back up': S:Set('settings.enabled', false), then NS.Slash:ResetEverything(). Assert NS.IsDisabled() == false, NS.IsStoodDown() == false, the registration set is non-empty, and the enabled checkbox reads true. Red under removing ReevaluateEnabled. 'ResetEverything announces LedgerChanged exactly once': a spy on a NS.NewBusTarget() registered for NS.MSG.LEDGER_CHANGED counts 1, and an Insights/Browser refresh spy ran. Red at HEAD. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output; ResetEverything at or below CCN 15, so move a call into refreshAfterReset if needed) && line-cap check as BL-01.",
   "smoke": "C-02: with History and Insights open, Reset all settings -> Yes. Both views empty at once and the storage read-out reads 0. Repeat with the addon disabled first: after Yes the addon is enabled and a bank deposit records.",
   "finding_ids": [
    "BankLedger-R-02",
    "BankLedger-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-03",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-05",
   "repo": "BankLedger",
   "title": "One global reset (options-ui-\u00a712, Option A): Reset all settings, the page/footer Defaults and /bl resetall all raise the one confirm popup, which also closes the debug console; the expired register row goes",
   "change": "Owner decision S2-0 is taken as Option A (unify up), the only compliant end state. It is a player-visible data-loss change: Defaults and /bl resetall now discard recorded history after the confirm. Flag it in the commit body. (1) settings/Slash.lua, above the library branch: `function Sl:RequestResetAll()`, which calls StaticPopup_Show('KA0S_BANKLEDGER_RESETALL') when StaticPopup_Show is a function and otherwise calls Sl:ResetEverything(). It is the single entry point. The popup's OnAccept (:29-40) stays Sl:ResetEverything. (2) Sl:CliResetAll on the live arm (:513-523) and on the degraded arm (:330-350) both become `return Sl:RequestResetAll()`. Delete the degraded bracketed row walk and the Filters:ClearAll/ResetView wrap. The wipe already empties blacklist, whitelist and savedView, because they live in db.global. If a characterization test shows NS.Filters or Browser holds an in-memory copy, call their refresh after the wipe in ResetEverything; never keep a second implementation. Keep the member name CliResetAll for NS.Slash parity. (3) settings/Schema.lua S:ComposeMaster spec.onResetAll (:333-339) calls NS.Slash:RequestResetAll(). Delete the 'Note for whoever reads this next' paragraph (:325-332). The NS.COMMANDS resetall row (:669) wording becomes 'Reset everything to defaults, including recorded history (asks first)'. (4) settings/Panel.lua P:RestoreDefaults (:696-705) becomes `NS.Slash:RequestResetAll()`. ResetEverything's refreshAfterReset already recenters both windows and refreshes the panel. The Blizzard footer OnDefault forwards to defaultsOnClick, so it follows (settings/Panel.lua:50-52, :761). (5) Sl:ResetEverything sweeps the session-only rows by name, with no per-row [Set] line: after the test-mode end, `if NS.DebugLog and NS.DebugLog.IsShown and NS.DebugLog:IsShown() then NS.DebugLog:Hide() end` (state.debugConsole default false). The minimap table carve-out is unchanged. (6) Comments: settings/Schema.lua:663, settings/Panel.lua:118/:747/:759, settings/OptionsSetup.lua:103-106, settings/Slash.lua:60-70 (Filters clear note). (7) Docs: delete docs/ARCHITECTURE.md:505 (the options-ui-\u00a712 row; its own closing recipe is this item) and fix the register's row-count sentence. Rewrite the reset routes in docs/settings-panel.md and docs/slash-dispatch.md. Reword README.md Usage (:88-96) and Troubleshooting (:148): every reset control asks first and discards history, and /bl purge deletes history only.",
   "tests": "Test first. Characterize the contract, then fix. New cases in tests/test_panel.lua, or a new sibling tests/test_reset_routes.lua if test_panel.lua passes about 900 lines. 'every reset control raises the one confirm popup and changes nothing before accept': spy StaticPopup_Show, then drive P:RestoreDefaults(), NS.Slash:OnSlash('resetall'), the panel's defaults action (the setDefaultsAction callback / OnDefault forwarder) and S.masterTail's onResetAll. Each shows 'KA0S_BANKLEDGER_RESETALL' exactly once, and the ledger, the filter lists and the settings are unchanged. 'accepting the popup empties the ledger, both filter lists and savedView, ends test mode, closes the debug console and keeps db.global.minimap whole' (drive StaticPopupDialogs.KA0S_BANKLEDGER_RESETALL.OnAccept()). Degraded, via tests/degraded_env.lua: 'library-absent /bl resetall raises the same popup'. Rewrite every existing case that pinned the old non-destructive CliResetAll/RestoreDefaults (grep -n 'CliResetAll\\|RestoreDefaults' tests/*.lua) to the new contract, each with a red-under comment. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -n 'options-ui-\u00a712' docs/ARCHITECTURE.md (no register row); grep -rn 'cli:CliResetAll' settings (no output).",
   "smoke": "/bl resetall, the General page Defaults, the Blizzard footer Defaults and Master controls Reset all settings each show the one popup. No leaves everything. Yes empties History and the filter lists, closes the debug console, turns test mode off and leaves a hidden minimap button hidden.",
   "finding_ids": [
    "BankLedger-A-02",
    "BankLedger-A-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-04",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-06",
   "repo": "BankLedger",
   "title": "Adopt LibKa0s-Core SafeRegisterEvent: one NS.RegisterEventSafely (IsEventValid-gated, pcalled) for all eight registration sites, one event record",
   "change": "LK-11 consumer follow-up. (1) core/CoreSetup.lua, ABOVE the `if not lib` branch: `NS.EventRecord = { registered = {}, unavailable = {} }`, with a comment saying it is read by /bl debug scan and reset by NS.StandDown. The live arm: `function NS.RegisterEventSafely(target, event, handler) local ok = lib.SafeRegisterEvent(target, event, handler, NS.EventRecord.unavailable); if ok then local r = NS.EventRecord.registered; r[#r + 1] = event end; return ok end` (IsEventValid front gate plus pcall, from the library). The degraded arm carries the one-rung body the Core doc prescribes: `local ok = pcall(target.RegisterEvent, target, event, handler)`, appending to registered or unavailable. The namespace parity case (tests/test_surface_parity.lua:59-76) then holds. (2) Route every site through it: core/BankLedger.lua:87,91,92 (NS.StandUp's three RegisterEvent calls, so a raise can no longer abort Ledger/Browser/SessionWindow:Enable); modules/Browser.lua:1218 and modules/SessionWindow.lua:673 (B.__ev/SW.__ev PLAYER_LOGOUT on the bus target, keeping the `.RegisterEvent` guard); modules/Ledger.lua:858-881. Delete L:RegisterEventSafely (:840-854), L.registeredEvents/L.unavailableEvents (:837-838) and the reset at :862. (3) modules/Ledger.lua Diagnose (:392-394) reads NS.EventRecord. (4) NS.StandDown step 2 (core/BankLedger.lua:162) resets `NS.EventRecord.registered, NS.EventRecord.unavailable = {}, {}`. (5) docs/ARCHITECTURE.md ## Event Subscriptions (:371-393): name NS.RegisterEventSafely over LibKa0s-Core-1.0's SafeRegisterEvent (events-frames-taint-\u00a71, standard v2.65.0), the IsEventValid front gate, and the record surfaced by /bl debug scan. Replace the prose argument that the names cannot retire.",
   "tests": "Test first. tests/test_ledger_settling.lua:38,99-139: re-point every L.registeredEvents/L.unavailableEvents read to NS.EventRecord, and rename 'Ledger:RegisterEventSafely reports whether the binding took' to 'NS.RegisterEventSafely reports whether the binding took'. New red cases in tests/test_lifecycle.lua (or test_disabled.lua, whichever sits further under the cap). 'a rejected event name in the stand-up does not stop Ledger:Enable': set mocks.__badEvents = { PLAYER_REGEN_DISABLED = true } (kit 26 raises on the AceEvent path), disable(), enable(), assert BANKFRAME_OPENED is registered and PLAYER_REGEN_DISABLED is in NS.EventRecord.unavailable. It is red at HEAD, where the raise aborts NS.StandUp. 'C_EventUtils.IsEventValid rejects a name before any RegisterEvent call': stub IsEventValid false for one name, spy RegisterEvent, assert no call. 'the stand-down clears the event record'. Degraded arm, tests/test_surface_parity.lua: 'Core degraded: NS.RegisterEventSafely isolates a raising RegisterEvent'. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -rnE ':RegisterEvent\\(' core modules settings | grep -v RegisterEventSafely (only the helper bodies in core/CoreSetup.lua).",
   "smoke": "/reload, then /bl debug scan: 'events registered' lists the bank/bag/money/guild set plus PLAYER_ENTERING_WORLD and the combat pair, and 'events UNAVAILABLE (0): none'. /bl disable then /bl debug scan: the record is empty.",
   "finding_ids": [
    "BankLedger-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-03",
    "LK-11",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-07",
   "repo": "BankLedger",
   "title": "Drop the dead GUILDBANKFRAME_OPENED/_CLOSED registrations (adopt PLAYER_INTERACTION_MANAGER only if /etrace proves it fires)",
   "change": "Step 1, in client, before coding: /etrace with the filter GUILDBANK and PLAYER_INTERACTION while opening and closing a guild vault on the current client. Record which events fire and their first argument in the commit body. Step 2, always: modules/Ledger.lua:817-826 removes GUILDBANKFRAME_OPENED from OPEN_EVENTS and GUILDBANKFRAME_CLOSED from CLOSE_EVENTS. Rewrite the :694-712 comment: the frame's OnShow/OnHide hooks are the guild bank's only open/close, and the event names were removed because they never fire. Step 3, only if /etrace shows PLAYER_INTERACTION_MANAGER_FRAME_SHOW/_HIDE with Enum.PlayerInteractionType.GuildBanker: register both through NS.RegisterEventSafely (BL-06) in L:Enable. The handlers check `type == Enum.PlayerInteractionType.GuildBanker` and drive L:OpenContext(C.Context.GUILD_BANK) (never stealing an open context) and, when openContext == GUILD_BANK, L:CloseContext(). The HookScript pair stays as the backstop. docs/ARCHITECTURE.md Event Subscriptions: correct the registration count (14 -> 12, or 14 with the PIM pair). docs/performance.md: the sweep row.",
   "tests": "Test first. tests/test_ledger_settling.lua:120-132 (the case that injects bad names and expects GUILDBANKFRAME_OPENED in the unavailable list) is re-pinned to a name that still ships. New case 'Ledger:Enable registers no GUILDBANKFRAME_* event': NS.EventRecord.registered has no GUILDBANKFRAME_OPENED/_CLOSED. It is red before step 2. With step 3 also: 'a GuildBanker PIM show opens the guild context and a hide closes it; another interaction type is ignored' (drive mocks.__fire('PLAYER_INTERACTION_MANAGER_FRAME_SHOW', type)). Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -n 'GUILDBANKFRAME_' modules/Ledger.lua (comment mentions only).",
   "smoke": "IN CLIENT ONLY: /etrace capture (step 1). After the change, open a guild vault: the session window opens, and depositing a stack records a guild-bank row. Close the vault: the session ends. /bl debug scan lists no GUILDBANKFRAME_* name.",
   "finding_ids": [
    "BankLedger-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-06",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-08",
   "repo": "BankLedger",
   "title": "Launcher minor 2: hand isEnabled/disabledLine to the descriptor and delete the host's left-click gate",
   "change": "LK-16 consumer follow-up. core/LauncherSetup.lua descriptor (:143-158): add `isEnabled = function() return not (NS.IsDisabled and NS.IsDisabled()) end` and `disabledLine = function() return NS.Slash and NS.Slash.DisabledLine and NS.Slash:DisabledLine() end`. onClick becomes `function() if NS.Browser and NS.Browser.Toggle then NS.Browser:Toggle() end end`. Delete the `NS.Slash:RefuseIfDisabled()` hand gate at :156 and rewrite the comment block (:145-155): the library's rung (a)/(b) gate refuses a disabled left click with the collection's one line, and the right click is unchanged. settings/Slash.lua Sl:RefuseIfDisabled (:263-267): grep for other callers (grep -rn RefuseIfDisabled core modules settings tests). If the launcher was the only one, delete it and drop it from the comment at :258-262. It is a host method defined above the branch, so both arms lose it together and parity holds. The library now prints NO_BROKER/NO_ICON/NO_MINIMAP once and untagged, so the host printer's [BL] is the only tag.",
   "tests": "Test first. tests/test_disabled.lua:448-470 (the rung-(a) left-click case): add an assertion that a Browser:Toggle spy was NOT called while disabled and was called once after enable, so the case now exercises the library gate. Add 'launcher: the left click carries no host gate' (a source read of core/LauncherSetup.lua: no RefuseIfDisabled). It is red before the deletion, and it pins the anti-regression. tests/test_launcher.lua:102-125: add 'the missing-broker notice prints once across two Register calls, without a [LibKa0s] tag'. Re-pin any case that matched '[LibKa0s]' (grep -n 'LibKa0s\\]' tests/*.lua). Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -n RefuseIfDisabled core/LauncherSetup.lua (no output).",
   "smoke": "/bl disable, then left-click the minimap button: one chat line '<brand> is disabled \u2014 enable it with /bl enable', and the ledger does not open. Right-click opens settings. /bl enable, then left-click toggles the ledger.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-09",
   "repo": "BankLedger",
   "title": "Slash minor 15: /bl set prints the seam's refusal; re-pin the echo cases",
   "change": "LK-17 consumer follow-up. settings/Slash.lua:439 already hands NS.SchemaRuntime.Set (the three-value instance member) to the descriptor, so the library now prints INVALID for the path, then the reason and the validate's why, on indented lines, instead of echoing the unchanged value. No production change beyond comments. Update the descriptor comment at :431-440 to say the library prints the refusal from `set`'s `false, err, why`. Update the S:Set docblock (settings/Schema.lua:596-604) to say that only NS.Schema:Set trims to two values, and the CLI reads the instance's three. If any row has no default (grep S.Schema and the composed rows for a nil default), /bl reset on it now prints NO_DEFAULT ('%s has no default to restore').",
   "tests": "Test first. grep -n 'CliSet' tests/test_slash.lua tests/test_panel*.lua tests/test_schema*.lua for cases that pass a value the row's validate refuses and assert the old echo. Re-pin each one to the three-line refusal, with the stored value unchanged. Add 'slash: /bl set with a refused value prints INVALID, the reason and the why, and stores nothing' (e.g. /bl set settings.visibility bogus, or a slider out of range). It is red before RV-BL's Slash 15 and green after. If a no-default row exists, add '/bl reset <path> on a row with no default prints NO_DEFAULT'. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01.",
   "smoke": "/bl set settings.visibility bogus: an 'invalid value' line for the path plus the reason, and /bl get settings.visibility is unchanged.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-10",
   "repo": "BankLedger",
   "title": "Library-absent /bl enable|disable write settings.enabled through the Schema seam's writeThrough (WS-02 route a); the stub DisabledLine format is pinned to the library's bytes",
   "change": "LK-23/LK-18 consumer follow-up, per the WS-02 ruling. (1) settings/Schema.lua: `S.WRITE_THROUGH = { 'settings.enabled' }`, with a comment. It is WS-02 route (a) for the one composed row a host verb writes. settings.locked has no verb. Test mode and the debug console are session state switched through LT:SetTestMode and NS.DebugLog, never the seam. Pass `writeThrough = S.WRITE_THROUGH` in the SchemaLib:New descriptor (:566-579). The live Schema minor 2 honors it on a partial load (Schema present, Options absent). (2) The stub:New (:502-561) builds a set from d.writeThrough once, plus one synthetic row per path `{ path = p, writeThrough = true }` built once. In R.Set (and SetMany's phase 1), when FindRow answers nil and the path is in the set: resolve the root (NO_ROOT if missing), stub.Write(root, path, copy(value)), d.announce(synthetic, path, value), return true. Every other row-less path still answers NOT_FOUND. (3) settings/Slash.lua Sl:CliEnabled (:213-215), shared by both arms: after Sl:CliSet(...), call `if NS.ReevaluateEnabled then NS.ReevaluateEnabled() end`. A write-through store runs no onChange, and ReevaluateEnabled fires only on a real edge, so the full load is unaffected. The degraded branch (:291-) overrides Sl:CliEnabled, because its CliSet prints the CLI-unavailable line: `local ok, err = NS.Schema:Set(ENABLED_PATH, on)`. On a refusal it prints err, never raises and never acknowledges. Otherwise it calls NS.ReevaluateEnabled() and prints `('%s = %s'):format(ENABLED_PATH, tostring(on))`. Comment why the degraded arm carries its own body. (4) Degraded Sl:DisabledLine (:357-360): hoist the literal to `local DISABLED_LINE_FORMAT = '%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r'`, published as `Sl.__DISABLED_LINE_FORMAT` (the __ prefix keeps it out of public parity), and build the line from it. (5) Docs: docs/schema.md 'degradation stub' and docs/slash-dispatch.md degraded section say that enable/disable keep working without the library by write-through. No Documented deviations row is needed, because route (a) is taken.",
   "tests": "Test first, in tests/test_surface_parity.lua (328 lines). 'degraded: /bl disable writes settings.enabled through and stands the addon down, without a Lua error': loadDegraded; pcall(degraded.Slash.OnSlash, degraded.Slash, 'disable') is ok; stored settings.enabled == false; degraded.IsStoodDown() == true; exactly one chat line 'settings.enabled = false'. 'degraded: /bl enable reverses it'. Both are red today (the CLI-unavailable line and an unchanged store). 'Schema stub: a writeThrough path with no row is stored raw and announced; a path outside the list still answers unknown path'. 'Slash stub DisabledLine format is the library's bytes': T.assertLibraryConstant(degraded.Slash.__DISABLED_LINE_FORMAT, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT'). In tests/test_schema_runtime.lua: 'the live seam takes the row, not writeThrough, when settings.enabled has one' (the onChange spy ran). Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01.",
   "smoke": "Temporarily rename libs/LibKa0s in a test install and /reload: /bl disable prints 'settings.enabled = false' with no Lua error and capture stops. /bl enable restores it. Restore the folder.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-02",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-11",
   "repo": "BankLedger",
   "title": "Migration stamp per standard v2.65.0: defaults declare schemaVersion = 0; the runner walks steps and stamps after each success",
   "change": "WS-03 consumer follow-up. (1) defaults/Global.lua:8-22: replace the 'DELIBERATELY NOT DECLARED' comment with `schemaVersion = 0,` and a short comment. 0 is never a real version, so AceDB's logout strip never removes a real stamp, and AceDB's backfill of 0 onto a legacy unstamped store reads as 'unstamped' rather than masking it (savedvariables-\u00a71, v2.65.0). (2) core/Database.lua:30-61 NS:RunMigrations: add a module-level `NS.MIGRATIONS = { [2] = function(g) <strip vendorPrice, return rows touched> end }`. The runner reads `local from = tonumber(g.schemaVersion) or 0`, returns when from >= NS.SCHEMA_VERSION, and treats 0 (and nil) as unstamped: `local v = math.max(from, 1)`. That covers both a legacy v1 store and a fresh install, because a step over an empty ledger is a no-op. For each target = v+1..NS.SCHEMA_VERSION it runs `rows = rows + NS.MIGRATIONS[target](g)` and then sets `g.schemaVersion = target`. A raising step propagates and leaves the stamp at the last completed version. Keep the [Migrate] debug line. There is no profile scope: this addon stores only db.global (the savedvariables-\u00a72 register row), so the per-profile rule has nothing to walk. Say so in the docblock. (3) settings/Slash.lua Sl:ResetEverything: after the defaults merge, call `NS:RunMigrations()` so a wiped store is re-stamped rather than reading v0 until the next login. (4) docs/ARCHITECTURE.md / docs/schema.md: the SavedVariables stamp paragraph.",
   "tests": "Test first, tests/test_database.lua. Rewrite 'Database: schemaVersion is NOT a shipped AceDB default' (:505-513) to 'Database: defaults declare schemaVersion = 0 (savedvariables-\u00a71)', asserting NS.defaults.global.schemaVersion == 0. It is red today. Add 'RunMigrations walks an AceDB-backfilled 0 with vendorPrice rows to v2 and strips them'. Add 'RunMigrations leaves the stamp at the last completed step when a step raises' (swap NS.MIGRATIONS[2] for a raiser, pcall, assert the stamp is unchanged, restore). Add 'ResetEverything leaves the store stamped at NS.SCHEMA_VERSION'. Keep :375 (nil treated as v1) and :479 (a future version is left alone). Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01.",
   "smoke": "On an existing account, /reload, then /logout and inspect WTF/Account/<acct>/SavedVariables/BankLedger.lua: `schemaVersion = 2` is present and survives a second login. /bl debug on shows the [Init] summary with 'schema v2'.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-05",
    "WS-03",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-12",
   "repo": "BankLedger",
   "title": "Minimap row's CLI path reads in its own sense: minimap.hide -> minimap.shown (storage stays LibDBIcon's minimap.hide)",
   "change": "WS-06 consumer follow-up (launcher-\u00a73, v2.65.0). settings/Schema.lua:143-147 changes to `S.MINIMAP_PATH = 'minimap.shown'`, with the comment rewritten: the row path is the CLI name, the stored key is still db.global.minimap.hide, and a stored `shown` key stays forbidden (anti-pattern #81). The row's get/set in S.MASTER_DECOR (:266-280) already invert onto minimap.hide, and S.RESET_EXEMPT keys by the constant, so both move with it. S:Register's defaultsRoot (:620-627): when row.path == S.MINIMAP_PATH, answer `{ minimap = { shown = not ((NS.defaults.global.minimap or {}).hide) } }, 1`, so Validate resolves the CLI path against the declared hide default without a stored `shown`. Confirm S:Default(S.MINIMAP_PATH) reads true. If the composer carries a hide-sense default, stamp `default = true` in the decoration. Respell every '/bl reset minimap.hide' and path mention: settings/Schema.lua:169,402-403,428,616; settings/Slash.lua; docs/settings-panel.md:153,224-249; docs/schema.md:108; docs/ARCHITECTURE.md:200,229,244,257; README if it names the path. No SavedVariables change and no migration.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Test first. Re-pin every test string (grep -rn 'minimap.hide' tests/*.lua) that uses the CLI path. Keep assertions on db.global.minimap.hide as the storage. New cases in tests/test_launcher.lua: '/bl get minimap.shown answers true while db.global.minimap.hide is false'; '/bl set minimap.shown false stores minimap.hide = true, hides the button and writes no shown key'; 'a targeted /bl reset minimap.shown restores shown'; 'S:Register reports 0 failures with the renamed path'. They are red before the rename. Rename the test-cases row at docs/test-cases.md:814's case and regenerate.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -rn 'minimap\\.hide' settings docs/*.md README.md | grep -v 'db.global.minimap.hide\\|stored' (review each hit).",
   "smoke": "/bl get minimap.shown -> true. /bl set minimap.shown false hides the button. /reload keeps it hidden. Right-click the LibDBIcon menu to show it again, and /bl get minimap.shown -> true.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-10",
    "WS-06",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-13",
   "repo": "BankLedger",
   "title": "ApplyVisibility and ApplyMasterChrome iterate module-level name lists (no table per combat edge)",
   "change": "core/Util.lua: add module-level `local VISIBILITY_OWNERS = { 'Browser', 'SessionWindow' }` and `local CHROME_OWNERS = { 'Browser', 'SessionWindow', 'Export' }`. Util.ApplyMasterChrome (:239-244) iterates CHROME_OWNERS with ipairs and resolves `NS[key]` at call time. Util.ApplyVisibility (:274-288) iterates VISIBILITY_OWNERS with ipairs, `local owner = NS[key]`, and keeps hidden[key] string keys. The order becomes deterministic (Browser then SessionWindow). This is a pure refactor, and docs/performance.md:46's 'No allocation' becomes true (fix the same row's 'OnEnable' -> NS.StandUp in BL-20).",
   "tests": "Characterization first, green before and after, in tests/test_util.lua. 'ApplyVisibility hides only open windows and re-shows exactly those on the way back' (inCombat mode, toggle InCombatLockdown). 'ApplyVisibility and ApplyMasterChrome build no table literal per call': a source read of core/Util.lua asserting neither body contains `pairs({` or `ipairs({`. It is red today, and pins the allocation claim performance.md makes. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && line-cap check as BL-01; grep -n 'pairs({' core/Util.lua (no output).",
   "smoke": "C-04: set General visibility to 'Only out of combat', open the ledger and the session window, pull a target dummy: both hide, and both return when combat ends. A window you closed yourself stays closed.",
   "finding_ids": [
    "BankLedger-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-14",
   "repo": "BankLedger",
   "title": "Pin the live and library-absent schema row counts and the composed delta",
   "change": "Tests only. First re-measure after BL-10/BL-12 with the real harness: `lua5.1 -e` through tests/run.lua's bootstrap and tests/degraded_env.lua's loadDegraded, printing #S.Schema and #S:PageRows() on both arms. The verified values at HEAD are 16/8 (Schema) and 17/9 (PageRows). writeThrough adds no row, so expect the same.",
   "tests": "tests/test_schema.lua, beside the tab-count case (:325-340): 'schema: the live and library-absent row counts, and the composed delta'. It asserts #S.Schema == 16 live and 8 degraded, #S:PageRows() == 17 live and 9 degraded, and live minus degraded == 8 on both measures. The failure message attributes the delta to S:ComposeMaster / O.MasterControls (the hollow composer, options-ui-\u00a71). Falsification comment: 'red under a composer that stops being hollow, or a host row added or removed'. Count-pinning case, +1. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && line-cap check as BL-01 (test_schema.lua stays under 1000).",
   "smoke": "",
   "finding_ids": [
    "BankLedger-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-10",
    "BL-12",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-15",
   "repo": "BankLedger",
   "title": "Tests use NS.MSG constants at every bus call site; test_bus.lua stays the one wire-name oracle",
   "change": "Replace every 'Ka0s_...' literal with NS.MSG.ENTRY_ADDED / LEDGER_CHANGED / SETTINGS_CHANGED / SESSION_CHANGED. Register/Send sites: tests/test_database.lua:40,53,54; test_lifecycle.lua:64; test_mock.lua:312,313,315,320; test_panel.lua:614; test_schema.lua:36; test_sessionwindow.lua:143,145. Paired UnregisterMessage: test_database.lua:42,56,57; test_panel.lua:618; test_schema.lua:40. Message compare at test_database.lua:276. __msgRegistry keys at test_lifecycle.lua:86,92. test_mock's 'Ka0s_Scratch_Ping' becomes one local constant, spelled once. test_surface_parity.lua:276 compares the degraded MSG against NS.MSG from the live load. tests/test_bus.lua:19-22 stays the single literal oracle.",
   "tests": "Behavior-neutral: the full suite stays green with the same case count. The closing check is the grep in verify.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md; grep -n '\"Ka0s_' tests/*.lua (only test_bus.lua:19-22 and the one scratch constant in test_mock.lua).",
   "smoke": "",
   "finding_ids": [
    "BankLedger-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-04",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-16",
   "repo": "BankLedger",
   "title": "Annotate the two load-bearing TOC positions (Insights after InsightsWidgets, Slash after Schema) and pin them",
   "change": "BankLedger.toc: above modules\\Insights.lua (:85) add '# LOAD-BEARING: modules/Insights.lua takes NS.InsightsWidgets as a file-scope upvalue; modules\\InsightsWidgets.lua above creates it. Swapped, W is nil and every chart call raises.' Above settings\\Slash.lua (:90) add '# LOAD-BEARING: hands NS.SchemaRuntime's members to LibKa0s-Slash at file load; settings\\Schema.lua above builds it.' (toc-file-\u00a75).",
   "tests": "tests/test_libka0s.lua (987 lines; if it would pass 1000, use test_harness.lua): 'TOC: InsightsWidgets loads before Insights, and Schema before Slash, each annotated LOAD-BEARING'. It derives the order and the preceding comment line from BankLedger.toc and is red today on the missing annotation. +1. Regenerate docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && line-cap check as BL-01; grep -n 'LOAD-BEARING' BankLedger.toc (two new lines).",
   "smoke": "/reload with no Lua error. Insights charts render and /bl list prints.",
   "finding_ids": [
    "BankLedger-A-06",
    "BankLedger-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-17",
   "repo": "BankLedger",
   "title": "Printer call sites pass argument lists instead of pre-formatted strings (events-frames-taint-\u00a78)",
   "change": "Pass values as printer arguments at the eight sites. modules/LedgerTable.lua:1074,1080: `print('blacklisted', label, '\\226\\128\\148 manage it in Settings \\226\\150\\184 General \\226\\150\\184 Filters \\226\\150\\184 Blacklist.')` (and Whitelist), where label = entry.itemName or ('item ' .. id), which is addon-owned. settings/Schema.lua:677: `print('session window sample', on and 'on' or 'off')`, byte-identical. settings/Schema.lua:691: `print('test mode', on and 'on' or 'off')`, byte-identical. settings/Slash.lua:51,62: `print('blacklist cleared:', n, n == 1 and 'id.' or 'ids.')` (and whitelist). settings/Slash.lua:316 (degraded CliVersion) and :395 (degraded unknown command): match the live library's CliVersion and unknown-verb wording (read libs/LibKa0s/Slash.lua) and pass the version or verb as an argument. Where a literal must abut a value with no separator (the 'v' prefix), keep the one concatenation of addon-owned, secret-free text and comment it.",
   "tests": "Characterization first. grep -rn 'blacklisted\\|whitelist cleared\\|blacklist cleared\\|session window sample\\|test mode \\(on\\|off\\)\\|unknown command' tests/*.lua finds the cases that capture these lines. Re-pin the ones whose text changes (the LedgerTable pair and the two cleared lines) to the new wording. Add 'LedgerTable: blacklisting from the row menu prints one line naming the item and where to manage it' if no case captures it. The degraded CliVersion output is pinned against the live arm's bytes.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && line-cap check as BL-01; grep -nE 'print\\(\\(\"|print\\(.*\\):format' modules/LedgerTable.lua settings/Schema.lua settings/Slash.lua (no output).",
   "smoke": "Right-click a ledger row -> Blacklist item: one line naming the item. /bl test prints 'test mode on'.",
   "finding_ids": [
    "BankLedger-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-05",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-18",
   "repo": "BankLedger",
   "title": "The brand name is spelled once: NS.BRAND_NAME in the launcher tooltip and the options parent title",
   "change": "core/LauncherSetup.lua:163 changes to `tt:AddLine(NS.BRAND_NAME, 1, 0.82, 0)`. settings/OptionsSetup.lua:24 changes to `local PARENT_TITLE = NS.BRAND_NAME` (the TOC loads LauncherSetup at :73 before OptionsSetup). core/CoreSetup.lua:34 NS.LIBKA0S_MISSING loads before NS.BRAND_NAME exists: add a comment there naming it the one early literal. Narrow the core/LauncherSetup.lua:80-89 header from 'THE ONE PLACE IT IS SPELLED' to 'the one place it is spelled after core/CoreSetup.lua's load-time missing-library clause'.",
   "tests": "tests/test_launcher.lua:223 already checks the literal once in its file. Extend it to 'the brand literal appears only at NS.BRAND_NAME and the CoreSetup missing-library clause' (grep 'Ka0s Bank Ledger' across core/ settings/ modules/ Lua, excluding comments). Red today on LauncherSetup.lua:163 and OptionsSetup.lua:24.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && line-cap check as BL-01; grep -rn '\"Ka0s Bank Ledger' core settings modules (LauncherSetup.lua:89 and CoreSetup.lua:34 only).",
   "smoke": "C-06: hover the minimap button, and the tooltip title reads Ka0s Bank Ledger. Esc > Options > AddOns lists Ka0s Bank Ledger.",
   "finding_ids": [
    "BankLedger-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-08",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-19",
   "repo": "BankLedger",
   "title": "Spill ARCHITECTURE.md ## Settings Schema into schema.md and settings-panel.md (hub under ~400 lines)",
   "change": "Move text; do not rewrite it. From docs/ARCHITECTURE.md: the runtime and stub narrative (:51-73) and the registry, recorded data and named-state entries (:140-210) go to docs/schema.md. The bulk-bracket and reset routes (:75-100, already reshaped by BL-05) and the tabs/Filters text (:102-138) go to docs/settings-panel.md. Leave a ## Settings Schema of 60 lines or fewer: the row count, the one write seam, one sentence per registry and named state (architecture-\u00a75 names stay findable), and exactly one link. Target about 400 lines for the file. Fix any in-file anchors that pointed at the moved text.",
   "tests": "none \u2014 docs only. tests/test_docs.lua must stay green. If it checks section presence or links, update only the anchors it follows.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && wc -l docs/ARCHITECTURE.md (about 400 or fewer) && awk '/^## Settings Schema/{s=NR} /^## Launcher/{print NR-s}' docs/ARCHITECTURE.md (60 or fewer) && grep -c '](schema.md\\|](settings-panel.md' in that section == 1.",
   "smoke": "",
   "finding_ids": [
    "BankLedger-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-05",
    "BL-06",
    "BL-12",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-20",
   "repo": "BankLedger",
   "title": "Citation sweep: rotted file:line references cite symbols; 'four title bars' becomes three; .pkgmeta cites packaging bare; the register rows' Why/evidence are corrected",
   "change": "Comments and docs only, after the BL-19 spill so each fix lands where the text ends up. R-06. docs/ARCHITECTURE.md Event Subscriptions: cite NS.StandUp and the Browser/SessionWindow PLAYER_LOGOUT on the private bus target by symbol, not :45/:49-50/:1206, and replace 'each window's own event frame' with 'a private bus target'. In the standalone-windows row (:504), cite B:MakeCloseButton, NS.Icon('close') and its three callers by symbol. In the performance-\u00a712 row (:501), the Why points at the docs/performance.md table (five events can fire in combat; the combat pair also runs ApplyVisibility). docs/performance.md:46 changes OnEnable to NS.StandUp. settings/Schema.lua:139 and :396-401 cite B:ApplyMasterFrame / B:Enable by symbol. settings/Schema.lua:178 and tests/test_schema.lua:278 cite 'the SetMovable call in B:Enable' by symbol. settings/Schema.lua:402-403 changes B:SetupMinimap to core/LauncherSetup.lua. settings/Panel.lua:767 and settings/OptionsSetup.lua:65 change 'two ... filter tabs' to the one Filters tab. core/Database.lua:581 cites the 'undo a recorded row' test by name. A-09: 'four' becomes 'three', and the copy window is named as LibKa0s-Widgets-1.0's CopyWindow, at core/MediaSetup.lua:86, docs/media.md:46 (split the cell: the close mark is on four windows, the factory on three), docs/smoke-tests.md:597-599 (the copy window checked separately), tests/test_libka0s.lua:63,65, tests/test_marks.lua:71,98-100. Rename the :98 case. A-15: .pkgmeta:17,22 change 'packaging.md:28' to 'packaging (its strong-form ignore rule)'. A-18/A-20: after the rewrite, re-check both rows' triggers (not fired) and leave them standing.",
   "tests": "The one case rename in tests/test_marks.lua:98 is behavior-neutral; regenerate docs/test-cases.md in this commit. Otherwise none, docs and comments only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md; grep -rn 'four title bars\\|all four\\|packaging.md:28\\|SetupMinimap\\|two host-drawn filter tabs\\|two item-id filter tabs' core settings modules tests/*.lua docs/*.md .pkgmeta (no output); grep -nE 'BankLedger\\.lua:(45|49)|Browser(\\.lua)?:(1206|1123|1165|1007|98|104|1047)' docs/*.md settings core tests/*.lua (no output).",
   "smoke": "C-05 desk check: open each cited symbol from ARCHITECTURE's register and Event Subscriptions and confirm it resolves. docs/smoke-tests.md: the copy window's close is the library's.",
   "finding_ids": [
    "BankLedger-R-06",
    "BankLedger-A-09",
    "BankLedger-A-15",
    "BankLedger-A-18",
    "BankLedger-A-20"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-19",
    "BL-13",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-21",
   "repo": "BankLedger",
   "title": "Write docs/debug.md for /bl debug scan and /bl debug panel; flip the Documentation map row to Present",
   "change": "New docs/debug.md covers these things. The two verbs (settings/Schema.lua:708-721): /bl debug scan runs NS.Ledger:Diagnose() (modules/Ledger.lua:279) and /bl debug panel runs NS.Panel:Diagnose() (settings/Panel.lua:638). What each prints and its [Scan]/[Panel] tags. The raw-append rule: ungated by the logging flag (debug-logging-\u00a74). The NS.EventRecord registered/unavailable record (BL-06), which the stand-down resets. When to paste each into an issue. Link to the LibKa0s console (/bl debug). docs/ARCHITECTURE.md:467 changes the debug.md row to Present with the trigger 'addon-owned debug verbs beyond the LibKa0s console'. Link it from docs/testing.md or docs/smoke-tests.md where they walk the verbs.",
   "tests": "none \u2014 docs only. tests/test_docs.lua stays green; if it enumerates Present docs, it now finds the file.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && test -f docs/debug.md && grep -n 'debug.md' docs/ARCHITECTURE.md | grep Present.",
   "smoke": "/bl debug scan and /bl debug panel output matches what docs/debug.md describes.",
   "finding_ids": [
    "BankLedger-A-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-06",
    "BL-19",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-22",
   "repo": "BankLedger",
   "title": "README discloses the 30-day retention default",
   "change": "README.md FAQ: add a row. 'Why did old history disappear?' By default, movements older than 30 days are removed at login. Keep them under Settings > History > Keep history for (Always keeps everything). Add a note beside /bl purge in Usage. The default stays 30. Changing it is owner decision CP2, not taken here: AceDB strips default-equal values, so a player who picked 30 would silently move to Always. Drop any savedvariables-\u00a71 citation.",
   "tests": "none \u2014 docs only (player-facing README).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (test_prose/test_docs green) && grep -n '30 days' README.md.",
   "smoke": "",
   "finding_ids": [
    "BankLedger-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-23",
   "repo": "BankLedger",
   "title": "Consolidated re-vendor span bundle docs/revendor/<date>-v1.18.0-v1.53.0/ for the unrecorded tags",
   "change": "WS-01/WA-01 consumer follow-up. Derive the span: `git log --format='%h %ad %s' --date=short -- libs/LibKa0s` since 2026-08-25 00:00, plus the provenance tag each commit carried (`git show <c>:CLAUDE.md | grep -m1 'Bundles \\[LibKa0s\\]'`, or README.md before kit revision 9). Write docs/revendor/<today>-v1.18.0-v1.53.0/ in the WA-01 shape. 01_DELTA.md line 1 is exactly `Delta: LibKa0s v1.15.0 -> v1.54.2 (span: v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0, v1.27.0, v1.28.0, v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0)`, corrected to the tags the log actually shows. Below it, the 36 vendoring commits with their tags, noting which sweeps carried them. 05_SUMMARY.md records what was adopted: launcher (v1.39.0), Lifecycle latch (v1.42.0), combat lock (v1.46.x), IdList (v1.47.0/v1.50.0), and so on. Frozen bundles are not edited, and there are no per-tag back-fill folders.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none \u2014 docs only (frozen record bundle).",
   "verify": "Run the WS-01 AUDIT.md re-vendor comparison from ../WowAddonStandards/standards/AUDIT.md (the `git log --since=\"<horizon> 00:00\"` tag list against every tag on line 1 of each docs/revendor/*/01_DELTA.md). It prints nothing. ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua is green (the prose gate skips docs/revendor/).",
   "smoke": "",
   "finding_ids": [
    "BankLedger-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "WA-01",
    "BL-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-24",
   "repo": "BankLedger",
   "title": "Issue-store and register bookkeeping: close #3 (row landed), and record the 2026-09-23 re-check of the untriggered rows",
   "change": "GitHub, throttled at least 5 s between writes: `gh issue close 3 -R tusharsaxena/BankLedger --comment 'Row landed: docs/ARCHITECTURE.md -> Documented deviations, localization-\u00a71 (M5-02).'`, then `gh issue edit 3 -R tusharsaxena/BankLedger --remove-label state:triaged --add-label state:done`. docs/ARCHITECTURE.md ## Documented deviations: in the savedvariables-\u00a72 row (A-17), the localization-\u00a71 row (A-19), the performance-\u00a712 row (A-18) and the standalone-windows row (A-20), add 'Re-checked 2026-09-23 (audit): trigger not fired' to the trigger cell. A-21 needs no row: the will-not-do issues #4, #6, #7, #8, #10, #15 and #20 decline adoptions, not rules. Say so in one sentence under the register. The commit carries the doc edit. The gh calls are run from the item, not committed.",
   "tests": "none \u2014 docs and issue-store bookkeeping only.",
   "verify": "gh issue view 3 -R tusharsaxena/BankLedger --json state,labels (CLOSED, state:done); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua green; grep -c 'Re-checked 2026-09-23' docs/ARCHITECTURE.md == 4.",
   "smoke": "",
   "finding_ids": [
    "BankLedger-A-10",
    "BankLedger-A-19",
    "BankLedger-A-17",
    "BankLedger-A-21"
   ],
   "issue_refs": [
    "BankLedger#3"
   ],
   "depends_on": [
    "RV-BL",
    "BL-20",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "BL-DOCS",
   "repo": "BankLedger",
   "title": "Sync docs, regenerate docs/test-cases.md, and record a fresh automated-test bundle (perf skip reason 2; watch-list dispositions)",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md and docs/ARCHITECTURE.md: count claims (tests, lint files, verbs, event registrations), the slash/COMMANDS parity, the provenance line naming LibKa0s v1.56.0 and kit revision 26, and the comment-citation check. (2) `lua5.1 tests/run.lua --list > docs/test-cases.md` (kit 26 case names carry \u00a7), and move the README test count or badge to its Totals. (3) Record the bundle with the vendored runner: `tests/_kit/run-automated-tests.sh` writes docs/automated-tests/<YYYYMMDD-HHMMSS>/ and regenerates RESULTS.md. Do not hand-edit RESULTS.md. The perf suite records skip reason (2) from the docs/ARCHITECTURE.md performance-\u00a712 row (LK-09), not reason (1). Write <stamp>/ANALYSIS.md per AUTOMATED_TESTS.md. It records the watch-list movement since 20260916-184426 (tests 943 -> new, lint 67 -> new, NLOC 16,380 -> new, band 3 -> 4 or more) and proposes dispositions for the owner in the watch-list Disposition cells. modules/Insights.lua (1002): accept, re-check trigger 1300 lines or a new section renderer, with the I:Layout section-renderer peel named as the seam. modules/Browser.lua (1220): accept under BL-24 (issue), and the stale 'moving the right way' note is gone. LedgerTable 1132 and tests/test_ledger.lua 1028: carry their existing dispositions. No version bump and no tag (the owner decides). A-22's two closed items stay closed: test_prose is green, and settings/Panel.lua says five tabs.",
   "tests": "none \u2014 docs and record only. The bundle is itself the full four-suite run.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && ~/.claude/wow-addon/bin/ka0s-bounded tests/_kit/run-automated-tests.sh (lint pass, tests pass, perf SKIPPED with a skipReason naming performance-\u00a712, complexity pass with 0 functions above CCN 15) && jq -r '.suites.perf.skipReason // .perf.skipReason' docs/automated-tests/<stamp>/manifest.json | grep 'performance-\u00a712' && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && test -f docs/automated-tests/<stamp>/ANALYSIS.md && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output).",
   "smoke": "LK-12 in client: deposit an epic item this character has never seen this session (uncached). The ledger row shows epic (purple) quality from its |cnIQ4 link, and with Settings > General quality threshold at Epic the movement is still recorded. Walk docs/smoke-tests.md's C-01..C-07 cases for BL-01..BL-18.",
   "finding_ids": [
    "BankLedger-R-07",
    "BankLedger-A-04",
    "BankLedger-A-22"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "LK-12",
    "BL-00",
    "BL-03",
    "BL-04",
    "BL-05",
    "BL-06",
    "BL-07",
    "BL-08",
    "BL-09",
    "BL-10",
    "BL-11",
    "BL-12",
    "BL-13",
    "BL-14",
    "BL-15",
    "BL-16",
    "BL-17",
    "BL-18",
    "BL-19",
    "BL-20",
    "BL-21",
    "BL-22",
    "BL-23",
    "BL-24"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-00",
   "repo": "PartyFrameEnhanced",
   "milestone": "M3",
   "title": "Clear the 9 re-vendor red(s) RV-PF recorded, so every later M3 commit starts green",
   "change": "RV-PF copied LibKa0s v1.56.0 and recorded 9 failing test(s) in its commit body (`git log --grep=\"^RV-PF\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned PF-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PF"
   ],
   "effort": "M"
  },
  {
   "id": "PF-01",
   "repo": "PartyFrameEnhanced",
   "title": "Profile sub-verbs refuse bad names: `new` never wipes an existing profile, `use`/`copy`/`delete` refuse a missing one, `copy` refuses the current one",
   "change": "settings/Slash.lua PROFILE_VERBS (:254-290). (1) Add `local function exists(db, name) for _, n in ipairs(db:GetProfiles()) do if n == name then return true end end return false end` above PROFILE_VERBS. (2) `new` (:270-274): `if exists(db, name) then return print(L[\"Profile '%s' already exists \\226\\128\\148 use /pfe profile use or /pfe profile reset\"]:format(name)) end` before SetProfile. (3) `use` (:265-268): refuse a missing name with `L[\"No profile named '%s' \\226\\128\\148 /pfe profile list shows them\"]` instead of letting SetProfile create it. (4) `copy` (:275-278): refuse `name == db:GetCurrentProfile()` with `L[\"Cannot copy the current profile onto itself\"]`, and a missing name with the no-profile line, BEFORE db:CopyProfile (no pcall-and-print of the AceDB error). (5) `delete` (:279-285): refuse a missing name with the no-profile line before DeleteProfile (the current-profile refusal at :280-282 stays). Every refusal is ONE tagged line through the existing printer; add the three new format strings to the settings/Slash.lua key list in locales/enUS.lua (the `for _, key in ipairs({...}) do L[key] = key end` block, :134-150). Keep each verb function at CCN <= 4. docs/slash-dispatch.md profile row: state that `use` no longer creates a profile and `new` refuses an existing one; record the same player-facing change in the commit body for the next Version History roll.\n\nUpstream follow-up LK-03 made explicit: under kit revision 26 the AceDB fake's CopyProfile/DeleteProfile raise the way AceDB-3.0 :531-537/:581-587 does. Case (c) and the delete case are the red-under-the-new-kit characterization for PartyFrameEnhanced-R-05. Land the exists() guards and the three refusal lines against that raising fake, and do not wrap db:CopyProfile in pcall.",
   "tests": "Red first in tests/test_slash.lua (290 lines, room under the cap), one case per defect, each with a `-- red under:` comment: (a) `profile new Healer`, `set castbar.width 222`, `profile use Default`, `profile new Healer` -> castbar.width in Healer is still 222 and the chat line is the 'already exists' line (`-- red under: drop the exists() guard in new`); (b) `profile use Typo` -> db:GetProfiles() does not contain 'Typo' and one refusal line; (c) `profile copy Missing` and `profile copy <current>` -> no Lua error (pcall(cli.OnSlash) returns true), one refusal line each, store unchanged (under kit revision 26 the AceDB fake raises exactly as AceDB-3.0 :581-587, so the unguarded code errors); (d) `profile delete Missing` -> no 'Deleted profile' line, one refusal line. Clean up created profiles at the end of each case (SetProfile back to the starting profile, DeleteProfile the test profile) so later suites see the starting state. Regenerate docs/test-cases.md and the README Tests badge in the same commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PartyFrameEnhanced && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0 warnings / 0 errors) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed) && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (no output) && grep -c 'Tests-' README.md matches the Totals row && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (empty). Mutation check: remove the exists() guard in `new` and re-run -> case (a) fails.",
   "smoke": "In game: /pfe profile new Healer; /pfe set castbar.width 222; /pfe profile use Default; /pfe profile new Healer -> one 'already exists' line and Healer keeps width 222. /pfe profile copy Nope and /pfe profile copy <current> -> one refusal line each, no Lua error frame. /pfe profile use Typo -> refusal; /pfe profile list shows no 'Typo'.",
   "finding_ids": [
    "PartyFrameEnhanced-R-02",
    "PartyFrameEnhanced-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "LK-03",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-02",
   "repo": "PartyFrameEnhanced",
   "title": "Anchor reads the live profile section, so a profile switch, copy or reset places elements by the NEW profile whatever the bus dispatch order",
   "change": "modules/CastBars.lua:372 (Anchor.Register spec), modules/TargetFrames.lua:258-262, modules/PetFrames.lua:139 and its slotSize closure: `config = function() return NS.db.profile.castbar end` (target / pet respectively), and every slotSize closure reads `NS.db.profile.<key>` live instead of the `cfg` upvalue (e.g. TargetFrames: `local c = NS.db.profile.target; return (c.width or 110) * scale, (c.height or 20) * scale`). CastBars' module-level `slotSize` local does the same. The features keep their own `cfg` upvalues for their own rendering (re-bound in their PROFILE handlers as today). Keep placementOf's shipped-default fallback (Anchor.lua:130-132): it is still correct because the outgoing profile has defaults stripped. Rewrite the comment at modules/Anchor.lua:119-129: CallbackHandler-1.0 dispatches with next() over per-target tables (libs/CallbackHandler-1.0/CallbackHandler-1.0.lua:16-22), so PROFILE handler order is hash order and undefined; Anchor therefore reads the live section and never depends on a feature having re-bound first. Rewrite tests/test_anchor.lua:178-183's comment to the same effect. Rejected: deferring to the next frame, a second ordered PROFILE message.",
   "tests": "New suite tests/test_profile_switch.lua, registered in tests/run.lua's suite list after test_anchor. Red first: (1) deterministic isolation case: create profile 'Free' with castbar/target/pet anchorMode = 'free' via NS.SetByPath, SetProfile back to Default; then `NS.BusStandDown()`, `NS.db:SetProfile('Free')`, call `NS.Anchor.ApplyAll()` directly (this is Anchor hearing PROFILE before any feature re-bound), assert every element's `__aTarget == NS.Anchor.__features[key].holder`; then `NS.BusStandUp()` and settle (`-- red under: revert config() to the captured upvalue`). (2) end-to-end: real `NS.db:SetProfile` between Default (attached) and Free and back, assert `__aTarget` is the provider frame / the holder each way; (3) `NS.Helpers.RestoreAllDefaults()` on Free -> attached placement; (4) `NS.db:CopyProfile('Free')` from Default -> free placement. Under kit revision 26, SetProfile strips defaults from the outgoing profile, which these cases exercise. Delete the test profiles at the end. This lands the real-switch coverage PartyFrameEnhanced-R-08 asks for (together with PF-01's profile-verb cases and PF-03's combat toggle). Regenerate docs/test-cases.md and the README badge.",
   "verify": "Standard gate (run from /mnt/d/Profile/Users/Tushar/Documents/GIT/PartyFrameEnhanced): ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty) and badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0, anchorUnchanged still 0 SetPoint and <= 24 B/iter); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -n 'return cfg end' modules/CastBars.lua modules/TargetFrames.lua modules/PetFrames.lua` returns nothing. Mutation: restore `config = function() return cfg end` in TargetFrames -> case (1) fails.",
   "smoke": "In game with two profiles (Default attached, Free with target frames in free placement): /pfe profile use Free then /pfe profile use Default repeatedly; the target and pet frames follow each profile immediately with no /reload. Reset all settings on Free returns them to attached.",
   "finding_ids": [
    "PartyFrameEnhanced-R-01",
    "PartyFrameEnhanced-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-01",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-03",
   "repo": "PartyFrameEnhanced",
   "title": "Secure-write memos compare against the REQUESTED value, so a setting toggled back in combat is not lost",
   "change": "modules/UnitButtons.lua ApplyDriver (:53-60) and ApplyClicks (:63-71): early-return on request-side memos `btn.__driverWant` / `btn.__clicksWant`, set at request time before NS.RunSecure; `btn.__driver` / `btn.__clicks` stay the applied state, set inside the deferred closure. Because NS.RunSecure keeps the latest write per key (core/PartyFrameEnhanced.lua:62-72), a later request replaces the queued one. Update the file header's SECURE RULES paragraph with one sentence on the two memos.",
   "tests": "Red first in tests/test_targetframes.lua and tests/test_petframes.lua: `mocks.InCombatLockdown = function() return true end`; NS.SetByPath('target.clickToTarget', false) then NS.SetByPath('target.clickToTarget', true); restore InCombatLockdown to false; NS.addon:OnLeaveCombat(); assert `buttons.party1:GetAttribute('*type1') == 'target'` and `buttons.party1.__clicks == true` (`-- red under: compare against btn.__clicks`). Same shape for the driver: in combat set visibility 'never' then back to 'always', leave combat, assert `__drivers.visibility == '[@party1target,exists] show; hide'`. Pet frames: the clickToTarget variant on `pet`. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0; targetTickUnchanged 0 API calls); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. Mutation: revert the early return to `btn.__clicks == on` -> the new case fails.",
   "smoke": "In game, in combat: untick then re-tick Target frames -> Click to target; after combat, left-clicking a target frame targets the unit. Add this step to docs/smoke-tests.md.",
   "finding_ids": [
    "PartyFrameEnhanced-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-04",
   "repo": "PartyFrameEnhanced",
   "title": "The stand-down UNREGISTERS the ten target/pet state drivers instead of replacing them with 'hide'",
   "change": "modules/UnitButtons.lua: add `UnitButtons.Release(btn)`: sets `btn.__driverWant = nil` at request time, then `NS.RunSecure('driver:' .. btn.__key, function() UnregisterStateDriver(btn, 'visibility'); btn:Hide(); btn.__driver = nil end)` (same key as ApplyDriver, so a queued install is replaced). modules/TargetFrames.lua refresh (:185-191) and modules/PetFrames.lua's refresh: when `suspended`, call UnitButtons.Release(btn), set `btn.__allowed = false` and return, instead of ApplyDriver(... 'hide'). Resume's refreshAll re-installs the driver (the want memo is nil, so ApplyDriver runs). A feature that is merely off (not suspended) keeps today's 'hide' driver. An in-combat stand-down queues the release and core/PartyFrameEnhanced.lua's armPendingRegen completes it (existing path). .luacheckrc keeps UnregisterStateDriver (it now has a real use). docs/ARCHITECTURE.md 'The disabled state is total' paragraph: say the state drivers are unregistered out of combat and deferred to PLAYER_REGEN_ENABLED in combat.",
   "tests": "Red first in tests/test_disabled.lua, before the final 'leaves the world enabled' case: (a) after an out-of-combat `enable(false)`, no target or pet button has `__drivers.visibility` (`-- red under: keep the \"hide\" driver`); (b) `enable(true)` restores `[@party1target,exists] show; hide` (or the preview 'show'); (c) with `mocks.InCombatLockdown` returning true, `enable(false)` leaves the driver in place and NS.PendingSecureCount() > 0; after restoring InCombatLockdown and firing PLAYER_REGEN_ENABLED on the regen watcher (mocks.__fire through the live registry), the driver is gone. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -rn 'UnregisterStateDriver(' modules/` shows the Release body.",
   "smoke": "In game: /pfe disable out of combat, then /pfe enable -> target and pet frames return. Enter combat, untick Enable Party Frame Enhanced, leave combat -> no ADDON_ACTION_BLOCKED on the debug console and the frames are gone; re-enable out of combat restores them. Add as a docs/smoke-tests.md step (review S-005).",
   "finding_ids": [
    "PartyFrameEnhanced-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-03",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-05",
   "repo": "PartyFrameEnhanced",
   "title": "The stand-down unregisters the EditMode.Exit callback and burst() arms nothing while suspended; the stand-down suite reads callback registrations",
   "change": "modules/Providers.lua: add `local function editModeCallback(on)` behind the existing presence guard (`EventRegistry and type(EventRegistry.RegisterCallback) == 'function'`): on -> `pcall(EventRegistry.RegisterCallback, EventRegistry, 'EditMode.Exit', burst, Providers)`; off -> `pcall(EventRegistry.UnregisterCallback, EventRegistry, 'EditMode.Exit', Providers)`. OnEnable (:339-346) and Resume (:355-359) call editModeCallback(true); Suspend (:348-353) calls editModeCallback(false). burst() (:61-67) returns before touching burstGen or arming timers when `suspended`. No addon-side tracked-callback helper (anti-pattern #85). tests/test_disabled.lua sig() (:30-32): kit revision 26 records callbacks as `{ kind = 'callback', event, owner }` with no `target`, so read `local who = r.target or r.owner` and name it by `who.__name` when present, else `r.kind`, so a callback entry renders instead of raising. docs/ARCHITECTURE.md Event Subscriptions table (:151-163): add a row `EditMode.Exit (EventRegistry callback) | modules/Providers.lua | re-resolve after Edit Mode closes`; the sentence at :165 then holds as written.\n\nUpstream follow-up LK-04 made explicit: tests/test_disabled.lua regs() and sig() read the kit's recording EventRegistry through mocks.__registrations() entries of kind 'callback' (LK-04, kit revision 26). The surviving 'callback|EditMode.Exit' entry is the red that Providers:Suspend's UnregisterCallback (PartyFrameEnhanced-R-03) turns green.",
   "tests": "Under kit revision 26 (recording EventRegistry, LK-04), step 'every registration the addon owns is UNREGISTERED' in tests/test_disabled.lua goes red on the surviving 'callback|EditMode.Exit' entry; that red is the characterization. Add, in tests/test_disabled.lua after the timers step: `EventRegistry:TriggerEvent('EditMode.Exit')` while disabled, then assert `#mocks.__timers() == 0` (`-- red under: drop the suspended guard in burst`). Add the falsification comment on step 3: `-- red under: drop editModeCallback(false) from Providers:Suspend`. In tests/test_providers.lua, assert the baseline registers exactly one 'EditMode.Exit' callback owned by NS.Providers. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -n 'EditMode.Exit' docs/ARCHITECTURE.md` shows the new row. Mutation: remove editModeCallback(false) from Suspend -> step 3 fails.",
   "smoke": "In game: /pfe disable, open and close Edit Mode, /pfe debug -> no Providers resolve lines while disabled; /pfe enable, open and close Edit Mode -> the resolve burst runs.",
   "finding_ids": [
    "PartyFrameEnhanced-R-03",
    "PartyFrameEnhanced-R-10",
    "PartyFrameEnhanced-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "LK-04",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-06",
   "repo": "PartyFrameEnhanced",
   "title": "Stand-down hides the five fade frames and three free-placement holders; stand-up shows them before VISIBILITY",
   "change": "modules/RangeFade.lua: Suspend (:188-193) adds `NS.RunSecure('fade:shown', function() for _, f in pairs(fades) do f:Hide() end end)` (secure buttons are parented to the fade frames, so the hide is deferred under lockdown); Resume (:195-198) runs the matching Show through the same key BEFORE update(). Rewrite the header's 'never hidden' sentence (:11-12): shown whenever the addon is up, hidden on stand-down. modules/Anchor.lua: add `function Anchor:Suspend()` hiding every `features[key].holder` and `function Anchor:Resume()` showing them, both through `NS.RunSecure('holder:shown', ...)` (secure elements anchor to the holders). core/PartyFrameEnhanced.lua's StandUp already runs each('Resume') before NS.PublishVisibility, so frames are up before elements re-decide; add `files[\"modules/Anchor.lua\"] = { ignore = { \"212/self\" } }` to .luacheckrc only if luacheck flags the colon receiver. docs/ARCHITECTURE.md Taint Notes (:250-254): the fade frame is never moved; it is hidden on stand-down (deferred in combat) and shown on stand-up.\n\nUpstream follow-up LK-05 made explicit: with the kit's CreateFrame now starting frames shown (LK-05), the F_on baseline in tests/test_disabled.lua includes the never-shown container frames (PartyFrameEnhanced_Fade_* and *_Holder). This item lands PartyFrameEnhanced-A-01 (hide them on stand-down) and keeps the direct step-5 IsShown() assertions alongside the baseline check. If the LK-05 dry-run (scratchpad dryrun-lk05-PartyFrameEnhanced.txt) lists other reds in this addon, fix them here by giving the setup the Hide() production performs. Never weaken an assertion.",
   "tests": "Under kit revision 26, CreateFrame starts frames shown (LK-05), so the F_on baseline in tests/test_disabled.lua now contains PartyFrameEnhanced_Fade_* and PartyFrameEnhanced_*_Holder and step 'every frame that was on screen is hidden' goes red; that is the characterization. Keep the direct step-5 assertions too: after `enable(false)`, `_G`-less lookups via mocks (iterate NS.RangeFade.Parent(unit) for each NS.Units.LIST unit and NS.Anchor.__features[k].holder) assert `IsShown() == false`, with `-- red under: drop the Hide from RangeFade:Suspend / Anchor:Suspend`; after `enable(true)` they are shown again. In-combat variant: with InCombatLockdown true, disable leaves them shown and queues; leaving combat hides them. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines.",
   "smoke": "In game: /fstack over the party area after /pfe disable shows no PartyFrameEnhanced_Fade_* or *_Holder frame; disabling in combat hides them once combat ends with no ADDON_ACTION_BLOCKED; /pfe enable brings cast bars, target and pet frames back.",
   "finding_ids": [
    "PartyFrameEnhanced-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-04",
    "LK-05",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-07",
   "repo": "PartyFrameEnhanced",
   "title": "The Lock-frame row's disabled refusal prints the collection's one DisabledLine, not a second wording",
   "change": "settings/Slash.lua: publish `function NS.DisabledLine() return cli:DisabledLine() end` next to NS.ToggleLock, and have NS.ToggleLock (:135-138) use it. modules/Preview.lua: delete REFUSED_DISABLED (:40); in NS.AcceptLock (:106-117) the disabled branch (after the combat check) prints `NS.DisabledLine()` unwrapped (no gray) read at call time, then refreshes the panel and returns false; combat and perf-suspend refusals keep their gray lines. Rewrite the header paragraph at :23-27 to say the disabled refusal is the collection line. locales/enUS.lua: delete the 'cannot unlock \\226\\128\\148 the addon is disabled' key. The Slash descriptor's `set` wrapper (settings/Slash.lua:413-416) keeps returning nothing, so `/pfe set locked false` while disabled prints only the refusal and the echo (unchanged shape); note that in the comment.",
   "tests": "Red first: re-point tests/test_preview_standin.lua:123 to assert the refusal equals `NS.Slash.__cli:DisabledLine()` byte for byte (red against the old gray line). Add in tests/test_disabled.lua: while disabled, `NS.SetByPath('locked', false)` returns false, prints exactly one chat line equal to cli:DisabledLine(), and NS.db.profile.locked stays true (`-- red under: restore REFUSED_DISABLED`). Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -rn 'REFUSED_DISABLED\\|the addon is disabled' modules/ locales/` returns nothing.",
   "smoke": "In game: /pfe disable, open the panel and untick Lock frame -> one line 'Ka0s Party Frame Enhanced is disabled \u2014 enable it with /pfe enable'; the box snaps back.",
   "finding_ids": [
    "PartyFrameEnhanced-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-08",
   "repo": "PartyFrameEnhanced",
   "title": "Migration runner per WS-03: schemaVersion 0 in defaults, NS.SCHEMA_VERSION is the target, profile steps run over every stored profile, stamp only after success",
   "change": "defaults/Profile.lua:134 `schemaVersion = 1` -> `schemaVersion = 0`, with a comment: AceDB strips a value equal to its default at logout, so a current-version default never persists (WS-03). core/Database.lua: add `NS.SCHEMA_VERSION = 1` (the shape v0.1.0 ships); SCHEMA_STEPS rows become `{ to = N, scope = 'profile'|'global', apply = function(tbl) ... end }`; publish `NS.__schemaSteps = SCHEMA_STEPS` for tests only. RunMigrations: `local stamp = g.schemaVersion or 0`; for each step with `stamp < step.to`: a 'profile' step runs `step.apply(p)` for every table in `NS.db.profiles` (AceDB's raw stored profiles, which include the active one) or `{ NS.db.profile }` on the no-AceDB path (:21-27), a 'global' step runs `step.apply(g)`; the whole step runs under pcall; on failure log it ungated through NS.DebugLog:Add('Migrate', ...), print one line `L[\"Settings migration to v%d failed; your settings were left as they were\"]` and stop without stamping; on success `stamp = step.to; g.schemaVersion = stamp` and NS.Debug('Migrate', 'v%d -> v%d (%d profiles)'). After the walk, `if stamp < NS.SCHEMA_VERSION then g.schemaVersion = NS.SCHEMA_VERSION end`. Comment: a raw stored profile has defaults stripped, so every step reads with a fallback and is idempotent against a fresh default profile. Keep RunMigrations under CCN 10 by lifting `targetsFor(step)`. docs/schema.md :85, :112, :117: default 0, stamp 1 after the first run, the per-profile rule.",
   "tests": "Red first in tests/test_database.lua: re-pin :17-27 to 'InitDB stamps NS.SCHEMA_VERSION over a default of 0' (NS.defaults.global.schemaVersion == 0 and NS.db.global.schemaVersion == NS.SCHEMA_VERSION after InitDB). New case with a test-local injected step: create profile 'Second', set g.schemaVersion = 1, push `{ to = 2, scope = 'profile', apply = function(p) p.__migrated = true end }` into NS.__schemaSteps with NS.SCHEMA_VERSION = 2, run NS:RunMigrations(), assert both Default and Second carry __migrated and the stamp is 2 (`-- red under: step.apply(NS.db.profile) only`); a raising step leaves the stamp at 1 and prints one line; a second run is a no-op. Restore SCHEMA_STEPS, SCHEMA_VERSION, the stamp and delete 'Second' afterwards. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -n 'schemaVersion = 0' defaults/Profile.lua` hits.",
   "smoke": "In game: /reload, then inspect WTF/.../SavedVariables/PartyFrameEnhanced.lua after logout: global.schemaVersion = 1 is stored (not stripped).",
   "finding_ids": [
    "PartyFrameEnhanced-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-09",
   "repo": "PartyFrameEnhanced",
   "title": "Session-long registrations move into each owner's syncEvents/listen, and debug arguments are built only with debug on",
   "change": "modules/TargetFrames.lua: move PLAYER_TARGET_CHANGED (:320-323) and RAID_TARGET_UPDATE (:326-332) off file load into syncEvents (:208-222): registered on the module's bus target `ev` while `featureOn() and Units.InParty()` and unregistered otherwise, tracked by a module-local `moduleListening` boolean (hoist `ev` above syncEvents). modules/PetFrames.lua: same for RAID_TARGET_UPDATE (:189) inside its syncEvents (:98-114). modules/Preview.lua: move PLAYER_REGEN_DISABLED (:154-156) into listen(on) (:79-85) beside GROUP_ROSTER_UPDATE, so it is registered only while previewing. Guard debug argument construction: TargetFrames.lua:205 and PetFrames.lua:83 become `if NS.State.debug then NS.Debug(..., UnitName(btn.token) or ...) end`. Update docs/ARCHITECTURE.md Event Subscriptions rows for TargetFrames, PetFrames and Preview (PLAYER_REGEN_DISABLED 'only while preview is on').",
   "tests": "Red first: tests/test_targetframes.lua: with `target.enabled = false`, no registration in mocks.__registrations() has target NS.TargetFrames.__ev and event PLAYER_TARGET_CHANGED or RAID_TARGET_UPDATE; solo (`mocks.__context.inGroup = false` + NS.addon:OnRosterUpdate()) the same; back in a party with the feature on both are registered (`-- red under: register them at file load`). tests/test_petframes.lua: the RAID_TARGET_UPDATE equivalent. tests/test_preview.lua: locked -> no PLAYER_REGEN_DISABLED on NS.Preview.__ev; unlocked -> registered; entering combat while unlocked still re-locks. Debug-args case in tests/test_targetframes.lua: wrap mocks.UnitName with a counter, fire UNIT_TARGET on party1 with NS.State.debug false and then true; the debug-off count is one lower (`-- red under: build the NS.Debug argument unconditionally`). tests/test_disabled.lua baseline R_on stays non-empty. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output; syncEvents stays <= CCN 10); no authored .lua over 1500 lines.",
   "smoke": "In game solo: /pfe debug on, change target -> no Target line; join a party -> the player's own target frame updates on PLAYER_TARGET_CHANGED; raid markers repaint. Unlock, enter combat -> 'Locked \u2014 combat started' still prints.",
   "finding_ids": [
    "PartyFrameEnhanced-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-05",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-10",
   "repo": "PartyFrameEnhanced",
   "title": "Every event registration goes through LibKa0s-Core's SafeRegisterEvent family with a host-owned rejected list reachable from /pfe status",
   "change": "core/CoreSetup.lua live half: `NS.SafeRegisterEvent = lib.SafeRegisterEvent`, `NS.SafeRegisterUnitEvent = lib.SafeRegisterUnitEvent`, `NS.SafeRegisterEvents = lib.SafeRegisterEvents` (Core minor 8, LK-11). Degraded half (:17-69): one-rung bodies per the Core version-8 doc's Degradation note: pcall only, no C_EventUtils front gate, append the name to `rejected` once, return the boolean / count. core/State.lua: `NS.RejectedEvents = {}` (host-owned). Route every site: core/PartyFrameEnhanced.lua:140 (loop of NS.SafeRegisterEvent(self, event, method, NS.RejectedEvents)) and the regen watcher (:116); modules/Providers.lua registerEvents (:329-337); modules/CastBars.lua:336 (NS.SafeRegisterUnitEvent(el, EVENTS[i], NS.RejectedEvents, unit)); modules/TargetFrames.lua:214 and the two moved in PF-09; modules/PetFrames.lua:103-107 and RAID_TARGET_UPDATE; modules/RangeFade.lua:168; modules/Preview.lua listen(). core/Bus.lua NS.BusStandUp (:81-88): also append the record's rejected names to NS.RejectedEvents. settings/Slash.lua runStatus: when `#NS.RejectedEvents > 0`, print `L[\"Events the client refused: %s\"]` with the joined names (new locale key). docs/midnight-quirks.md: one paragraph on the probing trade-off (the IsEventValid gate is optional, the pcall is the guard). docs/ARCHITECTURE.md Event Subscriptions: one sentence that every row goes through SafeRegister*.",
   "tests": "Red first, new cases in tests/test_providers.lua and tests/test_castbars.lua using the kit's M.__badEvents (kit revision 26 honors it on AceEvent targets and on frame RegisterEvent/RegisterUnitEvent, and C_EventUtils.IsEventValid): set `mocks.__badEvents = { EDIT_MODE_LAYOUTS_UPDATED = true }`, disable then enable the addon, assert PLAYER_REGEN_ENABLED and ADDON_LOADED are still registered on NS.Providers.__ev and NS.RejectedEvents contains EDIT_MODE_LAYOUTS_UPDATED exactly once (`-- red under: a bare RegisterEvent loop`); a bad UNIT_SPELLCAST_* name leaves the other cast events registered on the bar; `/pfe status` prints the refused-events line. Clear __badEvents and NS.RejectedEvents after. test_surface_parity's Core case derives the three new publications from core/CoreSetup.lua and pins the degraded stub; add a degraded case: NS2.SafeRegisterEvent on a raising target returns false and appends once. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0; castStartStop within its ceiling); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -rnE ':Register(Unit)?Event\\(' core modules settings | grep -v SafeRegister` returns only comments.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-09",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PF-11",
   "repo": "PartyFrameEnhanced",
   "title": "Adopt LibKa0s-Schema-1.0 (minor 2) with writeThrough {'enabled','locked'}, so the library-less build still writes its composed rows",
   "change": "Re-apply the rolled-back adoption (issue #14; design in Ka0sAddonsCommonTasks/docs/2026-09-22-SUITE_STANDARDS_AND_LIBKA0S_SWEEP/3b-specs/schema.md:507-518) with the WS-02 route (a). settings/Schema.lua becomes the setup file: `local SchemaLib = LibStub and LibStub('LibKa0s-Schema-1.0', true) or HostSchemaStub` where HostSchemaStub is the runtime-completing stub from LibKa0s docs/api/Schema/version-2-docs.md 'The degradation stub' (copied from LibKa0s tests/test_schema.lua referenceStub, trimmed to what PFE calls, carrying SetMany and honoring row.normalize, with a comment naming that section), which takes the same writeThrough list and stores a row-less path in it (copy written at the path) while refusing every other row-less path. `inst = SchemaLib:New{ rows = NS.Schema, resolveRoot = function() return NS.db and NS.db.profile, 1 end, announce = function(row) if not row.sessionOnly and not row.writeThrough then publishConfig(sectionOf(row)) end end, debug = NS.Debug, debugEnabled = function() return NS.State and NS.State.debug end, format = NS.FormatSchemaValue, print = NS.Print, resetExempt = { [MINIMAP_PATH] = true }, writeThrough = { 'enabled', 'locked' } }` (the paths written by settings/Slash.lua:117 and :152 and modules/Preview.lua:134, plus the combat re-lock). Bind: NS.RegisterSchemaRows = function(rows) inst.AddRows(rows) end; NS.FindSchemaRow = inst.FindRow; NS.SetByPath = inst.Set; NS.ApplyDefault = inst.ApplyDefault; NS.Bulk.Begin/End/Run = inst.BulkBegin/BulkEnd/BulkRun; NS.ProfileRowsOffDefault = function() return inst.CountOffDefault(profilePred) end; NS.ResetProfileCounted = function(db) inst.ResetCounted(function() db:ResetProfile() end, profilePred) end; NS.ConsumeResetCount = inst.ConsumeResetCount; NS.ValidateSchema = function() return inst.Validate{ pages = VALID_PAGES, defaultsRoot = NS.defaults and NS.defaults.profile } end; NS.ResolvePath/NS.SetPath = SchemaLib.Read/SchemaLib.Write; NS.GetSetting = `local v = inst.Get(path); if v ~= nil then return v end; return SchemaLib.Read(NS.defaults and NS.defaults.profile, path)`. Delete byPath, the host bracket, SetSetting, the session and global registries: settings/General.lua stamps `row.get`/`row.set` onto the composed debug-console and minimap rows by path (the bodies of the current RegisterSessionSetting/RegisterGlobalSetting specs), and NS.IsGlobalSetting survives only as the source of profilePred and resetExempt. The `'%s refused'` debug line goes (a refused write is not a mutation). The `locked` row's AcceptLock stays in row.validate (the gate-in-validate rule). settings/OptionsSetup.lua keeps call-time forwarders for `set` and `applyDefault` (tests/test_optionssetup.lua:12-31 observes the Reset All veto through NS.ApplyDefault); settings/Slash.lua's descriptor keeps its wrappers, and `set` keeps returning nothing (AcceptLock prints its own one line, so Slash minor 15's CliSet refusal print would double it). Publish `NS.__schema = inst` for parity. tests/run.lua setSurfaceSource: add `['LibKa0s-Schema-1.0'] = mocks.LibStub('LibKa0s-Schema-1.0', true)`. docs/schema.md and docs/ARCHITECTURE.md Settings Schema: the seam is the library's; the writeThrough list and why; Known Limitations: on a library-less build `/pfe enable|disable|lock|unlock` store the switch without the row's onChange (unchanged from today). Commit body: 'Closes #14'.\n\nUpstream follow-up LK-23 made explicit: this item is the PartyFrameEnhanced#14 re-application LK-23's adoption notes name. It needs LibKa0s-Schema-1.0 minor 2 with `writeThrough` in the vendored v1.56.0 payload (RV-PF), and the host stub takes the same list. The list {'enabled','locked'} covers every path the follow-up names: settings/Slash.lua runEnabled (:152, `NS.SetByPath(\"enabled\", on)`), runLock (:117, `NS.SetByPath(\"locked\", locked)`), and modules/Preview.lua forceLock (:134, `NS.SetByPath(\"locked\", true)`, the combat/master-switch/perf re-lock). Done when tests/test_schema.lua:209 ('without the library, /pfe disable and /pfe enable still write the stored path') is green on the adopted seam.",
   "tests": "Characterize first (green on the host seam, must stay green): tests/test_schema.lua:209 ('without the library, /pfe disable and /pfe enable still write the stored path'); add beside it degraded `/pfe lock` / `/pfe unlock` writing `locked`, and a degraded write to a row-less path NOT in writeThrough ('general.__nope') refused and not stored. Then adopt and re-pin the deliberate deltas per the Schema adoption notes: unknown path refused on the live build (re-point test_schema.lua:170-181's bracket probe from 'general.__probe' to a real row path), table values copied into the store, no '%s refused' line. New parity cases in tests/test_surface_parity.lua: `T.assertSurfaceParity(NS.__schema, NS2.__schema, 'schema instance vs host stub')` and `T.assertSurfaceParity(NS2.__schemaLib, 'LibKa0s-Schema-1.0', { 'STRINGS' })` (publish NS.__schemaLib = SchemaLib). tests/perf.lua resolveUnchanged must stay 0.0 B/iter and within 24. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed, including test_schema's library-absent case); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0; resolveUnchanged 0.0 B/iter, settingsDrag reported); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines (settings/Schema.lua well under). `grep -n 'local byPath\\|RegisterSessionSetting\\|RegisterGlobalSetting' settings/` returns nothing.",
   "smoke": "In game: tick/untick every Master controls checkbox, drag a color picker, /pfe set castbar.width 150, /pfe reset castbar.width, Reset all settings, Defaults on a page -> all behave as before; the minimap button survives both resets. Rename libs/LibKa0s temporarily in a test install, /reload: /pfe disable then /reload keeps the addon disabled; /pfe enable restores it; no Lua error.",
   "finding_ids": [],
   "issue_refs": [
    "PartyFrameEnhanced#14"
   ],
   "depends_on": [
    "RV-PF",
    "PF-07",
    "PF-02",
    "LK-23",
    "PF-00"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "PF-12",
   "repo": "PartyFrameEnhanced",
   "title": "Library-absent Slash stub takes the LK-18 shape: no FormatRow copy, DisabledLine pinned to the library bytes, the WS-02 library-absent line",
   "change": "settings/Slash.lua degraded stub (:322-391): delete the copied `FormatRow` (`cmd .. ' \u2014 ' .. desc`, :327). Help, landing and profile rows render plainly as `cmd .. '  ' .. desc` through one local `plainRow` used only when SlashLib is absent (printProfileHelp at :243 calls `(SlashLib.FormatRow or plainRow)`; the stub table carries no FormatRow). Keep `DISABLED_LINE` as the verbatim DISABLED_LINE_FORMAT bytes (LibKa0s/Slash.lua:83) and publish it as `Sl.__disabledLineFormat` on the degraded load only. The Cli verbs' `absent(verb)` prints `L[\"%s is unavailable: the LibKa0s library did not load.\"]:format('/pfe ' .. verb)` (WS-02's one-sentence, one-placeholder line); delete the old `missing` concatenation and the 'is unavailable.' locale key; add the new key. Correct the false comment at :322-324: the stub carries the minimal OnSlash dispatch slash-commands-\u00a71 sanctions, the verbatim refusal format (pinned), plain rows and no library formatter or parser. enable/disable/lock/unlock need no stub change: they write through PF-11's writeThrough (route (a)); none raises.",
   "tests": "Red first in tests/test_surface_parity.lua: `T.assertLibraryConstant(NS2.Slash.__disabledLineFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')` per docs/api/testkit/version-26-docs.md (tests/run.lua maps that name to the Slash INSTANCE; read the doc's resolution rule first, and if the helper resolves through that map, pin instead `NS2.Slash.__cli:DisabledLine() == NS.Slash.__cli:DisabledLine()` byte for byte and say so in the case comment). New case: degraded `/pfe help` rows contain no '\\226\\128\\148' em-dash row separator (`-- red under: keep the FormatRow copy`); degraded `/pfe list` prints exactly one line equal to the WS-02 library-absent line for '/pfe list'; degraded `/pfe profile` with no AceDB prints plain rows without raising. Adjust the Slash parity case's exclusion comment (:64-65). Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -n 'FormatRow = function' settings/Slash.lua` returns nothing.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-11",
    "PF-07",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-13",
   "repo": "PartyFrameEnhanced",
   "title": "Minimap row's CLI path reads in its own shown sense: global.minimap.shown, inverting onto the stored minimap.hide",
   "change": "settings/General.lua:51 MINIMAP_PATH = 'global.minimap.shown'; the row's get/set closures (stamped in PF-11) keep inverting onto `NS.db.global.minimap.hide` and calling NS.Launcher:SetShown; no `shown` key is ever stored (anti-pattern #81). settings/OptionsSetup.lua exemptFromReset and the Schema descriptor's resetExempt use the new path; settings/Schema.lua comments (:105, :318 equivalents after PF-11). Validation: the row is a closure row, so Validate's resolution check must not look for `global.minimap.shown` in defaults; exempt it the way sessionOnly rows are exempted (profilePred / the Validate spec) and pin the storage default separately. Docs: docs/schema.md Global rows, docs/settings-panel.md, docs/ARCHITECTURE.md launcher table 'Visibility' cell, README if it names the path. SavedVariables are unchanged.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first in tests/test_launcher.lua: MINIMAP_PATH constant -> 'global.minimap.shown'; `/pfe get global.minimap.shown` answers true while the button is shown and `NS.db.global.minimap.hide == false`; `/pfe set global.minimap.shown false` stores hide = true and registration().shown is false; `NS.db.global.minimap.shown == nil` always. Re-point tests/test_schema.lua:183-191 to the new path. New case: `NS.defaults.global.minimap.hide ~= nil` (the storage default the validator no longer resolves). Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output); no authored .lua over 1500 lines. `grep -rn 'minimap.hide' settings/ docs/*.md` shows only storage references, never the CLI path.",
   "smoke": "In game: /pfe get global.minimap.shown -> true with the button visible; /pfe set global.minimap.shown false hides it; /reload keeps it hidden; the Master controls checkbox agrees.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-11",
    "WS-06",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-14",
   "repo": "PartyFrameEnhanced",
   "title": ".luacheckrc drops the eleven unused read_globals, including UnitExists",
   "change": ".luacheckrc read_globals (:14-38): remove GetTime, wipe, Mixin, UnitExists, UnitGUID, UnitIsUnit, UnitIsDeadOrGhost, UnitIsConnected, C_Secrets, RegisterUnitWatch, UnregisterUnitWatch. Keep UnregisterStateDriver (PF-04 calls it) and EventRegistry. Add a comment where UnitExists stood: deliberately absent, a raw UnitExists(compound token) is the secret-value hazard core/Compat.lua:205-208 documents removing. Re-run the zero-use grep for every remaining name after PF-09/PF-10 and remove any other name that lost its last use.",
   "tests": "None \u2014 lint configuration only; luacheck itself is the check (a removed name still in use fails lint with 113).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PartyFrameEnhanced && for g in GetTime wipe Mixin UnitExists UnitGUID UnitIsUnit UnitIsDeadOrGhost UnitIsConnected C_Secrets RegisterUnitWatch UnregisterUnitWatch; do grep -rnw \"$g\" core modules settings locales defaults tests/*.lua | grep -v '^\\s*--' ; done (only comments or mocks.X accesses) && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output).",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-04",
    "PF-10",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-15",
   "repo": "PartyFrameEnhanced",
   "title": "Locale hygiene: dead keys out, near-duplicate pairs collapsed, provider labels and the DebugLog stub strings routed through NS.L",
   "change": "locales/enUS.lua: delete 'Position' (:31 list) and '/pfe %s does nothing while the addon is off \\226\\128\\148 /pfe enable turns it back on' (:150); collapse 'All settings reset to defaults' / '...defaults.' and 'Cannot reset settings \\226\\128\\148 the settings helpers failed to load' / '...load.' to the no-period spelling; settings/General.lua:200/:202 read the kept keys. modules/Providers.lua:96, :119, :138: labels become `L[\"Blizzard (raid-style)\"]`, `L[\"Blizzard (classic)\"]` and `L[\"EllesmereUI\"]` at definition (`local L = NS.L` at the top; locales load first). core/DebugLogSetup.lua degraded stub: :40 prints `L[\"debug logging %s\"]:format(on and '|cff40ff40ON|r' or '|cffff4040OFF|r')` (one format string, not a routed fragment), :54 label = `L[\"Debug console\"]` (reuse the key if it exists). Add the new keys to enUS.lua. Remove any other key PF-07/PF-12 left without a reader.",
   "tests": "tests/test_optionssetup.lua:88 keeps working (the L metatable returns the key). Add in tests/test_slash.lua: `/pfe status` prints 'Frame system: ' followed by the L-routed label (the mock provider's label equals its key in enUS, so assert against NS.L lookup). Add a degraded case in tests/test_debuglog.lua: NS2.DebugLog:SetEnabled(true) prints one line matching L['debug logging %s']. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). `grep -n 'defaults\\.\"\\]\\|load\\.\"\\]\\|does nothing while the addon is off' locales/enUS.lua settings/*.lua` returns nothing; `grep -n 'label = \"' modules/Providers.lua` returns nothing.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-14",
    "PartyFrameEnhanced-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-07",
    "PF-12",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-16",
   "repo": "PartyFrameEnhanced",
   "title": "Holders and the stand-in opt out of the client's layout cache",
   "change": "modules/Anchor.lua after holder:SetMovable(true) (:257) and modules/StandIn.lua after f:SetMovable(true) (:153): `if f.SetDontSavePosition then f:SetDontSavePosition(true) end` (presence-guarded; the addon re-anchors both from its own stored or default position, so a cached client position is never wanted). One comment line each; cite no standards clause (none requires it).",
   "tests": "In tests/test_anchor.lua and tests/test_standin.lua: give the mock frame a recording SetDontSavePosition before the module builds (a test-local wrapper around mocks.CreateFrame for named frames) and assert it was called with true; a frame without the method still builds without raising. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output).",
   "smoke": "In game: drag a free-placement holder, /reload, log out; WTF/Account/<acct>/<realm>/<char>/layout-local.txt has no PartyFrameEnhanced_*_Holder or PartyFrameEnhancedStandIn entry.",
   "finding_ids": [
    "PartyFrameEnhanced-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-06",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-17",
   "repo": "PartyFrameEnhanced",
   "title": ".pkgmeta: ship no screenshots, drop the two phantom ignores; fix the screenshot filenames",
   "change": ".pkgmeta ignore list: add `- media/screenshots   # README links the CurseForge copies; the client cannot load .png`; delete `- .claude` and `- .superpowers` (:12-13), neither directory exists (packaging branch c). `git mv media/screenshots/partframeenhanced.screenshot.0{1,2,3}.png media/screenshots/partyframeenhanced.screenshot.0{1,2,3}.png` (nothing references the local files; README links forgecdn).",
   "tests": "None \u2014 packaging metadata and file renames only; no Lua behavior changes.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PartyFrameEnhanced && grep -n 'media/screenshots' .pkgmeta (one hit) && ! grep -n '\\.claude\\|\\.superpowers' .pkgmeta && ls media/screenshots (partyframeenhanced.*) && grep -rn 'partframe' --exclude-dir=.git --exclude-dir=libs --exclude-dir=audits --exclude-dir=reviews --exclude-dir=automated-tests . (nothing) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed; test_eol stays green on the renamed binaries).",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-18",
    "PartyFrameEnhanced-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-18",
   "repo": "PartyFrameEnhanced",
   "title": "Zero-overhead perf scenario pins capture-off against instrumentation absent (call counters), not against capture-on",
   "change": "tests/perf.lua scenario 8 (:226-240): wrap `debugprofilestop` (mocks global) and `NS.Perf.Note` with call counters for the probeOverheadOff measurement, and assert 0 calls per iteration while `NS.Perf.on == false` (the dormant bracket `local t0 = Perf.on and debugprofilestop()` must reach neither). Keep the probeOverheadOff <= 24 B ceiling and the same-API-count assertion; drop `probeOff.bytesPerIter <= probeOn.bytesPerIter + 1`. Comment: counting the bracket's two entry points is the stand-in for 'instrumentation absent' (performance-\u00a79), because the bracket cannot be compiled out. No wall-clock assertion. The scenario stays outside docs/test-cases.md.",
   "tests": "Red first: run the new assertion against a deliberately broken bracket (temporarily change CastBars' `Perf.on and debugprofilestop()` to `debugprofilestop()` in a scratch copy) and confirm tests/perf.lua exits non-zero; then restore. The committed change is the perf.lua assertion.",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0, prints probeOverheadOff 0 bracket calls/iter) && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output).",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-10",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-19",
   "repo": "PartyFrameEnhanced",
   "title": "docs/performance.md and the perf.lua comments carry today's measured figures, with the settingsDrag growth attributed",
   "change": "After every code item above has landed, run `~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua` three times and copy the measured figures into docs/performance.md's scenario table (:84-92) and the 'Figures from' line (:95), in the same commit. Attribute settingsDrag's growth (596 -> ~896 B/iter) by measurement, not theory: run tests/perf.lua at the commits that touched modules/Element.lua's structureSignature (`git log --format=%h -- modules/Element.lua`) and at their parents (git worktree in the scratchpad), and write the commit that moved it and why into performance.md. Update tests/perf.lua's ceiling comment (:243-258) to the measured castStartStop figure without changing any ceiling value. If castStartStop is no longer 16.6, say so in the doc beside issue #12's reference.",
   "tests": "None \u2014 documentation of measured figures; tests/perf.lua is run, not changed (ceilings unchanged).",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (exit 0) and every figure in docs/performance.md's table equals that run's output; `grep -n '16.6' tests/perf.lua docs/performance.md` hits only where the doc records history; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed; prose gate green).",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-18",
    "PF-11",
    "PF-10",
    "PF-09",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-20",
   "repo": "PartyFrameEnhanced",
   "title": "TOC annotates LifecycleSetup's load-bearing position and a load-order case pins it",
   "change": "PartyFrameEnhanced.toc:57-59: above core\\LifecycleSetup.lua put `# LOAD-BEARING: publishes NS.lifecycle, which core\\PerfSetup.lua hands LibKa0s-Perf-1.0 at file load.`; move the existing 'publishes NS.Perf before any module takes `local Perf = NS.Perf`' note directly above core\\PerfSetup.lua.",
   "tests": "Red first in tests/test_loadorder.lua, in the MediaSetup case's shape (:41): 'loadorder: LifecycleSetup loads before PerfSetup, and the TOC says why' \u2014 assertBefore('core/lifecyclesetup.lua', 'core/perfsetup.lua', 'Perf requires d.lifecycle') and `readFile(TOC):find('publishes NS.lifecycle', 1, true) ~= nil` (red until the TOC comment lands). Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-21",
   "repo": "PartyFrameEnhanced",
   "title": "Bootstrap on line 1 and the self-naming header beneath it in every file; LOGO_PATH derived from addonName",
   "change": "Move `local _, NS = ...` (or `local addonName, NS = ...`) to line 1, with the `-- <path> \u2014 ...` header directly beneath, in settings/About.lua, settings/ElementRows.lua, settings/Profiles.lua, core/EnvSetup.lua, locales/enUS.lua, core/MediaSetup.lua, settings/PetFrames.lua, settings/General.lua, settings/CastBars.lua, settings/TargetFrames.lua. Add path headers to core/Namespace.lua, core/Constants.lua and core/State.lua. core/Constants.lua line 1 becomes `local addonName, NS = ...` and :17 `C.LOGO_PATH = \"Interface\\\\AddOns\\\\\" .. addonName .. \"\\\\media\\\\logos\\\\\" .. addonName:lower() .. \".logo.tga\"`.",
   "tests": "Add in tests/test_loadorder.lua (already reads the TOC file list): 'every TOC-listed authored file opens on the namespace bootstrap' \u2014 for each Loader.tocFiles entry, line 1 matches `^local [%w_]+, NS = %.%.%.$` and one of lines 2-4 is `-- <that path>` (red today on the 13 files). Add in tests/test_optionssetup.lua or a Constants case: NS.Constants.LOGO_PATH == 'Interface\\\\AddOns\\\\PartyFrameEnhanced\\\\media\\\\logos\\\\partyframeenhanced.logo.tga'. Regenerate docs/test-cases.md and the badge.",
   "verify": "Standard gate: ~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty), badge = Totals; for f in $(lua5.1 -e 'for l in io.lines(\"PartyFrameEnhanced.toc\") do local p=l:match(\"^([%w\\\\_]+%.lua)$\") if p and not p:find(\"^libs\") then print((p:gsub(\"\\\\\\\\\",\"/\"))) end end'); do head -1 $f | grep -q '^local .*, NS = \\.\\.\\.$' || echo $f; done (no output).",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-13",
    "PartyFrameEnhanced-A-14",
    "PartyFrameEnhanced-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-15",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-22",
   "repo": "PartyFrameEnhanced",
   "title": "Deviation register: retire the stale events-frames-taint-\u00a71 row, record the EllesmereUIDB read as a library-stack-\u00a76 deviation, and correct scope.md",
   "change": "docs/ARCHITECTURE.md ## Documented deviations (:352): delete the events-frames-taint-\u00a71 row (v2.63.0 changelog item 5 ruled it is not a violation) and move its one-sentence rationale, citing that ruling, into ## Event Subscriptions under the RegisterUnitEvent rows. Add the row for the owner's A-05 decision; this plan takes option (b), keeping the read because the stand-in is sized wrong without it: `| library-stack-\u00a76 | modules/Providers.lua ellesmereConfiguredSize() reads EllesmereUIDB.profiles[active].addons.EllesmereUIRaidFrames.partyFrameWidth/Height, read-only and nil-guarded, only to size the preview stand-in out of a party | EllesmereUI's hidden party buttons carry its raid size until it lays out a party, so measuring one copies the wrong frame; the fallback is EllesmereUI's own 125 x 60 | <date> | EllesmereUI exposes its configured party size through a frame or API, or its hidden party buttons report party size |`. Keep '### Files over the 1500-line cap' under the heading (test_layout_cap parses it). docs/scope.md:24-26: name the EllesmereUIDB size read beside 'reads their position and unit'. modules/Providers.lua:286-291 comment cites the register row. If the owner picks option (a) at execution instead: delete ellesmereConfiguredSize, measure ERFPartyHeader's first child with FALLBACK.ellesmere (125 x 60), drop EllesmereUIDB from .luacheckrc, delete the Taint Notes sentence at ARCHITECTURE.md:251-254, add no row, and a test in tests/test_standin.lua pins the fallback size.",
   "tests": "None under option (b) \u2014 register and prose only; the kit's register parser (tests/_kit/framework.lua deviationRows, run by test_layout_cap and test_prose) is the check. Under option (a) the test named in the change is written red first.",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed; register parses) && grep -n 'library-stack-\u00a76' docs/ARCHITECTURE.md (one row) && ! grep -n '^| `events-frames-taint-\u00a71`' docs/ARCHITECTURE.md && grep -n 'EllesmereUIDB\\|saved settings' docs/scope.md.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-04",
    "PartyFrameEnhanced-A-05",
    "PartyFrameEnhanced-A-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-10",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-23",
   "repo": "PartyFrameEnhanced",
   "title": "Doc drift: Preview in the bus consumer cells, and the six sites the doc set lags the code",
   "change": "docs/ARCHITECTURE.md:107 (ConfigChanged) and :109 (ProfileChanged) Consumers cells add Preview (modules/Preview.lua:161, :170). CLAUDE.md:33 and docs/ARCHITECTURE.md:3-6: repoint the resume pointer from the v0.1.0 build ledger to docs/ARCHITECTURE.md + GitHub issues (the build plan is history). docs/module-map.md:17: drop `test` from State's fields. docs/compat-layer.md: remove the blank lines at :16 and :22 that split the table; soften :4 'never the raw API' to name the raw calls CastBars.lua:216, RangeFade.lua:116 and TargetFrames.lua:81 make and why they are safe. docs/ARCHITECTURE.md:140 Right click: restate per v2.57.0 (the settings panel is setup, reachable in either state), not v2.56.0's 'narrows the slash surface'. defaults/Profile.lua:4: header says every profile default lives here, without 'the phase that builds each feature'. KEEP '15 shims' at ARCHITECTURE.md:324 (14 function statements plus Compat.IsSecret) and add the counting command beside it: `grep -cE '^function Compat\\.|^Compat\\.[A-Za-z]+ *=' core/Compat.lua`.",
   "tests": "Add in tests/test_bus.lua: derive each message's receivers from source (grep modules/*.lua and core/*.lua for `RegisterMessage(NS.MSG.<KEY>`) and assert every receiving module's name appears in that message's Consumers cell of docs/ARCHITECTURE.md (red today on Preview). Regenerate docs/test-cases.md and the badge.",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed) && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty) && grep -cE '^function Compat\\.|^Compat\\.[A-Za-z]+ *=' core/Compat.lua (15) && ! grep -n 'narrows the \\*slash\\* surface' docs/ARCHITECTURE.md && ! grep -n 'plans/2026-09-15' CLAUDE.md.",
   "smoke": "",
   "finding_ids": [
    "PartyFrameEnhanced-A-10",
    "PartyFrameEnhanced-A-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-09",
    "PF-10",
    "PF-22",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-24",
   "repo": "PartyFrameEnhanced",
   "title": "Consolidated span bundle for the re-vendors the amended AUDIT.md check reports unrecorded",
   "change": "Run WowAddonStandards AUDIT.md's amended re-vendor check (WS-01: `git log --since=\"$horizon 00:00\"`, span-bundle aware) against this repo with the horizon the 2026-09-23 audit used (docs/audits/2026-09-15). Expected unrecorded: v1.37.0, v1.38.0, v1.39.0, v1.43.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0 (commits c381dc8..2a18e3b); docs/revendor/ records only v1.55.0 and v1.56.0. Write `docs/revendor/<today>-v1.37.0-v1.53.0/01_DELTA.md` (use the first and last tags the check actually lists) whose line 1 is exactly `Delta: LibKa0s v<base> -> v1.53.0 (span: v1.37.0, v1.38.0, ...)`, base = the tag the CLAUDE.md provenance line named at 246dba6. Per tag: the commit, date, `git -C ../LibKa0s diff --stat <prev> <tag>` summary and the per-file minors from `grep -hoE 'local (MAJOR, )?[A-Z_]*MINOR *= *(\"[^\"]+\", *)?[0-9]+'` at each tag, in the WA-01 shape. Frozen bundles are not edited.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "None \u2014 a frozen documentation record; the prose gate and test_eol scan it through the normal run.",
   "verify": "head -1 docs/revendor/*-v1.37.0-v1.53.0/01_DELTA.md matches '^Delta: LibKa0s v[0-9.]+ -> v1.53.0 \\(span: ' && re-running the amended AUDIT.md check reports no unrecorded re-vendor && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed).",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "WA-01",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PF-DOCS",
   "repo": "PartyFrameEnhanced",
   "title": "Sync docs, regenerate docs/test-cases.md, and record a fresh automated-test bundle (no version bump)",
   "change": "Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md and docs/ARCHITECTURE.md (count claims: 17 verbs, test totals, 15 shims, Event Subscriptions and Message Bus tables vs source, the LibKa0s v1.56.0 provenance line, Schema adoption, SafeRegister*, writeThrough, the minimap .shown path, the new register row) and docs/slash-dispatch.md, docs/schema.md, docs/settings-panel.md, docs/testing.md, docs/smoke-tests.md (the combat steps PF-03/PF-04/PF-06 added). `lua5.1 tests/run.lua --list > docs/test-cases.md` and the README Tests badge from its Totals. Then `tests/_kit/run-automated-tests.sh` (all four suites) to write docs/automated-tests/<YYYYMMDD-HHMMSS>/ with manifest (commit cell, kit revision 26 runner), roll RESULTS.md, and write the bundle's ANALYSIS.md per the AUTOMATED_TESTS.md playbook; this replaces the stale 20260918-121606 record. Re-run the dotted-section sweep `grep -rEn '\u00a7[0-9]+\\.[0-9]' --exclude-dir=libs --exclude-dir=.git --exclude-dir=audits --exclude-dir=reviews .` and confirm every hit still cites the addon's own design spec (no action). No version bump, no tag.",
   "tests": "None \u2014 documentation and the generated record; the automated-test run is itself the full battery.",
   "verify": "The bundle's manifest shows lint pass, tests pass (N = Totals), perf pass, complexity pass with 0 functions above CCN 15; diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty); README badge = Totals; docs/automated-tests/RESULTS.md's newest row is the new stamp; test -f docs/automated-tests/<stamp>/ANALYSIS.md; git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (empty); `grep -n 'Version:' PartyFrameEnhanced.toc` still 1.0.1.",
   "smoke": "Full docs/smoke-tests.md pass in game at the owner's convenience before any release.",
   "finding_ids": [
    "PartyFrameEnhanced-A-17",
    "PartyFrameEnhanced-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-01",
    "PF-02",
    "PF-03",
    "PF-04",
    "PF-05",
    "PF-06",
    "PF-07",
    "PF-08",
    "PF-09",
    "PF-10",
    "PF-11",
    "PF-12",
    "PF-13",
    "PF-14",
    "PF-15",
    "PF-16",
    "PF-17",
    "PF-18",
    "PF-19",
    "PF-20",
    "PF-21",
    "PF-22",
    "PF-23",
    "PF-24",
    "PF-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-00",
   "repo": "MultiMeters",
   "milestone": "M3",
   "title": "Clear the 7 re-vendor red(s) RV-MM recorded, so every later M3 commit starts green",
   "change": "RV-MM copied LibKa0s v1.56.0 and recorded 7 failing test(s) in its commit body (`git log --grep=\"^RV-MM\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned MM-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM"
   ],
   "effort": "M"
  },
  {
   "id": "MM-01",
   "repo": "MultiMeters",
   "title": "The stand-down latch holds on every show path: WindowProto:Show, test-mode exit, /mm toggle and a window created while stood down",
   "change": "(1) modules/Window_Placement.lua:231 WindowProto:Show(): add first line `if NS.IsStoodDown and NS.IsStoodDown() then return false end` and `return true` at the end, with a comment that an explicit Show never overrides the latch (slash-commands-\u00a77, performance-\u00a76). (2) modules/WindowManager.lua:659 SetTestMode: replace `not (NS.Perf and NS.Perf.suspended)` with `not (NS.IsStoodDown and NS.IsStoodDown())`. IsStoodDown (core/LifecycleSetup.lua:270) covers both the disabled and the perf hold, so it replaces the perf-only check. Rewrite the :647-650 comment to say 'NOT while stood down'. (3) modules/WindowManager.lua:690-708 M:Toggle: first statement `if NS.IsStoodDown and NS.IsStoodDown() then return false, L[\"Windows are suspended while a performance capture runs.\"] end`. The disabled case never reaches Toggle, because the slash gate (settings/Slash.lua:254) and the launcher (MM-02) refuse first, so the only live caller that reaches this refusal is the perf hold. Check that doToggle in settings/Slash.lua prints `err` when Toggle answers false, and make it print if it does not. (4) modules/Window.lua:1382 Window.New: `if not (NS.IsStoodDown and NS.IsStoodDown()) then inst.frame:SetScript(\"OnUpdate\", onUpdate) end`. Stand-up re-arms it through WindowManager:Resume; confirm that Resume arms every instance, including one created while stood down. (5) locales/enUS.lua: add L[\"Windows are suspended while a performance capture runs.\"]. Window.lua goes 1424 -> about 1427.",
   "tests": "Write these first in tests/test_disabled.lua (647 lines) and confirm each is red on the current code. (a) 'Disabled 10: unticking Test mode while disabled re-shows nothing': enable, disable through NS.SetByPath('enabled', false), WindowManager:SetTestMode(true) then (false), and assert that no instance frame IsShown(). It is red under reverting either the Show() guard or the :659 change. (b) 'Disabled 11: /mm toggle under a perf suspend shows nothing and says why': take the perf hold (NS.lifecycle:Set('perf', true), or the Perf suspend seam test_lifecycle uses), run NS.Slash:OnSlash('toggle'), and assert no frame is shown and the last chat line equals the new locale string. (c) 'Disabled 12: a window created while disabled carries no OnUpdate, and enable arms it': disable, WindowManager:Create(), and assert frame:GetScript('OnUpdate') == nil. Then enable, and assert every M.All() frame has one. Keep Disabled 3, 3b, 4, 5 and test_lifecycle.lua:288 green. Kit 26's CreateFrame starts shown, so assert on the window frames the paths touch, never on a global shown count.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +3 cases); ka0s-bounded lua5.1 tests/perf.lua (exit 0; the suspended scenario stays 0.0 B/iter); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v '^libs/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output); grep -n 'IsStoodDown' modules/Window_Placement.lua modules/WindowManager.lua modules/Window.lua (each file has a hit)",
   "smoke": "In client (SM-01, SM-02, SM-03; add them to docs/smoke-tests.md \u00a73 'Lock, drag, resize, Test mode'). SM-01: /mm disable, tick then untick Test mode in the General page, and no window appears. SM-02: /mm perf start and, during the suspended arm, /mm toggle: nothing shows and the suspend line prints. SM-03: /mm disable, create a window from the Windows page, /mm enable, and the new window refreshes.",
   "finding_ids": [
    "MultiMeters-R-02",
    "MultiMeters-R-04",
    "MultiMeters-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-02",
   "repo": "MultiMeters",
   "title": "Launcher left-click gates through Launcher minor 2's isEnabled/disabledLine, and the Slash stub carries a pinned DisabledLine",
   "change": "(1) core/LauncherSetup.lua:229-237: the onClick body becomes just the toggle (`local wm = NS.WindowManager or (NS.GetModule and NS:GetModule(\"WindowManager\", true)); if wm and wm.Toggle then wm:Toggle() end`). Add descriptor fields per LibKa0s docs/api/Launcher/version-2-docs.md. `isEnabled = function() return not (NS.IsStoodDown and NS.IsStoodDown()) end` is latch-inclusive, so a perf suspend refuses too. `disabledLine = function() if NS.IsDisabled and NS.IsDisabled() then local Sl = NS.Slash; return Sl and Sl.DisabledLine and Sl:DisabledLine() or nil end; return L[\"Windows are suspended while a performance capture runs.\"] end`, reusing MM-01's key. Rewrite the :200-228 comment block: the gate is the library's now; drop the false claim at :213-214 that Toggle writes 'shown'; keep the rung (a)/(c) reasoning. (2) settings/Slash.lua stub branch (:185-236), per LibKa0s docs/api/Slash/version-15-docs.md '## The degradation stub' (LK-18). Give the stub `SlashLib.DISABLED_LINE_FORMAT`, a verbatim byte copy of the live Slash member of the same name (use the member path that doc names), and `stub.DisabledLine = function() return SlashLib.DISABLED_LINE_FORMAT:format(<the brandName the descriptor passes>) end`. (3) settings/Slash.lua:743: publish `Sl:DisabledLine` only `if cli.DisabledLine then ... end` (R-06), so a stub without it can never raise through the wrapper. Update the :733-742 doc comment. (4) Surface the stub's format constant for the pin: the test reads it from a degraded load through `NS.Slash.__stubFormat`, a debug seam set only in the stub branch. Name the seam in the comment.\n\nThis item is the LK-16 consumer follow-up for MultiMeters-R-04. The hand check at core/LauncherSetup.lua:229-237 is removed. The Launcher descriptor carries isEnabled, which is latch-inclusive through NS.IsStoodDown so a perf suspend also refuses, and disabledLine. The gate is enforced by the library's Launcher minor 2 that LK-16 ships.",
   "tests": "Write these first and see them red. tests/test_launchersetup.lua (641): (a) 'launcher: a left click under a perf suspend shows nothing and prints the suspend line' (red today: onClick calls Toggle, which after MM-01 returns an err nobody prints); (b) 'launcher: a left click while disabled prints the Slash DisabledLine once and calls no Toggle' (spy on WindowManager.Toggle). tests/test_degraded.lua (716): (c) 'Degraded: with only LibKa0s-Slash missing, a disabled left click prints the refusal and raises nothing'. Load with libFiles = LIB_FILES minus Slash.lua, keeping Launcher.lua, disable, call the launcher descriptor's click path, and assert pcall ok, one chat line and no SavedVariables write (snapshot NS.db.profile before and after). This is red today with 'attempt to call method DisabledLine'. (d) 'Degraded: the stub's disabled-line format is the library's, byte for byte': T.assertLibraryConstant(degraded.NS.Slash.__stubFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT'). Optionally add one line to tests/test_surface_parity.lua's Slash note saying (c) covers it.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'isEnabled\\|disabledLine' core/LauncherSetup.lua (both present); grep -n 'IsDisabled' core/LauncherSetup.lua (only inside disabledLine)",
   "smoke": "In client (SM-11 + SM-02 launcher half; add to docs/smoke-tests.md \u00a714 'Slash surface'). /mm disable, left-click the minimap button: one refusal line and no window. /mm enable; /mm perf start, and during the suspended arm left-click: the suspend line and no window. Right-click opens settings in both states.",
   "finding_ids": [
    "MultiMeters-R-04",
    "MultiMeters-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-01",
    "LK-16",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-03",
   "repo": "MultiMeters",
   "title": "'Always show yourself' pins the player into the rows the window actually draws, scroll-aware",
   "change": "(1) modules/Aggregator.lua, beside ApplyRowLimit (:1289-1314): add `function Aggregator.SelfPinIndex(entries, first, visible, rowsConfig)`. It is pure and allocation-free. It answers nil when `not (rowsConfig and rowsConfig.alwaysShowSelf)`, when visible < 1, or when an entry with isPlayer sits in entries[first .. first+visible-1]. Otherwise it answers the index of the first isPlayer entry outside that slice, above it or below it. Keep ApplyRowLimit's own pin. (2) modules/Window.lua Render (:1164-1221, CCN 14): the CCN must not rise, so add NO inline branch. Add a file-local helper above Render, `local function slotEntry(entries, i, pin, lastIndex)`, which answers `entries[pin], pin` when pin and i == lastIndex, else `entries[i], i`. Add a method `WindowProto:SelfPin(entries, offset, isDrill)`, which answers nil in a drill-down and otherwise Aggregator.SelfPinIndex(entries, 1 + offset, self.layout.maxRows, (self.config or {}).rows). In Render, compute `local pin = self:SelfPin(entries, offset, isDrill)` and `local lastIndex = offset + layout.maxRows` once before the loop. Inside the loop, `local entry, rank = slotEntry(entries, i, pin, lastIndex)` and `row:Update(entry, rank)`. The pinned row then shows the player's real rank if the name cell draws one. Window.lua goes 1424 -> about 1445. Aggregator.lua goes 1360 -> about 1385.",
   "tests": "First, and red: tests/test_window.lua (1240), three cases. (a) Default window (maxRows=0, height 220, so layout.maxRows=10) with the player at rank 15 of 20: the 10th drawn row's entry is the player's, and its rank argument is 15 (spy on Row:Update). (b) scrollOffset=6 puts the player (rank 15) inside the slice: no pin, and row 9 is the player in its natural place. (c) alwaysShowSelf=false: row 10 is rank 10. tests/test_aggregator.lua (1255), one case: (d) SelfPinIndex answers nil in view, the index when above or below the slice, and nil when the flag is off. Wrap it in collectgarbage('count') before and after 1000 calls to assert zero growth. Case (a) reproduces the finding's headless repro: 'self-in-view=false'.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0; refresh20x7 keeps 8 API calls/iter and bytes/iter <= 303438.1); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output; lizard modules/Window.lua | grep Render shows CCN <= 14); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client (SM-04; add to docs/smoke-tests.md \u00a79 'Live ranking mid-pull'). Default window in a raid, ranked below the visible rows: your row sits in the last slot with your true rank. Scroll so you are in view: no duplicate row.",
   "finding_ids": [
    "MultiMeters-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-04",
   "repo": "MultiMeters",
   "title": "Chat export sends through Compat.ChatSender (C_ChatInfo first) and names a missing sender instead of silently self-printing",
   "change": "(1) core/Compat.lua: add `function Compat.ChatSender()`, resolved at call time: `local c = _G.C_ChatInfo; if c and c.SendChatMessage then return c.SendChatMessage end; return _G.SendChatMessage`. It is the only file allowed to name the deprecated global (compat.md). (2) modules/Export.lua:879: `local send = chatType and NS.Compat and NS.Compat.ChatSender and NS.Compat.ChatSender()`. At :885-889, when `chatType` is non-nil (a real channel was asked for) and `send` is nil, print one localized notice, L[\"This client has no way to send chat messages, so the export was printed to you instead.\"], before the self-print. SELF (chatType nil) stays silent. Update the :861-868 docstring. (3) locales/enUS.lua: the key. (4) .luacheckrc: add C_ChatInfo to read_globals if absent. (5) tests/test_surface_parity.lua: if assertSurfaceParity flags host-only members on NS.Compat, add 'ChatSender' to COMPAT_NOT_ON_NS_COMPAT's host-extras handling with a one-line reason: one consumer, so no LibKa0s-Compat member (anti-pattern #55). (6) docs/compat-layer.md and the docs/ARCHITECTURE.md compat-layer row: 29 -> 30 shims, and name ChatSender.",
   "tests": "First, red, in tests/test_export.lua (1275): (a) with both C_ChatInfo.SendChatMessage and _G.SendChatMessage mocked, a RAID export calls the C_ChatInfo one and never the global; (b) with neither present, a RAID export prints exactly one notice line followed by the export lines; (c) SELF with neither present prints the lines with no notice. tests/test_compat.lua: (d) ChatSender prefers C_ChatInfo, falls back to the global, and answers nil with neither.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'SendChatMessage' modules/*.lua | grep -v '^\\s*--' (no code hit outside comments); grep -n 'ChatSender' core/Compat.lua modules/Export.lua (both)",
   "smoke": "In client (SM-05; docs/smoke-tests.md \u00a714 or the export section). Export to PARTY in a group: lines arrive in party chat. Export to SELF: prints locally with no notice.",
   "finding_ids": [
    "MultiMeters-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-05",
   "repo": "MultiMeters",
   "title": "Renaming a window no longer closes its drill-down",
   "change": "modules/DrillDown.lua:850-855 OnWindowsChanged: exit only when `payload.action ~= \"renamed\"`. Write it as `local action = type(payload) == \"table\" and payload.action or nil` and `if id ~= nil and views[id] ~= nil and action ~= \"renamed\" then`. 'copied' (WindowManager.lua:499) and 'deleted' still exit, because a copy can change the target's columns. Add a one-line comment naming why.",
   "tests": "First, red, in tests/test_drilldown.lua (829): (a) drill into window 1, WindowManager:Rename(1, 'Renamed'), and the view is still open; (b) the same after CopyFrom(2, 1) exits, pinning that 'copied' still closes it.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +2); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client (SM-10; docs/smoke-tests.md \u00a710). Open a spell breakdown, rename the window from the Windows page: the breakdown stays open with the new title.",
   "finding_ids": [
    "MultiMeters-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-06",
   "repo": "MultiMeters",
   "title": "Window commands answer with the right error: unknown name, empty rename",
   "change": "modules/WindowManager.lua. Toggle unknown (:704) -> L[\"No window named '%s'.\"]:format(tostring(name)). That key already exists and settings/Slash.lua:574 uses it. Delete unknown (:272), Rename unknown (:307), Duplicate unknown (:324) and CopyFrom unknown source/target (:471-472) -> the same key formatted with the name the caller passed. Keep L[\"No window is selected.\"] only where the key argument is nil, which is the panel path with nothing picked. Rename empty (:309) -> new L[\"A window name cannot be empty.\"]; L[\"Window name\"] stays for its label use. locales/enUS.lua: add the new key, and confirm tests/test_locale.lua's 'every key read' gate stays green.",
   "tests": "First, red, in tests/test_windowmanager.lua (788): (a) Toggle('nope') answers false plus \"No window named 'nope'.\"; (b) Delete('nope'), Duplicate('nope') and CopyFrom('nope', 1) answer the same shape; (c) Rename(1, '') answers the empty-name sentence. In tests/test_slash.lua, if it asserts the old 'Setting not found' text for toggle, update that expectation in this commit.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +3); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'Setting not found' modules/WindowManager.lua (no hit)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-01",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-07",
   "repo": "MultiMeters",
   "title": "Every event registration goes through Core.SafeRegisterEvent with a rejected list reachable from /mm debug diag",
   "change": "(1) core/CoreSetup.lua. Live branch: publish NS.SafeRegisterEvent, NS.SafeRegisterUnitEvent and NS.SafeRegisterEvents = lib.<same> (LibKa0s-Core-1.0 minor 8, LK-11). Stub branch (:90-): one-rung bodies, as the Core version-8 doc's 'Degradation' note prescribes. `NS.SafeRegisterEvent = function(target, event, handler, rejected) local ok = pcall(target.RegisterEvent, target, event, handler); if not ok and rejected then <append once> end; return ok end`, with the Unit and plural forms the same shape and no front gate. (2) core/MultiMeters.lua: add a module-level `local EVENTS` array of {event, handler} for all 22 registrations at :149-213: the 21 bare ones plus PLAYER_IS_GLIDING_CHANGED. Keep each block's explanatory comment beside its entries. OnEnable resets `NS.State.rejectedEvents = {}`, then calls `NS.SafeRegisterEvents(self, EVENTS...)`, or loops NS.SafeRegisterEvent(self, e[1], e[2], NS.State.rejectedEvents) if the plural's array shape does not take per-event handlers. target stays `self`, so Disabled 3 and 9 still see AceEvent registrations. When #rejected > 0, emit one NS.Debug('Init', 'rejected events: %s', table.concat(rejected, ', ')). Delete registerIfValid (:124-132). Fix the :153-156 comment: an unknown name RAISES, which is why every registration is pcalled. (3) core/Diagnostics.lua: /mm debug diag prints 'rejected events: none' or the list (localized through L if the diag lines are). (4) docs/midnight-quirks.md: a short note on registration isolation and where to read the rejected list. docs/ARCHITECTURE.md ## Event subscriptions: one sentence naming the helper (events-frames-taint-\u00a71).",
   "tests": "First, red, in tests/test_lifecycle.lua (674): (a) with mocks.__badEvents = { UNIT_SPELLCAST_SUCCEEDED = true }, enable, and the three DAMAGE_METER_* events are still in __registrations() while NS.State.rejectedEvents == { 'UNIT_SPELLCAST_SUCCEEDED' }. It is red today, because the bare RegisterEvent raises and aborts every later registration. (b) With C_EventUtils = nil and the same bad event, the result is the same (pcall rung). (c) A disable/enable cycle resets the list rather than appending duplicates. tests/test_degraded.lua: (d) with no library and a bad event, the addon still enables end to end, and the stub records the name. tests/test_diagnostics.lua: (e) diag prints the rejected line.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +5); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output; OnEnable's CCN falls); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -c 'self:RegisterEvent' core/MultiMeters.lua (0); grep -n 'registerIfValid' -r core (no hit)",
   "smoke": "In client: /mm debug diag shows 'rejected events: none' on the live client.",
   "finding_ids": [
    "MultiMeters-A-20"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-08",
   "repo": "MultiMeters",
   "title": "Visibility:Evaluate runs only under debug and feeds /mm debug diag; LifecycleSetup's stand-down comment corrected",
   "change": "(1) modules/Visibility.lua:389-419 Evaluate: first line `if not (NS.State and NS.State.debug) then return end`. Keep the subscriptions at :461-471 and keep LastResult (:373), now read by a consumer: (2) core/Diagnostics.lua /mm debug diag adds one 'visibility' line per window: id, shown/hidden and the ladder reason from Visibility.LastResult(id), or 'not evaluated (debug was off)'. (3) core/LifecycleSetup.lua:102-106: rewrite the comment claiming standDownVisibility's Refresh 'asks the windows to go'. Evaluate hides nothing; the windows are hidden by the stand-down's own frame pass. Callers ignore Refresh's return (LifecycleSetup:106,173, Schema_Compose:58), so the early return is safe.",
   "tests": "First, red, in tests/test_visibility.lua (899): (a) with State.debug false, a ZONE edge makes zero NS.ShouldShow calls from Visibility:Evaluate (spy counter), which is red today; (b) with debug true, LastResult(id) answers the ladder's reason, keeping the existing :451-498 cases green by turning debug on in their setup. tests/test_diagnostics.lua: (c) diag prints one visibility line per window.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +3); ka0s-bounded lua5.1 tests/perf.lua (exit 0; no bytes/iter regression); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-09",
   "repo": "MultiMeters",
   "title": "Slash descriptor returns the write seam's refusal so CliSet/CliReset print it (Slash minor 15 contract)",
   "change": "No finding: this is a contract change in LibKa0s v1.56.0 (LK-17). CliSet now prints the seam's refusal when `set` answers false, instead of echoing the unchanged value. settings/Slash.lua:295: `set = function(path, v) if not NS.SetByPath then return false, NS.LIBKA0S_MISSING end return NS.SetByPath(path, v) end`. :301 `applyDefault = function(row) if not NS.ApplyDefault then return false end return NS.ApplyDefault(row) end`, and make NS.ApplyDefault (settings/Schema_Paths.lua:809-824) answer exactly false for a row with no default. Update the comment block above the descriptor.",
   "tests": "First, red: add a sibling suite tests/test_slash_refusal.lua (test_slash.lua is at 1345), registered in tests/run.lua after test_slash. (a) `/mm set window.frame.width -5` (validate refuses) prints the INVALID line plus the seam's reason and leaves the stored width unchanged; today it echoes the old value. (b) `/mm reset <a row with no default>` prints the library's NO_DEFAULT line.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +2); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client: /mm set window.frame.width -5 prints the refusal and the width is unchanged.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-10",
   "repo": "MultiMeters",
   "title": "The test mock reads the addon version from MultiMeters.toc instead of hard-coding 0.1.0",
   "change": "tests/wow_mock.lua:~1086: build M.__toc.Version from the '## Version:' line of root .. '/MultiMeters.toc' (the mock already receives root), falling back to '0.0.0' with a loud comment if the line is missing. Title and Notes stay literals. tests/test_envsetup.lua: rewrite the comments at :12, :27-30 and :164 that describe the fixture as '0.1.0'; its withTOC/mutate literals ('9.9.9') are unchanged. The frozen docs/automated-tests/20260916-184449/perf.json is not touched.",
   "tests": "First, red, in tests/test_envsetup.lua: 'the default fixture's Version is the TOC's' asserts NS.Version() equals the value read from MultiMeters.toc (1.0.0). It is red today at 0.1.0.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +1); ka0s-bounded lua5.1 tests/perf.lua (exit 0; its printed version is 1.0.0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n '0.1.0' tests/wow_mock.lua tests/test_envsetup.lua (no hit)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-11",
   "repo": "MultiMeters",
   "title": "Drop the removed global GetSpellInfo from .luacheckrc read_globals",
   "change": ".luacheckrc:69: remove 'GetSpellInfo'. The code reaches spell info only through Compat.GetSpellInfo (core/Compat.lua:78); test mocks write it as a table field, which is not a global read. If tests/test_lintconfig.lua enumerates read_globals, update it in this commit.",
   "tests": "A lint-config change with no runtime behaviour. The test is luacheck itself: the run stays 0/0. As a red proof, a scratch `local x = GetSpellInfo` in any module must now warn; revert it before committing.",
   "verify": "ka0s-bounded luacheck . (0 warnings / 0 errors); ka0s-bounded lua5.1 tests/run.lua (0 failed); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'GetSpellInfo' .luacheckrc (no hit)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-12",
   "repo": "MultiMeters",
   "title": "Migration stamp per savedvariables-\u00a71 v2.65.0: defaults declare schemaVersion 0, the runner owns the stamp",
   "change": "WS-03 adoption. (1) defaults/Profile.lua:818: `schemaVersion = 0`. Rewrite the :717 doc to say why: AceDB strips a stored value equal to its default, and backfills a declared default onto an unstamped legacy account, and 0 has neither problem. (2) core/Database.lua RunMigrations (:933-): `local v = tonumber(g.schemaVersion) or 0; if v < 1 then v = 1 end`. 0 means unstamped, a legacy or fresh account. v1 is the shipped shape, and every step is idempotent against a fresh default profile, so both start at v1. Loop: `local step = migrations[v]`, `step(db)`, then `g.schemaVersion = v + 1` (the runner advances the stamp only after the step returned without raising) and log. Delete the 15 `db.global.schemaVersion = N` lines inside migrations[1..14] (:346 ... :928). Keep the missing-step fallthrough, but log it. (3) Profile-scoped steps already walk every stored profile through allProfiles(db) (:271); keep that, and state the savedvariables-\u00a71 rule in the runner's header comment (:245-255). Fix the :117 comment ('backfills schemaVersion to the CURRENT value') to describe the 0 default. (4) Expose `Database.__migrations = migrations` as a documented test seam if none exists.",
   "tests": "First: a new suite tests/test_migrations.lua, registered in tests/run.lua after test_database (which is at 1470). (a) NS.defaults.global.schemaVersion == 0, which is red today. (b) A fresh install with no stored stamp runs every step and ends at CURRENT_DB_VERSION, and the seeded profile deep-equals the one a migration-free InitDB builds, so every step is idempotent against fresh defaults. (c) A step that raises leaves the stamp at its `from` value: replace Database.__migrations[3] with an erroring function for one run, then restore it in a guaranteed-run wrapper. (d) After the walk, the raw sv.global.schemaVersion is CURRENT and survives the kit 26 AceDB fake's default stripping. (e) A legacy v1-shaped account with no stamp, on two profiles, migrates both profiles.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +5; test_database unchanged and green); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -c 'db.global.schemaVersion = ' core/Database.lua (0)",
   "smoke": "In client: /reload on an existing account; /dump Ka0s_MultiMetersDB.global.schemaVersion shows the current version (the SV table name as the TOC declares it).",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "WS-03",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-13",
   "repo": "MultiMeters",
   "title": "US spelling 'minimize' everywhere authored, with the v15->v16 migration of the two stored keys; waiver cut to the one library icon key",
   "change": "(1) core/Database.lua: migrations[15] (v15 -> v16). For every profile in allProfiles(db), and every window table in profile.windows, where `w.frame` is a table: `if w.frame.minimized == nil then w.frame.minimized = w.frame.minimised end` and the same for showMinimize <- showMinimise, then nil both old keys. It is idempotent and writes no stamp (MM-12). CURRENT_DB_VERSION 15 -> 16, with the header list extended by 'v16 spells the window's minimize keys the US way'. (2) defaults/Profile.lua:130 showMinimise -> showMinimize and :148 minimised -> minimized. (3) settings/Schema.lua:634 path window.frame.showMinimize, label L[\"Show minimize\"]; :657 path window.frame.minimized, label L[\"Minimized\"]. Fix the comments at :578, :591 and :643-670. (4) modules/HeaderControls.lua:127 `{ key = \"minimize\", setting = \"showMinimize\", art = \"minimise\", ... }`: the art value stays, because it is LibKa0s-Media-1.0's catalog key (libs/LibKa0s/media/icons/minimise.tga), a library field name. Also update :285-287 (key test and the art return), :337 handler key and :343 write key. modules/Window_Header.lua: ApplyMinimised -> ApplyMinimized (:751, :767, :771) and every caller (modules/Window.lua:730, etc.); Window.lua :634, :992-1100 and Window_Placement.lua:131-135 read frame.minimized. core/Diagnostics.lua:370 key 'minimize'. (5) locales/enUS.lua:214 and :231: rename the keys to their US source strings, and update every call site in the same commit. (6) Prose, comments and README.md:50: correct across the authored scope, with a de-AI pass on the README sentence. (7) tests/prose_waivers.lua: delete every entry, and the docs/superpowers ones, which LK-07's kit now skips. Keep one entry per file that must spell the library icon key, citing localization-\u00a75's sanctioned 'library field name' shape. That is expected to be only modules/HeaderControls.lua, plus tests/test_headercontrols.lua if it asserts the art. Rewrite the header: no longer a DEBT. Delete the file if no entry remains. (8) The last remaining identifier renames are in tests: test_headercontrols, test_window_header, test_window_placement, test_schema, test_mediasetup and test_diagnostics_deathrecap.",
   "tests": "First, red, in tests/test_migrations.lua (from MM-12): (a) a v15 account with two profiles, each with two windows carrying frame.minimised=true / showMinimise=false, ends at v16 with minimized=true / showMinimize=false and the old keys nil in every window of every profile; (b) running the step twice is a no-op; (c) a fresh install has frame.minimized=false and no British key. tests/test_headercontrols.lua: (d) clicking the minimize control writes window.frame.minimized and draws the 'minimise' catalog art. Rename the existing cases' strings in the same commit, so docs/test-cases.md is regenerated in MM-DOCS.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); git grep -n -i 'minimis' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/automated-tests' ':!docs/revendor' ':!docs/superpowers' ':!docs/perf-analysis' ':!docs/test-cases.md' (only the art = \"minimise\" key lines and the waiver entry); NS.ValidateSchema() green (test_schema)",
   "smoke": "In client: /reload on an account with a collapsed window. It is still collapsed, and the Header page shows 'Show minimize'.",
   "finding_ids": [
    "MultiMeters-A-18",
    "MultiMeters-A-18a"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-12",
    "MM-00"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "MM-14",
   "repo": "MultiMeters",
   "title": "Adopt LibKa0s-Schema-1.0 minor 2 for the settings runtime: Set with window instanceId, SetMany with one announce, the registry and the bracket; delete the host copies (MultiMeters#52)",
   "change": "settings/Schema_Paths.lua (972 lines). (1) `local SchemaLib = LibStub and LibStub(\"LibKa0s-Schema-1.0\", true)` and one instance `S = SchemaLib:New{ rows = NS.Schema, resolveRoot = resolveRoot, announce = ..., announceBatch = ..., debug = ..., debugEnabled = ..., resetExempt = { [MINIMAP_PATH] = true }, L = ... }`. resolveRoot is the existing :179 function, reshaped to `function(parts, windowId) -> root, first, resolvedWindowId`, so the 'window.' prefix resolves against the id handed in or State.activeWindowId. announce(row, path, value, rid) runs announceWrite(row.page, rid). announceBatch(writes, rid) sends CONFIG_CHANGED ONCE, through announceWrite with the common page/id or nil, exactly as SetByPaths does at :786-795. (2) Delete the host primitives splitPath/readFrom/writeInto/copy (:97-132), the index/reindex/FindSchemaRow/RegisterSchemaRows registry (:245-280) and the bulk bracket bulkOpen/bulkClose/runBulk/sameValue/bulkBegin/bulkEnd/bulkRun (:549-634). The public names stay as shims so no caller moves: NS.FindSchemaRow = S:FindRow, NS.RegisterSchemaRows = S:AddRows, NS.GetSetting(path, id) = S:Get(path, id), NS.SetByPath(path, v, id) = S:Set(path, v, id), NS.ApplyDefault(row, id) = S:ApplyDefault(row, id), and NS.Bulk = { begin = S.BulkBegin, finish = S.BulkEnd, run = S.BulkRun } for settings/OptionsSetup.lua:258-263. NS.SetByPaths(writes, windowId, summary) maps each { path, value } onto { path =, value = } and calls S:SetMany(entries, { instanceId = windowId, act = <summary act>, scope = <summary scope> }). Split `summary` so the bracket line reads as today's (characterize it first). (3) The columns carve-out becomes a hidden row { path = 'window.columns', hidden = true, validate = <structural check>, normalize = normalizeColumns }, using row.normalize from Schema minor 2. The in-array refusal ('window.columns.2.width') stays, because no row matches that path. (4) The minimap carve-out moves into the row's own get/set closures. The composer builds the row from Schema_Compose.lua's decor at :786, so the closures go there and resolve NS.db.global.minimap at call time. They keep TODAY's polarity: the row value means shown, and the store is hide = not v plus the LibDBIcon refresh. MM-16 renames the path. (5) Degradation stub, when SchemaLib is absent: a runtime-completing stub in the shape LibKa0s docs/api/Schema/version-2-docs.md 'The degradation stub' prescribes, modelled on BankLedger/settings/Schema.lua:441- and trimmed to what this addon calls: SplitPath, Read, Write, SameValue, and New -> { Get, Set, SetMany, FindRow, AddRows, ApplyDefault, BulkBegin, BulkEnd, BulkRun }. It is log-silent and honors resolveRoot, validate, normalize, onChange and announce/announceBatch; the header comment names each trimmed member. Keep ValidateSchema (:943-960) and sameDefault/checkMinimapRow/checkTreeRow host-side. Schema_Paths.lua should land around 880 lines. (6) tests/run.lua LIB_FILES EXPECTED list: add \"Schema.lua\" with a one-line reason. (7) docs/schema.md and docs/ARCHITECTURE.md ## Settings schema: the runtime is the library's, what the descriptor supplies, and the stub. (8) After the commit: `gh issue comment 52 -R tusharsaxena/MultiMeters` naming the commit, then `gh issue close 52` and swap the label to state:done, with the calls at least 5 s apart.\n\nThis item is the LK-22 consumer follow-up (MultiMeters#52). SetByPaths becomes S:SetMany, and its announceBatch fires CONFIG_CHANGED exactly once per batch, pinned by test (b). The host primitives (splitPath/readFrom/writeInto/copy), the registry (index/reindex/FindSchemaRow/RegisterSchemaRows) and the bulk bracket are deleted from settings/Schema_Paths.lua. Single-path writes go through S:Set (NS.SetByPath = S:Set), which LK-22 makes expressible.\n\nPLAN-REVIEW CORRECTION: LibKa0s-Schema-1.0 instance members are dot-defined (LibKa0s/Schema.lua: `function S.Get(path, instanceId)` ...). Write the shims in dot form: NS.FindSchemaRow = S.FindRow; NS.RegisterSchemaRows = function(rows) S.AddRows(rows) end; NS.GetSetting = function(p, id) return S.Get(p, id) end; NS.SetByPath = S.Set; NS.ApplyDefault = function(row, id) return S.ApplyDefault(row, id) end; and S.SetMany(entries, { instanceId = windowId, act = ..., scope = ... }). `NS.X = S:FindRow` is not valid Lua.",
   "tests": "First, green against the host code, as a characterization: a new suite tests/test_schema_batch.lua (test_schema is at 1350), registered after test_schema_paths. (a) SetByPaths with entry 2 invalid stores nothing and answers false, err. (b) A valid 3-entry SetByPaths sends exactly one CONFIG_CHANGED. (c) With a summary it logs exactly one '[Set] <summary>: N rows' line. (d) 'window.frame.width' written with windowId 2 lands in window 2, not the active window. (e) A 'window.columns' whole-array write is normalized and deep-copied; 'window.columns.2.width' is refused. (f) The minimap row reads shown and stores hide inverted. (g) Store-then-react ordering: record whether any onChange observes a later entry's stored value. SetMany stores and reacts per entry, so if (g) differs, document the change in the commit body and assert the new order. Then swap to the library and keep all of it green. tests/test_surface_parity.lua: (h) a new case, `T.assertSurfaceParity(<degraded instance's runtime>, <live instance>, 'NS.SchemaRuntime', { <trimmed members, each named> })`, both from real loads. tests/test_degraded.lua's row-count and path-for-path cases stay green.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +8); ka0s-bounded lua5.1 tests/perf.lua (exit 0; refresh20x7 bytes/iter <= 303438.1); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'local function splitPath\\|local function reindex\\|local function runBulk' settings/Schema_Paths.lua (no hit); gh issue view 52 -R tusharsaxena/MultiMeters --json state,labels (CLOSED, state:done)",
   "smoke": "In client: in the settings panel, change a Frame slider on window 2 while window 1 is active in the picker; only window 2 moves. Copy settings between windows from the Windows page: one refresh, and one [Set] line in /mm debug. Minimap checkbox toggles the button.",
   "finding_ids": [],
   "issue_refs": [
    "MultiMeters#52"
   ],
   "depends_on": [
    "RV-MM",
    "MM-09",
    "MM-13",
    "LK-22",
    "MM-00"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "MM-15",
   "repo": "MultiMeters",
   "title": "Library-absent /mm enable and /mm disable write through the Schema seam's writeThrough list (options-ui-\u00a71 v2.65.0 route (a))",
   "change": "WS-02 route (a) with LK-23. (1) settings/Schema_Paths.lua: the SchemaLib:New descriptor, and the degradation stub from MM-14, both get `writeThrough = { \"enabled\" }` as data. `enabled` is the only composed Master-controls row a host verb writes: /mm lock writes each window's hand-written frame.locked row, and /mm test goes through WindowManager, so neither needs writeThrough (check with grep for CliSet/SetByPath on composed paths). The stub stores a writeThrough path raw at db.profile.enabled and refuses every other row-less path. announce: when `row.writeThrough and path == \"enabled\"`, call NS.SyncEnabledHold(). The composed row's onChange (Schema_Compose.lua:712-724) is absent on these loads, so this is the latch reaction. (2) settings/Slash.lua doEnabled (:406-408): when the Slash library is absent (a file-local `degraded` flag set in the stub branch), call `local ok, err = NS.SetByPath(\"enabled\", want)` and print L[\"enabled = %s\"]:format(tostring(want)) on success, or err. It never raises. The live path keeps cli:CliSet. (3) docs/ARCHITECTURE.md: record that enable/disable take WS-02 route (a) on a library-absent or partial load, and replace any register row that said the degraded enable prints 'set is unavailable'.",
   "tests": "First, red, in tests/test_degraded.lua (716). (a) With the library absent, `/mm disable` stores enabled=false, NS.IsDisabled() is true, every registration is gone (Disabled-3 style), and no Lua error. Red today: it prints '/mm set is unavailable' and stores nothing. (b) `/mm enable` then restores registrations. (c) A partial load (libFiles without Options*.lua, Schema present) gives the same result as (a), through the live instance's writeThrough. (d) `/mm set some.unknown x` on the stub is refused, never stored.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-14",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-16",
   "repo": "MultiMeters",
   "title": "Minimap row's CLI path reads in its own shown sense: global.minimap.shown, storage unchanged at minimap.hide",
   "change": "WS-06 (launcher-\u00a73 v2.65.0). settings/Schema_Paths.lua:222 MINIMAP_PATH = \"global.minimap.shown\". settings/Schema_Compose.lua:658 minimapPath = \"global.minimap.shown\". :786 decor key \"global.minimap.shown\", whose get/set closures (from MM-14) invert onto db.global.minimap.hide. Update the :780-785 and Schema_Paths :210-221 comments. There is no SavedVariables change and no migration: the stored key stays LibDBIcon's `hide`, and no `shown` key is ever stored (anti-pattern #81). settings/OptionsSetup.lua's reset exemption reads NS.MINIMAP_PATH and follows automatically. Sweep every other 'global.minimap.hide' path mention (63 hits across code, tests and docs outside the frozen bundles): docs/schema.md, docs/settings-panel.md, docs/ARCHITECTURE.md, README.md and the tests.\n\nSavedVariables continuity for existing players (WS-06): only the CLI and schema path is renamed. The stored key stays db.global.minimap.hide (LibDBIcon's), which the v15 migration in core/Database.lua (:52, :850-891) already moved to the global store. An existing player's choice therefore carries over as it is, and no data migration step or CURRENT_DB_VERSION bump is added, because a stored `shown` key is forbidden (anti-pattern #81). Pin this with extra tests in tests/test_schema_batch.lua. (e) A legacy account loaded with db.global.minimap.hide=true reads `/mm get global.minimap.shown` as false, and the button stays hidden after load. (f) A pre-v15 account with profile.minimap.hide=true, run through the migration runner, also reads shown=false, and no `shown` key exists in the raw SV. If the implementer finds any path string persisted in SavedVariables (a saved reset-exempt list, a bookmark or similar), STOP and raise it with the owner: this plan adds no SavedVariables migration for the rename.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.\n\nPLAN-REVIEW CORRECTION: correct cites: CURRENT_DB_VERSION is core/Database.lua:53, and the step that moves minimap to the global store is migrations[14] (:895-935; the runner indexes by from-version). Test (f) seeds schemaVersion 14.",
   "tests": "First, red, in tests/test_schema_batch.lua (from MM-14): (a) `/mm get global.minimap.shown` answers true when minimap.hide=false; (b) `/mm set global.minimap.shown false` stores minimap.hide=true and hides the button (LibDBIcon mock); (c) `/mm get global.minimap.hide` is now an unknown path; (d) raw SV has no `shown` key after (b). Update the existing minimap cases' paths in the same commit.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); git grep -n 'minimap.hide' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/automated-tests' ':!docs/revendor' ':!docs/superpowers' (only storage-key mentions, never as a schema path)",
   "smoke": "In client: /mm set global.minimap.shown false hides the button; the General page checkbox follows; /reload keeps it hidden.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-14",
    "WS-06",
    "MM-12",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-17",
   "repo": "MultiMeters",
   "title": "scheduleTimer: document that Options ignores its return (OptionsWidgets minor 31) and pin the descriptor",
   "change": "LK-26 follow-up. settings/OptionsSetup.lua:266-272: keep `C_Timer.After(delay, fn)`. The optional NewTimer switch is declined: from OptionsWidgets minor 31 the library keeps its own armed flag, so a handle buys nothing, and C_Timer.After allocates no cancelable object. Rewrite the comment to say the 50 ms slider/color throttle now holds with a nil-returning wrapper, citing the Options version doc for key 24.31.x.",
   "tests": "In tests/test_options_panel.lua (1178): 'scheduleTimer schedules once through C_Timer.After with the given delay'. Call the descriptor's scheduleTimer and assert one mock C_Timer.After call with (0.05, fn), and that the return is ignored (nil). It guards against a future edit returning a handle it expects to be used. The library's throttle itself is pinned by LibKa0s's test_options_throttle and is not duplicated (testing-\u00a78).",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +1); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client: drag a column color in the Columns page's swatch; the window recolors smoothly without per-frame stutter.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-18",
   "repo": "MultiMeters",
   "title": "Columns page adopts RenderTabbedSchema opts.tabs for its bespoke block-editor tab (or files the decline); ReorderList drag smoke",
   "change": "LK-28 follow-up. settings/Columns.lua:321-345 render(): replace the hand-built H.TabStrip plus the if/elseif branch with H.RenderTabbedSchema(ctx, PAGE, <per LibKa0s docs/api/Options version doc>, opts). The opts are `tabs = { { key = TAB_COLUMNS, label = TAB_COLUMNS, render = renderBlockEditor, before = TAB_HEADER_TEXT } }` and `chrome = function(c) H.WindowBanner(c) end`. The two row tabs come from the page's group partition (TAB_HEADER_TEXT, TAB_HEADER_BG), with noHeadings kept if the library forwards RenderRows opts. The reorder cancel must still run before the scroll clear on every tab switch. Call NS.CancelReorder(ctx) at the top of render (unchanged), and confirm the library's tab switch re-enters render; the comment at :330-334 explains why that ordering matters. DECISION GATE: if the library's tab switch does not re-enter the host render, so the cancel cannot precede its scroll clear, or if it cannot express noHeadings, do not adopt. Instead file `gh issue create -R tusharsaxena/MultiMeters --label enhancement --label state:will-not-do --label severity:low` titled 'Decline RenderTabbedSchema opts.tabs for the Columns page', naming the missing hook, and commit only a comment at :321 citing the issue.\n\nThe smoke field's drag check is the LK-21 consumer follow-up. It is an in-client smoke of the library ReorderList drag in settings/ColumnBlocks.lua and needs no code change of its own. Run it and record it in docs/smoke-tests.md \u00a75 'Column editor' even if the RenderTabbedSchema adoption is declined at the decision gate.",
   "tests": "First, green against today's code, as a characterization in tests/test_columns.lua (292): (a) three tabs in order Columns, Header text, Header background; (b) selecting Header text renders exactly rowsOfGroup(TAB_HEADER_TEXT) with no headings; (c) switching away from Columns with a live reorder calls NS.CancelReorder before H.ClearScroll (spy order). Keep these green after the swap. If the item is declined, they stay as the pin.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +3); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'H.TabStrip' settings/Columns.lua (no hit if adopted)",
   "smoke": "In client (docs/smoke-tests.md \u00a75 'Column editor'). Switch the Columns page's three tabs. Then the LK-21 check: drag a column block by its handle mid-list and drop it lower. The insertion line draws in the list color, the order changes, a second drag in another window's list draws its own line, and no row's OnUpdate stays armed after drop (no stutter).",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "LK-21",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-19",
   "repo": "MultiMeters",
   "title": "Measure UNIT_SPELLCAST_SUCCEEDED and CHAT_MSG_SYSTEM: two perf buckets and their brackets",
   "change": "core/PerfSetup.lua:103-122 buckets: add { key = \"spellEvent\" } (the UNIT_SPELLCAST_SUCCEEDED handler, feign detection) and { key = \"systemEvent\" } (CHAT_MSG_SYSTEM, the whisper-to-nobody check), with a comment. core/MultiMeters.lua OnSpellSucceeded (:377) and OnSystemMessage (:371): `local t0 = Perf.on and debugprofilestop()` at entry and `if t0 then Perf.Note(\"spellEvent\"|\"systemEvent\", debugprofilestop() - t0) end` on every return path, matching the meterEvent brackets at :419-440. Keep each handler at or below CCN 15. Measurement only: the registration decision is deferred to the next cycle from the numbers.",
   "tests": "First, red, in tests/test_perfsetup.lua (638): (a) with perf armed, firing UNIT_SPELLCAST_SUCCEEDED once records one spellEvent sample and CHAT_MSG_SYSTEM records one systemEvent sample; (b) the existing 'every declared bucket is reached' gate, if present, now covers both. tests/perf.lua: add scenarios spellEventOff/On asserting 0 B/iter while disarmed.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +2); ka0s-bounded lua5.1 tests/perf.lua (exit 0, the new scenarios printed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client: /mm perf capture during a raid pull. The report lists spellEvent and systemEvent with calls and ms, and is recorded with /wow-addon:perf-analysis into docs/perf-analysis/<stamp>/ to inform the registration decision.",
   "finding_ids": [
    "MultiMeters-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-20",
   "repo": "MultiMeters",
   "title": "Render keeps rows bound across passes and Row:Update walks only live cells, gated on before/after in-client captures",
   "change": "MEASURE FIRST. (0) Record a baseline in-client capture (SM-07) with /wow-addon:perf-analysis, before any code change, and note render ms/call and renderRow ms/row. (1) modules/Window.lua Render (:1164-1221) and HideAll (:912-914). Keep `self.slots` (an array of acquired rows) bound across passes. Acquire only when drawn exceeds #self.slots and release only the surplus past `drawn`, via NS.Pool.Release per surplus row. Re-anchor (ClearAllPoints/SetPoint) only when `self.layoutVersion ~= row.__anchoredVersion`. ApplyConfig bumps self.layoutVersion. HideAll remains for stand-down and drill transitions. (2) modules/Row.lua ApplyLayout (:1311-1326): build `self.liveCells` (an ordered array reused across calls, cleared with a numeric loop, never reallocated) of the cells the layout shows. Row:Update (:1376-1378) iterates self.liveCells instead of pairs(self.cells). Row.lua must stay at or under 1500 lines, from 1469. If it would exceed that, first peel the cell-clear helpers (:1446-1455) into modules/Row_NameCell.lua's sibling. (3) Record a second capture with the same scenario. REVERT the item (no commit) if render ms/call does not fall, and record the no-go on the review finding's follow-up issue instead.\n\nPLAN-REVIEW CORRECTION: the peel target is a new modules/Row_Cells.lua holding RowProto:Release (Row.lua:1446-1455) and the cell-clear loop, added to MultiMeters.toc right after modules\\Row.lua with a LOAD-BEARING comment; and the `local live = {}` at Row.lua:1311 is replaced by the reused self.liveCells (no per-call allocation).",
   "tests": "First, red, in tests/test_window.lua (about 1300 after MM-03): (a) two consecutive Render passes over the same 10 entries make zero pool Acquire calls on the second pass (spy); (b) a pass with fewer entries releases exactly the surplus; (c) no SetPoint call on the second pass until ApplyConfig bumps layoutVersion. tests/test_row.lua (1490, at the edge): move (d) 'Update never touches a cell the layout hid' to a new sibling tests/test_row_cells.lua, registered after test_row. tests/perf.lua refresh20x7 must keep 8 API calls/iter and bytes/iter <= 303438.1, and should fall.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +4); ka0s-bounded lua5.1 tests/perf.lua (exit 0; refresh20x7 API calls 8, bytes/iter <= 303438.1); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output; wc -l modules/Row.lua <= 1500)",
   "smoke": "In client, REQUIRED (SM-07): the before and after /mm perf captures in the same content (a dungeon pull, solo target dummy as fallback), each recorded as a docs/perf-analysis/<stamp>/ bundle. The after bundle's render ms/call must be lower. Also check visually that rows do not flicker or misorder while ranks change mid-pull.",
   "finding_ids": [
    "MultiMeters-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-03",
    "MM-00"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "MM-21",
   "repo": "MultiMeters",
   "title": "Bound the remembered roster (db.global.roster), with the branch chosen by the SM-06 login observation",
   "change": "BLOCKED ON SM-06, the in-client observation that picks the branch: after a fight, log out fully, log back in, and check whether the meter still shows the previous data. Branch A (the meter is empty after a fresh login): modules/Roster.lua subscribes to ENTERING_WORLD (the bus message core/MultiMeters.lua:242 sends) and calls Roster.Forget() when payload.isLogin is true and isReload is not. Branch B (the data survives logout): in build() (:355-361), after recording, when the remembered count exceeds 4 * Const.MAX_ROWS, prune the byGuid entries (and their owner map) that are not in the live roster. Count with a numeric counter kept beside the map, so the prune never walks with # on a hash. Either way there is no SavedVariables write from a game event while disabled: the handler lives on the bus, which is torn down on stand-down. Document the choice in the Roster.lua header and in docs/ARCHITECTURE.md's non-setting state note.",
   "tests": "First, red, in tests/test_roster.lua (785). Branch A: (a) ENTERING_WORLD with isLogin=true empties byGuid, and isReload=true keeps it. Branch B: (b) after 4 * MAX_ROWS + 5 distinct members across builds, byGuid holds at most 4 * MAX_ROWS entries plus the live group, and every live member survives. Both: (c) while disabled, no write reaches db.global.roster.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +2); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "In client, BEFORE coding (SM-06; add to docs/smoke-tests.md \u00a72 '/reload integrity'): fight, /logout, log in, and note whether the meter still has data. That picks branch A or B. After: a /reload keeps the roster, and a fresh login behaves per the branch.",
   "finding_ids": [
    "MultiMeters-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-22",
   "repo": "MultiMeters",
   "title": "Route every user-facing line in settings/Slash.lua through the locale, with whole-sentence keys and a real plural",
   "change": "settings/Slash.lua: replace the raw strings at :415 (lock ack), :428 (test ack), :446-447 (reset positions, where '1 window'/'N windows' is concatenated; use two keys, L[\"Moved 1 window back to the center.\"] and L[\"Moved %d windows back to the center.\"]), :580, :632-633 (feign), :696-697 (tooltip), and also :193/:207 (the stub's version line), :232 (\"unknown command '%s'\"), :367, :556 and WINDOW_USAGE (:453-455), plus the degraded doEnabled ack from MM-15. Each becomes NS.L[\"<whole English sentence with %s/%d>\"]:format(...). Add every key to locales/enUS.lua and leave no enUS key unread. Cite localization-\u00a71 (routing SHOULD) in the comment, not \u00a72/\u00a73. The stub branch runs without the locale if NS.L is absent; keep a plain-English fallback there, via `local L = NS.L or setmetatable({}, { __index = function(_, k) return k end })`.",
   "tests": "First, red: add to tests/test_slash_refusal.lua (from MM-09; test_slash.lua is 1345). (a) With a spy locale (a metatable recording reads), `/mm lock on`, `/mm test`, `/mm reset-positions` with 1 and with 3 windows, `/mm debug feign` and `/mm debug tooltip` each read their key, and the plural uses the two distinct keys. tests/test_locale.lua's unread-key gate stays green.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +1); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -nE 'out\\(\"[A-Za-z]' settings/Slash.lua (no hit outside the documented fallback)",
   "smoke": "In client (SM-09): /mm lock, /mm test, /mm reset-positions with one window and with several print the same English sentences, with a correct plural.",
   "finding_ids": [
    "MultiMeters-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-15",
    "MM-09",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-23",
   "repo": "MultiMeters",
   "title": "Peel tests/test_provider.lua (1500, at the cap) by concern",
   "change": "Move, byte-identical apart from each new file's header, the death-recap probe rounds, GetRecap and the death/segment offset sections (tests/test_provider.lua ~:776-1214) into a new tests/test_provider_recap.lua, and the raw source-row field probe sections for issues #22/#24 (~:1215-1500) into a new tests/test_provider_fields.lua. Copy the shared file-top locals and helpers (:1-35) each file needs, and never share them through a global. Register both in tests/run.lua directly after test_provider. test_provider.lua drops to about 775 lines. Update the ARCHITECTURE.md files-by-band note if it names this file.",
   "tests": "none \u2014 a behaviour-neutral move. The proof is that the case count and names are unchanged: `lua5.1 tests/run.lua --list | sort` before and after differ only in suite-file attribution, and the pass count is identical.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; same total as before); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); wc -l tests/test_provider*.lua (each under 1000)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-24",
   "repo": "MultiMeters",
   "title": "TOC: at-line LOAD-BEARING comments for CoreSetup, PerfSetup and OptionsSetup, and a 'conventional' note per group",
   "change": "MultiMeters.toc. :51 core\\CoreSetup.lua gets '# LOAD-BEARING POSITION: defines NS.LIBKA0S_MISSING, which core\\DebugLogSetup.lua and core\\LauncherSetup.lua read at FILE SCOPE, so it precedes both.' :58 core\\PerfSetup.lua gets '# LOAD-BEARING POSITION: its descriptor calls NS.Version() at FILE SCOPE (core\\EnvSetup.lua must precede it) and takes lifecycle from LifecycleSetup above.' (Verify EnvSetup's line first.) :134 settings\\OptionsSetup.lua gets '# LOAD-BEARING POSITION: publishes NS.Helpers, which every page file below (settings\\General.lua:61 onward) captures at FILE SCOPE.' Add one '# conventional order within this group' note to each group whose internal order is not load-bearing (toc-file-\u00a75).",
   "tests": "First, red, in tests/test_loadorder.lua (292): pair assertions that CoreSetup precedes DebugLogSetup and LauncherSetup, EnvSetup precedes PerfSetup, and OptionsSetup precedes every settings page file. These are green today and pin the order; a scratch swap of any pair reddens them. Plus (b): each of the three lines is immediately preceded by a '# LOAD-BEARING' comment. That one is red today.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +2); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -c 'LOAD-BEARING' MultiMeters.toc (up by 3)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-22"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-25",
   "repo": "MultiMeters",
   "title": "Correct the stale and misplaced comments: ShouldShow doc, spell/system handler docs, ladder step numbers, load order, launcher",
   "change": "Comments only. core/MultiMeters.lua: move the ShouldShow doc block (:459-463) onto ShouldShow, so fighting() keeps only its own. Move the UNIT_SPELLCAST_SUCCEEDED doc (:346-361) onto OnSpellSucceeded (:377), leaving OnSystemMessage its own. Renumber the ladder steps 0, 1, ... contiguously (:500, :522 and any later ones). modules/Window_Placement.lua:202: point at the renumbered step, not the removed step 1. modules/HeaderControls.lua:302 and :372: settings/ loads AFTER modules/ (MultiMeters.toc :82-103 vs :124+). core/LauncherSetup.lua:213-214, if MM-02 left any trace: Toggle only shows and hides, and writes no 'shown'.",
   "tests": "none \u2014 comments only; luacheck and the suite must be byte-for-byte unaffected in behaviour (same pass count).",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; same total); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'STEP 2' core/MultiMeters.lua (preceded by a STEP 1); grep -n 'loads ahead of modules' modules/HeaderControls.lua (no hit)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-02",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-26",
   "repo": "MultiMeters",
   "title": "Citation sweep to filename-\u00a7N: space forms, \u00a7-less forms, and literal \\194\\167 in comments",
   "change": "A mechanical sweep outside libs/, tests/_kit/, the frozen docs/audits, docs/reviews, docs/automated-tests, docs/revendor and docs/perf-analysis bundles, and generated docs/test-cases.md. (1) 'library-stack \u00a78' -> 'library-stack-\u00a78' at docs/ARCHITECTURE.md:519, :671 and :679. In the same commit, change tests/test_texture_paths.lua:48 COLUMNBLOCKS_RULE to \"library-stack-\u00a78\". (2) Add \u00a7 to the 18 \u00a7-less forms (slash-commands-7, performance-6, localization-5 and similar) in core/LifecycleSetup.lua (x8), tests/test_disabled.lua (x7), modules/Export.lua:812 and tests/prose_waivers.lua. Do NOT touch markdown anchor slugs (e.g. '#files-by-layout-1-band'). (3) Replace the literal escape \\194\\167 with a raw \u00a7 on the 35 COMMENT lines only (e.g. core/MultiMeters.lua:135, core/Database.lua:1006, core/LauncherSetup.lua). String literals keep their escapes. Files stay UTF-8 and LF.",
   "tests": "none \u2014 comment/doc text. The three greps below each return 0, and the suite passes unchanged, including test_texture_paths' register assertion against the new COLUMNBLOCKS_RULE spelling.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; same total); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); git grep -nE '[a-z-] \u00a7[0-9]' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/automated-tests' ':!docs/revendor' (0); git grep -nE '\\b(slash-commands|performance|localization|library-stack|testing|launcher|layout|toc-file|documentation|options-ui|events-frames-taint|savedvariables)-[0-9]' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/automated-tests' ':!docs/revendor' ':!docs/test-cases.md' | grep -v '#' (0); git grep -n -- '--.*\\\\194\\\\167' -- ':!libs' ':!tests/_kit' (0)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-23"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-25",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-27",
   "repo": "MultiMeters",
   "title": "Ship the three owed Tier 2 docs: slash-dispatch.md, message-bus.md, profiles.md",
   "change": "MOVE, never copy: (1) docs/slash-dispatch.md takes docs/ARCHITECTURE.md ## Slash commands (:188-245, including '### Disabled'): the 18 verbs, the window sub-tree, the debug words, the disabled gate and live list, degraded behaviour (MM-02/MM-15) and the refusal lines. (2) docs/message-bus.md takes ## Message bus (:133-187): the 14-message catalog with sender, consumers and payload. (3) docs/profiles.md: the AceDBOptions page, the PROFILE_CHANGED fan-out, the reset-all veto, the global-vs-profile split (minimap, roster) and the migration runner's per-profile rule (MM-12). Leave a two-line summary and link in each hub section. Flip the three documentation-map rows (:475-477) to Present, with the trigger each meets: 8+ commands and a sub-tree, more than 10 messages, user-visible profiles. Update docs/module-map.md links if they pointed at the hub anchors.",
   "tests": "none \u2014 docs. tests/test_docmap.lua and tests/test_doc_structure.lua must stay green. If test_doc_structure pins the hub's mandated section names, the summary sections keep those headings.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; test_docmap and test_doc_structure green); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); ls docs/slash-dispatch.md docs/message-bus.md docs/profiles.md; grep -c 'Not applicable' on the three map rows (0)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-15",
    "MM-12",
    "MM-26",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-28",
   "repo": "MultiMeters",
   "title": "Slim the hub under 560 lines: the texture census moves to docs/texture-paths.md, and the two catalog declines get register rows",
   "change": "(1) Create docs/texture-paths.md (Tier 3) and move '### Hard-coded texture paths' (docs/ARCHITECTURE.md:621-681) into it. Register it under the documentation map's 'Addon-specific (documentation-\u00a73, Tier 3)' table. Repoint tests/test_texture_paths.lua's CENSUS_HEADING source file to docs/texture-paths.md; REGISTER_HEADING stays '## Documented deviations' in ARCHITECTURE.md. (2) A-25, option (b) ratify, with the owner's option (a) noted. Add two rows to ## Documented deviations. First: | library-stack-\u00a78 | modules/Tooltip.lua:107 hard-codes Ability_Hunter_FocusedAim instead of the catalog's 'target' | A white alpha glyph in a column of colored spell icons reads as foreign | 2026-09-23 | the catalog ships a colored/iconic variant, or the tooltip stops drawing spell icons |. Second: | library-stack-\u00a78 | modules/Window.lua:645-646 hard-code the chat SizeGrabber -Up/-Highlight pair instead of the catalog's 'resize' | a two-state hover pair; the catalog publishes no hover variant | 2026-09-23 | LibKa0s-Media-1.0 gains a hover variant of 'resize' |. Point the two census rows at these register rows. Extend test_texture_paths' register assertion (today only ColumnBlocks) to require a library-stack-\u00a78 register row for the Tooltip.lua and Window.lua pairs. (3) Move '## The segment selector' (:350-393) to docs/data-flow.md, leaving a summary and link. The target is at most 560 lines.",
   "tests": "First, red, in tests/test_texture_paths.lua: the register assertion requires the two new rows. It is red until they are added, and red under deleting either. Existing both-direction census cases stay green against the new file.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +1); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); wc -l docs/ARCHITECTURE.md (<= 560)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-06",
    "MultiMeters-A-25"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-27",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-29",
   "repo": "MultiMeters",
   "title": "Doc figures: the hub's inventory line, and a dated statement beside the historical feign figure",
   "change": "(1) docs/ARCHITECTURE.md:14: 'Fifty-eight ... 19 core/', matching :45 and docs/module-map.md:6. Re-derive at commit time with `git ls-files '*.lua' | grep -v '^libs/\\|^tests/' | wc -l` and `git ls-files 'core/*.lua' | wc -l`, since MM-01..MM-23 may move the count (no new source file is planned). (2) docs/performance.md:289-290: keep the historical sentence as the closing run's figures, and add a separate dated line: 'Re-measured 2026-MM-DD with tests/perf.lua: feignTraceAbsent and feignTraceOff both X bytes/iter; the arms are still equal.' X is the value printed by a fresh run in this commit, 71544.1 as of 2026-09-23 unless later items moved it.",
   "tests": "First, red, in tests/test_doc_structure.lua (272): 'the hub's file count matches git ls-files' parses the spelled number word, or a digits form if the sentence is rewritten, and compares it. It is red today at 57/18.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed, +1); ka0s-bounded lua5.1 tests/perf.lua (exit 0; note the feign figures); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-24",
    "MultiMeters-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-28",
    "MM-19",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-30",
   "repo": "MultiMeters",
   "title": "Consolidated re-vendor span bundle for the unrecorded LibKa0s tags (v1.18.0 ... v1.54.2)",
   "change": "WS-01/WA-01 format. (1) Derive the carried tags: `git log --format=%h --since='2026-08-25 00:00' -- libs/LibKa0s tests/_kit | while read h; do git show $h:CLAUDE.md | grep -oE 'LibKa0s\\]\\([^)]*\\) v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -oE 'v[0-9.]+$'; done | sort -uV`. Subtract the recorded set: folder names under docs/revendor/ plus every vX.Y.Z on line 1 of each 01_DELTA.md (v1.15.0, v1.25.0, v1.30.0, v1.31.0-v1.34.0, v1.55.0). Also subtract v1.56.0, which RV-MM's own bundle records. Expect about 28 tags from v1.18.0 to v1.53.0, plus the kit-only v1.54.2 (d2169d4). (2) Write docs/revendor/2026-MM-DD-v<first>-v<last>/, expected '-v1.18.0-v1.54.2'. Its 01_DELTA.md line 1 is exactly `Delta: LibKa0s v1.15.0 -> v1.54.2 (span: <every unrecorded tag, comma-separated>)`, followed by a per-tag table: tag, carrying commit, date, and payload (lib, kit or both). 05_SUMMARY.md records the carrying commit and any adoption per tag, found by reading each commit subject/body. 02_CANDIDATES/03_DECISIONS/04_EXECUTION_PLAN each say 'retrospective: decisions were taken in the carrying commits, listed in 05'. Do not back-fill a folder per tag, and do not edit any frozen earlier bundle. If an earlier bundle misstated its base, note the correction once in this 01_DELTA.md.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none \u2014 a docs record. Its check is the WS-01 AUDIT re-vendor comparison: provenance tags since the horizon minus recorded tags (folder names plus line-1 tags) is empty, apart from v1.56.0's own RV bundle.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; prose gate green on the new bundle); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); comm -23 <(provenance tags) <(recorded tags) (empty)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "WA-01",
    "MM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "MM-31",
   "repo": "MultiMeters",
   "title": "Release-record hygiene: README records the 1.0.1 re-publish; closed issues #4/#21/#24 get terminal labels",
   "change": "(1) README.md Version History (:101-104): add a '1.0.1' row, 'Re-published 1.0.0 unchanged (a rebuild trigger); the TOC still read 1.0.0'. Add a note in docs/testing.md's release section: the next release goes through /wow-addon:bump-version, to 1.1.0 because v16's schema migration (MM-13) has landed, and the four-suite battery passes before tagging. No version is bumped here; the owner decides. (2) GitHub, spaced at least 5 s apart. #4: `gh issue edit 4 -R tusharsaxena/MultiMeters --remove-label state:triaged --add-label state:done`; its closing comment says it shipped through LibKa0s-Media-1.0. #21: the same, state:done, shipped as ReorderList in v1.19.0. #24: --remove-label state:untriaged --add-label state:done; it no longer reproduces, and the probe fix it asked for shipped (last comment). Name the three relabels in the commit body.",
   "tests": "none \u2014 docs and issue labels. The check is the queries below.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); gh issue list -R tusharsaxena/MultiMeters --state closed --label state:triaged (empty); gh issue list -R tusharsaxena/MultiMeters --state closed --label state:untriaged (empty)",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-21",
    "MultiMeters-A-16"
   ],
   "issue_refs": [
    "MultiMeters#4",
    "MultiMeters#21",
    "MultiMeters#24"
   ],
   "depends_on": [
    "RV-MM",
    "MM-13",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "MM-DOCS",
   "repo": "MultiMeters",
   "title": "Sync docs, regenerate docs/test-cases.md and the badge, and record a fresh automated-test bundle",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md, docs/ARCHITECTURE.md and the topic docs. Cover counts (verbs, messages, shims 30, files, tests), slash/COMMANDS parity, the comment-citation check, the LibKa0s v1.56.0 provenance line and kit revision 26 wording, and the Schema adoption (MM-14) across docs/schema.md and module-map.md. (2) Regenerate docs/test-cases.md with `lua5.1 tests/run.lua --list` in the kit's inventory format. Kit 26 case names carry \u00a7, and MM-13's renames also change names. Update the README test badge from its Totals. (3) Run `tests/_kit/run-automated-tests.sh`, which writes docs/automated-tests/<YYYYMMDD-HHMMSS>/ and rolls RESULTS.md with commit cells and the complexity watch list. The stale 20260916-184449 row, the CCN-19 ValidateSchema watch entry and the 0.1.0 perf version fall out. Write the bundle's ANALYSIS.md per the AUTOMATED_TESTS.md prompt and fill any watch-list Disposition cells. No version bump and no tag: the owner decides (MM-31 names 1.1.0).",
   "tests": "none \u2014 generated record and docs. The runner's four suites must all pass (lint, tests, perf, complexity with 0 above CCN 15) for the bundle to be recorded as clean.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed; total equals docs/test-cases.md Totals and the README badge); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty apart from the header); jq '.suites' docs/automated-tests/<stamp>/manifest.json (all pass); test -f docs/automated-tests/<stamp>/ANALYSIS.md",
   "smoke": "",
   "finding_ids": [
    "MultiMeters-A-26"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-00",
    "MM-01",
    "MM-02",
    "MM-03",
    "MM-04",
    "MM-05",
    "MM-06",
    "MM-07",
    "MM-08",
    "MM-09",
    "MM-10",
    "MM-11",
    "MM-12",
    "MM-13",
    "MM-14",
    "MM-15",
    "MM-16",
    "MM-17",
    "MM-18",
    "MM-19",
    "MM-20",
    "MM-21",
    "MM-22",
    "MM-23",
    "MM-24",
    "MM-25",
    "MM-26",
    "MM-27",
    "MM-28",
    "MM-29",
    "MM-30",
    "MM-31"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-00",
   "repo": "LootHistory",
   "milestone": "M3",
   "title": "Clear the 2 re-vendor red(s) RV-LH recorded, so every later M3 commit starts green",
   "change": "RV-LH copied LibKa0s v1.56.0 and recorded 2 failing test(s) in its commit body (`git log --grep=\"^RV-LH\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned LH-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH"
   ],
   "effort": "M"
  },
  {
   "id": "LH-01",
   "repo": "LootHistory",
   "title": "Clear the Mythic+ keystone context when the player leaves the party instance or the key resets",
   "change": "core/Compat.lua (beside GetActiveKeystoneLevel, :27-34): add `function Compat.InPartyInstance()`. It returns false when `IsInInstance` is not a function; otherwise `local inInst, kind = IsInInstance(); return (inInst and kind == \"party\") and true or false`. modules/Attribution.lua: (1) new `Attribution:OnZoneChanged()`. When `NS.Compat.InPartyInstance()` is false, set `State.keystone = nil`. When it is true and State.keystone is nil, read `local lvl = NS.Compat.GetActiveKeystoneLevel()` and set `State.keystone = { level = lvl }` only when `lvl and lvl > 0`; this re-arms a key the player zoned back into, because CHALLENGE_MODE_START does not fire again. Each branch writes a debug-gated `NS.Debug(\"Attr\", ...)` line. (2) New `Attribution:OnChallengeModeReset()`: `State.keystone = nil` plus a debug line. (3) OnChallengeModeCompleted (:235-243): `local lvl = NS.Compat.GetActiveKeystoneLevel(); if lvl and lvl > 0 then State.keystone.level = lvl end`, which replaces the `or` at :238 so a completion-time 0 is never stored. (4) Enable (:346-359): register `ZONE_CHANGED_NEW_AREA` -> OnZoneChanged and `CHALLENGE_MODE_RESET` -> OnChallengeModeReset on `bus`, and append both names to `self.__events` (nine names), so Disable tears them down by name. Do NOT register PLAYER_ENTERING_WORLD on NS.addon: core/LifecycleSetup.lua:112 binds OnEnterWorld there. (5) Disable's docstring (:401-404) says nine shared-target events. .luacheckrc read_globals: add \"IsInInstance\". tests/test_attribution.lua: ENABLE_EVENTS (:348-351) gains CHALLENGE_MODE_RESET and ZONE_CHANGED_NEW_AREA (kept sorted); the case at :396 is renamed from 'seven' to 'nine'; the comments at :333 and :345 say nine. tests/test_disabled.lua OWNED (:69-86) gains \"event:ZONE_CHANGED_NEW_AREA:nil\" and \"event:CHALLENGE_MODE_RESET:nil\". Docs: docs/data-flow.md:88 and docs/midnight-quirks.md:22 describe the lifetime (kept through completion, cleared on leaving the party instance or on CHALLENGE_MODE_RESET, re-armed on re-entry to an active key). docs/ARCHITECTURE.md ## Event subscriptions gains both events. The performance-\u00a712 register row (ARCHITECTURE.md:485, 'all thirteen registrations') and the docs/performance.md sweep count move to fifteen, and each new handler is described as one API call per zone change. The commit body notes that existing mislabelled MPLUS rows cannot be repaired, because no signal exists.",
   "tests": "Test first, in tests/test_attribution.lua. Five new cases, all red today because the handlers do not exist (a nil method call) or because of the `or` at :238. (a) With State.keystone={level=12} and M.__context.inInstance=false, OnZoneChanged() is followed by ResolveLootSource(OBJECT) -> CONTAINER. (b) With inInstance=true and instanceType='party', the keystone is kept -> MPLUS 12. (c) OnChallengeModeReset() clears it. (d) A GetActiveKeystoneInfo mock answering 0 at completion leaves level 12. (e) Re-entry: keystone nil, a party instance, and GetActiveKeystoneInfo answering 15 means OnZoneChanged re-arms level 15. Save and restore M.__context and C_ChallengeMode in each case. Then update the Enable case to nine events and test_disabled's OWNED list; both are red until Enable registers the two events.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md && git diff --stat docs/test-cases.md (+5 cases; README tests badge moved to match) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l modules/Attribution.lua core/Compat.lua tests/test_attribution.lua (each under 1500). grep -n 'ZONE_CHANGED_NEW_AREA\\|CHALLENGE_MODE_RESET' modules/Attribution.lua docs/ARCHITECTURE.md. There is no tests/perf.lua (performance-\u00a712 exemption), so no perf run.",
   "smoke": "S-001 (in client, `/lh debug on`): run a key, complete it, and loot the reward chest; it records as MPLUS. Leave the dungeon, then mine an ore node or pick a herb: it records as CONTAINER, and the debug console shows the 'keystone cleared' line on the zone change. Zoning out and back in mid-key keeps chest and object loot as MPLUS.",
   "finding_ids": [
    "LootHistory-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-02",
   "repo": "LootHistory",
   "title": "Keep the encounter context for a grace window after a successful ENCOUNTER_END, so boss-corpse loot carries encounterID",
   "change": "core/Constants.lua (beside C.CONTEXT_TTL, :67): `C.ENCOUNTER_GRACE = 60`, with a comment saying the corpse is looted after ENCOUNTER_END and that trash KILL loot within the window also carries the id (accepted). modules/Attribution.lua: the ENCOUNTER_END registration (:351) forwards varargs: `function(...) self:OnEncounterEnd(...) end`. OnEncounterEnd(_, encounterID, _, _, _, success) (:223-226): when `success == 1 and State.encounter`, it sets `State.encounter.expires = GetTime() + Constants.ENCOUNTER_GRACE`; otherwise (a wipe, or no context) it sets `State.encounter = nil`. The debug line names the outcome. ResolveLootSource (:163-180): `local enc = state.encounter; if enc and (enc.expires == nil or (state.now or GetTime()) <= enc.expires) then detail.encounterID = enc.id; detail.difficulty = enc.difficulty end`. The pure resolver takes `now` through state so tests do not depend on the clock. OnEncounterStart still replaces the table, so a new pull drops the old expiry. Docs: docs/data-flow.md:87 and docs/midnight-quirks.md:17 describe the grace window and the wipe clear.",
   "tests": "Test first, in tests/test_attribution.lua. Three cases. (a) state={encounter={id=2902, difficulty=16, expires=100}, now=90}: KILL detail carries encounterID 2902. (b) now=101: detail.encounterID is nil. (c) OnEncounterEnd(nil, 2902, 'x', 16, 5, 0) (a wipe) leaves NS.State.encounter nil, while success=1 leaves it set with `expires`. Case (c) is red today because OnEncounterEnd always nils the context; (a) and (b) are red because the resolver ignores expires, which (b) exposes.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+3; README badge moved) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l modules/Attribution.lua tests/test_attribution.lua. No tests/perf.lua (performance-\u00a712 exemption).",
   "smoke": "S-002, a checkpoint to run before merging (`/lh debug on`). Kill a boss and loot the corpse. The console shows 'encounter end' BEFORE 'LOOT_OPENED', and the recorded row's sourceDetail carries encounterID (check through /dump LootHistoryDB.global.history[#LootHistoryDB.global.history].sourceDetail). If LOOT_OPENED arrives first in the client, the finding is not reproduced: revert the code half and keep only the doc correction.",
   "finding_ids": [
    "LootHistory-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-01",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-03",
   "repo": "LootHistory",
   "title": "Rebuild the currency-category cache once per missed id instead of freezing the first session snapshot",
   "change": "core/Compat.lua (:370-394): keep buildCurrencyCategoryCache. Add a module-local `local currencyCategoryMissed = {}`. Compat.CurrencyCategory(currencyID): after the lazy first build, `local h = currencyCategoryCache[currencyID]`. When h is nil and `not currencyCategoryMissed[currencyID]`, set `currencyCategoryMissed[currencyID] = true`, call buildCurrencyCategoryCache(), and look up again. Return h, and keep unknown-id -> nil (the tests/test_compat.lua:338 contract). The sentinel means an id that is truly absent costs at most one list walk per session. Comment the collapsed-header limitation: C_CurrencyInfo.GetCurrencyListInfo enumerates only the children of expanded headers, so an id under a header the player collapsed is still missed. Deliberately do NOT call ExpandCurrencyList from a loot handler, because that mutates the player's Currency tab. docs/midnight-quirks.md gains a 'Currency category' entry stating the rebuild-on-miss rule and the collapsed-header gap, with the S-003 outcome recorded. docs/ARCHITECTURE.md ## Known limitations gains one line: such a row keeps itemSubType nil and falls out of the subtype filter.",
   "tests": "Test first, in tests/test_compat.lua. (a) Install a mock C_CurrencyInfo list holding header 'Season' plus id 100. Call CurrencyCategory(100) -> 'Season'. Then append id 200 under the same header and call CurrencyCategory(200) -> 'Season'. This is red today, which answers nil from the frozen snapshot. (b) An absent id 999 answers nil twice, and a counter on GetCurrencyListSize shows exactly one extra walk across the two calls. Restore the mock afterward. The cache is module-local, so reset it through a reload of core/Compat.lua via the harness loader, or order the case so its ids are fresh. State in the case which approach is used.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+2; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l core/Compat.lua tests/test_compat.lua. No tests/perf.lua.",
   "smoke": "S-003 (in client). At a season start, loot a currency never seen this session. The row's Subtype column shows its Currency-tab header. Then collapse a header in the Currency tab, /reload, and loot a currency under it; record in midnight-quirks.md whether the subtype resolves.",
   "finding_ids": [
    "LootHistory-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-04",
   "repo": "LootHistory",
   "title": "Confirm before a shorter 'Keep history for' deletes records; declining restores the previous retention",
   "change": "core/Database.lua (beside PruneOld, :786): add `Database:CountOlderThan(days)`. It answers 0 for nil or 0 days; otherwise it counts history rows with (r.ts or 0) < time() - days*86400, allocation-free. settings/Slash.lua StaticPopupDialogs block (:7-80): add `KA0S_LOOTHISTORY_PRUNE`. Its text is `Shorten 'Keep history for' to %s? %s older records will be deleted. This cannot be undone.`, with button1 YES, button2 NO, timeout 0, whileDead, hideOnEscape, showAlert and preferredIndex 3, matching the house pair. OnAccept(_, data) calls `NS.Schema:ConfirmRetention(data.days, true)`. OnCancel(_, data) calls `NS.Schema:ConfirmRetention(data.days, false)`. settings/Schema.lua: add a module-local `confirmedRetention`, `function S:SyncRetention()` (reads NS.db.global.settings.retentionDays into it), and `function S:ConfirmRetention(days, accepted)`. On accept it runs NS.Database:PruneOld() and sets confirmedRetention = days. On decline it sets a module-local `restoring = true`, calls `S:Set(\"settings.retentionDays\", confirmedRetention)`, sets `restoring = false`, and prints one line: `retention kept at <label>; no records were deleted.` The row's onChange (:370-375) becomes `function(value) S:OnRetentionChanged(value) end`. OnRetentionChanged: return while restoring. `local n = NS.Database:CountOlderThan(value)`. When n == 0, set confirmedRetention = value and return. When StaticPopup_Show is not a function, run PruneOld, set confirmedRetention = value and return. Otherwise `StaticPopup_Show(\"KA0S_LOOTHISTORY_PRUNE\", label, n, { days = value })`, where label is the RETENTION_OPTIONS text. core/LootHistory.lua OnInitialize calls `NS.Schema:SyncRetention()` right after NS:InitDB(). Sl:ResetEverything (settings/Slash.lua:180-200) calls it after wipeGlobal, because the raw wipe fires no onChange. The login prune (core/LootHistory.lua:81-84) is unchanged, because the stored value is already confirmed. docs/settings-panel.md (History tab): describe the confirm and the decline-restores behavior. The row keeps no options-ui-\u00a712 citation.",
   "tests": "Test first, in tests/test_schema.lua. The kit's StaticPopup_Show is a no-op function, so the popup-present path is the default. (a) Popup present: seed 3 rows older than 7 days and 2 fresh, spy on StaticPopup_Show, then call NS.Schema:Set('settings.retentionDays', 7). History still holds 5 rows; the spy saw 'KA0S_LOOTHISTORY_PRUNE' with n=3 and data.days=7. Red today: the onChange prunes immediately. (b) Accept: StaticPopupDialogs.KA0S_LOOTHISTORY_PRUNE.OnAccept(nil, {days=7}) leaves 2 rows. (c) Decline: after SyncRetention at 30, Set 7 then OnCancel(nil, {days=7}). retentionDays reads 30 again, 5 rows remain, and exactly one line is printed. (d) Popup absent: set T.mocks.StaticPopup_Show = nil; Set 7 prunes to 2. (e) Nothing older: no popup and no prune. Save and restore history, the setting and the mock in every case.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+5; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l settings/Schema.lua settings/Slash.lua core/Database.lua tests/test_schema.lua (each under 1500; test_schema stays in the 1000-1500 watch band). No tests/perf.lua.",
   "smoke": "S-004 (in client). With old records present, change Keep history for from 90 to 7 days. A confirm names the record count. No keeps every record and puts the dropdown back at 90 with one chat line. Yes deletes. `/lh set settings.retentionDays 7` shows the same confirm.",
   "finding_ids": [
    "LootHistory-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-05",
   "repo": "LootHistory",
   "title": "General visibility and the test-mode refusal decide from the combat edge, not InCombatLockdown()",
   "change": "modules/Browser.lua: `B:VisibilityAllows(inCombat)` (:1124-1138). When inCombat is nil it reads `UnitAffectingCombat and UnitAffectingCombat(\"player\") and true or false` in place of InCombatLockdown(). `B:ApplyVisibility(inCombat)` (:1142-1146) passes the argument through. The PLAYER_REGEN_DISABLED handler (:1246-1251) calls `B:ApplyVisibility(true)` and the PLAYER_REGEN_ENABLED handler (:1252) calls `B:ApplyVisibility(false)`. Every other caller (the settings onChange, Show, Toggle and the tab restore) passes nothing. Rewrite the :1134 comment: PLAYER_REGEN_DISABLED fires before lockdown engages (events-frames-taint-\u00a72), so the edge itself is the state. modules/BrowserTable.lua testModeRefusal (:542-549) reads UnitAffectingCombat('player') in place of InCombatLockdown(). .luacheckrc read_globals: add \"UnitAffectingCombat\". The commit body carries the release-note line, which bump-version rolls into Version History: 'Only out of combat now hides the History window at the pull'.",
   "tests": "Test first, in tests/test_browser.lua. (a) With InCombatLockdown mocked false (the real state at PLAYER_REGEN_DISABLED), visibility=outOfCombat and the window shown, `M.__fire(\"PLAYER_REGEN_DISABLED\")` hides the window. Red today, because VisibilityAllows reads lockdown=false. (b) The mirror case: with visibility=inCombat, M.__inCombat=true and the window shown, `M.__fire(\"PLAYER_REGEN_ENABLED\")` hides it. (c) BrowserTable:SetTestMode(true) with M.__inCombat=true and InCombatLockdown false is refused. Rewrite the four-mode case at :620-641 to drive M.__inCombat (UnitAffectingCombat) in place of InCombatLockdown. Its two halves then pin the nil-argument read, and the red-under comment names the InCombatLockdown read.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+3; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l modules/Browser.lua modules/BrowserTable.lua tests/test_browser.lua && grep -n 'InCombatLockdown' modules/Browser.lua modules/BrowserTable.lua (expect none). No tests/perf.lua.",
   "smoke": "S1 (in client). Set General > Window visibility to 'Only out of combat', open the window and pull a target dummy: the window hides at the pull. Set 'Only in combat' with the window open in combat: it hides when combat ends. Test mode cannot be started mid-fight.",
   "finding_ids": [
    "LootHistory-A-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-06",
   "repo": "LootHistory",
   "title": "The warbound repair warms the item cache from the link when a row has no itemID",
   "change": "core/Database.lua examineRow (:208-216), in the unsettled branch: `NS.Item.LoadItem(r.itemID or NS.Item.ItemIDFromLink(r.itemLink))`. NS.Item.ItemIDFromLink is published by core/ItemSetup.lua:41 on both paths. Nothing else changes.",
   "tests": "Test first, in tests/test_database.lua. Spy on T.mocks.C_Item.RequestLoadItemDataByID (restore it afterwards). Make NS.Compat.ItemBindState answer unsettled for the case, then run RepairBoundStates over one WARBAND-candidate row with itemID=nil and an itemLink for 258586. The spy saw 258586. Red today: LoadItem(nil) returns early, so the spy is never called.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-07",
   "repo": "LootHistory",
   "title": "Database:Export deep-copies auctionPrice and sourceDetail, as its contract says",
   "change": "core/Database.lua Database:Export (:430-445): `auctionPrice = r.auctionPrice and NS.Util.DeepCopy(r.auctionPrice)` and `sourceDetail = r.sourceDetail and NS.Util.DeepCopy(r.sourceDetail)`. The :430 comment already promises a plain copy. The cost is paid once per export.",
   "tests": "Test first, in tests/test_database.lua. Add a row with auctionPrice={tsm={dbmarket=100}} and sourceDetail={npcID=1}, export it, then mutate out[1].auctionPrice.tsm.dbmarket=1 and out[1].sourceDetail.npcID=2. The live row still reads 100 and 1. Red today because the nested tables are shared.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-08",
   "repo": "LootHistory",
   "title": "PruneOld fires HistoryChanged only when it removed rows",
   "change": "core/Database.lua PruneOld (:786-803): swap the history table and call `fireHistoryChanged()` only when `removed > 0`. With nothing removed the store is untouched, and the [Prune] debug line still logs. Update the :784 comment ('Fires HistoryChanged when it removes rows').",
   "tests": "Test first, in tests/test_database.lua. Use a recorded-message spy on a NS.NewBusTarget() subscribed to NS.MSG.HISTORY_CHANGED. First show that it records a positive send: PruneOld over a history holding an old row gives count 1. Then PruneOld over an all-fresh history gives count 0. The second half is red today because the fire is unconditional. The existing :309 case removes rows and stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-04",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-09",
   "repo": "LootHistory",
   "title": "One bind-state vocabulary across both CSV exports; comment the two sentinels",
   "change": "modules/Export.lua: delete BOUND_LABEL_CSV's own words (:140-143) and make the Insights CSV resolve bound labels through the row-CSV table (BOUND_LABEL, :11-14), with UNBOUND mapped to the NONE entry: `local function insightsBoundLabel(k) return E:BoundLabel(k == \"UNBOUND\" and \"NONE\" or k) end` at every read of BOUND_LABEL_CSV. The Insights CSV then says Bind on Pickup, Bind on Equip, Warbound Until Equipped and Not Bound, where it said Soulbound, BoE, Warbound (UE) and Unbound. core/Database.lua :357 and :524: keep both sentinels and add a one-line comment at each naming the other and why they differ. NONE is a persisted savedView.bound key; UNBOUND keys the Analytics and BrowserTable styling. The commit body carries the release note listing the four Insights CSV label changes.",
   "tests": "Test first. tests/test_export.lua:217 changes from 'A-Realm / Unbound' to 'A-Realm / Not Bound', an intended output change and red until the code moves. Add one case asserting that the Insights CSV labels for BOP, BOE and WARBAND_UE equal E:BoundLabel of the same token.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output; Export E is at CCN 15 today, so keep the helper outside it) && grep -n 'Soulbound\\|Warbound (UE)' modules/Export.lua (expect none). No tests/perf.lua.",
   "smoke": "S-008.1 (in client). Export Insights, then History, as CSV, and paste both into a text editor: bound labels read the same in both.",
   "finding_ids": [
    "LootHistory-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-10",
   "repo": "LootHistory",
   "title": "The resize grip honors Lock frame; keep the collection's Blizzard grabber under a library-stack-\u00a78 register row",
   "change": "modules/Browser.lua:1049: the grip's OnMouseDown becomes `function() if B:IsLocked() then return end frame:StartSizing(\"BOTTOMRIGHT\") end`. OnMouseUp stays as it is (StopMovingOrSizing on a no-op is harmless). Keep the UI-ChatIM-SizeGrabber textures (:1042-1048): the comment there records why the catalog 'resize' mark was reverted (every window corner in the collection uses the Blizzard grabber: BankLedger Browser/SessionWindow, MultiMeters Window). Ratify that choice with a docs/ARCHITECTURE.md ## Documented deviations row. Rule `library-stack-\u00a78`. What differs: the resize grip draws Interface\\ChatFrame\\UI-ChatIM-SizeGrabber-Up/Highlight where the LibKa0s catalog ships `resize`. Why: collection-wide corner consistency (cite Browser.lua:1042-1046 and the same grabber in BankLedger and MultiMeters); decided on the commit date. Re-check trigger: the standard rules the resize grip a catalog mark (an upstream standalone-windows or library-stack-\u00a78 ruling), or any other collection window adopts the catalog mark. The comment at :1042 points at the row. docs/settings-panel.md:40 and :191: Lock frame stops the window being dragged or resized, and it gates the title-bar OnDragStart and the grip's OnMouseDown.",
   "tests": "Test first, in tests/test_browser.lua. Build the window, set settings.locked=true, and spy frame:StartSizing. Firing the grip's OnMouseDown script (frame.resizeGrip:GetScript('OnMouseDown')()) must not call StartSizing. Red today. With locked=false it does call it.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -n 'library-stack-\u00a78' docs/ARCHITECTURE.md (the register row resolves). No tests/perf.lua.",
   "smoke": "S-007 (in client). With Lock frame ticked the grip does not resize the window; unticked, it does, and the size persists across /reload.",
   "finding_ids": [
    "LootHistory-R-09",
    "LootHistory-A-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-11",
   "repo": "LootHistory",
   "title": "Launcher: adopt Launcher minor 2's isEnabled/disabledLine gate; the tooltip uses NS.BRAND and shows the disabled line when off",
   "change": "core/LauncherSetup.lua. The descriptor (:57-121) gains `isEnabled = function() return not (NS.AddonIsOff and NS.AddonIsOff()) end` and `disabledLine = function() return NS.Slash.DisabledLine() end` (LibKa0s-Launcher-1.0 minor 2, v1.56.0, LK-16). onClick (:104-108) drops its own AddonIsOff branch and is just `if NS.Browser and NS.Browser.Toggle then NS.Browser:Toggle() end`, because the library now refuses a disabled left click and prints disabledLine() through the descriptor's print. Rewrite the comment block at :91-103 to say the gate is the library's and the line is still NS.Slash.DisabledLine. onTooltipShow (:111-119): `tt:AddLine(NS.BRAND, 1, 0.82, 0)`. When `NS.AddonIsOff()`, the left-click line is replaced by the gray `tt:AddLine(NS.Slash.DisabledLine(), 0.5, 0.5, 0.5)`, derived and never hand-built (LK-18 consumer follow-up). Otherwise it reads 'Left-click: show/hide the history window'. The right-click line is unchanged. docs/ARCHITECTURE.md and docs/module-map.md launcher notes: the gate moved to the library.",
   "tests": "Test first, in tests/test_launcher.lua. (a) Capture tooltip lines through a fake tt collecting AddLine text. Enabled: line 1 == NS.BRAND and a line reads 'Left-click: show/hide the history window'. Red today because of the 'open the history window' text. (b) With settings.enabled=false, the tooltip carries NS.Slash.DisabledLine() and no 'Left-click:' line. (c) The registered descriptor has isEnabled and disabledLine functions (red until adopted). tests/test_disabled.lua's 'left click is refused and writes nothing' case (:412) must stay green through the library gate.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+3; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -n '\"Ka0s Loot History\"' core/LauncherSetup.lua (expect none). No tests/perf.lua.",
   "smoke": "S-008.2 and S-008.3 (in client). Hover the minimap button enabled: brand title, 'show/hide' line. `/lh disable`, hover again: a gray disabled line and no left-click hint. Left-click prints the one refusal line; right-click still opens settings.",
   "finding_ids": [
    "LootHistory-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-12",
   "repo": "LootHistory",
   "title": "defaults declare schemaVersion 0; the runner owns the stamp (savedvariables-\u00a71, v2.65.0)",
   "change": "defaults/Global.lua:6-13: `schemaVersion = 0`. Rewrite the comment to cite savedvariables-\u00a71 (standard v2.65.0, WS-03). The defaults declare 0 because AceDB's removeDefaults strips a stored value equal to its default and backfills a declared default onto a legacy account; 0 has neither problem; the runner's highest `to` (8) is the version a migrated DB carries. core/Database.lua RunMigrations (:127-138): `g.schemaVersion = g.schemaVersion or 0`. The runner already stamps only after apply returns, which satisfies the stamp-advances-past-a-successful-step rule. Rewrite the :119-124 comment likewise. Every MIGRATIONS step is already idempotent against an empty history (2..8 each guard on content), and the commit body records that check. This addon has no profile section, so the per-profile rule does not apply; docs/schema.md says so in one line and documents the 0 floor. No Documented deviations row is needed: the versioning-git conflict LootHistory-A-18 names is resolved by the standard itself.",
   "tests": "Test first, in tests/test_database.lua. (a) `assertEqual(NS.defaults.global.schemaVersion, 0)`, red today at 1. (b) A fresh store with schemaVersion = 0 (what AceDB backfills) walks to 8 and every step's migrateLog line reads v1->v2 .. v7->v8. The existing :440-455 cases (absent -> 8) stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+2; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -n 'schemaVersion = 0' defaults/Global.lua. No tests/perf.lua.",
   "smoke": "S (in client). On a fresh character with LootHistoryDB deleted, log in, /reload, then `/dump LootHistoryDB.global.schemaVersion` answers 8. An existing account still answers 8 and keeps its history.",
   "finding_ids": [
    "LootHistory-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "WS-03",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-13",
   "repo": "LootHistory",
   "title": "The minimap row's CLI path reads in its own shown sense: minimap.hide -> minimap.shown (launcher-\u00a73, v2.65.0)",
   "change": "The stored key stays `db.global.minimap.hide`, so no SavedVariables change and no migration. settings/Schema.lua: `minimapPath = \"minimap.shown\"` (:91). The stamp key `[\"minimap.hide\"]` becomes `[\"minimap.shown\"]` (:213), and its get and set still invert onto mm.hide and call NS.Launcher:SetShown. RESET_EXEMPT (:434) becomes a map from row path to stored path: `S.RESET_EXEMPT = { [\"minimap.shown\"] = \"minimap.hide\" }`. Keys are what the library's resetExempt veto and traceSettingsReset test; values are what the raw wipe carries. S:Register (:618-625): `defaultsRoot = function(_, row) if row and type(row.get) == \"function\" and type(row.set) == \"function\" then return nil end return g, 1 end`, so a row that owns its storage is neither resolved nor missing (the library's resolvesInDefaults answers nil). The host stub's R.Validate (:565-578) gets the same skip. settings/Slash.lua wipeGlobal (:119-131): `for _, storedPath in pairs(S.RESET_EXEMPT) do kept[storedPath] = S:ReadPath(g, storedPath) end`, with the write-back loop unchanged. Rewrite the comments at Schema.lua:79-91, :205-213 and :417-434, Slash.lua:113-118, :148 and :377-378, and OptionsSetup.lua:182: the row path is minimap.shown, the one stored key is minimap.hide, and no `shown` key is ever stored (anti-pattern #81). Tests: every `\"minimap.hide\"` used as a schema path in tests/test_launcher.lua, test_panel.lua, test_schema.lua, test_slash.lua and test_util.lua moves to \"minimap.shown\" with the value sense inverted where Set or Get is asserted. Assertions on the raw NS.db.global.minimap.hide stay. Docs: settings-panel.md:193, schema.md, slash-dispatch.md, smoke-tests.md, ARCHITECTURE.md, module-map.md and message-bus.md name the CLI path minimap.shown and the stored key minimap.hide. The commit body carries the release note: `/lh set minimap.hide` macros must become `/lh set minimap.shown <inverted>`.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Test first. (a) tests/test_slash.lua: `/lh get minimap.shown` prints true while NS.db.global.minimap.hide == false, and `/lh set minimap.hide true` answers the unknown-path refusal. Red today. (b) tests/test_schema.lua: NS.Schema:Register() returns 0 (no missing path) with the renamed row. (c) tests/test_launcher.lua: after Schema:Set('minimap.shown', false), Sl:ResetEverything() keeps minimap.hide == true, and Sl:CliResetAll() also keeps it. This is red if the rename lands without the RESET_EXEMPT map, which is the regression the map prevents. Record that ordering in the case's red-under comment.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (renamed and added cases; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -rn '\"minimap.hide\"' settings core tests/*.lua (only the RESET_EXEMPT value and raw-store assertions remain) && grep -rn 'minimap.show\\b' settings core (expect none). No tests/perf.lua.",
   "smoke": "S (in client). Untick Master controls > Minimap button: the button hides, and `/lh get minimap.shown` prints false. Reset all settings: the button stays hidden. `/lh reset minimap.shown` brings it back.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-12",
    "WS-06",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-14",
   "repo": "LootHistory",
   "title": "Adopt the v1.56.0 write-seam refusal contract (Slash minor 15) and restate the scheduleTimer contract (OptionsWidgets minor 31)",
   "change": "settings/Slash.lua, the Dispatcher descriptor (:365-367): `set = function(path, v) return NS.Schema:Set(path, v) end`. That hands the seam's `false, err[, why]` to LibKa0s-Slash minor 15's CliSet, which prints INVALID and the reason instead of echoing the unchanged value. applyDefault stays a statement: it answers nothing, so CliReset never prints NO_DEFAULT for the reset-exempt veto. Add a comment saying why it does not return. settings/OptionsSetup.lua:202-207, scheduleTimer: keep C_Timer.After, per the LK-26 option. Rewrite the comment: since OptionsWidgets minor 31 the library tracks its own armed flag, so the return value is unused and the 50 ms slider and color throttles work with this nil-returning wrapper. Do not switch to C_Timer.NewTimer (no behavior gain). docs/slash-dispatch.md: `set` refusals print the seam's reason.",
   "tests": "Test first, in tests/test_slash.lua. Inject a test-local row through NS.SchemaRuntime.AddRows({ { path = 'settings.__probe', type = 'number', validate = function(v) return v < 5 end, default = 1 } }) with NS.db.global.settings.__probe = 1. Capture printed lines for `/lh set settings.__probe 9`: an INVALID line naming the path, and the value is still 1. Red today, because the descriptor's set discards the refusal and CliSet echoes. Remove the row and the stored key in a finally-style wrapper.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-15",
   "repo": "LootHistory",
   "title": "Schema seam: declare writeThrough {settings.enabled}; the host stub gains writeThrough storage and the full SetMany (Schema minor 2 parity)",
   "change": "settings/Schema.lua. The SchemaLib:New descriptor (:587-595) gains `writeThrough = { \"settings.enabled\" }` (LibKa0s-Schema-1.0 minor 2, LK-23; WS-02 route (a)), a data list only, with no row, label, default or validate. hostSchemaStub (:452-583), stubLib:New(d): build `local wt = {}; for _, p in ipairs(d.writeThrough or {}) do wt[p] = true end` once. R.Set: when no row is found and `wt[path]`, resolve the root. It refuses with 'nowhere to store' when that is not a table; otherwise it does stubLib.Write(root, path, copy(value), first) and returns true, with no validate and no onChange, as the library does. Every other row-less path still refuses 'unknown path'. Add `R.SetMany(entries, opts)`, the library's all-or-nothing batch. Phase 1: for each entry, find the row (or the writeThrough path) and run validate, and return `false, err, nil, i` on the first failure before any store. Phase 2: R.Set each in order and return true. When opts.act is given it runs inside R.BulkBegin/BulkEnd so the sweep veto sees the bracket. If RV-LH already added a minimal SetMany to keep the parity case green, this replaces it. Also align R.Get and R.ApplyDefault with minor 2's instanceId forwarding: `R.Get(path, instanceId)` calls row.get(instanceId), and `R.ApplyDefault(row, instanceId)` forwards to Set. LootHistory rows ignore the id. Update the stub's header comment (:436-451): the writeThrough and SetMany semantics and the version-2 doc it mirrors. docs/schema.md: a writeThrough paragraph.\n\nUpstream LK-22 (LibKa0s-Schema-1.0 minor 2): this item is the consumer follow-up that gives the host schema instance stub the SetMany member (with LK-22's all-or-nothing semantics and log-silence) plus the instanceId forwarding in R.Get/R.ApplyDefault, so the existing surface-parity case against the live v1.56.0 instance, which lists SetMany, stays green.",
   "tests": "Test first, in a new tests/test_schema_stub.lua registered in tests/run.lua's SUITES after test_schema, which keeps test_schema.lua out of further band growth. It loads the degraded env (tests/degraded_env.lua, as test_surface_parity does). (a) degradedNS.SchemaRuntime.Set('settings.enabled', false) returns true and stores false in the degraded db. Red today: the stub answers 'unknown path' because the hollow composer emitted no row. (b) Set('settings.nope', 1) still refuses. (c) SetMany over {valid, invalid} stores nothing and returns false with index 2; over two valid entries it stores both. (d) Live instance: Set('settings.enabled', ...) still takes the composed row (its onChange runs; spy NS.OnEnabledChanged). The existing parity case 'Schema stub instance carries every member of the live runtime' must stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+4..5; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output: keep R.Set and R.SetMany at CCN 10 or below with a shared local prepare helper) && wc -l settings/Schema.lua. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-13",
    "LK-22",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-16",
   "repo": "LootHistory",
   "title": "Degraded Slash stub: the refusal line from the verbatim DISABLED_LINE_FORMAT, enable/disable write through (WS-02 route a), and a counted resetall line",
   "change": "settings/Slash.lua. (1) Above the `if not lib` branch: `Sl.DISABLED_LINE_FORMAT = lib and lib.DISABLED_LINE_FORMAT or \"%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r\"`. It is present on both paths so the Slash parity case stays symmetric. The degraded `Sl.DisabledLine` (:255-257) becomes `return Sl.DISABLED_LINE_FORMAT:format(NS.BRAND, \"/lh enable\")`, the library's argument (libs/LibKa0s/Slash.lua:596, slash .. ' enable'). Correct the 'byte-identical' comment (:249-254). (2) WS-02 route (a) for enable/disable on a library-absent load. settings/Schema.lua lifts the settings.enabled onChange body (:152-156) into `function S.OnEnabledWritten() NS.OnEnabledChanged(); if NS.bus then NS.bus:SendMessage(NS.MSG.SETTINGS_CHANGED, \"enabled\") end end`, and the stamp calls it. The degraded stub's `Sl.CliSet` becomes `function(_, rest)`: parse `path value`; when path == \"settings.enabled\" and value is \"true\" or \"false\", call `NS.Schema:Set(path, value == \"true\")` (it stores through LH-15's writeThrough) and then `NS.Schema.OnEnabledWritten()`, because writeThrough runs no onChange, and print `Sl.FormatKV(path, value)`. Everything else calls unavailable(). Remove enable and disable from UNAVAILABLE_WITHOUT_LIB (:282-286) and rewrite that comment: they now work degraded, and set stays out of the help because only the enable path writes. (3) R-14: the degraded Sl.CliResetAll (:317-320) prints `NS.Format(\"filters reset (%d %s cleared); other settings need the LibKa0s library.\", n, n == 1 and \"id\" or \"ids\")` with n from NS.Filters:ClearAll(), and drops the bare unavailable(). No composed-row host verb raises or silently acknowledges on this path. `test` toggles BrowserTable directly and needs no row, so no library-absent line is required and none is added. docs/slash-dispatch.md (degraded section): the working enable/disable and the counted resetall line.",
   "tests": "Test first, in tests/test_slash.lua's degraded block (use the degradedNS load). (a) `T.assertLibraryConstant(degradedNS.Slash.DISABLED_LINE_FORMAT, \"LibKa0s-Slash-1.0\", \"DISABLED_LINE_FORMAT\")` (kit 26, LK-18). (b) assertEqual(degradedNS.Slash.DisabledLine(), LibStub('LibKa0s-Slash-1.0').DISABLED_LINE_FORMAT:format(NS.BRAND, '/lh enable')). Red today ('/lh'). (c) Degraded `/lh disable`: pcall raises nothing, degraded settings.enabled == false, degradedNS.IsStoodDown() is true, and one ack line is printed. `/lh enable` reverses it. Red today, because both verbs print 'unavailable'. (d) Degraded resetall with 3 ids across the lists prints 'filters reset (3 ids cleared)...'. (e) Degraded help now lists enable and disable. Update the existing degraded help case that asserted they were absent.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+5; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l settings/Slash.lua tests/test_slash.lua (test_slash under 1500; if it would pass 1100, move the degraded cases into tests/test_slash_degraded.lua registered in run.lua). No tests/perf.lua.",
   "smoke": "S-005 and S-008.4 (in client, library-less). Temporarily rename libs/LibKa0s. `/lh disable` prints an ack and recording stops; `/lh` help lists enable; `/lh enable` restores it with no Lua error. `/lh resetall` prints the counted filters line. Restore the folder afterwards.",
   "finding_ids": [
    "LootHistory-R-05",
    "LootHistory-R-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-15",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-17",
   "repo": "LootHistory",
   "title": "Route every event registration through Core.SafeRegisterEvent with an addon-owned rejected list; mirror LK-10 in the degraded NS.Format",
   "change": "core/CoreSetup.lua. Declare `NS.RejectedEvents = {}` before the branch. Live path (after :180): `NS.SafeRegisterEvent = lib.SafeRegisterEvent`, `NS.SafeRegisterUnitEvent = lib.SafeRegisterUnitEvent`, `NS.SafeRegisterEvents = lib.SafeRegisterEvents` (LibKa0s-Core-1.0 minor 8, LK-11). Degraded branch (:80-150): one-rung bodies in the same `NS.X = function` shape, so the Core parity derivation at tests/test_surface_parity.lua:66-85 finds them on both paths. `NS.SafeRegisterEvent = function(target, event, handler, rejected) local ok = pcall(target.RegisterEvent, target, event, handler); if not ok and rejected then <append once> end; return ok end`, and likewise for SafeRegisterUnitEvent(frame, event, rejected, u1, u2) and SafeRegisterEvents(target, events, handler, rejected) -> count. There is no C_EventUtils front gate in the stub (LK-11 degradation note). The degraded NS.Format (:95-100) becomes `local ok, out = pcall(string.format, safeToString(fmt), unpack(parts)); NS.Print(ok and out or table.concat(<fmt + parts>, \" \"))`, mirroring Core minor 8's printer.Format (LK-10), so a `%d` fed '<secret>' no longer raises. Call sites: modules/Attribution.lua Enable (the nine bus registrations after LH-01/LH-02) builds `self.__events` only from names whose NS.SafeRegisterEvent returned true, so Disable stays symmetric. The spell frame uses `NS.SafeRegisterUnitEvent(spellFrame, \"UNIT_SPELLCAST_SUCCEEDED\", NS.RejectedEvents, \"player\")`. modules/Collector.lua:218-219, modules/Browser.lua:1246/:1252 (on B.__ev) and core/LifecycleSetup.lua:112 (NS.addon, handler 'OnEnterWorld') route the same way. Surface the list with a `/lh debug events` argument in the COMMANDS debug entry (settings/Schema.lua:772-779): it prints `rejected events: <names>` or `rejected events: none`. The help text becomes \"Toggle window; 'on'/'off' set logging; 'events' lists rejected events\". Also log one NS.Debug('Init', ...) line when a name is rejected. docs/ARCHITECTURE.md ## Event subscriptions: the helper and the rejected list, per events-frames-taint-\u00a71 (v2.65.0). docs/midnight-quirks.md: the event-to-edge trade note \u00a71 asks for.",
   "tests": "Test first. (a) tests/test_attribution.lua: with T.mocks.__badEvents = { ENCOUNTER_START = true } (kit mock_base.lua:715-727; kit 26 also answers C_EventUtils.IsEventValid false), Enable registers the other eight bus events, NS.RejectedEvents contains ENCOUNTER_START exactly once, Attribution.__events omits it, and Disable leaves no stray registration. Red today: the bare call raises and every later registration in the block is lost. (b) The same with __badEvents on UNIT_SPELLCAST_SUCCEEDED (kit 26 frame RegisterUnitEvent honors it): the eight bus events stay registered. (c) tests/test_slash.lua: `/lh debug events` prints the list. (d) tests/test_libka0s.lua or test_surface_parity: the degraded NS.Format('%d', <a table secret mock>) raises nothing and prints a line (red today). tests/test_disabled.lua must still reach its empty set. Reset __badEvents and NS.RejectedEvents after each case.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+4; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -rnE ':Register(Unit)?Event\\(' core modules settings | grep -v SafeRegister (expect none apart from the helper bodies) && wc -l core/CoreSetup.lua modules/Attribution.lua. No tests/perf.lua.",
   "smoke": "S2 (in client). `/lh debug events` prints 'rejected events: none' on 12.1; loot still records; `/lh disable` then `/lh enable` restores every registration.",
   "finding_ids": [
    "LootHistory-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-01",
    "LH-02",
    "LH-05",
    "LK-11",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-18",
   "repo": "LootHistory",
   "title": "The loot hot path reuses one gate-config table instead of allocating one per CHAT_MSG_LOOT",
   "change": "modules/Collector.lua: a module-level `local gateCfg = {}`. RefreshUpvalues (:78-89) writes gateCfg.qualityThreshold, excludedSources, excludeQuestItems, blacklist and whitelist whenever the upvalues refresh. OnChatMsgLoot (:116-119) sets `gateCfg.itemID = itemID` and passes gateCfg to self:ShouldRecord. ShouldRecord (:41-46) only reads cfg and never keeps it (verified), so reuse is safe. The comment names events-frames-taint-\u00a77.",
   "tests": "Test first, in tests/test_collector.lua. Spy ShouldRecord's 4th argument across two OnChatMsgLoot lines for different items. The same table is passed both times (rawequal), and its itemID equals the second item's id at the second call. Red today: a fresh table per call.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-17",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-19",
   "repo": "LootHistory",
   "title": "Measure, then memoize the AH priority-tag parse and bucket accumulateTime's date() calls",
   "change": "Step 1, uncommitted: a scratch script (in the session scratchpad, not the repo) loads the harness, builds 50,000 synthetic records with auctionPrice maps, and times Database:Stats and Export:CSV before and after. Record the numbers in the commit body. If Pick plus accumulateTime is under 5% of Stats time, land only the step-3 test and a one-line comment at AuctionPrice:Pick recording the measurement, and close the finding as measured-no-action. Step 2, otherwise: modules/AuctionPrice.lua (:88-100) adds module-local `TAG_PROV, TAG_KEY = {}, {}` and `local function splitTag(tag)`, which caches tag:match('^(.-):(.+)$') per tag string. Tags are immutable strings, so there is no invalidation, no bus target and no stand-down change. Pick calls splitTag. core/Database.lua accumulateTime (:534-544) takes a per-Stats-call memo, created in newAccumulator as `A.timeMemo = {}`, keyed by `math.floor(r.ts / 900)`. Real timezone offsets are multiples of 15 minutes, so a 900 s bucket never straddles a local day or hour. The memo value holds {day, hour, wday} from one date('*t') plus string.format('%04d-%02d-%02d'), so a repeated bucket makes no date() call. Drop the finding's events-frames-taint-\u00a77 citation from any comment (Stats is user-initiated, not per-frame).",
   "tests": "Test first (step 3, always). tests/test_auctionprice.lua: Pick honors a reorder. With map {tsm={dbmarket=5}, auctionator={price=9}}, Pick answers the first tag in the priority; after MovePriorityWithin puts the other first, Pick answers it. It passes today and pins the memo against staleness. If step 2 lands, tests/test_stats.lua adds a case: Stats over records at 10:00, 10:05 and a local-midnight boundary gives the same byDay, byHour and byWeekday as today's implementation, computed in the case via date().",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1 or +2; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output; AuctionPrice:MovePriorityWithin is at CCN 15 today, so do not touch it) && git status --short (no scratch script tracked). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-20",
   "repo": "LootHistory",
   "title": "Stop re-testing the library's DebugLog formatters; the degraded stub's formatters carry no library format string",
   "change": "tests/test_debuglog.lua: delete the four cases at :10-28 (FormatPlain x3 and FormatColored). LibKa0s's suite owns them (testing-\u00a78). Keep FONT_MONO (:4-8) and the NS.Debug / NS.SafeToString secret-sink cases, which also drive the _frameForTest and _toggleClickForTest seams test_surface_parity.lua:142-146 relies on. core/DebugLogSetup.lua:34-40: the stub's FormatPlain and FormatColored become `function(_, _, msg) return tostring(msg) end`. Both members stay, so the DebugLog parity case needs no ignore-list change, and the stub holds no library format string or color code (library-stack-\u00a77, anti-pattern #47). Comment: no addon caller reaches either member (`grep -rn 'FormatPlain\\|FormatColored' core modules settings` returns only this stub).",
   "tests": "Test first, in tests/test_debuglog.lua. Add one case: load the degraded env and assert `not degradedNS.DebugLog.FormatColored('t', 'x', 'm'):find('|c', 1, true)` and FormatPlain('t', 'x', 'm') == 'm'. Red today, because the stub copies the colored format. Then delete the four library cases. Net -3 cases.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (-3; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -n 'cff6f8faf\\|cffc9a66b' core/DebugLogSetup.lua tests/test_debuglog.lua (expect none). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-07",
    "LootHistory-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-21",
   "repo": "LootHistory",
   "title": "Item seam tests use the 11.1.5+ |cnIQ4: link shape, keeping one legacy |cff case",
   "change": "tests/test_itemsetup.lua:14: EPIC_LINK becomes the current client shape `|cnIQ4:|Hitem:258586::::::::80:250::5:3:10356:10355:1540:1:28:2462:::|h[Bloodfeather Chestguard]|h|r`. Add `LEGACY_EPIC_LINK` holding today's |cffa335ee string for stored rows. The quality cases (:66-72, and the uncached resolver case at :87-99) use EPIC_LINK. Item minor 2 (LK-12) reads |cnIQ<n> first. One new case asserts QualityFromLink(LEGACY_EPIC_LINK) == 4 under withQualityPalette, pinning the hex rung for stored links. Update the comment block at :17-26: the hex map is now a fallback rung, and it is cached only when non-empty (Item minor 2). docs/midnight-quirks.md: the link-color shape note cites Item minor 2.",
   "tests": "Test first. The uncached-resolver case with the new EPIC_LINK outside withQualityPalette: NS.Compat.GetItemInfo(EPIC_LINK) answers quality 4 with no palette installed. That is red under a v1.55.0 payload and green under v1.56.0, so it pins the adoption. Plus the legacy hex case (+2 total).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+2; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output). No tests/perf.lua.",
   "smoke": "S (in client). With the item cache cold (fresh login), loot an epic you have never seen. The History row shows Epic quality and the purple name immediately, not Poor.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-22",
   "repo": "LootHistory",
   "title": "Surface-parity cases for the Env, Item, Media, Pool and Lifecycle seams",
   "change": "tests/test_surface_parity.lua (after the Schema cases, :214). Five cases, each with a '-- members from: grep ...' comment and the degraded arm from the file-scope `degradedNS` (tests/degraded_env.lua). (1) Item: `T.assertSurfaceParity(degradedNS.Item, \"LibKa0s-Item-1.0\", {<live-only members with zero addon callers, each justified by a grep>})`. (2) Pool: `T.assertSurfaceParity(degradedNS.Pool, \"LibKa0s-Pool-1.0\", {...})`. (3) Lifecycle: the two-table form, `T.assertSurfaceParity(NS.Lifecycle, degradedNS.Lifecycle, \"Lifecycle latch vs host stand-in\")`. The stand-in carries Hold, Release, Set, IsHeld, IsDown, Reevaluate, Holds and PrintHolds (core/LifecycleSetup.lua:148-162); any live instance member it lacks is either added to the stand-in or ignored with a grep proving zero callers. (4) Env and (5) Media: by-name NS-member derivation, exactly like the Core case. `grep -nE '^\\s*function NS\\.[A-Za-z_]+|^\\s*NS\\.[A-Za-z_]+\\s*=' core/EnvSetup.lua` (and core/MediaSetup.lua) builds live and degraded maps, and asserts the derivation found at least one known member (NS.Version for Env, NS.MediaFont for Media) so an empty derivation fails. Record in the commit body the mutation check for each case: deleting one stub member turns it red (testing-\u00a712).",
   "tests": "These are the tests. For each case, first delete one member from the stub locally (for example PoolSetup's Release) and watch the new case go red; restore it before committing; record all five mutations in the commit body. Any member the case finds missing on a stub is added to the stub in this commit with a no-op or degraded body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+5; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && wc -l tests/test_surface_parity.lua. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-15",
    "LH-17",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-23",
   "repo": "LootHistory",
   "title": "Tests send and subscribe bus messages by NS.MSG constant, never by literal",
   "change": "tests/test_browser.lua:536,552 use NS.MSG.RECORD_ADDED and NS.MSG.HISTORY_CHANGED. tests/test_collector.lua:440,444,473 use NS.MSG.SETTINGS_CHANGED. tests/test_panel.lua:232,252 use RECORD_ADDED and HISTORY_CHANGED. tests/test_harness.lua:145-146 declare `local PROBE = \"Ka0s_LootHistory_HarnessProbe\"` once at file scope and use it at both call sites. The literal assertions in tests/test_constants.lua stay (architecture-\u00a74: each Ka0s_ literal appears once).",
   "tests": "Pure refactor with no behavior change. The existing cases stay green with an unchanged case count. Before the edit, confirm that a typo'd literal (e.g. 'Ka0s_LootHistory_RecordAddedX') would still pass test_browser.lua:536's case (vacuous), and that a typo in the constant name raises through the strict Bus.Catalog. Note both in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md (no change) && grep -rnE '(Send|Register)Message\\(\"Ka0s_' --include='*.lua' . | grep -v '^./libs/\\|^./tests/_kit/' (expect 0 hits; run under bash).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-24",
   "repo": "LootHistory",
   "title": "Resolvable citations: disabled-\u00a77 test names become slash-commands-\u00a77 step N, and .pkgmeta cites packaging",
   "change": "tests/test_disabled.lua:174-516: rename the 12 case names. `disabled-\u00a77.1:` -> `slash-commands-\u00a77 step 1:`, likewise for .3 through .10, and the bare `disabled-\u00a77:` (:516) -> `slash-commands-\u00a77:`. Rename comments in the file that cite disabled-\u00a77 the same way. .pkgmeta:14,19,20: replace `packaging.md:28` with `packaging` (the bare section name, documentation-\u00a76). docs/test-cases.md is regenerated (the count stays; names change).",
   "tests": "Rename only, with the count unchanged. The inventory diff shows exactly 12 renamed lines.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md && git diff --stat docs/test-cases.md (12 lines changed, count unchanged) && grep -rn 'disabled-\u00a7' tests/*.lua docs/test-cases.md (0) && grep -rnE '\u00a7[0-9]+\\.[0-9]+' tests/*.lua (0 dotted) && grep -n 'packaging.md:28' .pkgmeta (0).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-25",
   "repo": "LootHistory",
   "title": "Chat lines hand their values to the shared printer's Format instead of pre-formatting",
   "change": "settings/Slash.lua:40,51,62,76: `print((\"blacklist cleared (%d %s).\"):format(n, noun))` becomes `NS.Format(\"blacklist cleared (%d %s).\", n, noun)`, and likewise for the whitelist, currency and filters lines. The output bytes are unchanged, and the printer's Format is secret-safe (Core minor 8, LK-10). settings/Schema.lua:785: `print(\"test mode \" .. (on and \"on\" or \"off\"))` becomes `NS.Format(\"test mode %s\", on and \"on\" or \"off\")`. Leave the local `print` capture where other lines still use it.",
   "tests": "Test first, in tests/test_slash.lua. A case captures printed lines for the KA0S_LOOTHISTORY_CLEAR_BLACKLIST OnAccept with 2 ids and for `/lh test` on and off, asserting today's exact text ('[LH] blacklist cleared (2 ids).' and '[LH] test mode on'). It is green before and after, which pins that the refactor changes no bytes.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (+1; README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -n '):format(' settings/Slash.lua settings/Schema.lua (only non-print uses remain).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-21"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-16",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-26",
   "repo": "LootHistory",
   "title": "Delete the dead global GetAddOnMetadata rung in the Env seam (compat applicability, v2.65.0)",
   "change": "core/EnvSetup.lua:52-58: keep `if Env then return Env.GetAddOnMetadata(...)` and the `C_AddOns.GetAddOnMetadata` rung. Delete the `if type(GetAddOnMetadata) == \"function\"` global rung (:56-58), because every supported client (Interface 120100) provides the call only through C_AddOns. The WS-05 ruling says such a rung is dead code to delete, not shim. Update the rung comment. Drop \"GetAddOnMetadata\" from .luacheckrc read_globals if nothing else reads it (grep first). docs/compat-layer.md or docs/ARCHITECTURE.md: the one-line rung list.",
   "tests": "Test first, in tests/test_envsetup.lua. With C_AddOns = nil and a global GetAddOnMetadata stub answering 'X', the library-absent NS.Version path does not answer 'X' and falls to the in-code constant. Red today, because the global rung answers. Replace any existing case that pinned the global rung, and note the replacement in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list > docs/test-cases.md (README badge) && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && grep -rn '[^.]GetAddOnMetadata(' core modules settings (only Env and C_AddOns forms). No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "WS-05",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-27",
   "repo": "LootHistory",
   "title": "Annotate every load-bearing TOC position and mark the conventional groups; restate the load-bearing set in ARCHITECTURE.md and module-map.md",
   "change": "LootHistory.toc. Above core\\CoreSetup.lua (:48): `# LOAD-BEARING POSITION: lib:New{ prefix = NS.PREFIX } runs at file load (core\\Namespace.lua must be above), and NS.Print, published here, is captured as a file-scope local by modules\\Browser.lua, settings\\Schema.lua and settings\\Slash.lua (must be below them).` Above core\\DebugLogSetup.lua (:56): `# LOAD-BEARING POSITION: the file-load lib:New descriptor reads NS.Constants.FONT_MONO; core\\Constants.lua must be above.` Above settings\\Slash.lua (:87): `# LOAD-BEARING POSITION: the dispatcher is built at file load with commands = NS.COMMANDS, assigned by settings\\Schema.lua; above Schema, LibKa0s-Slash :New raises (Slash.lua:480) and /lh is never registered.` Add one-line conventional notes: `# Locales` (:29) 'conventional: nothing below reads a locale key at load'; `# Defaults` (:67) 'conventional: read only from functions after ADDON_LOADED' (check that settings\\Schema.lua's file-scope `NS.defaults.global` read at :20 makes Defaults load-bearing before Schema; if it does, write a LOAD-BEARING line instead); and the plain Core run (Constants, Namespace, State, Util, LootHistory, Database). The PoolSetup comment (:57) becomes 'CONVENTIONAL: nothing resolves at load; every NS.Pool.New call is inside a function (modules/Analytics.lua:616).' docs/ARCHITECTURE.md:42-48 and docs/module-map.md restate the load-bearing set from the tree. Item and Media sit above Constants. CoreSetup sits below Namespace and above every file-scope NS.Print capture. DebugLogSetup sits below Constants (FONT_MONO). Widgets sits below Media. OptionsSetup sits above Schema. Defaults sits above Schema, if confirmed. Slash sits below Schema. Pool, Lifecycle, Launcher and Env are conventional.",
   "tests": "none; docs and TOC comments only. The tests/test_harness.lua TOC-derivation cases must stay green, because comment lines are skipped by the TOC reader.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md (no change) && grep -c 'LOAD-BEARING' LootHistory.toc (6, or 7 with Defaults) && grep -n 'CONVENTIONAL\\|conventional' LootHistory.toc.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-04",
    "LootHistory-A-05",
    "LootHistory-A-06",
    "LootHistory-A-07",
    "LootHistory-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-28",
   "repo": "LootHistory",
   "title": "Correct the stale comments and doc citations",
   "change": "Code comments. settings/Schema.lua:759-760 and settings/Slash.lua:279: the reader of settings.enabled is NS.AddonIsOff (core/LifecycleSetup.lua:175-178) and the latch, not modules/Collector.lua. core/Util.lua:14: move the SplitPath comment line above Util.SplitPath (:25), and delete the stale `-- Compact date (MM/DD/YY)` line at :38. docs/midnight-quirks.md:9 cites core/Compat.lua:234 (C_TooltipInfo.GetHyperlink), and :64 cites :98-104 (Compat.GetMailHeader). Re-derive both with grep -n after the earlier items, since LH-03 moves Compat lines. DEPENDENCIES.md:53 re-points to tests/_kit/run-automated-tests.sh's `declare -A` and EPOCHREALTIME lines, re-derived with `grep -n 'declare -A\\|EPOCHREALTIME' tests/_kit/run-automated-tests.sh`, because the v1.56.0 runner (LK-09) moved them again. DEPENDENCIES.md:49 and :112 point at docs/testing.md's luacheck 0/0 line and regenerate line (grep -n). docs/ARCHITECTURE.md:207 cites slash-commands-\u00a73 for bare /lh. ARCHITECTURE.md:447's compat trigger becomes `21 shims (grep -cE '^\\s*function\\s+[A-Za-z_][A-Za-z0-9_]*\\.' core/Compat.lua)`, with the count re-measured after LH-01 and LH-03 add shims.",
   "tests": "none; comments and docs only. The suite and inventory are unchanged.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && grep -n 'Collector.lua' settings/Schema.lua settings/Slash.lua (no reader claim) && grep -n 'MM/DD/YY' core/Util.lua (0) && sed -n \"$(grep -n 'GetHyperlink' core/Compat.lua | head -1 | cut -d: -f1)p\" core/Compat.lua (the cited line resolves) && grep -cE '^\\s*function\\s+[A-Za-z_][A-Za-z0-9_]*\\.' core/Compat.lua (matches the ARCHITECTURE figure).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-R-10",
    "LootHistory-R-11",
    "LootHistory-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-01",
    "LH-03",
    "LH-16",
    "LH-27",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-29",
   "repo": "LootHistory",
   "title": "Ratify the deconstruct name-matching with a localization-\u00a74 register row",
   "change": "docs/ARCHITECTURE.md ## Documented deviations: add the row. Rule `localization-\u00a74`. What differs: modules/Attribution.lua resolves deconstruct casts by comparing the cast's client-locale name against names derived from seed spellIDs (NAME_SEEDS :20-50, seedToken via NS.Compat.GetSpellName :64, match :94), rather than by spellID alone. Why: the per-herb and per-ore Mass Mill and Mass Prospect ids are not enumerable; the names are derived from ids on the same client, so there is no English literal and the match is locale-correct by construction. Cite closed issue #2. Decided on the commit date. Re-check trigger: Blizzard exposes a stable deconstruct token or category, or the Mass Mill/Prospect id set becomes enumerable. Add a comment at Attribution.lua:20 pointing at the row.",
   "tests": "none; register row and comment only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (tests/test_doc_structure.lua still parses the register) && grep -n '^| `localization-\u00a74`' docs/ARCHITECTURE.md.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-10",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-30",
   "repo": "LootHistory",
   "title": "Spill ARCHITECTURE.md under the hub budget: the Settings schema detail to schema.md, the disabled state to disabled-state.md",
   "change": "docs/ARCHITECTURE.md ## Settings schema (:95-163): move the named-state and registry paragraphs (:118-160) into docs/schema.md under their own headings, and leave a summary of 15 lines or fewer with one link. ## The disabled state (:255-334): move the body, including its four ### subsections, to a new Tier 3 docs/disabled-state.md. Leave a summary of 10 lines or fewer and one link, and register the doc in ### Addon-specific (documentation-\u00a73, Tier 3). Re-point every in-repo link that targeted the moved anchors (grep -rn 'ARCHITECTURE.md#the-disabled-state\\|#one-latch' docs README.md CLAUDE.md). Target: the hub under 400 lines, and Settings schema 60 lines or fewer.",
   "tests": "none; docs only. tests/test_doc_structure.lua must stay green; if it pins the disabled-state heading in ARCHITECTURE.md, update the pin to the new doc and say so in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && wc -l docs/ARCHITECTURE.md (under 400) && awk '/^## Settings schema/{f=1;n=0;next} /^## /{if(f)exit} f{n++} END{print n}' docs/ARCHITECTURE.md (60 or fewer) && grep -n 'disabled-state.md' docs/ARCHITECTURE.md && lua5.1 tests/run.lua --list | diff - docs/test-cases.md.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-01",
    "LH-17",
    "LH-28",
    "LH-29",
    "LH-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "LH-31",
   "repo": "LootHistory",
   "title": "performance.md shrinks to the one-screen exempt shape; the sweep moves to combat-path-sweep.md",
   "change": "New Tier 3 docs/combat-path-sweep.md takes the sweep section (docs/performance.md:53-129), updated for LH-01's two events (fifteen registrations) and LH-17's helper. docs/performance.md becomes one screen, about 30 lines. It states that the addon brackets nothing, that criteria (a) and (c) apply, that the sweep lives in combat-path-sweep.md, and what re-arms the full wiring (the register row's trigger). The two allocation sections (:141-184) move into docs/browser.md (the Insights widget pools) or are dropped where they restate code comments. The complexity section (:185-) moves to docs/testing.md. Register combat-path-sweep.md in ARCHITECTURE.md ### Addon-specific. Re-point the performance-\u00a712 register row's sweep link at combat-path-sweep.md. Keep that row's Rule cell literally `performance-\u00a712`, so the v1.56.0 runner's reason-(2) detection (LK-09) matches it.",
   "tests": "none; docs only. tests/test_doc_structure.lua stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && wc -l docs/performance.md (about 40 or fewer) && grep -n 'combat-path-sweep.md' docs/ARCHITECTURE.md docs/performance.md && grep -n '^| `performance-\u00a712`' docs/ARCHITECTURE.md.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-01",
    "LH-17",
    "LH-30",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-32",
   "repo": "LootHistory",
   "title": "README Usage closes on a one-sentence configuration signpost",
   "change": "README.md:58: move the enable/disable, off-means-off and disabled-command-surface sentences into the Usage body, as their own paragraph before the closing one, or into FAQ/Troubleshooting. Usage then ends on exactly one sentence, for example 'Everything else is configured under **Settings \u25b8 AddOns \u25b8 Loot History**, which `/lh` (or `/loothistory`) opens; `/lh help` (or `/loothistory help`) lists the commands.' Run the humanize/de-AI pass over the moved sentences only.",
   "tests": "none; docs only. The prose gate (kit test_prose) stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && awk '/^## Usage/{f=1} /^## How attribution works/{f=0} f' README.md | tail -3 (the last paragraph is one sentence naming both forms).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-16",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-33",
   "repo": "LootHistory",
   "title": "Root CLAUDE.md: the green gate before the provenance line, as documentation-\u00a72 orders",
   "change": "CLAUDE.md: move `## Green gate` (:62-) above `## Vendored LibKa0s` (:49-61), with content unchanged apart from the order. tests/test_vendor_sync.lua greps the provenance pattern and does not read a position.",
   "tests": "none; docs only. tests/test_vendor_sync.lua stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n '^## ' CLAUDE.md (Green gate precedes Vendored LibKa0s).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-34",
   "repo": "LootHistory",
   "title": "One consolidated re-vendor span bundle for the 25 unrecorded LibKa0s tags",
   "change": "New docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.53.0/, the WS-01 and WA-01 span form, no per-tag folders. 01_DELTA.md line 1 is exactly the revendor-libka0s span line, `Delta: LibKa0s v1.15.0 -> v1.53.0 (span: v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0, v1.27.0, v1.28.0, v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0)`. Re-derive the list first from each libs/LibKa0s commit's CLAUDE.md provenance line (`for c in $(git log --since='2026-08-25 00:00' --format=%h -- libs/LibKa0s); do git show $c:CLAUDE.md | grep -m1 -oE 'v1\\.[0-9]+\\.[0-9]+'; done | sort -uV`) minus the tags already recorded (v1.15.0, v1.25.0, v1.30.0-v1.34.0, v1.55.0, v1.56.0). Below line 1: a table of each tag with its vendoring commit (`git log --format='%h %ad %s' --date=short -- libs/LibKa0s`). One correction note: the frozen 2026-09-23-v1.55.0 bundle's line 1 names v1.54.2 as its base, but this repo's previous vendored tag was v1.53.0 (v1.54.x never landed here). The frozen bundle is not edited. 05_SUMMARY.md states that the intermediate tags were carried by sweeps with nothing adopted beyond the commits themselves, and that later re-vendors (v1.56.0, RV-LH) write their own bundles. Include whatever other files the revendor bundle convention names (02-04) as short stubs pointing at the commits.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none; docs only (a frozen record).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (test_eol, test_prose green on the new files) && the AUDIT.md re-vendor ledger loop from WowAddonStandards v2.65.0 (step 4, with the span-folder branch emitting every tag on line 1): unrecorded count = 0.",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "WA-01",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-35",
   "repo": "LootHistory",
   "title": "Track the Analytics.lua peel as a GitHub issue",
   "change": "File one issue: `gh issue create -R tusharsaxena/LootHistory --title 'Peel modules/Analytics.lua: renderers vs formatting/segmenting helpers' --label state:triaged --label severity:low --body '<the file is 1200 lines in the 1000-1500 watch band (RESULTS.md Disposition: Peel next for seven runs); split the chart renderers from the formatting and segmenting helpers; source: docs/audits/2026-09-23 LH-52>'`. This is a single throttled GitHub write. Record the issue number for LH-DOCS, which writes the RESULTS.md Disposition 'already tracked as #N'. The commit carries only the issue number, in docs/ARCHITECTURE.md ### Files over the 1500-line cap or its watch-band note if one lists Analytics; otherwise this item makes no repo change and is recorded in LH-DOCS's commit.",
   "tests": "none; issue tracking only.",
   "verify": "gh issue list -R tusharsaxena/LootHistory --search 'Peel modules/Analytics.lua' --state open (one issue, labels state:triaged and severity:low).",
   "smoke": "",
   "finding_ids": [
    "LootHistory-A-20"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "LH-DOCS",
   "repo": "LootHistory",
   "title": "Sync docs, regenerate the case inventory and badge, and record a fresh automated-test bundle (perf skip reason 2)",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md, docs/ARCHITECTURE.md and the Tier 2/3 docs. It verifies count claims (case count, lint file count, the 15 registrations, the Compat shim count), slash and COMMANDS parity (the `debug events` argument, the degraded enable/disable), the comment-citation check and module-map drift after LH-01..LH-36. (2) `lua5.1 tests/run.lua --list > docs/test-cases.md` and set the README tests badge to the pass/total. (3) Record a bundle with the vendored runner: `bash tests/_kit/run-automated-tests.sh` (kit 26), which writes docs/automated-tests/<YYYYMMDD-HHMMSS>/. Its manifest skipReason must be the reason-(2) string 'performance-\u00a712 no-combat-path exemption (ratified; docs/ARCHITECTURE.md -> Documented deviations)' (LK-09; LootHistory-A-12). RESULTS.md is regenerated by the runner (LootHistory-R-20). Then make the only permitted hand edits, to Disposition cells: modules/Analytics.lua 'already tracked as #<LH-35 issue>', and the Browser.lua row's wording refreshed (it grew; it did not shrink), plus any new band files (tests/test_schema.lua, tests/test_slash.lua). Write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md playbook. No version bump and no tag; the owner decides.",
   "tests": "none; docs and record only. This item runs the whole battery.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && lua5.1 tests/run.lua --list | diff - docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . (no output) && S=$(ls -d docs/automated-tests/2*/ | tail -1) && grep -n 'performance-\u00a712' $S/manifest.json && test -f $S/ANALYSIS.md && grep -n 'already tracked as #' docs/automated-tests/RESULTS.md && grep -o 'tests-[0-9]*%2F[0-9]*' README.md (matches the inventory Totals) && find . -name '*.lua' -not -path './libs/*' -not -path './tests/_kit/*' -exec wc -l {} + | sort -n | tail -3 (all under 1500).",
   "smoke": "Re-run every smoke check this plan touched, per docs/smoke-tests.md: S-001, S-002, S-003, S-004, S1, S2, S-005, S-007, S-008.1 to S-008.4, the minimap row, and the cold-cache epic.",
   "finding_ids": [
    "LootHistory-A-12",
    "LootHistory-R-20"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-35",
    "LH-00",
    "LH-01",
    "LH-02",
    "LH-03",
    "LH-04",
    "LH-05",
    "LH-06",
    "LH-07",
    "LH-08",
    "LH-09",
    "LH-10",
    "LH-11",
    "LH-12",
    "LH-13",
    "LH-14",
    "LH-15",
    "LH-16",
    "LH-17",
    "LH-18",
    "LH-19",
    "LH-20",
    "LH-21",
    "LH-22",
    "LH-23",
    "LH-24",
    "LH-25",
    "LH-26",
    "LH-27",
    "LH-28",
    "LH-29",
    "LH-30",
    "LH-31",
    "LH-32",
    "LH-33",
    "LH-34"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-02",
   "repo": "AbsorbTracker",
   "title": "Slash and profile tests stop asserting nil==nil and stop leaking per-unit writes",
   "change": "tests/test_slashcmds.lua only. (1) Replace every `NS.Helpers.RestoreDefaults(\"bar\")` / `RestoreDefaults(\"border\")` cleanup, which are no-ops because VALID_PAGES (settings/Schema.lua:397) is {general, appearance, profiles}, with `NS.Helpers.RestoreDefaults(\"appearance\")`. The sites are :224-225, :371, :552 and :574, plus any others from `git grep -n 'RestoreDefaults(\"\\(bar\\|border\\)\")' tests`. (2) Rewrite ':545-548' (/at profile new) to seed `T.rawSet(\"units.player.barWidth\", 456)` and assert `NS.GetSetting(\"units.player.barWidth\") == NS.unitDefaults.barWidth`, with a '-- red under: new that skips ResetProfileCounted' note. Rewrite ':604-606' (/at profile reset) the same way with 478 and a '-- red under: reset that switches instead of resetting' note. (3) Fix any ordering dependency this exposes where it lives, never by re-adding a leak. The likeliest is a later suite that read units.target.barWidth=300 from :218. No production file changes.",
   "tests": "Test-only item. First confirm the vacuity: temporarily delete `NS.ResetProfileCounted(db)` from settings/Slash.lua's `new` and see that the current :545-548 case stays green. After the rewrite, the same mutation must turn it red. Revert the mutation before committing. Case count is unchanged.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -nE 'RestoreDefaults\\(\"(bar|border)\"\\)|rawSet\\(\"barWidth\"' -- tests (empty); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l tests/test_slashcmds.lua (<1500)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-03",
   "repo": "AbsorbTracker",
   "title": "/at profile new refuses an existing name; copy and delete check the name before AceDB sees it",
   "change": "settings/Slash.lua PROFILE_VERBS (:398-454). Add a file-local `profileExists(db, name)` that walks `db:GetProfiles()` for an exact, case-sensitive match. `new`: when profileExists, print `Profile '<name>' already exists \u2014 /at profile use <name> switches to it, /at profile reset resets it`, and return without SetProfile or ResetProfileCounted. Rewrite the comment at :427-430, which claims `new` on an existing name resets it. `copy`: when name == db:GetCurrentProfile(), print 'Cannot copy a profile onto itself'; when not profileExists, print `Profile '<name>' not found \u2014 /at profile list shows them`; only then call db:CopyProfile(name). `delete`: keep the current-profile refusal and add the same not-found line before DeleteProfile. No pcall anywhere. PROFILE_HELP :383 stays 'Create new profile with defaults', which is now true. docs/profiles.md :79 and the copy/delete rows, and docs/smoke-tests.md profile steps: document the three refusals. This also meets the LK-03 consumer obligation: under kit 26 the AceDB fake raises on these names, and no raw AceDB error may reach the player or a suite. Pre-RV-eligible: it is green on kit 25 and kit 26.",
   "tests": "Red-first in tests/test_slashcmds.lua's '/at profile' section. (a) SetProfile('Keep'), rawSet units.player.barWidth 333, back to Default; `slash('profile new Keep')`: the current profile is still 'Default', the output contains 'already exists', and after SetProfile('Keep') the barWidth is still 333 (-- red under: removing the profileExists guard). (b) `profile copy NoSuchProfile`: pcall(slash) is ok, the output has 'not found' and no 'Copied', and the current values are unchanged (-- red under: dropping the existence check; under kit 26 the fake's raise also reddens it). (c) `profile copy Default` while on Default: 'Cannot copy a profile onto itself'. (d) `profile delete NoSuchProfile`: 'not found', no 'Deleted'. Four new cases. Regenerate docs/test-cases.md and the README Tests badge. If tests/test_slashcmds.lua would pass 1400 lines, move the whole '/at profile' section (:500-744) into a new tests/test_profilecmds.lua listed in tests/run.lua right after test_slashcmds.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' ':!libs' ':!tests/_kit' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (empty); lua5.1 tests/run.lua --list > /tmp/l && diff <(tr -d '\\r' </tmp/l) <(tr -d '\\r' <docs/test-cases.md) (empty)",
   "smoke": "In client (real AceDB-3.0): /at profile new Default prints 'already exists' and the bars do not reset; /at profile copy NoSuchProfile and /at profile copy <current> print their refusals with no Lua error (BugSack/BugGrabber clean); /at profile delete NoSuchProfile prints 'not found'.",
   "finding_ids": [
    "AbsorbTracker-R-01",
    "AbsorbTracker-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-02",
    "LK-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-04",
   "repo": "AbsorbTracker",
   "title": "/at lock, /at unlock and /at enable|disable echo the STORED value in the set shape and refresh the panel; the enable comment tells the truth",
   "change": "settings/Slash.lua. (1) Add a file-local `echoStored(path)`: `if NS.RefreshOptionsPanel then NS.RefreshOptionsPanel() end; local row = NS.FindSchemaRow(path); local stored = NS.GetSetting(path); local value = row and NS.FormatSchemaValue(row, stored) or tostring(stored); print(SlashLib.FormatKV and SlashLib.FormatKV(path, value) or (path .. ' = ' .. value))`. setEnabled (:310-317) becomes `NS.SetByPath('enabled', on); echoStored('enabled')`. `lock` and `unlock` (:89-98) become `NS.SetByPath('locked', true|false); echoStored('locked')`, which removes the fixed 'Bar locked'/'Bar unlocked' lines. Help texts become 'Lock the bars in place' and 'Unlock the bars so they can be dragged'. (2) R-05: rewrite the comment at :292-309. The enabled row's onChange is NS.SyncEnabledHold, which moves the latch's `disabled` hold (core/Lifecycle.lua). StandDown unregisters the unit-event frames, the five AceEvent registrations and the bus. The dispatcher, config, help and bare /at stay live (slash-commands-\u00a72/\u00a77). Delete the 'VISIBILITY then REPAINT' and 'gates ShouldShowBar's second rung ... no event registration changes' claims. (3) README.md and docs/slash-dispatch.md: replace any quoted 'Bar locked'/'Bar unlocked' with the `locked = true` shape (`grep -rn 'Bar locked\\|Bar unlocked' README.md docs/*.md`), and fix the enable/disable row's description in docs/slash-dispatch.md if it repeats the draw-gate claim.",
   "tests": "Red-first in tests/test_slashcmds.lua. Change ':98-106' to assert the outputs contain 'locked = true' and 'locked = false', and no longer 'Bar locked'. Add an in-combat case: set T.mocks InCombatLockdown to answer true (restore it after), run `slash('unlock')`, and assert the output contains 'Cannot unlock the bars during combat', its LAST line ends with 'locked = true', and NS.GetSetting('locked') is true (-- red under: restoring the fixed 'Bar unlocked' line). Add a panel-refresh case: spy NS.RefreshOptionsPanel and assert lock and unlock each call it once (-- red under: echoStored without the refresh). R-05 is comment-only. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'Bar locked\\|Bar unlocked\\|second rung and nothing else' -- settings README.md docs/slash-dispatch.md docs/smoke-tests.md (empty); test-cases diff empty",
   "smoke": "In client: start combat unlocked (the combat re-lock prints 'Bars locked \u2014 combat started'), then /at unlock in combat prints the refusal and 'locked = true'; out of combat /at unlock with the options panel open ticks Lock frame off at once.",
   "finding_ids": [
    "AbsorbTracker-R-03",
    "AbsorbTracker-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-05",
   "repo": "AbsorbTracker",
   "title": "A profile adopt publishes POSITION/APPEARANCE/REPAINT once, not twice, on an enable edge",
   "change": "core/AbsorbTracker.lua adoptProfile (:298-325). Capture `local wasDown = NS.lifecycle:IsDown()` before NS.SyncEnabledHold(). Delete the no-op `NS.lifecycle:Reevaluate()` at :318: lifecycle:Set already runs edge() (libs/LibKa0s/Lifecycle.lua Reevaluate is just edge()). Always send UNITS. Send POSITION, APPEARANCE and REPAINT only when the latch did not just stand up (`if not (wasDown and not NS.lifecycle:IsDown()) then ... end`), because StandUp (core/Lifecycle.lua:123-131) has already published all three. Rewrite the comment at :310-316 to say so. Keep the MigrateProfileToV3 call and RefreshOptionsPanel.",
   "tests": "Characterization first, in tests/test_database.lua: spy NS.bus:SendMessage and count NS.MSG.APPEARANCE deliveries. (a) A same-state profile switch (both profiles enabled): exactly 1, and that holds before and after the fix. (b) An off-to-on switch (switch from a profile storing enabled=false to one with the default): record today's count of 2, then change the assertion to 1 in the fix commit (-- red under: restoring the unconditional publishes). (c) An on-to-off switch leaves the latch down and publishes nothing that reaches a subscriber. Restore the Default profile and enabled=true in cleanup. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'lifecycle:Reevaluate' -- core (empty); test-cases diff empty",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-06",
   "repo": "AbsorbTracker",
   "title": "Migration stamp per WS-03: defaults declare schemaVersion 0, the runner stamps only past a step that returned, and the profile-scoped v2 step runs for every stored profile",
   "change": "(1) defaults/Profile.lua :103-115: `NS.defaults.global.schemaVersion = 0`. Rewrite the comment with the savedvariables-\u00a71 (v2.65.0) reasons: AceDB's removeDefaults strips a stored value equal to its default, and AceDB backfills a declared default onto a legacy account; 0 has neither problem. Leave the per-profile stamp (:68, default 1, register row savedvariables-\u00a71) unchanged. (2) core/Database.lua: publish `NS.SCHEMA_VERSION = SCHEMA_STEPS[#SCHEMA_STEPS].to`, derived and never a second literal, plus the suite seam `NS.__migrationSteps = SCHEMA_STEPS`. RunMigrations: `g.schemaVersion = g.schemaVersion or 0`. For each step with `g.schemaVersion < step.to`, run `local ok, err = pcall(step.apply, profile)`. On failure, emit `NS.Debug('Migrate', 'v%s -> v%s failed: %s', ...)` and one chat line `NS.Print('Settings upgrade stopped at v' .. from .. ' -> v' .. step.to .. '; saved settings were left as they were')`, and `break`, so the stamp does not advance and later steps do not run. On success, log as today and stamp `step.to`. (3) The v2 step (:164-167) is profile-scoped and was gated by the account-wide stamp. Make it `forEachProfile(function(p) if type(p) ~= 'table' or p.updateInterval == nil then return false end; p.updateInterval = nil; return true end)` with a Debug count line, like v4/v5. (4) docs: ARCHITECTURE.md's '### The per-profile schema stamp, in full' and savedvariables register row text, docs/schema.md's migration section, and CLAUDE.md if it quotes 'schemaVersion = 1'.",
   "tests": "Red-first in tests/test_database.lua. (a) `assertEqual(NS.defaults.global.schemaVersion, 0)` (red today: 1). (b) Build db with global {schemaVersion=1} and sv.profiles {Default={updateInterval=0.1}, Raid={updateInterval=0.2}}, then RunMigrations: neither profile keeps updateInterval (red today: Raid keeps it). (c) Append `{ to = NS.SCHEMA_VERSION + 1, apply = function() error('boom') end }` to NS.__migrationSteps with global.schemaVersion = NS.SCHEMA_VERSION. RunMigrations does not raise (pcall-wrapped assert), the stamp stays NS.SCHEMA_VERSION, and one chat line names the stop. Remove the step in cleanup (-- red under: stamping before apply, or no pcall). (d) An existing case: a nil stamp still reaches 5. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (NS:RunMigrations at CCN 10 or below); git grep -n 'schemaVersion or 1' -- core (only MigrateProfileToV3's per-profile read remains); test-cases diff empty",
   "smoke": "In client: /reload an existing install; /at debug on shows the [Init] line 'schema v5'; the settings are unchanged.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "WS-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-07",
   "repo": "AbsorbTracker",
   "title": "Every event registration goes through LibKa0s-Core's SafeRegister helpers; rejected names are kept and surfaced by /at debug events",
   "change": "(1) core/CoreSetup.lua. Live arm: `NS.SafeRegisterEvent = lib.SafeRegisterEvent`, `NS.SafeRegisterUnitEvent = lib.SafeRegisterUnitEvent`, `NS.SafeRegisterEvents = lib.SafeRegisterEvents`. Degraded arm (inside `if not lib`): three one-rung bodies with no front gate, per docs/api/Core/version-8-docs.md 'Degradation'. A local appendOnce(rejected, name) skips duplicates. SafeRegisterEvent does `local ok = pcall(target.RegisterEvent, target, event, handler); if not ok then appendOnce(rejected, event) end; return ok`. SafeRegisterUnitEvent(frame, event, rejected, u1, u2) is the same over RegisterUnitEvent. SafeRegisterEvents walks the array and returns the count. test_surface_parity's Core case derives from `^NS.X =` lines, so it covers the three names automatically. (2) core/State.lua: `NS.State.rejectedEvents = NS.State.rejectedEvents or {}` (session). (3) core/AbsorbTracker.lua. RegisterLifecycleEvents (:119-123): three `NS.SafeRegisterEvent(self, '<EVENT>', '<Method>', NS.State.rejectedEvents)` calls, driven from a local {event, method} array. SyncUnitEventFrames: `NS.SafeRegisterUnitEvent(f, 'UNIT_ABSORB_AMOUNT_CHANGED', rejected, unit)` and the same for UNIT_MAXHEALTH (:164-165), and the two swap registrations (:176, :181) via NS.SafeRegisterEvent. Each false return emits `NS.Debug('Events', 'rejected %s', event)`. That is 7 call sites; StandUp inherits them through the two methods. Update the comment at :114-117. core/Lifecycle.lua ADDON_EVENTS comment (:63-67): name the helper. (4) settings/Slash.lua runDebug: add sub-verb `events`, which prints 'Rejected events: none' or the comma-joined list, through a small sub-verb table so runDebug stays at CCN 10 or below. Help row: '`on`/`off` logging, `events`'. (5) core/DebugLogSetup.lua initSummary: append ', rejected events: <n>' when the list is non-empty. (6) docs/midnight-quirks.md: a note that a retired or unknown event name raises at registration (events-frames-taint-\u00a71), and that the helper front-gates with C_EventUtils.IsEventValid where present. Add docs/ARCHITECTURE.md '## Event Subscriptions' and docs/slash-dispatch.md rows for `/at debug events`.",
   "tests": "Red-first in a new suite tests/test_events.lua, listed in tests/run.lua immediately before test_disabled. (a) With T.mocks.__badEvents = { PLAYER_ENTERING_WORLD = true }, NS.addon:RegisterLifecycleEvents() does not raise; PLAYER_REGEN_DISABLED and PLAYER_REGEN_ENABLED are still registered (T.mocks.__registrations()); NS.State.rejectedEvents lists PLAYER_ENTERING_WORLD once, and still once after a second call (-- red under: a bare self:RegisterEvent). (b) Frame half, kit 26 (frame RegisterUnitEvent honors __badEvents): with __badEvents = { UNIT_MAXHEALTH = true }, SyncUnitEventFrames leaves UNIT_ABSORB_AMOUNT_CHANGED registered on the player frame and rejects UNIT_MAXHEALTH (-- red under: bare f:RegisterUnitEvent). (c) With C_EventUtils.IsEventValid mocked false for one name, that name is rejected without a RegisterEvent call. (d) `/at debug events` prints the list, and 'none' after cleanup. Cleanup clears __badEvents and rejectedEvents and re-runs RegisterLifecycleEvents/SyncUnitEventFrames so later suites see the full set. In tests/test_coresetup.lua: the degraded arm (loadDegraded) SafeRegisterEvent on a target whose RegisterEvent errors returns false and appends once (-- red under: a stub body without pcall). test_disabled stays green. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua (absorbEvent 0.0 api/iter unchanged); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -nE ':(RegisterEvent|RegisterUnitEvent)\\(' -- core modules settings (only inside SafeRegister bodies in core/CoreSetup.lua); test-cases diff empty",
   "smoke": "In client: /reload, /at debug on (the [Init] line has no 'rejected events'), /at debug events prints 'none'; absorbs, target and focus swaps and combat edges still repaint.",
   "finding_ids": [
    "AbsorbTracker-A-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "LK-11",
    "LK-04"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-08",
   "repo": "AbsorbTracker",
   "title": "Adopt Schema writeThrough {enabled, locked}; the Options stub's composers go hollow and the host copies are deleted; degraded pins become full, degraded and named-gap figures",
   "change": "(1) settings/Schema.lua: `local WRITE_THROUGH = { 'enabled', 'locked' }`, passed as `writeThrough = WRITE_THROUGH` to SchemaLib:New (:241). It covers both the live instance, for a partial load with Schema present and Options absent, and the AT-01 stub. The `announce` (:260-262) becomes: `if row.writeThrough then local react = NS.MasterReactions and NS.MasterReactions[row.path]; if react then return react() end end; if not row.onChange and NS.bus then ... APPEARANCE end`. On a load where the composed row is missing, the host's OWN reaction (SyncEnabledHold, and the locked combat refusal plus preview clear) still runs. This dispatches host code, not a copy of any library row. (2) settings/General.lua: publish `NS.MasterReactions = masterOnChange` (:132-226) after the table, and say why in a comment. (3) settings/OptionsSetup.lua degraded stub: delete ORDER_STEP, composeBlock and the ColorPair/FontGroup/BorderGroup/BarGroup/MasterControls bodies (:237-377) and Helpers.MASTER_GROUP (:327, whose only reader is inside General.lua's build(), which a degraded load never reaches). Replace them with hollow members: `for _, n in ipairs({'ColorPair','FontGroup','BorderGroup','BarGroup'}) do Helpers[n] = function() return {} end end` and `Helpers.MasterControls = function() return {}, function() end end`. Rewrite the stub comment (:188-236): the load-time set is still six members, and the five composers now answer {} per options-ui-\u00a71 (v2.65.0) and anti-pattern #73. tests/test_surface_parity.lua's Options ignore list gains 'MASTER_GROUP' with that reason. (4) settings/Schema.lua HostSchemaStub comment (:66-81): the host writers that reach composed rows now write through the declared list (WS-02 route (a)). (5) docs: docs/schema.md and docs/settings-panel.md degradation sections; ARCHITECTURE.md Settings Schema text naming the stub composers; module-map rows for OptionsSetup and Schema.",
   "tests": "Characterization first, green before and after, in tests/test_optionssetup.lua using its degradedWithDb helper. (a) `NS2.Slash:OnSlash('disable')` stores profile.enabled == false AND NS2.lifecycle:IsDown() is true; 'enable' brings it back up (-- red under: removing 'enabled' from WRITE_THROUGH, or dropping the announce's reaction dispatch). (b) 'lock'/'unlock' store `locked`; with InCombatLockdown mocked true, 'unlock' leaves locked true and prints the refusal. (c) tests/test_perf.lua's 'with LibKa0s absent, entering combat still re-locks' stays green. Then re-pin. tests/test_perf.lua:499-515 becomes three figures measured at this commit: full = #NS.Schema, degraded = #NS2.Schema, and a named gap = live paths minus degraded paths, asserted as a per-group count table ({['Master controls'] = n1, <each Appearance composed tab> = nk}) whose sum is full minus degraded. Also assert degraded is a subset of live (-- red under: a stub composer emitting rows). tests/test_optionssetup.lua:216-275: each composer answers #rows == 0 and MasterControls also answers a function tail (-- red under: a host copy of any composer, anti-pattern #73). Delete the leaf-shape asserts (:239-275). Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'composeBlock\\|ORDER_STEP' -- settings (empty); wc -l settings/OptionsSetup.lua (about 370, down from 509); test-cases diff empty",
   "smoke": "Library-less client: temporarily rename Interface/AddOns/AbsorbTracker/libs/LibKa0s and /reload. /at disable makes the bars go away and stay away through a target swap; /at enable brings them back; /at unlock then /at lock; no Lua error. Restore the folder after.",
   "finding_ids": [
    "AbsorbTracker-A-02",
    "AbsorbTracker-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "AT-01",
    "AT-04",
    "RV-AT",
    "LK-23",
    "WS-02"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-09",
   "repo": "AbsorbTracker",
   "title": "Slash degradation stub takes the LK-18 shape: DisabledLine from the verbatim library format, pinned with Kit.assertLibraryConstant; plain help rows with no FormatRow copy; the library-absent line for the schema verbs",
   "change": "settings/Slash.lua. (1) At file scope, `local STUB_DISABLED_LINE_FORMAT = '%s is disabled <em dash> enable it with |cFFFFFF00%s|r'`. These must be the verbatim bytes of LibKa0s lib.DISABLED_LINE_FORMAT at v1.56.0, with the em dash written as the three escaped bytes 226/128/148 as the library writes it. Publish it as `Sl.__STUB_DISABLED_LINE_FORMAT` (suite seam). The stub's DisabledLine (:503-505) becomes `STUB_DISABLED_LINE_FORMAT:format(NS.Constants.BRAND, '/at enable')`. Rewrite the comment at :499-502, which said the format must not be copied; LK-18 now prescribes the copy plus the pin. (2) Delete the stub's `FormatRow` (:491). PrintCmd (:32-34) prints `SlashLib.FormatRow and SlashLib.FormatRow(cmd, desc) or (cmd .. '  ' .. desc)`. The stub's LandingRows uses `'/at ' .. e[1] .. '  ' .. e[2]`. (3) The schema-verb refusal (:490, :506-511) prints the WS-02 library-absent line through the locale: add `L['LIBRARY_ABSENT'] = '%s is unavailable: the LibKa0s library did not load.'` to locales/enUS.lua and print `NS.L.LIBRARY_ABSENT:format('/at ' .. verb)`. Record in ARCHITECTURE.md's localization-\u00a71 register row that this line is routed. (4) docs/slash-dispatch.md degraded section (:240-255): the new stub shape.",
   "tests": "Red-first in tests/test_slashcmds.lua's 'degraded / live dispatcher parity' section. (a) `T.assertLibraryConstant(NS.Slash.__STUB_DISABLED_LINE_FORMAT, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')` (red today: the member does not exist; -- red under: any byte drift). (b) Degraded `/at list` prints exactly '/at list is unavailable: the LibKa0s library did not load.' (c) Degraded `/at help` rows contain no '|c' escape. (d) Degraded NS2.Slash:DisabledLine() equals the live NS.Slash:DisabledLine() with color escapes intact. If LK-18's assertLibraryConstant resolves only the registered surface source (tests/run.lua:55 registers the Slash INSTANCE, which has no DISABLED_LINE_FORMAT), stop and raise it against LK-18 rather than weakening the pin. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'FormatRow = function' -- settings (empty); test-cases diff empty",
   "smoke": "Library-less client (libs/LibKa0s renamed): /at help lists plain rows; /at list prints the library-absent line; no Lua error.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "AT-08",
    "RV-AT",
    "LK-18",
    "WS-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-10",
   "repo": "AbsorbTracker",
   "title": "The launcher's rung-(b) disabled gate moves into LibKa0s-Launcher minor 2's isEnabled/disabledLine",
   "change": "core/LauncherSetup.lua lib:New descriptor (:99-169). Add `isEnabled = function() return NS.GetSetting('enabled') ~= false end` and `disabledLine = function() return NS.Slash:DisabledLine() end`, both resolved at click time. Delete the hand-written gate at :160-162 from onClick, and cut the comment block :142-159 down to two lines naming the descriptor fields and launcher-\u00a72/slash-commands-\u00a77. The right-click and openSettings paths are unchanged. docs/ARCHITECTURE.md '## Launcher' and docs/slash-dispatch.md: the gate is the library's.",
   "tests": "tests/test_disabled.lua step 8 ('a disabled addon's launcher click wrote SavedVariables') and the single-refusal-line case must stay green with the host gate deleted. Verify by mutation that the pin is real: remove `isEnabled` from the descriptor, confirm step 8 goes red, then restore it. Add a tests/test_launcher.lua case: while enabled, a left click still toggles `locked` (-- red under: isEnabled answering false). Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'GetSetting(\"enabled\") == false' -- core/LauncherSetup.lua (empty); test-cases diff empty",
   "smoke": "In client: /at disable, left-click the minimap button, which prints the disabled line once and leaves the bars locked; right-click opens settings; /at enable, and a left-click toggles the lock.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "LK-16"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-11",
   "repo": "AbsorbTracker",
   "title": "/at test <value> [secs] moves to /at debug hold <value> [secs], with a validated duration and honest announce",
   "change": "settings/Slash.lua. (1) Remove the {'test', ...} row from NS.COMMANDS (:109-110) and runTest (:365-372). Rename runTestHold to runHold(args) and extend runDebug's sub-verb table from AT-07 with `hold`. (2) Validation: `n = tonumber(args[1])`; a missing n prints HOLD_USAGE = 'Usage: /at debug hold <value> [secs] \u2014 secs from 0.5 to 60, default 5'. `secs = args[2] and tonumber(args[2]) or 5`; a non-number or a value outside 0.5..60 prints the usage and holds nothing. Announce with `('Holding %s on the bars for %s s'):format(AbbreviateNumbers(n), ('%g'):format(secs))`. (3) `debug` is a live verb, but a hold paints the addon's bars, which is a feature. While `NS.GetSetting('enabled') == false`, `hold` prints NS.Slash:DisabledLine() and does nothing. `on`/`off`/`events`/bare stay live. (4) The debug help row names `hold <value> [secs]`. (5) Comments and docs: settings/Slash.lua :224-229 and :324-332; settings/General.lua :191 ('`/at test <value>` hold'); core/Lifecycle.lua :43-44; README.md :34-36 and :120 (plain prose); docs/slash-dispatch.md :85 (remove `test` from the refused list and note hold's own gate) and :152; docs/smoke-tests.md :67-68 and :485; the ARCHITECTURE.md Slash Commands table. This meets options-ui-\u00a715, preview-mode and the WS-06 (2) hold location.",
   "tests": "Move the '-- /at test' section (tests/test_slashcmds.lua:382-499, about 118 lines) into a new tests/test_debughold.lua, listed in tests/run.lua right after test_perfcmds, and retarget every case at `debug hold`. That also keeps test_slashcmds.lua away from the 1500 cap. Red-first additions: (a) `debug hold 1000 -3` prints the usage, and NS.HoldPreview (spied) is not called (-- red under: `tonumber(args[2]) or 5` with no range check); (b) `debug hold 1000 2.5` announces '2.5 s' and HoldPreview gets 2.5 (-- red under: '%d s'); (c) `debug hold 1000 61` is refused; (d) `/at test 1000` prints \"unknown command 'test'\"; (e) with enabled=false, `debug hold 1000` prints the disabled line and holds nothing. tests/test_disabled.lua's feature-verb walk loses `test` automatically because it iterates COMMANDS; confirm it has no literal 'test' entry (grep) and add hold's refusal to its step 7. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (runDebug and runHold at CCN 10 or below); git grep -n '/at test' -- ':!docs/audits' ':!docs/reviews' ':!docs/investigations' ':!docs/superpowers' ':!docs/automated-tests' ':!docs/revendor' (empty); wc -l tests/test_slashcmds.lua (about 1200); test-cases diff empty",
   "smoke": "In client: /at debug hold 50000 2.5 shows 50K for about 2.5 s and reverts on its own; /at debug hold 50000 -3 prints the usage; /at test 1 prints unknown command.",
   "finding_ids": [
    "AbsorbTracker-A-14",
    "AbsorbTracker-A-24",
    "AbsorbTracker-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-07",
    "WS-06"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-12",
   "repo": "AbsorbTracker",
   "title": "The minimap row's CLI path reads in the row's own sense: global.minimap.shown, inverting onto LibDBIcon's stored hide",
   "change": "(1) core/Constants.lua :56-77: `C.MINIMAP_PATH = 'global.minimap.shown'`. Move its comment to sit directly above the constant, after the BRAND block (this is also R-07's split-comment item). Rewrite the comment: the CLI name reads SHOWN (launcher-\u00a73 v2.65.0), storage stays LibDBIcon's `minimap.hide`, the row's get/set invert (core/Data.lua), and no `shown` key is ever stored (anti-pattern #81). (2) settings/Schema.lua defaultsRoot (:404-409): return nil for a row that owns its storage (`type(row.get) == 'function'`), so the validator does not report global.minimap.shown as missing from the defaults. Update the :228-231 comment. resetExempt and reachedByProfileReset key off the constant and need no change. (3) The core/Data.lua comments (:45-53) name the new path. The settings/OptionsSetup.lua :59 comment names the new path. defaults/Profile.lua minimap.hide = false is unchanged. (4) Docs: ARCHITECTURE.md :128, :322, :378-392; docs/settings-panel.md :228, :257; docs/slash-dispatch.md :130; README.md if it lists paths. No SavedVariables migration.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red-first in tests/test_launcher.lua. (a) `slash('get global.minimap.shown')` prints 'global.minimap.shown = true' while db.global.minimap.hide is false (red today: Setting not found). (b) `slash('set global.minimap.shown false')` leaves db.global.minimap.hide == true and NS.MinimapShown() false. (c) `slash('get global.minimap.hide')` prints 'Setting not found' (-- red under: keeping the hide spelling as the path). (d) NS.ValidateSchema() reports missing == 0. The existing :276-400 cases use the constant and stay green. Re-pin any literal `resolved` count in tests/test_schema.lua down by one (git grep -n 'resolved' tests/test_schema.lua), because the minimap row no longer resolves against defaults. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git grep -n 'global\\.minimap\\.hide' -- core settings docs/ARCHITECTURE.md docs/settings-panel.md docs/slash-dispatch.md README.md (empty); test-cases diff empty",
   "smoke": "In client: /at get global.minimap.shown answers true; the Minimap button checkbox hides the button and the value survives /reload; Reset all settings leaves a hidden button hidden.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "WS-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-13",
   "repo": "AbsorbTracker",
   "title": "Delete the test-only player aliases NS.bar / NS.statusBar / NS.valueText / NS.backdropInfo from the production namespace",
   "change": "modules/Bar.lua: delete :163-172, the comment and the four assignments. Rewrite every test read to NS.bars.player / NS.bars.player.statusBar / .valueText / .backdropInfo: tests/test_display.lua (about 46 lines), tests/test_data.lua (3) and tests/test_slashcmds.lua (3, or tests/test_debughold.lua after AT-11). Keep the reads identical in meaning. Update docs/module-map.md's Bar.lua row if it lists the aliases.",
   "tests": "Test-only rewiring with no new case. Before editing the tests, delete the four assignments and confirm the suite goes red at exactly the alias reads. Then rewrite the reads until it is green. Case count is unchanged.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua; git grep -nE 'NS\\.(bar|statusBar|valueText|backdropInfo)([^A-Za-z0-9_s]|$)' -- core modules settings tests ':!tests/_kit' (empty); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-11"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-14",
   "repo": "AbsorbTracker",
   "title": "Stub-surface parity cases for LibKa0s-Perf-1.0 and LibKa0s-Lifecycle-1.0",
   "change": "tests/test_surface_parity.lua: raise the header count (:3-4) from seven seams to nine. Add two cases. (a) Perf, grep-derived: the case comment names `grep -rohE '\\bPerf[.:][A-Za-z_]+' core modules settings | sort -u`. Assert the members the host reaches, {on, suspended, Note, OnCommand}, are present on BOTH NS.Perf (live) and loadDegraded().Perf, as functions where live is a function. The whole-surface form is not used because the live Perf instance has far more public members than the four-member stub (core/PerfSetup.lua:23-30). (b) Lifecycle: register `['LibKa0s-Lifecycle-1.0'] = NS.lifecycle` in tests/run.lua's Kit.setSurfaceSource (:51-63), then `T.assertSurfaceParity(NS2.lifecycle, 'LibKa0s-Lifecycle-1.0', { <live-only members, each with a reason> })`. The ignore set is whatever the live instance publishes beyond the stub's Hold/Release/Set/IsHeld/IsDown/Holds/Reevaluate/PrintHolds/name (core/Lifecycle.lua:155-177), measured at this commit. The case comment names `grep -rohE 'lifecycle[.:][A-Za-z_]+' core modules settings`.",
   "tests": "New cases only. Prove each by mutation: delete `Note` from core/PerfSetup.lua's stub and see the Perf case go red; delete `Holds` from core/Lifecycle.lua's stub and see the Lifecycle case go red; revert both. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); test-cases diff empty",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-15",
   "repo": "AbsorbTracker",
   "title": "Evaluate O.RenderTabbedSchema's v1.56.0 opts for the Appearance page's hand-built strip: adopt, or file a state:triaged decline",
   "change": "settings/UnitPanel.lua renderUnitPanelBody step 2 (:313-343) hand-builds partitionTabs, then Helpers.TabStrip, then RenderRows or renderMirroredHint. Read LibKa0s docs/api/Options/version-24.31.4.7.4-docs.md (RenderTabbedSchema opts). ADOPT only if all four hold: (i) a PageHeader drawn BEFORE the call survives, and its ctx.__bannerHeight still feeds the strip's reservation; (ii) rows come from descriptor rowsForPage(pageKey, ctx.unit); (iii) disabledFor(cfg) is re-evaluated on every render and tab click; (iv) skipRender rows stay out of the strip. If adopting: replace step 2 with `Helpers.RenderTabbedSchema(ctx, pageKey, nil, <per the doc>, { disabledFor = function() return NS.Units.IsMirrored(ctx.unit) end, disabledNotice = 'Linked to Player <em dash> uncheck to customize.' })`, and delete partitionTabs, Helpers.__partitionTabs, renderMirroredHint and the onSelect closure. Keep the step-1 PageHeader, the chrome-widget release and the two-tier refresher. If ANY condition fails: make no code change, and file `gh issue create -R tusharsaxena/AbsorbTracker --label enhancement --label state:triaged --label severity:low --title 'Adopt O.RenderTabbedSchema opts for the Appearance page strip'` whose body names the failing condition and the re-check trigger (the next Options minor). The commit then carries only the A-04 comment below. A-04, either way: tests/test_widgets.lua :850 gets a comment that the strip is library-drawn (O.TabStrip / O.RenderTabbedSchema), so the wrap-invariance pin (band and row offsets identical for every tab selection) is LibKa0s's suite's and is not duplicated here (options-ui-\u00a713 Testing, WS-06 (3), testing-\u00a78).",
   "tests": "Characterization first, green before and after an adoption, in tests/test_widgets.lua. For each unit in NS.Units.LIST and each tab: the strip's tab keys equal the schema group order; the rendered labels equal that group's rows; a mirrored unit (units.focus.mirror = true) renders the hint text and no appearance rows; ctx.__bannerHeight > 0 and ctx.chromeHeight >= ctx.__bannerHeight. The adoption must keep these green; the mutation 'disabledFor answers false' reddens the mirrored case. If the item declines, the characterization cases still land as the host pin of its own composition.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); if adopted, git grep -n 'partitionTabs' -- settings tests (empty); if declined, gh issue list -R tusharsaxena/AbsorbTracker --search 'RenderTabbedSchema' shows the new issue; test-cases diff empty",
   "smoke": "If adopted, in client: Settings > Ka0s Absorb Tracker > Appearance. Each unit shows the same five tabs, clicking tabs keeps the picker block in place, Focus with 'Linked to Player' shows the hint only, and unchecking it restores the rows.",
   "finding_ids": [
    "AbsorbTracker-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "LK-28",
    "WS-06"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-16",
   "repo": "AbsorbTracker",
   "title": "Delete the dead GetAddOnMetadata global rung; compat-layer stays Not applicable under compat's new applicability condition",
   "change": "core/EnvSetup.lua NS.Meta (:65-73): delete the `if GetAddOnMetadata then ... end` rung (:70-71), and cut the doc comment's ladder to Env, then C_AddOns, then nil. .luacheckrc read_globals: remove 'GetAddOnMetadata' (it becomes unreferenced). docs/ARCHITECTURE.md :626 compat-layer.md row: 'Not applicable \u2014 this addon calls no deprecated or version-variant client API outside LibKa0s's majors (compat, v2.65.0 applicability condition); the TOC metadata read is LibKa0s-Env-1.0's'. The core/EnvSetup.lua header (:9) and docs/module-map.md EnvSetup row: the same ladder.",
   "tests": "In tests/test_envsetup.lua: with the Env lib and C_AddOns both absent and a global GetAddOnMetadata mock present, NS.Meta('Version') answers nil and the mock is never called (-- red under: restoring the global rung). The existing ladder cases stay green. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . (0/0 with the name removed); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -n 'GetAddOnMetadata' -- core settings modules .luacheckrc | grep -v 'C_AddOns\\|Env\\.' (empty); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); test-cases diff empty",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "WS-05"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-17",
   "repo": "AbsorbTracker",
   "title": "Prune the eight read_globals no authored file reads",
   "change": ".luacheckrc read_globals (:40-55): remove C_Timer, hooksecurefunc, CreateColor, PlaySound, strsplit, strtrim, tinsert and tremove. The only authored C_Timer hit is a comment at settings/OptionsSetup.lua:144, and it stays. After this, a raw C_Timer or hooksecurefunc call lints red, which enforces library-stack-\u00a71's AceTimer rule.",
   "tests": "None \u2014 lint config only. The proof is luacheck staying 0/0. tests/test_lintconfig.lua checks ignore shape only and is unaffected.",
   "verify": "luacheck . (0 warnings / 0 errors); for g in C_Timer hooksecurefunc CreateColor PlaySound strsplit strtrim tinsert tremove; do git grep -nw \"$g\" -- core modules settings defaults locales tests ':!tests/_kit' | grep -v -- '--'; done (empty); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-16"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-18",
   "repo": "AbsorbTracker",
   "title": "One literal each: PARENT_TITLE reads C.BRAND, and the schema printer loses its hand-typed [AT] fallback",
   "change": "(1) settings/OptionsSetup.lua:26: `local PARENT_TITLE = NS.Constants.BRAND`. Keep the local name. Constants loads at TOC line 44, long before OptionsSetup. (2) settings/Schema.lua :233-239: `local function chatPrint(line) NS.Print(line) end`. It is resolved at call time so suites can still spy on it; the dead `elseif DEFAULT_CHAT_FRAME` arm with its '|cFF00FFFF[AT]|r' literal is deleted, because CoreSetup (TOC :48) defines NS.Print on both arms before Schema (:73).",
   "tests": "tests/test_optionssetup.lua:297 already pins the panel name 'Ka0s Absorb Tracker'. Add an assert that it equals NS.Constants.BRAND (-- red under: re-typing a different literal). In tests/test_schema.lua: the Validate print path routes through NS.Print (spy) (-- red under: a DEFAULT_CHAT_FRAME write). Regenerate docs/test-cases.md and the badge if the count moves.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -n 'Ka0s Absorb Tracker\"' -- core settings modules (only core/Constants.lua); git grep -n 'cFF00FFFF\\[AT\\]' -- settings (empty); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output)",
   "smoke": "In client: Options > AddOns lists 'Ka0s Absorb Tracker'.",
   "finding_ids": [
    "AbsorbTracker-R-08",
    "AbsorbTracker-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-19",
   "repo": "AbsorbTracker",
   "title": "Three chat lines hand their parts to the printer; the events-frames-taint-\u00a78 register row's count is re-measured",
   "change": "(1) core/DebugLogSetup.lua:47: `NS.Print('debug logging', on and '|cff40ff40ON|r' or '|cffff4040OFF|r')`. (2) core/Lifecycle.lua:173: `NS.Print(addonName .. ':', #self:Holds() > 0 and table.concat(self:Holds(), ', ') or 'no holds')`. (3) settings/UnitPanel.lua:409: `NS.Print('Unit panel render failed:', err)`. NS.Print SafeToStrings each argument (core/CoreSetup.lua:67-76), so the raw err loses nothing. (4) docs/ARCHITECTURE.md events-frames-taint-\u00a78 register row: re-measure the pre-formatted print sites in settings/Slash.lua and settings/Schema.lua with the row's recorded grep (17 at the audit; AT-03/AT-04/AT-11 changed Slash.lua lines, so measure at this commit) and write that figure.",
   "tests": "Update any test asserting the old strings (git grep -n 'Unit panel render failed\\|debug logging' tests). Output must be byte-identical for these three lines, because NS.Print joins parts with one space: add one output assert per site where none exists (-- red under: a changed separator). Regenerate docs/test-cases.md and the badge if the count moves.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -nE 'NS\\.Print\\(\\(\"|NS\\.Print\\(\"[^\"]*\" *\\.\\.' -- core/DebugLogSetup.lua core/Lifecycle.lua settings/UnitPanel.lua (empty); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-11"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-20",
   "repo": "AbsorbTracker",
   "title": "Annotate the five load-bearing TOC lines and the State/Data/Database group",
   "change": "AbsorbTracker.toc, comment lines only, each placed directly above its file line. Above core\\Namespace.lua (:45): '# LOAD-BEARING: publishes NS.version, captured into the perf descriptor by core/PerfSetup.lua at file load.' Above core\\Bus.lua (:47): '# LOAD-BEARING: publishes NS.NewBusTarget / NS.MSG; core/AbsorbTracker.lua subscribes UNITS with them at file load behind a guard, so a later position fails silently.' Above core\\CoreSetup.lua (:48): '# LOAD-BEARING: publishes NS.Print / NS.Util.print and NS.LIBKA0S_MISSING, captured at file load by LauncherSetup, AbsorbTracker and the stub branches of DebugLogSetup and PerfSetup.' Above core\\PerfSetup.lua (:53): '# LOAD-BEARING: publishes NS.Perf, a file-scope upvalue in core/AbsorbTracker.lua, modules/Display.lua and modules/Timer.lua (performance-\u00a71).' Above core\\Units.lua (:55): '# LOAD-BEARING: publishes NS.Units, whose DeepCopy core/Database.lua takes at file load.' Also one conventional-group note over State.lua, Data.lua and Database.lua ('# Conventional order, not load-bearing: ...'). Keep the Lifecycle comment (:49-51) as is. Lines stay CRLF/LF per .gitattributes.",
   "tests": "None \u2014 TOC comments only. tests/test_loadorder.lua compares the file list, which is unchanged, and must stay green. test_eol stays green.",
   "verify": "grep -c 'LOAD-BEARING' AbsorbTracker.toc (5 new); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed, test_loadorder and test_eol green); luacheck .",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-05",
    "AbsorbTracker-A-06",
    "AbsorbTracker-A-07",
    "AbsorbTracker-A-08",
    "AbsorbTracker-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-21",
   "repo": "AbsorbTracker",
   "title": "Canonical standard citations in the addon's own files (localization-\u00a75, bare packaging and lint)",
   "change": "Addon half of A-17; LK-08 plus RV-AT already carried the kit's case names with the section sign. (1) 'localization-5' becomes 'localization-\u00a75' at tests/prose_waivers.lua:3 and :7, tests/test_docs.lua:194 and docs/module-map.md:852, plus any other hit from `git grep -nE 'localization-5|line-endings-[0-9]|layout-1\\b' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/investigations' ':!docs/superpowers' ':!docs/automated-tests/2*' ':!docs/revendor'`. (2) .pkgmeta :17, :20, :21: 'packaging.md:28' becomes 'packaging' (comments). (3) .luacheckrc :9, :14, :78 and every other 'lint.md' hit becomes 'lint' (comments; the code comment is edited on the finding's authority). First confirm that the addon's prose/ASCII scans tolerate the section sign in tests/prose_waivers.lua (outside the scan) and tests/test_docs.lua.",
   "tests": "None \u2014 comment and doc citations only. The kit's test_prose and the addon's test_docs must stay green; if a scan rejects the section sign in a .lua comment, stop and report it rather than reverting to the malformed form.",
   "verify": "git grep -nE 'localization-5|packaging\\.md:28|lint\\.md' -- ':!libs' ':!tests/_kit' ':!docs/audits' ':!docs/reviews' ':!docs/investigations' ':!docs/superpowers' ':!docs/automated-tests/2*' ':!docs/revendor' (empty); luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "LK-08"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-22",
   "repo": "AbsorbTracker",
   "title": "Comment and doc drift sweep: General diagram, stale figures, TOC-order claims, Suspend/Resume, file counts, module map, settings-panel row, and the documentation map's wording",
   "change": "One sweep, correcting each site to the code. settings/General.lua: redraw the :14-27 diagram with no [Test mode] row and with [Minimap button] (seven rows plus the button pair). Fix :38-43 ('all nine rows', 'That includes Test mode'). Move the console-path comment :73-75 onto DEBUG_CONSOLE_PATH (:80), above unlockGuard's own comment. At :209-210, drop '384.5 bytes' and point at tests/perf.lua `appearancePass`. At :308-309, the throttle reference becomes NS.GetThrottleWindow() (modules/Timer.lua:61). core/PerfSetup.lua :11-13 and ARCHITECTURE.md:74: the order is CoreSetup -> Lifecycle -> PerfSetup ('loads after core/Lifecycle.lua, whose latch the descriptor carries'). core/CoreSetup.lua :12-14: the same adjacency correction. core/AbsorbTracker.lua :114-117: 'StandUp restores what StandDown tore down' (no Suspend/Resume), if AT-07 did not already reword it. defaults/Profile.lua :145: settings/Appearance.lua. The MINIMAP_PATH comment was moved in AT-12; verify it. ARCHITECTURE.md:655 v1.41.0 vs :570/enUS v1.42.0: settle with `git -C ../LibKa0s log -S 'DISABLED_LINE_FORMAT' --oneline` and the tag, then fix the wrong one. ARCHITECTURE.md :30-34: '9 files bind addonName, 19 open `local _, NS`' with the nine named (measure with git grep -l 'local addonName, NS = ...'). docs/module-map.md:756: add Lifecycle.lua and LauncherSetup.lua. .luacheckrc :25-27: drop CoreSetup from the binding list and name the files that do bind. ARCHITECTURE.md:728: measure the census figure (wc -l tests/test_helpers.lua) or drop it for a pointer to RESULTS.md. docs/settings-panel.md:68: add the Minimap button row. A-26: ARCHITECTURE.md ~:590-592 names docs/automated-tests/<run>/ and docs/perf-analysis/<run>/ rather than whole directories, and the RESULTS.md row (:639) says 'generated, except the watch list's Disposition column'. A-18/A-25: add two rows to '### Recorded, but not deviations'. First: standalone-windows, no host window and no close-button wrapper; the perf panel's close control is the library's (core/PerfSetup.lua:112-122). Second: documentation-\u00a79, 19 files open `local _, NS = ...` and 7 carry self-naming headers, grandfathered.",
   "tests": "None \u2014 docs and comments only. tests/test_docs.lua heading and quote pins must stay green; update a pin only where it quotes a corrected line. Lint stays 0/0 and the case count does not move.",
   "verify": "luacheck .; ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -n 'all nine rows\\|384\\.5\\|GetSetting(.throttleWindow.)\\|Suspend()/Resume\\|settings/{Bar,Border,Font}' -- core settings defaults docs/ARCHITECTURE.md (empty); grep -c 'Lifecycle.lua\\|LauncherSetup.lua' docs/module-map.md (both present in the file table)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-R-07",
    "AbsorbTracker-A-26",
    "AbsorbTracker-A-18",
    "AbsorbTracker-A-25"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-07",
    "AT-11",
    "AT-12"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-23",
   "repo": "AbsorbTracker",
   "title": "The per-unit RegisterUnitEvent frames are the events-frames-taint-\u00a71 unit-filter carve-out, not a deviation: retire the register row",
   "change": "docs/ARCHITECTURE.md: delete the events-frames-taint-\u00a71 row from '## Documented deviations' (:652), leaving 3 rows (savedvariables-\u00a71, events-frames-taint-\u00a78, localization-\u00a71), and fix any 'N rows' sentence. Replace '### The per-unit event frames, in full' (:676-697) with a short design note under '## Event Subscriptions'. The note cites the carve-out and names each condition by symbol: the frame's only job is RegisterUnitEvent (through NS.SafeRegisterUnitEvent since AT-07) plus one OnEvent; it is held at addon.__unitEventFrames; it is unregistered in core/Lifecycle.lua StandDown; it is created once and reused. Reword ARCHITECTURE.md:507, the core/AbsorbTracker.lua:84-86 comment (comment only), docs/data-flow.md:77, docs/module-map.md:425 and :807, and docs/midnight-quirks.md:120 to 'the events-frames-taint-\u00a71 unit-filter carve-out'. midnight-quirks.md:120 also corrects the frame description to one frame per unit (player, target, focus). Frozen copies under docs/investigations and docs/superpowers are left as they are.",
   "tests": "None \u2014 docs and a comment. The kit's test_layout_cap reads the census under Documented deviations and must stay green. Update any tests/test_docs.lua pin that names the removed heading.",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); git grep -n 'events-frames-taint-\u00a71 deviation\\|documented events-frames-taint-\u00a71' -- ':!docs/audits' ':!docs/reviews' ':!docs/investigations' ':!docs/superpowers' ':!docs/automated-tests' ':!docs/revendor' (empty); awk '/^## Documented deviations/{f=1} f&&/^\\| `/{n++} /^### /{if(f)exit} END{print n}' docs/ARCHITECTURE.md (3); luacheck .",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-07",
    "AT-22"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AT-24",
   "repo": "AbsorbTracker",
   "title": "ARCHITECTURE.md hub back under documentation-\u00a73's spill rule: Settings Schema to docs/schema.md, Message Bus to docs/message-bus.md, the disabled-state essay to docs/lifecycle.md",
   "change": "docs/ARCHITECTURE.md is 788 lines. (1) '## Settings Schema' (:118-233, 116 lines): keep about 20 lines (the seam, the row count, writeThrough) and one link. Move the runtime, bulk-bracket and named-state detail into docs/schema.md without duplicating what it already says. (2) '## Message Bus' (:234-306, 73 lines): spill to a new docs/message-bus.md, which documentation-\u00a73 names as that section's spill target. Flip its Conditional-table row from 'Not applicable' to 'Present \u2014 spill target of the hub's ~60-line rule (the >10-message trigger is not met: 5 messages)'. If WowAddonStandards rules that the >10 threshold forbids the file, raise it upstream rather than spilling into data-flow.md. (3) '## The disabled state is total' essay (:400-486): move it to a new Tier 3 docs/lifecycle.md. Add '### Addon-specific (documentation-\u00a73, Tier 3)' after '### Verification and record' (:630) and register lifecycle.md there. Target: the hub at 400 lines or fewer, with no mandated section over about 60. Every moved paragraph leaves a one-line pointer. (4) Update README.md/CLAUDE.md doc maps if they list the docs, and tests/test_docs.lua heading pins.",
   "tests": "None \u2014 docs move. tests/test_docs.lua heading pins are updated to the new files, and the suite stays green with the case count unchanged, or regenerate docs/test-cases.md if a pin case is added for the new Tier-3 row.",
   "verify": "wc -l docs/ARCHITECTURE.md (<=400); awk '/^## /{if(n)print s, NR-n; s=$0; n=NR}' docs/ARCHITECTURE.md (no mandated section over about 60); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); ls docs/message-bus.md docs/lifecycle.md",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "AT-22",
    "AT-23"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-25",
   "repo": "AbsorbTracker",
   "title": "One consolidated span bundle records the 24 unrecorded LibKa0s re-vendors, v1.18.0 to v1.53.0",
   "change": "Create docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.55.0/ per the WS-01 / WA-01 format, with no per-tag 02-04 files. 01_DELTA.md line 1 is exactly `Delta: LibKa0s v1.18.0 -> v1.55.0 (span: v1.18.0 v1.18.1 v1.19.0 v1.23.0 v1.24.0 v1.26.0 v1.27.0 v1.28.0 v1.29.0 v1.36.0 v1.36.1 v1.36.2 v1.37.0 v1.38.0 v1.39.0 v1.42.0 v1.44.0 v1.45.0 v1.46.1 v1.47.0 v1.50.0 v1.51.0 v1.52.0 v1.53.0)`. The list must be the one the amended AUDIT.md step-4 script prints as unrecorded, not re-typed by hand. The body maps each tag to its vendoring commit, from `git log --format='%h %ad %s' --date=short -- libs/LibKa0s` and the CLAUDE.md provenance history (`git log -p -- CLAUDE.md | grep -n 'LibKa0s v'`). 05_SUMMARY.md has one line per tag: either 'carried by sweep, nothing adopted' or the adoption commit's hash and subject (from `git log --oneline <vendor-commit>..<next-vendor-commit> -- core settings modules`). Any frozen earlier bundle whose base was misstated is never edited; 01_DELTA.md notes the correction once. Update docs/ARCHITECTURE.md:592 and CLAUDE.md:43 if they enumerate revendor bundles.",
   "tests": "None \u2014 docs only (record-keeping, audit-review-history). The proof is the amended AUDIT.md step-4 check.",
   "verify": "Extract and run WowAddonStandards AUDIT.md's amended step-4 re-vendor script from this repo root (horizon per the script): it prints 0 unrecorded tags. head -1 docs/revendor/*-v1.18.0-v1.55.0/01_DELTA.md | grep -oE 'v[0-9]+\\.[0-9]+\\.[0-9]+' | wc -l (26: the 24 span tags plus the two range ends); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (test_prose green over the new files)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01",
    "WA-01",
    "RV-AT"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AT-DOCS",
   "repo": "AbsorbTracker",
   "title": "Close-out: sync-docs, regenerate docs/test-cases.md, record a fresh automated-test bundle, refresh the watch list, and tidy the issue store",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md and docs/ARCHITECTURE.md (count claims, slash/COMMANDS parity after AT-11, module map after AT-07/AT-13, the comment-citation check, v1.56.0 surfaces adopted). (2) `lua5.1 tests/run.lua --list > docs/test-cases.md`, and set the README Tests badge from its Totals. (3) Run `tests/_kit/run-automated-tests.sh` through ~/.claude/wow-addon/bin/ka0s-bounded to write docs/automated-tests/<YYYYMMDD-HHMMSS>/, write its ANALYSIS.md per the AUTOMATED_TESTS.md prompt, and let the runner regenerate docs/automated-tests/RESULTS.md (A-20). (4) In RESULTS.md's watch-list Disposition cells (the only hand-edited column): tests/test_helpers.lua 'Peel next by helper group \u2014 carried forward' (R-14), and tests/test_slashcmds.lua at its measured size (about 1200 after AT-11). (5) Issue store (A-19, A-27), with gh calls spaced at least 5 s apart. Close #25 with state:done and a comment giving `find libs/LibStub -type f` output (LibStub.lua only). Comment on #26 that its decline covers the Widgets Dropdown only and that the WidgetsDragHandle surface is adopted (modules/Bar.lua:27, :46); it stays closed as will-not-do. Comment on #10 that the 2026-09-23 audit referred it and it stays state:triaged, severity:high, outside this remediation's scope. No version bump, no Version History entry, no tag: the owner decides.",
   "tests": "None \u2014 docs and record only. The bundle itself is the verification record.",
   "verify": "lua5.1 tests/run.lua --list > /tmp/l && diff <(tr -d '\\r' </tmp/l) <(tr -d '\\r' <docs/test-cases.md) (empty); grep -o 'Tests-[0-9]*%2F[0-9]*' README.md (matches Totals); ls docs/automated-tests/ | tail -1 (the new stamp) and test -f docs/automated-tests/<stamp>/ANALYSIS.md; the bundle's manifest shows lint pass, tests pass, perf pass, complexity pass with 0 above CCN 15; git ls-files '*.lua' ':!libs' ':!tests/_kit' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (empty); gh issue view 25 -R tusharsaxena/AbsorbTracker --json state (CLOSED)",
   "smoke": "",
   "finding_ids": [
    "AbsorbTracker-A-20",
    "AbsorbTracker-R-14",
    "AbsorbTracker-A-19",
    "AbsorbTracker-A-27"
   ],
   "issue_refs": [
    "AbsorbTracker#25",
    "AbsorbTracker#26",
    "AbsorbTracker#10"
   ],
   "depends_on": [
    "RV-AT",
    "AT-01",
    "AT-02",
    "AT-03",
    "AT-04",
    "AT-05",
    "AT-06",
    "AT-07",
    "AT-08",
    "AT-09",
    "AT-10",
    "AT-11",
    "AT-12",
    "AT-13",
    "AT-14",
    "AT-15",
    "AT-16",
    "AT-17",
    "AT-18",
    "AT-19",
    "AT-20",
    "AT-21",
    "AT-22",
    "AT-23",
    "AT-24",
    "AT-25"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-00",
   "repo": "PanelMaster",
   "milestone": "M3",
   "title": "Clear the 1 re-vendor red(s) RV-PM recorded, so every later M3 commit starts green",
   "change": "RV-PM copied LibKa0s v1.56.0 and recorded 1 failing test(s) in its commit body (`git log --grep=\"^RV-PM\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned PM-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PM"
   ],
   "effort": "M"
  },
  {
   "id": "PM-01",
   "repo": "PanelMaster",
   "title": "Schema degradation stub matches v1.56.0: SetMany, instance-id forwarding on Get/ApplyDefault, row.normalize",
   "change": "PRE: RV-PM has landed, so libs/LibKa0s/ and tests/_kit/ are v1.56.0 / kit 26, and NS.SchemaRuntime (live, Schema minor 2) has SetMany. The stub is the only thing out of step: tests/test_surface_parity.lua 'Parity: the Schema seam's degraded surface matches the live one, on both levels' goes red naming SetMany. If RV-PM already put a SetMany member on the stub to keep its commit green, this item keeps only the behavior below and the cases.\n\nsettings/Schema.lua, hostSchemaStub() -> stubLib.New(_, d). Mirror LK-22 without the log or the tally.\n(1) Extract a local `prepare(row, path, value, id)`. It resolves `stored` / target(path, id) as R.Set does today, runs row.validate(value, rid) (refusal: `false, \"invalid value\", why`), and then runs `row.normalize(value, rid)` when present: the returned value replaces the input, and `nil, why` refuses with `false, \"invalid value\", why`. It checks nowhere-to-store and returns a plan table {row, parts, root, first, rid, value}.\n(2) Add a local `commit(plan)`: row.set(value), or stubLib.Write(root, parts, copy(value), first); then row.onChange(value, rid).\n(3) R.Set(path, value, id): find the row (unknown path answers `false, \"unknown path: \"..path`), then prepare, commit, d.announce, and return true. The observable order is unchanged.\n(4) New R.SetMany(entries, opts). opts = opts or {}. Phase 1: for each entry i, find the row; an unknown path returns `false, \"unknown path: \"..tostring(e.path), nil, i`. Run prepare with opts.instanceId, and on refusal return `false, err, why, i`. Nothing is stored in phase 1. Phase 2: when opts.act is set, R.BulkBegin() ... R.BulkEnd() (depth only), and commit each plan in order. Then, if d.announceBatch is a function, call it once with an array of {row=, path=, value=} and rid. Otherwise call d.announce per write. Return true.\n(5) R.Get(path, id) calls row.get(id). R.ApplyDefault(row, id) forwards id to R.Set(row.path, copy(row.default), id) (LK-22 R-14 parity).\nEvery stub function stays at CCN 10 or below. Update the stub header comment ('write-completing and log-silent') to name SetMany and normalize.\nAlso fix any other PanelMaster red the LK-05/LK-06/LK-07/LK-33 dry-runs recorded (<scratchpad>/dryrun-*-PanelMaster.txt) that RV-PM left. Give the setup what production does; never weaken an assertion.",
   "tests": "Red first, in tests/test_schema.lua (645 lines), on a degraded arm built with tests/degraded_env.lua loadPartial({ Schema = true }) and on the live NS.SchemaRuntime.\n(a) 'Schema stub: SetMany stores every entry in order and runs each onChange': two rows, settings.gridSize and settings.snapToGrid.\n(b) 'Schema stub: SetMany is all-or-nothing': entry 2 is invalid (gridSize 999). Expect `false, \"invalid value\", why, 2`, and entry 1 is still its old value.\n(c) 'Schema stub: SetMany with opts.act runs inside one bracket': R.InBulk() is true from inside an onChange and false after.\n(d) 'Schema: the live runtime and the stub answer SetMany identically': the same entries go to both arms, and the return tuples and stored values are compared.\nBefore the stub change, (a)-(c) fail with 'attempt to call field SetMany', and the parity case is red. Add a '-- red under: delete R.SetMany from hostSchemaStub' comment.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` gives 0 warnings / 0 errors.\n- `lua5.1 tests/run.lua` gives 0 failed, and the Schema parity case passes.\n- `lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -w -C 15 .` prints nothing.\n- `find . -name '*.lua' -not -path './libs/*' -not -path './tests/_kit/*' | xargs wc -l | awk '$2!=\"total\" && $1>1500'` prints nothing.\n- No tests/perf.lua exists (perf skip).\n- `lua5.1 tests/run.lua --list > docs/test-cases.md`, and the README Tests badge is updated in the same commit (docs/testing.md 'The case inventory').",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "LK-22",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-02",
   "repo": "PanelMaster",
   "title": "Stand-down beats unlock: Canvas:Render strips the overlay while the latch is down, and the conformance suite gains the unlocked routes",
   "change": "modules/Canvas.lua, Canvas:Render (around :786-825). Read the latch once, before applySpec: `local down = NS.Lifecycle and NS.Lifecycle:IsDown()`. Replace `if NS.Lifecycle and NS.Lifecycle:IsDown() then spec.shown = false end` with `if down then spec.shown = false end`. Replace the unconditional `if NS.Unlock and NS.Unlock.Decorate then NS.Unlock:Decorate(f, rec) end` with the following, so a stood-down panel is hidden, mouse-transparent and not movable:\n\n```lua\nif down then\n  if NS.Unlock and NS.Unlock.StripOverlay then NS.Unlock:StripOverlay(f) end\nelseif NS.Unlock and NS.Unlock.Decorate then\n  NS.Unlock:Decorate(f, rec)\nend\n```\n\nLeave NS.State.unlocked and NS.State.unlockedPanels untouched. NS.StandUp -> RenderAll then re-decorates from current state (performance-\u00a76). Add no imperative Hide or force-lock in NS.StandDown (anti-pattern #85). Update the stand-down rung comment above it to say that the overlay is also stripped at this one source, and why Decorate's own f:Show() cannot override the latch. Update the header note at Canvas.lua:20-24, which names the two method-presence guards, to cover both branches.\n\ndocs/smoke-tests.md \u00a77 Enable / disable: add the step 'Unlock, then /pm disable: no outline, no panel, no drag. Untick Lock frame while disabled: still nothing. /pm enable: the outlines come back.'",
   "tests": "Red first, in tests/test_disabled.lua (580 lines). Add 'Disabled 5b: an unlocked panel is hidden, stripped and undraggable while stood down (routes A, B, C)', which uses seed()/shownPanels()/cleanup().\n- Route A: NS.Unlock:SetUnlocked(true), then S:Set(ENABLED, false).\n- Route B: S:Set(ENABLED, false), then S:Set('state.locked', false), and again via NS.Slash:OnSlash('set state.locked false').\n- Route C: disabled, then NS.Unlock:SetPanelUnlocked(id, true).\n- After each route: `#shownPanels() == 0`, and for every record `NS.Canvas:FrameFor(id):IsMouseEnabled() == false` (the wow_mock __mouse flag).\n- Positive half: S:Set(ENABLED, true). Both panels are shown with the mouse enabled.\n- Cleanup with SetUnlocked(false).\n\nAdd '-- red under: call NS.Unlock:Decorate unconditionally in Canvas:Render (modules/Canvas.lua)'. Run the suite before the Canvas change and watch A, B and C fail on shown=2/1.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` gives 0 warnings / 0 errors.\n- `lua5.1 tests/run.lua` gives 0 failed.\n- `lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -w -C 15 .` prints nothing; Canvas:Render stays at CCN 15 or below.\n- The 1500-line check prints nothing.\n- docs/test-cases.md is regenerated with `lua5.1 tests/run.lua --list`, and the README badge is bumped in the same commit.",
   "smoke": "In client (docs/smoke-tests.md \u00a77), with two panels:\n- /pm unlock, then /pm disable: no panel, outline or label is drawn, and dragging the empty spot moves nothing.\n- While disabled, untick Master controls > Lock frame, and tick a panel's Unlock on the Panels page: still nothing drawn.\n- /pm enable: the unlocked panels come back outlined and draggable.",
   "finding_ids": [
    "PanelMaster-R-01",
    "PanelMaster-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-03",
   "repo": "PanelMaster",
   "title": "Combat visibility flips at combat start: thread the REGEN event's truth into the render, Compat.InCombat prefers UnitAffectingCombat, and fix the reversed test",
   "change": "(1) core/Compat.lua:79-82, Compat.InCombat:\n\n```lua\nif type(UnitAffectingCombat) == \"function\" then\n  return UnitAffectingCombat(\"player\") and true or false\nend\nif type(InCombatLockdown) == \"function\" then\n  return InCombatLockdown() and true or false\nend\nreturn false\n```\n\nRewrite its comment: combat-reactive display reads the combat flag (events-frames-taint-\u00a72), and lockdown starts after PLAYER_REGEN_DISABLED.\n(2) modules/Canvas.lua. Canvas:Render(id, inCombat): `if inCombat == nil then inCombat = NS.Compat.InCombat() end`, then BuildSpec(rec, currentSettings(), inCombat). BuildSpec stays pure. Canvas:RenderAll(inCombat) passes it to each Render. Canvas:RenderForCombat(inCombat) passes it to RenderAll.\n(3) core/PanelMaster.lua. OnRegenDisabled calls NS.Canvas:RenderForCombat(true). OnRegenEnabled calls ResumePending first, then NS.Canvas:RenderForCombat(false). Update both comments.\n(4) modules/Unlock.lua keeps its InCombatLockdown deferral, unchanged. Add one line to its SetUnlocked comment saying why: an unlock is a lockdown question, not a display one.\n(5) .luacheckrc read_globals: add UnitAffectingCombat. If tests/test_lintconfig.lua pins the read_globals list, extend it.\n(6) docs/smoke-tests.md \u00a78 Combat gating: add the Only-in-combat / Only-out-of-combat pull check.",
   "tests": "Red first.\n\ntests/test_canvas.lua 'Canvas: leaving and entering combat both reach the renderer' (:422-446). Deliver NS.addon:OnRegenDisabled() with T.mocks.__inCombat still FALSE (the client's order), and assert the outOfCombat panel is hidden. Then set __inCombat = true, run the fight, set __inCombat = false, and call OnRegenEnabled: the panel is shown. Add a mirrored case for visibility = 'inCombat': hidden before, shown after OnRegenDisabled with the flag still false, hidden after OnRegenEnabled. Add '-- red under: drop the explicit inCombat argument from OnRegenDisabled (core/PanelMaster.lua)'.\n\ntests/test_compat.lua:\n- 'Compat.InCombat follows UnitAffectingCombat, not lockdown': set T.mocks.__inCombat = true, locally override the mock's InCombatLockdown to return false, assert Compat.InCombat() == true, and restore it.\n- The fallback: UnitAffectingCombat = nil, so the answer follows InCombatLockdown.\n- Both APIs nil: the answer is false.\nDo not edit tests/_kit/.\n\nRun before the fix: the reordered case fails ('entering combat did not hide the panel').",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean, including the new read_global.\n- `lua5.1 tests/run.lua` gives 0 failed; tests/test_disabled.lua 'Disabled 5' still drives OnRegenDisabled/OnRegenEnabled with no args.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "In client (docs/smoke-tests.md \u00a78):\n- General visibility 'Only in combat': panels hidden out of combat; pull a target dummy and they appear on the first swing; they hide when combat ends.\n- 'Only out of combat': the reverse.\n- While in combat, /pm unlock still queues ('unlock queued').",
   "finding_ids": [
    "PanelMaster-R-03",
    "PanelMaster-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-04",
   "repo": "PanelMaster",
   "title": "/pm recover bounds offsets in the panel's scaled space: one Util.EffectiveScale shared by the renderer and Recover",
   "change": "(1) core/Util.lua: add `function Util.EffectiveScale(rec, settings)`, which returns the own scale times the master scale:\n- own = Util.Clamp(rec and rec.scale, C.MIN_PANEL_SCALE, C.MAX_PANEL_SCALE, C.PANEL_TEMPLATE.scale).\n- master = tonumber(settings and settings.scale); use 1 when it is nil or <= 0 (the guard moved from Canvas.masterScale).\nDocument it as the one definition render and recover share (options-ui-\u00a715).\n(2) modules/Canvas.lua addGeometry: `spec.scale = Util.EffectiveScale(rec, settings)`. Delete the now-unused local masterScale (:80-89) and move its comment to Util.\n(3) modules/Registry.lua R:Recover (:876-904): read settings once (`local settings = NS.db and NS.db.profile and NS.db.profile.settings or {}`). Per record, `local s = Util.EffectiveScale(rec, settings)`, and bound with offsetRange(relPoint, w / s) and offsetRangeY(relPoint, h / s). Add a comment: stored offsets are in the frame's scaled units because applySpec calls SetScale before SetPoint (Canvas.lua:566-569).",
   "tests": "Red first, in tests/test_registry.lua next to the Recover cases (:283-330). Use `local w = NS.Compat.GetScreenSize()`.\n- 'Registry.Recover: at an effective scale of 0.5 a visible TOPLEFT panel at 1.5 x screen width is left alone': rec.scale = 0.5, master 1.\n- 'Registry.Recover: at an effective scale of 2 a panel at 0.75 x screen width is off-screen and is moved': rec.scale 1, master scale 2.\nThe five scale-1 cases are unchanged. Add a tests/test_util.lua case for Util.EffectiveScale: the clamp bounds, master nil/0/negative giving 1, and the product. Before the fix, the scale-0.5 case moves the panel (red).",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .`: no unused masterScale.\n- `lua5.1 tests/run.lua` gives 0 failed; the tests/test_canvas.lua scale cases are still green.\n- lizard: no function above CCN 15; R:Recover stays low.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "In client (docs/smoke-tests.md \u00a710): set Master scale 0.5, drag a panel to the far right half, then /pm recover. It does not move. Set Master scale 2 and a panel whose scaled position is off-screen, then /pm recover. It comes back into view.",
   "finding_ids": [
    "PanelMaster-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-05",
   "repo": "PanelMaster",
   "title": "RenderAll releases every mismatched frame before rendering, so a profile swap of frame names never creates a duplicate named frame",
   "change": "modules/Canvas.lua Canvas:RenderAll(inCombat) (:829-842, keeping PM-03's parameter). Make it two passes:\n1. Build `want[id] = NS.Registry.FrameName(rec)` for every record in NS.Registry:All(). Use a file-local scratch table that is wiped at the top of each call, so RenderAll allocates nothing per call.\n2. For every `id, f in pairs(active)` where `want[id] ~= f.__frameName` (a retired id or a changed name): release(f); active[id] = nil.\n3. Canvas:Render(rec.id, inCombat) for each record.\nKeep Render(id)'s single-id mismatch branch (:811-820) as the fallback for the per-id MSG_PANEL path, and say so in its comment. Put the release pass in a local helper (releaseMismatched) so RenderAll stays at a small CCN. Update the pool comment (:25-40): the one-frame-per-name bound now holds across a profile switch that swaps names between ids, because the release pass runs before any acquire.",
   "tests": "Red first, in tests/test_profiles.lua: 'Profiles: swapping frame names across ids creates no second named frame'.\n1. Seed profile P1 with {1 = Alpha, 2 = Xray} and P2 with {1 = Xray, 2 = Alpha}, through the mock AceDB profiles.\n2. Switch between them six times via NS.db:SetProfile (fires OnProfileChanged).\n3. Wrap the mock's CreateFrame inside the case to count calls whose name argument is non-nil, and restore it afterwards.\n4. Assert the count is 0 after the first render.\n5. Assert `_G[name] == NS.Canvas:FrameFor(id)` for both ids after every switch.\n6. Assert NS.Canvas.PooledCount() does not grow.\nBefore the fix, the count is 6 (red).",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed, including tests/test_disabled.lua 'Disabled 5' and the existing test_profiles cases.\n- lizard: RenderAll and its helper are at CCN 15 or below.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "In client (docs/smoke-tests.md \u00a712): two profiles whose panels carry swapped names. Switch back and forth five times, then run /pm debug dump. The 'frames: N active, M pooled, 0 orphaned' line has no orphans, and /framestack over each panel names the expected PanelMaster_Panel_<slug>.",
   "finding_ids": [
    "PanelMaster-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-03",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-06",
   "repo": "PanelMaster",
   "title": "The Panels page's per-panel Unlock tick always shows the panel's real state",
   "change": "settings/PanelEditor.lua (1476 lines; this item adds at most 12 net lines, so it stays under 1500; the open split issue #47 is not in scope).\n(1) At :743-758 (the per-panel Unlock CheckBox):\n- Add `addRefresher(ctx, rec, function() unlocked:SetValue(NS.Unlock:IsPanelUnlocked(rec.id) and true or false); unlocked:SetDisabled(NS.State.unlocked == true) end)`.\n- Call `unlocked:SetDisabled(NS.State.unlocked == true)` at build.\n- Append to the existing tooltip body: ' Greyed out while Lock frame is unticked \\226\\128\\148 every panel is already unlocked.'\n- Replace the 'No refresher' comment with one line saying the refresher is driven by NS.PanelEditor:RefreshUnlock.\n(2) In wirePanelsBus (:1436), remember the context: `E.__ctx = ctx`.\n(3) Add `function E:RefreshUnlock() local ctx = E.__ctx; if ctx and NS.Helpers and NS.Helpers.RefreshPanel then NS.Helpers.RefreshPanel(ctx, false) end end`. RefreshPanel is the scalar tier, and it only repaints a shown page. No new bus message.\n(4) modules/Unlock.lua: at the end of U:SetUnlocked, U:SetPanelUnlocked (the non-deferred path) and U:ResumePending, add `if NS.PanelEditor and NS.PanelEditor.RefreshUnlock then NS.PanelEditor:RefreshUnlock() end`, the presence-guard precedent at modules/Registry.lua:516.",
   "tests": "Red first, in tests/test_panel.lua (1353 lines; stay under about 60 added lines) or tests/test_unlock.lua if the page builder is reachable there. Build the Panels page with the suite's existing page helpers, select a panel, and find the Unlock checkbox through the kit's AceGUI fake.\n- (a) NS.Unlock:SetUnlocked(true): the box reads true and is disabled. SetUnlocked(false): it reads false and is enabled.\n- (b) Set T.mocks.__inCombat = true, tick the box: it is deferred and reads false. Set __inCombat = false and call NS.addon:OnRegenEnabled(): the box reads true.\nBefore the change, (a) and (b) fail because no refresher runs.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.\n- lizard: no function above CCN 15.\n- `wc -l settings/PanelEditor.lua` is 1488 or less, and the 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "In client (docs/smoke-tests.md \u00a79): open Panels, select a panel. Untick Lock frame on General: the per-panel Unlock box on Panels shows ticked and greyed. Re-lock: unticked and enabled. Tick it in combat, leave combat: the box shows ticked.",
   "finding_ids": [
    "PanelMaster-R-08"
   ],
   "issue_refs": [
    "PanelMaster#47"
   ],
   "depends_on": [
    "RV-PM",
    "PM-02",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-07",
   "repo": "PanelMaster",
   "title": "One grid maximum: C.MAX_GRID becomes 64, matching the slider",
   "change": "core/Constants.lua:100: C.MAX_GRID = 64. No UI or CLI path could ever store more, because the CLI clamps to row.max (libs/LibKa0s/Slash.lua) and the slider stops at 64. settings/Schema.lua:71-76: the gridSize row declares `max = C.MAX_GRID`, so the literal and the constant cannot drift again; validate already uses C.MAX_GRID. modules/Unlock.lua:39's clamp now bounds at 64. A hand-edited SavedVariables value of 65-128 snaps at 64 on the next drag and is otherwise harmless; say so in the Constants comment. If tests/test_constants.lua pins 128, update it.",
   "tests": "Red first, in tests/test_schema.lua:\n- 'Schema: the grid-size slider and the write seam share one maximum': `S:FindRow('settings.gridSize').max == C.MAX_GRID`, and `S:Set('settings.gridSize', C.MAX_GRID + 1)` is refused.\n- tests/test_unlock.lua: U.SnapPosition with gridSize = 100 snaps on a grid of 64.\nBefore the change, max (64) is not C.MAX_GRID (128): red.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-08",
   "repo": "PanelMaster",
   "title": "Every event registration goes through Core's SafeRegisterEvent with a rejected-names list the debug dump prints",
   "change": "(1) core/CoreSetup.lua. Live arm, at column zero so tests/test_surface_parity.lua's Core derivation (`\\nNS%.([A-Za-z_]+)%s*=`) picks it up: `NS.SafeRegisterEvent = lib.SafeRegisterEvent` (LibKa0s-Core-1.0 minor 8, LK-11). Degraded arm, indented inside `if not lib then`: a one-rung body with no front gate:\n\n```lua\nfunction NS.SafeRegisterEvent(target, event, handler, rejected)\n  local ok = pcall(target.RegisterEvent, target, event, handler)\n  if not ok and type(rejected) == \"table\" then\n    -- append event once (no duplicates)\n  end\n  return ok\nend\n```\n\nPublish only SafeRegisterEvent. This seam's stated rule is that its surface is exactly what the addon calls. PanelMaster registers no unit events and no event arrays, so SafeRegisterUnitEvent and SafeRegisterEvents are not re-exported (note this beside the assignment).\n(2) core/State.lua: `State.rejectedEvents = {}`, session-only, with a comment.\n(3) core/LifecycleSetup.lua NS.StandUp (:126-129): the three registrations become `NS.SafeRegisterEvent(NS.addon, \"PLAYER_ENTERING_WORLD\", \"OnEnterWorld\", NS.State.rejectedEvents)`, and the same for the two REGEN events. A rejected name is logged with `NS.Debug(\"Events\", \"rejected %s\", name)` after the call, only when the call returned false. NS.StandDown keeps its bare UnregisterEvent calls.\n(4) core/PanelMaster.lua:63, OnInitialize: `NS.SafeRegisterEvent(self, \"PLAYER_LOGIN\", function() ... end, NS.State.rejectedEvents)`.\n(5) core/DebugLogSetup.lua attachDiagnose: add a line `rejected events: <n> (<comma-separated names>)`, or `rejected events: 0` when the list is empty, after addFrames. /pm debug dump then reaches it (events-frames-taint-\u00a71).\n(6) docs/debug.md and the ARCHITECTURE event-subscription text: registrations are pcalled through Core, and the rejected list is surfaced in the dump.",
   "tests": "Red first, in tests/test_disabled.lua or a new block in tests/test_harness.lua. The new file is tests/test_events.lua only if the kit inventory accepts it; if so, add it to docs/module-map.md in PM-22.\n- 'Events: a rejected event name is recorded and the rest still register'. Set T.mocks.__badEvents = { PLAYER_REGEN_DISABLED = true }. Drive S:Set(ENABLED, false) then S:Set(ENABLED, true), a real stand-down and stand-up.\n- Expect: no Lua error (pcall around the Set). PLAYER_ENTERING_WORLD and PLAYER_REGEN_ENABLED are registered (T.mocks.__registrations()). NS.Canvas.__ev is non-nil, so Canvas:Enable ran. NS.State.rejectedEvents equals {'PLAYER_REGEN_DISABLED'} after two stand-ups, with no duplicate. NS.DebugLog:Diagnose() contains 'rejected events: 1 (PLAYER_REGEN_DISABLED)'.\n- Reset __badEvents and the list afterwards.\n- The kit-26 C_EventUtils.IsEventValid mock front-gates the live arm. Add a second case with `C_EventUtils = nil` so the pcall rung is exercised.\n- Add '-- red under: bare NS.addon:RegisterEvent in NS.StandUp'. Before the change, the Set raises 'Attempt to register unknown event'.\nThe Core parity case must stay green; it now also checks SafeRegisterEvent on both arms.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed, including 'Parity: the Core seam's degraded surface matches the live one' and the survivor-set assertions in tests/test_disabled.lua.\n- `grep -rn 'RegisterEvent(' core modules settings | grep -v SafeRegisterEvent | grep -v UnregisterEvent` lists only non-registration text such as comments.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "LK-11",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-09",
   "repo": "PanelMaster",
   "title": "Library-absent composed-row verbs follow WS-02: enable/disable write through Schema writeThrough; lock/unlock print the library-absent line; the stub's DisabledLine is pinned to the library constant",
   "change": "Implements WS-02 route (a) for enable/disable (the SHOULD) and route (b) for lock/unlock. The composed Master-controls rows are absent whenever the Options major is: on a partial load where only Options is missing, and on a fully library-less load.\n(1) settings/Schema.lua. Pass `writeThrough = { S.ENABLED_PATH }` on the SchemaLib:New descriptor (Schema minor 2, LK-23). In hostSchemaStub's R.Set, when FindRow misses and the path is in the descriptor's writeThrough set (built once in New), store a copy raw via target()/stubLib.Write with no validate, normalize or onChange. Then call d.announce with a synthetic row `{ path = path, writeThrough = true }` built once per path, and return true. Every other row-less path is still refused ('unknown path: ...'). Update the stub header comment.\n(2) settings/Slash.lua, the shared part above the `if not lib` branch. Add `function Sl:CliEnable(on)`:\n- If NS.Schema:FindRow(S.ENABLED_PATH) finds the row, `return Sl:CliSet(S.ENABLED_PATH .. \" \" .. tostring(on))`. This is the live path, byte-identical to today.\n- Else, if NS.Schema:Set(S.ENABLED_PATH, on) succeeds, call NS.RefreshEnabled() (writeThrough skips onChange, so the latch is re-evaluated here), then print Sl.FormatKV(path, tostring(NS.Schema:Get(path))).\n- Else print Sl:LibraryAbsentLine('/pm ' .. (on and 'enable' or 'disable')).\nAdd `function Sl:CliLock(locked)`: a row present means CliSet('state.locked ' .. tostring(locked)); otherwise print Sl:LibraryAbsentLine('/pm ' .. (locked and 'lock' or 'unlock')) with no write and no error. `Sl:LibraryAbsentLine(verb)` returns `NS.L[\"%s is unavailable: the LibKa0s library did not load.\"]:format(verb)`, the slash-commands-\u00a71 line (WS-02). NS.COMMANDS enable/disable/unlock/lock call these four handlers. Fix the stale comment at :292-293 in the same edit: the enabled row's onChange is NS.RefreshEnabled() and it does not announce.\n(3) The Slash degradation arm, per LK-18's prescribed stub shape (docs/api/Slash/version-15-docs.md 'The degradation stub'):\n- Publish `Sl.DISABLED_LINE_FORMAT` on BOTH arms: live `= lib.DISABLED_LINE_FORMAT`; stub = the existing literal. Sl:DisabledLine() on the stub formats from it. It is a string, so the Slash parity case's function-only extra-member check is unaffected.\n- The degraded Sl.PrintHelp prints the UNAVAILABLE line once, then one `/pm <cmd>  <desc>` row per NS.COMMANDS entry, the `cmd  desc` shape with no FormatRow copy.\n(4) locales/enUS.lua: add the key, with a comment saying it is the collection's library-absent line and routes through the locale per slash-commands-\u00a71. docs/ARCHITECTURE.md: re-word the localization-\u00a71 register row: one string, the library-absent line, now routes through NS.L; everything else is still English-only. docs/slash-dispatch.md: a 'Library-absent load' paragraph.",
   "tests": "Red first.\n\ntests/test_libka0s.lua, fully degraded (Env.loadDegraded()) and partial (Env.loadPartial({ Options = true, OptionsWidgets = true, OptionsScroll = true, OptionsCompose = true })):\n- 'Degraded install: /pm disable and /pm enable write through and flip the latch without raising'. Run `pcall(ns.Slash.OnSlash, ns.Slash, 'disable')`: ok is true, ns.db.profile.settings.enabled is false, ns.Lifecycle:IsDown() is true, and the last chat line is PREFIX .. FormatKV('settings.enabled', 'false'). Then enable and assert the reverse.\n- 'Degraded install: /pm unlock and /pm lock print the library-absent line and change nothing': exactly one line, `NS.L[...]:format('/pm unlock')`; NS.State.unlocked is unchanged; no error.\n- 'Degraded install: the help index still lists every verb'.\n- On the live arm, the tests/test_slash.lua:741-744 echo cases stay unchanged.\n\ntests/test_surface_parity.lua, in the Slash case: `assertLibraryConstant(Sl.DISABLED_LINE_FORMAT, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')`, where Sl is the degraded arm (kit 26 LK-18). Add `['LibKa0s-Slash-1.0'] = mocks.LibStub('LibKa0s-Slash-1.0')` to tests/run.lua's Kit.setSurfaceSource map if the kit's name resolution needs it.\n\ntests/test_schema.lua:\n- 'Schema stub: a writeThrough path is stored without a row; any other row-less path is refused'.\n- The same on the live instance on the partial load.\n\nBefore the change, the degraded /pm disable prints the UNAVAILABLE text and the latch stays up (red).",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed: the Slash, Schema and Options parity cases, the 'L trap' cases in tests/test_libka0s.lua (no descriptor receives NS.L), and 'Degraded install: the schema loses the composed Master controls rows and NOTHING else' (the row count is unchanged; writeThrough adds no row).\n- lizard: CliEnable, CliLock and the stub R.Set are at CCN 10 or below.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "Optional, in client: temporarily rename libs/LibKa0s. /pm disable then /pm enable. The panels go and come back, the chat shows the echo, and there is no Lua error. /pm unlock prints '/pm unlock is unavailable: the LibKa0s library did not load.' Restore the folder.",
   "finding_ids": [
    "PanelMaster-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-01",
    "LK-23",
    "LK-18",
    "WS-02",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-10",
   "repo": "PanelMaster",
   "title": "Migration stamp per WS-03: defaults declare schemaVersion = 0, the v1->v2 step walks every stored profile, and the docs stop claiming the stamp is undeclared",
   "change": "(1) defaults/Global.lua: `NS.defaults.global = { schemaVersion = 0, minimap = { hide = false } }`. Replace the 'DELIBERATELY NOT SEEDED' block (:8-20) with the v2.65.0 reason (savedvariables-\u00a71). 0 masks no legacy account, because an unstamped account reads 0 and the runner opens its gate. A current-version default would be stripped by removeDefaults and never persist. The runner owns the stamp.\n(2) core/Database.lua NS:RunMigrations (:153-186):\n- `local from = tonumber(g.schemaVersion) or 0`.\n- Extract `local function stampFrameNames(profile) ... return rows end`.\n- The v1->v2 step (from < 2) walks EVERY stored profile: `for _, prof in pairs(NS.db.sv and NS.db.sv.profiles or {}) do ... end`, plus NS.db.profile if it is not among them (identity check). The account-wide stamp alone no longer gates a profile-scoped step.\n- The stamp is written only after the step returns without raising.\nUpdate the RegisterProfileCallbacks comment (:95-103): inactive profiles are now stamped at init, and R.Sanitize stays as the per-record repair for imported profiles. Fix the stale sentences R-09 names in these files: core/Database.lua:16 ('global holds only the schema stamp' becomes 'global holds the schemaVersion stamp and LibDBIcon's minimap table'), the :170 example ('schema v1' becomes 'schema v2'), and defaults/Profile.lua:4-5.\n(3) Docs (A-06). docs/ARCHITECTURE.md:34, docs/common-tasks.md:12-13, docs/module-map.md:24 and docs/profiles.md:11 each say that defaults/Global.lua declares schemaVersion = 0 (the runner's floor; the runner writes the real stamp) and LibDBIcon's minimap table. docs/schema.md:21 is left as is. Add no Documented-deviations row: declaring 0 is now compliance with toc-file-\u00a72 and savedvariables-\u00a71 (v2.65.0), which closes A-05 and A-17.",
   "tests": "Red first, in tests/test_database.lua:\n- 'Database: defaults declare schemaVersion 0': NS.defaults.global.schemaVersion == 0.\n- 'Database.RunMigrations: a legacy record in a NON-active stored profile gets its frame name': seed db.sv.profiles.Other.panels = {{ id = 1, name = 'Chat BG' }} with no frameName, reset db.global.schemaVersion to nil (reads the 0 default), run NS:RunMigrations(), and assert db.sv.profiles.Other.panels[1].frameName == NS.Util.FrameName('Chat BG').\n- 'Database.RunMigrations: the stamp lands above the declared default': db.global.schemaVersion == NS.SCHEMA_VERSION.\n- Update 'stamps a version onto an unstamped DB' and the MigrationSummary expectation to 'v0 -> v2'.\nBefore the change, the inactive profile stays unstamped (red).",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed; `S:Register()` returns 0.\n- `grep -rn 'only the schema stamp\\|carries only the\\|schemaVersion stamp \u2014 the one' docs core defaults` prints nothing outside frozen bundles.\n- lizard: RunMigrations at CCN 10 or below.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-05",
    "PanelMaster-A-06",
    "PanelMaster-A-17",
    "PanelMaster-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "WS-03",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-11",
   "repo": "PanelMaster",
   "title": "Minimap row CLI path reads in its shown sense: global.minimap.shown, stored at minimap.hide",
   "change": "settings/Schema.lua:\n- :187: S.MINIMAP_PATH = \"global.minimap.shown\".\n- Add S.MINIMAP_STORE = \"global.minimap.hide\", LibDBIcon's key and the only stored key.\n- The wired row (:530-539): get = `not SchemaLib.Read(NS.db, S.MINIMAP_STORE)`; set writes `SchemaLib.Write(NS.db, S.MINIMAP_STORE, not v)`, then calls NS.Launcher:SetShown(v).\n- S:SnapshotPersisted keeps excluding the row by S.MINIMAP_PATH.\n- S:Register: the defaultsRoot callback receives (parts, row). Return nil for `row and row.path == S.MINIMAP_PATH`: the row owns its storage and is checked by its store. After R.Validate, add one missing count, with a printed schema error, if `SchemaLib.Read(NS.defaults, S.MINIMAP_STORE) == nil`.\n- Rewrite the comments at :170-186 and :516-529 per launcher-\u00a73 (v2.65.0, WS-06). The row's path is the CLI name and reads SHOWN; the store stays hide; no `shown` key is ever written (anti-pattern #81).\ncore/LauncherSetup.lua:147: the comment names S.MINIMAP_STORE.\nDocs: docs/ARCHITECTURE.md:41, :201 and :215 and docs/schema.md:48 say `/pm get global.minimap.shown`, stored at db.global.minimap.hide. No SavedVariables migration is needed.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first, in tests/test_launcher.lua (:207-378).\n- Change the pin `assertEqual(S.MINIMAP_PATH, \"global.minimap.hide\")` to \"global.minimap.shown\".\n- Add 'Launcher: /pm get global.minimap.shown answers true while the button shows, and writes land on hide': `NS.Slash:OnSlash('get global.minimap.shown')` echoes 'true' when hide = false. `OnSlash('set global.minimap.shown false')` sets db.global.minimap.hide == true and db.global.minimap.shown == nil.\n- `S:Register()` returns 0.\n- Keep every hide-side assertion, which now reads through the store.\nBefore the change, the path pin and the get echo are red.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed, including both Defaults-survival cases in test_launcher.\n- `grep -rn 'global.minimap.hide' settings core docs/*.md` shows only the MINIMAP_STORE definition and docs that name the store.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "In client (docs/smoke-tests.md \u00a77b):\n- /pm get global.minimap.shown prints true.\n- /pm set global.minimap.shown false: the button goes.\n- LibDBIcon's right-click hide, then /pm get global.minimap.shown: prints false.\n- /reload: the button stays hidden.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "WS-06",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-12",
   "repo": "PanelMaster",
   "title": "Wrap the bus message constants in LibKa0s-Bus-1.0's Catalog behind a stub seam, with a parity case (#52)",
   "change": "(1) New core/BusSetup.lua:\n\n```lua\nlocal _, NS = ...\nlocal lib = LibStub and LibStub(\"LibKa0s-Bus-1.0\", true)\nif not lib then\n  NS.BusLib = { Catalog = function(_, messages) return messages end }\n  return\nend\nNS.BusLib = lib\n```\n\nThe header says only Catalog is adopted. The record half (Bus:New, tracked targets) is not offered, and NS.NewBusTarget in core/PanelMaster.lua stays. A missing library means plain tables with the same wire names.\n(2) PanelMaster.toc: add core\\BusSetup.lua right after core\\CoreSetup.lua, with a load-bearing comment: it must load before modules\\Registry.lua and settings\\Schema.lua, which call NS.BusLib.Catalog at FILE LOAD.\n(3) modules/Registry.lua:19-20: `local addonName, NS = ...`, then\n\n```lua\nlocal MSG = NS.BusLib.Catalog(addonName, {\n  PANELS = \"Ka0s_PanelMaster_PanelsChanged\",\n  PANEL  = \"Ka0s_PanelMaster_PanelChanged\",\n})\nR.MSG = MSG\n```\n\nfire(MSG.PANELS) and fire(MSG.PANEL). Delete R.MSG_PANELS and R.MSG_PANEL.\n(4) settings/Schema.lua:31: `S.MSG = NS.BusLib.Catalog(addonName, { SETTINGS = \"Ka0s_PanelMaster_SettingsChanged\" })`, and announce() sends S.MSG.SETTINGS. Delete S.MSG_SETTINGS.\n(5) Every reader moves to the strict catalog read: modules/Canvas.lua Enable (:858-860), settings/PanelEditor.lua wirePanelsBus (a rename only; the line count is unchanged), settings/Panel.lua, tests/test_canvas.lua, tests/test_disabled.lua, tests/test_panel.lua, docs/rendering.md and docs/settings-panel.md. Find them with `grep -rn 'MSG_PANELS\\|MSG_PANEL\\b\\|MSG_SETTINGS' core modules settings tests/*.lua docs/*.md`, which must print nothing afterwards.\n(6) tests/run.lua Kit.setSurfaceSource: add ['LibKa0s-Bus-1.0'] = mocks.LibStub('LibKa0s-Bus-1.0'). tests/test_harness.lua: add BusSetup to the TOC-order pin. docs/ARCHITECTURE.md Message Bus section: the Catalog and the strict read. docs/module-map.md: a core/BusSetup.lua row. CLAUDE.md:38: the adopted-majors count goes from 'Nine of the fifteen' to ten.\n(7) After the commit, close PanelMaster#52: `gh issue edit 52 -R tusharsaxena/PanelMaster --remove-label state:triaged --add-label state:done`, wait at least 5 s, then `gh issue close 52 -R tusharsaxena/PanelMaster --comment 'Built in <sha> (PM-12): Catalog over the three constants, core/BusSetup.lua stub, parity case.'`.",
   "tests": "Red first.\ntests/test_surface_parity.lua: 'Parity: the Bus seam's degraded surface matches the live one'. It uses loadPartial({ Bus = true }), then `assertSurfaceParity(degradedNS.BusLib, 'LibKa0s-Bus-1.0', { 'New' })`, with a comment that the record half is not adopted. It asserts degradedNS.Registry.MSG.PANELS == NS.Registry.MSG.PANELS (same wire names on both arms). Update the header count.\ntests/test_libka0s.lua:\n- 'Bus seam: a mistyped message key raises at the read': `assertErrorMatches(function() return NS.Registry.MSG.PANELZ end, 'no bus message named PANELZ')` (kit 26 LK-02).\n- The same case for NS.Schema.MSG.\n- On the degraded arm, the same read answers nil and does not raise.\nBefore the change, NS.BusLib and R.MSG do not exist (red).",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed, including tests/test_harness.lua's TOC order and tests/test_disabled.lua's survivor set (no new registration).\n- The grep in (5) prints nothing.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.\n- `gh issue view 52 -R tusharsaxena/PanelMaster --json state,labels` shows CLOSED with state:done.",
   "smoke": "In client: /reload. There is no Lua error at load. Dragging a panel and changing Master scale still repaint, so the bus is wired.",
   "finding_ids": [],
   "issue_refs": [
    "PanelMaster#52"
   ],
   "depends_on": [
    "RV-PM",
    "PM-09",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-13",
   "repo": "PanelMaster",
   "title": "Surface-parity case for the Lifecycle degradation stub",
   "change": "tests/test_surface_parity.lua: add 'Parity: the Lifecycle seam's degraded surface matches the live one'.\n- Build the degraded arm with loadPartial({ Lifecycle = true }). If Perf.lua or PerfPanel.lua refuses to load without Lifecycle (their NEEDS_* floor), omit them too, as the Options case omits its files together, and say so in a comment.\n- Assert degradedNS.Lifecycle ~= NS.Lifecycle.\n- Two-table form: `assertSurfaceParity(NS.Lifecycle, degradedNS.Lifecycle, 'Lifecycle stub', { ... })`. Each ignored member gets a one-line reason. `name` goes in the set only if it is walked. Any live-instance-only member is ignored only if the addon has no caller, per `grep -rn 'NS\\.Lifecycle[:.]' core modules settings`; name that grep in a comment.\n- Add the function-only reverse check the Slash case uses.\n- Add ['LibKa0s-Lifecycle-1.0'] = NS.Lifecycle to tests/run.lua's Kit.setSurfaceSource.\n- Update the file header count from six to eight cases (Core, DebugLog, Launcher, Slash, Options, Schema, Bus from PM-12, Lifecycle).\ncore/LifecycleSetup.lua:147-148 and docs/module-map.md:19 are now true; re-word them only if they give a count. docs/testing.md:340-351: the parity-case list and count are updated here, and PM-22 recounts again.",
   "tests": "This item is the test. Show it can fail: temporarily delete `LC.PrintHolds` from the stub in core/LifecycleSetup.lua, run it and watch the case go red naming PrintHolds, then revert.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.\n- lizard: no function above CCN 15.\n- The 1500-line check prints nothing.\n- test-cases.md and the badge are regenerated.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-12",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-14",
   "repo": "PanelMaster",
   "title": "TOC: comment the two load-bearing positions (Util after Constants, Slash after Schema) and mark the conventional groups",
   "change": "PanelMaster.toc:\n- Above core\\Util.lua (:56): '# Util AFTER Constants, load-bearing: core/Util.lua:4 captures `local C = NS.Constants` at file scope, so Util loading first would hold nil forever.'\n- Above settings\\Slash.lua (:97): '# Slash AFTER Schema, load-bearing: its dispatcher descriptor takes NS.SchemaRuntime's members as VALUES at file load (settings/Slash.lua:554-558).' Re-derive the line numbers at edit time.\n- One '# conventional from here' note, once per unannotated group: the core\\Namespace.lua / State / PanelMaster / Database run, # Defaults, and # Modules. Where a group already carries a load-bearing note, say which lines within it are conventional.\ntests/test_harness.lua already pins TOC order; confirm it ignores comment lines.",
   "tests": "none \u2014 comments only, no behavior change. tests/test_harness.lua's TOC-order pin must stay green, which proves the comments did not disturb the file list.",
   "verify": "All commands run under ~/.claude/wow-addon/bin/ka0s-bounded:\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.\n- `grep -n 'load-bearing' PanelMaster.toc` shows the Util and Slash lines.\n- `git diff --stat` touches only PanelMaster.toc.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-02",
    "PanelMaster-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-12",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-15",
   "repo": "PanelMaster",
   "title": "Issue store and register re-check: re-decide the architecture-\u00a75 trigger and close the five issues whose premises are resolved",
   "change": "(1) A-07, default decision (b) keep. The owner may pick (a) instead; that only changes the issue body. docs/ARCHITECTURE.md Documented deviations, the architecture-\u00a75 row (:378):\n- Re-word the re-check trigger to the host-side condition: 'settings/Schema.lua's resolveRoot maps an instance id to a registry record AND NS.Schema:Set/Get forward that id (the library has forwarded instanceId since Schema minor 2; the host does not)'.\n- Re-date Decided to the execution date, and cite the new issue.\n- File the issue first: `gh issue create -R tusharsaxena/PanelMaster --label enhancement --label state:triaged --label severity:low --title 'Instance-addressed schema rows for per-panel fields (architecture-\u00a75 option a)'`. The body is A-07's option (a): resolveRoot(parts, id) -> the panel record, instance-relative rows for C.PANEL_FIELD_TYPE fields, Registry:Set through NS.SchemaRuntime.Set(path, v, id), and the row retiring.\n(2) A-19. With at least 5 s between gh calls:\n- #19, #20, #22 and #23: `gh issue edit N -R tusharsaxena/PanelMaster --remove-label state:triaged --add-label state:done`, then `gh issue close N -R tusharsaxena/PanelMaster --comment '<one-line evidence>'`. The evidence: #19, docs/agent-context.md was deleted (CLAUDE.md:83). #20, registered at WowAddonStandards/standards/ADDONS.md:26. #22, PanelMaster.toc:13 X-Curse-Project-ID 1642836. #23, the curseforge badge at README.md:4.\n- #24: --add-label state:will-not-do, then close with 'Ratified performance-\u00a71 deviation, docs/ARCHITECTURE.md Documented deviations, owner 2026-08-25'.\nThe commit is the ARCHITECTURE.md row edit, and it names the new issue number.",
   "tests": "none \u2014 docs-only register edit plus GitHub issue writes; no code changes.",
   "verify": "- `gh issue list -R tusharsaxena/PanelMaster --state open --json number --jq '.[].number'` no longer lists 19, 20, 22, 23 or 24.\n- `gh issue view <new> -R tusharsaxena/PanelMaster` shows state:triaged.\n- `grep -n 'instance id' docs/ARCHITECTURE.md` shows the re-worded trigger.\n- `lua5.1 tests/run.lua` gives 0 failed, in case tests/test_docs.lua reads the register.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-07",
    "PanelMaster-A-19"
   ],
   "issue_refs": [
    "PanelMaster#19",
    "PanelMaster#20",
    "PanelMaster#22",
    "PanelMaster#23",
    "PanelMaster#24",
    "PanelMaster#49"
   ],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-17",
   "repo": "PanelMaster",
   "title": "Restore the two frozen specs' 'catalogued' now that the kit skips docs/superpowers/",
   "change": "Restore line 5 of docs/superpowers/specs/2026-07-31-panel-artwork-design.md and docs/superpowers/specs/2026-08-02-wiki-artwork-import-design.md byte-for-byte from `git show e30e329^:<path>`, putting back the 'catalogued' that e30e329 respelled to 'catalogd'. The kit-26 test_prose SKIPPED_DIRS now includes docs/superpowers/ (LK-07). Nothing in tests/prose_waivers.lua is needed; confirm there is no interim skipDirs entry to remove.",
   "tests": "none \u2014 restores frozen text. tests/_kit/test_prose.lua stays green because docs/superpowers/ is skipped from kit 26, which is the proof.",
   "verify": "- `git diff e30e329^ -- docs/superpowers/specs/2026-07-31-panel-artwork-design.md docs/superpowers/specs/2026-08-02-wiki-artwork-import-design.md` shows no line-5 difference.\n- `lua5.1 tests/run.lua` gives 0 failed, with the prose gate green.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "LK-07",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-18",
   "repo": "PanelMaster",
   "title": "One consolidated re-vendor span bundle for the 27 tags vendored without a record (v1.16.0 .. v1.54.2)",
   "change": "Write docs/revendor/<execution-date>-v1.16.0-v1.54.2/ with 01_DELTA.md and 05_SUMMARY.md. There is no folder per tag.\n\n01_DELTA.md line 1 is exactly the WA-01 span form: `Delta: LibKa0s v1.16.0 -> v1.54.2 (span: ...)`. Check the exact prefix against the updated commands/revendor-libka0s.md template. The span lists every unrecorded vendored tag, because the WS-01 recorded-side loop reads every vX.Y.Z on line 1 of a two-tag folder:\nv1.16.0, v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0, v1.27.0, v1.28.0, v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0, v1.54.2.\nRe-derive the list at edit time with `for c in $(git log --reverse --format=%h -- libs/LibKa0s tests/_kit); do git show $c:CLAUDE.md | grep -o 'LibKa0s[^v]*v[0-9.]*' | head -1; done`, minus the tags the existing bundles name (v1.15.0, v1.24.0->v1.25.0, v1.29.0->v1.34.0, v1.54.2->v1.55.0) and v1.56.0 (RV-PM's own bundle).\n\nThe body is a table: tag, vendoring commit (e.g. eddcf3d v1.16.0 \u2026 f2b08e2 v1.53.0; e30e329 carried v1.54.2 through tests/_kit and CLAUDE.md), and date. Add one note that each commit's own message is the record of what it adopted, and that frozen earlier bundles are not edited.\n\n05_SUMMARY.md: why this is consolidated (audit-review-history allows it), and that the AUDIT.md check now prints nothing.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none \u2014 docs-only frozen record; no code.",
   "verify": "- Re-run the AUDIT.md re-vendor two-listing check (WS-01 version: `git log --since=\"<horizon> 00:00\"`, with the recorded side reading every tag on line 1 of a two-tag folder). The 'vendored minus recorded' difference prints nothing.\n- `head -1 docs/revendor/*-v1.16.0-v1.54.2/01_DELTA.md` shows all 27 tags.\n- `lua5.1 tests/run.lua` gives 0 failed (docs/revendor/ is a skipped prose dir).",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01",
    "WA-01",
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-19",
   "repo": "PanelMaster",
   "title": "Correct the remaining stale comments and doc sentences: printer home, AceConsole embed, and the PLAYER_ENTERING_WORLD rationale",
   "change": "These are the R-09 sentences PM-09 and PM-10 did not already fix. PM-09 fixed the Slash.lua enabled-announce comment; PM-10 fixed defaults/Profile.lua:4-5, core/Database.lua:16 and core/Database.lua:170. Text only:\n- core/PanelMaster.lua:10: 'defined in core/Util.lua' becomes 'built by core/CoreSetup.lua (the LibKa0s-Core printer)'.\n- core/CoreSetup.lua:17-18: 'AceGUI's own :Print' becomes 'AceConsole's :Print'.\n- core/PanelMaster.lua:101-103, core/LifecycleSetup.lua:112-115 (NS.StandUp's gate comment), docs/ARCHITECTURE.md:228 and docs/data-flow.md:200: drop 'the registry's off-screen recovery measures UIParent'. Recovery is on demand only (modules/Registry.lua:854-858). State the real reason for painting at PLAYER_ENTERING_WORLD, which is that UIParent's final size and the frame anchors are settled by then and not at OnEnable. Re-read the code before writing it, and keep the boot-stand-up explanation.\nIf tests/test_docs.lua pins any of these strings, update it in the same change.",
   "tests": "none \u2014 comment and doc text only. `lua5.1 tests/run.lua` must stay green, which covers any test_docs pins.",
   "verify": "- `grep -rn 'recovery measures\\|AceGUI.s own :Print\\|defined in core/Util.lua' core docs/*.md` prints nothing.\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-10",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-20",
   "repo": "PanelMaster",
   "title": "Write docs/compat-layer.md for the eight Compat shims and flip the map row",
   "change": "New docs/compat-layer.md. One section per shim in core/Compat.lua, with line numbers re-derived:\n- AddOnFolders :27: the C_AddOns roster with a presence-guarded global rung, answering nil when neither exists.\n- GetScreenSize :56.\n- GetUIScale :65.\n- InCombat :79, in its PM-03 form: UnitAffectingCombat('player') first, then InCombatLockdown, then false.\n- RegisterMedia :103.\n- FetchMedia :124.\n- MediaList :139.\n- MouseIsOver :164.\nEach section covers what varies, what the guard answers when the API is absent, and who calls it (grep NS.Compat.<X>).\nLink PanelMaster#53, the declined LibKa0s-Compat-1.0 adoption, as the reason all eight stay addon-owned. Cite compat.md's v2.65.0 applicability condition (WS-05): this addon owns version-variant calls, so Compat.lua is required.\ndocs/ARCHITECTURE.md:327: the map row becomes '| compat-layer.md | Present | 8 shims in core/Compat.lua (threshold is 3) |'. If tests/test_docs.lua pins the map, update it.",
   "tests": "none \u2014 docs only; the shim count is checked by the documentation-\u00a73 grep in verify.",
   "verify": "- `grep -c '^function Compat\\.' core/Compat.lua` gives 8, matching the doc's section count.\n- `grep -n 'compat-layer' docs/ARCHITECTURE.md` shows Present.\n- `lua5.1 tests/run.lua` gives 0 failed, with the prose gate green on the new doc.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-04"
   ],
   "issue_refs": [
    "PanelMaster#53"
   ],
   "depends_on": [
    "RV-PM",
    "PM-03",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-21",
   "repo": "PanelMaster",
   "title": "Restore docs/ARCHITECTURE.md to the hub shape: spill the schema narrative to schema.md, collapse retired-row prose, canonical headings",
   "change": "docs/ARCHITECTURE.md is 471 lines; the target is under 400.\n- Move the schema-runtime adoption narrative, the stub and the Master-controls composition (:47-78, as re-located after PM-09 to PM-11) into docs/schema.md under '## The schema runtime'. Include the writeThrough enable path (PM-09) and the minimap.shown path (PM-11).\n- '## Settings Schema' keeps a summary of five lines or fewer, the registry-naming block (:80-103) and one link.\n- Collapse the retired-row narratives (about :380-428) to one line each, citing PM-029, #48 and the bundle that retired them.\n- Rename '## Message bus (`architecture-\u00a74`)' to '## Message Bus' and '## Taint' to '## Taint Notes' (documentation-\u00a73). Fix the in-repo anchors that point at the old headings (grep -rn '#message-bus\\|#taint' docs README.md CLAUDE.md).\n- Check tests/test_docs.lua for heading pins.",
   "tests": "none \u2014 docs-only restructure; the heading and anchor checks are in verify.",
   "verify": "- `wc -l docs/ARCHITECTURE.md` is under 400.\n- `awk '/^## Settings Schema/,/^## [^S]/' docs/ARCHITECTURE.md | wc -l` is about 60 or less.\n- `grep -n '^## Message Bus\\|^## Taint Notes' docs/ARCHITECTURE.md` shows both.\n- `lua5.1 tests/run.lua` gives 0 failed.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-15",
    "PM-20",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-22",
   "repo": "PanelMaster",
   "title": "Inventory sweep: module-map covers every test and tool file, and every stale count and comment is re-derived",
   "change": "(1) A-11, docs/module-map.md.\n- Add a '## tests/' table: rows for run.lua, wow_mock.lua, degraded_env.lua and prose_waivers.lua, plus one row per test_*.lua suite (or a suite-family table that names every file), including any file added by PM-01 to PM-13.\n- Add a '## tools/' table with four rows (tools/artwork/artwork_cleaner.py, make_poster.py, update_catalog.py, tools/sunn/build_manifest.py), each saying what it writes.\n(2) A-12. Recount with commands, never by hand:\n- Adopted seams: `grep -rhoE 'LibKa0s-[A-Za-z]+-1\\.0' core modules settings | sort -u | wc -l`, now ten with Bus.\n- Parity cases: `grep -c '^test(\"Parity' tests/test_surface_parity.lua`, now eight.\n- Majors: `grep -c 'Script file' libs/LibKa0s/LibKa0s.xml` or the majors list.\nFix docs/ARCHITECTURE.md:21-24 ('six of the eight seams'), docs/testing.md:106 ('Nine of the ten majors'), docs/testing.md:340-351 ('Four seams adopted', 'Two of the four'), and docs/module-map.md:69-70 ('the other nine').\nFix ARCHITECTURE.md:339 'never hand-edited': RESULTS.md's Disposition column is authored (docs/automated-tests/RESULTS.md:5).\n(3) settings/PanelEditor.lua comment blocks. :178-190: General is back and first. :1210: the band is one row, not three. :1220: refreshHeaderActs is gone. Rewrite them accurately and shorter, so the file shrinks or stays net-neutral.\n(4) .gitignore:6: re-point 'tools/artwork/wiki_import.py' at tools/artwork/artwork_cleaner.py, which writes manifest.tsv.root and .stamps.tsv (confirm with grep).",
   "tests": "none \u2014 docs and comments only. The basename check in verify is the mechanical test.",
   "verify": "- `for f in tests/*.lua tools/artwork/*.py tools/sunn/*.py; do grep -q \"$(basename $f)\" docs/module-map.md || echo MISSING $f; done` prints nothing.\n- `grep -rn 'six of the eight\\|Nine of the ten\\|Four LibKa0s seams\\|the other nine\\|wiki_import' docs .gitignore` prints nothing.\n- `wc -l settings/PanelEditor.lua` is not above its PM-06 figure.\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-11",
    "PanelMaster-A-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-13",
    "PM-21",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PM-23",
   "repo": "PanelMaster",
   "title": "DEPENDENCIES.md cites the kit by function name as well as by re-derived line",
   "change": "DEPENDENCIES.md:63, the POSIX-shell row, against the v1.56.0 payload (LK-01 moved the assertions into tests/_kit/asserts.lua; listDir stays in framework.lua).\n- Replace `tests/_kit/framework.lua:515-516` with `tests/_kit/framework.lua` listDir (`:<re-derived>`), behind Kit.assertSuiteInventory.\n- Replace `framework.lua:498-500` with the LuaFileSystem note at listDir (`:<re-derived>`).\n- Re-derive every other tests/_kit/*:NN citation in DEPENDENCIES.md, docs/testing.md and CLAUDE.md: `grep -rnoE 'tests/_kit/[a-z_]+\\.(lua|sh):[0-9]+' DEPENDENCIES.md docs/*.md CLAUDE.md`. Each is checked with sed -n and given its function name.",
   "tests": "none \u2014 docs only; the verify loop resolves every citation mechanically.",
   "verify": "- For every `tests/_kit/<f>:<n>` citation found by the grep above, `sed -n '<n>p' tests/_kit/<f>` shows the cited construct; check them one by one.\n- `lua5.1 tests/run.lua` gives 0 failed.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-24",
   "repo": "PanelMaster",
   "title": "README: no angle-bracket placeholders, Usage ends on the configuration pointer, one disabled-state paragraph",
   "change": "README.md.\n- :51 `/pm delete <name>` becomes `/pm delete ChatBG`.\n- :79-80 become `/pm get settings.gridSize` and `/pm reset settings.gridSize`.\n- Any other `<...>` in shipped README content becomes a bare example (grep '<[a-z]').\n- Merge the two disabled-state paragraphs (:86-95) into one.\n- Move the 'Everything else is configuration...' pointer (:67-69) to be the last line of Usage.\n- Where the README mentions the minimap row's CLI, use global.minimap.shown (PM-11).\n- Then run the /humanize de-AI pass over the edited paragraphs only.",
   "tests": "none \u2014 README prose only.",
   "verify": "- `grep -nE '<[a-zA-Z][a-zA-Z _-]*>' README.md` prints nothing apart from HTML or badge markup, if any.\n- The last paragraph of the Usage section is the configuration pointer.\n- `lua5.1 tests/run.lua` gives 0 failed (the prose gate scans README).",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-11",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PM-DOCS",
   "repo": "PanelMaster",
   "title": "Sync docs, regenerate the case inventory and badge, and record a fresh automated-test bundle (no version bump)",
   "change": "(1) Run the /wow-addon:sync-docs procedure:\n- README.md, CLAUDE.md (provenance line v1.56.0 as RV-PM rolled it; adopted majors ten of fifteen; kit revision 26), DEPENDENCIES.md, docs/ARCHITECTURE.md and the Tier-2 docs.\n- Count-claim verification, slash/COMMANDS parity, dead-export check, and the comment-citation check over core/, modules/ and settings/. A comment citation is corrected only on confirmation.\n(2) `lua5.1 tests/run.lua --list > docs/test-cases.md`. The kit-26 case names now carry \u00a7, e.g. localization-\u00a75 and line-endings-\u00a7N (LK-08). Update the README Tests badge from its Totals.\n(3) Record the bundle: `~/.claude/wow-addon/bin/ka0s-bounded tests/_kit/run-automated-tests.sh --label remediation-2026-09-23`, without --release, so there is no version bump and no tag.\n- The runner writes docs/automated-tests/<stamp>/ and rolls RESULTS.md.\n- Write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md playbook (/wow-addon:automated-tests).\n- Carry the four band dispositions forward in RESULTS.md (settings/PanelEditor.lua with #47, tests/test_artwork.lua, tests/test_panel.lua, modules/Artwork.lua).\n- Record once, forward-only (A-18): 1.1.1-release (dd04000) was tagged with no release bundle and with a TOC reading 1.1.0 (automated-tests-\u00a76). No bundle is stamped 1.1.1. The gap closes at the next release run through /wow-addon:bump-version.\n- The run is made after the doc edits, on a tree whose only uncommitted changes are docs. ANALYSIS.md says so. Everything goes in one commit.\n(4) Perf skip reason. LK-09 reads the Documented-deviations register for a performance-\u00a712 rule cell. PanelMaster's row is performance-\u00a71, and its own text says '\u00a712 does not apply and is not claimed'. So expect skip reason (1), no tests/perf.lua, NOT reason (2). Record whichever the runner writes, and do not re-label the register row to trigger (2).",
   "tests": "none \u2014 generated inventory and test record; the bundle is itself the test evidence.",
   "verify": "- `lua5.1 tests/run.lua --list | tr -d '\\r' | diff - <(tr -d '\\r' < docs/test-cases.md)` is empty.\n- The README badge equals the Totals.\n- `jq '.suites' docs/automated-tests/<stamp>/manifest.json` shows lint pass, tests pass, perf skip (read skipReason), and complexity pass with 0 functions above CCN 15.\n- `test -f docs/automated-tests/<stamp>/ANALYSIS.md`.\n- RESULTS.md's newest row names this commit's parent sha.\n- `luacheck .` is clean.\n- `lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -w -C 15 .` prints nothing.\n- The 1500-line check prints nothing.\n- `git log -1 --stat` shows no TOC Version change.",
   "smoke": "",
   "finding_ids": [
    "PanelMaster-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "PM-01",
    "PM-02",
    "PM-03",
    "PM-04",
    "PM-05",
    "PM-06",
    "PM-07",
    "PM-08",
    "PM-09",
    "PM-10",
    "PM-11",
    "PM-12",
    "PM-13",
    "PM-14",
    "PM-15",
    "PM-17",
    "PM-18",
    "PM-19",
    "PM-20",
    "PM-21",
    "PM-22",
    "PM-23",
    "PM-24",
    "LK-09",
    "LK-33",
    "PM-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-00",
   "repo": "PrettyChat",
   "milestone": "M3",
   "title": "Clear the 3 re-vendor red(s) RV-PC recorded, so every later M3 commit starts green",
   "change": "RV-PC copied LibKa0s v1.56.0 and recorded 3 failing test(s) in its commit body (`git log --grep=\"^RV-PC\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned PC-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PC"
   ],
   "effort": "M"
  },
  {
   "id": "PC-01",
   "repo": "PrettyChat",
   "title": "Carry the v1.56.0 stub surfaces: Core SafeRegister* bodies, and the Schema instance stub's SetMany, normalize and instanceId forwarding",
   "change": "Restores the surface-parity gate after RV-PC. The live Schema instance now has SetMany, so test_surface_parity's 'the Schema stub carries the whole live surface' case is red until this lands.\n(1) core/CoreSetup.lua, live arm, after `Util.SafeToString = lib.SafeToString` (~:95). Bind the helpers as values: `Util.SafeRegisterEvent = lib.SafeRegisterEvent`, `Util.SafeRegisterUnitEvent = lib.SafeRegisterUnitEvent`, `Util.SafeRegisterEvents = lib.SafeRegisterEvents`.\n(2) core/CoreSetup.lua, degraded arm, before `function NS.MakeCloseButton` (~:84). Add one-rung bodies as prescribed by LibKa0s docs/api/Core/version-8-docs.md 'Degradation': a pcall and no C_EventUtils front gate.\n- `local function addRejected(list, name)` appends only when list is a table and the name is not already in it.\n- `Util.SafeRegisterEvent(target, event, handler, rejected)` runs `pcall(target.RegisterEvent, target, event, handler)`. It calls addRejected on failure and returns ok.\n- `Util.SafeRegisterUnitEvent(frame, event, rejected, unit1, unit2)` does the same through frame.RegisterUnitEvent.\n- `Util.SafeRegisterEvents(target, events, handler, rejected)` walks the array and returns the number registered.\nExtend the degraded-arm comment to list them.\n(3) tests/test_surface_parity.lua coreSurface (:97-108): add SafeRegisterEvent, SafeRegisterUnitEvent and SafeRegisterEvents from instance.NS.Util, and add all three to the non-vacuity list at :114.\n(4) settings/Schema.lua SchemaStub (:614-715). Extract `local function stubPrepare(row, path, value, rid)`. It holds the existing validate call and a new `row.normalize(value, rid)` arm (answers the value to store, or nil, why, which refuses with 'PrettyChat: invalid value for <path>', why). It returns ok, value, err, why. stubSet (currently CCN 14) uses it and stores the prepared value.\n(5) Add `function S.SetMany(entries, opts)` to the stub instance in SchemaStub.New.\n- Phase 1 runs FindRow and stubPrepare for every entry, and returns `false, err, why, i` before any store.\n- Phase 2 calls `S.Set(e.path, prepared[i], opts.instanceId)` for each entry. When opts.act is given it runs inside `S.BulkRun(opts.act, opts.scope, fn)`. It returns true.\n- The stub does not call announceBatch. This descriptor declares none, so the per-write announce runs, the same as the live fallback.\n(6) Forward the instance id, mirroring LK-22 R-14: the stub's S.Get calls `row.get(id)` (:633), and `S.ApplyDefault(row, id)` passes id to S.Set (:688-693).\nKeep every stub function at CCN 10 or below. Update the stub's header comment (the 'write seam's order' note at :642).\n\nUpstream follow-up LK-11 (LibKa0s Core minor 8 adds SafeRegisterEvent / SafeRegisterUnitEvent / SafeRegisterEvents): parts (1)-(3) of this item are the consumer half of LK-11. The Core stub in core/CoreSetup.lua gains the SafeRegister* one-rung bodies, as LibKa0s docs/api/Core/version-8-docs.md 'Degradation' prescribes, and the live arm binds lib.SafeRegister*. PC-12 routes the Override.lua registration loop through them (PRETTYCHAT-A-09).",
   "tests": "Red first. Run `lua5.1 tests/run.lua` right after RV-PC and record the red Schema-instance parity case, where the live instance has SetMany and the stub does not. Add the three coreSurface keys before the Core code, which is red because the bare arm answers nil. In tests/test_libka0s.lua, add these degraded-arm cases, each with a `-- red under:` line:\n(a) 'degraded SafeRegisterEvents records a rejected name and registers the rest'. Setup: loadAddon({ skip = {'libs/LibKa0s/Core.lua'}, mock = function(m) m.__badEvents = { PLAYER_REGEN_DISABLED = true } end }), with a CreateFrame target and {'PLAYER_REGEN_DISABLED','PLAYER_REGEN_ENABLED'}. Assert it returns 1, rejected equals {'PLAYER_REGEN_DISABLED'}, nothing raises, and ENABLED appears in env.__registrations(). A second call leaves #rejected at 1.\n(b) 'degraded SetMany is all-or-nothing'. Entries: {Loot.enabled=false, <a Loot format path> = '%s %s %s %s', a surplus conversion formatAccepted refuses}. Expect false, err, why, 2 with Loot.enabled unchanged. A valid pair stores both and answers true.\n(c) 'degraded Get forwards the instance id', using a spy row added with NS.SchemaRuntime.AddRows.\nRegenerate docs/test-cases.md (`lua5.1 tests/run.lua --list > docs/test-cases.md`) and the README Tests badge in this commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # largest authored file < 1500. There is no tests/perf.lua (performance-\u00a712 exemption), so perf is skipped. `grep -n 'SafeRegister' core/CoreSetup.lua` shows both arms, and `grep -n 'SetMany\\|stubPrepare' settings/Schema.lua` resolves.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "LK-11",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-02",
   "repo": "PrettyChat",
   "title": "Slash v15 / kit 26 contract: the degraded DisabledLine is built from the library's verbatim format, pinned with Kit.assertLibraryConstant; pin CliSet's refusal echo",
   "change": "(1) settings/Slash.lua, degraded branch (:138-210).\n- Add `local STUB_DISABLED_LINE_FORMAT = \"%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r\"`. These are the verbatim bytes of libs/LibKa0s/Slash.lua:83 lib.DISABLED_LINE_FORMAT.\n- Hoist the descriptor's brandName 'Ka0s Pretty Chat' (:259) into one file-level `local BRAND_NAME` that both arms read.\n- Replace `DisabledLine = function() end` (:202) with `DisabledLine = function(_) return STUB_DISABLED_LINE_FORMAT:format(BRAND_NAME, \"/pc enable\") end`.\n- Publish the constant as `__disabledLineFormat = STUB_DISABLED_LINE_FORMAT` on the stub table. The `__` prefix keeps it out of Kit.publicMembers parity.\n- Rewrite the :196-201 comment and the header paragraph 'THE DEGRADED PATH DOES NOT REFUSE' (:122-128). The stub now builds the collection's line, as the LibKa0s Slash version-15 doc 'The degradation stub' and slash-commands-\u00a71 v2.65.0 (WS-02) require. It still refuses nothing, because the degraded enable/disable verbs write through the stub-composed General.enabled row (setEnabled, :404-421). Name that as the WS-02 route in the comment.\n- If tests/test_surface_parity.lua or tests/test_locale.lua:232 carry prose saying the stub DisabledLine answers nil, correct it.\n(2) LK-17 contract (Slash minor 15). CliSet now prints the write seam's refusal (INVALID, then err and why indented) instead of echoing the unchanged value. PrettyChat's formatAccepted gate (settings/Schema.lua:325) is reachable from `/pc set`, so pin that. No production code changes for this half.\n\nUpstream follow-up LK-18 (Kit.assertLibraryConstant plus the Slash version-15 doc's '## The degradation stub'): part (1) is the consumer half. The degraded Slash stub's DisabledLine (settings/Slash.lua) is built from STUB_DISABLED_LINE_FORMAT, and tests/test_surface_parity.lua pins it with Kit.assertLibraryConstant(\u2026, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT').",
   "tests": "(1) New case in tests/test_surface_parity.lua after the Slash case: 'the degraded DisabledLine is the library's line, byte for byte'. Call `ctx.assertLibraryConstant(parityBare.NS.SlashCommands.__disabledLineFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')` (Kit.assertLibraryConstant if Kit.expose does not carry it), then `t.eq(parityBare.NS.SlashCommands:DisabledLine(), parityLive.NS.SlashCommands:DisabledLine())`. It is red under the current stub, which answers nil. CAVEAT: this file registers the Slash INSTANCE as the surface source (:82-88), and the instance has no DISABLED_LINE_FORMAT. Check how docs/api/testkit/version-26-docs.md says assertLibraryConstant resolves. If it goes through the registered source, re-register `['LibKa0s-Slash-1.0'] = parityLive.env.LibStub('LibKa0s-Slash-1.0')` for this one call and restore the instance mapping straight after, so the parity cases still compare instances.\n(2) tests/test_slash.lua: '/pc set refuses a format the game cannot fill, and names why'. Run slash('set ' .. formatPath .. ' %s %s %s %s'). The output must contain 'Invalid value for ' .. formatPath and the formatAccepted reason on an indented line, and Schema.Get(formatPath) must be unchanged. It is green on arrival (a characterization of Slash 15); note '-- red under: Slash minor 14, which echoed the unchanged value'.\nRegenerate docs/test-cases.md and the README Tests badge in this commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua (performance-\u00a712). `grep -n '__disabledLineFormat' settings/Slash.lua tests/test_surface_parity.lua` resolves in both.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "LK-18",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-03",
   "repo": "PrettyChat",
   "title": "Migration runner: profile-scoped steps run on every stored profile, and the stamp advances only past a step that succeeded (WS-03)",
   "change": "core/Database.lua (:41-115). The migrations table becomes `migrations[v] = { scope = 'profile' | 'global', run = function(target, db, profileName) end }`, published as `Database.migrations` so a suite can inject steps on a fresh instance. It is still empty after this item.\n- New `local function runProfileStep(db, step)`. It walks the RAW SavedVariables profiles, `db.sv and db.sv.profiles` (the kit AceDB fake and real AceDB both expose db.sv), and pcalls step.run(profileTable, db, name) for every table-valued profile. It falls back to db.profile only when db.sv is absent. It returns false, err on the first raise.\n- runSteps(db, from) returns ran and `reached`. For v = from+1..SCHEMA_VERSION, a present step runs: profile scope through runProfileStep, global scope through pcall(step.run, db.global, db). On failure it prints the existing 'schema migration v failed: <err>' line and STOPS, returning the last v that completed. A missing step advances reached.\n- RunMigrations stamps `db.global.schemaVersion = reached` when from < SCHEMA_VERSION. It keeps normalizing a stamp that is already ahead to SCHEMA_VERSION, which the pinned case tests/test_database.lua:59 requires.\n- The [Migrate] trace reports from->reached.\n- Rewrite the header (:3-18) and the :78-97 comments. SCHEMA_VERSION is the runner's target. The runner owns the stamp. `global.schemaVersion = 0` is the declared default (already true at :36) and it survives AceDB's removeDefaults. Every step is idempotent against a fresh default profile.\nRewrite the core/PrettyChat.lua:54-56 comment. Profiles are lifted at load across every stored profile, so a copied profile is already current, and the re-run in the profile callbacks is an idempotent no-op plus the orphan repair. Keep RunMigrations and runSteps at CCN 10 or below; split helpers if needed.",
   "tests": "tests/test_database.lua. Each case uses a fresh ctx.loadAddon and injects into NS.Database.migrations with a test-local SCHEMA_VERSION, restored at the end. Mark each '-- red under: runSteps calling step(db) once and RunMigrations stamping unconditionally'.\n(a) 'a profile-scoped step lifts every stored profile, not only the active one'. The SV holds profiles Default and Alt, each with a marker, at global.schemaVersion = 1. A v2 profile step renames the marker. Assert both raw profiles are lifted, and SetProfile('Alt') reads the lifted shape.\n(b) 'a raising step leaves the stamp where it was'. A v2 global step raises. The stamp stays 1 and one 'schema migration 2 failed' line is printed.\n(c) 'a step after a failed one does not run'.\n(d) 'a profile step runs again safely on an already-lifted profile' (idempotence).\nExisting cases :10-157 stay green. Regenerate docs/test-cases.md and the README Tests badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua (performance-\u00a712). `lizard -l lua core/Database.lua` shows RunMigrations and runSteps at CCN 10 or below.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-04",
   "repo": "PrettyChat",
   "title": "Drop the dead Loot copies of LOOT_ITEM_CREATED_SELF/_MULTIPLE, with an idempotent v2 profile migration onto Tradeskill; no global registered twice",
   "change": "(1) defaults/Defaults.lua: delete the Loot entries for LOOT_ITEM_CREATED_SELF and LOOT_ITEM_CREATED_SELF_MULTIPLE (:39-46). Tradeskill (:329-336) is the only registration left.\n(2) core/Database.lua: SCHEMA_VERSION 1 -> 2, plus `migrations[2] = { scope = 'profile', run = liftLootCreatedSelf }`. For each of the two globals, the step does this:\n- If cats.Loot.strings[G] is set and cats.Tradeskill.strings[G] is nil, it copies the value there, unless the value equals NS.Defaults.Tradeskill.strings[G].default, which setters store as absence. NS.Defaults is loaded by OnInitialize time.\n- It clears cats.Loot.strings[G] and cats.Loot.disabledStrings[G].\n- It prunes an emptied strings or disabledStrings table and an emptied Loot table.\nThe step is idempotent. The runner's existing v1->v2 [Migrate] line is the one log line.\n(3) settings/Schema.lua: delete the Schema.crossRegisteredGlobals block and its stale comment (:446-468). Replace it with a load-time duplicate check that collects any globalName registered under two categories into `Schema.duplicateGlobals`. runValidation (:522) reports it through its existing loud-at-load channel, the same one unwiredMasterPaths use, and does not raise.\n(4) settings/Panel.lua: delete the 'Shared with' tooltip branch (:228-247). Delete its key from locales/enUS.lua:58.\n(5) Docs, all in this commit:\n- docs/ARCHITECTURE.md: delete the Known Limitations bullet 'Cross-registered globals: last-writer-wins' (:186). In the localization-\u00a71 register row (:276), change the 'three routed strings' list to two by dropping `Shared with %s \u2026`.\n- docs/data-flow.md:145.\n- docs/slash-dispatch.md:77.\n- docs/module-map.md:153 (the crossRegisteredGlobals entry).\n- docs/smoke-tests.md T-53 (:416): rewrite it as a single-registration check.\n- Update any schema row count that moves (4 rows fewer): grep '174\\|rows' in the docs and in comments such as settings/Schema.lua:882. The '79 globals' figure is unchanged, because the removed entries were duplicates.",
   "tests": "Red first.\n- tests/test_defaults.lua: 'no Blizzard global is registered under two categories' walks NS.Defaults. It replaces the :246 cross-registration pin and is red today.\n- tests/test_apply.lua: replace the :167 case with 'the Tradeskill format is the one that reaches _G for LOOT_ITEM_CREATED_SELF, and no Loot row exists for it'.\n- tests/test_panel.lua: replace :917-937 with 'no per-string tooltip carries a Shared with line'.\n- tests/test_schema.lua: replace the :249 loop with an assertion that Schema.duplicateGlobals is empty.\n- tests/test_database.lua, migration v2: (a) a Loot-only override moves to Tradeskill; (b) an existing Tradeskill override is kept and Loot's copy is dropped; (c) Loot disabledStrings is dropped and empties are pruned; (d) a second run changes nothing; (e) an inactive stored profile is lifted too, which depends on PC-03.\n- Update the full-vs-degraded row counts pinned in tests/test_libka0s.lua, and any row-count assertion; both arms fall by the same 4.\n- tests/test_locale.lua: the removed key leaves the manifest.\nRegenerate docs/test-cases.md and the README Tests badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500 (tests/test_panel.lua 1053 stays in band). No tests/perf.lua. `grep -rn 'crossRegisteredGlobals\\|Shared with' --include='*.lua' --include='*.md' . | grep -v '^./docs/\\(audits\\|reviews\\|revendor\\|automated-tests/2\\|superpowers\\)'` prints nothing.",
   "smoke": "In-client: set a custom Tradeskill 'Created' format, then create or loot-create an item. The chat line uses it. `/pc test category Tradeskill` shows the string once, and `/pc test category Loot` no longer lists it. After /reload with an old SavedVariables that had a Loot-only override, the override now shows under Tradeskill.",
   "finding_ids": [
    "PRETTYCHAT-R-02"
   ],
   "issue_refs": [
    "PrettyChat#4"
   ],
   "depends_on": [
    "RV-PC",
    "PC-03",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-05",
   "repo": "PrettyChat",
   "title": "Move Schema.ResetRows onto the schema runtime's BulkRun/BulkAdd bracket (issue #18)",
   "change": "settings/Schema.lua, resetRowsBody (:932-956) and Schema.ResetRows (:958-968).\n(1) Filter first. Build `eligible` from rows that pass `S.FindRow(row.path) == row and not refusedBySignature(row, row.default)`. If it is empty, return 0 without opening a bracket, so an act with no eligible row still logs nothing, as today.\n(2) Run `S.BulkRun('reset', label, walk)` (NS.SchemaRuntime). act = 'reset' and scope = label keep the console line byte-identical, `[Set] reset <label>: N rows`, and the library's STOPPED suffix equals NS.Util.STOPPED.\n(3) In walk, for each eligible row: `local before = row.get(); PrettyChat.Batch(function() row.set(row.default) end); if row.get() ~= before then S.BulkAdd(1); n = n + 1 end`. That is a read-back count, the runtime's own semantic.\n(4) Keep the reapply and category tracking. The single `PrettyChat:ApplyStrings()` (when reapply) and the single `Schema.NotifyPanelChange(category or nil)` stay INSIDE walk, so a raise in either is stopped-marked, as today.\n(5) Delete the `{wrote, changed}` tally, the line() closure and the NS.Util.RunAct call. Delete differsFromDefault if nothing else reads it.\n(6) Keep returning n if `grep -n 'ResetRows(' modules settings tests` shows a reader of the return value; otherwise drop the return.\n(7) Rewrite the comments at :57-60 and :880-931, and any RunAct mention for ResetRows in docs/schema.md, docs/ARCHITECTURE.md and docs/module-map.md.\nThe degraded stub's BulkRun (:700-705) already brackets and re-raises. It logs nothing, which matches the degraded NS.Debug, a no-op in core/DebugLogSetup.lua:70.\nThe re-raise now comes from the library's `error(err, 0)` after a plain pcall, not Util.RunAct's xpcall with a traceback. If a test_override case matches on stack text, assert the message only and say so in the commit body.",
   "tests": "Characterization pass before the code change; issue #18 asks for one.\n- Record green runs of the existing bulk-line pins in tests/test_override.lua (:433-470: the stopped-line wording and count for a row set raising mid-batch and for the re-apply raising) and of the one-line reset cases (:244, :353).\n- Add 'an all-already-default reset still logs `[Set] reset Loot: 0 rows`'.\n- Add 'a reset list with no eligible row logs nothing and returns 0'.\n- Red case: 'ResetRows runs inside the runtime's bracket'. Spy NS.SchemaRuntime.BulkRun and assert one call with ('reset', 'Loot') on addon:ResetCategory('Loot'). It is red under RunAct.\n- Add a read-back count case: General.enabled stored false, then ResetCategory('General'), counts 1 even though its set stores nil.\nAll existing pins stay green UNCHANGED (the issue's Done-when). Regenerate docs/test-cases.md and the README Tests badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -n 'RunAct' settings/Schema.lua` no longer shows ResetRows, and `grep -n 'BulkRun\\|BulkAdd' settings/Schema.lua` does.",
   "smoke": "In-client, with `/pc debug on`: change two Loot strings, press Categories > Defaults on the Loot tab (Categories page after PC-06), and see exactly one `[Set] reset ...: 2 rows` line in the console.",
   "finding_ids": [],
   "issue_refs": [
    "PrettyChat#18"
   ],
   "depends_on": [
    "RV-PC",
    "PC-01",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-06",
   "repo": "PrettyChat",
   "title": "Page-scoped Defaults buttons per options-ui: Categories resets every category tab, and General draws a Defaults button behind the same reset-all path",
   "change": "Rulings: the owner's A-02 option (a), per options-ui-\u00a713's MUST; A-21 per options-ui-\u00a75 and \u00a712.\n(1) modules/Override.lua: add `function PrettyChat.ResetCategoriesPage()`. It concatenates Schema.RowsByCategory(c) for every c in Schema.CATEGORY_ORDER except 'General', and calls `Schema.ResetRows(list, Schema.CATEGORY_PAGE)`. That is one batch, one ApplyStrings and one `[Set] reset Categories: N rows` line.\n(2) settings/Panel.lua, Categories page (:727-757). defaultsTooltip = L[\"Reset the strings on every category tab to their defaults.\"], and `ctx.panel.defaultsOnClick = function() PrettyChat:ResetCategoriesPage() end`. The footer OnDefault forwards to it as today. Delete the old per-tab tooltip key from locales/enUS.lua and rewrite the :730-750 comment.\n(3) settings/Panel.lua, General page (:719-725). Set `defaultsButton = true` and `defaultsTooltip = L[\"Reset every setting to its default.\"]`, with the key in locales/enUS.lua, and `ctx.panel.defaultsOnClick = function() PrettyChat:ConfirmResetAll() end`. This is the same popup and PrettyChat:ResetAll body that the composed 'Reset all settings' and `/pc resetall` use. The minimap choice survives because it lives in the global store (launcher-\u00a73).\n(4) PrettyChat:ResetCategory stays as the public per-category method. Its General arm and GENERAL_RESET_PATHS (Override.lua:314-354) keep the minimap allow-list. Update their comment: no panel button calls them now, and `/pc` never did.\n(5) Docs in the same commit:\n- README.md:31 and :62: Categories Defaults is page-wide, and General has a Defaults button.\n- docs/settings-panel.md:79 and its Categories section.\n- docs/schema.md:16 and :158.\n- docs/ARCHITECTURE.md:124 ('the General page draws no Defaults button' becomes a button bound to the reset-all path).\n- The docs/smoke-tests.md Defaults steps.",
   "tests": "Red first.\n- tests/test_panel.lua: 'the Categories Defaults button resets every category, not only the selected tab'. Store overrides in Loot and in Money, select the Loot tab, and invoke the panel's defaultsOnClick. Both are cleared, and there is exactly one `[Set] reset Categories:` line. Red under ResetCategory(activeCategory).\n- tests/test_panel.lua: 'the footer OnDefault forwards to the same page-wide body'.\n- tests/test_panel.lua: 'the General page declares a Defaults button whose click opens the reset-all popup' (StaticPopup_Show called with PRETTYCHAT_RESET_ALL). Red under defaultsButton = false.\n- tests/test_launcher.lua: 'a hidden minimap button survives the General page's real Defaults button'. Drive defaultsOnClick, accept the popup (OnAccept -> ResetAll), and db.global.minimap.hide stays true. Keep the existing ResetCategory('General') cases at :366 and :385.\nRegenerate docs/test-cases.md and the README Tests badge. tests/test_panel.lua (1053) must stay under 1500; the band re-check is at 1200.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -n 'selected category tab' settings/Panel.lua locales/enUS.lua README.md` prints nothing.",
   "smoke": "In-client:\n- Categories page: customize one Loot and one Money string, then press Defaults on the Loot tab. Both revert.\n- Blizzard's footer Defaults does the same.\n- General page: a Defaults button is present. Its click shows the reset-all confirmation, and Accept resets the profile while a hidden minimap button stays hidden.",
   "finding_ids": [
    "PRETTYCHAT-A-02",
    "PRETTYCHAT-A-21"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-05",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-07",
   "repo": "PrettyChat",
   "title": "NS.OriginalFormat answers the snapshot's nil instead of PrettyChat's own override when the client lacks the global",
   "change": "modules/Override.lua:589-592. Replace the body with this:\n`if addon and addon.snapshotKeys and addon.snapshotKeys[globalName] then return addon.originalStrings[globalName] end; return _G[globalName]`\nThe key set is the authority, the same one ApplyStrings uses (:295-302, PC-R-07). Rewrite the comment block above it (:577-588): a snapshotted nil is a real answer, and the live-global fallback is only for a key registered since the last /reload.\nCheck the /pc test path. printStringRow calls renderOrError(origFmt) (:600-603). If renderOrError(nil) raises or prints 'nil', map a nil original to the same '(original not available)' text the panel shows (settings/Panel.lua:268-269).\nUpdate docs/common-tasks.md:51.",
   "tests": "Add to tests/test_render.lua, or test_override.lua if the render suite has no instance hook: 'OriginalFormat answers nil for a global the client never defined, even after ApplyStrings'. Load with `mock` clearing LOOT_ITEM_SELF before OnEnable, so the snapshot records nil. After ApplyStrings, _G holds the override and NS.OriginalFormat(addon, 'LOOT_ITEM_SELF') is nil. Mark it '-- red under: `originalStrings[g] or _G[g]`'.\nAlso add: '/pc test's Original line for that global reads (original not available)'.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-08",
   "repo": "PrettyChat",
   "title": "Degraded install: /pc test and the Test button fall back to chat when the DebugLog library is absent",
   "change": "settings/Panel.lua. At file scope, resolve `local DebugLogLib = LibStub and LibStub(\"LibKa0s-DebugLog-1.0\", true)` once. In PrettyChat:TestToConsole (:101-104), add `if not DebugLogLib then return PrettyChat:Test(filter) end` before `NS.DebugLog:Show()`, so NS.Print, Test's default sink, serves the report.\nThe DebugLog stub's member set is unchanged, so test_surface_parity stays green.\nRewrite the comments that promise the chat fallback so they are true: Panel.lua:80-100 and modules/Override.lua:655-668.\nUpdate docs/settings-panel.md:231 and docs/module-map.md:236.",
   "tests": "Add to tests/test_libka0s.lua: 'degraded: /pc test category Loot prints the report to chat every time'. Use loadAddon({ skip = {'libs/LibKa0s/Core.lua'} }). Run `/pc test category Loot` twice, and assert that each run adds a 'Category: Loot' line to the chat mock. Mark it '-- red under: the stub's no-op Add, where the second run prints nothing'.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-09",
   "repo": "PrettyChat",
   "title": "Panel New box: parenthesize the gsub so its substitution count never reaches Schema.Set as instanceId",
   "change": "settings/Panel.lua:281: change `NS.Schema.Set(formatPath, (value or \"\"):gsub(\"||\", \"|\"))` to `NS.Schema.Set(formatPath, ((value or \"\"):gsub(\"||\", \"|\")))`. Add a one-line comment citing the PC-R-10 class, the same guard as :270-272.",
   "tests": "Add to tests/test_panel.lua: 'the New box hands Schema.Set exactly (path, value)'. Wrap NS.Schema.Set with a spy that records select('#', ...), commit 'A||B %s' through the New box's enter handler, and assert 2 arguments and the unescaped value 'A|B %s'. Restore the spy. Mark it '-- red under: the unparenthesized gsub, which passes 3 arguments'.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-06",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-10",
   "repo": "PrettyChat",
   "title": "Stale user-facing text: the Test tooltip says the console, the resetall row says every setting, and three stale comments go",
   "change": "(1) Test tooltip: settings/Schema.lua:162 (MASTER_SPEC.leadButton.tooltip) and its locales/enUS.lua manifest entry (:40) change together to L[\"Write a sample of every active format string to the debug console, so you can see what real loot/currency/XP messages will look like. `/pc test` writes the same report there.\"]. Remove the old key.\n(2) resetall help row: settings/Slash.lua:60 and enUS:88 become L[\"Reset every setting to defaults\"]. It is a full profile reset (Override.lua:396-402). Remove the old key.\n(3) Comments:\n- settings/Panel.lua:86-88: drop 'still prints to chat'; /pc test routes through TestToConsole.\n- settings/Panel.lua:551-552: 'one SECONDARY tab per format string' becomes the TreeGroup list.\n- settings/Schema.lua:450-451 ('pairs() order non-deterministic'): PC-04 deletes this block. If it survived, correct it to the PC-16 fixed order.\n(4) README.md and docs/slash-dispatch.md, wherever they quote either old string.",
   "tests": "Add to tests/test_locale.lua: 'no locale string tells a player /pc test prints to chat'. Walk the enUS manifest; any key containing '/pc test' must not contain 'chat' while runTest routes to TestToConsole. It is red today on the tooltip.\nAdd to tests/test_slash.lua: 'the resetall help row says it resets every setting' (the HelpRows entry). It is red today.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -rn 'prints the same report to chat\\|Reset every category to addon defaults' --include='*.lua' --include='*.md' . | grep -v 'docs/\\(audits\\|reviews\\|automated-tests/2\\)'` prints nothing.",
   "smoke": "In-client: hover the General > Master controls Test button, and the tooltip names the debug console. `/pc help` lists resetall as 'Reset every setting to defaults'.",
   "finding_ids": [
    "PRETTYCHAT-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-04",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-11",
   "repo": "PrettyChat",
   "title": "One memoized sorted name list per category (NS.SortedStringNames) replaces four rebuild-and-sort loops",
   "change": "modules/Override.lua, near the top after LABEL: add the cache `local sortedNames = {}` and `function NS.SortedStringNames(category)`. On first call it builds the sorted global-name array from NS.Defaults[category].strings (or {}) and caches it. It returns the cached array, with a '-- READ-ONLY: callers must not mutate' comment. NS.Defaults is static after load; there are no runtime writes.\nReplace the four sites:\n- ApplyStrings (:283-287) iterates NS.SortedStringNames(category).\n- collectNames (:566-575) filters INTO A NEW TABLE from the cached array and never filters the cache.\n- settings/Schema.lua:434-438 (the schema build loop).\n- settings/Panel.lua:454-458.\nThe rule_ref is none: this is a maintainability and perf tidy-up, and the perf benefit is unmeasured (there is no tests/perf.lua, by exemption). docs/module-map.md gets a row for NS.SortedStringNames.",
   "tests": "Characterization first, green before and after:\n- tests/test_override.lua 'the /pc test report lists each category's strings in sorted order'. Capture PrettyChat:Test(nil, sink) and assert that the Name: sequence per category equals the sorted keys of NS.Defaults.\nThen new cases:\n- 'SortedStringNames answers the same sorted table on every call'. It is red until the function exists.\n- 'a formatstring-filtered report does not shrink the cached list'. Run Test with the filter, then compare #NS.SortedStringNames(cat) to the full count.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -n 'table.sort' modules/Override.lua settings/Schema.lua settings/Panel.lua` shows the one sort inside SortedStringNames.",
   "smoke": "SMK-C09, in-client: copy the `/pc test` console output before and after the change and diff it. The order must be unchanged.",
   "finding_ids": [
    "PRETTYCHAT-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-04",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-12",
   "repo": "PrettyChat",
   "title": "The combat watcher registers through Core's SafeRegisterEvents with a rejected-name record, and the event/bus docs cite the v2.65.0 carve-out and threshold",
   "change": "(1) modules/Override.lua.\n- Publish `NS.RejectedEvents = NS.RejectedEvents or {}` and `local WATCH_EVENTS = { \"PLAYER_REGEN_DISABLED\", \"PLAYER_REGEN_ENABLED\" }`.\n- In SyncCombatWatch, replace the bare loop (:138-140) with `local n = NS.Util.SafeRegisterEvents(combatWatcher, WATCH_EVENTS, nil, NS.RejectedEvents)`. When n < #WATCH_EVENTS, write one `NS.Debug(\"Events\", \"rejected %s\", table.concat(NS.RejectedEvents, \", \"))` line.\n- The helper is LibKa0s-Core's SafeRegisterEvents: the C_EventUtils.IsEventValid front gate, then a pcall. Its degraded body comes from PC-01.\n- Rewrite the :83-96 comment. The frame is events-frames-taint-\u00a71's no-AceEvent carve-out (v2.65.0, WS-04): one lazily created private watcher for non-unit boundary events, fully unregistered on stand-down, and listed in ## Event Subscriptions with no register row.\n(2) core/DebugLogSetup.lua sessionSummary (:22-37): when #NS.RejectedEvents > 0, append `, rejected events: <names>`. This surfaces the list in the [Init] summary. The summary is unchanged when the list is empty, so the test_debuglog shape pins hold.\n(3) docs/ARCHITECTURE.md.\n- ## Event Subscriptions (:149-158): registration goes through NS.Util.SafeRegisterEvents; rejected names land in NS.RejectedEvents and in the [Init] summary; the watcher is the ef-\u00a71 carve-out, so no register row (A-08).\n- ## Message Bus (:137-143): justify the absence by the amended architecture-\u00a74 threshold. PrettyChat has one feature module (modules/Override.lua), and no second feature module reacts to its events. The AceAddon shell's handlers are not a feature module. Keep the re-check trigger, reworded to 'a second feature module, or one that must react to the watcher's events' (A-11).\n(4) docs/module-map.md: add a row for NS.RejectedEvents.\n\nUpstream follow-up LK-11: this item completes it. The modules/Override.lua SyncCombatWatch registration loop (:138-140) goes through Core.SafeRegisterEvents (NS.Util.SafeRegisterEvents, bound by PC-01), and the caller-owned rejected list is NS.RejectedEvents (PRETTYCHAT-A-09).",
   "tests": "Red first, in tests/test_override.lua:\n(a) 'a rejected combat event is recorded and the other still registers'. Use loadAddon({ mock = function(m) m.__badEvents = { PLAYER_REGEN_DISABLED = true }; m.C_EventUtils = nil end }) and write General.visibility = 'inCombat'. Nothing raises, PLAYER_REGEN_ENABLED is in env.__registrations(), and NS.RejectedEvents equals {'PLAYER_REGEN_DISABLED'}. It is red under the bare loop: kit 26's frame RegisterEvent raises through SyncCombatWatch.\n(b) 'IsEventValid rejects a name without calling RegisterEvent', using the C_EventUtils fake (LK-04).\n(c) 'toggling visibility twice does not duplicate a rejected name'.\n(d) 'the [Init] summary names a rejected event' (NS.DebugLog:SessionSummary()).\ntests/test_disabled.lua stays green, because the stand-down still unregisters everything. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -n ':RegisterEvent(' modules/ core/ settings/ -r` prints nothing outside the Core helper binding.",
   "smoke": "In-client: set General visibility to 'In combat', enter and leave combat, and the overrides toggle. `/pc debug on` shows the [Init] summary with no 'rejected events' tail on a current client.",
   "finding_ids": [
    "PRETTYCHAT-A-09",
    "PRETTYCHAT-A-08",
    "PRETTYCHAT-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-01",
    "LK-11",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-13",
   "repo": "PrettyChat",
   "title": "Minimap row's CLI path reads in the row's own shown sense: global.minimap.hide -> global.minimap.shown (WS-06)",
   "change": "The stored key stays LibDBIcon's db.global.minimap.hide. There is no SavedVariables change and no migration.\n- settings/Schema.lua:146: `minimapPath = \"global.minimap.shown\"`.\n- The MASTER_WIRING key at :229 becomes [\"global.minimap.shown\"]. Its get/set closures already invert onto `hide`.\n- Rewrite the comments at :33, :139-146, :216-228 and :902 to state WS-06: the path names the row's sense, and the storage stays `hide` (launcher-\u00a73, anti-pattern #81).\n- Update the comment mentions in modules/Override.lua:319, core/LauncherSetup.lua:71 and :81, core/PrettyChat.lua:127 and settings/OptionsSetup.lua:184. The stub leaf takes spec.minimapPath, so no code change is needed there.\n- Docs: docs/schema.md:16, docs/settings-panel.md:71, docs/ARCHITECTURE.md:62, :90 and :124, docs/module-map.md:48.\n- README: if any README or smoke step quotes `/pc get|set global.minimap.hide`, change it.\n- Note for the next Version History row (owner): the CLI name changed.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first:\n- tests/test_launcher.lua: change `MINIMAP_PATH` (:35) to 'global.minimap.shown'. The cases that resolve the row go red until the rename.\n- tests/test_launcher.lua: add '/pc get global.minimap.shown answers true on a fresh install, and /pc set global.minimap.shown false stores hide = true'.\n- tests/test_launcher.lua: add 'global.minimap.hide is no longer a settings path' (Schema.FindByPath is nil).\n- tests/test_schema.lua:154: the composed path list reads '...,global.minimap.shown'.\nThe db.global.minimap.hide assertions (:236-560) are unchanged. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -rn 'global\\.minimap\\.hide' --include='*.lua' --include='*.md' . | grep -v '^./libs\\|^./tests/_kit\\|docs/\\(audits\\|reviews\\|revendor\\|automated-tests/2\\|superpowers\\)'` prints nothing. Storage references read `db.global.minimap.hide`.",
   "smoke": "In-client: `/pc get global.minimap.shown` prints true. `/pc set global.minimap.shown false` hides the minimap button, and the General > Master controls 'Minimap button' checkbox unticks.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "WS-06",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-14",
   "repo": "PrettyChat",
   "title": "Declare every default once in defaults/Profile.lua: NS.GlobalDefaults and NS.GeneralDefaults replace the copies in Database, Override and the Master spec",
   "change": "(1) defaults/Profile.lua.\n- Add `NS.GlobalDefaults = { schemaVersion = 0, minimap = { hide = false } }`. It carries WS-03's declared stamp and LibDBIcon's table.\n- Add `NS.GeneralDefaults = { enabled = true, visibility = \"always\" }`.\n- Move the reasoning from core/Database.lua:16-33 here: why the stamp is 0, and why the minimap is global and declared rather than written.\n- Do not create a defaults/Global.lua, because savedvariables-\u00a72 names defaults/Profile.lua.\n(2) core/Database.lua: delete `Database.defaults` (:34-39) and leave a pointer comment.\n(3) core/PrettyChat.lua:31-35: OnInitialize sets `defaults.global = NS.GlobalDefaults`. Keep the fresh-table comment.\n(4) The readers:\n- modules/Override.lua:38 `if self.db.profile.enabled == nil then return NS.GeneralDefaults.enabled end`, keeping the `== nil` form per savedvariables-\u00a75.\n- Override.lua:71-72: both 'always' fallbacks become NS.GeneralDefaults.visibility.\n- settings/Schema.lua MASTER_SPEC.defaults (:119-120): enabled and visibility read NS.GeneralDefaults.\n- The visibility set arm (:212) compares against NS.GeneralDefaults.visibility.\n- minimapDefaultDeclared (:488-493) reads NS.GlobalDefaults.minimap.\n- The core/LauncherSetup.lua:148 comment.\n(5) Docs: docs/module-map.md:52, docs/ARCHITECTURE.md:93 and docs/schema.md, wherever they name NS.Database.defaults or core/Database.lua as the declaration site.",
   "tests": "Characterization first, green before and after:\n- tests/test_override.lua: 'IsAddonEnabled answers true with no stored key' and 'GetVisibility answers always with no stored key'.\n- tests/test_launcher.lua: 'the global minimap default survives ResetAll' (may already exist; reuse it).\nThen red:\n- tests/test_database.lua:42 moves from Database.defaults to NS.GlobalDefaults.schemaVersion == 0. It is red until the table exists.\n- tests/test_defaults.lua: 'defaults are declared only in defaults/Profile.lua'. It scans the TOC-derived authored sources for `schemaVersion = 0`, `hide = false`, `visibility = \"always\"` and `enabled = true` literals outside defaults/Profile.lua and expects none. Red today at core/Database.lua and settings/Schema.lua.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -rn 'Database.defaults' --include='*.lua' --include='*.md' core settings modules defaults docs/*.md tests/*.lua` prints nothing.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-03",
    "PC-13",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-15",
   "repo": "PrettyChat",
   "title": "Delete the dead legacy GetAddOnMetadata rung, and cite compat's applicability condition in the compat-layer row (WS-05)",
   "change": "(1) core/EnvSetup.lua: delete the global rung (:75-77) of NS.Meta, so the ladder is Env, then C_AddOns, then nil. Every supported client provides C_AddOns.GetAddOnMetadata, and compat v2.65.0 says such a rung is dead code to delete, not to shim. Rewrite the header comments (:16-20 and :49-57): the degraded ladder is two rungs, and core/Compat.lua returns only when compat's condition fires, meaning a deprecated or version-variant client API outside LibKa0s's majors.\n(2) .luacheckrc:77: drop `GetAddOnMetadata` if nothing else reads it (`grep -rn GetAddOnMetadata core modules settings`).\n(3) docs/ARCHITECTURE.md:228, the compat-layer.md row: 'Not applicable \u2014 compat's applicability condition (Standard v2.65.0): PrettyChat calls no deprecated or version-variant client API outside LibKa0s's majors, so it carries no core/Compat.lua.'",
   "tests": "Red first. In tests/test_envsetup.lua (:129-148), rewrite 'the fallback prefers C_AddOns and falls back to the legacy global' as 'the fallback reads C_AddOns and never the legacy global'.\n- A legacy-only surface (C_AddOns nil, GetAddOnMetadata returning 'LEGACY') yields nil. It is red under the current rung.\n- The namespaced-wins, missing-getter and no-surface cases keep their assertions, adjusted so a missing getter yields nil.\ntest_envsetup :162 ('the deleted shim is gone') stays. Regenerate docs/test-cases.md if a case name changed.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua. `grep -n 'GetAddOnMetadata(addonName' core/EnvSetup.lua` shows only the Env and C_AddOns calls.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "WS-05",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-16",
   "repo": "PrettyChat",
   "title": "Self-naming file headers on the eleven files that lack one, and an idempotent NS.Schema publish",
   "change": "(1) settings/Schema.lua:5-6 becomes `NS.Schema = NS.Schema or {}` / `local Schema = NS.Schema` (architecture-\u00a73). Check that nothing earlier in the TOC pre-seeds NS.Schema with a conflicting table.\n(2) documentation-\u00a79. Directly under the vararg line, the first comment reads `-- <path> \u2014 <one-line purpose>` in these files: core/Constants.lua, core/Database.lua, core/Namespace.lua, core/PrettyChat.lua, core/State.lua, core/Util.lua, defaults/Defaults.lua, defaults/Profile.lua, locales/enUS.lua, modules/Override.lua, settings/Schema.lua.\n- Where a purpose comment already exists (Database '-- NS.Database \u2014 \u2026', Util, Profile, enUS, Namespace, PrettyChat, State, Override), rewrite its first line to lead with the path instead of stacking a second header.\n- Constants, Defaults and Schema get a new line.",
   "tests": "Red first.\n- tests/test_doc_structure.lua: 'every TOC-loaded authored file names its own path in its first comment'. Derive the list from PrettyChat.toc minus libs/, and match `^%-%- <path> \u2014 ` on the first comment line after the vararg. It is red today for the eleven files.\n- tests/test_doc_structure.lua (or test_harness): 'no module publishes NS.<X> with a bare table constructor'. Scan the same sources for `^NS%.%u%w* = {}` and for `local X = {}` followed by `NS.X = X`. It is red on settings/Schema.lua:5-6.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/\\|^GlobalStrings/GlobalStrings.lua$' | xargs wc -l | sort -rn | sed -n 2p  # must be < 1500. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-20",
    "PRETTYCHAT-A-25"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-14",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-17",
   "repo": "PrettyChat",
   "title": "Delete the 12 unused globals from .luacheckrc",
   "change": ".luacheckrc: delete these entries:\n- UISpecialFrames (:55), the only writable one;\n- date, wipe, SettingsPanel, GameTooltip, InCombatLockdown and UIParent (:64, :65, :67, :70, :71, :78);\n- the five GameFont* entries (:82-86).\nThey are left over from code that moved into LibKa0s; libs/ is excluded from lint. Do not touch the stanza comments except where they count entries.",
   "tests": "None new: a lint-config change with no behavior. tests/test_lintconfig.lua must stay green (it pins the config's shape). luacheck is the check, and must still report 0 warnings / 0 errors in 48 files with the entries gone.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && grep -cE 'UISpecialFrames|GameFont|\"wipe\"|\"date\"' .luacheckrc  # 0. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-18",
   "repo": "PrettyChat",
   "title": "TOC: at-line comments on the four load-bearing lines, conventional-position notes on # Core/# Modules/# Settings, and the load-order doc aligned",
   "change": "PrettyChat.toc (CRLF preserved). Comment only, no reordering.\n- Above core\\CoreSetup.lua (:52): it loads AFTER core\\PrettyChat.lua and the position is load-bearing. NewAddon's AceConsole embed overwrites NS.Print, and CoreSetup's file-scope `NS.Print = printer.Print` reclaims it (anti-pattern #36). Moved earlier, nothing errors, but every chat line turns AceConsole-green.\n- Above core\\DebugLogSetup.lua (:53): it loads AFTER core\\Constants.lua and is load-bearing. Its file-scope lib:New descriptor reads NS.Const.FONT_MONO; NS.State and NS.Print are read at call time.\n- Above settings\\OptionsSetup.lua (:77): it loads AFTER settings\\Schema.lua and is load-bearing. Its file-scope descriptor takes NS.SchemaRuntime.Get/Set/ApplyDefault/AllRows as values, and it calls NS.Schema.InstallMasterControls at file scope.\n- Above settings\\Slash.lua (:78): it loads AFTER settings\\Schema.lua and is load-bearing. The library-path descriptor takes NS.SchemaRuntime.Get/Set/FindRow/AllRows/ApplyDefault as values at file load.\n- The # Core header (:34): Constants, Namespace, State, Database and PrettyChat are in conventional order, and each load-bearing line carries its own note.\n- The # Modules header (:73): conventional. Override.lua reads NS.Defaults, NS.Util and the AceAddon object, all published above.\n- The # Settings header (:76): replace the template text. The annotated lines (OptionsSetup, Slash, Panel) are load-bearing, and the rest of the order is conventional.\nThen align the load-order prose with the TOC. docs/ARCHITECTURE.md:33 adds core/LifecycleSetup in TOC position; this is A-10's item 2, delivered here. Do the same in the docs/module-map.md load-order list (~:175-190).",
   "tests": "Add to tests/test_doc_structure.lua: 'ARCHITECTURE's load-order line names every TOC-loaded authored file in TOC order'. Derive the list from PrettyChat.toc minus libs/, and compare it to the backticked paths on the ARCHITECTURE.md load-order line. It is red today because core/LifecycleSetup is missing.\nRegenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && grep -n -B1 'core\\\\CoreSetup.lua\\|core\\\\DebugLogSetup.lua\\|settings\\\\OptionsSetup.lua\\|settings\\\\Slash.lua' PrettyChat.toc  # each is preceded by a '#' comment. `git ls-files --eol PrettyChat.toc` shows w/crlf. No tests/perf.lua.",
   "smoke": "In-client: /reload; the addon loads, chat lines are cyan '[PC]' (not AceConsole green), and the console opens with its monospace font.",
   "finding_ids": [
    "PRETTYCHAT-A-03",
    "PRETTYCHAT-A-04",
    "PRETTYCHAT-A-05",
    "PRETTYCHAT-A-06",
    "PRETTYCHAT-A-24"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-19",
   "repo": "PrettyChat",
   "title": "Move the authored generator to tools/split_globalstrings.py, repo-root relative, with tools/ packaged out",
   "change": "(1) `git mv GlobalStrings/split_globalstrings.py tools/split_globalstrings.py`.\n(2) In main() (:117-119 and :194), in the same commit:\n- `repo_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`\n- `input_path = os.path.join(repo_root, 'GlobalStrings', 'GlobalStrings.lua')`\n- `output_dir = os.path.join(repo_root, 'GlobalStrings')`\n- the TOC check reads `os.path.join(repo_root, TOC_NAME)`\n- fix any other `script_dir`-relative path in the file.\n(3) .pkgmeta: add `- tools   # authored dev tooling (the GlobalStrings splitter); never shipped`. Cite packaging-\u00a71 by section, not line.\n(4) Update the path in:\n- DEPENDENCIES.md:146-150 (Why and Run: `python3 tools/split_globalstrings.py`);\n- docs/common-tasks.md:70;\n- docs/global-strings.md:17, :25 and :39;\n- docs/module-map.md:248;\n- GlobalStrings/README.md:11-22;\n- tests/prose_waivers.lua:7, plus re-key any waiver entry for the script's own path;\n- the regenerate command inside the docs/ARCHITECTURE.md:270 layout-\u00a72 row.\n(5) Re-check the three conditions of layout-\u00a71's generated-data exemption in ARCHITECTURE's 'Files over the 1500-line cap' section against the generator's new home. Reword any sentence that says the splitter lives in GlobalStrings/.",
   "tests": "No Lua behavior change. The check is the generator itself: `python3 tools/split_globalstrings.py` must succeed and leave `git status --porcelain GlobalStrings/` empty. It is red under a plain git mv, which would look for tools/GlobalStrings.lua. tests/test_defaults.lua (which reads the chunks) and the kit prose/layout gates stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && python3 tools/split_globalstrings.py && test -z \"$(git status --porcelain GlobalStrings/)\" && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && grep -rn 'GlobalStrings/split_globalstrings' --include='*.md' --include='*.lua' --include='*.py' . | grep -v 'docs/\\(audits\\|reviews\\|revendor\\|automated-tests/2\\|superpowers\\)'  # prints nothing. No tests/perf.lua.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-20",
   "repo": "PrettyChat",
   "title": "Doc and comment drift: the General-page description, the Slash minor, the 40/60 comment, the second-reader comment, and the .pkgmeta line citations",
   "change": "This fixes A-10 items 1, 3, 4, 5 and 7. PC-18 delivered item 2 (load order). Items 6 and 8 were rejected on verification and stay unchanged: scope.md:45 and ARCHITECTURE.md:193.\n(1) Rewrite the General-page description as RenderTabbedSchema over Master controls, with the composer's Test leadButton and Reset pair, and no explainer TextRow or afterGroup Test button. Sites: docs/ARCHITECTURE.md:71, docs/module-map.md:240, docs/settings-panel.md:61, docs/smoke-tests.md:898.\n(3) docs/ARCHITECTURE.md:147: Slash minor 13 becomes the vendored minor, 15 after v1.56.0. Read it with `grep -n 'MINOR' libs/LibKa0s/Slash.lua`. Leave settings/Slash.lua:243, a historical note.\n(4) core/Constants.lua:24-25: drop 'bespoke 40/60 editor' and describe the TreeGroup pane.\n(5) settings/Slash.lua:405-408: stop counting readers of IsAddonEnabled. Say the disabled gate reads the latch, and that IsAddonEnabled is the stored-path reader.\n(7) .pkgmeta:21 and :25 (and any other `packaging.md:28` citation, e.g. the .claude comment block): cite `packaging-\u00a71` with no line number.",
   "tests": "None: documentation and comments only, with no behavior. tests/test_doc_structure.lua (the TOC/doc pins, including PC-18's load-order case) stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -rn 'packaging.md:28\\|40/60\\|second reader\\|Slash minor 13' .pkgmeta core settings docs/*.md  # prints nothing. `grep -n 'explainer\\|afterGroup Test' docs/ARCHITECTURE.md docs/module-map.md docs/settings-panel.md docs/smoke-tests.md` prints nothing.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-18",
    "PC-06",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-21",
   "repo": "PrettyChat",
   "title": "Register hygiene: narrow the toc-file-\u00a71 row to the brand mark, retire the debug-logging-\u00a72 row, and re-point the dangling LIBKA0S-05 citation",
   "change": "docs/ARCHITECTURE.md ## Documented deviations.\n(A-12) Row toc-file-\u00a71 (:271): strike the X-Wago-ID clauses from What and Why. X-Wago-ID is a MAY, and omitting it is compliant. Reduce the trigger to 'a decision to retire the brand mark'. Record the narrowing in the retired block with the date. Issue #7 stays will-not-do.\n(A-13) Row debug-logging-\u00a72 (:272): move it to the retired block with the date and the reason. core/MediaSetup.lua calls Media.RegisterLSM like every addon, which is what \u00a72 asks, and the 0,0 without LSM follows library-stack-\u00a73. Keep the fixed-monospace note as one sentence of prose under ## External dependencies if useful.\n(A-14) The LIBKA0S-05 citation dangles; it was never migrated from the retired ledger. Re-point it to `LibKa0s README.md, 'The L trap'` in three places: the localization-\u00a71 row (:276), locales/enUS.lua:26 and tests/test_locale.lua:237.\nUpdate any 'N rows' sentence or count about the register.",
   "tests": "None for behavior. If tests/test_register.lua pins the register's row count or rule keys, update it in the same commit; it must stay green. Regenerate docs/test-cases.md only if a case name moved.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && grep -rn 'LIBKA0S-05' --include='*.lua' --include='*.md' . | grep -v 'docs/\\(audits\\|reviews\\)'  # prints nothing. `grep -n 'X-Wago-ID' docs/ARCHITECTURE.md` shows only the retired-block note.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-12",
    "PRETTYCHAT-A-13",
    "PRETTYCHAT-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-04",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-22",
   "repo": "PrettyChat",
   "title": "Document that PrettyChat rewrites the loot/currency/money chat globals for every addon (handoff H-1), with a smoke step",
   "change": "(1) docs/ARCHITECTURE.md ## Known Limitations (:182-188): add a bullet. PrettyChat rewrites the CHAT_MSG_LOOT, CURRENCY, MONEY and COMBAT_* payload globals for EVERY addon. A parser that compiles patterns from those globals must build them from the live global, or re-check them against it on each parse. The inCombat and outOfCombat visibility modes change these globals at every combat boundary, as do a settings write, a profile change and a stand-down. Name handoff H-1: LootHistory caches its patterns once (LootHistory/core/Util.lua:118/147/187/210), and the fix is LootHistory's.\n(2) docs/scope.md: one sentence to the same effect.\n(3) docs/smoke-tests.md: add SMK-F001. With LootHistory installed and visibility inCombat, run `/etrace` on CHAT_MSG_LOOT, loot something in and out of combat, and confirm that arg1's wording changes with combat state and that LootHistory records both. docs/ARCHITECTURE.md:151 gets a cross-reference.\nNo PrettyChat API, bus message or chat filter is added.",
   "tests": "None: documentation only. The mechanism is visible in-client only, and no headless suite can observe a sibling addon's cache. test_doc_structure stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'H-1' docs/ARCHITECTURE.md docs/scope.md docs/smoke-tests.md && grep -n 'SMK-F001' docs/smoke-tests.md",
   "smoke": "SMK-F001, in-client with LootHistory loaded: `/etrace` shows CHAT_MSG_LOOT arg1 in PrettyChat's format, and the wording changes between combat and non-combat under visibility inCombat. Once LootHistory's H-1 fix lands, both loots are recorded.",
   "finding_ids": [
    "PRETTYCHAT-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-23",
   "repo": "PrettyChat",
   "title": "performance-\u00a712 exemption docs: performance.md shrinks to one screen with the sweep moved to a Tier-3 page, and testing.md names the exemption",
   "change": "(1) docs/performance.md (230 lines) is cut to one screen stating documentation-\u00a73's four facts:\n- it brackets nothing;\n- which of criterion (b)/(c) applies;\n- WHERE the committed sweep lives;\n- what re-arms the wiring.\n(2) Move '## The sweep' through the Panel next-frame subsection (:26-134) and the PC-R-05 history (:197-224) VERBATIM into a new docs/performance-sweep.md (Tier 3, CRLF). Register it in docs/ARCHITECTURE.md ## Documentation map \u2192 Addon-specific (Tier 3) (:244), and link to it from performance.md.\n(3) The performance-\u00a712 register row keeps citing the sweep commit only; check that its Why cell does not inline the sweep.\n(4) performance.md:178, which says the runner does not name performance-\u00a712, is now false. The vendored kit-26 runner reads that register row and records skip reason (2) (LK-09), so say that instead.\n(5) docs/testing.md:238-239: 'The one narrow exception is `perf`, skipped under the ratified performance-\u00a712 no-combat-path exemption (register row in docs/ARCHITECTURE.md), which the release notes name.'",
   "tests": "None: documentation only. tests/test_doc_structure.lua must stay green; if it pins the Documentation map against docs/*.md files, the new page's map row satisfies it. The kit prose gate (US English) must pass on the new page.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && wc -l docs/performance.md  # about 40 lines or fewer, one screen. `grep -n 'performance-sweep.md' docs/ARCHITECTURE.md docs/performance.md` resolves, `grep -n 'ships no .tests/perf.lua., which the release notes' docs/testing.md` prints nothing, and `git ls-files --eol docs/performance-sweep.md` shows w/crlf after `rm docs/performance-sweep.md && git checkout -- docs/performance-sweep.md` post-add.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-16",
    "PRETTYCHAT-A-29"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "PC-24",
   "repo": "PrettyChat",
   "title": "DEPENDENCIES names Pillow for the logo recipe, and the README 1.5.0 row speaks to players",
   "change": "(1) DEPENDENCIES.md, Release / assets group (:157-163): replace 'no image tooling' with a Pillow (python3-pil) entry:\n- Why: layout-\u00a74's recipe regenerates media/logos/prettychat.logo.128.tga from the 2000x2000 .png, as core/LauncherSetup.lua:96-97 records.\n- Install: `sudo apt install -y python3-pil`.\n- Verify: `python3 -c 'import PIL; print(PIL.__version__)'`.\n- Note: it is not needed to build, run or test.\n(2) README.md:74, the 1.5.0 Version History row: reword the two jargon highlights for players. 'The write seam now refuses a format string\u2026' becomes 'A format with a placeholder the game cannot fill is now refused when you save it, instead of failing when the message prints'. Remove the 'read a nil result as silence' clause, or restate it as what a player sees. Run the reworded cells through a de-AI pass (humanize).",
   "tests": "None: documentation only. The kit prose gate stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'python3-pil' DEPENDENCIES.md && ! grep -n 'write seam\\|as silence' README.md",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-22",
    "PRETTYCHAT-A-23"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-25",
   "repo": "PrettyChat",
   "title": "Record the 26 unrecorded vendored tags in one consolidated span bundle, docs/revendor/<date>-v1.18.0-v1.53.0/ (WS-01)",
   "change": "Create docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.53.0/ in the WA-01 / WS-01 consolidated format. Do not back-fill per tag.\n(1) 01_DELTA.md. Line 1 is exactly: `Delta: LibKa0s v1.18.0 -> v1.53.0 (span: v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.25.0, v1.26.0, v1.27.0, v1.28.0, v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0)`. Re-derive the list with the verify command before writing; the tags v1.30.0-v1.34.0 keep their own bundles. Then comes a table of tag, carrying commit, date and subject, from `git log --format='%h %ad %s' --date=short -- libs/LibKa0s` with each commit's CLAUDE.md provenance tag (`git show <c>:CLAUDE.md | grep -oE 'v[0-9]+\\.[0-9]+\\.[0-9]+' | head -1`).\n(2) 05_SUMMARY.md: Launcher adopted at v1.39.0; the Lifecycle latch after v1.42.0; the rest carried by collection sweeps with no adoption decision. Name the frozen earlier bundles unchanged.\nWrite both files CRLF: `git add`, then `rm <f> && git checkout -- <f>`. The bundle is frozen once committed.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "None: a record-only bundle. The kit gates (test_eol, test_prose's scan of docs/ outside the frozen stores) stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && S=$(mktemp -d) && for c in $(git log --since='2026-08-25 00:00' --format=%H -- libs/LibKa0s); do git show $c:CLAUDE.md | grep -oE 'LibKa0s\\]\\([^)]*\\) v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -oE 'v[0-9.]+$'; done | sort -uV > $S/vendored && for d in docs/revendor/*/; do head -1 $d/01_DELTA.md | grep -oE 'v[0-9]+\\.[0-9]+\\.[0-9]+'; done | sort -uV > $S/recorded && comm -23 $S/vendored $S/recorded  # prints nothing (the v1.56.0 bundle comes from RV-PC). Then ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "WA-01",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "PC-DOCS",
   "repo": "PrettyChat",
   "title": "Sync docs, regenerate the test inventory and badge, and record a fresh automated-test bundle on kit 26 (no version bump, no tag)",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md, docs/ARCHITECTURE.md and the Tier-2/3 docs. It covers:\n- count claims: schema rows after PC-04, test totals, files, the 'N rows' register sentence;\n- slash/COMMANDS parity;\n- new names: NS.RejectedEvents, NS.SortedStringNames, NS.GlobalDefaults and NS.GeneralDefaults, PrettyChat.ResetCategoriesPage, the tools/ folder, the global.minimap.shown path;\n- the LibKa0s v1.56.0 / kit revision 26 mentions (docs/testing.md:114 and :135 'kit revision 25', the tests/run.lua:69 comment);\n- the tests/prose_waivers.lua:19 measurement line, re-measured against the kit-26 gate;\n- the dated line counts in ARCHITECTURE 'Files over the 1500-line cap';\n- docs/settings-panel.md checked against documentation-\u00a73's v2.65.0 settings-panel row (a Page | Covers table plus the page -> tab -> row tree; WS-07).\nComment-citation corrections go comment-only, on confirmation.\n(2) Run `lua5.1 tests/run.lua --list > docs/test-cases.md` (re-checked out CRLF), and set the README Tests badge from its Totals. Kit 26's \u00a7 case names change the inventory.\n(3) Run `tests/_kit/run-automated-tests.sh` from the repo root, all four suites. It writes docs/automated-tests/<stamp>/ and regenerates RESULTS.md.\n- Confirm that manifest.json's perf skipReason is reason (2), naming the performance-\u00a712 no-combat-path exemption from ARCHITECTURE's register (LK-09; A-15). RESULTS.md's Perf paragraph must match.\n- Set the authored Disposition cells: GlobalStrings/GlobalStrings.lua becomes 'exempt \u2014 see docs/ARCHITECTURE.md \u2192 Files over the 1500-line cap', replacing the stale .pkgmeta:24 argument (A-17). tests/test_panel.lua is carried forward with the re-read figure and the 1200 re-check.\n- Write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md prompt.\nThe new bundle closes A-27's staleness.\n(4) Confirm that RV-PC's docs/revendor/<date>-v1.56.0/ bundle exists and its 01_DELTA.md line 1 names v1.55.0 -> v1.56.0.\nThere is NO version bump, no Version History row and no tag; the owner decides. Re-check out every new or generated file so it is CRLF.",
   "tests": "none \u2014 docs and records only. The runner re-runs every suite, so lint, tests and complexity must be pass and perf a reasoned skip.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff --strip-trailing-cr <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && S=$(ls -d docs/automated-tests/2*/ | tail -1) && grep -n 'performance-\u00a712' $S/manifest.json && test -f $S/ANALYSIS.md && ! grep -n '.pkgmeta:24' docs/automated-tests/RESULTS.md && git ls-files --eol | grep -E 'w/(lf|mixed)' | grep -v 'eol=lf'  # prints nothing. `grep -n 'v1.56.0' CLAUDE.md` shows the provenance line, and the README badge equals the Totals in docs/test-cases.md.",
   "smoke": "",
   "finding_ids": [
    "PRETTYCHAT-A-15",
    "PRETTYCHAT-A-17",
    "PRETTYCHAT-A-27"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-01",
    "PC-02",
    "PC-03",
    "PC-04",
    "PC-05",
    "PC-06",
    "PC-07",
    "PC-08",
    "PC-09",
    "PC-10",
    "PC-11",
    "PC-12",
    "PC-13",
    "PC-14",
    "PC-15",
    "PC-16",
    "PC-17",
    "PC-18",
    "PC-19",
    "PC-20",
    "PC-21",
    "PC-22",
    "PC-23",
    "PC-24",
    "PC-25",
    "PC-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-00",
   "repo": "WhatGroup",
   "milestone": "M3",
   "title": "Clear the 3 re-vendor red(s) RV-WG recorded, so every later M3 commit starts green",
   "change": "RV-WG copied LibKa0s v1.56.0 and recorded 3 failing test(s) in its commit body (`git log --grep=\"^RV-WG\" --format=%B`). Make every one of them green by adapting THIS addon (its stubs, fixtures, test drivers, adoption pins, or code) to the v1.56.0 contract. Never edit libs/ or tests/_kit/, and never weaken an assertion: re-pin it to the new library behavior the plan intends (03_SPEC.md, 02_UPSTREAM_CHANGES.md consumer follow-ups). Where a planned WG-nn item already names the fix (e.g. a stub gaining SetMany, a re-pin item such as WG-02 or PF-05), do exactly that part of that item here, say so in the commit body, and leave the rest of it to that item. The later item will then find that part done, record it, and carry on.",
   "tests": "the recorded reds are the red tests; they must go green",
   "verify": "/home/tushar/.claude/wow-addon/bin/ka0s-bounded luacheck .; /home/tushar/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua -> 0 failed; lizard CCN<=15; line cap",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-WG"
   ],
   "effort": "M"
  },
  {
   "id": "WG-02",
   "repo": "WhatGroup",
   "title": "Re-pin settings registration in combat to LibKa0s Options' park-and-replay (LK-25) and settle any other kit-26 dry-run red",
   "change": "LK-25 makes CreateOptionsPanel park under InCombatLockdown() and replay on PLAYER_REGEN_ENABLED. WhatGroup's Settings.Register (settings/Panel.lua:412-425) calls Helpers.CreateOptionsPanel() and sets `_settingsRegistered = true` unconditionally. That stays correct, because the library replays its own parked registration. Two cases pin the old immediate registration and will be red under v1.56.0. tests/test_panel.lua:106 'registering during combat still registers' becomes 'panel: a registration taken in combat is parked and lands at PLAYER_REGEN_ENABLED, with no second host call'. In combat it asserts `_settingsRegistered` is true and `#mock.categories == 0`. It then fires PLAYER_REGEN_ENABLED on the library's private frame (walk mock.frames for a frame with `__events.PLAYER_REGEN_ENABLED` and fire its OnEvent) and asserts `#mock.categories == 2`. After that the event is unregistered, and a second Settings.Register() registers nothing more. tests/test_panel.lua:114 'a login taken in combat needs no second registration' makes the same re-pin across OnEnable. Rewrite the options-ui-\u00a79 comments on these cases, at settings/Panel.lua:419-421, and at core/WhatGroup.lua:382-393 (OnEnable: 'a /reload taken in combat still lands the category'). The new wording: the category still registers at login with no user action, and in combat the library parks it and lands it at combat end. docs/settings-panel.md and docs/midnight-quirks.md get the same sentence wherever they say registration happens in combat. Run the full suite once on the re-vendored tree. Fix any other red here, under the owner's ruling that a red exposes a real bug: tighten the setup, never weaken an assertion. The candidates checked are LK-03 (AceDB SetProfile now strips the outgoing profile's defaults) at tests/test_disabled.lua:516-529, LK-13 (Media counts only LSM-accepted registrations) at tests/test_mediasetup.lua, and LK-08 (case names now carry \u00a7). Any red at RV-WG is listed in that commit's body and this item clears it; it is never folded into the copy-only RV-WG commit.",
   "tests": "Characterization: on the re-vendored tree, run `lua5.1 tests/run.lua` and record the red list. Expect the two test_panel.lua cases to fail on `#mock.categories` (0 vs 2). Rewrite them to the park/replay behavior described. Each carries '-- red under: LibKa0s Options registering immediately under InCombatLockdown (pre-LK-25)'. Regenerate docs/test-cases.md and the README badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && diff -rq ../LibKa0s/LibKa0s libs/LibKa0s && diff -rq ../LibKa0s/testkit tests/_kit",
   "smoke": "In client: enter combat (target dummy), /reload while in combat. Ka0s WhatGroup is missing from Settings > AddOns until combat ends, then appears without a second /wg config. There is no Lua error and no taint on Game Menu > Logout.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "LK-03",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-03",
   "repo": "WhatGroup",
   "title": "Reopening a soft-hidden popup in combat with no capture never calls the secure teleport button's protected Hide (NO_CAPTURE sentinel)",
   "change": "modules/Frame.lua. (1) At file scope near :33, add `local NO_CAPTURE = {}`: a sentinel so a deferred configure can carry 'no capture' through the combat stash. (2) deferTeleportUntilCombatEnds (:526-541): stash `f._pendingTeleportInfo = (info == nil) and NO_CAPTURE or info`. On replay, `if pending then ConfigureTeleportButton(fields.teleportBtn, fields.teleportIcon, pending ~= NO_CAPTURE and pending or nil) end`. (3) PopulateFields no-info branch (:843-854): replace `fields.teleportBtn:Hide()` with `ConfigureTeleportButton(fields.teleportBtn, fields.teleportIcon, nil)`. It already defers under lockdown, and out of combat resolveTeleportState(nil) answers nil, so it clears type/macrotext and Hides. `fields.teleportNote:Hide()` stays a direct call (unprotected FontString). (4) endTestMode (:938-950): keep the `not InCombatLockdown()` guard, and change its comment to say the protected Hide is now deferred by ConfigureTeleportButton itself. New sibling suite tests/test_frame_secure.lua, because tests/test_frame.lua is at 1421 lines and these cases would push it to the 1500 cap. Header: 'the popup's secure child: reopening a soft-hidden popup, its alpha, and the teleport handlers'. Declare it in tests/run.lua's suites list right after 'test_frame'.",
   "tests": "Red first, in tests/test_frame_secure.lua. The case is 'frame: reopening a soft-hidden popup in combat with no capture never Hides the secure button'. Setup: enableAddon, show the popup with a capture, set mock.combat = true, press Close (soft hide, alpha 0), wipe the capture (NS.addon:WipeCapture('test')), then NS.addon:ToggleFrame(). Assert `#mock.blocked == 0` (the kit records protected calls on secure frames under lockdown). Then set mock.combat = false and fire PLAYER_REGEN_ENABLED: the teleport button is hidden and its 'type' attribute is nil. Add '-- red under: PopulateFields calling fields.teleportBtn:Hide() directly', and confirm it is red on the current code with mock.blocked = {'<anonymous>:Hide()'}. Second case: 'frame: a deferred no-capture configure is replayed, not dropped'. A plain nil stash is lost at the `if pending` replay; the sentinel survives it. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -n 'fields.teleportBtn:Hide()' modules/Frame.lua",
   "smoke": "Review smoke S-001 in client. Show the popup, pull a dummy, press Close in combat, leave the group or let the capture wipe, then click the minimap button (or /wg show) while still in combat. Expect no ADDON_ACTION_BLOCKED. After combat the teleport button is gone and the popup shows 'No data'.",
   "finding_ids": [
    "WHATGROUP-R-01",
    "WHATGROUP-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-04",
   "repo": "WhatGroup",
   "title": "ApplyFrameAlpha honors the soft-hidden state, so a gate-declined or alpha-only reopen in combat no longer reveals a popup the launcher and Escape cannot close",
   "change": "modules/Frame.lua. Hoist `local softHidden = false` and `local pendingHide = false` (:227-228), with their two-line comments, above ApplyFrameAlpha (:148). ApplyFrameAlpha becomes `if not f then return end; f:SetAlpha(softHidden and 0 or masterAlpha())`. showPopup (:264-282) already clears softHidden before calling ApplyFrameAlpha, and hidePopup's real route (:251-254) clears it before calling it too, so both still restore the master alpha. Update the :141-147 comment: opacity is still not combat-refused, but a soft-hidden popup stays at alpha 0 until something really shows it. No change to preparePopup (:883-893) or ShowFrame's gate: once the alpha respects softHidden, the gate-declined branch (:1045-1053) leaves the popup invisible and onScreen() truthful. Cases go in tests/test_frame_secure.lua (from WG-03).",
   "tests": "Red first in tests/test_frame_secure.lua, with db.profile.visibility = 'outOfCombat'. (a) 'frame: a gate-declined reopen in combat leaves a soft-hidden popup at alpha 0, and the launcher still closes it': popup shown, mock.combat = true, fire PLAYER_REGEN_DISABLED (the gate soft-hides it), /wg show. Assert alpha is 0 and onScreen() is false (ToggleFrame opens rather than no-ops); the ESC proxy stays hidden. (b) 'frame: an alpha write while soft-hidden does not reveal the popup': `/wg set alpha 0.8` in combat while soft-hidden, then assert GetAlpha() == 0. (c) 'frame: a real show after the soft hide restores the master alpha'. Each carries '-- red under: ApplyFrameAlpha setting masterAlpha() unconditionally'. Confirm (a) and (b) are red first; the review's repro was alpha=1 after SHOW. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4",
   "smoke": "In client: set General visibility to 'Only out of combat', open the popup, pull a dummy (the popup goes invisible), then /wg show and the chat link mid-fight. The popup stays invisible. After combat it is gone for real, and Escape opens the game menu.",
   "finding_ids": [
    "WHATGROUP-R-02",
    "WHATGROUP-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-03",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-05",
   "repo": "WhatGroup",
   "title": "Replace modules/Frame.lua's two private PLAYER_REGEN_ENABLED frames with a combat-end queue drained by the addon's AceEvent handler",
   "change": "modules/Frame.lua. (1) Add a file-scope queue `local combatEndQueue = {}` with two functions. `NS.FrameQueueForCombatEnd(key, fn)` stores `combatEndQueue[key] = fn`, one slot per key, so repeat queueing replaces rather than stacks. `NS.FrameDrainCombatEnd()` walks the fixed keys 'teleport' then 'firstShow', takes each fn, sets its slot to nil, then calls it. There is no table allocation when the queue is empty, which keeps perf combatGateSteady at 0 B. (2) deferTeleportUntilCombatEnds (:526-541): replace f:RegisterEvent/OnEvent with `f._pendingTeleportInfo = (info == nil) and NO_CAPTURE or info; NS.FrameQueueForCombatEnd('teleport', replayTeleport)`. replayTeleport is a file-scope function holding WG-03's replay body (latest-info semantics, NO_CAPTURE preserved). (3) ShowFrame's first-show defer (:1014-1040): delete `buildWaitFrame` (:232 and its creation). Queue 'firstShow' with a file-scope replay that clears `_frameBuildQueued`, restores `pendingInfo` from the stashed `WhatGroup._deferredShowInfo`, and calls ShowFrame. (4) NS.FrameStandDown (:1115-1123): replace the raw UnregisterEvent/SetScript calls with wiping the queue and clearing `f._pendingTeleportInfo`, `_deferredShowInfo` and `_frameBuildQueued`. core/WhatGroup.lua OnCombatStateChanged (:900-908): on 'PLAYER_REGEN_ENABLED', call `NS.FrameDrainCombatEnd()` first, before ApplyFrameVisibility. OnDisabledCombatEnded (:352-355) does NOT drain. Rewrite the Frame.lua comments that describe the two raw registrations (:230-232, :999-1011, :1109-1114). Also update the tests/test_disabled.lua:56-63 rawRegs comment: raw registrations are expected to be none, and the survey stays as a regression guard. docs/ARCHITECTURE.md '## Event Subscriptions' drops the two private-frame rows and names the queue. docs/frame.md and docs/data-flow.md follow if they mention buildWaitFrame.",
   "tests": "Characterization first: the combat-defer cases in tests/test_frame.lua (:620-716, which fire `waitFrame.__fire('OnEvent', ...)` and `popup(mock).__fire('OnEvent', ...)`) pass on the current code. Retarget them to fire the addon event with `mock.__fireEvent('PLAYER_REGEN_ENABLED')` and keep every assertion. They go red while the private frames still carry the replay, then green after the move. Add to tests/test_disabled.lua 'disabled: no raw frame registration exists at any point, in combat or out' (rawRegs(mock) is empty after a combat first-show defer and a combat teleport defer), with '-- red under: a frame:RegisterEvent in modules/Frame.lua'. Add to tests/test_frame_secure.lua 'frame: a stand-down in combat drops a queued first show and a queued teleport configure' (after /wg disable and PLAYER_REGEN_ENABLED, nothing is shown or configured). Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -nE ':RegisterEvent\\(|buildWaitFrame' modules/Frame.lua",
   "smoke": "In client: (1) never open the popup, enter combat, and trigger a show (/wg show with a capture). You get the 'deferred' line, and the popup appears after combat. (2) Open the popup out of combat with a teleport on cooldown, pull, and let the capture change in combat. After combat the button reflects the latest capture. Check /etrace shows no extra WhatGroup frame watching PLAYER_REGEN_ENABLED.",
   "finding_ids": [
    "WHATGROUP-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-03",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-06",
   "repo": "WhatGroup",
   "title": "Route the four event registrations through LibKa0s-Core's SafeRegisterEvent, with the rejected names in the [Init] summary",
   "change": "core/CoreSetup.lua live branch (after :108): `NS.SafeRegisterEvent = lib.SafeRegisterEvent` (Core minor 8, LK-11). Degraded branch (:40-99): a one-rung body with no front gate, as the Core version-8 doc's Degradation note prescribes: `function NS.SafeRegisterEvent(target, event, handler, rejected) local ok = pcall(target.RegisterEvent, target, event, handler); if not ok and rejected then <append event once, no duplicates> end; return ok end`. Only SafeRegisterEvent is published: WhatGroup registers no unit events and walks no list, and the Core parity case compares the namespace live against degraded, so an unpublished member needs no stub. Say so in a comment. core/WhatGroup.lua: add `NS.RejectedEvents = {}` next to NS.State (session-only, never persisted). registerFeatureEvents (:293-304) makes four calls: `NS.SafeRegisterEvent(self, 'GROUP_ROSTER_UPDATE', nil, NS.RejectedEvents)`, and the same for LFG_LIST_APPLICATION_STATUS_UPDATED and for PLAYER_REGEN_DISABLED/ENABLED with 'OnCombatStateChanged'. The rest of OnEnable (Settings.Register, Launcher:Register, the latch) then always runs. InitSummary (:419-433): when #NS.RejectedEvents > 0, append `, rejected events: <names joined by ', '>`. The string is unchanged when the list is empty. docs/midnight-quirks.md: one paragraph on the probing trade. C_EventUtils.IsEventValid is asked first when present, and a pcall catches a retail raise on an unknown name. The rejected list is the only record. docs/ARCHITECTURE.md '## Event Subscriptions' names the helper. Before this item, check that tests/test_surface_parity.lua's namespace case is green on both arms.",
   "tests": "Red first, in tests/test_harness.lua. 'events: one retired event name does not abort OnEnable' seeds `mock.__badEvents = { GROUP_ROSTER_UPDATE = true }` through the enableAddon mock hook. It asserts the other three are in NS.addon.__events, `#mock.categories == 2`, `NS.Launcher:IsRegistered()`, `NS.Lifecycle` evaluated (IsDown false), `NS.RejectedEvents` equals {'GROUP_ROSTER_UPDATE'}, and NS.addon:InitSummary() contains 'rejected events: GROUP_ROSTER_UPDATE'. It carries '-- red under: a bare self:RegisterEvent in registerFeatureEvents'; confirm it is red first (OnEnable raises). Variant with `mock.C_EventUtils = nil` (the pcall rung). Variant 'degraded: the Core stub's SafeRegisterEvent survives a bad name' with skip = NO_LIBKA0S. 'events: a stand-up after a rejection records the name once'. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -nE 'self:RegisterEvent\\(' core/WhatGroup.lua | grep -v OnDisabledCombatEnded",
   "smoke": "In client: /wg debug on. The [Init] line has no 'rejected events' clause on 12.1.0, where all four events are current.",
   "finding_ids": [
    "WHATGROUP-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-05",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-07",
   "repo": "WhatGroup",
   "title": "End a capture on the terminal LFG statuses timedout, invitedeclined and failed",
   "change": "First confirm the exact status spellings. Read them in Blizzard's source (Blizzard_GroupFinder LFGList.lua: grep for 'invitedeclined' / 'timedout' / 'failed') and record them in-client (smoke S-008). core/WhatGroup.lua:935-940 APPLICATION_ENDED gains `timedout = true, invitedeclined = true, failed = true`. Update the :929-934 comment: every status that ends an application with no joined group, with the source the spellings came from. docs/data-flow.md (the status table, if present) lists the three.",
   "tests": "Red first, in tests/test_capture.lua, one case per status: 'capture: a <status> application drops its capture'. Use the pattern of the existing declined/cancelled cases: apply to a search result, fire LFG_LIST_APPLICATION_STATUS_UPDATED 'applied', then '<status>', then 'inviteaccepted' for the same appID. The accept finds no capture ('accepted appID=... -> no capture') and pendingInfo stays nil. Each carries '-- red under: <status> missing from APPLICATION_ENDED'. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4",
   "smoke": "S-008 in client, before coding: `/etrace` filtered to LFG_LIST_APPLICATION_STATUS_UPDATED. Let one application time out, and decline one invite. Record the exact newStatus strings in the commit body.",
   "finding_ids": [
    "WHATGROUP-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-08",
   "repo": "WhatGroup",
   "title": "Declare global.schemaVersion = 0 so the stamp persists, and make the migration runner step-driven (savedvariables-\u00a71 at v2.65.0)",
   "change": "settings/Schema.lua:609 BuildDefaults: `global = { schemaVersion = 0, windows = {}, minimap = { hide = false } }`. Rewrite the :596-603 comment. AceDB's removeDefaults strips a stored value equal to its default at logout, so a default equal to the current version never persists and the first real migration is skipped (WS-03). core/Database.lua: keep `NS.SCHEMA_VERSION = 1` and add `NS.MIGRATIONS = { [1] = function(db) end }`. Step N takes the store from N-1 to N, and 0 -> 1 is the no-op stamp. RunMigrations: `local g = self.db and self.db.global; if not g then return end; local from = g.schemaVersion or 0; local v = from; while v < NS.SCHEMA_VERSION do local step = NS.MIGRATIONS[v + 1]; if step then step(self.db) end; v = v + 1; g.schemaVersion = v end`. The stamp advances only past a step that returned, and a raising step propagates. Delete the dead `g.schemaVersion = g.schemaVersion or NS.SCHEMA_VERSION`. The Migrate debug line becomes `NS.Debug('Migrate', 'v%s -> v%s', from, g.schemaVersion)`, the format form (the core/Database.lua:43 site of WHATGROUP-R-05). Header comment: steps are idempotent against a fresh default profile. WhatGroup has no profile-scoped step today; the first one MUST walk every stored profile (raw WhatGroupDB.profiles), per WS-03. docs/schema.md: the global block shows schemaVersion = 0, and a paragraph explains why.",
   "tests": "tests/test_database.lua. Rewrite :27 'BuildDefaults seeds global.schemaVersion from NS.SCHEMA_VERSION' as 'database: defaults declare global.schemaVersion 0 (savedvariables-\u00a71)', asserting 0. Add the red case 'database: the stamp survives AceDB's logout strip, so the first real migration runs'. Boot. Mirror removeDefaults on the raw SV: delete global.schemaVersion when it equals the declared default. Re-boot from that SV with `NS.SCHEMA_VERSION = 2` and `NS.MIGRATIONS[2] = function(db) db.global.__stepRan = true end` set before OnInitialize (a loader mock hook). Assert __stepRan is true and schemaVersion == 2. It carries '-- red under: the default equal to NS.SCHEMA_VERSION', and must fail on the current code, where the stamp is stripped and the default reads 2. Add 'database: a raising step leaves the stamp at the last completed version'. Keep 'fresh DB lands at schemaVersion 1' green; the runner stamps it. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -n 'or NS.SCHEMA_VERSION' core/Database.lua settings/Schema.lua",
   "smoke": "S-003 in client: /reload, log out, and inspect WTF/Account/<acct>/SavedVariables/WhatGroup.lua. WhatGroupDB.global.schemaVersion = 1 is present.",
   "finding_ids": [
    "WHATGROUP-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WS-03",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-09",
   "repo": "WhatGroup",
   "title": "The degraded Options stub keeps the host's real RestoreAllDefaults instead of overwriting it with a no-op",
   "change": "settings/OptionsSetup.lua:150: change `H.RestoreAllDefaults = function() end` to `H.RestoreAllDefaults = H.RestoreAllDefaults or function() end`. Add a comment citing options-ui-\u00a71 (the stub keeps the global reset real): H is Settings.Helpers, where settings/Schema.lua:691 already defined the host reset, and RestoreAllDefaults is the one stub member that collides. Check the stub list for any other member Schema.lua defines first (`grep -n '^function Helpers\\.' settings/Schema.lua`) and give it the same `or` treatment.",
   "tests": "Red first, in tests/test_libka0s.lua beside the degraded block (~:630). The case is 'degraded: Reset all settings still resets the profile (options-ui-\u00a71)'. Load NO_LIBKA0S, OnInitialize, OnEnable, set db.profile.notify.delay = 7, call NS.addon.Settings.Helpers.RestoreAllDefaults(), and assert notify.delay is back at NS.C.notify.delay. Run `/wg resetall` through the popup body path as well. It carries '-- red under: the stub assigning a no-op RestoreAllDefaults'; confirm it is red first (the value stays 7). Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-10",
   "repo": "WhatGroup",
   "title": "Shrink the local EventRegistry fake to the kit's recording one; the disabled survey reads kind 'callback'",
   "change": "Kit revision 26 (LK-04) ships a recording EventRegistry in tests/_kit/mock_events.lua. Every live callback appears in mock.__registrations() as { kind = 'callback', event, owner }. tests/wow_mock.lua:640-694: delete the local EventRegistry fake (registry, eventRegistryLog, RegisterCallback/UnregisterCallback/TriggerEvent, __callbacks). Keep note 7's chat-link click path (LinkUtil.RegisterLinkHandler -> mock.EventRegistry:TriggerEvent('SetItemRef', ...)), which reads the kit's global at click time. Keep the closure-form and numeric-owner raises only if the kit's fake lacks them, as a thin wrapper, and say which in the header's note 7. tests/test_disabled.lua regNames: drop WG-01's `__callbacks` loop. The kit survey now yields `callback:SetItemRef` through the existing `r.kind .. ':' .. r.event` line. tests/test_lifecycle.lua:160 (mock.eventRegistryLog) and :227 (mock.EventRegistry.__callbacks) read mock.__registrations() filtered on kind 'callback' instead. Update the wow_mock.lua header list of fidelity notes.",
   "tests": "Characterization: the WG-01 cases (disabled 3's red-under, lifecycle's drop/restore case) must stay green with no assertion changed. Re-prove the survey by temporarily deleting the UnregisterCallback in NS.StandDown, seeing 'disabled 3' go red with 'callback:SetItemRef', then reverting. Record that in the commit body. No new case, so the count is unchanged; still re-run --list.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -n '__callbacks\\|eventRegistryLog' tests/*.lua",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-01",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-11",
   "repo": "WhatGroup",
   "title": "Rename the minimap row's CLI path to global.minimap.shown; storage stays LibDBIcon's minimap.hide (launcher-\u00a73 at v2.65.0)",
   "change": "settings/Schema.lua:361: `local MINIMAP_PATH = 'global.minimap.shown'`. The GLOBAL get/set closures are unchanged and still invert onto db.global.minimap.hide. Rewrite the :340-360 comment: the path reads in the row's own shown sense, and the stored key stays hide (no parallel shown key, anti-pattern #81). settings/Panel.lua:221: `minimapPath = 'global.minimap.shown'`. BuildDefaults (:604-617) keeps `minimap = { hide = false }` literally, and its comment names the row by its new path. No SavedVariables migration: the stored key does not move. Docs: docs/ARCHITECTURE.md:83 and :99, docs/schema.md:44/:51/:100-101, docs/settings-panel.md:357/:414/:465 and docs/slash-dispatch.md (if it quotes the path) name `global.minimap.shown` as the CLI path and `db.global.minimap.hide` as the store. Tests: tests/test_launcher.lua:222/:239-248/:284-292/:343/:386-388, tests/test_settings.lua:574 and tests/test_testmode.lua:191 use the new path for FindSchema/Get/Set; assertions on `db.global.minimap.hide` stay.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first: tests/test_launcher.lua 'launcher: the row's CLI path reads in its own sense' asserts `H.Get('global.minimap.shown') == true` while the button is shown, and `FindSchema('global.minimap.hide') == nil`. It is red under the old path. Then rename the path in every case listed; store assertions are unchanged. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! git grep -n \"'global.minimap.hide'\\|\\\"global.minimap.hide\\\"\" -- settings tests ':!tests/_kit'",
   "smoke": "In client: /wg get global.minimap.shown answers true while the button is visible. /wg set global.minimap.shown false hides the button, and /reload keeps it hidden.",
   "finding_ids": [
    "WHATGROUP-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WS-06",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-12",
   "repo": "WhatGroup",
   "title": "Adopt LibKa0s-Schema-1.0 (WhatGroup#22); on a library-absent load /wg enable, /wg disable and /wg test print the library-absent line and raise no Lua error",
   "change": "Follows the seam-adopter map in the 2026-09-22 schema spec \u00a711 ('WhatGroup \u2014 seam adopter') and the Schema version-2 doc. (1) NEW settings/SchemaSetup.lua. Add it to WhatGroup.toc directly above settings\\Schema.lua with a load-bearing comment: Schema.lua calls its :New at file scope. It resolves `LibStub and LibStub('LibKa0s-Schema-1.0', true)`, or else HostSchemaStub, and publishes it as `WhatGroup.Settings.SchemaLib`. HostSchemaStub is the write-completing, log-silent stub copied from LibKa0s tests/test_schema.lua `referenceStub` at v1.56.0 and trimmed to what WhatGroup calls. Library level: SplitPath, Read, Write, SameValue, New. Instance: AllRows, AddRows, FindRow, Reindex, Get, Set (unknown path refused and never stored, then validate, missing root, store a copy, onChange, announce, true), SetMany (all-or-nothing: resolve and validate every entry, then store in order, with no log and one announce each), Default, ApplyDefault (resetExempt vetoed only while a bracket is open), a depth-only BulkBegin/BulkEnd/InBulk/BulkRun, BulkAdd no-op, CountOffDefault 0, ResetCounted running fn, ConsumeResetCount nil, and Validate returning 0,0,0 with one honest line. Comment: a deliberate duplication named by 'docs/api/Schema/version-2-docs.md, The degradation stub'. No writeThrough list is passed: route (b) is taken, per the deviation row below. (2) settings/Schema.lua. Delete Resolve (:288-305), the SESSION/GLOBAL dispatch inside Helpers.Get/RawSet (:388-419), Helpers.RawSet, sameValue, the bulk state and Bulk table (:435-527), silenceOpenBracket, FindSchema, countChangedProfileRows, pendingResetCount and Settings.ConsumeResetCount's body. Build `local S = Settings.SchemaLib:New{ rows = Schema, resolveRoot = function() return WhatGroup.db and WhatGroup.db.profile, 1 end, announce = function() Helpers.RefreshAll() end, debug = function(tag, fmt, ...) NS.Debug(tag, fmt, ...) end, debugEnabled = function() return NS.State.debug == true end, print = pout, resetExempt = { [MINIMAP_PATH] = true } }` and set `NS.SchemaRuntime = S`. Bind `Helpers.Get, Helpers.Set, Helpers.FindSchema, Helpers.ApplyDefault = S.Get, S.Set, S.FindRow, S.ApplyDefault`, `Settings.Bulk = { begin = S.BulkBegin, finish = S.BulkEnd }` and `Settings.ConsumeResetCount = S.ConsumeResetCount`. Add `Settings.StampClosureRows(rows)`: on each row whose path is in SESSION or GLOBAL, stamp `row.get` (session: `spec.get() or false`, as today) and `row.set` (the SESSION spec's set, or GLOBAL's inverting set). Helpers.ValidateSchema = `S.Validate{ types = { bool = true, number = true, string = true } }` plus the host loop for section and label (single-consumer checks), with errors summed. RestoreAllDefaults: `S.ResetCounted(function() db:ResetProfile() end, function(row) return not GLOBAL[row.path] end)` inside a pcall that keeps the host STOPPED line and re-raises with error(err, 0). The session sweep becomes `S.BulkRun('reset', 'profile', function(info) info.profileReset = true; for _, def in ipairs(Schema) do if def.sessionOnly then S.ApplyDefault(def) end end end)`. Keep BuildDefaults and the popup. (3) settings/Panel.lua:295-296: replace the head splice with `Settings.StampClosureRows(MASTER_ROWS); NS.SchemaRuntime.AddRows(MASTER_ROWS, 1)`. (4) settings/OptionsSetup.lua:194-219: bind the descriptor's get, set, applyDefault, bulkBegin and bulkEnd as values to S members. There is no pre-seam gate. (5) settings/Slash.lua:232-236: bind the descriptor's get, set, findRow and applyDefault to S members. `set = S.Set` means Slash minor 15's CliSet prints the seam's refusal. (6) THE OWNER RULING (route (b), WS-02). locales/enUS.lua adds `L['%s is unavailable: the LibKa0s library did not load.']`. settings/Slash.lua adds `local function libraryAbsent(verb) NS.Print(L['%s is unavailable: the LibKa0s library did not load.']:format(verb)) end`. runEnabled (:352-362): `if not H.FindSchema(ENABLED_PATH) then return libraryAbsent(on and '/wg enable' or '/wg disable') end; local ok, err = H.Set(ENABLED_PATH, on); if ok == false then return NS.Print(tostring(err)) end`, then the existing ack. It never acks a write that did not land. runTest (:280-294): for sub '' / 'on' / 'off', `if not H.FindSchema(TEST_MODE_PATH) then return libraryAbsent('/wg test') end` before any Set. 'notify' and the usage lines are unchanged. (7) docs/ARCHITECTURE.md '## Documented deviations' gets a new row. Rule `options-ui-\u00a71`. What differs: on a library-absent load `/wg enable` and `/wg disable` refuse with the library-absent line instead of writing through a Schema writeThrough list, which is the SHOULD route (a). Why: the owner's ruling on WhatGroup#22 (2026-09-23), accepted as a known gap; composers stay hollow and no host copy is written (#73). Decided: <commit date>. Re-check trigger: a report of a library-absent install that needs the switch, options-ui-\u00a71 raising (a) to a MUST, or the owner reopening #22. docs/ARCHITECTURE.md's settings section, docs/schema.md, docs/slash-dispatch.md and docs/settings-panel.md name the seam (NS.SchemaRuntime over LibKa0s-Schema-1.0; settings/SchemaSetup.lua holds the stub). CLAUDE.md: 'nine of LibKa0s's majors' becomes ten (adds Schema), and the seam-file list adds settings/SchemaSetup.lua. Commit body: 'Closes #22'.\n\nUpstream follow-up LK-23 (WhatGroup#22 adoption note) is this item: LibKa0s-Schema-1.0 minor 2 with the writeThrough descriptor field must be in the RV-WG payload, and WhatGroup deliberately passes NO writeThrough list (the owner's route (b) under WS-02). On a library-absent load /wg enable, /wg disable and /wg test on|off print the WS-02 library-absent line ('/wg enable is unavailable: the LibKa0s library did not load.') and raise no Lua error. The two tests/test_libka0s.lua characterization cases are rewritten to assert that line and the unchanged store, and the options-ui-\u00a71 SHOULD deviation row is recorded in docs/ARCHITECTURE.md. HostSchemaStub copies LK-23's referenceStub (which gains writeThrough) but leaves writeThrough unset.",
   "tests": "Test first. (a) Rewrite the two tests/test_libka0s.lua characterization cases (:629-661). 'degraded: `/wg disable` and `/wg enable` print the library-absent line and write nothing (options-ui-\u00a71, WhatGroup#22)': pcall(OnSlashCommand, 'disable') is ok, the last print ends with '/wg disable is unavailable: the LibKa0s library did not load.', and db.profile.enabled is still true (the unchanged store); the same for 'enable'. 'degraded: `/wg test on|off` print the library-absent line and move nothing': the line names '/wg test', NS.State.testMode is not true, and the popup was not built. Both are red on the pre-adoption seam, where RawSet writes the row-less path; confirm that, then swap the seam. (b) Run the suite after the swap. The expected reds are exactly the 8 re-pins the Schema doc's 'Adoption notes' sanction, as measured in the 2026-09-23 attempt. RawSet gone: tests/test_settings.lua:310 and tests/test_libka0s.lua:285's member list. opts.skipOnChange/skipRefresh gone: tests/test_settings.lua:318. An unknown path refused (JC-2), two cases. A raising onChange propagating (JC-3): tests/test_debuglog.lua ~:358. Two extra refreshes in the reset's session sweep. Re-pin each in this commit with a comment naming the Adoption-notes bullet. A 9th red is a defect: stop and investigate. (c) New parity cases in tests/test_surface_parity.lua: `T.assertSurfaceParity(live.SchemaRuntime, degraded.SchemaRuntime, 'schema instance vs host stub')` and `T.assertSurfaceParity(degraded.addon.Settings.SchemaLib, 'LibKa0s-Schema-1.0', { 'STRINGS' })`. tests/run.lua's setSurfaceSource map gains `['LibKa0s-Schema-1.0'] = surfaceMock.LibStub('LibKa0s-Schema-1.0', true)`. (d) 'degraded: Reset all settings still lands through the stub' (the WG-09 case stays green). Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -nE 'RawSet|skipOnChange|skipRefresh|bulkDepth|local function Resolve' settings/*.lua && grep -n 'SchemaSetup' WhatGroup.toc",
   "smoke": "In client, library-present: /wg disable, /wg enable, /wg test on|off, the Master controls checkboxes and /wg set notify.delay 3 all behave as before, and /wg debug on shows one [Set] line per write. Library-absent: temporarily rename Interface/AddOns/WhatGroup/libs/LibKa0s, /reload, then run /wg disable and /wg test on. Each prints '<verb> is unavailable: the LibKa0s library did not load.' with no Lua error. Restore the folder.",
   "finding_ids": [],
   "issue_refs": [
    "WhatGroup#22"
   ],
   "depends_on": [
    "RV-WG",
    "WG-08",
    "WG-09",
    "WG-11",
    "LK-23",
    "WG-00"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "WG-13",
   "repo": "WhatGroup",
   "title": "One refusal line: the degraded Slash stub builds DisabledLine from the library's format bytes (Kit.assertLibraryConstant), and the launcher uses Launcher minor 2's isEnabled/disabledLine",
   "change": "settings/Slash.lua degraded stub (:168-170). Add `local DISABLED_LINE_FORMAT = '%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r'`, a byte-for-byte copy of libs/LibKa0s/Slash.lua:83 with the reason in a comment (LK-18's prescribed stub shape). DisabledLine becomes `function() return DISABLED_LINE_FORMAT:format('Ka0s WhatGroup', '/wg enable') end`. Publish it as `__disabledLineFormat = DISABLED_LINE_FORMAT` on the stub table; a `__` key is outside Kit.publicMembers, so parity is unaffected. core/LauncherSetup.lua (:141-155). Move the gate into the descriptor (LibKa0s-Launcher-1.0 minor 2, LK-16): `isEnabled = function() return not NS.IsStoodDown() end` and `disabledLine = function() return NS.SlashCommands:DisabledLine() end`. onClick becomes `function() NS.addon:ToggleFrame() end`. Delete the in-onClick IsStoodDown branch and the `or 'Ka0s WhatGroup is disabled.'` literal. Rewrite the :141-147 comment, fixing the malformed 'slash-commands-@7' to 'slash-commands-\u00a77' (the core/LauncherSetup.lua:143 site of WHATGROUP-A-12). docs/ARCHITECTURE.md:340 (launcher rung) and docs/slash-dispatch.md: the gate is the library's, and the line is the dispatcher's.",
   "tests": "Red first. (a) tests/test_libka0s.lua 'libka0s: no seam re-spells the refusal line (slash-commands-\u00a77)': scan core/*.lua and settings/*.lua for the literal 'is disabled' outside the one DISABLED_LINE_FORMAT constant. It is red on the LauncherSetup.lua:152 literal. (b) 'degraded: the Slash stub's DisabledLine uses the library's DISABLED_LINE_FORMAT bytes' with `T.assertLibraryConstant(degraded.SlashCommands.__disabledLineFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')`. Caveat: tests/run.lua maps 'LibKa0s-Slash-1.0' to the Slash INSTANCE, not the library table. Check LK-18's resolver (`grep -n 'assertLibraryConstant' -A30 tests/_kit/asserts.lua`). If it cannot reach a lib-level constant through that map, compare with T.assertEqual against `select(2, T.loadAddon()).LibStub('LibKa0s-Slash-1.0').DISABLED_LINE_FORMAT` and record why in the case comment. (c) tests/test_launcher.lua 'launcher: a disabled left-click prints the dispatcher's line and does not toggle' (the library's gate). tests/test_disabled.lua 'disabled 8' must stay green. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -n 'is disabled\\.' core/LauncherSetup.lua && ! grep -n 'slash-commands-@' core/*.lua",
   "smoke": "In client: /wg disable, then left-click the minimap button. One line appears: 'Ka0s WhatGroup is disabled \u2014 enable it with /wg enable'. No popup opens. Right-click opens settings.",
   "finding_ids": [
    "WHATGROUP-A-19"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-12",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-14",
   "repo": "WhatGroup",
   "title": "Surface-parity cases for the Launcher and Lifecycle degradation stubs (testing-\u00a78)",
   "change": "tests/test_surface_parity.lua. Add 'parity: the Launcher stub carries the whole live surface': `local degraded = T.newAddon{ skip = NO_LIBKA0S }; T.assertSurfaceParity(degraded.Launcher, 'LibKa0s-Launcher-1.0')`, with the grep that names the live surface in a comment (`grep -nE '^ *function [a-z]+[:.]|^ *[a-z]+\\.[A-Z][A-Za-z]+ *=' libs/LibKa0s/Launcher.lua`). Add 'parity: the Lifecycle stub carries the whole live surface' with `T.assertSurfaceParity(degraded.Lifecycle, 'LibKa0s-Lifecycle-1.0', { <`name` only if the live instance lacks it, with the reason> })`. tests/run.lua's setSurfaceSource map gains `['LibKa0s-Launcher-1.0'] = surfaceNS.Launcher` and `['LibKa0s-Lifecycle-1.0'] = surfaceNS.Lifecycle`. Rewrite the header (:1-43) to name every stub: Core (namespace), DebugLog, Slash, Options, Compat, Launcher, Lifecycle and Schema (WG-12), with Env and Media covered by the namespace case. tests/test_launcher.lua:391-403 keeps its behavior checks (Register false, the store moves) and drops the hand-listed member presence now covered by parity.",
   "tests": "Add the two cases and prove each can fail. Temporarily delete `PrintHolds` from core/LifecycleSetup.lua's stub and `Object` from core/LauncherSetup.lua's stub; each case goes red naming the member. Revert and record both in the commit body. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-12",
    "WG-13",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-15",
   "repo": "WhatGroup",
   "title": "Teleport button script handlers defined once at file scope instead of three fresh closures per configure",
   "change": "modules/Frame.lua, applyTeleportAction (:403-456). Add three file-scope handlers above it. `local function onTeleportEnter(self) GameTooltip:SetOwner(self, 'ANCHOR_RIGHT'); GameTooltip:SetSpellByID(self.__wgSpellID); GameTooltip:Show() end`. `local function onTeleportLeave() GameTooltip:Hide() end`. `local function onTeleportPreClick(self, mouseButton, down) if down then NS.Debug('Frame', 'teleport button pressed \\226\\134\\146 /cast %s (spellID=%s, button=%s)', self.__wgSpellName, self.__wgSpellID, mouseButton) end end`. The wording is byte-identical to today's line, and this closes the Frame.lua:425-427 pre-format site of WHATGROUP-R-05. applyTeleportAction sets `btn.__wgSpellID, btn.__wgSpellName = spellID, spellName` BEFORE any SetScript, then passes the named handlers on both the ready branch and the known-but-not-ready branch (:444-449). The unlearned branch still clears to nil. The PreClick runs alongside the secure /cast and stays non-secure. Move the detached doc blocks (:385-402) onto their functions here only if WG-21 has not; WG-21 owns that. Perf: re-run tests/perf.lua and record the new showFrameRepeat row in docs/performance.md:146. In tests/perf.lua:285-294, CEILINGS.showFrameRepeat = ceil(shown) is computed from the same run and cannot trip. Pin it as a literal (new measured bytes/iter rounded, + 24), with a comment dated to this run.",
   "tests": "Red first, in tests/test_frame_secure.lua: 'frame: reconfiguring the teleport button reuses the same three script handlers'. Configure twice with two different captures and assert `btn:GetScript('OnEnter')`, `GetScript('OnLeave')` and `GetScript('PreClick')` are identical across the runs. The tooltip shows the second capture's spellID, and the PreClick debug line names the second spell. It carries '-- red under: per-call closures in applyTeleportAction'. The existing tooltip and PreClick cases in tests/test_frame.lua must stay green. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -n 'SetScript(\"OnEnter\", function' modules/Frame.lua",
   "smoke": "S-006 in client. Hover the teleport button: the tooltip shows the dungeon's spell. Click it while ready and it casts. /wg debug on logs one 'teleport button pressed' line per press. Reopen the popup on a different capture and hover again: the tooltip shows the new spell.",
   "finding_ids": [
    "WHATGROUP-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-03",
    "WG-05",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-16",
   "repo": "WhatGroup",
   "title": "NS.Debug call sites pass a format and raw values instead of building the message before the call (debug-logging-\u00a74)",
   "change": "The remaining sites after WG-08 (core/Database.lua:43), WG-12 (settings/Schema.lua:398/:465 deleted with the old seam; the library writes '%s = %s') and WG-15 (modules/Frame.lua:425). core/WhatGroup.lua :506 `NS.Debug('Capture', 'GetSearchResultInfo returned nil for id=%s', searchResultID)`, then :786, :818, :832, :885, :916-918, :973 (`'appID=%s status=%s', appID, newStatus`), :1026 and :1096. Also :216's tostring() args, which are passed raw. modules/Frame.lua :504-507 (resolveTeleportState, one format with four %s). :890-892 in preparePopup: split the ternary into `if info then NS.Debug('Frame', 'popup shown \"%s\" map=%s', info.title, info.mapID) else NS.Debug('Frame', \"popup shown (no pendingInfo \\226\\134\\146 'No data' fallbacks)\") end`. :948 and :1050-1051. Every rendered line stays byte-identical. After this, docs/ARCHITECTURE.md's events-frames-taint-\u00a78 register row (:504) drops the 'every NS.Debug(tag, \"\u2026\" .. tostring(x)) call' clause; the join summary's chat rows (:726-742) are a separate matter and stay. Re-run the census to 0.",
   "tests": "Characterization first. For each rewritten site, find its pin (grep the literal prefix in tests/test_debuglog.lua, test_capture.lua, test_notify.lua, test_frame*.lua). Where a site is unpinned, add a pin to the case that drives that path before the rewrite, asserting the full rendered line in NS.DebugLog.buffer (for example the LFG status line, the roster line, the preparePopup 'popup shown' line in both arms, and the test-injection line). Those go green before the rewrite and must stay green after it. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -rnE 'NS\\.Debug\\(.*(\\.\\.|tostring\\()' core modules settings | grep -v 'OptionsSetup.lua'",
   "smoke": "S-005 in client: /wg debug on, apply to a listing and get invited. The console lines read exactly as before (LFG appID/status, Invite, Capture, Frame 'popup shown').",
   "finding_ids": [
    "WHATGROUP-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-08",
    "WG-12",
    "WG-15",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-17",
   "repo": "WhatGroup",
   "title": "settings/Panel.lua: drop the two unread category handles and build the landing logo path from the folder name",
   "change": "settings/Panel.lua:387-390: delete `WhatGroup._settingsCategory = sub` and `WhatGroup._parentSettingsCategory = parentCategory` with their comment; `return sub` stays. settings/Panel.lua:87: `local MAIN_LOGO_TEXTURE = ('Interface\\\\AddOns\\\\%s\\\\media\\\\logos\\\\%s.logo.tga'):format(addonName, addonName:lower())`, where addonName is bound at :14 (library-stack-\u00a78). Docs: remove the 'records the two handles' sentence at docs/ARCHITECTURE.md:323 and docs/settings-panel.md:260. Rewrite the docs/midnight-quirks.md:38-40 example with a local `category` variable instead of self._settingsCategory.",
   "tests": "tests/test_panel.lua:90-91: drop the two non-nil asserts on _settingsCategory/_parentSettingsCategory and keep the rest of the case, so the count is unchanged. Red first: 'panel: the landing logo path is built from the folder this copy loaded from' loads under a different addonName through the loader option used by tests/test_launcher.lua:116, and asserts the logo texture path names that folder in lowercase-dot form. It carries '-- red under: the hand-typed WhatGroup literal'. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! git grep -n '_settingsCategory\\|_parentSettingsCategory' -- settings core modules docs/ARCHITECTURE.md docs/settings-panel.md docs/midnight-quirks.md",
   "smoke": "In client: Settings > AddOns > Ka0s WhatGroup landing page shows the logo, and /wg config opens the General page.",
   "finding_ids": [
    "WHATGROUP-R-13",
    "WHATGROUP-A-21"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-18",
   "repo": "WhatGroup",
   "title": ".luacheckrc: drop six unread read_globals and exclude docs/revendor/ as the lint template does",
   "change": ".luacheckrc:47/:52/:54: remove GetSpellInfo, GetSpellTexture, GetSpellCooldown, CastSpellByID, SettingsPanel and date from read_globals. If luacheck then flags one, restore it with a comment naming its reader. The review measured 0/0 with all six removed. .luacheckrc:17: exclude_files becomes { 'libs/', 'docs/audits/', 'docs/reviews/', 'docs/revendor/', '_dev/', 'tests/_kit/' }, with the :10-16 comment extended to name docs/revendor/ as a frozen bundle store (lint template).",
   "tests": "Red first, in tests/test_lintconfig.lua. 'lint: exclude_files carries the template's frozen stores' asserts docs/revendor/ is present. 'lint: read_globals grants no removed or unread global' asserts none of the six names appears in read_globals. Both are red on the current file. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-R-11",
    "WHATGROUP-A-20"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-19",
   "repo": "WhatGroup",
   "title": "Delete the unread 'WhatGroup is disabled' locale key and correct the locale header",
   "change": "locales/enUS.lua:119-123: delete `L['WhatGroup is disabled \u2014 |cffFFFF00/wg enable|r turns it back on']` and its comment. The refusal is LibKa0s-Slash's DISABLED_LINE_FORMAT through Sl:DisabledLine, and WG-13 made the degraded stub use the same bytes. Header :19-35: stop listing the disabled refusal as routed through L, drop the nonexistent 'Settings layer not ready yet' example, and name WG-12's library-absent line as a routed key. The localization-\u00a73 register row's class (1) example list (docs/ARCHITECTURE.md:503) also drops 'Settings layer not ready yet'. Coordinate with WG-22, which edits that row's Rule and Why cells.",
   "tests": "Red first: tests/test_libka0s.lua 'locale: every key enUS.lua defines has a reader'. For each `L[\"...\"]` key defined in locales/enUS.lua, it looks for the same literal in a `L[...]` read in core/, modules/ or settings/. It is red on the dead key. Regenerate docs/test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -n | tail -4 && ! grep -rn 'Settings layer not ready yet' locales core settings",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-12",
    "WG-13",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-20",
   "repo": "WhatGroup",
   "title": "TOC: annotate core\\Compat.lua as load-bearing and core\\Util.lua as conventional",
   "change": "WhatGroup.toc, above core\\Compat.lua (:42): '# Before core\\WhatGroup.lua: it calls NS.Compat.AddOnLinkType() at file scope (core/WhatGroup.lua:87) to choose the chat-link route. Load-bearing.' Above core\\Util.lua (:41): '# NS.Windows and NS.FormatDuration; reads nothing at load, so this slot is conventional.' Recheck the line numbers the other TOC comments cite (for example 'core/WhatGroup.lua:167', 'settings/Panel.lua:295', ':204', 'settings/Slash.lua:116') after WG-01..WG-19 moved them. Fix any that drifted.",
   "tests": "none \u2014 TOC comment lines only. tests/test_harness.lua already pins the load list, and it must stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && grep -n -B2 'core\\\\Compat.lua' WhatGroup.toc",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-12",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-21",
   "repo": "WhatGroup",
   "title": "Comments tell the truth: modules/Frame.lua's stale taint and ticker comments, the wrong file homes, the drifting counts, and the malformed citations",
   "change": "modules/Frame.lua (WHATGROUP-R-07). Rewrite the outOfCombat bullet (:198-201): the popup is soft-hidden at alpha 0 on the entering edge, and the real Hide lands at PLAYER_REGEN_ENABLED. Delete the :205-206 'f:Show() is not a secure write' paragraph, which :255-258 contradicts. Restate :748-751: the swipe has no Lua repeat, and the note's countdown is the ratified cooldown ticker (docs/performance.md, and the performance-\u00a712 register row). Move each detached doc block (:385-402) onto its function: deferTeleportUntilCombatEnds, resolveTeleportState, applyTeleportNote, applyTeleportAction. Delete whatever of the superseded :999-1005 paragraph survived WG-05. :608 `stopCooldownTicker(self)` becomes `stopCooldownTicker()`. core/WhatGroup.lua headers (WHATGROUP-R-08): :2 drops 'slash dispatch' (settings/Slash.lua); :20, :44 and :129 name core/CoreSetup.lua for NS.Util.print/NS.SafeToString. core/Util.lua: the header :2 names both seams (NS.Windows, NS.FormatDuration), and the Windows comment block (:17-24) moves directly above NS.Windows. Counts (WHATGROUP-R-09): settings/Schema.lua:5, :104, :125, :589; settings/Panel.lua:192, :284, :313; defaults/Profile.lua:15, :25. Use non-numeric wording ('the composed Master controls block', 'every Ka0s addon'). Malformed citations (WHATGROUP-A-12): tests/prose_waivers.lua:2 and :4 'localization-5' become 'localization-\u00a75'; tests/run.lua:140 'localization-5' becomes 'localization-\u00a75'; tests/run.lua:145 'layout-1' becomes 'layout-\u00a71'. core/LauncherSetup.lua:143 was fixed in WG-13. WHATGROUP-A-13's generated half: after RV-WG's kit-26 case names carry \u00a7, check that docs/test-cases.md has no ASCII-spelled kit citation left.",
   "tests": "none \u2014 comment-only edits, plus one harmless dropped argument (`stopCooldownTicker(self)`) that no test can observe. The suite and luacheck stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && lizard -l lua -x './libs/*' -x './tests/_kit/*' -C 15 -w . && ! grep -rnE '(localization|layout|line-endings|testing|slash-commands)-[0-9@]' core modules settings defaults locales tests/*.lua WhatGroup.toc && ! grep -nE 'localization-5|layout-1|line-endings-[0-9]|testing-1[0-9]' docs/test-cases.md && ! grep -n 'stopCooldownTicker(self)' modules/Frame.lua",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-R-07",
    "WHATGROUP-R-08",
    "WHATGROUP-R-09",
    "WHATGROUP-A-12",
    "WHATGROUP-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-05",
    "WG-13",
    "WG-15",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-22",
   "repo": "WhatGroup",
   "title": "Register evidence ids resolve, the register gate can fail, and the English-only row cites localization-\u00a71",
   "change": "tests/test_register.lua. (1) Drop the `not id:find('%-R%-')` skip (:94). Resolve review ids (`WG-R-NN` and `F-NNN`, with a dated bundle named in the cell) against docs/reviews/*/01_FINDINGS.md headings and table cells, the same way deviation ids resolve against docs/audits/. (2) isAssigned (:58-77) ignores table rows under a heading that echoes another bundle's register (a '## Recorded deviations' / echo table, as in docs/audits/2026-09-07/02_DEVIATIONS.md:46), so an echo cannot assign an id. docs/ARCHITECTURE.md. :503: the Rule cell becomes `localization-\u00a71` (WHATGROUP-A-17), and the Why cell's `WG-R-06` becomes `F-006 (docs/reviews/2026-08-05/)`. :504: `WG-A-08` becomes `WG-37 (docs/audits/2026-08-05/)`. :494-498: extend the key to name the F-NNN review shape and its dated bundle. :385: the cross-reference points at localization-\u00a71.",
   "tests": "Red first. Tighten test_register.lua as in (1) and (2) and run it against the current :503/:504. It must fail, naming WG-R-06 (resolves to the wrong finding) and WG-A-08 (assigned only by an echo table). Add '-- red under: restoring WG-A-08 in the :504 Why cell' above the case. Then fix the cells so it goes green. The case count may be unchanged; re-run --list.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ! grep -n 'WG-A-08\\|WG-R-06' docs/ARCHITECTURE.md && grep -n '^| `localization-\u00a71`' docs/ARCHITECTURE.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-05",
    "WHATGROUP-A-06",
    "WHATGROUP-A-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-16",
    "WG-19",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-23",
   "repo": "WhatGroup",
   "title": "## Message Bus cites the amended architecture-\u00a74 threshold: a shell plus one feature module is below it",
   "change": "docs/ARCHITECTURE.md:122-129 '## Message Bus'. Replace the below-threshold reasoning with a citation of architecture-\u00a74 as amended in v2.65.0 (WS-04): the bus MUST applies at two or more feature modules, or at a feature module that registers game events that a second feature module must react to, and the AceAddon object's own handlers are not a feature module. WhatGroup is the shell (core/WhatGroup.lua, which registers the four events) plus one feature module (modules/Frame.lua, which registers none since WG-05). It is below the threshold, so no bus and no register row. Keep the re-open condition: a second feature module, or a second consumer of the join data. If a Documentation map row for message-bus.md says 'not applicable', make it cite the same clause.",
   "tests": "none \u2014 docs only. tests/test_doc_structure.lua and tests/test_docmap.lua stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'architecture-\u00a74' docs/ARCHITECTURE.md && head -1 ../WowAddonStandards/standards/STANDARDS.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-05",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-24",
   "repo": "WhatGroup",
   "title": "compat-layer.md and debug.md document only what is WhatGroup's own",
   "change": "docs/compat-layer.md. Trim it to the six addon-owned shims: GetSpellLink, IsSpellKnown, GetSpellCooldownRemaining, GetSpellCooldownTimes, GetActivityInfoTable and AddOnLinkType. `grep -cE '^\\s*function\\s+[A-Za-z_.]+\\.' core/Compat.lua` must answer 6. Replace the GetSpellName/GetSpellTexture rung tables (:21-22) with one line pointing at LibKa0s docs/api/Compat/version-1-docs.md. The count becomes six at compat-layer.md:16, docs/ARCHITECTURE.md:388 and docs/ARCHITECTURE.md:464. docs/debug.md: keep the addon-owned content (the descriptor fields core/DebugLogSetup.lua passes, the tag vocabulary including WG-06's 'Events' tag, how to add a debug line, /wg debug semantics). Replace the library-restating sections (The window, Line format, Font, Copy/Clear) with a pointer to LibKa0s docs/api/DebugLog. docs/ARCHITECTURE.md:462: set the Tier 2 debug.md row to 'Not applicable' (the trigger, debug surfaces beyond the LibKa0s console, has not fired) and register debug.md as a Tier 3 addon-specific doc in the '## Documentation map' with its reason.",
   "tests": "none \u2014 docs only. tests/test_docmap.lua and tests/test_doc_structure.lua stay green; fix any pinned heading they read.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -cE '^\\s*function\\s+[A-Za-z_.]+\\.' core/Compat.lua && ! grep -in 'eight' docs/compat-layer.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-06",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-25",
   "repo": "WhatGroup",
   "title": "Correct the eight docs and comment sites that drifted from the tree",
   "change": "README.md:41 and :48: only the popup's position persists; the debug console's does not (issue #11, will-not-do). Use 'the place you dragged the popup to', with a de-AI pass per documentation-\u00a71, and fix the FAQ row that says 'popup and debug windows'. docs/ARCHITECTURE.md:19 and docs/module-map.md:68: 'FIFO queue' becomes 'keyed by search-result id' (core/WhatGroup.lua:123). docs/ARCHITECTURE.md:55: the seam count reads the number actually adopted after WG-12 (ten majors), or non-numeric wording. :319: list every degrading seam (Core, DebugLog, Env, Media, Compat, Launcher, Lifecycle, Options, Slash, Schema) and drop 'the other three'. :383: 'the 21 files LibKa0s.xml lists' becomes 'the files libs/LibKa0s/LibKa0s.xml lists', with no enumeration. :502: drop the retired 'the row above' reference. core/MediaSetup.lua:33-34: the footer mark was removed, and NS.Icon has no caller outside MediaSetup. Say so, or delete NS.Icon if `git grep -n 'NS.Icon' -- core modules settings` shows only its definition; in that case the namespace parity case covers both arms.",
   "tests": "none \u2014 docs and comment only. If NS.Icon is deleted, tests/test_surface_parity.lua's namespace case and tests/test_mediasetup.lua must stay green, and any case pinning NS.Icon is removed with a note.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && ! grep -n 'FIFO' docs/ARCHITECTURE.md docs/module-map.md && ! grep -n 'row above' docs/ARCHITECTURE.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-12",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-26",
   "repo": "WhatGroup",
   "title": "Re-check settings-panel.md and .pkgmeta against the v2.65.0 wording (Page | Covers; the .png excluded from the package)",
   "change": "docs/settings-panel.md. Add the documentation-\u00a73 table as amended by WS-07: `| Page | Covers |`, one row per settings subcategory page. WhatGroup has one, General: Master controls, Chat and Popup tabs. Place it before the existing per-tab table (:327-341), which stays as the page -> tab -> row tree. .pkgmeta:27-31: the `media/logos/*.png` ignore already matches layout-\u00a74 as amended ('the editable .png is committed but excluded from the package by a .pkgmeta ignore'). Re-word the comment above it to cite layout-\u00a74, with no behavior change. WHATGROUP-A-23's other two parts are upstream only: AUDIT.md's grade is fixed by WS-01, and toc-file-\u00a73's literal by WS-07. Confirm locally with `git grep -n 120007` (expect none).",
   "tests": "none \u2014 docs and a packaging comment only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n '| Page | Covers |' docs/settings-panel.md && grep -n 'layout-\u00a74' .pkgmeta && ! git grep -n '120007' -- ':!libs' ':!docs/audits' ':!docs/reviews'",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-22",
    "WHATGROUP-A-23"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-27",
   "repo": "WhatGroup",
   "title": "DEPENDENCIES.md names Python 3 + Pillow for the logo recipe and labels lizard as required at release",
   "change": "DEPENDENCIES.md:190-192: replace 'no image tooling needed' with a Release/assets entry. Python 3 + Pillow regenerate media/logos/whatgroup.logo.128.tga from the .png (layout-\u00a74); install with `sudo apt install python3-pil` or a venv (`python3 -m venv .venv && .venv/bin/pip install pillow`), and verify with `python3 -c 'import PIL; print(PIL.__version__)'`. It is not needed to build, run or test. DEPENDENCIES.md:92-99: lizard is optional per commit and required at release, because a complexity skip is a gate that did not pass (automated-tests-\u00a73).",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'Pillow' DEPENDENCIES.md && grep -n 'automated-tests-\u00a73' DEPENDENCIES.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-28",
   "repo": "WhatGroup",
   "title": "Bring docs/ARCHITECTURE.md under ~400 lines: spill The stand-down and the per-file load order",
   "change": "docs/ARCHITECTURE.md (533 lines, before the edits in WG-01..WG-27). Move '## The stand-down' (:193-282, about 90 lines, including WG-01's survivor text) to a new Tier 3 docs/stand-down.md with the standard doc header. Leave a 5-10 line summary and a link. Move '## Load order''s per-file list (:367-410) into docs/module-map.md, leaving the summary and a link. Register docs/stand-down.md in '## Documentation map' as an Addon-specific row. Update every citation of 'ARCHITECTURE.md \u2192 The stand-down' in code comments and docs: `git grep -n 'The stand-down' -- core modules settings docs ':!docs/audits' ':!docs/reviews'`. Target 400 lines or fewer, per `wc -l`.",
   "tests": "none \u2014 docs only. tests/test_docmap.lua and tests/test_doc_structure.lua must stay green; adjust any pin that reads the moved headings and say which in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && test $(wc -l < docs/ARCHITECTURE.md) -le 400 && test -f docs/stand-down.md",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-01",
    "WG-05",
    "WG-12",
    "WG-22",
    "WG-23",
    "WG-24",
    "WG-25",
    "WG-00"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "WG-29",
   "repo": "WhatGroup",
   "title": "Write the consolidated re-vendor span bundle for the 25 unrecorded LibKa0s tags (audit-review-history, WS-01 shape)",
   "change": "Derive the list rather than trust the finding. Run `git log --since='2026-08-25 00:00' --format=%h -- libs/LibKa0s | while read c; do git show $c:CLAUDE.md | grep -oE 'LibKa0s\\]\\([^)]*\\) v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -oE 'v[0-9.]+$'; done | sort -uV`, then subtract the tags docs/revendor/ records. The expected 25 are v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0-v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0-v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0 and v1.50.0-v1.53.0. Create docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.53.0/ with two files. 01_DELTA.md: line 1 exactly `Delta: LibKa0s v1.18.0 -> v1.53.0 (span: v1.18.0, v1.18.1, ..., v1.53.0)`, listing every unrecorded tag; the body gives the per-tag commit that vendored it and the three re-vendors folded into feature commits (f98ef41, 127baa1, f74893a). 05_SUMMARY.md: a Backlog section stating the span was not recorded at the time, and that no per-tag folder is back-filled (audit-review-history permits one consolidated bundle naming the span). No earlier frozen bundle is edited.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none \u2014 a docs-only record bundle.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && ls docs/revendor/ && head -1 docs/revendor/*-v1.18.0-v1.53.0/01_DELTA.md && echo 'Run the AUDIT.md re-vendor bundle check from ../WowAddonStandards/AUDIT.md (v2.65.0, WS-01) in this repo: it must print nothing'",
   "smoke": "",
   "finding_ids": [
    "WHATGROUP-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WA-01",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "WG-DOCS",
   "repo": "WhatGroup",
   "title": "Sync the docs, regenerate docs/test-cases.md, and record a fresh automated-test bundle (no version bump)",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md and docs/ARCHITECTURE.md. Check counts (majors adopted: ten after WG-12; seam files including settings/SchemaSetup.lua; suites including tests/test_frame_secure.lua), COMMANDS/slash parity (the library-absent line), toolchain versus DEPENDENCIES.md, and comment citations. (2) `lua5.1 tests/run.lua --list > docs/test-cases.md`, and the README Tests badge from its ## Totals. (3) On a clean tree, after committing WG-01..WG-30, run `tests/_kit/run-automated-tests.sh` (all four suites, no --release). It writes docs/automated-tests/<YYYYMMDD-HHMMSS>/ and regenerates RESULTS.md. Write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md playbook. In RESULTS.md's band table, the authored Disposition column: core/WhatGroup.lua, modules/Frame.lua and tests/test_frame.lua each get an accepted-with-trigger disposition (the 1450-line re-check) or a split issue (WHATGROUP-R-14). (4) LK-09 follow-up: the watch-list tables are headed even when empty. perf records `pass`, because tests/perf.lua exists: skip reason (2) from the performance-\u00a712 row applies only to an addon with no tests/perf.lua. Confirm the manifest's perf status is 'pass' and not a register-derived skip; if it is a skip, file it upstream against LK-09 and do not hand-edit. No version bump and no release tag; the owner decides.",
   "tests": "none \u2014 generated records and docs only. The run itself is the check: lint 0/0, tests all pass, perf pass, and complexity 0 functions above CCN 15.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && git status --porcelain | wc -l && tests/_kit/run-automated-tests.sh && ls -t docs/automated-tests/ | head -2 && diff <(lua5.1 tests/run.lua --list) docs/test-cases.md && grep -o 'Tests-[0-9]*%2F[0-9]*' README.md && grep -n 'Totals' -A4 docs/test-cases.md && test -f docs/automated-tests/$(ls -t docs/automated-tests/ | grep -E '^[0-9]{8}-' | head -1)/ANALYSIS.md",
   "smoke": "The full in-client pass from docs/smoke-tests.md after a libs/ refresh, plus the per-item smokes: WG-01, WG-02, WG-03, WG-04, WG-05, WG-07 (S-008), WG-08 (S-003), WG-11, WG-12, WG-13, WG-15 (S-006) and WG-16 (S-005).",
   "finding_ids": [
    "WHATGROUP-R-14",
    "WHATGROUP-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-00",
    "WG-02",
    "WG-03",
    "WG-04",
    "WG-05",
    "WG-06",
    "WG-07",
    "WG-08",
    "WG-09",
    "WG-10",
    "WG-11",
    "WG-12",
    "WG-13",
    "WG-14",
    "WG-15",
    "WG-16",
    "WG-17",
    "WG-18",
    "WG-19",
    "WG-20",
    "WG-21",
    "WG-22",
    "WG-23",
    "WG-24",
    "WG-25",
    "WG-26",
    "WG-27",
    "WG-28",
    "WG-29"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-02",
   "repo": "ConsumableMaster",
   "title": "The local AceDB fake strips defaults on a profile switch and raises on bad Copy/Delete, as AceDB-3.0 and kit 26 do",
   "change": "tests/wow_mock.lua makeAceDB (:386-518). This addon keeps its own AceDB fake instead of the kit's, for two reasons: the kit fires callbacks as plain functions with no CallbackHandler string-method form, and it keeps the store at sv.profiles, not db.profiles. So the LK-03 follow-up ('drop the SetProfile override') becomes this: make the local fake match the kit-26 fidelity, and do not delete it. (a) Add a local removeDefaults(dest, src) that mirrors AceDB-3.0.lua:460-463 over the scalar and plain-sub-table arms. It nils a stored scalar equal to its default, recurses into tables and deletes emptied sub-tables. Add a comment saying the '*'/'**' wildcard arms are unmodeled because defaults/Profile.lua uses neither, and cite libs/AceDB-3.0/AceDB-3.0.lua and tests/_kit/mock_record.lua's kit-26 mirror. (b) db.SetProfile (:491-499) runs removeDefaults(db.profile, defaults.profile) over the OUTGOING profile before it switches. (c) db.CopyProfile raises AceDB's message byte for byte at level 2 in two cases: name == current, and a missing source unless `silent`. Add db.DeleteProfile(name, silent) if absent; it raises on the active profile, and on a missing profile unless silent. Copy the messages from libs/AceDB-3.0/AceDB-3.0.lua:531-537 and :581-587. (d) Rewrite the block comment at :373-384 to say why this fake stays and which kit-26 fidelities it now shares. Grep tests/*.lua for CopyProfile/DeleteProfile calls with a bad name, and correct any that relied on the silent no-op. Change the call, never the assertion.",
   "tests": "Write first, in tests/test_profiles.lua, and watch them go red against today's fake. (1) 'AceDB fake: a profile switch strips at-default values from the outgoing profile': set a profile scalar to its default, SetProfile('Other'), and assert db.profiles.Default.<key> == nil. (2) 'the shipped default table survives a switch': SetProfile away and back, then assert #KCM.dbDefaults.profile.macroBar.<a color key> == 4. It stays green here because nothing aliases yet; it is the guard CM-03 turns red. (3) 'CopyProfile onto the active profile raises AceDB's own message', checked with assertErrorMatches on the AceDB text. (4) the same for DeleteProfile of the active profile. Then implement. Add about 4 cases. Regenerate docs/test-cases.md and the README badge in the same commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && wc -l tests/wow_mock.lua (under 1500)",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "LK-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-03",
   "repo": "ConsumableMaster",
   "title": "The color validator stores a copy, so /cm reset <color> can no longer alias and empty the shipped default",
   "change": "settings/Panel.lua VALIDATORS.color (:920-923). After the type check, build `local out = {}; for k, v in pairs(value) do out[k] = v end; return out`. Add a comment giving the same reason the order validator states at :925-930: row.default IS the dbDefaults table, the reset applyDefault (settings/Slash.lua:530-533) sends it through SetAndRefresh, and AceDB's removeDefaults on a later SetProfile then nils its channels in place. This is the only table validator that did not copy. The page reset at settings/MacroBar.lua:636-637 already copies, so it does not change.",
   "tests": "Red first, in tests/test_schema.lua or tests/test_profiles.lua. (1) 'resetting a color row stores a copy, not the dbDefaults table': run `/cm reset macroBar.<color path>` through KCM:OnSlashCommand, then t.truthy(KCM.db.profile.macroBar.<color> ~= KCM.dbDefaults.profile.macroBar.<color>). (2) 'a profile switch after a color reset leaves the shipped default intact': reset, SetProfile('Other'), then assert #KCM.dbDefaults.profile.macroBar.<color> == 4. It goes red because CM-02's fake now strips. Both must be red before the fix. Add a falsification comment: 'red under: return value unchanged from VALIDATORS.color'. Add 2 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md)",
   "smoke": "In client: /cm reset on a macro-bar color, switch profile and back, then open the Macro Bar page. The swatch shows the shipped color, not opaque black, and /cm get on the path shows four channels.",
   "finding_ids": [
    "ConsumableMaster-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-04",
   "repo": "ConsumableMaster",
   "title": "FlushPending replays the queued body through commitMacro, so a combat-deferred weapon-enchant macro keeps /use 16 and /use 17",
   "change": "modules/MacroManager.lua. (a) FlushPending (:598-602): composites keep `pcall(M.SetCompositeMacro, entry.cat, nil)`. Every other entry calls `pcall(commitMacro, name, entry.body, entry.itemID, entry.catKey)`. That is the one existing write tail, not a second write path (events-frames-taint-\u00a74), and M.SetMacro stays for live callers. (b) queueForCombat (:411-420) is called from commitMacro (:470-472) with the pre-oversize iconItemID. Pass `effectiveItemID` instead, so a replayed empty-state body carries a nil icon item exactly as the live write would. Update the call site and the parameter comment. (c) Update the header comment above FlushPending (:584-590) and the queueForCombat comment: the queued body is replayed, not rebuilt, and why (the per-hand body is not a single-item body).",
   "tests": "tests/test_macromanager.lua. First a characterization case: the existing FOOD flush case at :330 stays green unchanged. Then two red-first cases. (1) 'FlushPending replays a per-hand weapon-enchant body': own two enhancements, setCombat(true), M.SetWeaponEnchantMacro(cat, 944001, 944002), setCombat(false), FlushPending(). Then assert mock.macros.KCM_WPN_ENCH.body contains '/use 16', '/use 17' and both item ids. (2) '/cm rewritemacros in combat keeps the WPN_ENCH body': drive KCM:OnSlashCommand('rewritemacros') in combat, leave combat, KCM:OnRegenEnabled(), and assert the same. Both are red before (a). Falsification comment: 'red under: FlushPending routing non-composites through M.SetMacro'. Add 2 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md)",
   "smoke": "In client, with an oil or stone for each hand in the bags, enter combat, run /cm rewritemacros and leave combat. The KCM_WPN_ENCH macro body still has both /use item + /use 16 and /use item + /use 17 lines.",
   "finding_ids": [
    "ConsumableMaster-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-05",
   "repo": "ConsumableMaster",
   "title": "Adopt LibKa0s-Options CreateOptionsPanel/OpenOptionsPanel (library parks and self-replays in combat); delete the host park, category-ID capture and expand walk",
   "change": "This fixes ConsumableMaster-R-03. LK-25's park replays on a library-private PLAYER_REGEN_ENABLED frame whatever the host's stand-down state, so a registration parked while the addon is disabled is no longer lost. (a) settings/OptionsSetup.lua optionsLib:New descriptor: add `buildMain = function(ctx) return Helpers.BuildAboutContent(ctx) end` and `validate = function() Helpers.ValidateSchema() end`. Both are thunks, because Panel.lua loads after this file. Delete the `UI.AceGUI = AceGUI` restatement and its comment (:283-285), because the library now re-resolves AceGUI inside CreateOptionsPanel, and fix the comment at :287-288 that says the addon never calls it. (b) settings/Panel.lua. KCM.Settings.RegisterTab (:1222-1231) keeps recording builders in KCM.Settings.builders. registerPanel (:1233-1297) keeps the libAbsent/sayPanelUnavailable early return and the Settings-API check. It then calls `UI.RegisterOptionsPage(key, key, KCM.Settings.builders[key])` once per key in KCM.Settings.order, guarded by a file-local `pagesQueued`, so the sidebar order is unchanged. Last it calls `UI.CreateOptionsPanel()`. Delete the InCombatLockdown park and its comment (:1253-1270), KCM.Settings.registerPending, KCM.Settings.main/sub and their initializers (:37-39), and `KCM._settingsCategoryID` (:1286). The bootstrap frame (:1302-1311) unregisters both events once registerPanel has handed the request to the library, because the library now owns park and replay. If Helpers.CreatePanel's wrapper adds anything the main canvas needs, fold it into buildMain. (c) core/ConsumableMaster.lua OnRegenEnabled (:651-656): delete the registerPending replay block. CCN drops from 15. (d) settings/OptionsShim.lua: delete expandMainCategory (:184-205). O.Open (:207-236) keeps the libAbsent branch, then `local opened = UI.OpenOptionsPanel()`. true returns true. false means refused in combat, and the library has already printed COMBAT_REFUSED, so return false. nil prints 'settings panel unavailable on this client; use /cm help.' and returns false. Delete sayCombatOpenBlocked if it has no other caller, and its test_locale pin. (e) No Options stub member is added: both call sites sit behind the libAbsent guard, so OPTIONS_SEAM in tests/test_surface_parity.lua is unchanged. (f) docs/ARCHITECTURE.md 'The disabled state is total' -> the 'What SURVIVES, because it is SETUP' row (:197): add 'the settings-category registration, including one parked in combat (LibKa0s-Options-1.0 replays it on its own frame, whatever the stand-down state)'. Also update the :279 Options adoption cell (CreateOptionsPanel/OpenOptionsPanel adopted) and docs/settings-panel.md's registration paragraph.",
   "tests": "Red first, in tests/test_settingsui_optionsui.lua. First, a new case, 'a registration parked while the addon is stood down still registers on regen'. It stubs Settings.RegisterCanvasLayoutCategory to return { GetID = function() return 7 end } and counts Settings.RegisterAddOnCategory. The steps: H.SetAndRefresh('enabled', false) out of combat, setCombat(true), KCM.Settings.Register(), setCombat(false), fire PLAYER_REGEN_ENABLED to every registered frame (the kit's frame __fire / event dispatch), then assert RegisterAddOnCategory was called once. A second variant stands down IN combat. Both must be red on today's code: the stood-down branch returns before the replay. Falsification comment: 'red under: the replay living only in the host's OnRegenEnabled'. Then rewrite the two existing cases at :769-799 against the library. The first asserts that nothing registers under lockdown and that a second Register while parked is a no-op. The second asserts that regen replays exactly once and that a second regen does not re-enter. Add a case that KCM.Options.Open in combat answers false and prints the library's COMBAT_REFUSED line exactly once. Net about +3 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -nE '_settingsCategoryID|registerPending|expandMainCategory' -- core settings tests && lizard -l lua modules core settings | grep OnRegenEnabled (CCN below 15)",
   "smoke": "In client, docs/smoke-tests.md \u00a76a, run twice. Enabled: /reload in combat, leave combat, and the category appears and /cm config opens and expands it. Disabled: /cm disable, /reload in combat, leave combat, and the category and its Enable checkbox appear. /cm config in combat prints the library's refusal once.",
   "finding_ids": [
    "ConsumableMaster-R-03",
    "ConsumableMaster-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "LK-25"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-06",
   "repo": "ConsumableMaster",
   "title": "Flyout attribute drivers stand down with the bar and re-arm on the way up; the disabled suite reads mock.attributeDrivers",
   "change": "modules/MacroBarFlyout.lua: add a module constant `local FLYOUT_COMBAT_DRIVER = \"[combat] 1; 0\"` and use it at :289-290. Keep a module-level `built` array that FO.Create appends each flyout to. Add `FO.StandDown()`, which runs UnregisterAttributeDriver(flyout, 'kcmCombat') for every built flyout (guarded on the global), and `FO.StandUp()`, which runs RegisterAttributeDriver(flyout, 'kcmCombat', FLYOUT_COMBAT_DRIVER) for every built flyout. Keep the frames; nothing is rebuilt. modules/MacroBar.lua MB.Update: in the disable branch (:453-461), which is already behind the one combat gate, call `KCM.MacroBarFlyout.StandDown()` next to UnregisterStateDriver. On the enable path, after ensure() and applyVisibility(), call `KCM.MacroBarFlyout.StandUp()`. docs/ARCHITECTURE.md 'The disabled state is total': list the flyout attribute drivers under what stands down. docs/macro-bar.md: same note.",
   "tests": "Red first, in tests/test_disabled.lua step 4 ('Disabled 4', :209-224). Before the disable, assert a non-empty baseline: at least one flyout has mock.attributeDrivers[flyout].kcmCombat == '[combat] 1; 0'. Flyouts come from the built bar's buttons; count any frame in mock.attributeDrivers carrying kcmCombat. After H.SetAndRefresh('enabled', false), assert that no frame in mock.attributeDrivers still carries kcmCombat. Step 9 ('Disabled 9', :432): after re-enable, the same frames carry the driver again. Falsification comment: 'red under: drop FO.StandDown from MB.Update's disable branch'. The step-4 half must be red before the fix. There are no new cases, only assertions in existing ones, so test-cases.md is unchanged; confirm with the diff.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && git grep -n UnregisterAttributeDriver -- modules",
   "smoke": "In client, docs/smoke-tests.md \u00a711e. With the bar on, /cm disable out of combat and then in combat (completes on regen). /cm enable, then hover a slot in and out of combat: the flyout still opens and closes, and its combat-grace behavior works after the re-arm.",
   "finding_ids": [
    "ConsumableMaster-A-01",
    "ConsumableMaster-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-07",
   "repo": "ConsumableMaster",
   "title": "Standing back up runs login's discovery pass and stale sweep (Pipeline.DiscoverAndSweep)",
   "change": "core/ConsumableMaster.lua: extract `local function discoverAndSweep(reason)`. It runs runAutoDiscovery(reason), then `if KCM.Selector and KCM.Selector.SweepStaleDiscovered then KCM.Selector.SweepStaleDiscovered(time()) end`. Publish it as KCM.Pipeline.DiscoverAndSweep beside KCM.Pipeline.RunAutoDiscovery (:359). OnPlayerEnteringWorld (:582-597) calls discoverAndSweep('player_entering_world') instead of the two inline calls. core/LifecycleSetup.lua standUp (:110-119): before RequestRecompute('stand_up'), call `if KCM.Pipeline and KCM.Pipeline.DiscoverAndSweep then KCM.Pipeline.DiscoverAndSweep('stand_up') end`. docs/data-flow.md and docs/ARCHITECTURE.md (the stand-up description): name the discovery pass.",
   "tests": "Red first, in tests/test_disabled.lua: 'Disabled 9c: an item looted while disabled is discovered on the way back up'. Disable, add a non-seeded consumable to the mock bags, re-enable, and fire no BAG_UPDATE_DELAYED. Assert the item is in the category's discovered set, or is a candidate for Selector.PickBestForCategory. It is red before the fix. Falsification comment: 'red under: standUp without DiscoverAndSweep'. Add 1 case. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md)",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-08",
   "repo": "ConsumableMaster",
   "title": "Bare /cm bar toggles the stored flag, not the latch-inclusive IsEnabled",
   "change": "core/SlashCommands.lua runBar (:904): `local on = not (KCM.MacroBarModel.Config() or {}).enabled`. Add a comment saying that IsEnabled answers false under any hold, including a perf capture's suspended arm (core/MacroBarModel.lua:146-150), and that the toggle is about the player's own switch.",
   "tests": "Red first, in tests/test_slash.lua or tests/test_perfsetup.lua: 'bare /cm bar toggles the stored flag during a perf hold'. With macroBar.enabled = true, take the perf suspended hold the way tests/test_perfsetup.lua and test_disabled 10c take it. Dispatch 'bar', then assert KCM.db.profile.macroBar.enabled == false and a printed 'OFF'. Dispatch again and assert true. It is red before the fix: today it always writes true. Add 1 case. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md)",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-09",
   "repo": "ConsumableMaster",
   "title": "Unlocking a switched-off bar says the bar is off; the launcher reuses RunLock and adopts the Launcher-2 isEnabled/disabledLine gate",
   "change": "(a) core/SlashCommands.lua runLock (:862-871): keep the write, then read cfg. When `locked == false and cfg.enabled == false`, say 'macro bar unlocked (the bar is off \\226\\128\\148 /cm bar on to show it)'. Otherwise print today's wording. Route both strings through L if the file's other bar strings are, and update tests/test_locale.lua's registry. (b) core/LauncherSetup.lua: adopt the LibKa0s-Launcher-1.0 minor 2 descriptor gate (LK-16). Add `isEnabled = function() return not (KCM.IsAddonDisabled and KCM.IsAddonDisabled()) end` and `disabledLine = function() local Sl = KCM.SlashCommands and KCM.SlashCommands.instance; return Sl and Sl:DisabledLine() end`. Delete the onClick's own disabled branch (:175-179), whose comment moves to the descriptor fields. The onClick body becomes `local cfg = KCM.MacroBarModel and KCM.MacroBarModel.Config(); if not cfg then return KCM.Say('macro bar unavailable.') end; KCM.SlashCommands.Verbs.RunLock(not cfg.locked)`. The duplicated wording at :185-187 goes away. Check that the library prints NO_BROKER/NO_ICON once and untagged (LK-16) and that nothing host-side re-tags them.",
   "tests": "Red first. (1) tests/test_slash.lua: '/cm unlock on a switched-off bar says the bar is off' asserts the new wording, and '/cm bar unlock' says the same. (2) tests/test_launcher.lua: 'left-click unlock on a switched-off bar reuses RunLock's wording' asserts the same line. Both are red before the fix. (3) test_disabled 'Disabled 8' (:401) must stay green unchanged. The refusal is now the library's gate: left-click while disabled prints DisabledLine once and writes nothing. That is the characterization of the gate's move. Add 2-3 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n 'drag it, then /cm lock' -- core/LauncherSetup.lua",
   "smoke": "In client: /cm bar off, then left-click the minimap button. It says unlocked and that the bar is off. /cm disable, then left-click: the one disabled line, and nothing unlocks.",
   "finding_ids": [
    "ConsumableMaster-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "LK-16"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-10",
   "repo": "ConsumableMaster",
   "title": "/cm enable and /cm disable print one line: drop the row's 'Master enable ON|OFF' say",
   "change": "settings/General.lua enabled row onChange (:247-259): delete the `local state = ...` and `KCM.Say(\"Master enable \" .. state)` lines. Keep the KCM.OnEnabledChanged call and its comment. The canonical echo stays settings/Slash.lua's echoEnabled (Sl:CliGet, `enabled = true|false`). The checkbox path then prints nothing, which matches every other row. tests/test_locale.lua:267: remove the 'Master enable ' registry pin in the same commit.",
   "tests": "Red first, in tests/test_slash.lua: '/cm disable prints exactly one line, the enabled echo'. Capture printed lines around KCM:OnSlashCommand('disable') and assert the count == 1 and that the line contains 'enabled'. Do the same for 'enable'. Red before: two lines. Add 2 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n 'Master enable ' -- settings tests",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-11",
   "repo": "ConsumableMaster",
   "title": "One global reset act behind both doors: combat refusal inside ResetAllToDefaults, one popup, truthful help and tooltip",
   "change": "(a) core/ConsumableMaster.lua KCM.ResetAllToDefaults (:571-580): refuse under InCombatLockdown with `return false, 'combat'` before any write. 'DB not ready' answers `false, 'db'`. On success, call `H.RefreshAllPanels()` when H is present, so both doors refresh. Rewrite the doc comment at :550-570 so the 'cannot diverge' claim is true. (b) Collapse to one popup. Keep StaticPopupDialogs.KCM_CONFIRM_RESET (core/SlashCommands.lua:41-61). Its OnAccept switches on the second return: success says 'Reset complete \u2014 defaults restored.', 'combat' says 'in combat \u2014 reset deferred until regen.' (the page's inCombatNotice wording), and 'db' says 'Reset failed (DB not ready).'. settings/General.lua: delete StaticPopupDialogs.KCM_RESET_ALL (:146-159) and doResetAll (:107-118). Point the Master controls onResetAll (:242) at StaticPopup_Show('KCM_CONFIRM_RESET'). Fix any comment that names KCM_RESET_ALL, including settings/OptionsSetup.lua:227-229. (c) settings/Slash.lua:231 resetall help -> 'Reset this profile to the addon's defaults \u2014 every setting and list (asks first)'. (d) settings/General.lua:367 'Reset all priorities' tooltip: '...for the whole-profile reset, use Reset all settings on the Master controls tab.' (e) Update tests/test_locale.lua's pins for the changed strings and the removed popup text, and docs/slash-dispatch.md and docs/settings-panel.md where they describe the two popups.",
   "tests": "Red first. (1) tests/test_slash.lua: '/cm resetall confirmed in combat refuses and writes nothing'. Stage a non-default profile value, setCombat(true), then StaticPopupDialogs.KCM_CONFIRM_RESET.OnAccept(). Assert the value is unchanged and the combat line printed. Red before: the slash door resets in combat. (2) tests/test_settingsui.lua: 'the panel's Reset all settings raises the same popup as /cm resetall', asserting that StaticPopup_Show was called with 'KCM_CONFIRM_RESET' and that StaticPopupDialogs.KCM_RESET_ALL == nil. (3) '/cm resetall help names a whole-profile reset': read the COMMANDS row and assert the new text. (4) A tooltip-text case for R-09, if test_settingsui pins tooltips; otherwise the locale registry pin covers it. About +3 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n 'KCM_RESET_ALL\\|Reset all settings above' -- core settings",
   "smoke": "In client, in combat: /cm resetall and accept, then the panel's Reset all settings and accept. Both print the combat line and change nothing. Out of combat, both reset the profile and the open panel repaints.",
   "finding_ids": [
    "ConsumableMaster-R-08",
    "ConsumableMaster-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-12",
   "repo": "ConsumableMaster",
   "title": "The disabled suite's two assertions that cannot fail",
   "change": "tests/test_disabled.lua:292: replace `t.truthy((ran or 0) > 0 or true, ...)` with `t.truthy((ran or 0) > 0, \"the unconditional dispatch reaches OnPlayerEnteringWorld\")`. KCM carries that handler, so 0 is not a legitimate answer. :378: delete `t.truthy(true, \"this addon refuses its feature verbs\")` and keep its comment block as prose above the preceding assertion.",
   "tests": "Before the edit, plant the violation. Temporarily make mock.base.__fireUnconditional answer 0 and show that the new :292 goes red. Revert. Case count is unchanged.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! grep -nE 'or true,|truthy\\(true' tests/test_disabled.lua",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-13",
   "repo": "ConsumableMaster",
   "title": "GetItemInfo through KCM.Compat (C_Item first, guarded global), drop the unread subType, reconcile the compat register row",
   "change": "(a) core/Compat.lua: add `function Compat.GetItemInfo(item)`. It returns C_Item.GetItemInfo(item) when present, else _G.GetItemInfo(item) when present, else nil, with a comment in the file's existing style (anti-pattern #10). (b) modules/Ranker.lua itemFields (:83-96): call `KCM.Compat.GetItemInfo(itemID)` and read only quality (3rd) and ilvl (4th). Drop subType from the fetch, the cache table and the return, so the function returns `quality, ilvl, tt`. Update every caller: `local quality, ilvl, _, tt = itemFields(...)` becomes `local quality, ilvl, tt = itemFields(...)` at :235, 244, 253, 262, 275, 281, 287, 293, 303, 314 and :582. :271, :299 and :322 are unchanged. (c) core/TooltipCache.lua:459 -> `KCM.Compat.GetItemInfo(itemID)`. (d) modules/KCMItemRow.lua:96 (the fallback rung after C_Item.GetItemNameByID) and :139 (the link lookup in craftingQualityAtlas) -> KCM.Compat.GetItemInfo. (e) tests/wow_mock.lua's C_Item block (:859): add C_Item.GetItemInfo answering the same fields the mock's global does, if missing. (f) docs/ARCHITECTURE.md Documented deviations, the `compat` row (:415). GetItemInfo is now owned by core/Compat.lua, so the row's GetItemInfo half is resolved. Narrow the row to what is still direct, which is _G.GetItemCount at modules/KCMItemRow.lua:232 (and core/MacroDisplay.lua:76's fallback rung), and list every remaining direct site. If the owner prefers, also route GetItemCount and retire the row with a prose retirement line. Record the choice in the commit body. (g) .luacheckrc: remove GetItemInfo from read_globals if nothing reads the bare global any more.",
   "tests": "Test first, in tests/test_compat.lua: 'Compat.GetItemInfo prefers C_Item', which spies both and asserts C_Item was called and the global was not. Add 'falls back to the global when C_Item.GetItemInfo is absent', and 'answers nil with neither'. tests/test_ranker.lua: characterize before the edit that the scores for three seeded items are unchanged. That run is the regression guard for the destructure change. Add about 3 cases. Regenerate test-cases.md and the badge. Perf: run tests/perf.lua before and after and compare the offline recompute B/iter against the 2551.1 baseline the review recorded. It must not rise by more than noise, so record both numbers in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -nE '(^|[^.A-Za-z_])GetItemInfo\\(' -- modules core | grep -v 'core/Compat.lua' && ! git grep -n subType -- modules/Ranker.lua",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-05",
    "ConsumableMaster-A-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-14",
   "repo": "ConsumableMaster",
   "title": "Tell the truth about panel hydration while disabled: fix the comment, add a Known Limitation, file the LoadItem follow-up",
   "change": "(a) core/ConsumableMaster.lua:213-218: rewrite the comment. While the addon is disabled, the stand-down drops GET_ITEM_INFO_RECEIVED (core/LifecycleSetup.lua:92 UnregisterAllEvents) and the options bus target's PANEL_REFRESH (settings/OptionsShim.lua), so item rows in a panel opened in a fresh session while disabled stay [Loading] until re-enable. On re-enable, the recompute comes from standUp's RequestRecompute (core/LifecycleSetup.lua). Delete the dead pointer to a toggle onChange in settings/Panel.lua. (b) docs/ARCHITECTURE.md ## Known Limitations: add a bullet with the same facts, citing slash-commands-\u00a77 'Recorded, not ruled' for the panel-refresh question. (c) File the owner-decision follow-up: `gh issue create -R tusharsaxena/ConsumableMaster --title \"Hydrate item rows in a settings panel opened while disabled (LibKa0s-Item LoadItem)\" --label enhancement --label state:untriaged --label severity:low`. The body cites core/ItemSetup.lua:14-16, which declines LoadItem today, modules/KCMItemRow.lua:84-99 and this review finding. Put the issue number in the Known Limitations bullet. Only one gh write, so no spacing is needed.",
   "tests": "none, because the item is a comment, docs and an issue. Behavior is unchanged and today's disabled suite already pins the stand-down that causes it.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && gh issue list -R tusharsaxena/ConsumableMaster --search 'LoadItem in:title' --json number,title",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-15",
   "repo": "ConsumableMaster",
   "title": "Every event registration through LibKa0s-Core SafeRegisterEvent, with a KCM.EVENTS list and a player-reachable rejected list",
   "change": "Implements LK-11 for this addon (events-frames-taint-\u00a71 per WS-04). (a) core/CoreSetup.lua. Live arm: `KCM.SafeRegisterEvent = lib.SafeRegisterEvent`. Degraded arm: a one-rung body, `function KCM.SafeRegisterEvent(target, event, handler, rejected) local ok = pcall(target.RegisterEvent, target, event, handler); if not ok and rejected then <append once> end; return ok end`, with no front gate, as the Core version-8 doc's Degradation note prescribes. (b) core/ConsumableMaster.lua. Declare `KCM.EVENTS = { {\"PLAYER_ENTERING_WORLD\",\"OnPlayerEnteringWorld\"}, ... }` once, holding the nine pairs from :708-716, and `KCM.RejectedEvents = KCM.RejectedEvents or {}`. KCM:OnEnable loops `KCM.SafeRegisterEvent(self, e[1], e[2], KCM.RejectedEvents)`. Keep the IsStoodDown guard and the comment block, and add that one retired name can no longer deafen the rest. (c) core/LifecycleSetup.lua:99 goes through KCM.SafeRegisterEvent with the same list. (d) settings/Panel.lua's bootstrap frame (:1304-1305): register both events through KCM.SafeRegisterEvent(bootstrap, name, nil, KCM.RejectedEvents). (e) Surface the list. core/DebugLogSetup.lua initSummary appends `, rejected events: A, B` when the list is non-empty. core/SlashDump.lua gains a DUMP_TARGETS.events entry plus a DUMP_ORDER row: `/cm dump events` prints each KCM.EVENTS name with registered/rejected. (f) tests/test_surface_parity.lua CORE_SEAM: add \"SafeRegisterEvent\". (g) tests/test_disabled.lua 'Disabled 1' (:148): derive the expected set from KCM.EVENTS rather than a literal list. (h) docs/midnight-quirks.md: a note on the trade-off, where a rejected name is recorded and skipped instead of raising. docs/ARCHITECTURE.md ## Event Subscriptions: name the helper, KCM.EVENTS and the dump target. docs/slash-dispatch.md: the new dump target.",
   "tests": "Red first. (1) tests/test_events.lua: 'a retired event name leaves the other eight bound'. Set mock.base.__badEvents = { LEARNED_SPELL_IN_SKILL_LINE = true }, run KCM:OnEnable(), then assert that the eight other names are registered (the kit's registration record), that KCM.RejectedEvents == {'LEARNED_SPELL_IN_SKILL_LINE'}, and that no error was raised. Red before: the bare call raises at the sixth name, and the three after it never register. Falsification comment: 'red under: bare self:RegisterEvent in OnEnable'. (2) The same with C_EventUtils = nil, the older-client arm, which exercises the pcall rung. (3) C_EventUtils.IsEventValid answering false rejects without a call. (4) A second OnEnable (stand-up) does not duplicate a rejected name. (5) The degraded Core stub's SafeRegisterEvent records a raising name, via loadPureDegraded. (6) /cm dump events lists the rejected name. About +6 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -nE ':RegisterEvent\\(' -- core modules settings | grep -v SafeRegisterEvent",
   "smoke": "In client: /cm dump events lists nine events, all registered. /cm debug on then /reload: the [Init] line shows no rejected events.",
   "finding_ids": [
    "ConsumableMaster-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "LK-11",
    "LK-04",
    "WS-04"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-16",
   "repo": "ConsumableMaster",
   "title": "Migration stamps per WS-03: defaults declare schemaVersion 0, the runner owns the stamp and advances it only past a step that returned",
   "change": "(a) defaults/Profile.lua:31: `schemaVersion = 0`. Rewrite the comment at :24-30 per savedvariables-\u00a71 (v2.65.0). AceDB's removeDefaults strips a stored value equal to its default, so a default equal to a real version can mask a migration. AceDB backfills a declared default onto a legacy store. 0 has neither problem. (b) core/Database.lua RunMigrations (:163-184): `g.schemaVersion = g.schemaVersion or 0`. migrateProfile (:124-142) keeps `p.schemaVersion or 1`, because a profile with no stamp is a pre-stamp v1 profile and that is the documented one-time cost. Its steps keep the stamp-after-each-step order. Make the invariant explicit: the stamp line sits after each step call, so a raising step leaves the stamp at the last completed version. Add a comment that per-profile steps run on every profile through the OnProfileChanged hook and the per-profile stamp, and are never gated on the account stamp alone. That is WS-03's second allowed shape. Update the file header's two-stamps paragraph. (c) defaults/README.md:17 and docs/profiles.md: schemaVersion 0 and why. (d) core/DebugLogSetup.lua initSummary is unchanged.",
   "tests": "Red first, in tests/test_database.lua. (1) 'defaults declare global.schemaVersion = 0' asserts KCM.dbDefaults.global.schemaVersion == 0. Red today, when it is 1. (2) Characterization, green before and after: 'a raising profile step leaves the profile stamp at the last completed version and the account stamp unmoved'. Stub D.MigrateLabelFlagsV3 to error on a v1 profile, pcall D.RunMigrations, then assert p.schemaVersion == 2 and g.schemaVersion unchanged. (3) Characterization: 'every step is idempotent against a fresh default profile'. Run migrateProfile's steps twice on a deep copy of dbDefaults.profile and assert it equals itself. (4) Re-check that the multi-profile case from the 2026-09-07 cycle (M2-06) is still present and green, and name it in the commit body. Adjust :13-46 only where a case asserts the old default of 1. About +3 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && grep -n 'schemaVersion = 0' defaults/Profile.lua",
   "smoke": "In client, on a copy of the SavedVariables: log in and log out, then confirm that ConsumableMasterDB.global.schemaVersion persisted as 3 and that each profile carries schemaVersion = 3.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "WS-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-17",
   "repo": "ConsumableMaster",
   "title": "ConsumableMaster#39: adopt LibKa0s-Schema-1.0 (minor 2) for the settings write seam",
   "change": "Built on LK-22 (normalize, SetMany, announceBatch) and LK-17 (CliSet prints the seam's refusal). Read docs/api/Schema/version-2-docs.md first. (1) NEW settings/SchemaStub.lua, loaded in the TOC immediately before settings\\OptionsSetup.lua. Add the line only, with no other line moves, and comment it. It is the library-absent stub the Schema doc's 'The degradation stub' prescribes: a trimmed copy of LibKa0s tests/test_schema.lua's referenceStub, write-completing and log-silent, with Get, Set, normalize, SetMany (all-or-nothing, no log), ApplyDefault with the bracket-gated resetExempt, a depth-only BulkBegin/End/Run, and the SplitPath/Read/Write/SameValue primitives. writeThrough is empty (see CM-18). Add a header comment naming the doc section and the deliberate duplication. Publish it as KCM.SchemaStub. (2) settings/Panel.lua. Build `local SchemaLib = LibStub and LibStub(\"LibKa0s-Schema-1.0\", true) or KCM.SchemaStub` and `local S = SchemaLib:New({ rows = KCM.Settings.Schema, resolveRoot = function() return KCM.db and KCM.db.profile, 1 end, announce = announce, announceBatch = announceBatch, debug = function(tag, fmt, ...) if KCM.Debug then KCM.Debug(tag, fmt, ...) end end, debugEnabled = function() return KCM.State and KCM.State.debug end, format = function(_, v) return logValue(v) end, resetExempt = <set built from rows flagged neverReset> })`. Publish it as Helpers.schema. (a) Split VALIDATORS (:885-951) into TYPE_RULES[type] = { validate = fn(value) -> ok, why; normalize = fn(value) -> value | nil, why }. bool, string and enum are validate-only. number clamps in normalize. color, order, flag-map and plain map copy in normalize. A row that already carries its own `normalize` (stat priority's map) keeps it: that is LK-22's row.normalize signature. H.RegisterRows and the composed-block stamping put validate and normalize on each row by type, closed over the row, and call S.AddRows/S.Reindex. Delete validateSchemaValue, or keep it as a thin read of TYPE_RULES if the CLI coercion uses it. (b) Host reactions move off Schema's onChange. Rows carry the host field `apply` (General, MacroBar, StatPriority and Category decorate maps plus MacroBar.lua's row()/block() helpers at :46-67: `r.apply = r.apply or applyBar`). announce(row, path, value) runs `row.apply` through the existing fireOnChange pcall reporter, then RefreshScalars. announceBatch(writes) runs, through the same reporter, either the batch caller's reactor or each DISTINCT row.apply once in first-seen order (today's runBatchReactors), then RefreshAllPanels if the batch is structural, else RefreshScalars. So a 60-row page reset is still one applyBar and a raising reaction is still reported, not propagated. Before this, grep libs/LibKa0s for any direct call of row.onChange on a path row. If one exists, keep `onChange = apply` on those rows and say so. (c) The diversions become rows. state.debugConsole gets `sessionOnly = true` plus get/set (today's SESSION_PATHS bodies), and global.minimap.hide gets get/set (today's GLOBAL_PATHS bodies, inversion unchanged), both stamped through General.lua's decorate map. Delete SESSION_PATHS, GLOBAL_PATHS, divertedPath, Helpers.Resolve, sameValue, the bulk frame chain (:303-393) and fireOnChange's duplicate. (d) Rebind the seam. Helpers.Get = S.Get. Helpers.Set = S.Set. Helpers.SetAndRefresh(path, v) = `local ok, err, why = S.Set(path, v); if not ok then KCM.Say(('invalid value for %s: %s'):format(path, tostring(why or err))); return false end; return true`. Helpers.SetManyAndRefresh(entries, opts) sets a file-local batchCtx = { onChange = opts.onChange, structural = opts.structural }, calls S.SetMany(entries, { act = opts.bulk and opts.bulk.act, scope = opts.bulk and opts.bulk.scope }), clears batchCtx in every exit, and returns ok. KCM.Schema:Set/SetMany keep their signatures, so the three batch call sites (settings/MacroBar.lua:652, :810, settings/Category.lua:272) do not move. Helpers.FindSchema = S.FindRow. Helpers.Bulk(act, scope, fn) = S.BulkRun(act, scope, function() fn() end). Keep its return only if a caller reads it; grep shows none. Helpers.MuteSetLog(fn) = S.BulkRun('reset', 'profile', function(info) info.profileReset = true; fn() end). Helpers.SilenceOpenBulk: the profile handler (core/ConsumableMaster.lua:473) calls S.ConsumeResetCount() instead, which marks the open bracket as a profile reset, and KCM.ResetAllToDefaults wraps db:ResetProfile in S.ResetCounted so the handler's line carries the count. core/ConsumableMaster.lua:544 restoreSessionRows writes through S.ApplyDefault(row). (3) settings/OptionsSetup.lua descriptor: `get = function(p) return Helpers.Get(p) end` stays, and `set` stays SetAndRefresh because the panel needs the printed refusal. (4) settings/Slash.lua Slash descriptor: set = S.Set directly, so LK-17's CliSet prints INVALID plus why once and the host does not print a second line. findRow = S.FindRow. Point applyDefault (:530-533) at S.ApplyDefault. (5) tests/test_surface_parity.lua: `h.assertSurfaceParity(liveInstance, stubInstance, 'schema instance vs host stub')` and `h.assertSurfaceParity(KCM.SchemaStub, 'LibKa0s-Schema-1.0', {'STRINGS'})`. Register the by-name surface source if the runner uses a table map. (6) Docs. docs/schema.md: rewrite the write-seam section (pipeline, normalize, apply vs onChange, SetMany, the stub). docs/ARCHITECTURE.md:279 Options cell and the adoption table: add the Schema-1.0 row. docs/revendor/2026-09-23-v1.55.0/03_DECISIONS.md is frozen and is not edited. The commit body says 'Closes tusharsaxena/ConsumableMaster#39' and restates the adoption's behavior changes: unknown path refused, tables copied into the store, the log line before the reaction.",
   "tests": "Test-first. Characterize before any seam code moves, in a NEW tests/test_schema_adoption.lua, because tests/test_schema.lua is at 1023 and must not grow into the band. Wire it in tests/run.lua SUITES. Pin today's observable behavior through the public seams, where each case passes on the old seam and must still pass on the new one. (a) SetAndRefresh stores a coerced number (a clamp). (b) An enum refusal prints 'invalid value for <path>: allowed values: ...'. (c) An unknown path is refused and stores nothing. (d) A table value is stored as a copy, for color, order and map. (e) A Macro Bar page Defaults reset runs applyBar exactly once and logs exactly one `[Set] reset Macro Bar page: N rows`, with N the changed rows. (f) resetCompositeCategory logs `[Set] reset category KCM_HP_AIO: N rows` and recomputes once. (g) /cm resetall logs only the profile handler's line. (h) The debugConsole row shows and hides the window and is skipped by the global reset. (i) The minimap row inverts and survives both resets. (j) A raising apply is reported by 'onChange for X failed' and the write persists. (k) The degraded build (loadFullAddon(true)) still writes through a host verb that has a row, and prints no [Set] line. Commit these green on the old seam as the first half of the work. The Schema cutover follows, with the suite still green. New red-first cases: (l) row.normalize refusal ('nil, why') reaches the CLI as the library's INVALID line exactly once. It is red before (4). (m) The surface-parity pair in (5). Run tests/perf.lua before and after, since the adoption notes require it for a host with a measured read path, and record both in the commit body. About +14 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -nE 'SESSION_PATHS|GLOBAL_PATHS|divertedPath|Helpers.Resolve\\b|runFrame' -- settings core && wc -l settings/Panel.lua settings/SchemaStub.lua tests/test_schema_adoption.lua (all under 1500; record Panel.lua's new count for CM-30)",
   "smoke": "In client, on each page (General, Macro Bar, Stat Priority, Macros): change a row and press Defaults. The page repaints once and the bar re-applies. With /cm debug on, a page reset shows exactly one [Set] line. Then /cm set macroBar.<number> 9999, which clamps; /cm set on an enum row with a bad value, which shows one refusal; and /cm resetall, which shows one line.",
   "finding_ids": [],
   "issue_refs": [
    "ConsumableMaster#39"
   ],
   "depends_on": [
    "RV-CM",
    "CM-03",
    "CM-11",
    "LK-22",
    "LK-17"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "CM-18",
   "repo": "ConsumableMaster",
   "title": "Degraded (library-absent) composed-row verbs print the library-absent line, never a Lua error or a false success; pin the Slash-stub line with Kit.assertLibraryConstant",
   "change": "Implements WS-02 route (b) and LK-18's stub shape. On a library-absent load, the MasterControls and macro-bar composed rows do not exist (composers answer {}), and there is no Lifecycle latch (core/LifecycleSetup.lua:22 returns early). So a written `enabled` would not be obeyed this session, and route (a) writeThrough would acknowledge a switch nothing honors. (a) locales/enUS.lua: add L[\"%s is unavailable: the LibKa0s library did not load.\"], a single sentence with one placeholder (slash-commands-\u00a71). (b) settings/Slash.lua: add a local refuseLibraryAbsent(verb), which says that line with '/cm ' .. verb. setEnabled (:109-119) replaces 'settings unavailable.' with refuseLibraryAbsent(on and 'enable' or 'disable') when the row is absent. Rewrite the comment at :93-101 per WS-02. (c) core/SlashCommands.lua. KCM.MacroBar.SetEnabled and SetLocked return the seam's verdict. The BAR_COMMANDS on/off (:873-876), bare `bar` (:904) and runLock print their success line only when the write landed. On a false with a library-absent build, detected through the flag settings/OptionsSetup.lua publishes (KCM.Settings.optionsUI == nil), they print the library-absent line for their verb. Otherwise they print the seam's refusal. So no false 'macro bar ON'. (d) docs/ARCHITECTURE.md Documented deviations: a row keyed `options-ui-\u00a71` (the WS-02 composed-row ruling). What differs: /cm enable and /cm disable take route (b), refusing with the library-absent line, instead of writeThrough. Why: a library-absent build has no stand-down latch, so a stored switch would not take effect this session. Decided <date>. Re-check trigger: a library-absent latch stub, or a runtime reader of `enabled` outside the latch. (e) LK-18 pin. `git grep DISABLED_LINE_FORMAT -- core settings` proves that no host copy of the disabled refusal exists: the degraded arm has no latch, so it has no disabled state to refuse. State that in a test comment. Pin the one host copy that does exist, the locale string for the library-absent line, with `h.assertLibraryConstant(L[...], \"LibKa0s-Slash-1.0\", <member path the Slash version-15 doc's '## The degradation stub' section names for the line>)`. If the doc publishes no such member, assert the string byte for byte against WS-02's text and note it in the commit body. Also pin the live refusal: `h.assertLibraryConstant` on the format the live Sl:DisabledLine() is built from, 'DISABLED_LINE_FORMAT'.",
   "tests": "Red first, in tests/test_slash.lua's degraded block (loadFullAddon(true)). For each of 'enable', 'disable', 'bar', 'bar on', 'bar off', 'lock', 'unlock': run `local ok, err = pcall(KCM.OnSlashCommand, KCM, verb)`, then assert ok (no Lua error), exactly one printed line equal to the library-absent line for that verb, and an unchanged store. Red before: enable prints 'settings unavailable.' and 'bar on' prints 'macro bar ON' over a refused write. Falsification comment: 'red under: printing the success line without checking SetEnabled's verdict'. Add the assertLibraryConstant case(s) from (e). About +8 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n 'settings unavailable' -- settings core",
   "smoke": "In client, on a test install with libs/LibKa0s renamed away: /cm enable, /cm bar on and /cm lock each print one '... is unavailable: the LibKa0s library did not load.' line, and no Lua error appears with BugSack enabled.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17",
    "WS-02",
    "LK-18"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-19",
   "repo": "ConsumableMaster",
   "title": "Minimap row CLI path reads in its shown sense: global.minimap.hide -> global.minimap.shown (storage unchanged)",
   "change": "WS-06 (4). Storage stays LibDBIcon's db.global.minimap.hide, and no SavedVariables change is made. settings/General.lua:51: MINIMAP_PATH = \"global.minimap.shown\", and rewrite the comment above it. After CM-17 the row carries get/set closures that already invert onto minimap.hide, so only the path changes. Also update the resetExempt set built from neverReset. Rewrite the comments that name the old path: settings/General.lua:280, settings/OptionsSetup.lua:109, core/LauncherSetup.lua:66, defaults/Profile.lua:48, and settings/Panel.lua's (now moved) inversion comment. Docs: docs/settings-panel.md:172, :178 and :210 and docs/ARCHITECTURE.md:279 give the path as global.minimap.shown and say the store key is still `hide` (anti-pattern #81). Tests: tests/test_launcher.lua:216-363 and tests/test_schema.lua:602/:617 switch to the new path. Their inversion assertions against KCM.db.global.minimap.hide stay exactly as they are.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first: in tests/test_launcher.lua, before the rename, add '/cm get global.minimap.shown answers true while hide is false' and '/cm set global.minimap.shown false writes hide = true'. Both are red while the path is .hide. Then rename. The existing inversion cases move to the new path with their assertions unchanged. Add 2 cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n 'global.minimap.hide' -- core settings defaults tests/*.lua docs/settings-panel.md docs/ARCHITECTURE.md",
   "smoke": "In client: /cm get global.minimap.shown answers true with the button visible. /cm set global.minimap.shown false hides the button and it stays hidden after /reload.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17",
    "WS-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-20",
   "repo": "ConsumableMaster",
   "title": "Adopt RenderTabbedSchema (opts.tabs) on the General and Macro Bar pages; decline for Stat Priority and Macros by issue; delete GENERAL_TABS and the duplicated strip wrap-invariance cases",
   "change": "LK-28 evaluation. (a) settings/General.lua (:325-430). render becomes `H.ResetScroll(ctx); H.RenderTabbedSchema(ctx, <General page args per the Options version-24.31.4.7.4 doc>, ..., { tabs = { { key = \"Maintenance\", label = L[\"Maintenance\"], render = drawMaintenance } } })`, with Master controls first as the row group (options-ui-\u00a715). Delete TABS, `KCM.Settings.GENERAL_TABS = TABS` (:383; ConsumableMaster-R-16: no reader), activeTab and the hand-built strip. Rewrite the comment at :329-334. Keep O.SetMacroTab (settings/Category.lua:1106) as a deliberate test seam, with a one-line comment saying so (R-16 informational half). (b) settings/MacroBar.lua (:1100-1150). Wrap as `render = function(ctx) cancelReorder(ctx); H.RenderTabbedSchema(ctx, ..., { tabs = { {key='General', render=drawGeneral, before='Layout'}, {key='Buttons', render=drawButtons} } }) end`. cancelReorder still runs before the library's ResetScroll. The six row groups come from the schema. KCM.Settings.MACROBAR_TABS: keep it only if a suite reads it. Otherwise delete it and point the suite at the library's ctx.activeTab. If the library's render order cannot host cancelReorder before its ResetScroll, do not adopt on this page and add it to the issue in (c). (c) File one decline issue, `gh issue create -R tusharsaxena/ConsumableMaster --title \"RenderTabbedSchema not adopted on the Stat Priority and Macros pages\" --label state:will-not-do --label severity:low`. Stat Priority (settings/StatPriority.lua:702-740) is one bespoke list tab under a picker PageBanner, with no schema rows. The Macros page (settings/Category.lua:1082) has one tab per category, generated from KCM.Categories, each drawing a reorder list and not rows. Record the re-check trigger 'either page gains schema rows'. (d) WS-06 (3): the strips on all four pages are library-drawn (O.TabStrip / RenderTabbedSchema), so tests/test_settingsui_optionsui.lua's '\u00a713 wrapped-strip geometry' block duplicates the library's wrap-invariance pin (testing-\u00a78). Delete that block's cases and keep only host-specific strip assertions (tab order, labels, first tab). (e) docs/settings-panel.md: the page table and the tab trees for General and Macro Bar.",
   "tests": "Characterize first, green before and after, in tests/test_settingsui.lua. For General: the rendered tab keys, in order, are {'Master controls','Maintenance'}; Master controls renders the canonical rows; Maintenance renders the three buttons. For Macro Bar: the eight tab keys in today's order, and each row group renders the same row paths it renders today. Then adopt. Delete the duplicated wrap-geometry cases (count them in the commit body). Net case delta = the new characterization cases minus the deleted geometry cases. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! git grep -n GENERAL_TABS -- core settings tests && gh issue list -R tusharsaxena/ConsumableMaster --search 'RenderTabbedSchema in:title' --state all --json number",
   "smoke": "In client, at the narrowest settings width: open General and Macro Bar and switch every tab. The strip wraps with no overlap, Master controls is first, Maintenance's three buttons work, the Macro Bar's Buttons tab drag-reorder still works, and a drag in progress is canceled on a tab switch.",
   "finding_ids": [
    "ConsumableMaster-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17",
    "LK-28",
    "WS-06"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-21",
   "repo": "ConsumableMaster",
   "title": "About-page logo path derived from the addon folder name",
   "change": "settings/Panel.lua:148: `local LOGO_TEXTURE = ('Interface\\\\AddOns\\\\%s\\\\media\\\\logos\\\\%s.logo.tga'):format(KCM.name, KCM.name:lower())`. NS.name = addonName is set at core/Namespace.lua:13. The file opens `local _, NS = ...` (:26), so there is no addonName local. Add a comment saying this matches core/LauncherSetup.lua:107-108, and that it is about consistency, not a layout-\u00a74 deviation.",
   "tests": "Red first, in tests/test_settingsui.lua: 'the About logo path follows the folder name'. Load with KCM.name = 'ConsumableMasterRenamed', render the main page, and assert the logo texture path contains 'ConsumableMasterRenamed\\\\media\\\\logos\\\\consumablemasterrenamed.logo.tga'. Red before. Add 1 case. Regenerate test-cases.md and the badge.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ! grep -n 'AddOns\\\\ConsumableMaster\\\\' settings/Panel.lua",
   "smoke": "In client: the About page still shows the logo.",
   "finding_ids": [
    "ConsumableMaster-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-22",
   "repo": "ConsumableMaster",
   "title": "The harness-ping bus literal is declared once",
   "change": "tests/test_harness.lua:68-78. Declare `local PING = \"Ka0s_ConsumableMaster_HarnessPing\"` once at the top of the case and use PING at all three sites (:73, :74, :77). This is architecture-\u00a74's once-per-repo rule for Ka0s_ literals.",
   "tests": "none: a test-only refactor with an unchanged case count. The case itself is the guard. Confirm it still passes, and that `git grep -c Ka0s_ConsumableMaster_HarnessPing` finds 1 hit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && test \"$(git grep -o Ka0s_ConsumableMaster_HarnessPing -- tests | wc -l)\" = 1",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-23",
   "repo": "ConsumableMaster",
   "title": "Correct stale comments and docs that describe removed behavior (non-TOC sites), including the /cm set macroBar.point claim",
   "change": "Correct each comment in place (anti-pattern #66 and documentation-\u00a75). (1) modules/MacroBar.lua:175 and docs/ARCHITECTURE.md:340. Position is set only by dragging or by a reset (/cm bar reset, Reset position), and /cm set cannot reach it because point/relPoint/x/y have no row. Leave docs/ARCHITECTURE.md:130, which is already correct (R-14). (2) defaults/Profile.lua:54: 'early-returns' becomes a stand-down. :107: frames are hidden, not torn down. :112: every scalar EXCEPT point, relPoint, x and y has a schema row. (3) core/ConsumableMaster.lua:367-392: delete the orphaned restoreProfileDefaults doc header, and keep the 'USED TO LIVE HERE' note only if it still points somewhere real. (4) settings/Slash.lua:25: KCM.Say is defined in core/CoreSetup.lua. :271-305: shrink the gate essay to what is true since the library owns the gate (:288-291 no longer covers both dispatch arms). (5) modules/Selector.lua:5 (TECHNICAL_DESIGN) and :604 (Core.lua): cite the real files. (6) settings/OptionsSetup.lua:159-165 against :231: one true statement, that the row makers ARE the library's since LIBKA0S-04. Any site this plan's earlier items already rewrote (CM-05, CM-17) is skipped. List every edited site in the commit body. Leave the TOC sites (:50, :76, :81) to CM-24.",
   "tests": "none, because the change is docs and comments only. The code-comment half makes no behavior change, and a luacheck plus a full run prove nothing moved. Case count is unchanged.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && ! git grep -nE 'macroBar\\.point\\|x\\|y|restoreProfileDefaults|TECHNICAL_DESIGN' -- modules core settings defaults docs/ARCHITECTURE.md",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-14",
    "ConsumableMaster-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-24",
   "repo": "ConsumableMaster",
   "title": "TOC and load-order statements: LifecycleSetup and LauncherSetup in the chains, no ordinals, and one 'conventional' note per group",
   "change": "Comment text only in the TOC, with no line moves (anti-pattern #66). (1) ConsumableMaster.toc:50-54: the LibKa0s comment names 'fifteen majors, the file list is LibKa0s.xml' rather than five modules. :76-77: 'It sits here, second' becomes 'It sits here, after core\\\\LifecycleSetup.lua'. :81: drop the retired suspend/resume pair and name the Lifecycle hold. (2) Conventional-group notes (A-16), one comment line per group: above locales\\\\enUS.lua (:57-58), above modules\\\\Ranker.lua (:166), and above settings\\\\General.lua (:202). Each says the position within the group is conventional and not load-bearing. (3) Docs: docs/ARCHITECTURE.md:324 and docs/module-map.md:598 chains become Namespace -> LifecycleSetup -> PerfSetup -> MediaSetup ..., with LauncherSetup added where it loads. docs/module-map.md:38 ('Loads SECOND'), :642 and docs/performance.md:46 say 'after core/LifecycleSetup.lua'. docs/ARCHITECTURE.md:418 (the retirement paragraph) cites the TOC comments by what they annotate, the PerfSetup and LifecycleSetup comments, not by ':51-57 and :77-79'.",
   "tests": "none, because the change is a TOC comment and docs only. tests/test_load.lua or the TOC-parity suite proves that the file list and order did not move. The full run must stay green with an unchanged case count.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(git show HEAD~1:ConsumableMaster.toc | grep -v '^#') <(grep -v '^#' ConsumableMaster.toc) && ! git grep -niE 'sits here, second|loads SECOND|sits second' -- ConsumableMaster.toc docs/*.md",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-12",
    "ConsumableMaster-A-16",
    "ConsumableMaster-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-25",
   "repo": "ConsumableMaster",
   "title": "README: remove the retired 'What's new in 1.6.2' section",
   "change": "README.md:35-38: delete '## What's new in 1.6.2' and its bullet. The same fact is the top Version History row (:137). Run the de-AI pass over the edited neighborhood, as documentation-\u00a71 (v2.42.0) requires for README edits. No version bump.",
   "tests": "none, because the change is docs only. The prose gate (tests/_kit/test_prose.lua) runs over README.md in the full run.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ! grep -n \"What's new\" README.md",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-26",
   "repo": "ConsumableMaster",
   "title": "ARCHITECTURE.md hub: retire the stale preview-mode row and bring the hub to about 420 lines",
   "change": "(1) docs/ARCHITECTURE.md Documented deviations: delete the `preview-mode` row (:416). Add a prose retirement line under the table next to the toc-file-\u00a75 note. v2.49.0 exempts an addon whose unlocked view is its preview and names Ka0s Consumable Master (preview-mode.md:10). The row's trigger fired at 87ed204 and was reversed at 9c45eff. (2) Trim to at most about 420 lines (documentation-\u00a73). Move 'What the peel did' (:455-504, the narrative and the Was/Is-now table) into docs/module-map.md as a short 'Peel history' section, and leave one sentence in the hub linking to it and to issues #32/#33. Move the LibKa0s adoption per-major table (:267-316) to docs/module-map.md, leaving a 3-4 line summary and a link. By this point the table includes the Schema and SafeRegisterEvent rows from CM-15/CM-17. Keep byte-identical the census table header, the '**No authored file in this repository is over the cap.**' sentence and the section text that tests/_kit/test_layout_cap.lua reads. Update the band paragraph (:447 region) figures with today's `wc -l`. (3) Fix any docs/*.md anchors that pointed at the moved sections.",
   "tests": "none, because the change is docs only. tests/_kit/test_layout_cap.lua (the census reader) and tests/test_docmap.lua must stay green, which proves the load-bearing sentence and links survived. Case count is unchanged.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && test $(wc -l < docs/ARCHITECTURE.md) -le 420 && ! grep -n '^| `preview-mode`' docs/ARCHITECTURE.md && grep -n 'No authored file in this repository is over the cap' docs/ARCHITECTURE.md",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-11",
    "ConsumableMaster-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-18",
    "CM-24"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-27",
   "repo": "ConsumableMaster",
   "title": "Expand bare \u00a7N citations to filename-\u00a7N, and respell the dash citations",
   "change": "Run the audit's E12 command (docs/audits/2026-09-23/03_EVIDENCE.md, E12) at HEAD. It found 46 bare \u00a7N in 22 files on 2026-09-23, and earlier items moved some. Expand each hit to its full filename-\u00a7N form: settings/General.lua:346 and :377 ('\u00a715' -> 'options-ui-\u00a715'), settings/Slash.lua:85 ('\u00a72' -> 'slash-commands-\u00a72'), tests/test_settingsui_optionsui.lua:5, 71, 721 and 723, and the rest per E12. Also respell the dash-numbered forms in tests/run.lua:495-501 ('localization-5', 'layout-1', 'testing-9') and any other authored `<section>-<N>` citation to the \u00a7 form, so they match kit 26's case names (LK-08). Leave smoke-tests.md's own \u00a7N step numbering and Selector.lua's TECHNICAL_DESIGN \u00a74 alone (the audit's exclusions), unless CM-23 already rewrote the latter. Comment and prose only.",
   "tests": "none, because the change is comments and docs only. The full run and luacheck prove that no code moved. Show the E12 count going to 0 in the commit body.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && <E12 command from docs/audits/2026-09-23/03_EVIDENCE.md> | wc -l  # 0 outside the two exclusions",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-26"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-28",
   "repo": "ConsumableMaster",
   "title": "Consolidated span bundle for the 27 unrecorded LibKa0s tags (v1.18.0-v1.53.0)",
   "change": "New docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.53.0/, the WS-01 span bundle (AUDIT.md's re-vendor check now reads every tag on 01_DELTA.md line 1 when the folder carries two tags, so no Documented deviations row is needed). (1) 01_DELTA.md. Line 1 is exactly `Delta: LibKa0s v1.17.x -> v1.53.0 (span: v1.18.0 v1.18.1 v1.19.0 v1.23.0 v1.24.0 v1.26.0 v1.27.0 v1.28.0 v1.29.0 v1.35.0 v1.36.0 v1.36.1 v1.36.2 v1.37.0 v1.38.0 v1.39.0 v1.42.0 v1.44.0 v1.45.0 v1.46.1 v1.47.0 v1.48.0 v1.48.1 v1.50.0 v1.51.0 v1.52.0 v1.53.0)`. Use WA-01's exact line-1 grammar, and read the base from `git log --format=%H -- libs/LibKa0s` plus CLAUDE.md at the first vendoring commit. Then, for each tag: the commit that vendored it (`git log --format='%h %ad %s' -- libs/LibKa0s` joined to the CLAUDE.md provenance line at that commit) and the sweep that carried it. Note that v1.25.0 and v1.30.0-v1.34.0 already have bundles and are not re-recorded. Note once that the frozen docs/revendor/2026-09-23-v1.55.0/ bundle names its base as v1.54.2, while this addon's previous vendored tag was v1.53.0. Per WA-01, the frozen bundle is not edited. (2) 05_SUMMARY.md: the adoptions made along the span, drawn from the commits themselves (e.g. Lifecycle, Bus, Launcher, DragHandle, Compat, IdInput), each with its commit hash. Docs only, and nothing under libs/ or tests/_kit/ is touched.\n\nPLAN-REVIEW CORRECTION: delete the \"misstated base\" note; the v1.54.2 kit-only re-vendor makes the frozen bundle correct.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "none, because the change is docs only (a record bundle). Verify with the WS-01 version of AUDIT.md's re-vendor check, below.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && run WowAddonStandards AUDIT.md's re-vendor check (v2.65.0 text, with --since=\"$horizon 00:00\" and the span-bundle loop) from the repo root; the unrecorded count prints 0",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "WS-01",
    "WA-01",
    "RV-CM"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "CM-29",
   "repo": "ConsumableMaster",
   "title": "Record the v1.56.0 in-client checks in docs/smoke-tests.md: ReorderList drags on the library ghost frame, and the Options park/replay",
   "change": "docs/smoke-tests.md. Add steps to the existing sections, not a new dated store. (1) LK-21 (Widgets minor 10: the drag poll moved onto the library ghost frame, and the drop line comes from a library pool). Drag-reorder on each of the three ReorderList sites: settings/Category.lua (a single category's priority list and a composite section order, :725 and :940), settings/StatPriority.lua:614 (the secondary-stat list), and settings/MacroBar.lua:1014 (the Buttons tab slot order). For each, check that the drop line appears in the list's color, that the drag commits, that Escape or a tab switch mid-drag cancels with no stray line, and that a row's own hover still works after a drag, which proves the host row frame's OnUpdate was not touched. Open two pages in turn and drag on both, which checks the pooled line is not shared. (2) LK-25: \u00a76a gains the disabled variant from CM-05 if CM-05 has not already written it. (3) Add a one-line index entry naming LibKa0s v1.56.0 as the payload these steps prove.",
   "tests": "none, because the change is docs only: in-client smoke steps with no headless-testable change in this repo. The library pins its own behavior.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'ReorderList\\|ghost' docs/smoke-tests.md",
   "smoke": "Run the new steps in client: all three ReorderList sites and \u00a76a enabled and disabled. Record pass or fail in the owner's smoke checklist.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "LK-21",
    "CM-05"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-30",
   "repo": "ConsumableMaster",
   "title": "Watch-list terminal states for settings/Panel.lua and settings/Category.lua: file the peel issues automated-tests-\u00a74 requires",
   "change": "automated-tests-\u00a74 refuses a fourth 'Accepted' for a file in the 1000-1500 band (RESULTS.md :141/:143 record 1.6.0, 1.6.1 and 1.6.2). Measure after CM-17 and CM-20 with `wc -l settings/Panel.lua settings/Category.lua`. For each file still at 1000 lines or more, file one issue naming the seam the next peel follows, spaced at least 5 s apart. `gh issue create -R tusharsaxena/ConsumableMaster --title \"Peel settings/Panel.lua: <seam>\" --label state:triaged --label severity:low`. The likely seam after the Schema adoption is the About/landing-page renderer (Helpers.BuildAboutContent, aboutLogo, aboutNotes, aboutSlashCommands) into settings/About.lua; confirm it from the file's section headings. `sleep 5`. Then `gh issue create -R tusharsaxena/ConsumableMaster --title \"Peel settings/Category.lua: <seam>\" ...`: the composite (AIO) section editor (renderComposite and its section helpers) into settings/CategoryComposite.lua, mirroring the CategoryAddByID peel. A file that dropped below 1000 needs no issue: record the measurement instead. docs/ARCHITECTURE.md's band paragraph names each file, its measured count and its issue number. CM-DOCS's release-style run then writes RESULTS.md's Disposition cells to point at these issues.",
   "tests": "none, because the change is issue filing and one docs paragraph, with no code change.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && wc -l settings/Panel.lua settings/Category.lua && gh issue list -R tusharsaxena/ConsumableMaster --search 'Peel settings in:title' --json number,title && grep -n 'issues/' docs/ARCHITECTURE.md",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-R-18"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-17",
    "CM-20"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "CM-DOCS",
   "repo": "ConsumableMaster",
   "title": "Sync docs, regenerate docs/test-cases.md, record a fresh automated-test bundle (no version bump, no tag)",
   "change": "(1) The /wow-addon:sync-docs equivalent: rewrite README.md, CLAUDE.md, DEPENDENCIES.md, docs/ARCHITECTURE.md and the docs/ set to match HEAD. That covers count claims (test totals, file counts, slash verbs including `/cm dump events`, the dump targets and the event list), the COMMANDS/help parity after CM-11, and the Schema-1.0 and SafeRegisterEvent adoptions in the LibKa0s adoption summary and module-map. Also the removed popup, stubs and seams (KCM_RESET_ALL, SESSION_PATHS/GLOBAL_PATHS, _settingsCategoryID, GENERAL_TABS) and the kit revision. Any prose that says kit revision 25 or LibKa0s v1.55.0 where it describes the current payload becomes 26 / v1.56.0, for example tests/run.lua's 'kit revision 25 resolves' comment and docs/testing.md. Frozen bundles and released Version History rows are not touched. Run the comment-citation check and fix confirmed hits comment-only. (2) `lua5.1 tests/run.lua --list > docs/test-cases.md` and set the README Tests badge from its Totals. (3) Record a fresh bundle with the vendored runner: `tests/_kit/run-automated-tests.sh` writes docs/automated-tests/<YYYYMMDD-HHMMSS>/ and rolls docs/automated-tests/RESULTS.md. Then write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md playbook. The RESULTS.md watch list gives settings/Panel.lua and settings/Category.lua Disposition cells pointing at CM-30's issues, or at the below-band measurement, never a fourth 'Accepted'. This retires ConsumableMaster-A-17's stale 928-case, 8-function, pre-peel figures. Make no version bump and no tag: the owner decides the release.",
   "tests": "none, because the change is docs and the test record only. The bundle's four suites are the check: lint pass, tests pass, perf pass, complexity pass with 0 functions above CCN 15.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua && lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . && diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md) && ls -d docs/automated-tests/2026*/ | tail -1 | xargs -I{} test -f {}ANALYSIS.md && grep -c 'Accepted' docs/automated-tests/RESULTS.md && git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -rn | sed -n 2p",
   "smoke": "",
   "finding_ids": [
    "ConsumableMaster-A-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "CM-30",
    "CM-02",
    "CM-03",
    "CM-04",
    "CM-05",
    "CM-06",
    "CM-07",
    "CM-08",
    "CM-09",
    "CM-10",
    "CM-11",
    "CM-12",
    "CM-13",
    "CM-14",
    "CM-15",
    "CM-16",
    "CM-17",
    "CM-18",
    "CM-19",
    "CM-20",
    "CM-21",
    "CM-22",
    "CM-23",
    "CM-24",
    "CM-25",
    "CM-26",
    "CM-27",
    "CM-28",
    "CM-29"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-03",
   "repo": "AuraMaster",
   "title": "A container id that comes back out of combat revives its destroyed instance instead of building a second AuraMasterAnchor<id> frame set",
   "change": "FIRST ITEM AFTER RV-AM. modules/ContainerManager.lua. (1) Add `local dormant = {}` ([id] = destroyed instance) beside `retiring` (:63). (2) CM.Sync non-deferred branch (:107-108): after `inst:Destroy()`, set `dormant[id] = inst`. destroyParked (:114-119): after `inst:Destroy()`, move the instance to `dormant[id]` instead of dropping it. (3) follow() (:85-93): add a branch after `retiring[id]`: `elseif dormant[id] then local d = dormant[id]; dormant[id] = nil; d.staleData = true; revive(id, d, hold)`. Only when no instance exists anywhere does it call NS.Container.New(id). staleData=true is required because the id may name a different container now, and CM.Announce's RequestApply rebuilds it for the new data and unparks it. (4) If the round-trip test shows the anchor still hidden or unpointed after that apply (Destroy runs anchor:Hide() and ClearAllPoints, modules/Container.lua:539-546), add `ContainerClass:Revive()` to modules/Container.lua, called from revive() for a dormant instance, which clears `retired` state as Apply requires. Otherwise add nothing. (5) Add the test seam `function CM.__dormant() return dormant end`. (6) Rewrite the :51-63 comment: a destroyed instance is kept dormant under its id and revived when the id returns, so CreateFrame('Frame','AuraMasterAnchor'..id) runs once per id per session.",
   "tests": "Test first in tests/test_containermanager.lua, using its spyCreate (:342): 'manager: an id that returns out of combat revives its destroyed instance'. Store profile A with 5 containers and profile B with 2. SetProfile B, fire timers, SetProfile back to A, fire timers. Assert spyCreate(mocks,'AuraMasterAnchor3') counts 0, CM.instances[3] is the same table as before the switch, the instance is unparked, the engine is enabled, and it applies A's container 3 data (assert inst.unit == A's unit). Mark it `-- red under: CM.Sync dropping a destroyed instance`. A second case covers the ResetProfile path, which rewinds nextContainerId: Create -> Delete -> Create reuses id 5, with 0 new AuraMasterAnchor5 frames. Regenerate docs/test-cases.md with `ka0s-bounded lua5.1 tests/run.lua --list > docs/test-cases.md`, then re-checkout so the working tree stays CRLF, and roll the README test badge to its Totals.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0, the two new cases pass and fail when step (2) is reverted; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . -> no warning beyond the two AM-13 removes; git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' -> only the four #16-#19 files.",
   "smoke": "In client: /am new twice (5 containers) on profile A; create profile B with the starters; switch A -> B -> A out of combat; `/dump AuraMasterAnchor5` shows the same table address before and after; /framestack shows one AuraMasterAnchor5; repeat the switch 10 times and watch memory with /dump collectgarbage('count') for no climb.",
   "finding_ids": [
    "AuraMaster-R-02"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-01"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-04",
   "repo": "AuraMaster",
   "title": "A stood-down addon builds no container frames: CM.Init and CM.Announce honor the latch, and standUp syncs before its visibility pass",
   "change": "modules/ContainerManager.lua. CM.Init (:570-579): after the EnsureAuraContainer notice, `if NS.IsStoodDown() then if NS.TimedSpells and NS.TimedSpells.Sync then NS.TimedSpells.Sync() end return end`, so nothing is subscribed, synced, requested or flushed while down. CM.Announce (:127-131): skip CM.Sync (and the RequestApply, which already no-ops) when NS.IsStoodDown(), but still send CONTAINERS_CHANGED so an open panel re-renders. core/LifecycleSetup.lua standUp (:108-120): call `NS.ContainerManager.Sync()` after StartListening and before ApplyVisibility, so the stand-up builds or revives (AM-03) the instances a disabled login or a disabled profile switch never built. Fix the doc comments: ContainerManager.lua:568-569, 'only when the addon is actually running', which is now true; core/AuraMaster.lua:163-165 rebuildProfile's 'CM.Init both read the latch', which should now name CM.Announce; and the OnEnable comment at :42-45.",
   "tests": "Test first in tests/test_disabled.lua. (a) 'disabled: a disabled login builds no container frame': load a fresh env with profile.enabled=false before OnInitialize and spy CreateFrame for names starting 'AuraMasterAnchor'. Assert 0 frames and CM.instances empty. Mark it `-- red under: CM.Init building before the latch check`. Now that kit 26 starts frames shown, this case would also catch a shown anchor. Then `/am enable`: the instances exist, fire timers, each engine is enabled. (b) 'disabled: a profile switch while disabled builds nothing until enable': switch to a profile with more containers while disabled, assert 0 new anchors, then enable and assert they are built. Regenerate docs/test-cases.md and the README badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0, including the test_disabled baseline and re-enable cases (:152, :408) and the test_containermanager cases from AM-03; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am disable, /reload; /framestack over the screen shows no AuraMasterAnchor frame and /dump AuraMasterAnchor1 is nil; /am enable draws every container at once; switch to another profile while disabled and back, then enable: the containers draw.",
   "finding_ids": [
    "AuraMaster-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-03"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-05",
   "repo": "AuraMaster",
   "title": "Degraded latch is edge-triggered like the library: SyncEnabled stands down or up only on a change",
   "change": "core/LifecycleSetup.lua degraded branch (:120-131). Add `local down = false`, the library's empty-latch state, so the first SyncEnabled on an enabled load is a no-op as it is with LibKa0s-Lifecycle. `function NS.SyncEnabled() local want = not enabledStored(); if want == down then return end; down = want; if want then standDown() else standUp() end end`. `NS.IsStoodDown` and `NS.IsDisabled` read `down` rather than re-reading the store, so the show ladder and the edge agree. Comment: this mirrors the library's edge semantics, which is a correctness property of the degraded path and not a library-stack-\u00a77 member copy.",
   "tests": "Test first in tests/test_lifecycle.lua (it can load tests/degraded_env.lua; degraded_env is not a suite). 'lifecycle: the degraded latch stands up and down only on an edge': load degraded, count the timers a RequestApply arms (#mocks.__timers) and the lifecycle registrations (mocks.__registrations filtered to NS.addon). Call NS2.SyncEnabled() twice with enabled unchanged and assert no new timer and no second registration of PLAYER_ENTERING_WORLD. Mark it `-- red under: the level-triggered stub`. Then set profile.enabled=false and SyncEnabled: registrations gone. Then true: back once. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 (tests/test_setups.lua:115 'degraded: ... every seam answers' still green); ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-04"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-06",
   "repo": "AuraMaster",
   "title": "The coalescing apply timer and the timed-spell scan timer are canceled on stand-down, not left to wake and find the latch",
   "change": "modules/ContainerManager.lua. Add `local flushTimer`. In RequestApply (:153-156): `scheduled = true; flushTimer = C_Timer.NewTimer(0, function() flushTimer = nil; CM.FlushPending() end)`. CM.StopListening (:557-563): `if flushTimer then flushTimer:Cancel(); flushTimer = nil end; scheduled = false`. This must reset `scheduled`, or the stand-up's RequestApply(nil, true) is swallowed. modules/TimedSpells.lua. Add `local scanTimer`. scheduleScan (:103-107): `scanTimer = C_Timer.NewTimer(0.5, function() scanTimer = nil; scanTick() end)`. TS.Stop (:131-134): cancel scanTimer, set it to nil, and set scanScheduled = false. Keep scanTick's listening guard as defense in depth. Rewrite the TS.StandDown comment (:162-164), which says 'dropped by the guard rather than canceled -- C_Timer.After hands back no handle', and the matching sentence in the CM header, if any.",
   "tests": "Test first in tests/test_disabled.lua, extending 'disabled: nothing is left armed' (:205) or as a new case 'disabled: a queued apply and a queued scan are canceled, not left armed'. Enable a 'timeless' container so TimedSpells listens. Call NS.SetByPath('alpha', 0.5) to arm the flush and fire UNIT_AURA for player to arm the scan, then /am disable. Assert #mocks.__timers() == 0. Mark it `-- red under: C_Timer.After`; kit NewTimer:Cancel removes the handle from __timers since revision 17. Then /am enable, fire timers, and assert one apply pass ran: the stand-up request was not swallowed by a stale `scheduled`. The perf coalescing scenario (tests/perf.lua:116-118, armed == 1) must still hold. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0, with the coalescing line still '1 apply pass' and the byte figures recorded in the commit body; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-05"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-07",
   "repo": "AuraMaster",
   "title": "Every event registration goes through LibKa0s-Core's SafeRegisterEvent; rejected names are kept in NS.RejectedEvents and shown in [Init]",
   "change": "Consumer follow-up for LK-11 (events-frames-taint-\u00a71, WS-04). (1) core/CoreSetup.lua. Above the `if not lib` branch, add `NS.RejectedEvents = NS.RejectedEvents or {}` so the parity derivation sees it in both halves. Live half: `NS.SafeRegisterEvent = lib.SafeRegisterEvent; NS.SafeRegisterUnitEvent = lib.SafeRegisterUnitEvent; NS.SafeRegisterEvents = lib.SafeRegisterEvents`. Degraded half: one-rung bodies with the library's signatures, pcall and no IsEventValid front gate, as the Core version-8 doc's Degradation note prescribes. SafeRegisterEvent(target, event, handler, rejected) runs `pcall(target.RegisterEvent, target, event, handler)`, appends the name to `rejected` once on failure, and returns the ok boolean. SafeRegisterUnitEvent(frame, event, rejected, u1, u2) does the same through RegisterUnitEvent. SafeRegisterEvents walks an array and returns the count registered. (2) core/AuraMaster.lua. Add a module-level `local LIFECYCLE_EVENTS = { {'PLAYER_ENTERING_WORLD','OnEnterWorld'}, {'PLAYER_REGEN_DISABLED','OnCombatChanged'}, {'PLAYER_REGEN_ENABLED','OnCombatChanged'}, {'PLAYER_TARGET_CHANGED','OnUnitSwap'}, {'PLAYER_FOCUS_CHANGED','OnUnitSwap'}, {'UNIT_PET','OnUnitPet'}, {'ADDON_LOADED','OnAddonLoaded'}, {'ADDON_RESTRICTION_STATE_CHANGED','OnRestrictionChanged'} }`. RegisterLifecycleEvents loops it through NS.SafeRegisterEvent(self, e[1], e[2], NS.RejectedEvents), and UnregisterLifecycleEvents loops the same list, replacing the hand-kept copy at :69-77. (3) modules/TimedSpells.lua: TS.Sync (:140-142) and syncAuraListen (:121) register through NS.SafeRegisterEvent(events, ...). `listening` is set only when the registration returned true. (4) core/LifecycleSetup.lua holdPending (:71): `pendingHeld = NS.SafeRegisterEvent(addon, PENDING_EVENT, fn, NS.RejectedEvents)`. On false the hold is not taken and the secure half is retried on the next stand-down or up. (5) core/DebugLogSetup.lua initSummary (:91-99): append `, rejected events: A, B` when #NS.RejectedEvents > 0. It is read at call time, and the library calls it when logging turns on (libs/LibKa0s/DebugLog.lua:766). A rejection after that point is also traced with NS.Debug('Init', 'event %s rejected by this client', name) at the moment it happens. (6) docs: the ARCHITECTURE.md '## Event Subscriptions' section says every registration goes through the helper and where rejects surface; docs/midnight-quirks.md records the probe-vs-lost-edge trade (events-frames-taint-\u00a71 last bullet).",
   "tests": "Test first. tests/test_lifecycle.lua 'lifecycle: one bad event name leaves the other seven registered and is recorded': a fresh env with mocks.__badEvents = { ADDON_RESTRICTION_STATE_CHANGED = true } set before OnEnable. Assert that the other seven names are in mocks.__registrations() for NS.addon, that NS.RejectedEvents contains the name exactly once after a disable/enable cycle, and that enabling logging prints an [Init] line naming it. Mark it `-- red under: bare self:RegisterEvent`. With kit 26, set C_EventUtils = nil in a second case so only the pcall rung runs. tests/test_timedspells.lua: the same with UNIT_AURA bad, where TimedSpells is not marked listening and the rest of the addon loads. tests/test_surface_parity.lua 'parity: the Core stub ...' stays green, because it derives the three new publications from core/CoreSetup.lua. A degraded-env case: the stub's SafeRegisterEvent records a bad name. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; grep -nE ':RegisterEvent\\(|:RegisterUnitEvent\\(' core modules settings | grep -v SafeRegister -> no call outside core/CoreSetup.lua's stub bodies; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning, RegisterLifecycleEvents CCN <= 3; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am debug on shows the [Init] line with no 'rejected events' clause on 12.1; /am disable then /am enable, then /dump AuraMaster... NS.RejectedEvents via /run print(#select(2,...)) is not reachable in chat, so use /am debug on after a /reload and read the [Init] line.",
   "finding_ids": [
    "AuraMaster-R-05"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-06",
    "LK-11"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-08",
   "repo": "AuraMaster",
   "title": "UNIT_AURA moves to the carved-out player/pet RegisterUnitEvent frame, held on the module and torn down by hand",
   "change": "modules/TimedSpells.lua. This adopts the events-frames-taint-\u00a71 'one permitted private frame' carve-out. The finding is a MAY, but the owner has every finding in scope. Add `TS.unitFrame = TS.unitFrame or nil`, created lazily once and never rebuilt: `CreateFrame('Frame')` with ONE `SetScript('OnEvent', function(_, _, unit) onUnitAura(nil, unit) end)` and nothing else. syncAuraListen (:118-128) opens the gate with `NS.SafeRegisterUnitEvent(TS.unitFrame, 'UNIT_AURA', NS.RejectedEvents, 'player', 'pet')` (AM-07's helper) and closes it with `TS.unitFrame:UnregisterEvent('UNIT_AURA')`. TS.Stop (:131-134) also unregisters UNIT_AURA on the frame by hand, because AceEvent's UnregisterAllEvents never reaches it. onUnitAura keeps its IsSafeKey check. The player/pet comparison becomes redundant but stays as defense in depth, with a comment. Rewrite the header (:13-21): AceEvent still has no RegisterUnitEvent, and this is the carve-out, citing its MUSTs (held on the module, unregistered in the disable path, reused across cycles). tests/perf.lua (:196-210): replace the unitAuraOther scenario with 'unitAuraFiltered', which asserts that a non-player UNIT_AURA never reaches the handler (the frame's __unitEvents filter) and records ms/iter and bytes for a player UNIT_AURA. Update docs/performance.md, and ARCHITECTURE.md '## Event Subscriptions' with the frame row and no register row.",
   "tests": "Test first in tests/test_timedspells.lua: 'timedspells: UNIT_AURA is registered for player and pet only, on the module's own frame'. Enable a timeless container. Assert TS.unitFrame.__unitEvents.UNIT_AURA equals {'player','pet'} (the kit records RegisterUnitEvent at tests/_kit/mock_base.lua:226) and that no AceEvent UNIT_AURA registration exists. Mark it `-- red under: events:RegisterEvent('UNIT_AURA')`. Then 'timedspells: disable unregisters the unit frame and enable reuses it': the same table, with no UNIT_AURA after disable. tests/test_disabled.lua baseline census (:152-181): the frame's registration appears when enabled and is gone when disabled. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0, with the new scenario's figures in the commit body and docs/performance.md; grep -n \"RegisterEvent(\\\"UNIT_AURA\\\"\" modules -> nothing; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client, before and after, on one character: set a container to 'only auras without a duration'. `/am perf` capture a 2-minute raid-target-dummy session with nameplates on, then record both runs with /wow-addon:perf-analysis into docs/perf-analysis/<stamp>/ as the pre/post captures. Confirm a new timed buff is learned out of combat (/am debug on shows 'learned N timed spell(s)').",
   "finding_ids": [
    "AuraMaster-R-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-07"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-09",
   "repo": "AuraMaster",
   "title": "Test mode refuses to start while disabled on every surface: the panel row gates itself, and the launcher gate moves into LibKa0s-Launcher",
   "change": "(1) settings/General.lua TEST_MODE row (:107-114): `row.set = function(v) if v and NS.IsDisabled() then if NS.Slash and NS.Slash.DisabledLine then NS.Print(NS.Slash.DisabledLine()) end if NS.RefreshOptionsPanel then NS.RefreshOptionsPanel() end return end NS.Preview.SetTestMode(v) end`. Turning it off stays allowed, and the refresh re-reads the checkbox as off. (2) core/LauncherSetup.lua (LK-16 consumer follow-up): add descriptor fields `isEnabled = function() return not (NS.IsDisabled and NS.IsDisabled()) end` and `disabledLine = function() return NS.Slash and NS.Slash.DisabledLine and NS.Slash.DisabledLine() or '' end`, and reduce onClick (:118-124) to `if NS.Slash and NS.Slash.ToggleTestMode then NS.Slash.ToggleTestMode() end`, deleting the hand gate at :119-120. Rewrite the comment block at :107-117 to say the refusal is the library's gate fed by the host's isEnabled and disabledLine (launcher-\u00a72). The degraded Launcher stub is unchanged, because LK-16 adds descriptor fields, not members.",
   "tests": "Test first. tests/test_disabled.lua: 'disabled: the panel's Test mode row refuses to start while disabled and prints one refusal line'. Disable, call the row's set(true) through NS.SetByPath('state.testMode', true), and assert NS.State.testMode is false and exactly one line equal to NS.Slash.DisabledLine() was printed. Mark it `-- red under: row.set bound straight to Preview.SetTestMode`. Also assert set(false) while disabled is accepted. The existing launcher case (tests/test_disabled.lua:373) stays green through the library gate, and a new test_launcher case asserts the descriptor carries isEnabled and disabledLine and that onClick no longer reads NS.IsDisabled (grep in the case, or a spy showing ToggleTestMode not reached while disabled). Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; grep -n 'IsDisabled' core/LauncherSetup.lua -> only inside isEnabled; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am disable; tick General -> Master controls -> Test mode: one refusal line and the box stays unticked; left-click the minimap button: the same one line; right-click opens the panel; /am enable, then the box and the left click both toggle test mode.",
   "finding_ids": [
    "AuraMaster-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-08",
    "LK-16"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-10",
   "repo": "AuraMaster",
   "title": "Migration stamp per WS-03: defaults declare schemaVersion 0, the stamp advances only past a clean step, and every stored profile is migrated",
   "change": "WS-03 consumer follow-up. defaults/Profile.lua:84-88: `schemaVersion = 0`. Rewrite the comment: AceDB strips a stored value equal to its default at logout, and backfills a declared default onto a legacy account with no stamp, and 0 is safe against both (savedvariables-\u00a71 v2.65.0). core/Database.lua NS.RunMigrations (:897-908): `g.schemaVersion = g.schemaVersion or 0`. Each step runs as `local ok, err = pcall(step.apply, NS.db)`. On failure it stops the ladder with the stamp unchanged, prints ONE line through NS.Printf(L['%s: migration to schema v%s failed; your settings were left as they were. %s'], ...), and returns without stamping. The rest of InitDB still runs, so the addon still loads. On success it stamps step.to. Database.CurrentSchemaVersion's fallback (:892-895) becomes `or 0`. Declare `NS.SCHEMA_VERSION = Database.CurrentSchemaVersion()` as the runner's target. Every step already walks eachProfile over the raw db.sv.profiles (:812-827). Add a sentence to SCHEMA_STEPS' header that this is the per-profile rule and that every step is idempotent against a fresh default profile, which a fresh install (stamp 0) runs. Add the new L key to locales/enUS.lua. docs/schema.md migration section: stamp ownership, 0, pcall, per-profile.",
   "tests": "Test first in tests/test_database.lua. (a) 'migrations: a legacy v1 account with NO stamp runs every step': build a raw SV with a v1-shaped profile and no global.schemaVersion. Before the fix AceDB backfills the default 1, which happens to be right, so pair it with (b). (b) 'migrations: a stored stamp equal to the default is not lost at logout': with the kit-26 fake stripping defaults on SetProfile/logout, store schemaVersion at the default and reload. Assert that the reloaded stamp still reads as the current version, or that the steps re-run idempotently with no change to a migrated profile. Mark it `-- red under: default = 1 with a real v1 store` and state the exact failing branch in the comment. (c) 'migrations: a step that raises leaves the stamp where it was and the addon loads': monkeypatch one step to error, and assert the stamp is at the previous version, one failure line was printed, and NS.db exists. Mark it `-- red under: stamping without pcall`. (d) 'migrations: an inactive profile is migrated too': two stored v5 profiles, and both get the v6 stamping. Also assert 'every step is idempotent on a fresh default profile': run the ladder twice over a fresh profile and assert it is deep-equal. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; grep -n 'schemaVersion' defaults/Profile.lua -> '= 0'; lizard ... -C 15 -w . -> no new warning, NS.RunMigrations CCN <= 6; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client, with a BACKUP of WTF/Account/<acct>/SavedVariables/AuraMasterDB.lua: hand-edit a copy to delete global.schemaVersion; log in with /am debug on: the [Migrate] lines run v2..v6 (or no-op idempotently), /reload shows the stamp persisted ('schema v6' in [Init]); restore the backup.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-09",
    "WS-03"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-11",
   "repo": "AuraMaster",
   "title": "Slash descriptor set/applyDefault return the seam's answer; LibKa0s-Slash 15 prints refusals and NO_DEFAULT",
   "change": "LK-17 consumer follow-up. settings/Slash.lua descriptor (:467-477). `set = function(path, v) return NS.SetByPath(path, v) end`. Drop the host's two refusal prints, which CliSet now prints from `false, err, why` as INVALID, err and indented why. `applyDefault = function(row) return NS.ApplyDefault(row) end`. A noReset row answers false, and the library prints NO_DEFAULT ('<path> has no default to restore') instead of echoing the unchanged value, which is the problem the old comment describes. The host no longer prints the row's reason, because it would land before the library's line. So delete `noResetReason` (settings/Containers.lua:65) and its now-unread key in locales/enUS.lua, and reduce NS.ApplyDefault's noReset branch (settings/Schema.lua:784) to `return false`. Update the comments above both fields. settings/OptionsSetup.lua's panel `set` keeps its own two-line print, because the panel has no CliSet.",
   "tests": "Test first in tests/test_slash_verbs.lua. 'slash verbs: /am set with a refused value prints INVALID and the row's reason once each, and does not echo the unchanged value'. Use the Text template parser row, which answers false plus why. Assert the exact line sequence and that no 'key = value' echo follows. Mark it `-- red under: the host wrapper printing and returning nil`. 'slash verbs: /am reset container.name prints the library's no-default line once': assert the line equals SlashLib.STRINGS.NO_DEFAULT:format('container.name') and that no echo follows. Re-point any existing case that expected the host's printed reason. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 (tests/test_locale.lua no-dead-keys green); grep -rn noResetReason settings core -> nothing; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-10",
    "LK-17"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-12",
   "repo": "AuraMaster",
   "title": "enable/disable/lock/unlock confirm in slash-commands-\u00a75's set shape through the library's own read-back",
   "change": "settings/Slash.lua. Add a file-local `echo(path, prose)`. When the live dispatcher exists (`SlashLib.FormatKV` present), it runs `cli:CliGet(path)`, which prints the stored value through the library's formatter, the same line `/am get enabled` prints (`enabled = true`), with no private variant (\u00a75 MUST NOT). On the degraded stub, which has no formatter and whose CliGet prints the library-absent line, it prints `prose`. runEnabled (:191-195): after a successful NS.SetByPath('enabled', on), call echo('enabled', <the old sentence>). runLock (:282-285): the same with 'locked'. The two prose L keys stay, used by the degraded arm only.",
   "tests": "Test first. tests/test_slash_verbs.lua: '/am enable, /am disable, /am lock, /am unlock echo the stored value in the set shape'. Assert the last line equals SlashLib.FormatKV('enabled', SlashLib.FormatValue(row, true)) for the live row, and likewise for false and for locked. Mark it `-- red under: the prose confirmation`. Re-point tests/test_disabled.lua step 7 (:348-372), and any test_slash_verbs expectation of 'Aura Master enabled', to the set-shape line. tests/test_slash.lua:233 (degraded) keeps 'Aura Master enabled'. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> no new warning; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am disable prints 'enabled = false' in the gold/white shape, /am enable prints 'enabled = true', and /am lock and /am unlock print 'locked = true' and 'locked = false'.",
   "finding_ids": [
    "AuraMaster-A-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-11"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-13",
   "repo": "AuraMaster",
   "title": "Clear the release gate: split Cat.SyncUserCategories (CCN 25) into phases and table-dispatch renderCategories (CCN 16)",
   "change": "No behavior change (performance-\u00a711, anti-pattern #52). defaults/Categories.lua:1112-1194 Cat.SyncUserCategories becomes an orchestrator of CCN 5 or less over four file-local helpers, each of CCN 8 or less: `teardownUserDefinitions(template)` (the removal loop plus NS.UnregisterSchemaRows), `insertionAnchors()` (returns `before`), `materializeRecords(recs, order, template)` (returns built, made; the canonicalize-name branch stays inside with its WHY comment) and `registerUserRows(built, before)`. The WHY comments move with their code and are not copied. CAP GUARD: the file is 1478 lines. If `wc -l` exceeds 1500 after the split, the same commit moves the user-category section (the `-- ---` banner at :678 through EOF: CharCount .. ForgetUnusableUserRecords) into a new defaults/UserCategories.lua, loaded in AuraMaster.toc directly after defaults\\Categories.lua with a `# LOAD-BEARING:` comment, adds a module-map.md row, and updates tests/test_loadorder.lua expectations. settings/Filters.lua:500-543 renderCategories: add a module-level `GRID_RENDER = { custom = renderCustomGrid, blizzard = renderBlizzardGrid }` with a default `renderPlainGrid`. Each is a file-local function(ctx, g, mine, cfg, auraType, hideRow) holding today's branch body verbatim, and the loop calls `(GRID_RENDER[g.key] or renderPlainGrid)(...)`.",
   "tests": "Characterization first (testing-\u00a713), before touching code: confirm each phase is pinned. tests/test_defaults.lua asserts schema order follows Cat.For order per aura type. tests/test_database.lua user-category storage covers the canonicalized name and a skipped record left on disk. Add any missing pin now as its own case in the same commit, for example 'categories: a sync after a profile switch leaves no stale definition, row or template key', asserting all four mirrors. tests/test_pages_filters.lua pins each grid's heading, bulk buttons, notes and extra column; add one case for the Dispel Types grid drawing no 4th cell if none exists. Every added case must pass on the pre-split code (a characterization), and the suite must stay green after the split. Regenerate docs/test-cases.md and the badge if cases were added.",
   "verify": "lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . -> NO warning at all (the release gate is clear from here on); luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; wc -l defaults/Categories.lua -> 1500 or less (or the peel landed); the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: Filters -> Categories on a buff and a debuff container draws the Spell Categories, Blizzard, Dispel Types and Who Cast It grids as before; create and delete a user category on General and switch profiles: the grid rows follow.",
   "finding_ids": [
    "AuraMaster-R-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-12"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-14",
   "repo": "AuraMaster",
   "title": "The minimap row's CLI path reads in its shown sense: global.minimap.shown, stored on LibDBIcon's hide",
   "change": "WS-06 consumer follow-up (launcher-\u00a73). The stored key stays `global.minimap.hide`, with no SavedVariables change. The schema path (the CLI name) becomes `global.minimap.shown`. settings/Schema.lua:65 MINIMAP_PATH = 'global.minimap.shown'. Its read (minimapShown) and write (writeMinimap, storing `t.hide = not value`) already invert; they now key on the renamed path. NS.DefaultFor's special case (:162-168) and NS.CheckWrite (:764-767) key on the new constant. settings/General.lua:49 and settings/OptionsSetup.lua:36 use the same constant. Better, publish `NS.MINIMAP_PATH` once from settings/Schema.lua, which loads first, and read it in the other two instead of re-spelling it. Rewrite the three comment blocks that say the path is spelled 'global.minimap.hide' VERBATIM: Schema.lua:52-64, General.lua:33-36 and OptionsSetup.lua:31-35, where `/am reset global.minimap.hide` becomes `.shown`. core/LauncherSetup.lua:30-34 comment. docs: schema.md, settings-panel.md, slash-dispatch.md and ARCHITECTURE.md Launcher section: the CLI path is `global.minimap.shown`, which answers true while the button shows, and the storage is `global.minimap.hide`.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Test first. Re-point every test referencing 'global.minimap.hide' as a PATH (grep -rn \"minimap.hide\" tests/ | grep -v _kit): test_launcher, test_options_descriptor, test_slash_verbs, test_schema_paths. New case in tests/test_slash_verbs.lua: '/am get global.minimap.shown answers true while the button shows; /am set global.minimap.shown false stores hide = true'. Assert db.global.minimap.hide == true and NS.Launcher:IsShown() == false. Mark it `-- red under: the path spelled hide`. Also 'the old path global.minimap.hide is not a setting': /am get prints NOT_FOUND. Keep the page-Defaults veto case green under the new path. Regenerate docs/test-cases.md and the badge.",
   "verify": "grep -rn \"minimap\\.hide\" settings core modules -> only storage reads and writes (t.hide) and comments naming the storage key, no path constant; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am get global.minimap.shown -> true; /am set global.minimap.shown false hides the button; /reload keeps it hidden; /am reset global.minimap.shown brings it back; General -> Defaults does not un-hide it.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-13",
    "WS-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-15",
   "repo": "AuraMaster",
   "title": "AuraMaster#21: adopt LibKa0s-Schema-1.0's primitives, registry, bulk bracket, SameValue and Validate, with the host arm kept for the library-less build",
   "change": "The LK-22 consumer follow-up, taking the adoption mapping from the v1.56.0 docs/api/Schema/version-2-docs.md AuraMaster note and Ka0sAddonsCommonTasks/docs/2026-09-22-SUITE_STANDARDS_AND_LIBKA0S_SWEEP/3b-specs/schema.md:520-538. settings/Schema.lua. Add `local SchemaLib = LibStub and LibStub('LibKa0s-Schema-1.0', true)`. (1) Primitives: `splitPath`/`readFrom`/`writeInto` (:88-120) become thin locals calling lib.SplitPath / lib.Read(root, parts, first) / lib.Write(root, parts, value, first) when SchemaLib is present, else the existing host bodies. The host bodies stay as the library-absent arm and are renamed hostSplitPath etc. (2) Registry: `local S = SchemaLib and SchemaLib:New{ rows = NS.Schema }`, passing neither resolveRoot nor announce. FindSchemaRow uses S.FindRow when S exists. RegisterSchemaRows keeps its default stamping, then calls S.AddRows(rows, indexOf(beforePath)). UnregisterSchemaRows keeps its in-place rebuild and ends with S.Reindex(). (3) Bracket: NS.Bulk = { Begin = S.BulkBegin, End = S.BulkEnd, Run = S.BulkRun } when S exists. tally(n) calls S.BulkAdd(n), and every `bulk.depth > 0` test (:456, :485, :501, :617, :648, :686) calls an `inBulk()` that maps to S.InBulk(). Bulk.Run callers that returned true now set `info.profileReset = true` (JC-9): the degraded RestoreAllDefaults in settings/OptionsSetup.lua:295-306. modules/ContainerManager.lua copyThrough and ResetPositions return nothing and are unchanged. (4) changes() (:427-431) is `not lib.SameValue(old, new)` when present, which drops the FilterCompiler.Signature dependency on the live path. The spell-id sets are integer-keyed after normalizeIdSet. (5) NS.ValidateSchema (:819-835) runs S.Validate{ pages = VALID_PAGES, defaultsRoot = function(parts) if parts[1]=='container' then return NS.CONTAINER_TEMPLATE, 2 elseif parts[1]=='global' then return NS.defaults, 1 end return NS.defaults.profile, 1 end } and returns errors + missing. The host loop stays as the absent arm. (6) RE-EVALUATION OF Set (#21's triggers fired: row.normalize exists and ConsumableMaster is its second consumer). DECISION: keep NS.SetByPath and do not adopt S.Set. The front branches have no row-shaped equivalent in S.Set: the minimap inversion, the CARVE_OUTS whole-set normalizers, the whole-SECTION all-or-nothing writes, and NS.CheckWrite's dry run that must mirror them. The library-less build must keep the host seam in any case, so adopting Set would give the live and degraded builds two different write paths for the same rows, which is the cost the spec's verify round measured. Record this in docs/schema.md ('Write seam: why AuraMaster keeps SetByPath') with a re-check trigger: 'LibKa0s-Schema gains a resolve hook for container-relative paths plus section writes, or the standard makes Set adoption a requirement'. Update the header comment of settings/Schema.lua. After the owner merges, comment on #21 with the decision and label it state:done, spacing each gh write at least 5 s apart.",
   "tests": "Characterization first: the existing suites pin the seam (tests/test_schema.lua, test_schema_paths.lua, test_bulklog.lua incl. :99 'the degraded build's Reset all is one line in total', test_setups.lua:115, test_slash_verbs.lua:616). They must stay green unchanged, apart from JC-9 re-pins. New cases: tests/test_schema.lua 'schema: with LibKa0s the bracket, registry and validator are the library's'. Assert NS.Bulk.Begin == SchemaLib instance BulkBegin, NS.FindSchemaRow(path) == S.FindRow(path) for every row, and NS.ValidateSchema() == 0. Mark it `-- red under: the host copies on the live path`. 'schema: without LibKa0s the host arm still answers' is a degraded-env case asserting SetByPath, the bracket's one line and ValidateSchema == 0. 'schema: -0 over 0 is still no change under SameValue': the ResetPositions stagger, with a bracket tally of 0 for an unmoved container. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0 (the write-seam scenario's bytes/iter recorded in the commit body, no rise); lizard ... -C 15 -w . -> none; wc -l settings/Schema.lua -> 1500 or less; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: General -> Defaults, Containers -> copy settings from, and Reset position each log ONE [Set] line with /am debug on; /am set container.bars.width 300 works; Reset all settings logs the profile reset once.",
   "finding_ids": [],
   "issue_refs": [
    "AuraMaster#21"
   ],
   "depends_on": [
    "RV-AM",
    "AM-14",
    "LK-22"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "AM-16",
   "repo": "AuraMaster",
   "title": "Degraded Options stub composers answer {}; enable/disable/lock write through a declared writeThrough list; the Slash stub's lines are the library's own",
   "change": "WS-02 / LK-23 / LK-18 consumer follow-ups. (1) settings/OptionsSetup.lua stub (:175-280). Delete composeBlock. ColorPair, FontGroup, BorderGroup and BarGroup answer `{}`. MasterControls answers `{}, function() end`. Keep MASTER_GROUP and RestoreAllDefaults. Rewrite the header (:162-173): per options-ui-\u00a71 (v2.64.0+), a library-absent stub's composers answer an empty row list, and a host copy of a composed block is anti-pattern #73. (2) Route (a) for host verbs on composed rows. settings/Schema.lua declares `NS.WRITE_THROUGH = { enabled = true, locked = true }`, data only. In NS.SetByPath, a path with no indexed row that IS in NS.WRITE_THROUGH is stored raw into db.profile with writeInto(copy(value)), with no validate, normalize or onChange, is tallied, and is announced with announceWrite(nil, nil, path, value, false, false), returning true. A path with a row always takes the row. This mirrors LK-23's library semantics for AuraMaster's own seam, which AM-15 kept. When SchemaLib exists, the same list is also handed to S.New{ writeThrough = {'enabled','locked'} } so a future Set adoption inherits it. settings/Slash.lua runEnabled calls NS.SyncEnabled() after a successful write, which is idempotent on the live path where the row's onChange already ran and required on the degraded path where no row exists. runTest drives NS.Preview directly and needs no route. (3) settings/Slash.lua stub. absent(verb) prints the WS-02 library-absent line through the locale as one sentence, `printf(L['%s is unavailable: the LibKa0s library did not load.'], '/am ' .. verb)`, replacing L['/am %s is unavailable. %s.'], which is deleted from locales/enUS.lua. stub.DisabledLine (:388-391) uses the library's exact bytes: a file-level `local STUB_DISABLED_LINE_FORMAT = '%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r'` formatted with (brandName or slash, slash .. ' enable'), and published for the test as `Sl.__stubDisabledLineFormat` in both branches. (4) NS.ValidateSchema and every degraded consumer tolerate the smaller schema. CM's CONFIG_CHANGED handler already treats a nil row as the default apply. (5) docs/settings-panel.md 'The degraded panel': the composed rows are absent, /am set on them prints the library-absent line, and enable/disable/lock/unlock still work through writeThrough. docs/ARCHITECTURE.md '## The disabled state' names the route.",
   "tests": "Test first. tests/test_optionssetup.lua:369: replace `#NS2.Schema == #NS.Schema` with three pins: the full-load count equals N_full, the library-absent count equals N_degraded, and the delta equals the named composer rows. Compute the delta from the live composers' outputs so it is derived, not typed, and mark it `-- red under: a non-empty stub composer`. tests/test_options_descriptor.lua:445 'the stub's composers emit the paths and types the live composers do' is replaced by 'every stub composer answers an empty row list'. Fall-together case (tests/test_slash_verbs.lua): on a degraded load, `/am set container.bars.fontSize 12` and `/am set enabled false` each print exactly '/am set is unavailable: the LibKa0s library did not load.' and write nothing. tests/test_slash.lua:233: re-point it off recordSeam so the degraded `/am disable` / `/am enable` really store db.profile.enabled false/true through writeThrough with no row, and the latch follows (NS2.IsStoodDown()). Mark it `-- red under: writeThrough absent (Setting not found)`. The degraded `/am lock` does the same for locked. tests/test_surface_parity.lua: `T.assertLibraryConstant(NS2.Slash.__stubDisabledLineFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT')`, marked `-- red under: the library rewording`. tests/test_slash_verbs.lua:581 re-points to the new absent line. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; grep -n 'composeBlock' settings/OptionsSetup.lua -> nothing; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client, on a throwaway copy of the addon with libs/LibKa0s renamed: /reload; /am disable and /am enable toggle the containers with no Lua error; /am set enabled false prints the library-absent line; /am lock locks; restore the folder.",
   "finding_ids": [
    "AuraMaster-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-15",
    "LK-18",
    "LK-23",
    "WS-02"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-17",
   "repo": "AuraMaster",
   "title": "Adopt O.RenderTabbedSchema's tabs/disabledFor/disabledNotice/chrome and O.PageBanner's action; delete the host tab renderer and header fork",
   "change": "LK-28 consumer follow-up (C-04b; library-stack-\u00a75, anti-pattern #47). settings/OptionsSetup.lua. Delete collectTabs (:560-580), settleActiveTab (:583-588), renderActiveTab (:624-640), drawDisabledNotice (:604-608), renderBespoke (:613-619), placeTab (:548-557), Helpers.RenderTabbedPage (:659-685), buildContainerHeader (:480-504), placeInHeader and the HEADER_* constants (:458-478), and Helpers.ContainerHeader (:521-535). Replace them with one host wrapper, `Helpers.RenderPage(ctx, pageKey, spec, banner)`, that maps AuraMaster's page spec onto the library opts. `tabs`: spec.tabs filtered by `auraTypes` against the active container at render time, each wrapped as `render = function(c) t.render(c, cfg, rowsOfGroup(t.key)) end`. A tab keyed by a schema group replaces that group's rows, as the v1.56.0 Options doc specifies. If that doc says otherwise, STOP and raise it upstream; never fork. `disabledFor` and `disabledNotice` pass through. The muted-red NOTICE_COLOR stays via disabledNotice returning the colored string. `chrome = function(c) if spec.intro and cfg then spec.intro(c, cfg) end end` (the chrome hook runs after the strip and before rows). The empty-registry page becomes a single `tabs` entry `{ key = '__empty', label = L['Container'], render = <the 'No containers yet' TextRow> }` when there is no cfg and the page is not addonWide. Banners: Helpers.ContainerBanner keeps O.PageBanner, and the Containers page's banner becomes `Helpers.PageBanner(ctx, { label, tooltip, list, order, value, onSelect, action = { text = L['New container'], tooltip = ..., onClick = spec.onNew } })` (options-ui-\u00a714 picker+create band). Helpers.RenderContainerPage (:688-690) calls Helpers.RenderPage with ContainerBanner. General (settings/General.lua:186) and Containers (settings/Containers.lua:244-269) call Helpers.RenderPage. Delete Containers.lua's header() and its __chromeWidgets release wrapper (:244-269), because the library owns widget release. LK-27: remove every host write of `ctx.__bannerWidget` (ContainerBanner :424, :495, :525, :666), since the library keeps and releases it. Update the stub member list (:311-316): drop 'ContainerHeader' and 'RenderTabbedPage' and add 'RenderPage'. The header comment names the library as the renderer.",
   "tests": "Characterization first, on the old code: for each of the seven pages (general, containers, filters, layout, bars, icons, text), pin the drawn tab keys and labels in order, the active-tab heal after a container switch that removes the active tab, the disabled notice text and disabled state on Bars/Icons/Text for a mismatched style, the Filters intro warnings above rows, the empty-registry line, and the Containers band holding a picker and a New container button. Use tests/page_helpers.lua and the TabStrip / PageBanner widgets the kit records. Commit those pins green on the old code. Then swap and keep them green. Re-point the test_optionssetup and test_pages_* cases that read ctx.__tabs or ctx.__chromeWidgets to the library's documented seam or the recorded TabStrip. New leak case using kit 26's M.__aceguiLive('Dropdown'): rendering the Filters page 10 times and the Containers page 10 times leaves the live Dropdown and Button counts flat. Mark it `-- red under: the host clearing ctx.__bannerWidget`. Regenerate docs/test-cases.md and the badge.",
   "verify": "grep -nE 'collectTabs|settleActiveTab|renderActiveTab|RenderTabbedPage|buildContainerHeader|__bannerWidget\\s*=' settings/ -> nothing; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; wc -l settings/OptionsSetup.lua shrinks; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: open every page; each draws its tab strip; switch tabs; select a container whose aura type hides Categories and Overrides, and the strip heals to the first tab; Bars on an icons container shows the muted-red notice and disabled controls; Containers shows the picker and New container on one band and New container creates one; flip containers 20 times and /dump collectgarbage('count') stays flat.",
   "finding_ids": [
    "AuraMaster-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-16",
    "LK-27",
    "LK-28"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "AM-18",
   "repo": "AuraMaster",
   "title": "Library-missing lines route one whole sentence through the locale with a placeholder",
   "change": "localization-\u00a71. Four sites stop joining NS.LIBKA0S_MISSING with an unrouted ', ' or '; ' fragment. core/CoreSetup.lua:72: `DEFAULT_CHAT_FRAME:AddMessage(NS.SafeToString(NS.PREFIX) .. ' ' .. NS.L['%s; running on reduced built-in fallbacks.']:format(NS.LIBKA0S_MISSING))`. core/DebugLogSetup.lua:19: `local missing = NS.L['%s, so the debug console window is unavailable.']:format(NS.LIBKA0S_MISSING)`. core/LauncherSetup.lua:50: `NS.L['%s, so the minimap button is unavailable.']`, the same way. core/PerfSetup.lua:24: `NS.L['%s, so performance measurement is unavailable.']`, the same way. locales/enUS.lua: add the four whole-sentence keys and delete the four fragment keys ('running on reduced built-in fallbacks.', 'so the debug console window is unavailable.', 'so the minimap button is unavailable.', 'so performance measurement is unavailable.').",
   "tests": "Test first. tests/test_locale.lua: 'locale: no library-missing line joins a routed fragment'. It greps core/*.lua for `LIBKA0S_MISSING%s*%.%.` and asserts no match. Mark it `-- red under: the concatenation at the four sites`. The existing output pins (tests/test_setups.lua:168, test_debuglogsetup.lua:124/149, test_launcher.lua:396, test_perf.lua:132, test_slash_verbs.lua:616) keep asserting the same visible text, which is unchanged in enUS, and stay green. tests/test_locale.lua's no-dead-keys case goes red if a fragment key is left behind. Regenerate docs/test-cases.md and the badge.",
   "verify": "grep -n 'LIBKA0S_MISSING \\.\\.' core settings -> nothing; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-17"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-19",
   "repo": "AuraMaster",
   "title": "One enabled predicate, and the dead pre-11.0 GetMouseFocus rung deleted",
   "change": "(1) core/LifecycleSetup.lua:30-32: publish `function NS.EnabledStored() return NS.GetSetting('enabled') ~= false end` with its one comment, and use it inside the file. settings/Slash.lua:109-111: delete isEnabled and its duplicated comment, and pass `isEnabled = NS.EnabledStored` in the descriptor (:452). core/LifecycleSetup.lua loads before settings/Slash.lua (AuraMaster.toc). (2) core/Compat.lua:314: delete `if _G.GetMouseFocus then return _G.GetMouseFocus() end`. GetMouseFocus was removed in 11.0, so on a 120100-only TOC this rung is dead, and WS-05's compat rule now says a fallback to a global every supported client exposes only through a newer namespace MUST be deleted. Grep core/Compat.lua for any other `_G.<Global>` rung that 120100 does not provide and delete it too, listing each in the commit body. The documented test seams (CM.Rename, Database.Merge, NS.IsSection) stay as they are, per the verify note. docs/compat-layer.md: remove the GetMouseFocus fallback sentence and re-count the shims only if a shim disappears.",
   "tests": "Test first. tests/test_compat.lua: 'compat: GetMouseFocus answers from GetMouseFoci and has no pre-11.0 rung'. With _G.GetMouseFoci = nil and _G.GetMouseFocus stubbed to return a frame, Compat.GetMouseFocus() answers nil. Mark it `-- red under: the _G.GetMouseFocus fallback`. tests/test_slash.lua: 'slash: the dispatcher's isEnabled is NS.EnabledStored' asserts `NS.Slash.__cli` was built with the same function (a descriptor spy) or behaviorally matches before InitDB (nil read -> enabled). Regenerate docs/test-cases.md and the badge.",
   "verify": "grep -n 'GetMouseFocus()' core/Compat.lua -> nothing; grep -n \"GetSetting(\\\"enabled\\\") ~= false\" core settings -> exactly one hit; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am pick still resolves the frame under the cursor (GetMouseFoci path).",
   "finding_ids": [
    "AuraMaster-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-18",
    "WS-05"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-20",
   "repo": "AuraMaster",
   "title": "One frame-pick flow: NS.FramePicker.PickFor owns resolve, combat refusal and the two writes",
   "change": "modules/FramePicker.lua: add `function FP.PickFor(onDone, onCancel)`. It resolves `NS.ActiveContainer()` and answers false plus the no-container line when there is none. It refuses under InCombatLockdown() with the one gray line L['cannot pick a frame during combat \u2014 attaching to a frame waits until combat ends'] and answers false. Otherwise it calls FP.Start with a completion that does the two writes `NS.SetByPath('container.attach.frame', name, id)` and `NS.SetByPath('container.attach.mode', 'frame', id)`, then calls onDone(c, name), and answers true. settings/Slash.lua runPick (:303-317) and settings/Layout.lua pickFrame (:217-232) keep only their own messages and the panel re-open, passed as onDone/onCancel. The overlay stays named 'AuraMasterFramePicker': tests/test_anchors.lua:569,588,598 read the global, and no standard section forbids a prefixed name.",
   "tests": "Test first. tests/test_framepicker.lua: 'framepicker: PickFor refuses in combat, refuses with no container, and makes exactly the two attach writes'. Spy NS.SetByPath and assert the paths, values and id. Mark it `-- red under: PickFor absent` (it starts red because the function does not exist). The existing /am pick (test_slash_verbs) and Layout Pick button (test_pages_layout) cases stay green unchanged. Regenerate docs/test-cases.md and the badge.",
   "verify": "grep -n 'container.attach.frame' settings/Slash.lua settings/Layout.lua -> nothing; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "In client: /am pick then click the player frame attaches; Layout -> Pick frame does the same and re-opens the panel; both refuse in combat with one gray line.",
   "finding_ids": [
    "AuraMaster-R-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-19"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-21",
   "repo": "AuraMaster",
   "title": ".luacheckrc drops the four read_globals nothing reads, and a lint-config case keeps the list honest",
   "change": ".luacheckrc:13-15: delete 'GameTooltip', 'C_Spell', 'GetAddOnMetadata' and 'GetSpellInfo' from read_globals. Declaring the retired global GetSpellInfo would let a regression of anti-pattern #10 lint clean.",
   "tests": "Test first. tests/test_lintconfig.lua: 'lintconfig: every read_globals name is referenced as a global by some authored file'. Parse the read_globals block. For each name, search core/, modules/, settings/, defaults/, locales/ and tests/*.lua (not tests/_kit) for a use not preceded by '.' or ':' and not inside a comment (a line-level strip of '--' comments is enough). Assert one hit each. Mark it `-- red under: the four stale entries`.",
   "verify": "luacheck . -> 0 warnings / 0 errors (in the same file count); ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-20"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-22",
   "repo": "AuraMaster",
   "title": "Turn on --jobs for the commit-gate suite after measuring, in its own commit",
   "change": "testing-\u00a714. First measure and record in the commit body: the serial wall and CPU time (`/usr/bin/time -v ka0s-bounded lua5.1 tests/run.lua`), the per-suite share, and whether time goes to loadfile or to git spawns (test_eol, test_prose and test_layout_cap each read the tracked set). Apply the testing-\u00a714 cache and batching rules first if the measurement names one (for example one shared `git ls-files` result). Then run `ka0s-bounded lua5.1 tests/run.lua -j auto` and diff its totals and exit code against the serial run. Fix any suite that fails only when sharded (shared global state across suites, or order dependence). Then set `jobs = 'auto'` in tests/run.lua's Kit.run{...} (:57). docs/testing.md:16-19: replace the stale 'if the serial suite ever passes about ten seconds' with the measured serial and sharded figures and the rule that the sharded run must match the serial one.",
   "tests": "No new case: the red/green check is equivalence. Serial and `-j auto` runs must report identical pass/fail/skip totals and exit 0. A suite that failed only sharded gets its fix plus a comment naming the shared state it depended on.",
   "verify": "ka0s-bounded lua5.1 tests/run.lua (now sharded) and ka0s-bounded lua5.1 tests/run.lua -j 1 -> the same totals line and exit 0; wall time recorded in the commit body; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only the #16-#19 files.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-21"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-23",
   "repo": "AuraMaster",
   "title": "AuraMaster#16: peel Dispel Colors out of settings/GeneralSpells.lua into settings/GeneralDispel.lua",
   "change": "Move the '-- Dispel Colors' section (settings/GeneralSpells.lua:1458 through renderDispel, about 70 lines) whole into a new settings/GeneralDispel.lua that publishes `NS.GeneralDispel = { ROWS = DISPEL_ROWS, TAB = { key = DISPEL, label = DISPEL, render = renderDispel } }`. The shared BULLET / BULLET_GAP constants are published once on NS.GeneralSpells and read there, not copied. GeneralSpells keeps TABS = { SPELLS tab } and computes DISPEL = L['Dispel Colors'] only for its `before`. settings/General.lua: register NS.GeneralDispel.ROWS in place of GS.DISPEL_ROWS, and build tabs as { GS.TABS[1], NS.GeneralDispel.TAB }, in the AM-17 shape. AuraMaster.toc: add `settings\\GeneralDispel.lua` directly after `settings\\GeneralSpells.lua`, under the existing LOAD-BEARING comment, amended to name both files as loading before General. docs/module-map.md: a new row, and the TOC order note. Remove GeneralSpells' census row and its layout-\u00a71 register row from docs/ARCHITECTURE.md (AuraMaster-A-12), and rewrite the census preamble so it no longer says 'the layout-\u00a71 rows above ... none ratified'. Put 'Fixes #16' in the commit body. After the owner merges, `gh issue edit 16 -R tusharsaxena/AuraMaster --remove-label state:untriaged --add-label state:done` and close it, 5 s or more after any other gh write.",
   "tests": "No behavior change. tests/test_pages_general.lua's Dispel Colors cases (:1705-) and tests/test_loadorder.lua must stay green. Update test_loadorder only if it lists page files explicitly. Add 'loadorder: GeneralDispel loads after GeneralSpells and before General' if the TOC-derived order is not already asserted.",
   "verify": "wc -l settings/GeneralSpells.lua -> under 1500; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 (test_layout_cap green, with no census row naming a file under the cap); ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only tests/test_database.lua, tests/test_filtercompiler.lua, tests/test_pages_general.lua.",
   "smoke": "In client: General -> Dispel Colors draws its lead-in, four bullets and five swatches; changing a swatch recolors a dispel-typed bar.",
   "finding_ids": [
    "AuraMaster-A-12"
   ],
   "issue_refs": [
    "AuraMaster#16"
   ],
   "depends_on": [
    "RV-AM",
    "AM-22"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-24",
   "repo": "AuraMaster",
   "title": "AuraMaster#17: split the user-category suites out of tests/test_database.lua",
   "change": "Move the two sections from '-- \u2500\u2500 user categories: storage, keying, dynamic rows' (tests/test_database.lua:1196) to EOF, which includes 'deletion and cleanup' (:1429) and its local helpers reload (:1204) and inactiveProfileWith (:1437), into a new tests/test_database_categories.lua. Give it the same header locals (T, the assertions, fresh = dofile('tests/fresh_env.lua')). A helper used by both files moves to a shared tests/database_helpers.lua loaded with dofile and is never duplicated. Declare 'test_database_categories' in tests/run.lua's suites right after 'test_database'. Remove the file's census and register rows from docs/ARCHITECTURE.md (A-12). Put 'Fixes #17' in the commit body. The post-merge gh label and close is spaced as in AM-23.",
   "tests": "No case changes: every moved case keeps its exact name. `ka0s-bounded lua5.1 tests/run.lua --list | sort` before and after must be identical apart from the suite grouping, so regenerate docs/test-cases.md and check the Totals are unchanged.",
   "verify": "wc -l tests/test_database.lua tests/test_database_categories.lua -> both under 1500; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 with the same total as before; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only tests/test_filtercompiler.lua and tests/test_pages_general.lua.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-12"
   ],
   "issue_refs": [
    "AuraMaster#17"
   ],
   "depends_on": [
    "RV-AM",
    "AM-23"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-25",
   "repo": "AuraMaster",
   "title": "AuraMaster#18: split the user-category suites out of tests/test_filtercompiler.lua",
   "change": "Move the sections from '-- \u2500\u2500 user categories and the categorized union' (tests/test_filtercompiler.lua:1284), the overlap guardrail (:1373) and the single shown category (:1436) to EOF, with their locals (freshEnv, MINE, NOBODYS, envWithUserCategory, groupNamed, OWNER_IDS, RENEWING_MIST_AURA, onlyShown, userContainer), into a new tests/test_filtercompiler_categories.lua. Top-of-file helpers the moved cases read (grep for cfg, compile, setOf, hasWarning, only, onlyPlus) move into a shared tests/filtercompiler_helpers.lua loaded with dofile by both suites; none is duplicated. Declare 'test_filtercompiler_categories' after 'test_filtercompiler' in tests/run.lua. Remove the census and register rows (A-12). Put 'Fixes #18' in the commit body, with the post-merge gh as in AM-23.",
   "tests": "No case changes; the names are identical and the Totals unchanged. Regenerate docs/test-cases.md.",
   "verify": "wc -l of both files -> under 1500; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 with the same total; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> only tests/test_pages_general.lua.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-12"
   ],
   "issue_refs": [
    "AuraMaster#18"
   ],
   "depends_on": [
    "RV-AM",
    "AM-24"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-26",
   "repo": "AuraMaster",
   "title": "AuraMaster#19: split the category-editing suites out of tests/test_pages_general.lua; the over-cap census is empty",
   "change": "Move the sections from '-- \u2500\u2500 'Your categories': create, rename, delete and the shipped lock' (tests/test_pages_general.lua:754) through the end of the add-line section, just before the Weapon enchants block (:1641), into a new tests/test_pages_general_categories.lua. The shared helpers both halves read (general, inScroll, spyApply, spells, rendered, marked, ownedName, drawnEntries, entry, entryTooltip, entryMark, entryHelp, entryHelpTint, listedIds, starterIds, spyPaths) move into a new tests/general_page_helpers.lua that returns a table and is loaded with dofile by both. The section-local helpers (manage, lineOf, onlyUserKey, withNotice, withIds, spell, suggesting, seedZephyrs) move with their sections. Declare 'test_pages_general_categories' after 'test_pages_general'. docs/ARCHITECTURE.md (A-12, final): delete the last layout-\u00a71 register row. Keep the '### Files over the 1500-line cap' heading under '## Documented deviations', which the kit's gate locates, with the preamble rewritten to 'No authored file is over the cap; the census is measured by tests/_kit/test_layout_cap.lua.' and no table. Put 'Fixes #19' in the commit body, with the post-merge gh as in AM-23.",
   "tests": "No case changes; the names are identical and the Totals unchanged. Regenerate docs/test-cases.md. The kit's test_layout_cap must accept the empty census, which layout-\u00a71 sanctions. If it rejects the no-table form, keep the header row with no data rows.",
   "verify": "git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' -> EMPTY; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 with the same total; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-12"
   ],
   "issue_refs": [
    "AuraMaster#19"
   ],
   "depends_on": [
    "RV-AM",
    "AM-25"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-27",
   "repo": "AuraMaster",
   "title": "Comment hygiene: unfuse the Database doc blocks, re-wrap Container.lua, drop review-round narration, and fix the stale 500-line buffer",
   "change": "Comment-only. core/Database.lua:478-538: split the fused block into three doc comments, each directly above its own function: liftCategoryWhitelist (:596), categoriesDecided (:550) and filterableCategories (:539). Rewrite :521's 'CORRECTION (review, after this shipped once already wrong)' paragraph as a plain WHY. modules/Container.lua:414-416: re-wrap the merged ~150-character line into two sentences. Strip the review and fix-round framing and keep the reasoning at modules/FilterCompiler.lua:38,45,56,76,81, modules/Container.lua:195,490, core/Database.lua:724 and core/Constants.lua:243. core/DebugLogSetup.lua:5 'the 500-line buffer' becomes 'the 1500-line buffer', which is the library's MAX_BUFFER and the standard's number since WS-07 / LK-19. Line numbers may have shifted after AM-03..AM-26, so locate each site by its quoted text.",
   "tests": "none: comments only. `git diff -U0 | grep '^[-+]' | grep -vE '^[-+]\\s*--|^[-+]{3}'` must print nothing, which shows no code line changed. tests/test_docs.lua's citation gates stay green.",
   "verify": "grep -rnE 'fix round|review round|CORRECTION \\(review' core modules settings defaults -> nothing; grep -n '500-line' core/DebugLogSetup.lua -> nothing; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0; ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; the 1500-line check -> empty.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-26"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-28",
   "repo": "AuraMaster",
   "title": ".pkgmeta stops ignoring a .claude directory that does not exist; issue #10 gets its terminal label",
   "change": ".pkgmeta:12: replace `- .claude  # dev-only: agent tooling` with the packaging.md:19 commented form `#   - .claude  # only in a repo that has one`. Keep .superpowers at :13 because that directory exists. Separate GitHub write, no file change, run after the commit and spaced 5 s or more from any other gh call: `gh issue edit 10 -R tusharsaxena/AuraMaster --remove-label state:triaged --add-label state:done`. The issue is CLOSED (2026-09-22) with an open-status label (audit-review-history).",
   "tests": "none: packaging config plus one GitHub label repair. The packaging check (c) from AUDIT.md is the check.",
   "verify": "for p in $(grep -E '^\\s*- ' .pkgmeta | sed -E 's/^\\s*- ([^ #]+).*/\\1/'); do [ -e \"$p\" ] || echo FALSE CLAIM $p; done -> prints nothing; gh issue view 10 -R tusharsaxena/AuraMaster --json labels,state -q '[.state,[.labels[].name]]' -> CLOSED with state:done and no state:triaged; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-15",
    "AuraMaster-A-16"
   ],
   "issue_refs": [
    "AuraMaster#10"
   ],
   "depends_on": [
    "RV-AM",
    "AM-27"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-29",
   "repo": "AuraMaster",
   "title": "DEPENDENCIES.md: Python 3 and Pillow move to Release/assets, with install and check commands",
   "change": "DEPENDENCIES.md:13-14 summary table. Development keeps lizard's pipx route (:63-65), which brings its own Python, and drops only the spell-research generator mention. Release/assets changes from 'None.' to 'Python 3.8+ (the spell-research generator, tools/spell-research/research.py, which regenerates defaults/CastToAura.lua); Pillow (the 128 icon TGA). Not needed to build, run or test.' plus the network note. :117-133 gains Pillow's install line `sudo apt-get install -y python3-pil` and check line `python3 -c 'import PIL; print(PIL.__version__)'`, and python3's check `python3 --version` (documentation-\u00a77).",
   "tests": "none: docs only. tests/test_docs.lua's citation gates cover the file.",
   "verify": "grep -n 'python3-pil\\|import PIL' DEPENDENCIES.md -> both present; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-28"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-30",
   "repo": "AuraMaster",
   "title": "Name LibDBIcon's minimapPos as the third piece of named non-setting state",
   "change": "docs/ARCHITECTURE.md:216: 'two pieces' becomes 'three pieces', with a third bullet: storage key `global.minimap.minimapPos`, owner module core/LauncherSetup.lua (which hands `db.global.minimap` to LibKa0s-Launcher's `minimap` field), writer LibDBIcon-1.0 when the player drags the button (reached through the LibKa0s-Launcher seam), and no addon writer. docs/schema.md:39: add the owner module beside the key, and state that the CLI row is `global.minimap.shown` (AM-14) while `hide` and `minimapPos` are storage.",
   "tests": "none: docs only. tests/test_docs.lua's citation-within-3-lines gate checks any new file:line citation.",
   "verify": "grep -n 'minimapPos' docs/ARCHITECTURE.md docs/schema.md -> both name core/LauncherSetup.lua; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-29",
    "AM-14"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-31",
   "repo": "AuraMaster",
   "title": "Register docs/spell-research/ as a Tier 3 store and spell the revendor store the standard's way",
   "change": "docs/ARCHITECTURE.md '### Addon-specific (documentation-\u00a73, Tier 3)' (:817-819): replace 'None.' with a `| Doc | Covers |` table and one row: `spell-research/` | Frozen per-build derivation bundles written by tools/spell-research/research.py (dated bundles not enumerated). Documentation map prose (:778-781): drop `docs/spell-research/<date>/` from the out-of-scope list and respell `docs/revendor/<date>/` as `docs/revendor/<date>-v<tag>/`, adding that a span bundle is `<date>-v<A>-v<B>/` (WS-01). Optional upstream: proposing spell-research for the canonical frozen-store list is left to the owner and is not part of this commit.",
   "tests": "none: docs only. tests/test_docs.lua:115 'every .md under docs/ appears in the documentation map' must accept the Tier 3 row covering docs/spell-research/2026-09-20/{ANALYSIS,DIFF,SOURCES}.md. If the gate needs the store named as a directory row, extend its parser in this commit, with a red-first case showing that an unregistered docs/<store>/x.md fails.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 (test_docs green); grep -n 'spell-research' docs/ARCHITECTURE.md -> the Tier 3 row only.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-30"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-32",
   "repo": "AuraMaster",
   "title": "Screenshots register row re-stated for a published addon; Known Limitations and README stop saying unpublished; the options-ui-\u00a717 row is re-checked",
   "change": "docs/ARCHITECTURE.md register. The documentation-\u00a71 row (:826) cites item 4 (not 5), says the addon is published (X-Curse-Project-ID 1698345 since a326ed7, 2026-09-16) so Screenshots is a MUST, sets Decided to the owner's re-ratification date, and gets the re-check trigger 'retired when captioned images from media/screenshots/ land in README ## Screenshots (issue #3)'. OWNER DECISION: the row stands only if the owner re-ratifies it. Otherwise it is deleted and the finding stays open on issue #3. docs/ARCHITECTURE.md:744-745: delete the 'Unpublished' Known Limitations bullet. README.md:26-29: replace 'They'll come before the first release' with one line pointing at issue #3, with no promise tied to a release. Re-read the options-ui-\u00a717 row (:825, AuraMaster-A-17). Its two triggers have not fired: `grep -rn 'class' modules/Style.lua` shows no per-button engine class binding, and CM.MustDefer still gates applies. Leave it unchanged and say so in the commit body.",
   "tests": "none: docs only. tests/test_docs.lua:56 (README placeholders) and the register parser in the kit's test_layout_cap stay green.",
   "verify": "grep -n 'Unpublished' docs/ARCHITECTURE.md -> nothing; grep -n 'item 5' docs/ARCHITECTURE.md -> nothing; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "In client (issue #3, a follow-up commit once captured): capture a bar container, an icon container, the unlocked handle with Test mode on, and one image per settings page (General, Containers, Filters, Layout, Bars, Icons, Text, Profiles) into media/screenshots/; then add the captioned ## Screenshots section between the description and ## Usage, add `- media/screenshots` to .pkgmeta ignore, and delete the register row.",
   "finding_ids": [
    "AuraMaster-A-02",
    "AuraMaster-A-17"
   ],
   "issue_refs": [
    "AuraMaster#3"
   ],
   "depends_on": [
    "RV-AM",
    "AM-31"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "AM-33",
   "repo": "AuraMaster",
   "title": "docs/ARCHITECTURE.md hub: spill every mandated section over ~60 lines into its topic doc, moving content and deleting nothing",
   "change": "documentation-\u00a73 (a MUST for the 60-line spill; about 400 lines total is a SHOULD). Measure each section with awk over the '## ' headings first. Settings Schema (:107-244, ~137 lines): the detail goes to docs/schema.md, leaving a summary of 15 lines or fewer and exactly one link. Taint Notes (:488-585, ~98): detail to docs/midnight-quirks.md, with a summary and one link. Known Limitations (:586-775, ~190): detail to a new Tier 3 docs/known-limitations.md, registered in the Addon-specific table (AM-31) in the same commit, with a summary and one link. Non-mandated sections toward the ~400 SHOULD: Locale routing (:245-268) to docs/common-tasks.md or settings-panel.md; Filter priority (:270-318) to docs/data-flow.md; The disabled state (:444-486) to docs/data-flow.md; the Launcher detail (:391-419) to docs/settings-panel.md; the Overview library table (:36-74) to docs/module-map.md. Each leaves a 2-5 line summary and one link. Every file:line citation moves verbatim, and AM-34 re-checks them. Delete nothing: `git diff --stat` removed lines \u2248 added lines in the targets.",
   "tests": "none: docs only. tests/test_docs.lua (the map covers every docs .md, Tier 2 rows agree, citations resolve and sit within 3 lines of a backticked name) stays green. A moved citation that breaks the 3-line rule is fixed in this commit.",
   "verify": "wc -l docs/ARCHITECTURE.md -> about 400 or less; awk '/^## /{if(n)print s, n; s=$0; n=0; next}{n++} END{print s, n}' docs/ARCHITECTURE.md -> every mandated section 60 lines or fewer; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-32"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-34",
   "repo": "AuraMaster",
   "title": "Citation sweep over docs/ and the root docs, the README 0.1.0 row corrected, and the docs gate refuses a citation that lands on a comment or blank line",
   "change": "Re-resolve every file:line citation after AM-03..AM-33 moved code. The known-wrong ones: docs/ARCHITECTURE.md (or its spill target) Schema.lua:377->the CONFIG_CHANGED send (was :460), ContainerManager.lua:520->RegisterMessage(CONFIG_CHANGED) (was :535), :543->VISIBILITY_CHANGED (was :545), OptionsSetup.lua:380->the CONTAINERS_CHANGED handler (was :396), Style.lua:823-825->the one-click-phase code (was :826-828). DEPENDENCIES.md:118/:120: Constants.lua:25->C.LOGO_PATH (:26) and :31->C.LOGO_ICON_PATH (:32). DEPENDENCIES.md:22: ContainerManager.lua:568->the no-engine print_ (:572, shifted by AM-04). docs/performance.md:51: AuraMaster.lua:98->the OnUnitPet bracket (:115). performance.md:55: Style.lua:772-780->774-784. Use today's numbers, found with grep -n on the named symbol, not the ones above. README.md:156, the 0.1.0 history row (the current release, since there are no tags): say 'bars, icons or text', 'weapon enchants as a buff category', and 'Test mode', changing no other row. tests/test_docs.lua: strengthen the citation gate with a new case, 'docs: no file:line citation lands on a comment-only or blank line'.",
   "tests": "Red first: add the new tests/test_docs.lua case before fixing the citations. It must go red on at least the known comment-line citations (ARCHITECTURE Schema.lua:377, ContainerManager.lua:520, OptionsSetup.lua:380; DEPENDENCIES Constants.lua:25/:31, ContainerManager.lua:568). Mark it `-- red under: citations landing on '--' lines`. Then fix the citations and it goes green. Regenerate docs/test-cases.md and the badge.",
   "verify": "luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0 (all test_docs citation cases green); ka0s-bounded lua5.1 tests/perf.lua -> exit 0; lizard ... -C 15 -w . -> none; grep -n 'weapon enchant containers\\|preview mode' README.md -> nothing in the 0.1.0 row.",
   "smoke": "",
   "finding_ids": [
    "AuraMaster-A-04",
    "AuraMaster-A-05",
    "AuraMaster-R-12"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-33"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "AM-DOCS",
   "repo": "AuraMaster",
   "title": "Sync docs to the tree, regenerate the case inventory, and record a fresh automated-test bundle (no version bump, no tag)",
   "change": "(1) Run the /wow-addon:sync-docs pass over README.md, CLAUDE.md, DEPENDENCIES.md, docs/ARCHITECTURE.md and the topic docs. Re-verify every count claim: the suite count in tests/run.lua; the test Totals; NS.COMMANDS (22) against slash-dispatch.md; core/Compat.lua's shim count against compat-layer.md's '21 shims'; NS.MSG (4); luacheck's file count; the Tier 2 map triggers; the module-map rows, which gain settings/GeneralDispel.lua and, if AM-13 peeled it, defaults/UserCategories.lua; the Event Subscriptions section (SafeRegisterEvent, the UNIT_AURA unit frame, NS.RejectedEvents); the Options section, which says the library renders tabs; the degraded-panel section (composers {} and writeThrough); the minimap CLI path global.minimap.shown; schemaVersion 0; and the lizard band census (1000-1500 lines, for example defaults/Categories.lua and tests/test_anchors.lua). (2) Regenerate the inventory: `ka0s-bounded lua5.1 tests/run.lua --list > docs/test-cases.md`, then re-checkout for CRLF, and roll the README test badge to its Totals. (3) Record the battery: `tests/_kit/run-automated-tests.sh` (all four suites: lint, tests, perf, complexity), which writes docs/automated-tests/<YYYYMMDD-HHMMSS>/ and regenerates docs/automated-tests/RESULTS.md, replacing the stale 20260916-184324 figures. Write <stamp>/ANALYSIS.md per the AUTOMATED_TESTS.md prompt and fill a Disposition cell for every watch-list entry (the band files, and any perf delta from AM-06, AM-08 and AM-15). No version bump, no tag, no README Version History row: the owner decides the release.",
   "tests": "none: docs and the generated record. The four suites themselves are the check.",
   "verify": "docs/automated-tests/<stamp>/manifest.json -> lint pass, tests pass (N/0/N), perf pass, complexity pass with warnings 0; `ka0s-bounded lua5.1 tests/run.lua --list | diff - docs/test-cases.md` -> empty (modulo CR); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' -> empty; luacheck . -> 0/0; grep -c . docs/automated-tests/<stamp>/ANALYSIS.md -> non-zero.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-03",
    "AM-04",
    "AM-05",
    "AM-06",
    "AM-07",
    "AM-08",
    "AM-09",
    "AM-10",
    "AM-11",
    "AM-12",
    "AM-13",
    "AM-14",
    "AM-15",
    "AM-16",
    "AM-17",
    "AM-18",
    "AM-19",
    "AM-20",
    "AM-21",
    "AM-22",
    "AM-23",
    "AM-24",
    "AM-25",
    "AM-26",
    "AM-27",
    "AM-28",
    "AM-29",
    "AM-30",
    "AM-31",
    "AM-32",
    "AM-33",
    "AM-34"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-02",
   "repo": "KickCD",
   "title": "tests/run.lua stops swallowing an OnInitialize raise",
   "change": "tests/run.lua:105 `if initDB and NS.OnInitialize then pcall(NS.OnInitialize, NS) end` becomes `local ok, err = pcall(...)`, and the error is kept on the instance (inst.initError). loadInstance raises `error(\"OnInitialize raised: \" .. tostring(err), 0)` unless opts.allowInitError is set. The shared instance (:115 `loadInstance(true)`) therefore fails the whole run loudly. Rewrite the :106-108 comment, which currently calls the swallow deliberate. Before committing, run the suite under the strict version and list every case that depended on the swallow (expected: degraded-environment loads with libFiles = {}). Each such case passes { allowInitError = true } and asserts the error it expects, or the underlying init raise is fixed if it is a real defect. Record the list in the commit body. The other KICKCD-R-11 gaps land with their fixes: SetProfile in KC-03, the frame delta in KC-05, the throttle count in KC-10, the render count in KC-11 and the released-widget state in KC-12.",
   "tests": "Red first: add a case to tests/test_database.lua, 'a raising OnInitialize fails T.load instead of passing silently', with the line '-- red under: tests/run.lua pcall-swallowing OnInitialize'. It uses T.load(true, false, function(m) m.__libs['AceDB-3.0'].New = function() error('boom') end end) inside assertError, and a second call with { allowInitError = true } asserts inst.initError contains 'boom'. On HEAD the first call returns normally, so the case is red.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; total +2); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l tests/run.lua (< 1500); regenerate docs/test-cases.md with --list and move the README badge in the same commit.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-R-11"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-03",
   "repo": "KickCD",
   "title": "Migrations: schemaVersion 0 in defaults, the stamp advances only past a clean step, and the color and font-flag steps run for every profile",
   "change": "core/Database.lua. (1) WS-03: aceDBDefaults() (:70-72) declares `global.schemaVersion = 0`. Rewrite the comment: AceDB's removeDefaults strips a stamp equal to its default, and a current-version default also masks a legacy account, while 0 has neither problem. (2) The migrations table (:713-721) holds pure steps with no stamp writes: [1] FoldLegacyUnits, [2] MigrateSpecKeys, [3] MigrateColorShape, [4] MigrateFontFlags. (3) MigrateProfile (:734-767): keep the legacy profile.dbVersion adoption. `local v = g.schemaVersion or 0; if v < 1 then v = 1 end`, because 0 is a fresh or stripped-stamp account and every step is idempotent against a default profile. Loop: `local ok, err = pcall(step, self.db)`. On ok, `g.schemaVersion = v + 1`. On failure, log one NS.Util.print line 'settings migration v%d -> v%d failed: %s' plus NS.Debug('Migrate', ...), then break without advancing. Keep the missing-step jump. (4) KICKCD-R-01: Database:Init (:866-876) and OnProfileChanged (:821-824) call `self:MigrateColorShape(self.db)` and `self:MigrateFontFlags(self.db)` right after MigrateSpecKeys, so every profile is converted the first time it is active. MigrateColorShape gains `if not (db and db.profile) then return end`. Its walk becomes walk(t, d, depth), with d the NS.DEFAULT_PROFILE subtree at the same key. For a hybrid color, if the keyed part SameValue-equals d's keyed part (or d is absent), convert array -> keys as today. Otherwise the keys are a post-upgrade edit, so keep them and nil out [1]..[4]. State the rule in the docstring and in docs/schema.md's migration section. (5) KICKCD-A-08: docs/ARCHITECTURE.md:390 savedvariables-\u00a71 row 1 is rewritten, not retired. What differs names all five shape-driven steps (FoldLegacyUnits, BackfillLabelStyle, MigrateSpecKeys, MigrateColorShape, MigrateFontFlags) run on Init and every OnProfileChanged beside the version ladder. The trigger is restated as 'a sixth shape-driven step, or a LibKa0s per-profile migration runner'. Replace the schema.md#L256 anchor with the heading anchor for '### `units.<unit>.label.style` shape' (reasoning at schema.md:271). Row 2's schema.md#L266 becomes the '## Migration: folding legacy `icons`/`castbar`/`anchors` into `units.target`' anchor (reasoning at :281). ARCHITECTURE.md:468 load-order prose lists five steps. (6) tests/wow_mock.lua AceDB fake (:454-632): a real SetProfile(name). `db.__profiles` is seeded from the staged SV `profiles` table, and each profile is copyDefaults-merged on first access with its identity kept per name. SetProfile sets db.keys.profile, swaps db.profile and fires ('OnProfileChanged', db, name) through the existing fire(). GetCurrentProfile answers db.keys.profile, and GetProfiles lists the keys.",
   "tests": "Red first, in tests/test_color_shape.lua, each case carrying a '-- red under:' line. (a) 'a second stored profile has its positional colors converted when it becomes active': stage KickCDDB = { global = { schemaVersion = 5 }, profiles = { Default = {}, Alt = { units = { target = { castbar = { barColor = { 0.1, 0.2, 0.3, 1 } } } } } } }, T.load(true), NS.db:SetProfile('Alt'), then assert barColor.r == 0.1 and barColor[1] == nil. It is red on HEAD because migrations[3] is gated on the account stamp. (b) the same for a 'NONE' castbar fontFlags -> ''. (c) 'a hybrid whose keys differ from the default keeps its keys and loses its array'. (d) 'AceDB defaults declare schemaVersion 0' (capture the defaults argument through a mutate on AceDB.New). (e) 'a fresh install ends at v5 with no step raising'. (f) 'a raising step leaves the stamp where it was' (patch NS.Database.MigrateFontFlags to error, set g.schemaVersion = 4, call MigrateProfile, assert 4 and one printed failure line). Adapt tests/test_database.lua:26-31 and test_color_shape.lua:149-190 to the new stamp semantics.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +6 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output; MigrateProfile and walk stay at CCN 15 or below); wc -l core/Database.lua tests/wow_mock.lua (each < 1500); regenerate docs/test-cases.md and the README badge in the same commit.",
   "smoke": "In client: create a second profile in Settings -> Profiles, switch to it and back. The cast bar color swatch and the outline dropdown show stored values, never blank or defaults. /kcd debug on shows no 'migration ... failed' line.",
   "finding_ids": [
    "KICKCD-R-01",
    "KICKCD-A-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-04",
   "repo": "KickCD",
   "title": "Adopt Core.SafeRegisterEvent: every event block survives one bad name, with a rejected list, a debug verb and an [Init] note",
   "change": "LK-11 consumer follow-up. (1) core/State.lua: `State.rejectedEvents = {}` (session only). (2) core/CoreSetup.lua live arm: `NS.SafeRegisterEvent = lib.SafeRegisterEvent`, and likewise NS.SafeRegisterUnitEvent and NS.SafeRegisterEvents (Core minor 8). Stub arm: one-rung bodies, as the Core version-8 doc's Degradation note requires: pcall(target.RegisterEvent, target, event, handler), or RegisterUnitEvent with unit1/unit2, with no front gate, appending the name once to `rejected`. Also add one host helper, `NS.RegisterEventList(target, list)`, where list is an array of { event, handler }. It calls NS.SafeRegisterEvent(target, ev, handler, NS.State.rejectedEvents) and, on a first rejection of a name, logs NS.Debug('Events', 'rejected %s', name). Keep it at CCN 5 or below. (3) Route every AceEvent block through it using FILE-SCOPE lists, so a stand-up allocates nothing (AP #43): modules/Cooldowns.lua:544-555 RegisterLifecycleEvents (7 rows), modules/IconGrid.lua:935-949 (6 rows), modules/Castbar.lua:1081-1087 (3 rows), modules/UnitLabel.lua:253 (1 row), and settings/Spells.lua:1358-1364 RegisterPanelEvents (the three `ev:RegisterEvent` calls; the handlers are closures, so keep that list module-scope). (4) core/KickCD.lua DEBUG_COMMANDS gains {'events', 'List event names this client refused to register', ...}, which prints 'no rejected events' or one line per name. core/DebugLogSetup.lua initSummary (:166-172) appends ', %d rejected event(s)' only when the count is above 0, so the zero case stays byte-identical. (5) tests/wow_mock.lua: mocks.CreateFrame sets f.__mocks = mocks. FRAME_METHODS.RegisterEvent and RegisterUnitEvent (:385-394) raise `Attempt to register unknown event \"<NAME>\"`, the kit mock_base message, when the name is in self.__mocks.__badEvents, read at call time. The frame blocks are migrated in KC-05, KC-06 and KC-07. docs/slash-dispatch.md adds the debug events row.",
   "tests": "Red first, in a new tests/test_events.lua added to SUITES in tests/run.lua. (a) 'one bad name does not stop the rest of Cooldowns' block': T.load(true, true, function(m) m.__badEvents = { SPELL_UPDATE_USABLE = true } end). Assert that SPELL_UPDATE_CHARGES, PLAYER_ENTERING_WORLD and TRAIT_CONFIG_UPDATED are registered for the Cooldowns module (mocks.__registrations()) and NS.State.rejectedEvents == { 'SPELL_UPDATE_USABLE' }. It is red on HEAD because the raise aborts the block and the load. (b) the same with m.C_EventUtils = nil (the pcall rung). (c) the same with libFiles = {} (the stub bodies). (d) '/kcd debug events names the rejected event'. (e) 'the [Init] line is unchanged when nothing was rejected'. (f) 'a frame RegisterEvent honors __badEvents' (a mock-fidelity case). test_surface_parity's Core four-argument case covers the new NS members on both arms.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +6 cases incl. the new suite); lua5.1 tests/perf.lua (6 scenarios pass; castStart/probe pair unchanged); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -nE ':RegisterEvent\\(' modules/*.lua settings/Spells.lua (only inside NS.RegisterEventList callers' lists); wc -l on touched files (< 1500); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: /kcd debug events prints 'no rejected events' on live 12.1.x. /kcd debug on then /reload: the [Init] line has no rejected clause.",
   "finding_ids": [
    "KICKCD-A-03"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-05",
   "repo": "KickCD",
   "title": "One cast filter frame per (module, unit), armed and disarmed rather than rebuilt, carrying the EMPOWER events",
   "change": "(1) core/Util.lua:419-445: replace Util.RegisterUnitCastEvent with `Util.NewUnitCastFilter(module, unit, routes)`. It errors if any routes key does not start with 'UNIT_SPELLCAST_' (the helper's one job; events-frames-taint-\u00a71 forbids a general frame factory). It creates ONE frame whose OnEvent calls `module[routes[event]](module, event, evUnit, ...)` when that handler exists. It returns { frame = f, armed = false, Arm = fn, Disarm = fn }. Arm is idempotent: when not armed, it registers exactly the routes' fixed event set through NS.SafeRegisterUnitEvent(f, ev, NS.State.rejectedEvents, unit) and sets armed. Disarm calls f:UnregisterAllEvents() and clears armed. Nothing is allocated per arm. (2) modules/IconGrid.lua: a file-scope ICON_CAST_ROUTES maps the 8 existing events plus UNIT_SPELLCAST_EMPOWER_START, _UPDATE and _STOP to 'OnUnitCastEvent'. EnableUnit (:800-822) does `inst.castFilter = inst.castFilter or Util.NewUnitCastFilter(self, unit, ICON_CAST_ROUTES); inst.castFilter.Arm()`. DisableUnit (:828-835) and Suspend (:855-862) call castFilter.Disarm(). inst.eventFrames is deleted. (3) modules/Castbar.lua: a file-scope CASTBAR_CAST_ROUTES maps the 10 existing events plus EMPOWER_START -> OnChannelStart, EMPOWER_STOP -> OnCastStop and EMPOWER_UPDATE -> OnCastDelayed, wired the same way at :1016-1031, :1041-1047 and :1102-1113. The Resume guard at :1133-1139 becomes `if inst and inst.enabled and not (inst.castFilter and inst.castFilter.armed) then inst.enabled = false end`. Confirm OnChannelStart reads an empowered cast through Compat.GetCastingInfo/UnitChannelInfo; if it cannot, map EMPOWER_START to OnCastStart and say why in a comment. Keep Castbar.lua under 1500 (it is 1423). (4) Docs: the per-unit frames paragraph in docs/ARCHITECTURE.md (one frame per module per unit, re-armed and never rebuilt), the event lists in docs/castbar.md and docs/icon-grid.md, an EMPOWER note in docs/midnight-quirks.md, and the stale comments at tests/wow_mock.lua:381 and tests/test_perfsetup.lua:398.",
   "tests": "Red first. (a) tests/test_disabled.lua 'two disable/enable cycles create no frames': record #inst.mocks.__frames, run `/kcd disable` and `/kcd enable` twice, and also NS.Perf suspend/resume twice, then assert the delta is 0. It is red on HEAD with +36 per cycle. (b) tests/test_util_anchor.lua: the three RegisterUnitCastEvent cases are rewritten against NewUnitCastFilter (forwards, tolerates a missing handler, Disarm empties __events), plus new cases: 'Arm twice registers each event once', 'a non-UNIT_SPELLCAST route is refused', and 'a bad EMPOWER name is rejected and the other routes still arm'. (c) tests/test_castbar.lua 'UNIT_SPELLCAST_EMPOWER_START on target starts the bar and EMPOWER_STOP stops it'. It is red on HEAD because nothing is registered. (d) tests/test_icongrid_visibility.lua 'an empower start re-evaluates target_casting visibility'. tests/test_perfsetup.lua:398-450 is updated to count through castFilter.armed and __countFramesFor.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; about +7 cases); lua5.1 tests/perf.lua (castStart within its ceiling, and the probeOverheadOff/On pair unchanged); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l modules/Castbar.lua modules/IconGrid.lua core/Util.lua (each < 1500); grep -n 'RegisterUnitCastEvent\\|eventFrames' core modules tests/*.lua (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client, smoke C-03 step 4. Target an Evoker (PvP or a duel) and watch an empowered cast start and stop the bar and the glow; record it as untested if no Evoker is available. Then run /kcd disable and /kcd enable three times and confirm casts still drive the bar and grid, with no error.",
   "finding_ids": [
    "KICKCD-R-03",
    "KICKCD-R-06"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-04"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-06",
   "repo": "KickCD",
   "title": "core/SpellInput.lua: one resolver, the Cooldown Manager gate on the CLI, multi-word names, validated class and spec, and an AceEvent cache invalidator",
   "change": "(1) New core/SpellInput.lua (NS.SpellInput). Its TOC line goes right after core\\KickCD.lua with '# LOAD-BEARING POSITION: calls NS.NewBusTarget (core/KickCD.lua) at file load; everything else it reads resolves at call time.' It contains: SpellInput.Resolve(input), the single copy of core/KickCD.lua:570-584 resolveSpellInput and settings/Spells.lua:272-285 validateSpellInput. The Cooldown Manager cache moves from settings/Spells.lua:287-384 (cooldownViewerApi, collectCategorySpells, getCooldownManagerSpellSet, _cmCache/_CM_EMPTY, invalidate). SpellInput.IsLivePair(class, spec). SpellInput.Admissible(id, class, spec) returns true, or false plus the reason line 'Spell %s (#%d) is not tracked by the Blizzard Cooldown Manager for this specialization.', applied only when (class, spec) is the player's live pair, as the page does today. SpellInput.ParseTail(tokens), a greedy longest-prefix name match: 'Wind Shear' resolves before 'Wind'. Trailing tokens become CLASS and SPEC only if CLASS is a known class file token (walk GetClassInfo 1..GetNumClasses) and SPEC resolves for that class through Util.ResolveSpecID; otherwise it returns nil with 'Unknown class <x>' or 'Unknown spec <y> for <CLASS>'. The invalidator is an eager file-scope `local ev = NS.NewBusTarget()`; SpellInput.Arm() uses NS.RegisterEventList(ev, { {'TRAIT_CONFIG_UPDATED', invalidate}, {'PLAYER_SPECIALIZATION_CHANGED', invalidate} }) and runs at file load. SpellInput.StandDown() is ev:UnregisterAllEvents(), and SpellInput.StandUp() is `_cmCache = nil; Arm()`. core/LifecycleSetup.lua standDown and standUp call both beside SpellsPanel. This removes the settings/Spells.lua cacheEvents CreateFrame, the KICKCD-A-04 Spells half. (2) settings/Spells.lua: delete the moved code and call NS.SpellInput.*. Spells.StandDown and StandUp stop touching cacheEvents, and Spells.ValidateSpellInput stays as an alias of SpellInput.Resolve for the harness. (3) core/KickCD.lua: spellsAdd (:617-635) uses SpellInput.ParseTail and SpellInput.Admissible; resolveSpellInput is deleted; the bare `/kcd spells` line (:764) prints sd(spc). (4) core/Database.lua AddSpell (:350): refuse a class token that is neither a key of NS.DefaultSpells nor a GetClassInfo file token, answering nil, 'unknown class', without calling EnsureSpellList. (5) docs/ARCHITECTURE.md module-map row and docs/module-map.md row for core/SpellInput.lua.",
   "tests": "Red first. tests/test_slash.lua: (a) '`/kcd spells add Wind Shear` adds 57994 for a Shaman': mock GetSpellInfo('Wind Shear') -> id 57994 and player SHAMAN/262. It is red on HEAD, which answers 'Unknown spell: Wind'. (b) 'the CLI refuses a spell the Cooldown Manager does not track for the live spec' is red on HEAD. (c) '`spells add <id> WARLORD 99999` writes nothing' asserts profile.spells.WARLORD == nil; it is red on HEAD. (d) 'bare `/kcd spells` names the default spec by SpecDisplay' is red on HEAD. tests/test_spell_registry.lua: Resolve by id, by name and unknown; ParseTail multi-word plus class and spec; 'the CM cache is invalidated by TRAIT_CONFIG_UPDATED even when the Spells page was never built' (fire through mocks.__fireEvent). tests/test_disabled.lua keeps asserting an empty registration set while disabled, now including SpellInput's target.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; about +8 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output; ParseTail at CCN 15 or below); wc -l settings/Spells.lua core/KickCD.lua core/SpellInput.lua (Spells drops by about 100; all < 1500); grep -n 'CreateFrame' settings/Spells.lua (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client, as a Shaman: `/kcd spells add Wind Shear` adds it. `/kcd spells add <a non-CM spell>` prints the Cooldown Manager line. Switch spec with the Spells page closed, then open it: the Add box accepts only the new spec's CM spells.",
   "finding_ids": [
    "KICKCD-R-05",
    "KICKCD-R-18",
    "KICKCD-A-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-04"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-07",
   "repo": "KickCD",
   "title": "core/State.lua's combat listener becomes an AceEvent target, and the dead PLAYER_LOGIN re-registration goes",
   "change": "KICKCD-A-04 State half plus KICKCD-R-17. core/State.lua:143-146 `local boot = CreateFrame('Frame')` becomes `local boot = {}; LibStub('AceEvent-3.0'):Embed(boot)`. AceEvent-3.0 loads in the libs block before core/ and is not part of LibKa0s, so it exists on a degraded load. The OnEvent body becomes `local function onBootEvent(event) ... end` with the same three branches; the PLAYER_LOGIN branch calls boot:UnregisterEvent('PLAYER_LOGIN'). Registration moves out of file load into `State.Arm()`, called from NS:OnInitialize (core/KickCD.lua, after Database:Init; ADDON_LOADED always precedes PLAYER_LOGIN) through NS.RegisterEventList(boot, { {'PLAYER_LOGIN', onBootEvent}, {'PLAYER_REGEN_DISABLED', onBootEvent}, {'PLAYER_REGEN_ENABLED', onBootEvent} }), because NS.RegisterEventList (core/CoreSetup.lua) loads after State.lua. State.StandDown is boot:UnregisterAllEvents(). State.StandUp re-registers only the REGEN pair plus the InCombatLockdown seed: delete :206-213's `if not State.__seeded then boot:RegisterEvent('PLAYER_LOGIN') end` and State.__seeded. Rewrite the comments at :131-141, :176-193 and :199-205. docs/ARCHITECTURE.md:99 loses its private-frame argument: the addon now has no private frame for ordinary events, and events-frames-taint-\u00a71 carries no register row. tests/wow_mock.lua:520-524 and :762-767 comments no longer cite the State frame.",
   "tests": "Red first. tests/test_state.lua :58-107 stop reaching the listener through mocks.__findFrame and fire through mocks.__fireEvent('PLAYER_REGEN_DISABLED') etc. Add '(a) the combat listener is an AceEvent registration, not a frame': after T.load(true,true), mocks.__registrations() has PLAYER_REGEN_DISABLED of kind 'event' and no created frame has it in __events. It is red on HEAD. Add '(b) a stand-up never re-registers PLAYER_LOGIN': load disabled before login, `/kcd enable`, assert PLAYER_LOGIN is not in the registration set. It is red on HEAD. tests/test_disabled.lua's empty-set assertion stays green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -rn 'CreateFrame' core/State.lua (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: enter and leave combat with a visibility mode of 'In combat'; the grid shows and hides on the edges. /kcd disable, fight, leave combat, /kcd enable: the combat flag is correct (grid hidden out of combat).",
   "finding_ids": [
    "KICKCD-A-04",
    "KICKCD-R-17"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-04",
    "KC-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-08",
   "repo": "KickCD",
   "title": "IconGrid seeds a rebuilt icon from Cooldowns' current state instead of painting it ready",
   "change": "modules/Cooldowns.lua: add a read-only `function Cooldowns:StateFor(spellID) return self.watched and self.watched[spellID] end`, whose docstring says callers must not mutate it, and export it beside MasterEnabled (:719). modules/IconGrid.lua seedIcon (:340-353): add a file-scope `local READY_SEED = { ready = true, start = 0, duration = 0 }` and the call `local cd = NS:GetModule('Cooldowns', true); btn:Apply((cd and cd:StateFor(spellID)) or READY_SEED, true)`. The icon no longer depends on CallbackHandler's pairs-order dispatch between the two modules' PEW/SPELLS_CHANGED/TRAIT_CONFIG_UPDATED/PROFILE_CHANGED handlers. Rewrite the :345-350 comment. docs/icon-grid.md and docs/message-bus.md record the read-only pull (architecture-\u00a74: a query, not a second sender).",
   "tests": "Red first, in tests/test_icongrid_buildlist.lua: 'an icon rebuilt after Cooldowns already emitted keeps its cooldown'. Put a watched spell on cooldown in the mock (isActive true, not ready), run Cooldowns:Rebuild() BEFORE IconGrid:BuildActiveList(inst) (the losing dispatch order), then assert that button's last applied state has ready == false. It is red on HEAD, which reads ready = true. Also 'with no Cooldowns state the seed is ready' and 'every unwatched seed is the shared READY_SEED table' (no allocation).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +3 cases); lua5.1 tests/perf.lua (iconApply and spellState within their ceilings); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l modules/IconGrid.lua (< 1500); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client, smoke C-06: with an interrupt on cooldown, swap a talent (or summon a pet, or /reload): the icon keeps its cooldown swipe and does not flash ready.",
   "finding_ids": [
    "KICKCD-R-07"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-05"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-09",
   "repo": "KickCD",
   "title": "Every master-enabled reader goes through NS.MasterEnabled",
   "change": "modules/Cooldowns.lua:298-302 isEnabled becomes `return NS.MasterEnabled == nil or NS.MasterEnabled()`, resolved at call time (core/LifecycleSetup.lua defines it before modules/). The same change applies to modules/IconGrid.lua:133-136 and to core/Units.lua:35's first rung (`if NS.MasterEnabled and not NS.MasterEnabled() then return false end`, keeping the Units.Config check). The exports Cooldowns.MasterEnabled (:719) and IconGrid.MasterEnabled (:1343) stay as aliases. The Cooldowns early returns at :311 and :408 stay, commented as defense in depth behind the latch that direct test calls reach. core/LifecycleSetup.lua:59's 'THE one reader' claim becomes true; leave the text and add 'every module reader routes here'.",
   "tests": "Red first, in tests/test_cooldowns_gates.lua: 'module readers answer what NS.MasterEnabled answers'. Leave profile.enabled = true and stub NS.MasterEnabled to return false, then assert Cooldowns.MasterEnabled(), IconGrid.MasterEnabled() and NS.Units.IsEnabled('target') are all false. It is red on HEAD, where they read the profile. tests/test_source_style.lua: 'no module reads profile.enabled directly' greps modules/*.lua and core/Units.lua for '%.enabled ~= false' on a profile variable and must be empty; it is red on HEAD with 3 hits.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (spellPoll unchanged); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-R-13"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-08"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-10",
   "repo": "KickCD",
   "title": "scheduleTimer returns a real handle (C_Timer.NewTimer), belt and braces behind LibKa0s OptionsWidgets 31",
   "change": "settings/OptionsSetup.lua:243: `scheduleTimer = function(fn, delay) return _G.C_Timer.NewTimer(delay, fn) end`. Rewrite the :240-242 comment: since LibKa0s v1.56.0 (OptionsWidgets minor 31, LK-26) the library keeps its own armed flag and ignores the return value. The handle is returned anyway, so a payload older than that still throttles, and C_Timer.After would hand back nil.",
   "tests": "Red first, in tests/test_options_panel.lua. (a) 'the descriptor's scheduleTimer answers a cancelable handle' asserts that the returned value has a Cancel method. It is red on HEAD because After returns nil. (b) 'a color drag commits once per throttle window' is a characterization case, green behind v1.56.0 and red under the pre-LK-26 payload: render a page with a color row through the kit AceGUI fake, fire the picker's OnValueChanged 10 times without flushing, count descriptor set calls, flush with mocks.__flushTimers(), and assert exactly 1 commit.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client, with /kcd debug on: drag the cast bar color picker for 2 s. The console shows one [Set] line for the path, and the frame rate holds.",
   "finding_ids": [
    "KICKCD-R-04"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-11",
   "repo": "KickCD",
   "title": "Spells page: one render per commit, and a raise in the render can no longer latch the guard",
   "change": "settings/Spells.lua doCommit (:404-407): `if NS.IsDown and NS.IsDown() and panel and panel:IsShown() then Spells:RefreshRows() end; FireConfigChanged()`. While the addon is up, the CONFIG_CHANGED subscriber (:1345-1350) renders once. While it is stood down that subscriber is unregistered (Spells.StandDown), so the direct render is the only one. RefreshRows (:1179-1244): the body moves into a local renderRows(), and RefreshRows becomes `if not panel or not panel:IsShown() or rebuildScheduled then return end; rebuildScheduled = true; local ok, err = pcall(renderRows); rebuildScheduled = false; if not ok then error(err, 0) end`. The three explicit resets and the :1186-1188 'cleared on EVERY exit' comment go. The rule_ref is correctness, not slash-commands-\u00a77.",
   "tests": "Red first, in tests/test_settings_spells_editor.lua. (a) 'one commitSoon flush renders the Spells page once' counts H.PageHeader calls around a toggle plus mocks.__flushTimers(), asserting 1. It is red on HEAD (2). (b) 'a raising render does not latch the guard': make H.EnsureScroll raise once, assertError(Spells.RefreshRows), restore it, call RefreshRows, and assert PageHeader ran. It is red on HEAD because the second call returns early. (c) 'while stood down a commit still repaints the open page'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +3 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-R-08"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-12",
   "repo": "KickCD",
   "title": "Spells rows stop hooking pooled AceGUI frames, the category tooltip works, the remove button uses a catalog mark, and the ReorderList drag is pinned",
   "change": "settings/Spells.lua. (1) rowNameLabel (:655-668) uses AceGUI:Create('InteractiveLabel') with SetCallback('OnEnter', function(w) showSpellTooltip(w) end), SetCallback('OnLeave', hideSpellTooltip) and SetHighlight(nil). The label.frame EnableMouse/HookScript lines go; AceGUI's Release clears widget callbacks. (2) rowCategoryDropdown (:763-770): the dd.frame HookScript pair is replaced by `H.AttachTooltip(dd, L['Category'], L['Category for future filtering. Currently informational only.'])`, adding the 'Category' key to locales/enUS.lua if it is absent. (3) rowRemoveButton (:797-798) passes `image = NS.Icon and NS.Icon('close')` with `atlas = 'transmog-icon-remove'` only when NS.Icon returns nil (degraded). makeRowIconBtn (:590-622) tries opts.image before opts.atlas and takes an optional opts.tint {r,g,b}, applied through SetVertexColor when enabled, because catalog marks are white in the alpha. Pass a red tint so the glyph keeps its meaning. Use 'clear' if 'close' reads wrong in the smoke check. Rewrite the :563-567 builder comment, which says the atlas is required. (4) LK-21 consumer pin: a drag must never touch a host row frame's OnUpdate (Widgets minor 10). tests/wow_mock.lua FRAME_METHODS.HookScript records every call in self.__hooks so the suite can see it.",
   "tests": "Red first, in tests/test_settings_spells_editor.lua. (a) 'no Spells row widget hooks its pooled frame': render the list, then assert that no frame created under the page recorded a HookScript (__hooks empty) and that the name widget's type is 'InteractiveLabel'. It is red on HEAD. (b) 'hovering the category dropdown shows the category tooltip' fires the widget's OnEnter callback and checks the GameTooltip text. It is red on HEAD because the frame hook never fires. (c) 'the remove button draws the catalog mark, and the atlas only without LibKa0s' covers both the live load and libFiles = {}. (d) 'a reorder drag never writes a row frame's OnUpdate' is a characterization case, green on v1.56.0: start a drag through the ReorderList handle, then assert row.frame:GetScript('OnUpdate') == nil.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +4 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -n 'HookScript\\|transmog-icon-remove' settings/Spells.lua (only the degraded atlas fallback); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client, the LK-21 smoke on the Spells ReorderList (settings/Spells.lua ~:1001): drag row 3 above row 1. The drop line draws in the list color, the order persists after /reload, and no stray line is left after Esc mid-drag. Hover a spell name: the spell tooltip shows. Hover a category dropdown: the category tooltip shows. Close Settings, open another Ka0s addon's panel (e.g. /at config) and hover its labels: no KickCD spell tooltip appears and clicks land. The remove glyph reads as 'remove'.",
   "finding_ids": [
    "KICKCD-R-02",
    "KICKCD-A-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-11"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-13",
   "repo": "KickCD",
   "title": "Peel the Spells row builders into settings/Spells_Rows.lua (layout-\u00a71 band, behavior-neutral)",
   "change": "Mechanical split, with behavior and comments unchanged. MOVE alignIconArt, makeRowIconBtn, rowSpellIcon, rowNameLabel, rowEnableCheck, rowKnownGlyph, rowSpacer, rowCategoryDropdown, rowRemoveButton, buildRow and ROW_HEIGHT (settings/Spells.lua ~:555-835 after KC-06, KC-11 and KC-12) into the new settings/Spells_Rows.lua, which publishes Spells.BuildRow and Spells.ROW_HEIGHT. The builders' upvalues (writer, commitSoon, getSpellName, getSpellIcon, categoryList, L, the W handle-width read) reach them through a `Spells.__rowDeps` table that settings/Spells.lua fills at file end. Spells.lua calls Spells.BuildRow at call time. KickCD.toc adds settings\\Spells_Rows.lua right after settings\\Spells.lua with '# LOAD-BEARING POSITION: takes NS.Settings.SpellsPanel (settings/Spells.lua) as a file-scope upvalue.' Add the docs/module-map.md row. This is the fix half of KICKCD-R-10; the watch-list dispositions are KC-28 and KC-DOCS.",
   "tests": "None new: this is a behavior-neutral peel. tests/test_settings_spells_editor.lua and tests/test_settings_spells.lua must pass unchanged, byte-identical, which is the gate.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; count unchanged); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l settings/Spells.lua settings/Spells_Rows.lua (Spells about 1100 or below, Rows about 300); git diff --stat tests/ (empty).",
   "smoke": "In client: open the Spells page. Rows, drag, add and remove look and behave as before.",
   "finding_ids": [
    "KICKCD-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-12"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-14",
   "repo": "KickCD",
   "title": "Perf nesting tells the truth on the Rebuild path, and docs/performance.md's bucket table matches the descriptor",
   "change": "(1) modules/Cooldowns.lua Rebuild (:327-339): bracket the per-spell SPELL_STATE SendMessage as `rebuildEmit` (__e0 = Perf.on and debugprofilestop(); Perf.Note('rebuildEmit', dt)) and add `rebuild = true` to the existing payload literal (a constant field, no extra allocation). (2) core/PerfSetup.lua buckets (:99-110): add `{ key = 'rebuildEmit' }`, a root bucket commented 'Cooldowns:Rebuild's publish, outside any poll'. spellState stays declared within stateEmit, which is the steady-state path. (3) modules/IconGrid.lua:994: `Perf.Note('spellState', dt, payload.rebuild and 'rebuildEmit' or 'stateEmit')`, so LibKa0s-Perf reports spellState's Rebuild-path parent honestly (observedMixed) instead of silently claiming stateEmit. (4) docs/performance.md:36-55: regenerate the bucket table from the PerfSetup descriptor with stateEmit, glowGate and rebuildEmit rows, and delete the 'glowGate deliberately not declared' paragraph. docs/message-bus.md: the SPELL_STATE payload gains `rebuild`.",
   "tests": "Red first, in tests/test_perfsetup.lua. (a) 'stateEmit's observed parent is spellPoll' arms Perf, drives Cooldowns:Refresh, then reads the stateEmit bucket's observed parent. (b) 'every spellState note names its real parent' drives Rebuild and Refresh and asserts spellState's observed parents are exactly { stateEmit, rebuildEmit }, with observedMixed set. It is red on HEAD because the note passes no parent. (c) 'the descriptor declares rebuildEmit as a root'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +3 cases); lua5.1 tests/perf.lua (spellState and spellPoll within their ceilings, and the probeOverheadOff/On pair within its margin); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "Optional in client: /kcd perf start, fight about 30 s, change talents once, /kcd perf finish. The report's nesting lines show spellState under stateEmit plus the rebuildEmit mix. Record it later through /wow-addon:perf-analysis.",
   "finding_ids": [
    "KICKCD-R-09"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-08"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-15",
   "repo": "KickCD",
   "title": "/kcd resetposition restores every unit's icon grid; README stops promising a deferred /kcd config",
   "change": "settings/Panel_Render.lua Helpers.ResetIconPosition (:358-377) loops NS.Units.LIST, restoring each unit's anchors.icons from NS.DEFAULT_PROFILE.units[u].anchors.icons and skipping a unit with no default, then fires FireConfigChanged('general') once. Rewrite its header comment. Cast bars that move freely keep their place, and the comment says so. core/KickCD.lua:239 help text: 'Restore the icon grids to their default screen positions'. README.md:97 becomes 'Why won't the settings panel open in combat? | The game blocks it mid-fight. Run `/kcd config` again once combat ends.' README.md:113 becomes 'On purpose: the game blocks it in combat. Run `/kcd config` again after the fight.' The README FAQ at :91 and the troubleshooting rows at :118 and :119 say resetposition puts both icon grids back. Give the edited lines a de-AI pass (documentation-\u00a71).",
   "tests": "Red first, in tests/test_util_anchor.lua: '/kcd resetposition restores the focus grid too'. Drag focus's anchors.icons away, run the verb, and assert it equals DEFAULT_PROFILE.units.focus.anchors.icons. It is red on HEAD because focus is untouched. Also 'the target grid is still restored'.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -n 'waits until combat\\|opens the moment combat' README.md (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: /kcd unlock, drag both grids, /kcd lock, /kcd resetposition. Both grids jump to their defaults. General -> Reset position does the same.",
   "finding_ids": [
    "KICKCD-R-14"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-16",
   "repo": "KickCD",
   "title": "GateHint asks a gated row's valuesFor instead of writing the live profile",
   "change": "settings/Castbar.lua: each growDirection row carrying `valueGate` gains `valuesFor = function(gateValue) return <the same per-orientation keyed table its values() reads> end`, pure, sharing the file-scope table so nothing is duplicated. settings/Slash.lua NS.Slash.GateHint (:109-137): when row.valuesFor is present, loop the gate row's candidates, calling row.valuesFor(candidate) and collecting sorted keys, with no profile write. The existing pcall swap-and-restore probe stays only as the fallback for a row without valuesFor. Update the doc comment at :90-108. This lands before KC-18 deletes H.Resolve, so the fallback moves to Schema's Read and Write there.",
   "tests": "Red first, in tests/test_color_shape.lua: 'GateHint never writes the profile when the row declares valuesFor'. Wrap the gate's parent table in a proxy whose __newindex records writes (substitute via H.Resolve), call GateHint on the growDirection row, and assert 0 writes and the same hint text as before. It is red on HEAD, where the probe writes. The existing 'raising values() restores the gate' case still passes for a fallback row.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +1 case); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-R-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-17",
   "repo": "KickCD",
   "title": "The minimap row's CLI path reads in its own sense: global.minimap.shown, stored as minimap.hide",
   "change": "WS-06 consumer follow-up (launcher-\u00a73 v2.65.0). settings/General.lua:76 `minimapPath = 'global.minimap.shown'`. settings/Panel.lua GLOBAL_PATHS (:111-122): the key becomes 'global.minimap.shown'; get still answers `not hide` and set still writes `hide = not shown` plus NS.Launcher:SetShown. settings/OptionsSetup.lua:79-88: MINIMAP_HIDE_PATH becomes MINIMAP_SHOWN_PATH = 'global.minimap.shown', and the comments (`/kcd reset global.minimap.shown`) follow. Also update the core/LauncherSetup.lua:65 comment, the test_launcher.lua and test_schema.lua path strings, docs/ARCHITECTURE.md:207 and :304, docs/settings-panel.md:87, docs/module-map.md:312 and any docs/slash-dispatch.md example. Storage stays LibDBIcon's minimap.hide: no migration and no schemaVersion bump. Direct assertions on db.global.minimap.hide stay.\n\nMinimap rename carry-over (WS-06): the stored LibDBIcon key db.global.minimap.hide does NOT move, so every existing player keeps their setting with no SavedVariables migration and no schema-version bump (a stored `shown` key would violate anti-pattern #81). Pin it with a test written first: a legacy store with minimap = { hide = true, minimapPos = 200 } reads `<slash> get <path>.minimap.shown` as false, the button stays hidden, minimapPos is untouched, and no `shown` key is ever written to the raw SV after a set. Record the player-facing CLI rename (`...minimap.hide` -> `...minimap.shown`, old path answers unknown setting) in the commit body for the next Version History roll.",
   "tests": "Red first, in tests/test_launcher.lua: '`/kcd get global.minimap.shown` answers true while minimap.hide is false' and '`/kcd set global.minimap.shown false` stores hide = true'. Both are red on HEAD ('Setting not found'). The existing inversion, reset-exemption and page-Defaults cases are retargeted to the new path; tests/test_schema.lua:668/701/707 are renamed.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -rn 'minimap\\.hide\"' core settings (empty; only db reads of .hide remain); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: untick General -> Master controls -> Minimap button. The button hides, /kcd get global.minimap.shown prints false, and it survives /reload and Reset all settings.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02",
    "WS-06"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-18",
   "repo": "KickCD",
   "title": "Adopt LibKa0s-Schema-1.0 (minor 2) as the settings seam, with writeThrough for the composed master rows",
   "change": "KickCD#22 plus the LK-22 and LK-23 consumer follow-ups, following the version-2 adoption note. (1) New settings/SchemaSetup.lua. TOC: first line of the settings block, before settings\\Slash.lua, with '# LOAD-BEARING POSITION: creates NS.Settings.Schema (the rows array) and NS.Settings.Store, which both descriptors and every page file's add/AddComposed reach at load'. `NS.Settings.WRITE_THROUGH = { 'enabled', 'locked' }` is defined once. `NS.Settings.Store = SchemaLib:New(d)`, where SchemaLib = LibStub('LibKa0s-Schema-1.0', true) or HostSchemaStub. The descriptor d: rows = NS.Settings.Schema; resolveRoot = function() return NS.db and NS.db.profile, 1 end; writeThrough = NS.Settings.WRITE_THROUGH. announce(row, path, value): return for row.sessionOnly or row.get rows (as today, session and global writes send nothing); otherwise `local section = row.section or (row.writeThrough and 'general')`; if row.writeThrough and path == 'enabled', call NS.RefreshEnabledHold(); then Helpers.FireConfigChanged(section). announceBatch(writes) fires each distinct section once in first-seen order, replacing Helpers.Coalesced. debug is the existing per-path trailing debounce (logSet, flushPendingSets and fmtSetValue move here verbatim) for the single-write 'Set' line; any other line flushes pending lines first and then goes to NS.Debug. debugEnabled = function() return NS.State and NS.State.debug end; format = fmtSetValue; print = NS.Util.print; resetExempt = { ['global.minimap.shown'] = true }, which replaces survivesEveryReset. (2) Row wiring, all in settings/General.lua beside the visibility override (:98). The composed 'enabled' row gets `onChange = function() NS.RefreshEnabledHold() end`, which replaces Panel.lua:417. 'state.debugConsole' gets SESSION_PATHS' get/set closures. 'global.minimap.shown' gets GLOBAL_PATHS' get/set closures. units.focus.link (:152-160) gets a `set` closure that records whether the stored link moved before storing, and its onChange(value) calls H.RefreshAllPanels() only when it moved (Schema's onChange has no old value). settings/Castbar.lua:113-116's onChange writes through Store.Set('units.'..unit..'.castbar.growDirection', newGrow). (3) Seam bindings. Options descriptor (settings/OptionsSetup.lua): get = Store.Get (NS.Settings.ReadForPanel stays bound to it); set = function(p, v) local ok, e, w = Store.Set(p, v); if ok then H.RefreshScalars() end; return ok, e, w end; applyDefault through Store.ApplyDefault plus RefreshScalars; bulkBegin = flushPendingSets then Store.BulkBegin; bulkEnd = Store.BulkEnd; resetProfile = Store.ResetCounted(function() NS.db:ResetProfile() end, function(row) return row.panel ~= 'profiles' end); validate = Store.Validate({ defaultsRoot = function() return NS.DEFAULT_PROFILE, 1 end }). Slash descriptor: get = Store.Get; set = Store.Set, whose `false, err, why` Slash minor 15 prints; findRow = Store.FindRow; applyDefault = Store.ApplyDefault; bulk pair = Store's. Helpers.SetAndRefresh(path, v) stays as a two-line host helper (Store.Set, then RefreshScalars on ok, answering ok). core/Units.lua CopyStyling (:116-155): H.SetRows becomes Store.SetMany(entries, { act = 'copy', scope = from..'\u2192'..to }), which is all-or-nothing; H.Get and H.FindSchema become Store.Get and Store.FindRow. core/Database.lua consumeResetCount calls Store.ConsumeResetCount. settings/Slash.lua GateHint's fallback probe uses SchemaLib.Read/Write on NS.db.profile. (4) Deleted. From settings/Panel.lua: Resolve, SESSION_PATHS, GLOBAL_PATHS, Helpers.Get, pending/NIL_SECTION/Coalesced, sameValue, bulkEnter/bulkLeave/logAct/MuteSetLog/BulkBegin/BulkEnd, pendingResetCount/ConsumeResetCount/countChangedProfileRows/ResetProfileCounted, Helpers.Set, Helpers.FindSchema (callers move to Store.FindRow), ValidateSchema with _validPanels/_validSections/_validTypes/_printSchemaError. From settings/Panel_Render.lua: writeRow and Helpers.SetRows. Helpers.AddComposed stamps and then calls Store.AddRows, and each page's add{} calls Store.AddRows({row}). Helpers.FireConfigChanged and SchemaForPanel stay. (5) HostSchemaStub, in SchemaSetup.lua, follows the version-2 doc's 'The degradation stub': write-completing and log-silent. Lib level: SplitPath, Read, Write, SameValue and New(d). Instance: AllRows, AddRows, FindRow, Reindex, Get, Set, SetMany, Default, ApplyDefault, BulkBegin, BulkEnd, BulkRun, BulkAdd, InBulk, CountOffDefault, ResetCounted, ConsumeResetCount and Validate. Set honors d.writeThrough, storing a copy at the path and announcing with a synthetic { path = p, writeThrough = true } built once per path, and refuses every other row-less path in the host's own words (L['Setting not found: %s']). No STRINGS copy. (6) Behavior pins from the adoption notes: an unknown path is refused, a table value is copied in, a raising onChange propagates (it was pcall-printed), and group is required on every row. The panel and section enum check moves from runtime into tests/test_schema.lua. (7) Docs: docs/settings-panel.md (write seam), docs/schema.md, docs/ARCHITECTURE.md (Settings schema, External dependencies: one more adopted major), docs/module-map.md (SchemaSetup row), docs/slash-dispatch.md. The commit body says 'Closes #22' (the owner closes it on merge).",
   "tests": "Red first, in a new tests/test_schema_store.lua added to SUITES. (a) 'the settings seam is a LibKa0s-Schema-1.0 instance' (NS.Settings.Store.Set exists, Helpers.Set == nil); red. (b) 'a write announces CONFIG_CHANGED once with the row's section' (spy NS.SendMessage). (c) '`/kcd set enabled false` stands the addon down in the same turn'. (d) 'Copy styling is one SetMany: one [Set] copy target\u2192focus: N rows line, each section announced once'. (e) 'a bad value in a batch writes nothing'. (f) 'an unknown path is refused, never stored'. (g) 'a stored table is a copy'. (h) 'a raising onChange propagates after the store'. (i) degraded, libFiles = {}: 'Store.Set(\"enabled\", false) writes through and takes the disabled hold, with no Lua error'; 'Store.Set on any other composed path is refused'. tests/test_surface_parity.lua adds T.assertSurfaceParity(liveStore, stubStore, 'schema instance vs host stub') and T.assertSurfaceParity(HostSchemaStub, 'LibKa0s-Schema-1.0', { 'STRINGS' }), and tests/run.lua's Kit.setSurfaceSource map gains ['LibKa0s-Schema-1.0']. tests/test_schema.lua: 'every row's panel and section are known', 'Validate reports 0 errors on the shipped schema', 'every stored row resolves against DEFAULT_PROFILE'. Migrate the existing cases that call Helpers.Get/Set/FindSchema/SetRows/MuteSetLog/ResetProfileCounted/Coalesced to Store.*.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; about +14 cases); lua5.1 tests/perf.lua (6 scenarios pass, and panel reads stay off the hot path); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l settings/Panel.lua settings/SchemaSetup.lua settings/Panel_Render.lua (all < 1500; Panel.lua shrinks by about 300); grep -rn 'Helpers\\.Set(\\|H\\.Set(\\|Helpers\\.Get(\\|H\\.Get(\\|ValidateSchema\\|SetRows\\|MuteSetLog\\|FindSchema' core settings modules (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: open every settings page, edit one value per widget type, and /reload to confirm each value persisted. With /kcd debug on, drag a color: one debounced [Set] line. General -> Units -> Copy styling from Target: one '[Set] copy target\u2192focus: N rows' line. Untick Enable KickCD: the grid and bar stand down at once. Reset all settings: one profile-reset line. `/kcd set icons.nope 1` prints 'Setting not found'.",
   "finding_ids": [],
   "issue_refs": [
    "KickCD#22"
   ],
   "depends_on": [
    "RV-KC",
    "KC-03",
    "KC-16",
    "KC-17"
   ],
   "effort": "L",
   "milestone": "M3"
  },
  {
   "id": "KC-19",
   "repo": "KickCD",
   "title": "Degraded Slash stub: DisabledLine pinned to the library constant, the library-absent line, and enable/disable/lock that write through",
   "change": "LK-18 and WS-02 consumer follow-ups. settings/Slash.lua stub (:200-285). (1) `local DISABLED_LINE_FORMAT = '%s is disabled \\226\\128\\148 enable it with |cFFFFFF00%s|r'` holds the verbatim bytes of LibKa0s-Slash-1.0's DISABLED_LINE_FORMAT (the Slash version-15 doc's 'The degradation stub'), exposed as stub.__disabledLineFormat for the pin; the `__` prefix keeps it outside parity. stub.DisabledLine = function() return DISABLED_LINE_FORMAT:format(d.brandName or d.slash, d.slash .. ' enable') end. stub.OnSlash refuses a verb outside d.liveVerbs with DisabledLine while d.isEnabled() is false, keeping dispatch minimal. Rewrite the :230-239 'answers nil' comment. (2) The library-absent line: locales/enUS.lua gains L['%s is unavailable: the LibKa0s library did not load.'] (WS-02, one sentence and one placeholder). absent(verb) prints it formatted with '/kcd ' .. verb, replacing the ' is unavailable. ' .. NS.LIBKA0S_MISSING concatenation. (3) stub.CliSet(rest): split into path and value. When the path is in NS.Settings.WRITE_THROUGH and the value is a bool literal (true/false/on/off; a literal check, not a copy of the library parser), it calls NS.Settings.Store.Set(path, bool) and prints `path .. ' = ' .. tostring(bool)` on success. Every other path, and any failure, prints the library-absent line for '/kcd set' and writes nothing, never raising. The enable and disable verbs (core/KickCD.lua:219-222, setSetting) are unchanged and so take route (a). (4) core/KickCD.lua setLocked (:156-161) writes through `NS.Settings.Store and NS.Settings.Store.Set('locked', v)` and then calls H.RefreshScalars if present. lock, unlock and toggle therefore work on a library-absent load; 'Settings layer not ready yet' stays for a nil Store. Rewrite the comment at :138-155. docs/slash-dispatch.md gets a degraded-verbs section; docs/ARCHITECTURE.md's Slash paragraph is updated.",
   "tests": "Red first, in tests/test_slash.lua and tests/test_disabled.lua, on the degraded arm (T.load(true, true, nil, { libFiles = {} })). (a) 'the stub's DisabledLine format is the library constant, byte for byte': T.assertLibraryConstant(NS.Slash.cli.__disabledLineFormat, 'LibKa0s-Slash-1.0', 'DISABLED_LINE_FORMAT'), using the live source registered in tests/run.lua. It is red on HEAD (nil). (b) 'degraded `/kcd disable` writes enabled = false, stands down, prints the confirmation and raises nothing'; red on HEAD ('set is unavailable', enabled unchanged). (c) 'degraded `/kcd enable` brings it back up'. (d) 'degraded `/kcd lock` writes locked'; red on HEAD ('Settings layer not ready yet'). (e) 'degraded `/kcd list` prints the library-absent line' (wording pin; red on HEAD). (f) 'degraded `/kcd set visibility always` writes nothing and prints the library-absent line'. (g) 'degraded `/kcd lock` while disabled prints the DisabledLine'. Update tests/test_options_panel.lua's 'the hollow composers cost the degraded path no CLI reach' case to the new reach.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; about +7 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); wc -l settings/Slash.lua (< 1500); regenerate docs/test-cases.md and the README badge.",
   "smoke": "Optional in client, degraded build: rename libs/LibKa0s in a scratch copy of the addon and /reload. /kcd disable then /kcd enable toggle the addon with chat lines and no Lua error. /kcd list prints '/kcd list is unavailable: the LibKa0s library did not load.' Restore the folder afterwards.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-18"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-20",
   "repo": "KickCD",
   "title": "Evaluate RenderTabbedSchema's opts for the linked-Focus page; adopt it or record a decline",
   "change": "LK-28 consumer follow-up for settings/Panel_Render.lua:190-240 (RenderLinkedUnit plus deadenStrip at :150-189). Try `Helpers.RenderTabbedSchema(ctx, panelKey, afterGroup, nil, { disabledFor = function() return NS.Units.IsLinked(ctx.unit) end, disabledNotice = function(c) Helpers.LinkRow(c, <the same three L[] pieces>, open, tip) end, chrome = function(c) <desaturate the strip> end })` from RenderUnitPanel (:130-141). ADOPT only if all of the following hold: (a) the strip still draws every group; (b) the notice renders in place of the rows through the disabledNotice function; (c) the strip can be made inert, either through chrome(ctx) reaching the library's strip buttons or because the library disables them itself. If adopted, delete RenderLinkedUnit and deadenStrip, and RenderUnitPanel calls RenderTabbedSchema with the opts; the stub list in settings/OptionsSetup.lua needs no change. If any check fails, keep the host strip, add a comment naming the gap, and file `gh issue create -R tusharsaxena/KickCD --title 'Linked-Focus page keeps its host-drawn strip: RenderTabbedSchema opts cannot <gap>' --label enhancement --label state:will-not-do --label severity:low`, with the body citing LibKa0s docs/api/Options version-24.31.4.7.4 and this file:line. Close it at once as a recorded decline, and cite the issue in the comment and in docs/settings-panel.md. The commit is either the adoption or the comment plus the doc note.",
   "tests": "Characterization first, in tests/test_settings_refreshers.lua or test_options_panel.lua: 'a linked Focus page draws the full strip, inert, and only the link note'. It asserts the tab count equals the group count, every tab is disabled or desaturated, one LinkRow exists and no schema widget is present. It must pass before and after, whichever branch is taken.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +1 case); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); if declined: gh issue view <n> -R tusharsaxena/KickCD shows CLOSED with state:will-not-do; regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: Settings -> Icons (then Cast bar, then Text Label) -> Unit picker Focus while linked. The strip is present and greyed, the note's link opens General -> Units, and unlinking restores the editable tabs.",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-18"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-21",
   "repo": "KickCD",
   "title": "Style gates: no `or _G.print` fallback, and no deprecated spell/spec global whitelisted for shipped code",
   "change": "The three unreachable arms: core/KickCD.lua:127 `local fn = self.Util.print`, core/Compat.lua:441 `NS.Util.print`, and modules/Cooldowns.lua:661 `local p = NS.Util.print`. core/CoreSetup.lua defines Util.print on both arms, and every site resolves at call time. .luacheckrc:57 and :63 remove GetSpellInfo, GetSpecialization, GetSpecializationInfo, IsPlayerSpell, IsSpellKnown and IsSpellKnownOrOverridesKnown from the top-level read_globals; any that tests/ reads bare moves into the files['tests/'] stanza, and luacheck names which.",
   "tests": "Red first. tests/test_source_style.lua: 'no authored file falls back to the global print' scans `git ls-files '*.lua'` minus libs/ and tests/_kit/ for 'or _G%.print'. It is red on HEAD with 3 hits. tests/test_lintconfig.lua: 'no deprecated spell/spec global is whitelisted for shipped code' parses .luacheckrc's top-level read_globals. It is red on HEAD.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +2 cases); lua5.1 tests/perf.lua (6 scenarios pass); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -rn 'or _G.print' core modules settings (empty); regenerate docs/test-cases.md and the README badge.",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-05",
    "KICKCD-R-16"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-02"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-22",
   "repo": "KickCD",
   "title": "Comment truth sweep: four stale comments and every bare \u00a7N citation qualified",
   "change": "Comment and test-name changes only; re-grep line numbers, because earlier items moved them. KICKCD-R-12: core/KickCD.lua:13-19 drops the _G.KickCD rebind prose to agree with :23-27. modules/Cooldowns.lua:35-38 and :648-650: master-enable recovery is the Lifecycle latch's standUp -> Resume; a 'general' CONFIG_CHANGED still rebuilds for other general-section writes. core/Util.lua:101 'Leading-edge throttle' becomes 'Trailing throttle: the first call arms one timer and fn fires once at t=ms with the last call's args'. settings/Spells.lua ~:1411 'precisely one thing' names both costs (the open page does not re-render on a spec change made while disabled, and the CM cache is dropped on stand-up). KICKCD-A-15: core/Compat.lua:245 and tests/test_compat_api.lua:4 -> 'compat'; modules/IconGrid.lua:1207 -> debug-logging-\u00a79, and :850/:866 back-references qualified; settings/Panel.lua:179 and :306 -> debug-logging-\u00a710, and :187 '(\u00a74 zero-alloc)' -> debug-logging-\u00a74, if those comments survived KC-18, else at their new home in settings/SchemaSetup.lua; tests/test_debuglog.lua:108/115/124/128 -> debug-logging-\u00a711 (test titles); tests/test_settings_log.lua:1 -> debug-logging-\u00a710.",
   "tests": "None: comment and test-name only, with no behavior change. The renamed test_debuglog titles are regenerated into docs/test-cases.md.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; count unchanged); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); grep -rnE '(^|[^a-z0-9-])\u00a7[0-9]+' core modules settings defaults locales tests/*.lua (empty); lua5.1 tests/run.lua --list > docs/test-cases.md (diff limited to the renamed titles).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-R-12",
    "KICKCD-A-15"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-18"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-23",
   "repo": "KickCD",
   "title": "TOC position notes: the IconGrid_* and Castbar_* siblings are load-bearing, and every group says whether it is conventional",
   "change": "KickCD.toc. Above modules\\IconGrid_Layout.lua: '# LOAD-BEARING POSITION: modules/IconGrid_Layout.lua and modules/IconGrid_Render.lua call NS:GetModule(\"IconGrid\") at file scope, so both follow modules\\IconGrid.lua.' Above modules\\Castbar_Handle.lua: the same for Castbar_Handle, Castbar_Skin and Castbar_Debug with NS:GetModule(\"Castbar\"). Conventional-position notes (one SHOULD row for the file): under # Locales 'read through NS.L at call time; free within the block'; under # Core 'a line without a LOAD-BEARING POSITION comment is conventional'; under # Defaults 'read at call time by core/Database.lua aceDBDefaults; free within the block'; under # Modules 'the four parent modules are free relative to each other'; and under Settings after Panel 'page files are free relative to each other'. Confirm that the lines KC-06 (core\\SpellInput.lua), KC-13 (settings\\Spells_Rows.lua) and KC-18 (settings\\SchemaSetup.lua) added carry their LOAD-BEARING notes.",
   "tests": "Red first, in tests/test_doc_structure.lua (or tests/test_version.lua, whichever already parses the TOC): 'every file-scope NS:GetModule sits below its parent under a LOAD-BEARING comment'. For each TOC-listed modules/*.lua with an unindented `NS:GetModule(\"X\")`, assert that X's file appears earlier in the TOC and that the comment block directly above the line or its group contains 'LOAD-BEARING POSITION'. It is red on HEAD (4 files).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; luacheck . (0/0); lua5.1 tests/run.lua (0 failed; +1 case); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); regenerate docs/test-cases.md and the README badge.",
   "smoke": "In client: /reload loads with no error (a TOC comment-only change).",
   "finding_ids": [
    "KICKCD-A-16",
    "KICKCD-A-17",
    "KICKCD-A-18",
    "KICKCD-A-19",
    "KICKCD-A-20",
    "KICKCD-A-21"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-13",
    "KC-18"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-24",
   "repo": "KickCD",
   "title": "One consolidated re-vendor span bundle for the 25 unrecorded tags v1.18.0-v1.53.0",
   "change": "WS-01 consumer follow-up, in the format WA-01 writes. New docs/revendor/<YYYY-MM-DD>-v1.18.0-v1.53.0/. 01_DELTA.md line 1 is exactly `Delta: LibKa0s v1.18.0 -> v1.53.0 (span: v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0, v1.27.0, v1.28.0, v1.29.0, v1.35.0, v1.36.0, v1.36.1, v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, v1.50.0, v1.51.0, v1.52.0, v1.53.0)`. Below it, a table of tag -> carrying commit (`git log --format='%h %ad %s' --date=short -- libs/LibKa0s`, joined to the CLAUDE.md provenance line at each commit through `git show <sha>:CLAUDE.md | grep 'Bundles \\[LibKa0s\\]'`), and a statement that these arrived through bulk collection sweeps with no per-tag adoption run. 05_SUMMARY.md: one paragraph, no candidates, no decisions. No per-tag folders are back-filled and no frozen bundle is edited.\n\nPLAN-REVIEW CORRECTION: normalize the span bundle to WS-01's exact grammar: line 1 is `Delta: LibKa0s v<A> -> v<B> (span: v<A> ... v<B>)` with A = the first unrecorded tag and B = the last (derive the tag list from `git log -- libs/LibKa0s tests/_kit`, so kit-only re-vendors such as v1.54.2 are in the span), the folder named docs/revendor/<date>-v<A>-v<B>/, and the true previous base stated in the body, never on line 1.",
   "tests": "None: a frozen records bundle with no code change. The prose gate (kit 26) scans it, so the full suite still runs.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; run the WS-01-amended re-vendor bundle check from ../WowAddonStandards/AUDIT.md (horizon 2026-08-25, with `--since=\"$horizon 00:00\"`) and expect 0 unrecorded tags, including v1.56.0 recorded by RV-KC's bundle; head -1 docs/revendor/*-v1.18.0-v1.53.0/01_DELTA.md; lua5.1 tests/run.lua (0 failed); luacheck . (0/0).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-01"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "WA-01"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-25",
   "repo": "KickCD",
   "title": "Topic docs: a Tier 2 docs/debug.md, compat-layer.md stops restating LibKa0s contracts, and the spells enable/disable ruling is recorded",
   "change": "(1) KICKCD-A-09: new docs/debug.md covers the console (LibKa0s-DebugLog-1.0, `/kcd debug on|off|toggle|window`, session-only flag, 1500-line buffer) and the four chat dumps. Those are `spells` (modules/Cooldowns.lua Cooldowns:DebugDump), `castbar` (modules/Castbar_Debug.lua), `interrupt` (core/Compat.lua Compat.DebugInterrupt) and `events` (KC-04, the rejected list). It gives what each prints and when to use it, plus the debug tags in use. docs/ARCHITECTURE.md:356's row becomes Present, trigger 'debug surfaces beyond the LibKa0s console: four chat dumps'. (2) KICKCD-A-10: docs/compat-layer.md:13-24 collapses the IsSecret, GetSpellCooldown, GetSpellInfo, GetSpellTexture, GetSpecialization and GetSpecializationInfo rows into one line linking LibKa0s docs/api/Compat/version-1-docs.md. It keeps only KickCD's caveats: use isActive, the position-6 spellID read, and the IsSecret degraded-body rationale. (3) KICKCD-A-27 and WS-06: docs/slash-dispatch.md records that `/kcd spells enable|disable` stays. slash-commands-\u00a72 (v2.65.0) reserves enable and disable at the top level only, and a feature noun's sub-tree may reuse them because the first token disambiguates. The finding closes by rule.",
   "tests": "None: docs only. tests/test_doc_structure.lua (map rows resolve) must stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; lua5.1 tests/run.lua (0 failed, including test_prose over the new doc and test_doc_structure); luacheck . (0/0); grep -n 'debug.md' docs/ARCHITECTURE.md (Present row); grep -cE '^\\| `(IsSecret|GetSpellCooldown|GetSpellInfo|GetSpellTexture|GetSpecialization|GetSpecializationInfo)`' docs/compat-layer.md (0).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-09",
    "KICKCD-A-10",
    "KICKCD-A-27"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-04",
    "KC-19"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-26",
   "repo": "KickCD",
   "title": "docs/ARCHITECTURE.md: re-derived inventories, symbol citations, the RESULTS.md wording, and back under about 400 lines",
   "change": "(1) KICKCD-A-11. Re-derive every figure from the final tree, with the commands in the commit body. ARCHITECTURE.md:77-81 and .luacheckrc:24-28 'Five files read addonName' become the measured count (`grep -l '^local addonName, NS = \\.\\.\\.' $(git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/')`) and the `_` count, including the new SpellInput, SchemaSetup and Spells_Rows files. The :352 compat trigger counts shims with the documentation-\u00a73 grep (8 at audit), not lines. The :353 message-bus row states its trigger (5 messages, under the >10 trigger, Present by choice). :281 and core/LifecycleSetup.lua:113 'five subscriptions' become the recounted figure after KC-06 moved the cache events out of Spells. .luacheckrc:89 'tests/run.lua:217' becomes 'KICKCD_TEST = Kit.expose(...) in tests/run.lua'. .pkgmeta:17/20/21 'packaging.md:28' becomes 'packaging' (the section id). (2) KICKCD-A-25: :367 becomes 'generated by the runner, never hand-edited apart from the watch list's `Disposition` column (automated-tests-\u00a74)'. (3) KICKCD-A-22: move '## The stand-down: disabled is total' (:226-311, 86 lines) into docs/slash-dispatch.md as '## The disabled state', leaving a five-line summary and a link. Split the :117 LibKa0s bullet (3,914 characters) into a short list linking docs/module-map.md. Target under about 400 lines, with every Documentation-map row and inbound anchor still resolving (grep the repo for 'ARCHITECTURE.md#the-stand-down' and repoint).",
   "tests": "None: docs and config comments only. tests/test_doc_structure.lua (mandated headings and map rows) and tests/test_lintconfig.lua must stay green.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; wc -l docs/ARCHITECTURE.md (about 400 or less); grep -rn 'ARCHITECTURE.md#the-stand-down' --exclude-dir=libs --exclude-dir=_kit --exclude-dir=audits --exclude-dir=reviews --exclude-dir=automated-tests --exclude-dir=revendor . (repointed or empty); grep -n 'run.lua:217\\|packaging.md:28' .luacheckrc .pkgmeta (empty); luacheck . (0/0); lua5.1 tests/run.lua (0 failed).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-11",
    "KICKCD-A-22",
    "KICKCD-A-25"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-25"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "KC-28",
   "repo": "KickCD",
   "title": "Issue-store hygiene: close #15, retitle #9, annotate #12, and file a peel issue per band file",
   "change": "GitHub writes, each gh call at least 5 s apart. (1) `gh issue close 15 -R tusharsaxena/KickCD -c 'Shipped in 1.3.0 (README Version History cites #15). state:done is terminal and closed (audit-review-history).'` (2) `gh issue edit 9 -R tusharsaxena/KickCD --title 'Move time-varying icon render onto the cooldown ticker'`. (3) `gh issue comment 12 -R tusharsaxena/KickCD --body 'Superseded: KickCD now consumes LibKa0s-Widgets-1.0 twice, ReorderList (settings/Spells.lua and settings/Spells_Rows.lua) and DragHandle (modules/Castbar_Handle.lua), since <commit shas>. This decline is historical.'` (4) KICKCD-A-12: for every file still in the 1000-1500 band (measure with `wc -l $(git ls-files '*.lua' | grep -v '^libs/\\|^tests/_kit/') | awk '$1>=1000 && $1<1500'`; expected modules/Castbar.lua, modules/IconGrid.lua, modules/IconGrid_Render.lua and tests/wow_mock.lua, plus settings/Spells.lua only if still 1000 or more after KC-13), run `gh issue create -R tusharsaxena/KickCD --label enhancement --label state:triaged --label severity:low`. Each issue names its peel seam: Castbar cast-event handlers into modules/Castbar_Events.lua; IconGrid visibility and glow gate into modules/IconGrid_Visibility.lua; IconGrid_Render's cooldown-text ticker into modules/IconGrid_Text.lua; wow_mock's frame model into tests/wow_mock_frames.lua. The commit: each band file's row in docs/module-map.md cites '(1000-1500 band; peel tracked in #N)', replacing the retired A-2 and KCD-30 references.",
   "tests": "None: issue-store and docs only.",
   "verify": "gh issue view 15 -R tusharsaxena/KickCD --json state -q .state (CLOSED); gh issue view 9 -R tusharsaxena/KickCD --json title -q .title (no '[Optional]'); gh issue view 12 -R tusharsaxena/KickCD --comments (the new comment); gh issue list -R tusharsaxena/KickCD --label state:triaged (the new band issues); cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; grep -n 'A-2\\|KCD-30' docs/module-map.md (empty); lua5.1 tests/run.lua (0 failed).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-12",
    "KICKCD-A-13",
    "KICKCD-A-24",
    "KICKCD-A-26"
   ],
   "issue_refs": [
    "KickCD#15",
    "KickCD#9",
    "KickCD#12"
   ],
   "depends_on": [
    "RV-KC",
    "KC-13"
   ],
   "effort": "S",
   "milestone": "M3"
  },
  {
   "id": "KC-DOCS",
   "repo": "KickCD",
   "title": "Sync the docs, regenerate docs/test-cases.md, and record a fresh automated-test bundle (no version bump)",
   "change": "(1) Run the /wow-addon:sync-docs equivalent over README.md, CLAUDE.md, DEPENDENCIES.md and docs/ARCHITECTURE.md. That covers count claims, COMMANDS/slash parity (including `/kcd debug events` and the new help text), the adopted-majors list (Schema added), toolchain vs DEPENDENCIES.md, dead exports (Helpers.Set etc. gone), and the comment-citation check over core/, modules/ and settings/. Confirm the CLAUDE.md provenance line reads LibKa0s v1.56.0 and tests/_kit/framework.lua Kit.VERSION is 26 (the kit revision pairing). (2) `lua5.1 tests/run.lua --list > docs/test-cases.md`; the README Tests badge equals Totals. (3) `bash tests/_kit/run-automated-tests.sh` writes docs/automated-tests/<stamp>/ (manifest.json, suite outputs), and ANALYSIS.md follows the AUTOMATED_TESTS.md prompt. RESULTS.md is regenerated by the runner and its newest row names HEAD (KICKCD-A-23). The only hand-written cells are the watch list's Disposition column (KICKCD-A-12 and R-10): each band file cites its KC-28 issue and seam, IconGrid_Render gets its first disposition, Spells.lua records the KC-13 peel (or leaves the band), and nothing cites A-2 or KCD-30. No version bump and no tag: the owner decides.",
   "tests": "None: records and docs only. The bundle is itself the full four-suite run.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; bash tests/_kit/run-automated-tests.sh (exit 0); jq '.suites' docs/automated-tests/<stamp>/manifest.json (lint, tests, perf and complexity all pass; complexity warnings 0; no function above CCN 15); test -f docs/automated-tests/<stamp>/ANALYSIS.md; diff <(lua5.1 tests/run.lua --list) docs/test-cases.md (empty); grep -o 'Tests-[0-9]*%2F[0-9]*' README.md matches the Totals; head -30 docs/automated-tests/RESULTS.md (the newest row's commit = git rev-parse --short HEAD); grep -n 'A-2\\|KCD-30' docs/automated-tests/RESULTS.md (only in older rows' carried history, never in a current Disposition).",
   "smoke": "",
   "finding_ids": [
    "KICKCD-A-23",
    "KICKCD-A-12",
    "KICKCD-R-10"
   ],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-28",
    "KC-02",
    "KC-03",
    "KC-04",
    "KC-05",
    "KC-06",
    "KC-07",
    "KC-08",
    "KC-09",
    "KC-10",
    "KC-11",
    "KC-12",
    "KC-13",
    "KC-14",
    "KC-15",
    "KC-16",
    "KC-17",
    "KC-18",
    "KC-19",
    "KC-20",
    "KC-21",
    "KC-22",
    "KC-23",
    "KC-24",
    "KC-25",
    "KC-26"
   ],
   "effort": "M",
   "milestone": "M3"
  },
  {
   "id": "BL-25",
   "repo": "BankLedger",
   "title": "Revendor the standards reference to v2.65.0 (/wow-addon:revendor-standards)",
   "change": "WS-08 consumer follow-up. Run /wow-addon:revendor-standards on BankLedger against ../WowAddonStandards at v2.65.0. It updates the three-place reference (BankLedger.toc X-Standard, the README badge/text, CLAUDE.md '## Standards compliance') and sweeps retired notation and section references across the repo, excluding libs/, tests/_kit/ and the frozen docs/audits|reviews|automated-tests|revendor bundles. Confirm the v2.65.0 texts that touch this addon. debug buffer 1500 (docs/smoke-tests.md:376 already reads 1500). The minimap row at <root>.minimap.shown (BL-12). SafeRegisterEvent (BL-06). writeThrough (BL-10). schemaVersion 0 (BL-11). compat-layer applies, because core/Compat.lua owns 13 addon-specific shims (WS-05). The events-frames-taint-\u00a71 and architecture-\u00a74 thresholds (WS-04). A code-comment citation it reports is corrected comment-only.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER BL-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from BL-DOCS stands.",
   "tests": "none \u2014 docs only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/BankLedger && grep -n 'X-Standard' BankLedger.toc (v2.65.0) && grep -n 'v2.65.0' CLAUDE.md README.md && ~/.claude/wow-addon/bin/ka0s-bounded luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-BL",
    "BL-24",
    "WS-08",
    "BL-DOCS",
    "BL-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "PF-25",
   "repo": "PartyFrameEnhanced",
   "title": "/wow-addon:revendor-standards to v2.65.0",
   "change": "Run /wow-addon:revendor-standards in this repo against WowAddonStandards v2.65.0 (WS-08): the three-place reference (TOC ## X-Standard, README Standard badge, CLAUDE.md 'Standards compliance'), retired notation, retired file names and unresolvable section references swept out of the repo (excluding libs/, tests/_kit/ and frozen bundles). Confirm-only comment citations the command reports are corrected comment-only (e.g. any debug-buffer '500' mention -> 1500 per WS-07, the bootstrap '_' wording per WS-07(2)).\n\nExecutable only after WowAddonStandards' v2.65.0 branch (WS-02..WS-08) is merged to its default branch and pushed. /wow-addon:revendor-standards fetches the standard from GitHub, so running it earlier refreshes against v2.64.0 and records the wrong version. Before running, confirm `gh api repos/<owner>/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 'v2.65.0'` hits. If it does not, stop and leave this item open.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER PF-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from PF-DOCS stands.",
   "tests": "None \u2014 documentation references only.",
   "verify": "~/.claude/wow-addon/bin/ka0s-bounded luacheck . (0/0) && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed; prose gate green) && the command's closing report lists zero unresolved references.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PF",
    "PF-23",
    "WS-08",
    "PF-DOCS",
    "PF-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "MM-32",
   "repo": "MultiMeters",
   "title": "Re-vendor the standard reference to v2.65.0",
   "change": "Run the /wow-addon:revendor-standards equivalent against ../WowAddonStandards at v2.65.0 (WS-08). The three-place reference is the TOC X-Standard, the README badge, and CLAUDE.md 'Standards compliance'. Sweep for retired notation and unresolvable section refs outside libs/, tests/_kit/ and the frozen bundles. Carry the v2.65.0 rulings the addon now meets into its docs. That means debug-buffer 1500 wording where quoted (docs/ARCHITECTURE.md:516 row text), the savedvariables-\u00a71 schemaVersion 0 template (MM-12), the SafeRegisterEvent helper (MM-07), writeThrough/library-absent routing (MM-15), and minimap.shown (MM-16). Any citation in a code comment is corrected comment-only.\n\nEXECUTION GATE: /wow-addon:revendor-standards fetches the standard from GitHub, so this item can run only after WowAddonStandards's WS-08 branch (v2.65.0) is merged and pushed to origin. Before starting, check that the upstream default branch's standards/ reads v2.65.0. Until then the item is blocked. Do not point it at a local unmerged checkout.\n\nPLAN-REVIEW CORRECTION: runs AFTER MM-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from MM-DOCS stands.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling. This supersedes the \"run against ../WowAddonStandards\" wording above.",
   "tests": "none \u2014 docs only. The suite's doc-structure and prose gates stay green.",
   "verify": "ka0s-bounded luacheck . (0/0); ka0s-bounded lua5.1 tests/run.lua (0 failed); ka0s-bounded lua5.1 tests/perf.lua (exit 0); lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w . (no output); git ls-files '*.lua' | grep -v -e '^libs/' -e '^tests/_kit/' | xargs wc -l | awk '$2!=\"total\" && $1>1500' (no output) (no output); grep -n 'v2.65.0' MultiMeters.toc README.md CLAUDE.md (three hits)",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-MM",
    "MM-31",
    "WS-08",
    "MM-DOCS",
    "MM-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "LH-36",
   "repo": "LootHistory",
   "title": "Re-vendor the standards reference to v2.65.0",
   "change": "Run /wow-addon:revendor-standards on this repo against WowAddonStandards v2.65.0. It covers the three-place reference (the TOC X-Standard, the README Standard badge, CLAUDE.md 'Standards compliance'), and sweeps retired notation, retired file names and unresolvable section references from the whole repo, excluding libs/, tests/_kit/ and the frozen audits/, reviews/ and automated-tests/ bundles. Apply the v2.65.0 wording changes that touch this repo's docs: minimap row path minimap.shown (already done by LH-13), schemaVersion 0 (LH-12), SafeRegisterEvent (LH-17), the debug buffer 1500 (already correct in smoke-tests.md), and 'synchronis' (grep shows no hit). Any citation found in a code comment is corrected comment-only, on confirmation.\n\nUpstream WS-08 is the gate: /wow-addon:revendor-standards fetches the standard from GitHub (WowAddonStandards master), so this item can only run after WS-08's v2.65.0 ripple, and the WowAddonStandards branch that carries WS-02..WS-08, is merged to its default branch (master) and pushed. Before that, the plugin would read v2.64.0 and pin the wrong version. Before running, confirm upstream: `gh api repos/<owner>/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0`. The command sets the three-place reference to v2.65.0: LootHistory.toc `## X-Standard`, the README Standard badge, and CLAUDE.md 'Standards compliance'.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER LH-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from LH-DOCS stands.",
   "tests": "none; documentation only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && luacheck . && grep -rn 'v2\\.6[0-4]\\.0' README.md CLAUDE.md LootHistory.toc docs/*.md (no stale pin) && the command's own final sweep reports 0 unresolved section references.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-LH",
    "LH-12",
    "LH-13",
    "LH-17",
    "LH-30",
    "LH-31",
    "WS-08",
    "LH-DOCS",
    "LH-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "AT-26",
   "repo": "AbsorbTracker",
   "title": "Re-vendor the standards reference to v2.65.0",
   "change": "Run /wow-addon:revendor-standards in this repo against WowAddonStandards v2.65.0. It updates the three-place reference (TOC `## X-Standard`, the README Standard badge, and CLAUDE.md 'Standards compliance' naming v2.65.0) and sweeps retired notation, retired file names and unresolvable section references outside libs/, tests/_kit/ and the frozen bundles. Any code-comment citation it reports is corrected comment-only after confirmation. Review the diff by hand.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER AT-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from AT-DOCS stands.",
   "tests": "None \u2014 docs only (the standards reference). The suite must stay green.",
   "verify": "grep -n 'v2.65.0' CLAUDE.md README.md AbsorbTracker.toc (the reference present); head -1 ../WowAddonStandards/standards/STANDARDS.md (names v2.65.0); ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua (0 failed); luacheck .",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AT",
    "WS-08",
    "AT-DOCS"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "PM-16",
   "repo": "PanelMaster",
   "title": "Re-vendor the standards reference to v2.65.0",
   "change": "Run the /wow-addon:revendor-standards procedure against the local ../WowAddonStandards (after WS-08; `head -1 ../WowAddonStandards/standards/STANDARDS.md` reads v2.65.0).\n- The three-place reference: the TOC X-Standard line, the README standards badge, and CLAUDE.md 'Standards compliance'.\n- Sweep retired notation, retired file names and unresolvable section references out of the whole repo. Exclude libs/, tests/_kit/ and the frozen docs/audits/, docs/reviews/, docs/automated-tests/<stamp>/, docs/revendor/, docs/superpowers/ and docs/investigations/.\n- A citation in a code comment is corrected comment-only.\n- Also cite v2.65.0 in the places items PM-09, PM-10 and PM-11 relied on: slash-commands-\u00a71's library-absent line, savedvariables-\u00a71 schemaVersion = 0, and launcher-\u00a73's minimap.shown.\n- Normalize the tests/prose_waivers.lua header 'localization-5' to 'localization-\u00a75'.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER PM-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from PM-DOCS stands.",
   "tests": "none \u2014 documentation reference only; the procedure writes no Lua behavior.",
   "verify": "- `grep -n 'v2.65.0' README.md CLAUDE.md` shows both.\n- `grep -rn 'v2.64.0' README.md CLAUDE.md PanelMaster.toc docs/*.md` prints nothing.\n- `luacheck .` is clean.\n- `lua5.1 tests/run.lua` gives 0 failed; test_prose and test_docs are green.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PM",
    "WS-08",
    "PM-11",
    "PM-DOCS",
    "PM-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "PC-26",
   "repo": "PrettyChat",
   "title": "Re-vendor the standard reference to v2.65.0 (/wow-addon:revendor-standards)",
   "change": "Run /wow-addon:revendor-standards in PrettyChat. It updates the three-place standards reference:\n- the PrettyChat.toc X-Standard line (a URL; confirm it is unchanged);\n- the README Standard badge;\n- CLAUDE.md 'Standards compliance', which names v2.65.0.\nIt also sweeps retired notation and unresolvable section references out of the repo, excluding libs/, tests/_kit/ and the frozen bundles. A citation in a code comment is corrected only on confirmation, comment-only.\nIf the command reads the standard from raw GitHub, it must wait until the owner has approved and pushed WowAddonStandards v2.65.0. Otherwise it uses the local sibling on feat/2026-09-23-review-audit-remediation (`head -1 ../WowAddonStandards/standards/STANDARDS.md`).\n\nUpstream follow-up WS-08 ('/wow-addon:revendor-standards to v2.65.0'). EXECUTION GATE: this item can run only after the WowAddonStandards feat/2026-09-23-review-audit-remediation branch, carrying WS-02..WS-08 and the dated v2.65.0 changelog entry, is merged to its default branch and pushed. The /wow-addon:revendor-standards plugin fetches STANDARDS.md and the section files from GitHub, not from the local sibling, so if it runs earlier it finds v2.64.0 and rolls nothing. Before running, confirm with `gh api repos/<owner>/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | head -1`, which must name v2.65.0. Until then the item stays blocked. Do NOT hand-edit the three-place reference to v2.65.0 from the local sibling. That replaces the local-sibling fallback in the paragraph above.\n\nPLAN-REVIEW CORRECTION: runs AFTER PC-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from PC-DOCS stands.",
   "tests": "None: documentation references only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/PrettyChat && grep -n 'v2.65.0' CLAUDE.md && ! grep -rn 'v2.64.0' CLAUDE.md README.md PrettyChat.toc && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-PC",
    "PC-25",
    "WS-08",
    "PC-DOCS",
    "PC-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "WG-30",
   "repo": "WhatGroup",
   "title": "Revendor the standards reference to v2.65.0",
   "change": "Run /wow-addon:revendor-standards in this repo against ../WowAddonStandards at v2.65.0. It covers the three-place reference (WhatGroup.toc `## X-Standard`, the README Standard badge, the CLAUDE.md 'Standards compliance' section) and sweeps retired notation, retired file names and unresolvable \u00a7-references out of the repo. libs/, tests/_kit/ and the frozen docs/audits/, docs/reviews/, docs/revendor/ and docs/automated-tests/ bundles are excluded. Apply the reported doc fixes. A citation in a code comment is changed comment-only and only if it is wrong under v2.65.0.\n\nUpstream follow-up WS-08 ('/wow-addon:revendor-standards to v2.65.0') is this item. Executable only after WowAddonStandards's v2.65.0 branch (WS-02..WS-08) is merged to its default branch (master) and pushed. The plugin fetches the standard from GitHub, not from the sibling checkout, so running it earlier revendors to v2.64.x. Gate: `gh api repos/:owner/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | head -5` names v2.65.0 before the run. The TOC ## X-Standard, the README badge and CLAUDE.md all read v2.65.0 afterwards.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER WG-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from WG-DOCS stands.",
   "tests": "none \u2014 docs and comment references only.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && head -1 ../WowAddonStandards/standards/STANDARDS.md",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-WG",
    "WG-28",
    "WS-08",
    "WG-DOCS",
    "WG-00"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "CM-31",
   "repo": "ConsumableMaster",
   "title": "/wow-addon:revendor-standards to v2.65.0",
   "change": "Run the /wow-addon:revendor-standards procedure against WowAddonStandards v2.65.0 (`head -1 ../WowAddonStandards/standards/STANDARDS.md`). It updates the three-place reference: the TOC `## X-Standard`, the README standards badge, and CLAUDE.md 'Standards compliance'. It also sweeps retired notation, retired file names and unresolvable section references across the repo, excluding libs/, tests/_kit/ and the frozen docs/audits/, docs/reviews/ and docs/automated-tests/ bundles. A citation found in a code comment is corrected comment-only. Carry v2.65.0's rulings into the docs that restate them: the minimap row path (CM-19), SafeRegisterEvent (CM-15), schemaVersion 0 (CM-16), the library-absent line (CM-18) and the 1500-line debug buffer, if docs/debug.md says 500.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER CM-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from CM-DOCS stands.",
   "tests": "none, because the change is docs and metadata only. The full run proves that the TOC edit did not disturb load order (tests/test_load.lua).",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster && luacheck . && ~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua && grep -n 'v2.65.0' ConsumableMaster.toc README.md CLAUDE.md && ! git grep -n 'v2.64.0' -- ConsumableMaster.toc README.md CLAUDE.md",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-CM",
    "WS-08",
    "CM-27",
    "CM-DOCS"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "AM-35",
   "repo": "AuraMaster",
   "title": "Roll the standards reference to v2.65.0 with /wow-addon:revendor-standards",
   "change": "WS-08 consumer follow-up. Run /wow-addon:revendor-standards in AuraMaster against ../WowAddonStandards at v2.65.0 (`head -1 ../WowAddonStandards/standards/STANDARDS.md`). It updates the three-place reference: the TOC X-Standard (a URL, unchanged), the README standards badge to v2.65.0, and CLAUDE.md's 'Standards compliance' version mention. It also sweeps retired notation and unresolvable section references outside libs/, tests/_kit/ and the frozen bundles. Section references the v2.65.0 rulings moved are corrected, and any hit in a code comment is corrected comment-only after confirmation, per the command. Record in the commit body each changed citation.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER AM-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from AM-DOCS stands.",
   "tests": "none: documentation reference roll. tests/test_docs.lua and the kit's test_prose guard the result.",
   "verify": "grep -n 'v2.65.0' README.md CLAUDE.md -> the badge and the compliance line; grep -rn 'v2.64.0' README.md CLAUDE.md docs/*.md -> nothing outside frozen bundles; luacheck . -> 0/0; ka0s-bounded lua5.1 tests/run.lua -> exit 0.",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-AM",
    "AM-34",
    "WS-08",
    "AM-DOCS"
   ],
   "effort": "S",
   "milestone": "M4"
  },
  {
   "id": "KC-27",
   "repo": "KickCD",
   "title": "Re-vendor the standards reference to Ka0s WoW Addon Standard v2.65.0",
   "change": "WS-08 consumer follow-up: run /wow-addon:revendor-standards in KickCD. This rolls the three-place reference: KickCD.toc `## X-Standard` if it carries a version, the README Standard badge, and CLAUDE.md 'Standards compliance'. It sweeps retired notation and unresolvable section references across the repo, excluding libs/, tests/_kit/ and the frozen docs/audits/, docs/reviews/, docs/automated-tests/ and docs/revendor/ bundles. Examples: a 500-line debug buffer, 'currently 120007', the minimap CLI path described as `.hide`, and slash-commands-\u00a72 text quoting the pre-ruling reserved-verb scope. Code-comment citations change only on confirmation and stay comment-only.\n\nPLAN-REVIEW CORRECTION: EXECUTION GATE (owner): /wow-addon:revendor-standards fetches the standard from GitHub, so this item runs only after the WowAddonStandards remediation branch (WS-01..WS-08, v2.65.0) is merged to its default branch and pushed. Before running, confirm `gh api repos/tusharsaxena/WowAddonStandards/contents/standards/STANDARDS.md --jq .content | base64 -d | grep -m1 v2.65.0` hits; if it does not, stop and leave the item open. Never hand-edit the three-place reference from the local sibling.\n\nPLAN-REVIEW CORRECTION: runs AFTER KC-DOCS (which no longer waits on this gate). The same commit re-runs the docs ripple the rename touches (README badge, CLAUDE.md, TOC X-Standard) and nothing else; the automated-test bundle from KC-DOCS stands.",
   "tests": "None: docs and reference only, with no behavior change.",
   "verify": "cd /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD; head -1 ../WowAddonStandards/standards/STANDARDS.md (v2.65.0); grep -rn 'v2\\.64\\.0' --exclude-dir=libs --exclude-dir=_kit --exclude-dir=audits --exclude-dir=reviews --exclude-dir=automated-tests --exclude-dir=revendor . (empty); luacheck . (0/0); lua5.1 tests/run.lua (0 failed).",
   "smoke": "",
   "finding_ids": [],
   "issue_refs": [],
   "depends_on": [
    "RV-KC",
    "KC-26",
    "WS-08",
    "KC-DOCS"
   ],
   "effort": "S",
   "milestone": "M4"
  }
 ],
 "dispositions": {
  "BankLedger": [],
  "PartyFrameEnhanced": [
   {
    "finding_id": "PartyFrameEnhanced-R-09",
    "covered_by": "LK-03",
    "reason": "The defect is the kit's AceDB fake (testkit/mock_record.lua CopyProfile/DeleteProfile/SetProfile). LK-03 fixes it upstream and RV-PF vendors it whole. The addon-side consequence, profile verbs that must not raise, is PF-01 (R-05)."
   },
   {
    "finding_id": "PartyFrameEnhanced-A-02",
    "covered_by": "LK-05",
    "reason": "The kit mock's CreateFrame default (testkit/mock_base.lua:121) is fixed upstream and vendored by RV-PF. The addon half, hiding the container frames that the shown default now exposes, is PF-06 (A-01)."
   },
   {
    "finding_id": "PartyFrameEnhanced-A-19",
    "covered_by": "WS-01",
    "reason": "The finding is about WowAddonStandards AUDIT.md's `--since=\"$horizon\"`, not about the addon. WS-01 changes it to `$horizon 00:00`. The addon's span-bundle follow-up is PF-24."
   }
  ],
  "MultiMeters": [
   {
    "finding_id": "MultiMeters-R-19",
    "covered_by": "WA-03",
    "reason": "The stale cross-addon baseline lives in the wow-addon review brief (agents/review.md:357-368, :193, :365). WA-03 re-measures it at v1.56.0 and derives it at run time. Nothing changes in MultiMeters."
   },
   {
    "finding_id": "MultiMeters-R-20",
    "covered_by": "WA-02",
    "reason": "ka0s-bounded is missing from PATH because the plugin ships it only under scripts/ while PATH names <plugin>/bin. WA-02 ships bin/ka0s-bounded. No addon file is involved."
   }
  ],
  "LootHistory": [
   {
    "finding_id": "LootHistory-R-19",
    "covered_by": "WA-03",
    "reason": "The stale cross-addon baseline lives in the wow-addon plugin's review agent. WA-03 re-measures and derives it at v1.56.0, and LootHistory needs no change."
   },
   {
    "finding_id": "LootHistory-A-22",
    "covered_by": "WS-01",
    "reason": "The tension between AUDIT.md step 4's fixed High grade and step 5's impact grading is resolved in the playbook: WS-01 grades the re-vendor check by step 5. No addon-side change."
   }
  ],
  "AbsorbTracker": [
   {
    "finding_id": "AbsorbTracker-R-06",
    "covered_by": "LK-03",
    "reason": "The kit's AceDB fake (tests/_kit/mock_record.lua) raising on CopyProfile/DeleteProfile is fixed in LibKa0s testkit by LK-03 and arrives whole through RV-AT. The addon must not edit tests/_kit. The addon-side exposure it hid (the profile verbs) is its own finding, R-02, fixed in AT-03."
   },
   {
    "finding_id": "AbsorbTracker-A-21",
    "covered_by": "WS-07",
    "reason": "WS-07 (1) changes debug-logging-\u00a71 and NEW_ADDON_CONTEXT.md from 500 to 1500 lines, matching LibKa0s DebugLog MAX_BUFFER and its rationale. The addon carries no buffer constant of its own, so nothing changes addon-side."
   },
   {
    "finding_id": "AbsorbTracker-A-22",
    "covered_by": "WS-01",
    "reason": "A playbook inconsistency in WowAddonStandards AUDIT.md step 4 vs step 5. WS-01 regrades an unrecorded re-vendor through step 5 (Low) and removes both hard-coded High grades. The addon's missing bundles are A-01, fixed in AT-25."
   },
   {
    "finding_id": "AbsorbTracker-A-23",
    "covered_by": "WS-02",
    "reason": "The finding asks only for a ruling. WS-02 rules that hollow composers stand and that host verbs writing composed rows write through a declared writeThrough list or refuse with the library-absent line. AbsorbTracker's adoption of that ruling is A-02/A-03 in AT-08."
   }
  ],
  "PanelMaster": [
   {
    "finding_id": "PanelMaster-A-16",
    "covered_by": "WS-01",
    "reason": "The High-vs-Low grading conflict is in WowAddonStandards AUDIT.md. WS-01 replaces both hard-coded 'High' grades with step-5 grading (Low for an unrecorded re-vendor). PanelMaster needs no change; its own unrecorded-tag gap (A-15) is PM-18."
   }
  ],
  "PrettyChat": [
   {
    "finding_id": "PRETTYCHAT-A-26",
    "covered_by": "WS-06",
    "reason": "WS-06 (3) rules that for a strip the library draws (O.TabStrip, RenderTabbedSchema), the library's suite pins the wrap invariance, and a consumer MUST NOT duplicate it (testing-\u00a78). LibKa0s tests/test_options_tabs.lua:573 already pins it. The finding's own remediation says PrettyChat needs no change, so nothing is added to tests/test_panel.lua."
   },
   {
    "finding_id": "PRETTYCHAT-A-28",
    "covered_by": "WS-07",
    "reason": "WS-07 (2) rewrites architecture-\u00a71 to 'destructure both varargs; name the first `_` when the file never reads it'. That makes the eleven `local _, NS = ...` files compliant as written. The finding's remediation says to change nothing in the addon."
   }
  ],
  "WhatGroup": [],
  "ConsumableMaster": [
   {
    "finding_id": "ConsumableMaster-A-03",
    "covered_by": "WS-01",
    "reason": "The fix is entirely in WowAddonStandards AUDIT.md: grading by step 5, the same-day --since fix, and the span-bundle reader. The addon-side consequence, the span bundle itself, is ConsumableMaster-A-02 and is planned as CM-28."
   },
   {
    "finding_id": "ConsumableMaster-A-05",
    "covered_by": "LK-07",
    "reason": "The gate change (scanning the store-root README/RESULTS files and skipping only dated bundles) lives in the LibKa0s testkit's test_prose.lua and prose_lists.lua, and arrives with RV-CM's whole-payload re-vendor. The addon must not patch tests/_kit (library-stack-\u00a75). The spellings it exposes are fixed addon-side in CM-01 (ConsumableMaster-A-07, A-06)."
   },
   {
    "finding_id": "ConsumableMaster-R-19",
    "covered_by": "WA-02",
    "reason": "The PATH half is WA-02: bin/ka0s-bounded ships on the plugin's PATH entry. The stale cross-addon baseline half is WA-03, which re-measures and derives the review brief's baseline at v1.56.0. Both are wow-addon plugin changes with no addon-side code."
   }
  ],
  "AuraMaster": [
   {
    "finding_id": "AuraMaster-A-18",
    "covered_by": "LK-06",
    "reason": "The kit's lone-CR blind spot is fixed entirely in LibKa0s testkit/test_eol.lua (LK-06) and reaches AuraMaster through RV-AM's whole-payload re-vendor. The only addon-side work is the page_helpers.lua:80 fix, which is AuraMaster-A-11 in AM-01. The line-endings-\u00a77 note is WS-07."
   },
   {
    "finding_id": "AuraMaster-A-20",
    "covered_by": "WS-01",
    "reason": "The AUDIT.md contradiction (step 4 hard-codes High, step 5 grades a docs-only gap Low) is reconciled in WowAddonStandards by WS-01, which makes step 5's table govern every step-4 grade. AuraMaster itself needs no change."
   },
   {
    "finding_id": "AuraMaster-A-21",
    "covered_by": "WA-01",
    "reason": "The root cause is the revendor-libka0s command taking its base from the library's previous tag. WA-01 makes it read the base from the addon's CLAUDE.md provenance line and adds the cross-addon pre-flight. For AuraMaster, the frozen v1.55.0 bundle's base v1.54.2 is actually correct: 329e1a3 was a kit-only v1.54.2 re-vendor, verified by git show and the provenance history. So no addon-side correction is owed, and AM-02 records the span, including v1.54.2."
   }
  ],
  "KickCD": [
   {
    "finding_id": "KICKCD-A-02",
    "covered_by": "WS-01",
    "reason": "AUDIT.md's hard-coded High grade and the unspecified span-bundle line-1 format are fixed entirely in WowAddonStandards by WS-01: grading by step 5, the '$horizon 00:00' fix and every tag on a two-tag folder's line 1. KickCD's own span bundle is KC-24 (KICKCD-A-01)."
   },
   {
    "finding_id": "KICKCD-A-06",
    "covered_by": "LK-07",
    "reason": "The kit test_prose skip of docs/perf-analysis/ and docs/automated-tests/ is fixed in LibKa0s testkit by LK-07's file-by-file SCAN_BACK list, and it reaches KickCD whole through RV-KC. The one addon-side consequence, the two spellings the gate then catches, is KC-01 (KICKCD-A-07)."
   },
   {
    "finding_id": "KICKCD-R-19",
    "covered_by": "LK-26",
    "reason": "The library's use of scheduleTimer's return as its armed flag is fixed in LibKa0s OptionsWidgets minor 31 (LK-26) and arrives with RV-KC. The addon-side belt-and-braces switch to C_Timer.NewTimer (KICKCD-R-04) is KC-10."
   }
  ]
 },
 "consumer_followups": [
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole (libs/LibKa0s + tests/_kit, +x runner) with the CLAUDE.md provenance line in the same commit, using the WA-01 command (base = provenance tag); regenerate docs/test-cases.md (kit case names now carry \u00a7) and the README badge. Land the fixes the LK-05/LK-33 dry-run exposed first."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "WS-01",
   "what": "Write one consolidated span bundle docs/revendor/<date>-v1.18.0-v1.55.0/ per the new format (closes AbsorbTracker-A-01); re-run the amended AUDIT.md check and expect 0 unrecorded."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-11",
   "what": "Core degradation stub gains SafeRegisterEvent/SafeRegisterUnitEvent/SafeRegisterEvents one-rung pcall bodies (test_surface_parity stays green). Route the 7 registrations in core/AbsorbTracker.lua and core/Lifecycle.lua through them; keep NS.State.rejectedEvents and surface it via /at debug (closes AbsorbTracker-A-12, whose unit half now uses the kit's frame __badEvents from LK-04)."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-23",
   "what": "Options stub composers become hollow: delete composeBlock, ORDER_STEP and the ColorPair/FontGroup/BorderGroup/BarGroup/MasterControls host copies (settings/OptionsSetup.lua:237-377). Pass writeThrough {'enabled','locked'} to the Schema instance and to its stub (settings/Schema.lua). Re-pin tests/test_perf.lua:501 and tests/test_optionssetup.lua:233-243 as three figures: full count, degraded count and the named composer gap."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-22",
   "what": "Schema instance stub gains SetMany (all-or-nothing, log-silent) so the two-table parity against a live instance stays green."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-03",
   "what": "With the kit's AceDB fake now raising, AbsorbTracker-R-02's copy/delete refusal tests must see no raw AceDB error: guard names before calling AceDB (profileExists, current-profile checks)."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-16",
   "what": "core/LauncherSetup.lua: pass isEnabled and disabledLine (NS.Slash.DisabledLine) in the Launcher descriptor and delete the hand-written rung (a) gate at :160-161."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "WS-06",
   "what": "Move /at test <value> [secs] to /at debug hold <value> [secs] (closes AbsorbTracker-A-14 locally and fixes R-12's validation there). Rename the minimap row's path from its .minimap.hide spelling to .minimap.shown, with get/set inverting onto LibDBIcon's hide; update docs and tests."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "WS-05",
   "what": "Delete the dead legacy GetAddOnMetadata global rung at core/EnvSetup.lua:70-71; the compat-layer Not-applicable row cites compat's new applicability condition."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "WS-03",
   "what": "Declare global.schemaVersion = 0 in defaults. Make the migration runner stamp only after success, with profile steps running for every stored profile."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub's DisabledLine format with Kit.assertLibraryConstant; the stub prints help rows without a FormatRow copy."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "LK-28",
   "what": "Evaluate RenderTabbedSchema opts.tabs for settings/UnitPanel.lua:327's hand-built strip; adopt it, or file a state:triaged decline issue."
  },
  {
   "addon": "AbsorbTracker",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0 (TOC X-Standard, README badge, CLAUDE.md)."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-06",
   "what": "Before the re-vendor, remove the stray bare CR at tests/page_helpers.lua:80 and renormalize (AuraMaster-A-11), or kit 26's test_eol goes red."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line, using the WA-01 command (base v1.55.0); the new bundle notes once that the frozen v1.55.0 bundle's base should have read v1.53.0. Regenerate docs/test-cases.md and the badge. Land the dry-run fixes first."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle docs/revendor/<date>-v1.35.0-v1.54.2/ per the new format (AuraMaster-A-01); verify the kit-only v1.54.2 by hand."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* one-rung bodies. Route RegisterLifecycleEvents (loop over a module-level list), TS.Sync, syncAuraListen and the LifecycleSetup REGEN hold through Core.SafeRegisterEvent; NS.RejectedEvents appears in [Init] (AuraMaster-R-05's host half)."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-28",
   "what": "Adopt it (C-04b): replace Helpers.RenderTabbedPage/collectTabs/settleActiveTab/renderActiveTab (settings/OptionsSetup.lua:560-659) and buildContainerHeader (:480-504) with O.RenderTabbedSchema(..., {tabs, disabledFor, disabledNotice, chrome}) and O.PageBanner{action}; re-point the test_optionssetup and test_pages_* tab cases."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-16",
   "what": "core/LauncherSetup.lua: pass isEnabled/disabledLine and delete the hand gate at :119-120."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-17",
   "what": "settings/Slash.lua:467-471 set wrapper becomes `return NS.SetByPath(path, v)`; drop its own refusal prints, now printed by CliSet."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-22",
   "what": "AuraMaster#21: adopt Schema primitives, registry, bulk bracket and Validate in settings/Schema.lua per the version-2 adoption note. normalize now exists, so re-evaluate adopting Set as well, and keep the degraded_env arm."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-23",
   "what": "AuraMaster-R-06: Options stub composers answer {}; add the fall-together case (on a degraded load, /am set <composed path> prints the library-absent line); host verbs on composed rows take WS-02 route (a) via writeThrough if adopting Set, else route (b)."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "LK-18",
   "what": "Pin stub.DisabledLine (settings/Slash.lua:388-391) with Kit.assertLibraryConstant."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "WS-06",
   "what": "Rename MINIMAP_PATH 'global.minimap.hide' -> 'global.minimap.shown' in settings/Schema.lua:65, General.lua:49 and OptionsSetup.lua:36 (get/set invert onto hide); update docs and tests."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "WS-03",
   "what": "Declare schemaVersion 0 in defaults; per-profile migration steps; stamp only after success."
  },
  {
   "addon": "AuraMaster",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first (e.g. BankLedger-A-03's wow_mock C_Timer override)."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle docs/revendor/<date>-v1.18.0-v1.53.0/ (BankLedger-A-01)."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies. Replace the local pcall helper with Core.SafeRegisterEvent and route the five bypassing sites (core/BankLedger.lua:87,91,92; modules/Browser.lua:1218; SessionWindow.lua:673; Ledger.lua:849-854) through it (BankLedger-A-13)."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-16",
   "what": "core/LauncherSetup.lua: pass isEnabled/disabledLine and delete the hand gate at :156."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-17",
   "what": "BankLedger passes Schema.Set straight to the Slash descriptor, so /bl set now prints refusals: re-pin any test asserting the old echo."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-22",
   "what": "Schema instance stub gains SetMany (parity)."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-09",
   "what": "The next automated-tests run records perf skip reason (2) from the docs/ARCHITECTURE.md:501 performance-\u00a712 row; no hand edit of RESULTS.md."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub DisabledLine with Kit.assertLibraryConstant."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "WS-06",
   "what": "Rename S.MINIMAP_PATH 'minimap.hide' -> 'minimap.shown' (settings/Schema.lua:147) with inverting get/set; update docs and tests."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "WS-03",
   "what": "Declare schemaVersion 0 in defaults; per-profile steps; stamp after success."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "LK-12",
   "what": "In-client smoke: an uncached epic item's quality resolves from the |cnIQ link in the ledger."
  },
  {
   "addon": "BankLedger",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-07",
   "what": "Before the re-vendor, fix 'analysed'/'neighbours' in docs/perf-analysis/README.md (ConsumableMaster-A-07) and 'synchronisation' at docs/settings-panel.md:80, or kit 26's prose gate goes red."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle docs/revendor/<date>-v1.18.0-v1.53.0/ (ConsumableMaster-A-02); no register row needed once the check reads spans."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route KCM:OnEnable's registration block through Core.SafeRegisterEvent with a rejected list (ConsumableMaster-A-08)."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-25",
   "what": "Adopt it (B3): KCM.Options.Open delegates to UI.OpenOptionsPanel and registerPanel to UI.CreateOptionsPanel; delete expandMainCategory, KCM._settingsCategoryID and the host combat park (settings/OptionsShim.lua:192-236, Panel.lua:1253-1286), which also fixes ConsumableMaster-R-03; rewrite the two registration cases."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-22",
   "what": "ConsumableMaster#39: adopt Schema for the settings/Panel.lua write seam. The validate-coerce map becomes validate plus row.normalize, SetManyAndRefresh becomes S.SetMany with announceBatch running the page apply pass, and SESSION/GLOBAL paths become rows with get/set."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-03",
   "what": "Drop tests/wow_mock.lua's SetProfile override; the kit now strips defaults (ConsumableMaster-R-06)."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-21",
   "what": "In-client smoke of the ReorderList drags (settings/Category.lua, StatPriority.lua:122, MacroBar.lua:723)."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-28",
   "what": "Evaluate RenderTabbedSchema opts.tabs for settings/General.lua:402 and the three sibling files; adopt it or file a decline issue."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub DisabledLine with Kit.assertLibraryConstant."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "WS-06",
   "what": "Rename MINIMAP_PATH 'global.minimap.hide' -> '.shown' (settings/General.lua:51, Panel.lua:246) with inverting get/set."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "WS-03",
   "what": "Declare schemaVersion 0 in defaults; per-profile steps; stamp after success."
  },
  {
   "addon": "ConsumableMaster",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-07",
   "what": "Before the re-vendor, fix the two British spellings in docs/perf-analysis/README.md (KICKCD-A-07)."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first."
  },
  {
   "addon": "KickCD",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for the 25 unrecorded tags (KICKCD-A-01)."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route every registration block through Core.SafeRegisterEvent with a rejected list (KICKCD-A-03)."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-26",
   "what": "The throttle now works with the nil-returning scheduleTimer. Still switch settings/OptionsSetup.lua:243 to C_Timer.NewTimer as KICKCD-R-04 asks, belt and braces."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-22",
   "what": "KickCD#22: adopt Schema per the version-2 adoption note. The section payload is read from row.section in announce, session/global paths are rows with get/set, NS.RefreshEnabledHold is the enabled row's onChange, and Helpers.Set/Get/Resolve/sameValue/bulk/ValidateSchema are deleted."
  },
  {
   "addon": "KickCD",
   "upstream_item": "WS-06",
   "what": "/kcd spells enable|disable stays (sub-tree use is sanctioned); record the ruling in the slash-dispatch doc (KICKCD-A-27 closes by rule)."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-21",
   "what": "In-client smoke of the Spells ReorderList drag (settings/Spells.lua:1001)."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-28",
   "what": "Evaluate opts.tabs for settings/Panel_Render.lua:208; adopt it or file a decline issue."
  },
  {
   "addon": "KickCD",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub DisabledLine with Kit.assertLibraryConstant."
  },
  {
   "addon": "KickCD",
   "upstream_item": "WS-06",
   "what": "Rename 'global.minimap.hide' -> '.shown' (settings/General.lua:76, OptionsSetup.lua:83) with inverting get/set."
  },
  {
   "addon": "KickCD",
   "upstream_item": "WS-03",
   "what": "KICKCD-R-01: color-shape and font-flag steps run for every stored profile and on OnProfileChanged; declare schemaVersion 0 in defaults."
  },
  {
   "addon": "KickCD",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for the 25 unrecorded tags (LootHistory-A-01)."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-12",
   "what": "tests/test_itemsetup.lua:14 EPIC_LINK moves to the |cnIQ4: shape, keeping one legacy |cff case; in-client smoke that an uncached epic drop records its quality."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route the registration blocks through Core.SafeRegisterEvent with a rejected list (LootHistory-A-03)."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-26",
   "what": "Optionally switch settings/OptionsSetup.lua:205-207's scheduleTimer to C_Timer.NewTimer; the library throttle already works."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-18",
   "what": "LootHistory-R-05: format the degraded stub line from the verbatim DISABLED_LINE_FORMAT, pinned by Kit.assertLibraryConstant; R-13's launcher tooltip disabled line derives from NS.Slash.DisabledLine."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-22",
   "what": "Schema instance stub gains SetMany (parity)."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "LK-09",
   "what": "The next automated-tests run records perf skip reason (2) from docs/ARCHITECTURE.md:485's performance-\u00a712 row."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "WS-03",
   "what": "defaults/Global.lua schemaVersion 1 -> 0; the comment cites savedvariables-\u00a71 v2.65.0, so no register row is needed (LootHistory-A-18 closes)."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "WS-06",
   "what": "Rename 'minimap.hide' -> 'minimap.shown' in settings/Schema.lua:91, :213 and RESET_EXEMPT :434, with inverting get/set."
  },
  {
   "addon": "LootHistory",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first (tests/test_provider.lua sits at the cap, so any new case goes to a sibling file)."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for v1.18.0-v1.53.0 (MultiMeters-A-19)."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route registrations through Core.SafeRegisterEvent with a rejected list (MultiMeters-A-20)."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-22",
   "what": "MultiMeters#52: adopt Schema. SetByPaths becomes S.SetMany with announceBatch firing CONFIG_CHANGED once, and the primitives, registry and bracket are deleted from settings/Schema_Paths.lua. Adopting Set is now expressible."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-26",
   "what": "Optionally switch settings/OptionsSetup.lua:270-272's scheduleTimer to NewTimer."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-18",
   "what": "MultiMeters-R-06: publish Sl:DisabledLine only if cli.DisabledLine exists, pinned with Kit.assertLibraryConstant."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-16",
   "what": "MultiMeters-R-04: pass isEnabled (latch-inclusive, so a perf suspend refuses) and disabledLine to the Launcher descriptor instead of a hand check at core/LauncherSetup.lua:229-237."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-21",
   "what": "In-client smoke of the ReorderList drag (settings/ColumnBlocks.lua)."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "LK-28",
   "what": "Evaluate opts.tabs for settings/Columns.lua:321; adopt it or file a decline issue."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "WS-06",
   "what": "Rename 'global.minimap.hide' -> '.shown' (settings/Schema_Compose.lua:658, :786; Schema_Paths.lua:222) with inverting get/set."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "WS-03",
   "what": "Declare schemaVersion 0; per-profile steps; stamp after success (coordinate with MultiMeters-A-18a's v15->v16 migration)."
  },
  {
   "addon": "MultiMeters",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-07",
   "what": "After the re-vendor, restore line 5 of docs/superpowers/specs/2026-07-31-panel-artwork-design.md and 2026-08-02-wiki-artwork-import-design.md from `git show e30e329^:<path>` ('catalogued'); test_prose stays green because docs/superpowers/ is now skipped."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first (e.g. PanelMaster-R-02's unlocked-panel stand-down)."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for the 25 unrecorded tags (PanelMaster-A-15)."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route registrations through Core.SafeRegisterEvent with a rejected list (PanelMaster-A-01)."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-22",
   "what": "Schema instance stub gains SetMany (parity)."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-09",
   "what": "The next automated-tests run records perf skip reason (2) from its performance-\u00a712 row."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "WS-03",
   "what": "Declare schemaVersion = 0 in defaults/Global.lua (the ruling sanctions it, since 0 masks no legacy account); correct ARCHITECTURE.md and common-tasks.md (PanelMaster-A-05/A-06)."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub DisabledLine with Kit.assertLibraryConstant."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "WS-06",
   "what": "Rename S.MINIMAP_PATH 'global.minimap.hide' -> '.shown' (settings/Schema.lua:187) with inverting get/set."
  },
  {
   "addon": "PanelMaster",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first (EditMode.Exit survivor PartyFrameEnhanced-R-03, container frames A-01, profile verbs R-02/R-05)."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-23",
   "what": "PartyFrameEnhanced#14: re-apply the rolled-back adoption. settings/Schema.lua builds SchemaLib:New{..., writeThrough = {'enabled','locked', <the Preview.lua:134 path>}} and the stub takes the same list; tests/test_schema.lua:209 ('without the library, /pfe disable and /pfe enable still write the stored path') goes green."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-03",
   "what": "The profile sub-verb tests (PartyFrameEnhanced-R-05) now see copy raise without a guard: land the exists() guards and the refusal lines."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-04",
   "what": "test_disabled's regs() now sees the EditMode.Exit callback through __registrations() kind 'callback'; land Providers:Suspend's UnregisterCallback (PartyFrameEnhanced-R-03)."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-05",
   "what": "The F_on baseline now includes never-shown container frames; land PartyFrameEnhanced-A-01 (hide the fade and holder frames on stand-down) and keep the direct step-5 assertions."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route the registration loops through Core.SafeRegisterEvent with a rejected list (PartyFrameEnhanced-A-03)."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "LK-18",
   "what": "PartyFrameEnhanced-R-11 addon half: drop the copied FormatRow em-dash formatter, keep DISABLED_LINE bytes pinned by Kit.assertLibraryConstant, correct the false comment at settings/Slash.lua:322-324, and adjust test_surface_parity."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "WS-03",
   "what": "PartyFrameEnhanced-R-15: migration steps run for every stored profile; stamp after success; declare schemaVersion 0."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "WS-01",
   "what": "Write a consolidated span bundle for any unrecorded tags the amended check reports."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "WS-06",
   "what": "Rename its minimap row path to '.minimap.shown' with inverting get/set."
  },
  {
   "addon": "PartyFrameEnhanced",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for v1.18.0-v1.53.0 (PRETTYCHAT-A-01)."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route the Override.lua registration loop through Core.SafeRegisterEvent with a rejected list (PRETTYCHAT-A-09)."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-04",
   "what": "Combat watcher (modules/Override.lua:120-139) is covered by the new no-AceEvent carve-out: list it in ## Event Subscriptions with no register row (PRETTYCHAT-A-08 closes); the ## Message Bus sentence cites the amended architecture-\u00a74 threshold (PRETTYCHAT-A-11 closes)."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-05",
   "what": "Delete the dead GetAddOnMetadata global rung at core/EnvSetup.lua:75-76; the compat-layer Not-applicable row cites compat's condition (PRETTYCHAT-A-18 closes)."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "LK-22",
   "what": "Schema instance stub gains SetMany (parity). PrettyChat#18 (ResetRows onto BulkRun/BulkAdd) needs no upstream change and stays an addon item."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "LK-09",
   "what": "The next automated-tests run records perf skip reason (2); fix docs/testing.md's release-notes wording to cite the exemption (PRETTYCHAT-A-16)."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-03",
   "what": "PRETTYCHAT-R-03: profile steps on every stored profile, stamp only after success; declare schemaVersion 0."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "LK-18",
   "what": "Pin the Slash stub DisabledLine with Kit.assertLibraryConstant."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-06",
   "what": "Rename 'global.minimap.hide' -> '.shown' (settings/Schema.lua:146, :229) with inverting get/set."
  },
  {
   "addon": "PrettyChat",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-33",
   "what": "Re-vendor v1.56.0 whole with the provenance line (WA-01 command); regenerate test-cases and the badge; land the dry-run fixes first (EventRegistry SetItemRef survivor WHATGROUP-A-01)."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-01",
   "what": "Consolidated span bundle for the 25 unrecorded tags (WHATGROUP-A-10)."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-23",
   "what": "WhatGroup#22 under the owner ruling: adopt LibKa0s-Schema-1.0 (delete Helpers.RawSet, re-pin the 8 behavior changes the spec names). On a library-absent load, /wg enable, /wg disable and /wg test on|off print the WS-02 library-absent line ('/wg enable is unavailable: the LibKa0s library did not load.') and raise no Lua error; rewrite the two tests/test_libka0s.lua characterization cases to assert the line and the unchanged store; record the SHOULD deviation row."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-04",
   "what": "Shrink the local EventRegistry fake in tests/wow_mock.lua to the kit's; the test_disabled survey reads kind 'callback' (WHATGROUP-A-03)."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-11",
   "what": "Core stub gains the SafeRegister* bodies; route the four AceEvent registrations and Frame's through Core.SafeRegisterEvent with NS.RejectedEvents in [Init] (WHATGROUP-A-07)."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-18",
   "what": "WHATGROUP-A-19: the launcher fallback returns the stub's DisabledLine (pinned by Kit.assertLibraryConstant) instead of the re-spelled literal."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-03",
   "what": "WHATGROUP-R-03: global.schemaVersion default 0, delete the dead `or NS.SCHEMA_VERSION`, add the removeDefaults-then-reboot red case."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-04",
   "what": "The ## Message Bus sentence cites the amended architecture-\u00a74 (shell plus one feature module is below the threshold); WHATGROUP-A-04 closes."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-06",
   "what": "WHATGROUP-R-15: rename MINIMAP_PATH to 'global.minimap.shown' (settings/Schema.lua:361) with inverting get/set; update docs and tests."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-07",
   "what": "Re-check docs/settings-panel.md against the Page | Covers wording and .pkgmeta's png ignore against layout-\u00a74 (WHATGROUP-A-22/A-23 local halves)."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "LK-09",
   "what": "The next automated-tests run records perf skip reason (2) from its performance-\u00a712 row."
  },
  {
   "addon": "WhatGroup",
   "upstream_item": "WS-08",
   "what": "/wow-addon:revendor-standards to v2.65.0."
  }
 ],
 "upstream_notes": "ORDER. WS-01..WS-08 land first (WS-02, WS-04 and WS-07 carry rulings that LK-06, LK-07, LK-11, LK-18 and LK-23 implement). Then LK-01..LK-33 in id order. Kit groundwork LK-01 comes first because later items write into asserts.lua and prose_lists.lua and rely on framework.lua and test_prose.lua being under the cap. WA-01 and WA-02 can go at any point after WS-01. WA-03 goes last, after the tag. No addon is touched until LK-33's local tag exists and WA-01 has landed.\n\nBRANCHES. LibKa0s is already on feat/2026-09-23-review-audit-remediation. WowAddonStandards and wow-addon are on master and need that branch created before WS-01 and WA-01. The standards-audit and review agents fetch WowAddonStandards from raw GitHub at run time, so the v2.65.0 rulings reach those agents only after the owner approves and WS is pushed. revendor-libka0s reads the local sibling and is unaffected.\n\nVERSIONING. v1.56.0 is a minor release: every change is additive, no NEEDS_* floor rises, and no major changes. Only the first item in this release that touches a file bumps its LibStub minor. That item creates docs/api/<Major>/version-<new>-docs.md and members json. Later items extend the same unreleased document and regenerate the json; they never bump twice. The same rule applies to Kit.VERSION 26 (bumped by LK-01) and to CHANGELOG.md's `## v1.56.0 \u2014 unreleased` block, which LK-01 opens and LK-33 dates. tests/test_versioning.lua fails if the block and lib.MODULES disagree, so every minor-bumping item edits the block's versions line. The Options key moves 23.30.3.7.3 -> 24.30.3.7.4 (LK-24) -> 24.31.3.7.4 (LK-26) -> 24.31.4.7.4 (LK-27). Rename the one unreleased Options document each time the key moves; only 23.30.3.7.3 becomes Superseded. The standard goes v2.64.0 -> v2.65.0 as one version with one changelog entry, which WS-01 opens and WS-08 closes. The plugin goes 2.3.0 -> 2.4.0.\n\nTAG. LK-33 creates v1.56.0 LOCALLY on the branch. Neither the tag nor the branch is pushed until the owner approves the merge. Consumers' tests/test_vendor_sync.lua resolves the provenance tag from the local sibling LibKa0s checkout, so the Milestone-2 re-vendors work against the local tag.\n\nBLAST RADIUS (kit 26 changes behavior). CreateFrame now starts shown, the AceDB fake raises and strips defaults, EventRegistry callbacks are now recorded, frame RegisterEvent honors __badEvents, test_eol catches lone CRs, the prose gate scans store roots, and kit case names carry \u00a7. Expect red suites in consumers; the owner's standing ruling is that such reds expose real bugs. LK-05 and LK-33 dry-run the payload into git-archive copies of all eleven addons without touching them. Every red found is an addon fix to land BEFORE that addon's re-vendor commit, so the re-vendor commit is green. Known pre-fixes: AuraMaster tests/page_helpers.lua:80 lone CR; ConsumableMaster and KickCD perf-analysis README spellings; ConsumableMaster 'synchronisation'; BankLedger's wow_mock C_Timer override. Surface-parity churn: every Core degradation stub gains SafeRegisterEvent/SafeRegisterUnitEvent/SafeRegisterEvents. The five Schema adopters (AbsorbTracker, BankLedger, LootHistory, PanelMaster, PrettyChat) add SetMany to their instance stub. No Options instance member is added: LK-25 self-replays on PLAYER_REGEN_ENABLED instead of adding a ReplayPending member.\n\nDESIGN DECISIONS. (1) AbsorbTracker AT-62's proposed A1 (composers in a unit that loads without the Options major) is declined in favor of Schema writeThrough (LK-23 with the WS-02 ruling). A1 cannot help the truly library-less build that tests/degraded_env.lua models; writeThrough serves both that build and a partial load. (2) Schema adoption issues: ConsumableMaster#39 needs normalize and SetMany, MultiMeters#52 needs SetMany, AuraMaster#21's re-check trigger (a second normalize consumer) fires with ConsumableMaster, KickCD#22 needs no new surface (the adoption mapping in the version-2 doc suffices), PartyFrameEnhanced#14 needs writeThrough, and WhatGroup#22 follows the owner ruling (route (b): the library-absent line, no Lua error). PrettyChat#18, PanelMaster#52 (Bus Catalog, already shipped) and AuraMaster#16-#19 (file peels) need NO upstream work and are pure Milestone-2 addon items. (3) Recurring defects promoted to shared surfaces: bare event registration recurs in all 11 addons, so Core.SafeRegisterEvent (LK-11); the launcher disabled gate recurs in 3+, so Launcher isEnabled (LK-16); degraded refusal-line drift recurs in 5, so Kit.assertLibraryConstant plus the WS-02 ruling (LK-18); the tab-strip render fork recurs in 5, so the RenderTabbedSchema opts (LK-28); the scheduleTimer throttle affects 3, so LK-26. The per-profile migration bug (KickCD, PartyFrameEnhanced, PrettyChat) and the stripped schemaVersion stamp (WhatGroup, LootHistory, PanelMaster) are fixed at the standard's template (WS-03), not with a library runner, because the steps are host-specific and the runner is about 20 lines. The InCombatLockdown-vs-combat-edge defect (LootHistory, PanelMaster) recurs in only 2 addons and stays local. (4) The minimap CLI-path rename (WS-06) touches all eleven addons' schema, docs and tests but no SavedVariables: the stored key stays minimap.hide.\n\nCAPS. Files near the cap are handled in advance: framework.lua 1583 -> about 1360 (LK-01); test_prose.lua 1499 -> about 1450 (LK-01); Options.lua 1476 -> about 1416 (LK-24 peel plus LK-25); mock_base.lua 1446 takes new fakes only via mock_events.lua (LK-04); test_widgets 1493, test_slash 1327, test_schema 1233 and test_options_widgets 4086 get new sibling suites (test_widgets_reorder, test_slash_refusal, test_schema_batch, test_options_throttle). OptionsWidgets.lua shrinks by moving RenderTabbedSchema (LK-28). LibKa0s has no tests/perf.lua, so perf is recorded as a skip with reason (1).\n\nGITHUB WRITES. LK-32 files 4-5 LibKa0s issues; space each gh call at least 5 s apart. Every commit subject starts '<ITEM-ID>: '.",
 "plan_notes": {
  "BankLedger": "All 32 BankLedger findings are mapped to items. No finding is pure-upstream: BankLedger-A-04 (LK-09) still needs the BL-DOCS run to record perf skip reason (2), so it sits in BL-DOCS and not in a disposition. Owner-scope issues: OWNER_SCOPE lists no BankLedger enhancement to build. Open issues #1 (store-to-store transfers) and #2 (name backfill) are state:triaged and outside this remediation. #3 is closed in BL-24.\n\nORDERING CONFLICT to resolve upstream. The consumer follow-up for LK-33 says the dry-run fixes land BEFORE the re-vendor. BL-01 is BankLedger-A-03's wow_mock C_Timer override, and it is merged with R-05, because dropping the override alone reddens test_disabled until the prune moves to AceTimer. BL-02 (the Schema stub's SetMany) is needed for tests/test_surface_parity.lua:315-320 to stay green when the live Schema minor 2 instance gains SetMany. Kit.assertSurfaceParity is one-directional (the stub must carry every live member), so both are safe to land on v1.55.0. Schedule BL-01 and BL-02 immediately BEFORE RV-BL, or fold them into RV-BL's commit, so the re-vendor commit is green. Every other item assumes RV-BL has landed. Any extra kit-26 reds the LK-05/LK-33 dry-run records for BankLedger (the CreateFrame-shown flip against test_disabled's __shownFrames baseline is the likeliest) belong in BL-01 too.\n\nOWNER DECISIONS embedded. BL-05 takes Option A of BankLedger-A-02 (unify up), which the audit recommends as the only compliant end state. After it, the page/footer Defaults and /bl resetall discard recorded history after the confirm popup. This is a player-visible data-loss change and should be confirmed before execution. BL-04 makes a reset while disabled re-enable the addon. BL-22 keeps the 30-day default; changing it (CP2) is not taken.\n\nv2.65.0 consumer work beyond the listed follow-ups. BL-10 adopts WS-02 route (a): settings.enabled is on writeThrough in both arms, so library-absent /bl enable|disable write through and re-run the latch with no Lua error. Today they print the CLI-unavailable line and change nothing. No other BankLedger verb writes a composed row, so the library-absent line needs no locale string here. BL-11 applies WS-03. BankLedger is global-only, so the per-profile rule has nothing to walk, and ResetEverything now re-stamps after the wipe. BL-12 applies WS-06; Schema Validate would otherwise flag minimap.shown as unresolved, so S:Register's defaultsRoot answers an inverted view for that row. LK-26 needs no change: OptionsSetup's scheduleTimer returns a handle the library now ignores. LK-25, LK-27 and LK-28 add no instance member BankLedger's Options stub lacks, so they need no BankLedger item. WS-05: BankLedger keeps core/Compat.lua, because it owns 13 shims.\n\nTest-count deltas are given per item (+N), not as absolutes, because kit revision 26 changes the kit's own case names and count. BL-DOCS regenerates the final totals. No version bump or tag.",
  "PartyFrameEnhanced": "ORDERING AGAINST RV-PF. The brief says to assume RV-PF has already landed, but LK-33's consumer follow-up says the dry-run reds should land before the re-vendor commit so that it is green. Under kit revision 26 these existing cases go red: test_disabled step 3 (the EditMode.Exit callback is now recorded, and sig() raises on the callback entry's missing `target`) and step 5 (fade frames and holders start shown). If the orchestrator wants the RV-PF commit green, cherry-pick PF-05 and PF-06 ahead of it; PF-01 (profile verbs) can go ahead too. Each works under kit 25, except that its red-first step only goes red under kit 26. My dry-run check of the store-root prose files (docs/automated-tests/README.md, RESULTS.md, docs/perf-analysis/README.md) found no British spelling and no 'synchronis'. The authored tree has no lone CR. So LK-06 and LK-07 should not redden PartyFrameEnhanced.\n\nDESIGN CHOICES MADE HERE. (1) PF-11 (#14) binds writeThrough = {'enabled','locked'}. modules/Preview.lua:134 writes 'locked', which also covers the combat re-lock. On the library-less build these writes store without the row's onChange, which is today's behavior; the item records it in Known Limitations. The Slash descriptor's `set` keeps returning nothing: AcceptLock already prints its one refusal line, so Slash minor 15's CliSet refusal print would double it. (2) PF-22 takes option (b) for A-05: keep the EllesmereUIDB read and add a library-stack-\u00a76 register row. That keeps the stand-in sized correctly. The item spells out option (a) in full in case the owner picks it at execution time. (3) PF-12's Kit.assertLibraryConstant may resolve 'LibKa0s-Slash-1.0' through tests/run.lua's surface-source map, which points at the Slash INSTANCE. The item names a byte-equality fallback in that case. (4) PF-13 exempts the closure minimap row from Validate's defaults resolution and pins the storage default separately, because `global.minimap.shown` must never exist in defaults (anti-pattern #81).\n\nNOT PLANNED (not in scope and not listed as follow-ups). Adopting Launcher minor 2's isEnabled gate (LK-16). NS.ToggleLock already gates the left click and prints cli:DisabledLine, so it is optional and belongs to the re-vendor candidate interview. Issue #12 (attribute castStartStop's 16.6 B) is not in OWNER_SCOPE. PF-19's re-measure (today 5.4 B/iter) is the evidence the owner needs to close or re-triage it.\n\nMAPPING. R-08 has no separate code. It closes with PF-01 (profile verbs), PF-02 (real switch/copy/reset placement) and PF-03 (the in-combat toggle), and is listed on PF-02. R-10 is on PF-05 because the addon has to change test_disabled's sig() before the kit's new callback records render. Every one of the 37 findings is either in an item or in a disposition (R-09, A-02, A-19). Common verification commands run from /mnt/d/Profile/Users/Tushar/Documents/GIT/PartyFrameEnhanced through ~/.claude/wow-addon/bin/ka0s-bounded, the full path, until WA-02 puts the bare name on PATH.",
  "MultiMeters": "Scope: all 33 findings in MultiMeters.json are covered. 31 are mapped to items MM-01..MM-DOCS; R-04 appears in both MM-01 (the Toggle/Show half) and MM-02 (the launcher half). The 2 pure-upstream findings are dispositioned: R-19 -> WA-03 and R-20 -> WA-02. The only open GitHub issue for MultiMeters in OWNER_SCOPE is #52, built by MM-14, which closes it with a comment. The other open issues (#2, #8-#12, #18) are older enhancements outside scope. A-16's relabels of closed #4, #21 and #24 are in MM-31: #4 and #21 shipped and #24 no longer reproduces, so all three go to state:done.\n\nConsumer follow-ups mapped. LK-16 and LK-18 -> MM-02. LK-11 -> MM-07. LK-22 -> MM-14. WS-02/LK-23 writeThrough -> MM-15. WS-06 -> MM-16. WS-03 -> MM-12, which MM-13's v15->v16 step depends on. LK-26 -> MM-17: NewTimer is declined, with the reason in the comment. LK-28 -> MM-18, which carries a decision gate and a decline-issue fallback. LK-21 smoke -> MM-18's smoke field. WS-01 -> MM-30. WS-08 -> MM-32. LK-33 (regenerate test-cases and badge) -> MM-DOCS.\n\nOne more contract change was found beyond the listed follow-ups: LK-17's CliSet now prints a refusal when `set` answers false, but MultiMeters' Slash descriptor (settings/Slash.lua:295, :301) throws the seam's return away. MM-09 adopts it.\n\nRV-MM prelude (assumed done before MM-01, per the brief; flagged so it is not lost). The re-vendor commit must be green. Likely MultiMeters reds from the kit-26 dry-runs (LK-05/06/07/33) are:\n- CreateFrame now starts shown, which touches test_disabled Disabled 5 / F_on baselines and any 'frame hidden by default' assumption in the tests/mock_frame.lua or wow_mock.lua fakes.\n- AceDB Copy/DeleteProfile now raise, and SetProfile strips defaults, which may touch test_database and the profile tests.\n- Frame RegisterEvent honors __badEvents.\n- Perf label/armed/recording become false rather than nil, which may touch test_perfsetup and test_lifecycle reads of == nil.\n- Case names now carry \u00a7.\nA quick grep found no British spellings in MultiMeters' store-root files (docs/automated-tests/README.md, RESULTS.md, docs/perf-analysis/README.md). LK-07's new docs/superpowers skip makes those waiver entries moot; MM-13 deletes them.\n\nThe MM-DOCS runner run needs tests/_kit/run-automated-tests.sh from kit 26.\n\nCap-sensitive files are handled in the items:\n- tests/test_provider.lua (1500) is peeled in MM-23.\n- test_row.lua (1490), test_database.lua (1470), test_schema.lua (1350) and test_slash.lua (1345) get new sibling suites instead of new cases: test_row_cells, test_migrations, test_schema_batch and test_slash_refusal. Each is registered in tests/run.lua.\n- modules/Row.lua (1469) must stay at or under 1500 in MM-20.\n- modules/Window.lua (1424) grows by about 25 across MM-01 and MM-03.\n- Render's CCN (14) is protected by helper functions, not a part-2 split.\n\nJudgment calls:\n- MM-20 (R-07) and MM-21 (R-08) are gated on in-client captures (SM-07, SM-06). MM-20 is reverted rather than committed if render ms/call does not fall.\n- MM-28 takes A-25 option (b), ratifying with register rows, because the census reasoning already argues both declines. Option (a), adoption with an upstream hover variant, stays open to the owner.\n- MM-13 keeps the art key 'minimise' on the header control, because it is LibKa0s-Media-1.0's own icon name (libs/LibKa0s/media/icons/minimise.tga). That is a sanctioned library-field-name waiver. Renaming the icon upstream is not in Milestone 1.\n- MM-12 treats a 0 or missing stamp as v1, because every step already walks allProfiles(db) and is required to be idempotent on a fresh profile.\n\nNo version bump or tag anywhere. The owner decides; MM-31 records that the next release should be 1.1.0.",
  "LootHistory": "Every one of the 42 LootHistory findings is covered: 40 map to items and 2 are pure-upstream dispositions (R-19 -> WA-03, A-22 -> WS-01). Upstream-routed findings that still need addon work are items: A-12 goes to LH-DOCS, where the bundle records perf skip reason (2) from LK-09, and A-18 goes to LH-12, which declares schemaVersion 0 per WS-03. OWNER_SCOPE lists no GitHub issue for LootHistory. Its four open issues (#9, #11, #16, #17) are not in scope and are not planned.\n\nItems with no finding exist because v2.65.0 or v1.56.0 asks for them: LH-13 (WS-06, minimap.shown), LH-14 (the LK-17 set return and the LK-26 comment), LH-15 (LK-22/LK-23 stub parity and writeThrough), LH-21 (LK-12 link shape), LH-26 (WS-05 dead GetAddOnMetadata rung at core/EnvSetup.lua:56-58) and LH-36 (WS-08). LH-11 adopts LK-16's Launcher isEnabled/disabledLine together with R-13.\n\nTwo decisions for the owner to check:\n1. WS-02 route (a) for the degraded enable/disable (LH-15 and LH-16). writeThrough = {settings.enabled}. The degraded CliSet then calls S.OnEnabledWritten itself, because writeThrough runs no onChange. If the owner wants route (b) instead, drop the enable/disable half of LH-16 and add a Documented deviations row keyed slash-commands-\u00a71/options-ui-\u00a71 (a SHOULD deviation).\n2. The WS-06 rename makes the minimap row path-only. S:Register's defaultsRoot skips rows that own their get/set, and RESET_EXEMPT becomes a map from row path to stored path. Without that map, Reset all settings would un-hide a hidden button, and LH-13's case (c) pins this.\n\nRV-LH interaction: under v1.56.0 the Schema instance parity case (tests/test_surface_parity.lua:203-208) goes red unless the host stub carries SetMany. So RV-LH has to add at least a minimal SetMany in the same commit to keep the re-vendor green. LH-15 then replaces it with the full two-phase body. The Core parity case derives from CoreSetup's `NS.X =` lines, so it stays green at RV and LH-17 adds the SafeRegister* members on both paths.\n\nKit-26 flips may redden LootHistory suites in the LK-05/06/07 dry-runs. The likely spots are test_disabled's shownNames baseline (CreateFrame now starts shown) and the frame RegisterUnitEvent __badEvents path. Those are pre-RV fixes and are not in this list. The prose scan of docs/automated-tests/README.md and RESULTS.md shows only 'ANALYSIS', which ALLOWED covers.\n\nLine and count citations inside items were measured at 36072e5. LH-28 re-derives them with grep because earlier items move them. Every new test case moves docs/test-cases.md and the README badge in the same commit. There is no tests/perf.lua (performance-\u00a712 exemption), so perf is never run per item. Relevant files: /mnt/d/Profile/Users/Tushar/Documents/GIT/LootHistory/modules/Attribution.lua, settings/Schema.lua, settings/Slash.lua, core/CoreSetup.lua, core/LauncherSetup.lua, core/Compat.lua, core/Database.lua, modules/Browser.lua, tests/test_surface_parity.lua.",
  "AbsorbTracker": "Scope. All 41 findings in AbsorbTracker.json are mapped: 37 to AT items and 4 to dispositions. OWNER_SCOPE.md lists no AbsorbTracker enhancement issue to build. The issue-store findings (#25, #26, #10) are handled as gh housekeeping in AT-DOCS. All 12 consumer follow-ups are covered:\n- LK-33: RV-AT, plus AT-DOCS for test-cases and the badge.\n- WS-01: AT-25.\n- LK-11: AT-07.\n- LK-23: AT-01 (stub half) and AT-08.\n- LK-22: AT-01.\n- LK-03: AT-03.\n- LK-16: AT-10.\n- WS-06: AT-11 (hold verb), AT-12 (minimap path) and AT-15 (strip-pin ruling).\n- WS-05: AT-16.\n- WS-03: AT-06.\n- LK-18: AT-09.\n- LK-28: AT-15.\n- WS-08: AT-26.\n\nSequencing against RV-AT:\n(1) AT-01 (Schema stub gains SetMany) is pre-RV-eligible and should be folded into or placed before RV-AT. Kit.assertSurfaceParity is one-directional (live must be a subset of stub; tests/_kit/framework.lua:436-444). So a v1.56.0 payload makes tests/test_surface_parity.lua's two-table Schema instance case red until the stub carries SetMany, while an extra stub member is green on v1.55.0. If RV-AT has already landed, AT-01 is the item that turns it green.\n(2) AT-03's profile guards are green on kit 25 and kit 26. They are the LK-03 consumer obligation, so they can also be hoisted before RV-AT.\n(3) Any other kit-26 red that the LK-05/LK-33 dry-run records for AbsorbTracker (CreateFrame starts shown, SetProfile strips defaults, EventRegistry recorded, lone CR, store-root prose, section-sign case names) belongs in that pre-RV fix list, not in these items. No dry-run output existed when this was planned. Candidate suites that read shown state: test_disabled, test_display, test_draghandle, test_helpers, test_launcher, test_optionssetup, test_perf, test_perfcmds, test_widgets. A spot check of docs/automated-tests/README.md, RESULTS.md and docs/perf-analysis/README.md found no British spellings for LK-07's scan-back.\n\nUpstream risk to flag on LK-18: tests/run.lua:55 registers the Slash INSTANCE (NS.Slash.__cli) as the LibKa0s-Slash-1.0 surface source, and the instance has no DISABLED_LINE_FORMAT, which is lib-level (libs/LibKa0s/Slash.lua:83). If Kit.assertLibraryConstant resolves only the registered surface source, AT-09's pin cannot be written as prescribed. LK-18 should fall back to LibStub(major) when memberPath is absent on the source.\n\nDesign choice in AT-08: writeThrough stores enabled/locked with no row and no onChange (LK-23). A degraded /at disable would then store the value but never move the latch. So the host's Schema `announce` dispatches the host's OWN reaction table for a synthetic writeThrough row. General.lua's masterOnChange is published as NS.MasterReactions; this is host code, not a copy of any library row. That keeps the degraded disable truly inert and keeps the in-combat unlock refusal.\n\nObserved and not filed:\n- The degraded Slash stub's OnSlash has no disabled gate, so a library-less build runs lock/unlock/toggle/update while disabled. No finding covers it.\n- LK-17's CliSet refusal passthrough needs no adoption: no AbsorbTracker row carries `validate`, and CliSet resolves the row first. The descriptor's `set` could later return NS.SetByPath's answer at no cost.\n\nVerification: every run used ~/.claude/wow-addon/bin/ka0s-bounded, which is not on PATH until WA-02 ships bin/ka0s-bounded. Line-cap watch: tests/test_slashcmds.lua is 1304 and grows with AT-03/AT-04, but AT-11 moves the /at test section (about 118 lines) out to tests/test_debughold.lua. AT-03 carries a peel trigger at 1400. tests/test_helpers.lua (1416) is untouched apart from AT-13 reads.\n\nNothing was edited, committed or filed during this planning pass.",
  "PanelMaster": "I planned 25 items, PM-01 to PM-24 plus PM-DOCS, and filed one disposition. All 28 findings in PanelMaster.json are covered:\n- R-01/R-02 \u2192 PM-02; R-03/R-06 \u2192 PM-03; R-04 \u2192 PM-04; R-05 \u2192 PM-05; R-07 \u2192 PM-07; R-08 \u2192 PM-06.\n- R-09 is split: the Slash.lua comment is fixed in PM-09, defaults/Database stamp sentences in PM-10, and the rest in PM-19.\n- A-01 \u2192 PM-08; A-02/A-03 \u2192 PM-14; A-04 \u2192 PM-20; A-05/A-06/A-17 \u2192 PM-10; A-07/A-19 \u2192 PM-15; A-08 \u2192 PM-13; A-09 \u2192 PM-17; A-10 \u2192 PM-21; A-11/A-12 \u2192 PM-22; A-13 \u2192 PM-23; A-14 \u2192 PM-24; A-15 \u2192 PM-18; A-18 \u2192 PM-DOCS.\n- A-16 is the only pure-upstream finding (WS-01), so it is the only disposition.\n\nConsumer follow-ups:\n- LK-07 \u2192 PM-17. LK-11 \u2192 PM-08. LK-22 \u2192 PM-01. LK-18 \u2192 PM-09. WS-01 \u2192 PM-18. WS-03 \u2192 PM-10. WS-06 \u2192 PM-11. WS-08 \u2192 PM-16. LK-09 and LK-33 (test-cases and badge regeneration) \u2192 PM-DOCS.\n- Owner-scope issue PanelMaster#52 \u2192 PM-12. It includes closing the issue with state:done.\n\nConventions for every item:\n- Every lua/lizard command runs under ~/.claude/wow-addon/bin/ka0s-bounded. The bare `ka0s-bounded` name only works once WA-02 lands.\n- PanelMaster has no tests/perf.lua, so perf is always a skip.\n- The 1500-line check is `find . -name '*.lua' -not -path './libs/*' -not -path './tests/_kit/*' | xargs wc -l | awk '$2!=\"total\" && $1>1500'`, and it must print nothing.\n- docs/testing.md requires docs/test-cases.md and the README badge to be regenerated in the same commit as any change to the case count. So every item that adds tests regenerates them, and PM-DOCS regenerates them once more after the kit-26 \u00a7 case-name change.\n\nThings the orchestrator must know:\n\n(1) RV-PM green-ness. After the v1.56.0 re-vendor, the live Schema instance gains SetMany. The PanelMaster stub will then fail 'Parity: the Schema seam ... on both levels'. That can't be fixed before the re-vendor: a stub member ahead of v1.55.0's live surface is also a divergence. PM-01 is therefore written to follow RV-PM directly. If RV-PM must be green on its own, fold PM-01's stub change into it; PM-01 then keeps only its cases.\n- Also apply any reds that LK-05/06/07/33 recorded for PanelMaster (dryrun-*-PanelMaster.txt) in PM-01.\n- PanelMaster's own tests/wow_mock.lua stubFrame already starts shown (__shown = true), so the LK-05 flip should hit little here.\n- No PanelMaster suite calls EventRegistry, CopyProfile, DeleteProfile or SetProfile.\n- Nothing asserts the '[LibKa0s] ' launcher prefix that LK-16 drops.\n\n(2) The Core parity case in PanelMaster is derived from core/CoreSetup.lua's column-zero `NS.X =` publications, not from the library's member list. The seam's stated rule is that it publishes only what the addon calls. So PM-08 re-exports only SafeRegisterEvent, not all three SafeRegister* functions; parity stays symmetric by construction.\n\n(3) The LK-09 follow-up is wrong for PanelMaster: the next run will NOT record perf skip reason (2). The addon's register row is performance-\u00a71, and its text says '\u00a712 does not apply and is not claimed'. Reason (1) (no tests/perf.lua) is the expected result. PM-DOCS records what the runner writes and must not re-label the row.\n\n(4) PM-09 goes past the listed follow-ups on purpose. WS-02 makes composed-row verbs on a library-absent load a MUST: either write through or print the library-absent line, and never raise. It also makes route (a) a SHOULD for enable/disable. Today PanelMaster's degraded /pm enable only prints UNAVAILABLE. A player who disabled the addon earlier therefore cannot switch it back on in a library-less build, and on an Options-absent partial load the Slash library's CliSet cannot find the row.\n- PM-09 adopts LK-23 writeThrough for settings.enabled on both the live descriptor and hostSchemaStub.\n- It adds host handlers Sl:CliEnable and Sl:CliLock. When the composed row exists they are byte-identical to today; when it is absent they call NS.RefreshEnabled themselves, because writeThrough skips onChange.\n- lock/unlock take route (b), since state.locked is session-only.\n- The locale-routed line changes the localization-\u00a71 register row's wording.\n\n(5) A-07 needs an owner decision. PM-15 defaults to option (b): keep the row, re-word the trigger to the host-side condition, and file a state:triaged issue carrying option (a). If the owner picks (a), that is a large separate change and stays that issue.\n\n(6) PM-18's tag list is v1.16.0, the 25 tags v1.18.0-v1.53.0, and v1.54.2, which e30e329 carried through tests/_kit and CLAUDE.md only. It is re-derived at edit time with the command in the item. Line 1 must match WA-01's final template byte for byte, because WS-01's check reads every tag on it.\n\n(7) PanelEditor.lua is at 1476 lines. PM-06 may add at most 12; PM-22's comment rewrites must shrink or stay neutral. The open split issue #47 is not in scope.\n\n(8) GitHub writes are in PM-12 (#52) and PM-15 (five closes plus one new issue). Space every gh call at least 5 s apart.\n\n(9) A possible extra, not planned: WS-05's new compat.md text may make Compat.AddOnFolders' GetNumAddOns/GetAddOnInfo global rung dead code. That rung is also why the function sits at CCN 15. PM-20 only documents it. Deleting it would be an extra item needing test_compat changes; owner's call.\n\nNo version bump or tag in any item.",
  "PrettyChat": "1. RV-PC ordering hazard. After the v1.56.0 re-vendor, test_surface_parity's 'the Schema stub carries the whole live surface' case goes red. The live Schema instance gains SetMany (LK-22), and settings/Schema.lua's SchemaStub lacks it. The Core parity case stays green, because PrettyChat projects the Core surface by name (coreSurface), so the new SafeRegister* members only appear once PC-01 adds them. Either run PC-01 immediately after RV-PC, or fold PC-01's stub SetMany into RV-PC if the orchestrator requires a green re-vendor commit. RV-PC also owns any other kit-26 dry-run reds for PrettyChat. By inspection I predict none beyond the \u00a7 case-name inventory churn:\n- CreateFrame starting shown leaves test_disabled/5's F_on baseline equal;\n- test_debuglog:371's CopyProfile('Alt') copies an existing profile;\n- a grep of the kit's BRITISH list over docs/automated-tests/README.md and RESULTS.md found only 'ANALYSIS'.\n\n2. The owner has two alternatives:\n- PC-06 takes PRETTYCHAT-A-02 option (a), a page-wide Categories Defaults, because options-ui-\u00a713 is a MUST. Option (b) would instead keep the per-tab button and add a Documented deviations row keyed options-ui-\u00a713.\n- PC-13 renames a CLI path players can type (`global.minimap.hide` -> `global.minimap.shown`). The next Version History row should say so.\n\n3. GitHub writes are left to the owner, throttled at least 5 s apart:\n- PC-04 resolves PrettyChat#4 (the 'cross-registered globals' Known Limitation), which can be closed state:done after merge.\n- PC-05 closes PrettyChat#18.\n\n4. PRETTYCHAT-R-01's cross-repo half (handoff H-1: LootHistory/core/Util.lua rebuilds its loot and currency patterns when the source globals change) belongs to the LootHistory plan. PC-22 is only PrettyChat's documentation and smoke side. If the LootHistory planner did not pick up H-1, file a LootHistory issue citing F-001.\n\n5. A caveat no finding raised. settings/OptionsSetup.lua:156-203's degraded MasterControls stub emits leaves (path, type, default) rather than answering {}. WS-02 keeps 'composers answer {}' and anti-pattern #73. The 2026-09-23 audit did not file it, and tests/test_libka0s.lua pins full == degraded row counts, which the degraded enable/visibility verbs rely on. So it is not planned here. The next audit against v2.65.0 may grade it, and route (a) writeThrough would need a resolveRoot this descriptor does not have.\n\n6. A caveat for PC-02. Kit.assertLibraryConstant resolves through the registered surface source, and PrettyChat registers Slash as an INSTANCE. The item gives a scoped re-register fallback. Confirm the exact behavior against docs/api/testkit/version-26-docs.md.\n\n7. PRETTYCHAT-A-15 (the runner cannot record reason (2)) is fixed upstream by LK-09. It is placed in PC-DOCS rather than dispositioned, because the addon must re-run the runner to record the reason; PC-23 fixes the prose that described the old runner. WS-03's 'declare schemaVersion 0' is already true at core/Database.lua:36; PC-14 moves the declaration into defaults/Profile.lua.\n\n8. Every item that adds or renames a test case regenerates docs/test-cases.md and the README Tests badge in the same commit (docs/testing.md:207). All new files are re-checked out CRLF (the repo is CRLF-pinned, and kit 26's test_eol also catches lone CRs). There is no tests/perf.lua, so perf is always recorded as a skip under the performance-\u00a712 exemption.",
  "WhatGroup": "All 39 WhatGroup findings map to items, so dispositions is empty. The six upstream-routed findings (R-03, R-15, A-04, A-13, A-22, A-23) each still need an addon-side change: WG-08, WG-11, WG-23, WG-21/WG-DOCS and WG-26. Every consumer follow-up is covered. LK-33: WG-01 is the dry-run pre-fix, and RV-WG handles the rest. WS-01 is WG-29. LK-23/WS-02 and WhatGroup#22 are WG-12 (route (b), the library-absent line, the SHOULD deviation row). LK-04 is WG-10. LK-11 is WG-06. LK-18 is WG-13. WS-03 is WG-08. WS-04 is WG-23. WS-06 is WG-11. WS-07 is WG-26. LK-09 is WG-DOCS. WS-08 is WG-30.\n\nSEQUENCING CAVEATS.\n(1) WG-01 (the A-01 fix) is green on both kit 25 and kit 26 because it surveys the local wow_mock EventRegistry. The LK-33 follow-up names it the fix that must land BEFORE RV-WG, so the real commit order is WG-01, RV-WG, WG-02, and so on.\n(2) WG-02 is a contract change the new payload forces. LK-25 parks CreateOptionsPanel under InCombatLockdown, which turns tests/test_panel.lua:106 and :114 red. No order can make that re-pin green before the re-vendor, so if RV-WG's own run is red, fold WG-02 into the RV-WG commit. Flag for the owner: parking registration in combat sits uneasily with options-ui-\u00a79 ('category registration never taints', and WhatGroup's WG-A-12 history). WhatGroup follows the library, but the standard may want a sentence.\n(3) The 1500-line cap: tests/test_frame.lua is 1421 lines, so the R-01/R-02/R-06/R-16 cases go in a new sibling suite, tests/test_frame_secure.lua, declared in tests/run.lua. WG-03 creates it.\n(4) WHATGROUP-R-05's 16 sites are split across items. core/Database.lua is fixed in WG-08. settings/Schema.lua:398/:465 are deleted by WG-12's seam swap. modules/Frame.lua:425 is fixed in WG-15. WG-16 does the rest and closes the census.\n(5) WHATGROUP-A-12's core/LauncherSetup.lua:143 citation is fixed in WG-13; WG-21 handles the other four.\n(6) LK-18 caveat: tests/run.lua maps 'LibKa0s-Slash-1.0' to the Slash INSTANCE, but DISABLED_LINE_FORMAT is a library-level constant. WG-13 tells the implementer to check Kit.assertLibraryConstant's resolver and fall back to an assertEqual against a live load's library table if needed.\n(7) The LK-09 follow-up ('record perf skip reason (2)') does not apply as worded. WhatGroup ships tests/perf.lua, so perf runs and records pass, and reason (2) belongs only to the no-perf.lua branch. WG-DOCS verifies this instead.\n(8) tests/perf.lua's ceilings are computed from the same run (ceil(shown)), so they cannot trip. WG-15 pins showFrameRepeat as a literal.\n(9) Other kit-26 flips checked with low risk. WhatGroup's own CreateFrame already starts frames shown. The working tree has no lone CR. docs/automated-tests/README.md and RESULTS.md have no BRITISH-list hits. WG-02 still runs the full suite and absorbs any red.\n\nSCOPE. OWNER_SCOPE builds only WhatGroup#22 among this repo's issues. Open issues #1, #2 and #4 are not in the owner's table and are not planned. The version bump and release tag stay with the owner. Each item's commit subject starts '<ITEM-ID>: '. WG-12's body carries 'Closes #22'.\n\nKEY FILES: /mnt/d/Profile/Users/Tushar/Documents/GIT/WhatGroup/modules/Frame.lua, core/WhatGroup.lua, core/CoreSetup.lua, core/Database.lua, core/LauncherSetup.lua, settings/Schema.lua, settings/Slash.lua, settings/OptionsSetup.lua, settings/Panel.lua, settings/SchemaSetup.lua (new in WG-12), tests/test_frame_secure.lua (new in WG-03), docs/ARCHITECTURE.md.",
  "ConsumableMaster": "SEQUENCING EXCEPTION: CM-01 has to land BEFORE RV-CM, and RV-CM should list CM-01 in its depends_on. The LK-07/LK-33 consumer follow-ups require it: the kit-26 prose gate goes red on docs/perf-analysis/README.md ('analysed', 'neighbours') and on docs/settings-panel.md:80 ('synchronisation'), and the re-vendor commit must be green. CM-01 also runs the v1.56.0 dry-run into a git-archive copy and fixes every non-list red at its production cause. A planning-time scan found no other British spelling in the store-root files: docs/automated-tests/README.md and RESULTS.md are clean. Every other item assumes RV-CM landed and that RV-CM itself regenerated docs/test-cases.md and the badge for the kit's \u00a7-bearing case names (LK-08/LK-33).\n\nSTANDING GATE, run from /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster:\n- `luacheck .`\n- `~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/run.lua`, 0 failed\n- `~/.claude/wow-addon/bin/ka0s-bounded lua5.1 tests/perf.lua`; this repo has one\n- `lizard -l lua -x \"./libs/*\" -x \"./tests/_kit/*\" -C 15 -w .`, no output\n- `diff <(lua5.1 tests/run.lua --list | tr -d '\\r') <(tr -d '\\r' < docs/test-cases.md)`, empty\n- `git ls-files '*.lua' | grep -v '^libs/' | grep -v '^tests/_kit/' | xargs wc -l | sort -rn | sed -n 2p`, under 1500\n\nAfter WA-02 the bare `ka0s-bounded` works too. Every item that changes the case count regenerates docs/test-cases.md and the README Tests badge in the same commit. Every red-first case is shown red before the fix, and the commit body says so.\n\nDESIGN DECISIONS THE OWNER MAY WANT TO SEE:\n(1) The LK-03 follow-up ('drop the SetProfile override') cannot be taken literally. tests/wow_mock.lua keeps a whole local AceDB fake, not an override, because the kit's fake has no CallbackHandler string-method form and no db.profiles. CM-02 brings that fake to kit-26 fidelity instead: it strips the outgoing profile's defaults and raises on a bad Copy or Delete. If the owner wants the local fake retired, that is a LibKa0s kit issue (add the string-method form and db.profiles) and not in this plan.\n(2) CM-17 (#39) keeps host reactions off Schema's onChange. Rows carry a host `apply` field, which announce and announceBatch run through the existing pcall reporter. That preserves today's semantics exactly: one applyBar per batch, a caller override, and a raising reaction reported rather than propagated. The batch caller's onChange and structural flags ride in a file-local batchCtx, so KCM.Schema:Set/SetMany and all three batch call sites stay unchanged. The degraded stub is a new settings/SchemaStub.lua, copied from LibKa0s's referenceStub as the Schema doc prescribes, and pinned both ways with assertSurfaceParity.\n(3) WS-02: CM-18 takes route (b) for enable/disable and records the SHOULD deviation. The reason is that a library-absent load has no Lifecycle latch (core/LifecycleSetup.lua returns early), so a written-through `enabled` would not be obeyed that session. There is no host copy of DISABLED_LINE_FORMAT, because the degraded build has no disabled state. So the LK-18 'Slash stub DisabledLine' pin becomes Kit.assertLibraryConstant on the library-absent line (if the Slash v15 doc publishes it as a member) plus the live DISABLED_LINE_FORMAT.\n(4) LK-28: adopt on General and Macro Bar. Stat Priority and Macros get a will-not-do decline issue, because they are list pages with no schema rows. Under WS-06 (3), the addon's duplicated strip wrap-geometry cases are deleted in CM-20.\n(5) CM-09 also adopts LK-16's Launcher isEnabled/disabledLine gate, since it rewrites that exact onClick. CM-05 adds no Options stub member, because both call sites sit behind the libAbsent guard.\n\nGITHUB WRITES, spaced at least 5 s apart: CM-14 (1 issue), CM-20 (1 decline issue), CM-30 (up to 2 issues). #39 closes via 'Closes tusharsaxena/ConsumableMaster#39' in CM-17's commit body when the owner merges. Only #39 of ConsumableMaster's open issues is in OWNER_SCOPE. The others (#3, #4, #7-#9, #11-#16) are out of scope and untouched.\n\nFINDING MAP:\n- R-01 -> CM-04\n- R-02 -> CM-03\n- R-03 and A-04 -> CM-05\n- R-04 -> CM-14\n- R-05 and A-18 -> CM-13\n- R-06 -> CM-02\n- R-07 -> CM-07\n- R-08 and R-09 -> CM-11\n- R-10 -> CM-08\n- R-11 -> CM-09\n- R-12 -> CM-10\n- R-13 -> CM-12\n- R-14 -> CM-23\n- R-15 -> CM-23 (code and docs) and CM-24 (TOC)\n- R-16 -> CM-20\n- R-17 -> CM-21\n- R-18 -> CM-30 and CM-DOCS\n- R-19 -> disposition WA-02 and WA-03\n- A-01 and A-09 -> CM-06\n- A-02 -> CM-28\n- A-03 -> disposition WS-01\n- A-05 -> disposition LK-07, delivered by RV-CM\n- A-06 and A-07 -> CM-01\n- A-08 -> CM-15\n- A-10 -> CM-25\n- A-11 and A-14 -> CM-26\n- A-12 and A-16 -> CM-24\n- A-13 -> CM-22\n- A-15 -> CM-27\n- A-17 -> CM-DOCS\n\nCONSUMER FOLLOW-UP MAP:\n- LK-07 -> CM-01\n- LK-33 -> RV-CM plus CM-01\n- WS-01 -> CM-28\n- LK-11 -> CM-15\n- LK-25 -> CM-05\n- LK-22 -> CM-17\n- LK-03 -> CM-02\n- LK-21 -> CM-29 (smoke)\n- LK-28 -> CM-20\n- LK-18 -> CM-18\n- WS-06 -> CM-19 and CM-20\n- WS-03 -> CM-16\n- WS-08 -> CM-31\n\nNo version bump or tag anywhere. The key files are /mnt/d/Profile/Users/Tushar/Documents/GIT/ConsumableMaster/settings/Panel.lua, settings/OptionsSetup.lua, settings/OptionsShim.lua, core/ConsumableMaster.lua, core/LifecycleSetup.lua, modules/MacroManager.lua, modules/MacroBarFlyout.lua and tests/wow_mock.lua.",
  "AuraMaster": "EXECUTION ORDER AND RV-AM. AM-01 and AM-02 must land BEFORE RV-AM, even though RV-AM is listed as already done. The LK-06 and LK-33 consumer follow-ups require the lone-CR fix and every dry-run red to land first so the re-vendor commit is green. AuraMaster-A-01 also requires the span bundle before the collection-wide re-vendor writes its own. So the real sequence is AM-01 -> AM-02 -> RV-AM -> AM-03 ... AM-35 -> AM-DOCS. The orchestrator should make RV-AM depend on AM-02 and AM-03 depend on RV-AM. The AM-01 dry-run archives the v1.56.0 payload into a temp copy and fixes any kit-26 red: CreateFrame starts shown, the AceDB fake raises and strips defaults, lone CRs, store-root prose, \u00a7 case names. On 2026-09-23 the store roots showed no prose hit and the lone CR in tests/page_helpers.lua was the only one in the repo (the .gz files are text: unset).\n\nVERIFICATION BASELINE. Every item's verify runs luacheck ., `ka0s-bounded lua5.1 tests/run.lua`, `ka0s-bounded lua5.1 tests/perf.lua` (AuraMaster has one), `lizard -l lua -x \\\"./libs/*\\\" -x \\\"./tests/_kit/*\\\" -C 15 -w .`, and the 1500-line check. Two known carry-overs. (a) Until AM-13, lizard prints the two pre-existing warnings (Cat.SyncUserCategories CCN 25, renderCategories CCN 16), and every earlier item must add none. From AM-13 onward it prints nothing. (b) Until AM-23..AM-26, the cap check lists the four files that carry open issues #16-#19, and that list shrinks by one per peel item and is empty after AM-26. The bare `ka0s-bounded` name relies on WA-02; before that, use ~/.claude/wow-addon/bin/ka0s-bounded. Every item that adds or renames a case regenerates docs/test-cases.md and the README test badge in the same commit, then re-checks out the file so the working tree stays CRLF. The layout-cap gate does not assert census line figures, so red tests may land in the over-cap test files before they are peeled.\n\nDECISIONS AND CONFLICTS TO FLAG. (1) The LK-33 follow-up says the new bundle should 'note that the frozen v1.55.0 bundle's base should have read v1.53.0'. The verified AuraMaster-A-01 says the opposite, and I confirmed it: commit 329e1a3 re-vendored v1.54.2 kit-only and rolled the provenance line from v1.53.0 to v1.54.2, so 'v1.54.2 -> v1.55.0' is correct. AM-02 therefore writes NO correction paragraph, and RV-AM's bundle should not add one either. (2) The span list is 22 tags, not the finding's 20. The provenance history also shows v1.43.0 (76224d7) and v1.45.0 (8923a1a), and AM-02 derives the list from CLAUDE.md history. (3) The LK-23 follow-up says to take route (a) 'if adopting Set, else route (b)'. AM-15 re-evaluates Set, as #21's triggers require, and declines it. The reasons are that minimap inversion, carve-outs, whole-section writes and the CheckWrite dry run have no row equivalent, and that the library-less build must keep the host seam, so adopting Set would split the live and degraded write paths. AM-16 still takes route (a) by honoring a declared data-only writeThrough list {enabled, locked} in AuraMaster's own seam, which is also handed to the S instance. Route (b) would regress the degraded `/am enable`/`disable`/`lock`, which work and are pinned today (tests/test_slash.lua:233), and WS-02 says enable/disable SHOULD take (a). If the owner reads WS-02 as requiring library Set for route (a), the fallback is route (b) plus a Documented deviations row for enable/disable. (4) AM-11 goes slightly past the LK-17 follow-up. applyDefault now returns NS.ApplyDefault's answer, so LibKa0s-Slash 15 prints NO_DEFAULT instead of echoing an unchanged value. This makes the host's noResetReason line redundant, and it would print before the library's line, so the field and its key are deleted. (5) AuraMaster-R-03 (UNIT_AURA unit frame) is a MAY in the standard. It is planned as AM-08 because the owner has every finding in scope. It registers through AM-07's SafeRegisterUnitEvent. (6) AuraMaster-A-12 (unratified layout-\u00a71 rows) is resolved by building #16-#19 (AM-23..AM-26). Each peel deletes its own census and register row, and AM-26 leaves an empty census under the kept heading. (7) AM-13 has a cap guard: defaults/Categories.lua is 1478 lines, and if the phase split pushes it over 1500, the same commit peels the user-category section into defaults/UserCategories.lua. (8) AM-17's mapping of AuraMaster's page spec onto LK-28 relies on two things: a tab keyed by a schema group replaces that group's rows, and chrome runs after the strip. If the v1.56.0 Options doc says otherwise, the item stops and the gap goes upstream, with no local fork. (9) The consumer follow-up list for AuraMaster has no WS-04/WS-05 entries, but AM-07 implements WS-04's rejected-list surfacing and AM-19 applies WS-05's new MUST (delete the dead GetMouseFocus rung). Consumer follow-ups with no finding id: AM-10 (WS-03), AM-11 (LK-17), AM-14 (WS-06), AM-15 (LK-22/#21), AM-35 (WS-08).\n\nGITHUB WRITES (after owner approval and merge, each spaced at least 5 s apart): #10 relabel (AM-28); #16-#19 relabel to state:done and close (AM-23..AM-26); a #21 decision comment and state:done (AM-15). No push, no version bump and no tag anywhere in this plan.\n\nIN-CLIENT SMOKE SESSIONS. The items with in-client checks are AM-03, AM-04, AM-07, AM-08 (a pre/post perf capture recorded through /wow-addon:perf-analysis), AM-09, AM-10 (on a SavedVariables backup), AM-12, AM-13, AM-14, AM-15, AM-16 (a copy with libs/LibKa0s renamed), AM-17, AM-19, AM-20, AM-23 and AM-32 (screenshot capture for issue #3).",
  "KickCD": "ORDERING. KC-01 is the one KickCD item that must land BEFORE RV-KC. It clears the reds the LK-33 dry run of v1.56.0 will show in KickCD: the two perf-analysis README spellings the LK-07 follow-up names, and a mock-fidelity gap (KickCD's LSM fake answers a function for LOCALE_BIT_western, which LK-13's langmask sum would raise on). Treat RV-KC as depending on KC-01. KC-02 onward assume RV-KC landed green, which covers the whole payload, the provenance line, the kit 26 pairing, and test-cases plus the badge (the LK-33 follow-up). After that the order is: behavior fixes with red tests (KC-02..KC-16), then the structural and adoption items (KC-17 minimap rename, KC-18 Schema adoption (#22), KC-19 degraded Slash stub, KC-20 LK-28 evaluation), then standards and docs (KC-21..KC-28), then KC-DOCS last.\n\nKIT 26 BLAST RADIUS, MEASURED. KickCD owns its CreateFrame (frames already start shown), its AceDB fake and its frame RegisterEvent. The CreateFrame-shown and AceDB-raise flips in LK-03 and LK-05 therefore do not reach it. What reaches it: the store-root prose scan (KC-01), \u00a7 case names (a regenerate, done in RV-KC and KC-DOCS), C_EventUtils.IsEventValid from mock_events (used by the KC-04 tests), and the AceGUI survey (harmless). KC-04 teaches KickCD's own frame model to honor __badEvents with the kit's message.\n\nSURFACE PARITY. The KickCD Core parity case uses the four-argument NS-vs-NS form, so the re-vendor does NOT force the SafeRegister* members. They arrive as an adoption in KC-04, where both arms gain them together. KC-18 adds the Schema instance-vs-stub and stub-library-by-name parity cases, and the stub carries SetMany and writeThrough from the start.\n\nWS-02 / #22 DEGRADED ROUTE. KickCD takes route (a), writeThrough { enabled, locked }, for enable, disable, lock, unlock and toggle. `/kcd set` of any other path on a library-absent load prints the WS-02 library-absent line and writes nothing, and nothing raises. The live enabled row keeps RefreshEnabledHold as its onChange. On the degraded path the Schema stub's announce handles the synthetic writeThrough row, because a row-less write runs no onChange.\n\nDESIGN CHOICES MADE HERE, FOR OWNER REVIEW. (1) Spell input: core/SpellInput.lua is placed after core\\KickCD.lua, not after Database.lua as R-05 suggested, so the cache invalidator can be an AceEvent target at file load (A-04). (2) resetposition extends to both icon grids rather than relabeling the verb as target-only (R-14 allowed either). (3) Perf nesting: R-09 option (a), a rebuildEmit root bucket plus a payload flag, so the Rebuild-path parent is reported honestly. (4) The link row keeps its skip-if-unchanged refresh through a set closure, because Schema's onChange has no old value. (5) The panel and section enum checks move from runtime ValidateSchema to test_schema. A candidate outside the listed follow-ups was NOT made an item: LK-16's Launcher isEnabled/disabledLine would replace core/LauncherSetup.lua's hand-written left-click gate. Leave it for the RV-KC adoption interview.\n\nGITHUB WRITES. KC-20 (if declined) and KC-28 write to GitHub; space the calls at least 5 s apart. #22 is closed by the owner on merge, and KC-18's commit body says 'Closes #22'.\n\nKEY PATHS. Findings: /mnt/d/Profile/Users/Tushar/Documents/GIT/Ka0sAddonsCommonTasks/docs/2026-09-23-REVIEW_AND_STANDARDS_AUDIT_REMEDIATION/inputs/findings/KickCD.json. Repo: /mnt/d/Profile/Users/Tushar/Documents/GIT/KickCD. All 46 findings are mapped: 43 to items and 3 to dispositions (A-02 -> WS-01, A-06 -> LK-07, R-19 -> LK-26)."
 },
 "amendment_log": [
  "dropped PF-26 (minimap migration not needed: stored key unchanged)",
  "dropped PC-27 (minimap migration not needed: stored key unchanged)",
  "RV-BL now waits on pre-fixes ['BL-01', 'BL-02']",
  "RV-AT now waits on pre-fixes ['AT-01']",
  "RV-WG now waits on pre-fixes ['WG-01']",
  "RV-CM now waits on pre-fixes ['CM-01']",
  "RV-AM now waits on pre-fixes ['AM-01', 'AM-02']",
  "RV-KC now waits on pre-fixes ['KC-01']",
  "BL-DOCS decoupled from gated BL-25",
  "PF-DOCS decoupled from gated PF-25",
  "MM-DOCS decoupled from gated MM-32",
  "LH-DOCS decoupled from gated LH-36",
  "AT-DOCS decoupled from gated AT-26",
  "PM-DOCS decoupled from gated PM-16",
  "PC-DOCS decoupled from gated PC-26",
  "WG-DOCS decoupled from gated WG-30",
  "CM-DOCS decoupled from gated CM-31",
  "AM-DOCS decoupled from gated AM-35",
  "KC-DOCS decoupled from gated KC-27",
  "gated to M4: ['BL-25', 'PF-25', 'MM-32', 'LH-36', 'AT-26', 'PM-16', 'PC-26', 'WG-30', 'CM-31', 'AM-35', 'KC-27']",
  "M1 items not ancestors of RV-AT: ['WA-02', 'WA-03']",
  "M1 follow-up: WS-09 + LK-34 added (options-ui-\u00a79 park ruling per the approved plan; the tag is re-cut on the final commit); every RV depends on LK-34",
  "M2 checkpoint: <AB>-00 \"clear the re-vendor reds\" added for BL LH MM PM PF PC WG; every other M3/M4 item of that repo depends on it",
  "M3 prep: every <AB>-DOCS depends on all other M3 items of its repo (+200 edges)"
 ]
}